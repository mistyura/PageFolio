# S01: Provider Abstraction

**Goal:** OCR バックエンドを差し替え可能にするための抽象基底 `OCRProvider` と、その最初の実装 `LMStudioProvider` を新規ファイル `pagefolio/ocr_providers.
**Demo:** OCR バックエンドを差し替え可能にするための抽象基底 `OCRProvider` と、その最初の実装 `LMStudioProvider` を新規ファイル `pagefolio/ocr_providers.

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: 04-provider-abstraction 01
- [x] **T02**: 04-provider-abstraction 02
- [x] **T03**: 04-provider-abstraction 03
- [x] **T04**: 04-provider-abstraction 04
</tasks>
