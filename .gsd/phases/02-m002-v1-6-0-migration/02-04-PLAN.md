# S04: Ai C

**Goal:** V16-AI-01 の純ロジック層を新設する。OCR 結果の Markdown 文字列を「行種別 + インライン span」の構造データへ変換する Tk 非依存の純関数 `parse_markdown`（および内部ヘルパー `_split_inline`）を新規モジュール `pagefolio/md_render.
**Demo:** V16-AI-01 の純ロジック層を新設する。OCR 結果の Markdown 文字列を「行種別 + インライン span」の構造データへ変換する Tk 非依存の純関数 `parse_markdown`（および内部ヘルパー `_split_inline`）を新規モジュール `pagefolio/md_render.

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: 04-ai-c 01
- [x] **T02**: 04-ai-c 02
- [x] **T03**: 04-ai-c 03
</tasks>
