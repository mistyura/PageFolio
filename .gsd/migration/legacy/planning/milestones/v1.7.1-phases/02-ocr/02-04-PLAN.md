---
phase: 02-ocr
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - pagefolio/ocr_pipeline.py
  - pagefolio/ocr.py
  - pagefolio/ocr_dialog.py
  - tests/test_ocr_pipeline.py
  - tests/test_ocr.py
  - CLAUDE.md
  - .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md
autonomous: true
requirements:
  - V171-OCR-04
must_haves:
  truths:
    - "producer-consumer ロジックが ocr_pipeline.py の単一実装に一本化され、ocr.py の未使用ヘルパーが削除される"
    - "ocr_dialog.py の _render_next_page / _worker は ocr_pipeline の関数/状態を呼ぶ薄いラッパーへ縮小される"
    - "レンダー失敗ページでも進捗が 100% に到達する（L-6a）"
    - "fatal 発生後は producer が残ページの render を継続せず終了へ向かう（L-6g）"
    - "sentinel（終了シグナル）の容量不変条件が ocr_pipeline.py の docstring に明文化される（L-6h）"
    - "OCR の並列実行・キャンセル・進捗・リトライが回帰なく動作する（既存 OCR テスト群がグリーン）"
    - "バッファ上限 concurrency+1 を超える画像同時保持が起きない（V14-D-06 維持）"
  artifacts:
    - "pagefolio/ocr_pipeline.py（Tk/fitz 非依存の producer-consumer 純ロジック層）"
    - "tests/test_ocr_pipeline.py（移設+拡充したパイプラインテスト）"
    - "CLAUDE.md（ファイル構成表に ocr_pipeline.py を追記）"
  key_links:
    - "ocr_dialog._worker → ocr_pipeline.consume_one + PipelineState"
    - "ocr_dialog._render_next_page → ocr_pipeline.try_enqueue / send_sentinels"
    - "ocr.py から run_with_bounded_buffer 削除 → tests/test_ocr.py の該当テスト移設"
---

<objective>
V171-OCR-04（L-1）を解消する。producer-consumer の二重実装（`ocr.py::run_with_bounded_buffer`＝本番未使用・テストのみ消費 と `ocr_dialog.py` の `_render_next_page`/`_worker`＝実運用）を、新モジュール `pagefolio/ocr_pipeline.py`（Tk/fitz 非依存）へ一本化する（D-01/D-02）。**方向性は D-01 固定**: `ocr_dialog.py` の実戦挙動（非ブロッキング put・世代ガード・waiting 進捗・skip status・render 失敗時の終了処理）を仕様とし、純ロジック層をそれに合わせて書き直す。逆方向（ヘルパーの理想仕様に dialog を寄せる）は禁止（Pitfall 1・Anti-Patterns）。
あわせて D-03 の吸収項目を同時解消する: L-6a（レンダー失敗で進捗が 100% に届かない）・L-6g（fatal 後も producer が全ページ render 継続）・L-6h（sentinel の暗黙容量不変条件の明文化）。サマリ生成（`_summary_worker`）は対象外（D-04）。

> **高リスク隔離プラン（ROADMAP リスク注記）**: 本フェーズ内で本プランのみが producer-consumer を触る。他 OCR 磨き込みプラン（02-01/02-02/02-03）完了後の Wave 4 で単独実行・単独検証する。安全網は既存 OCR テスト群（並列・キャンセル・進捗・リトライ）のグリーン維持。

Purpose: 保守対象を 1 箇所に集約し、実行パイプラインの堅牢性（メモリ上限・fatal 停止・進捗整合）を単一実装で担保する。
Output: 新設 `ocr_pipeline.py`・薄いラッパー化した `ocr_dialog.py`・未使用ヘルパー削除済み `ocr.py`・移設テスト・更新 CLAUDE.md。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr/02-CONTEXT.md
@.planning/phases/02-ocr/02-RESEARCH.md
@.planning/phases/02-ocr/02-PATTERNS.md
@pagefolio/pagination.py
</context>

<artifacts_this_phase_produces>
本プランが新規に作成するシンボル（drift 検証で既存扱いしないこと）:
- 新モジュール `pagefolio/ocr_pipeline.py`
- `PipelineState` クラス（共有カウンタ・Lock 保護。record_success / record_retryable_failure / record_fatal / record_page_error / is_fatal / decrement_worker 等）
- `consume_one(...)` 関数（1 アイテム消費：リトライ/バックオフ/fatal 判定。Tk 非依存・コールバック境界）
- `try_enqueue(q, item) -> bool` 非ブロッキング enqueue ヘルパー
- `send_sentinels(q, count) -> int` 部分送出可能な sentinel 送出ヘルパー
- 新規テスト `tests/test_ocr_pipeline.py`
- CLAUDE.md ファイル構成表の `ocr_pipeline.py` 行
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ocr_pipeline.py 新設（Tk/fitz 非依存純ロジック層）+ 既存 bounded buffer テスト移設・拡充</name>
  <files>pagefolio/ocr_pipeline.py, tests/test_ocr_pipeline.py, tests/test_ocr.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py:1430-1585（_worker の consumer 中核：retry/fatal 分岐・OCRRetryableError バックオフ・ConnectionError/TimeoutError/RuntimeError/Exception 処理・最終ワーカー判定 1551-1585。これが「仕様」）
    - pagefolio/ocr_dialog.py:656-681（_record_page_success / _record_retryable_failure の Lock 配下カウンタ更新・サーキットブレーカーの連続失敗カウント）
    - pagefolio/ocr_dialog.py:135-143（_done_lock/_done_count/_workers_remaining/_fatal_msg/_fatal_kind/_consec_err_count の Tk 依存版＝抽出対象）
    - pagefolio/ocr_dialog.py:1352-1365, 1397-1406（sentinel 非ブロッキング送出と put_nowait/queue.Full 再試行）
    - pagefolio/ocr.py:306-498（run_with_bounded_buffer：置換対象。_producer の fatal 未チェック 358-386・sentinel 無条件 blocking put は踏襲しない）
    - pagefolio/pagination.py:1-23（Tk/fitz 非依存純ロジック層の docstring 作法・確定名固定の先行例）
    - tests/test_ocr.py:1230-1327（TestProducerConsumerMemory 3 件＝移設元。呼び出し :1262/:1282/:1316）
    - tests/test_pagination.py（純ロジック層テストの書き方）
    - .planning/phases/02-ocr/02-RESEARCH.md Patterns 1/2/3・Common Pitfalls 1・02-PATTERNS.md §ocr_pipeline.py
  </read_first>
  <behavior>
    - PipelineState.record_success(): done_count += 1、連続失敗カウントを 0 リセット（_record_page_success 656-671 の Tk 非依存部と一致）
    - PipelineState.record_retryable_failure(msg, breaker_threshold): done_count += 1、連続失敗 += 1、閾値到達で fatal を circuit_breaker として一度だけ設定し hit_breaker=True を返す
    - PipelineState.record_fatal(msg, kind): 最初の 1 回だけ fatal_msg/fatal_kind を設定、done_count += 1（ConnectionError/TimeoutError 経路 1504-1517 と一致）
    - PipelineState.is_fatal(): fatal_msg が設定済みなら True（consumer が API 呼び出しを skip する判定・1457-1461 と一致）
    - PipelineState.decrement_worker(): workers_remaining -= 1 し (is_last, fatal_msg, fatal_kind) を返す（1551-1556 と一致）
    - try_enqueue(q, item): put_nowait 成功で True、queue.Full で False（例外を投げない）
    - send_sentinels(q, count): None を最大 count 個 put_nowait し、実際に送れた数を返す（Full で打ち切り・部分送出）
    - consume_one(provider, item, prompt, state, cancel_check, callbacks): 1 アイテムを MAX_RETRIES までリトライしつつ処理し、成功/ページエラー/fatal をコールバックと state で表現する。self.after/self.text 等の Tk 依存を一切含まない
    - 同時保持画像数がバッファ上限（workers+1）を超えない（移設テストの不変条件）
    - キャンセル時に有限時間で終了しデッドロックしない
  </behavior>
  <action>
    `pagefolio/ocr_pipeline.py` を新設する（D-02）。冒頭に MIT ヘッダと、pagination.py と同型の docstring を置き、「ここに fitz / tkinter を一切 import しない」旨と参照決定（02-CONTEXT.md D-01/D-02）を明記する。L-6h の sentinel 容量不変条件（「終端シグナルは合計 workers 本・送信済み分は再送しない・バッファ満杯時は部分送出して呼び出し元が再送する」）を docstring に明文化する。

    `PipelineState` クラスを定義する（threading.Lock 保護の共有カウンタ）。ocr_dialog.py:135-143 の Tk 依存カウンタ群と 656-681 のカウント更新・1504-1556 の fatal/最終ワーカー判定を Tk 非依存メソッドへ集約する（record_success / record_retryable_failure(msg, breaker_threshold) / record_fatal(msg, kind) / record_page_error / is_fatal / decrement_worker）。サーキットブレーカーの連続失敗カウントと閾値も PipelineState 側へ移す（既存の閾値定数を流用）。RESEARCH.md Pattern 1 の実装イメージを出発点にする。

    `try_enqueue(q, item) -> bool` と `send_sentinels(q, count) -> int` を定義する（ocr_dialog.py:1352-1365/1397-1406 のキュー操作を「レンダリング方法を規定しない」薄いユーティリティとして抽出）。「いつ再試行するか」（after(100) 再スケジュール）は呼び出し元＝dialog に残す（Pattern 2/Pitfall 1：producer に専用スレッドを作らない）。

    `consume_one(...)` を定義する。ocr_dialog.py:1466-1528 の retry/fatal 分岐をそのまま移植し、`self.provider.ocr_image_ex` は引数 provider 経由、`_record_page_success`/`_record_retryable_failure`/errors 更新・待機進捗表示はコールバック引数（on_success / on_page_error / on_retry_wait 等）と PipelineState 経由に置換する。バックオフ（clamp_retry_after / interruptible_sleep）と OCRRetryableError は `pagefolio.ocr` / `pagefolio.ocr_providers` から関数内で遅延 import する（既存 :1479 の遅延 import 作法に倣い循環 import を回避）。MAX_RETRIES 等の定数は既存の出所から参照する。

    tests/test_ocr.py:1230-1327 の TestProducerConsumerMemory 3 件を tests/test_ocr_pipeline.py へ物理移設し、新 API（PipelineState/consume_one/try_enqueue/send_sentinels）へ書き換える（D-02）。test_ocr.py 側からは移設した 3 件を削除する。あわせて拡充ケースを新規追加する: render 失敗ページでも done がインクリメントされ最終進捗が全ページ数に到達する（render_failure_progress）・キャンセルで有限時間終了しデッドロックしない（cancel_terminates_without_deadlock）。test_pagination.py 同様に Tk/fitz 非依存で直接 assert する。

    裸の except 禁止・logger 経由記録（CLAUDE.md 規約）。
  </action>
  <verify>
    <automated>pytest tests/test_ocr_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ocr_pipeline.py -q` がグリーン（移設 3 件 + 新規 render_failure_progress / cancel_terminates_without_deadlock）
    - `grep -n "import fitz\|import tkinter\|^import tk\|from tkinter" pagefolio/ocr_pipeline.py` が 0 件（Tk/fitz 非依存）
    - `grep -n "class PipelineState\|def consume_one\|def try_enqueue\|def send_sentinels" pagefolio/ocr_pipeline.py` が各 1 件マッチ
    - ocr_pipeline.py の docstring に sentinel 容量不変条件（L-6h）が記載されている（`grep -n "sentinel\|終端\|workers" pagefolio/ocr_pipeline.py` で確認）
    - `pytest tests/test_ocr.py -q` がグリーン（移設で消えたテストの参照残りなし）
    - `ruff check pagefolio/ocr_pipeline.py tests/test_ocr_pipeline.py tests/test_ocr.py` がクリーン
  </acceptance_criteria>
  <done>Tk/fitz 非依存の ocr_pipeline.py が dialog の実戦挙動を仕様として実装され、移設+拡充テストがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: ocr_dialog.py を薄いラッパー化 + L-6a/L-6g 修正 + ocr.py 未使用ヘルパー削除 + CLAUDE.md/REVIEW.md 追記</name>
  <files>pagefolio/ocr_dialog.py, pagefolio/ocr.py, CLAUDE.md, .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md</files>
  <read_first>
    - pagefolio/ocr_dialog.py:1311-1428（_render_next_page 全体・_start_worker_thread）と :1430-1585（_worker 全体）— ラッパー化対象
    - pagefolio/ocr_dialog.py:135-143, 656-681（PipelineState へ移す共有状態と record 系メソッド。移行後は PipelineState インスタンス（例 self._pstate）を保持・参照する形へ）
    - pagefolio/ocr_dialog.py:1407-1409（_render_next_page の except ブロック：render 失敗時に errors のみ設定し progress 未更新＝L-6a）
    - pagefolio/ocr_dialog.py:1367-1394（skip パスの progress_var/_on_progress_bar 更新の同型：L-6a 修正の参考）
    - pagefolio/ocr.py:306-498（run_with_bounded_buffer 削除対象）と、その上下の import/他関数（run_parallel・build_provider・プロンプト解決系は変更しない）
    - CLAUDE.md ファイル構成表（undo_store.py / pagination.py 等の 1 行フォーマット）
    - .planning/phases/02-ocr/02-RESEARCH.md Runtime State Inventory / Open Question 1（外部参照ゼロの確認）
  </read_first>
  <action>
    <!-- planner-discipline-allow: run_with_bounded_buffer -->
    _render_next_page / _worker を ocr_pipeline の関数・PipelineState を呼ぶ薄いラッパーへ縮小する（D-01）。散在していた self._done_lock/_done_count/_workers_remaining/_fatal_msg/_fatal_kind/_consec_err_count（:135-143）を PipelineState インスタンス（例 self._pstate、_on_run のバッファ初期化 :1311-1318 付近で生成）へ置き換え、_worker は queue.get→consume_one→（世代ガード後に）progress を after で更新→最終ワーカーで _finish_* を after 投函、の構造にする。世代ガード（gen 不一致 early return）・winfo_exists・after 連鎖・put_nowait/queue.Full 再試行は dialog 側に残す（D-01 の核心・Pattern 2/3）。producer 側の enqueue/sentinel は try_enqueue/send_sentinels を使う。

    L-6a: _render_next_page の except ブロック（:1407-1409）で、当該ページを「処理済み」として計上し progress を更新する。skip パス（:1381-1394）の done_disp 算出 + progress_var.set + self.after(0, _on_progress_bar) と同型の更新をこの except 節にも追加し、render 失敗ページがあっても統合進捗が全ページ数（100%）に到達するようにする。errors[page_idx] の設定は維持する。

    L-6g: 一本化後の producer（_render_next_page）が fatal 発生後に残ページの render を継続しないようにする。cancel_flag チェック（:1339）と同型で、PipelineState.is_fatal() が True のとき残りページの render をやめて sentinel を送出し終了へ向かう分岐を先頭付近に追加する（未使用ヘルパーの fatal 未チェック :358-386 の欠陥を新実装へ持ち込まない）。

    ocr.py の run_with_bounded_buffer（:306-498）を削除する。削除前に外部参照ゼロを確認する（`grep -rn` で README.md / 開発履歴.md / plugins/ に本番参照がないこと。テストは Task 1 で test_ocr_pipeline.py へ移設済み）。run_parallel・build_provider・プロンプト解決系は変更しない。

    CLAUDE.md のファイル構成表へ ocr_pipeline.py の 1 行を追記する（D-02・L-6k。undo_store.py 等と同じ書式で「Tk/fitz 非依存の producer-consumer 純ロジック層」と記載）。モジュール構成の一覧にも整合する形で反映する。

    D-12: 解消した L-1 および吸収項目 L-6a/L-6g/L-6h について 260610-aaa-REVIEW.md の該当行へ ✅ + 解消コミットハッシュを追記する。

    裸の except 禁止・テーマ色は C[...]・フォントは self._font(delta) 経由（CLAUDE.md 規約）。V14-D-05（fitz.get_pixmap はメインスレッドのみ）・V14-D-06（バッファ上限）を維持する。
  </action>
  <verify>
    <automated>pytest tests/test_ocr.py tests/test_provider_ui.py tests/test_ocr_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "run_with_bounded_buffer" pagefolio/` がマッチなし（未使用ヘルパー完全削除）
    - `grep -n "PipelineState\|consume_one\|try_enqueue\|send_sentinels" pagefolio/ocr_dialog.py` が各 1 件以上マッチ（ラッパー化）
    - `grep -n "is_fatal()" pagefolio/ocr_dialog.py` が _render_next_page 内でマッチ（L-6g：fatal 後の render 停止）
    - _render_next_page の except 節に progress 更新（_on_progress_bar 呼び出し）が追加されている（L-6a）
    - `grep -n "ocr_pipeline" CLAUDE.md` が 1 件以上マッチ（ファイル構成表追記）
    - `pytest tests/test_ocr.py tests/test_provider_ui.py tests/test_ocr_pipeline.py -q` がグリーン
    - 260610-aaa-REVIEW.md の L-1 / L-6a / L-6g / L-6h 行に ✅ が追記されている
    - `ruff check pagefolio/ocr_dialog.py pagefolio/ocr.py` がクリーン
  </acceptance_criteria>
  <done>dialog が ocr_pipeline を呼ぶ薄いラッパーへ縮小され、L-6a/L-6g が修正され、未使用ヘルパーが削除され、CLAUDE.md/REVIEW.md が更新された。</done>
</task>

<task type="auto">
  <name>Task 3: 回帰ゲート（既存 OCR テスト群グリーン維持の単独検証・隔離プランの安全網）</name>
  <files>tests/test_ocr.py, tests/test_ocr_pipeline.py, tests/test_ocr_providers.py, tests/test_provider_ui.py, tests/test_plugins.py</files>
  <read_first>
    - .planning/phases/02-ocr/02-RESEARCH.md Validation Architecture（Quick run / Full suite コマンド・Sampling Rate）
    - .planning/phases/02-ocr/02-VALIDATION.md（フェーズ検証契約）
    - tests/test_undo_stress.py（重い実行がある場合の実行時間感覚。フルスイートに含める）
  </read_first>
  <action>
    L-1 一本化の回帰なしを単独検証する（ROADMAP 成功基準 4・隔離プランの安全網）。OCR 関連テスト群（並列・キャンセル・進捗・リトライ）と新設 ocr_pipeline テストを実行し、続いてフルスイートを実行してグリーンを確認する。最後に ruff を実行する。いずれかが赤の場合は Task 1/Task 2 へ戻って修正する（新規テストの追加や本番挙動の変更は行わず、あくまで一本化の回帰確認に限定する）。この検証結果（実行コマンドと結果）は SUMMARY へ記録する。
  </action>
  <verify>
    <automated>pytest tests/test_ocr.py tests/test_ocr_pipeline.py tests/test_ocr_providers.py tests/test_provider_ui.py tests/test_plugins.py -q && pytest -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - OCR テスト群（test_ocr.py / test_ocr_pipeline.py / test_ocr_providers.py / test_provider_ui.py / test_plugins.py）がすべてグリーン
    - フルスイート `pytest -q` がグリーン（707 件超・回帰なし）
    - `ruff check .` がクリーン、`ruff format --check .` が差分なし
    - 並列/キャンセル/進捗/リトライの既存テストが赤化していない（一本化による挙動変化がない）
  </acceptance_criteria>
  <done>OCR テスト群とフルスイートがグリーンで、L-1 一本化に回帰がないことが単独検証された。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （新規なし） | 本プランは内部リファクタリングであり、新たな信頼境界（外部入力の越境点）を導入しない |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-L01 | Denial of Service | ocr_pipeline のバッファ/sentinel 制御 | low | mitigate | バッファ上限 concurrency+1 を維持（V14-D-06）し全ページ base64 の同時保持を防ぐ。sentinel 容量不変条件を docstring 明文化 + テストで担保し、送りこぼしによるワーカー未終了（ハング/リーク）を防ぐ（L-6h） |
| T-2-L02 | Denial of Service | fatal 後の producer 継続 | low | mitigate | fatal 発生後に producer が残ページ render を継続しないよう is_fatal 分岐を追加（L-6g）。無駄なレンダリング/API 送信の継続を停止する |

本プランはパッケージインストールを伴わない（T-{phase}-SC 非該当）。fitz レンダリングはメインスレッドのみ（V14-D-05 維持）。
</threat_model>

<verification>
- Task 3 の回帰ゲート（OCR テスト群 + フルスイート + ruff）がすべてグリーン
- 同時保持画像数がバッファ上限（concurrency+1）を超えない（test_ocr_pipeline.py で担保）
- キャンセルが有限時間で終了しデッドロックしない（test_ocr_pipeline.py で担保）
- 手動確認は任意（実 API/実描画の目視は将来の human-verify 候補。コード検証は自動テストで完結）
</verification>

<success_criteria>
- ROADMAP 成功基準 4（producer-consumer が単一実装へ一本化され、並列・キャンセル・進捗・リトライが回帰なく動作）が満たされる
- run_with_bounded_buffer が削除され、実行パイプラインが ocr_pipeline.py の単一実装になる
- L-6a/L-6g/L-6h が同時解消される（D-03）
- V14-D-05/06 が一本化後も維持される
</success_criteria>

<output>
Create `.planning/phases/02-ocr/02-04-SUMMARY.md` when done
</output>
