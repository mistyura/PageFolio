---
phase: 03-v1-5-0
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pagefolio/page_ops.py
  - pagefolio/ui_builder.py
  - pagefolio/lang.py
  - pagefolio/constants.py
  - tests/test_page_polish.py
autonomous: true
requirements:
  - V171-PAGE-03
user_setup: []

must_haves:
  truths:
    - "回転90/180/270 のページで、トリミング矩形が『見たままの位置』へ適用される（D-08・_derotate_rect 共通ヘルパー）"
    - "確定済み矩形を矢印キーで 1pt 移動、Shift+矢印で右下辺リサイズできる（D-09）"
    - "『上下左右から何mm削るか』の余白指定で選択ページへ一括トリミングできる（D-10）"
    - "crop_info が『45×60mm（28%）』形式（mm 換算＋ページ占有率）で表示される（D-11）"
    - "mm→pt 換算は単一定数 PT_PER_MM に集約され、負値/過大値は幅高さ<1で安全側フォールバックされる"
  prohibitions:
    - statement: "回転座標変換ロジックを黒塗り/モザイク/トリミングで個別に重複実装しない（共通ヘルパー _derotate_rect に集約）"
      status: resolved
      verification: "_derotate_rect は page_ops.py に1定義のみ・呼び出し側は委譲（grep で定義数1を確認）"
  artifacts:
    - "pagefolio/page_ops.py に _derotate_rect 静的ヘルパー"
    - "pagefolio/page_ops.py に _nudge_crop_rect・_crop_by_margin メソッド"
    - "pagefolio/page_ops.py に _format_crop_info・compute_margin_crop_rect 純関数"
    - "pagefolio/constants.py に PT_PER_MM 定数"
    - "pagefolio/lang.py に mm トリミング関連 LANG キー（ja/en）"
    - "tests/test_page_polish.py の TestDerotateRect / TestCropPolish / TestMarginCrop"
  key_links:
    - "_canvas_rect_to_pdf → _derotate_rect → 既存 mediabox 相対化 の1本道（適用順序固定・二重補正防止）"
    - "_crop_drag_move → _format_crop_info への置換（mm 表示）"
    - "preview_canvas の矢印キーバインド → _nudge_crop_rect"
---

<objective>
回転/トリミングの棚卸し 3 項目（D-09/D-10/D-11）と、黒塗り/モザイク/トリミングが共有する回転座標対応の共通基盤（D-08 の `_derotate_rect` ヘルパー＋トリミング側統合）を実装する（V171-PAGE-03）。回転表示中のページで矩形が「未回転ページ座標系」にしか正しく適用されない既知制限を、PyMuPDF 公式の `page.derotation_matrix` を使う共通ヘルパーで構造的に解消する（D-08）。回転機能自体の追加改善は見送る（D-12・座標対応のみ）。`_derotate_rect` は本プランで新設し、黒塗り/モザイク側の統合は後続 03-04 が同ヘルパーを再利用する。

Purpose: 「見たままの位置に適用される」体験をトリミングで実現し、矢印微調整・mm 指定・mm 表示で操作性を底上げする。
Output: `_derotate_rect`/`_nudge_crop_rect`/`_crop_by_margin`（page_ops.py）、`_format_crop_info`/`compute_margin_crop_rect`（純関数）、`PT_PER_MM`（constants.py）、mm トリミング UI・LANG、TestDerotateRect/TestCropPolish/TestMarginCrop。
</objective>

<artifacts_produced>
## Artifacts this phase produces (このプラン新規シンボル)

- `_derotate_rect` — 静的メソッド（`pagefolio/page_ops.py`・表示座標→未回転座標変換・D-08）
- `_nudge_crop_rect` — メソッド（`pagefolio/page_ops.py`・矢印微調整・D-09）
- `_crop_by_margin` — メソッド（`pagefolio/page_ops.py`・mm 指定トリミング入口・D-10）
- `_format_crop_info` — module-level 純関数（`pagefolio/page_ops.py`・crop_info 文字列・D-11）
- `compute_margin_crop_rect` — module-level 純関数（`pagefolio/page_ops.py`・mm 余白差引・D-10）
- `PT_PER_MM` — 定数（`pagefolio/constants.py`・72/25.4）
- mm トリミング関連 LANG キー（`pagefolio/lang.py` ja/en・ボタン/入力ラベル）
- `TestDerotateRect` / `TestCropPolish` / `TestMarginCrop` — テストクラス（`tests/test_page_polish.py`）
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-v1-5-0/03-CONTEXT.md
@.planning/phases/03-v1-5-0/03-RESEARCH.md
@.planning/phases/03-v1-5-0/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 回転座標共通ヘルパー _derotate_rect + トリミング統合 + crop_info mm 表示（D-08/D-11）</name>
  <files>pagefolio/page_ops.py, pagefolio/constants.py, tests/test_page_polish.py</files>
  <read_first>
    - pagefolio/page_ops.py（`_canvas_rect_to_pdf`:253-267＝表示座標変換・維持対象。`_crop_page`:349-447＝単一分岐:362-391 と bulk 分岐:392-432 の両方に derotate を挟む。`_crop_drag_move`:279-329 の `crop_info_var.set`:327-329＝mm 表示へ置換）
    - pagefolio/redact_ops.py（`_page_rect_from_rel`:135-157＝mediabox 相対化の基準。derotate は「表示→未回転」のみ担当し mediabox 相対化は温存する・Pitfall 2）
    - pagefolio/constants.py（定数定義の並び。PT_PER_MM の追加先）
    - .planning/phases/03-v1-5-0/03-RESEARCH.md（Pattern 2＝`page.derotation_matrix`・Pitfall 2＝mediabox 基準を崩さない・Code Examples の `_format_crop_info`）
    - tests/test_pdf_ops.py（`_make_app` FakeApp:1477 と `_page_rect_from_rel` テスト:1440-1466 の「小さな fitz.Document を都度生成」記法）
  </read_first>
  <behavior>
    - `_derotate_rect(page, x0, y0, x1, y1)`: `page.rotation == 0` のとき恒等（入力の min/max 正規化のみ）。90/180/270 のとき `page.derotation_matrix` で 2 点を変換し、正規化した (x0,y0,x1,y1) を返す（D-08）。
    - crop への統合: `_canvas_rect_to_pdf(*self.crop_rect)` の直後、mediabox 相対化の前に `_derotate_rect(page, ...)` を通す1本道（適用順序固定・二重補正防止・Pitfall 2）。単一・bulk 両分岐に適用。
    - `_format_crop_info(rect_w_pt, rect_h_pt, mb_w_pt, mb_h_pt)`: "45×60mm（28%）" 形式を返す純関数（mm 換算＋面積占有率・D-11）。mb が 0 のとき pct=0 で安全。
  </behavior>
  <action>
    `pagefolio/constants.py` に `PT_PER_MM = 72 / 25.4` を追加する（mm↔pt の単一情報源・RESEARCH「Don't Hand-Roll」・分散禁止）。`pagefolio/page_ops.py` に静的メソッド `_derotate_rect(page, x0, y0, x1, y1)` を追加（RESEARCH Pattern 2）: `dm = page.derotation_matrix`、`fitz.Point(x0,y0)*dm` と `fitz.Point(x1,y1)*dm` を計算し `(min, min, max, max)` を返す。`_crop_page` の単一分岐（362-391）と bulk 分岐（392-432）の両方で、`x0_pdf,y0_pdf,x1_pdf,y1_pdf = self._canvas_rect_to_pdf(*self.crop_rect)` の直後に `x0_pdf,y0_pdf,x1_pdf,y1_pdf = self._derotate_rect(page, x0_pdf, y0_pdf, x1_pdf, y1_pdf)` を挿入する（単一は `page = self.doc[self.current_page]`、bulk は基準ページ `self.doc[self.current_page]` で derotate してから rel 化）。既存の `mb = page.mediabox` 基準の相対化ロジックはそのまま温存する（Pitfall 2・mediabox を崩さない）。`page_ops.py` に module-level 純関数 `_format_crop_info(rect_w_pt, rect_h_pt, mb_w_pt, mb_h_pt)` を追加（RESEARCH Code Examples）: `PT_PER_MM` で mm 換算し `f"{w_mm:.0f}×{h_mm:.0f}mm（{pct:.0f}%）"`（pct は面積比×100・mb<=0 で 0.0）。`_crop_drag_move` の `crop_info_var.set(...)`（327-329）を `_format_crop_info` の呼び出しへ置換する（現在の矩形 pt 幅高さと現在ページ mediabox 幅高さを渡す）。`from pagefolio.constants import ...` に PT_PER_MM を追加。tests/test_page_polish.py に `TestDerotateRect`（小さな fitz.Document を rotation 0/90/180/270 で生成し、既知矩形を `_derotate_rect` に通して未回転座標が期待どおり・回転0で恒等を検証）と `TestCropInfoFormat`（`_format_crop_info` の mm/％ 文字列を数値包含で検証・Tk/fitz 不要）を追加する。
  </action>
  <verify>
    <automated>pytest tests/test_page_polish.py -k "derotate or crop_info" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/page_ops.py` に `def _derotate_rect(` が1定義のみ存在する（`grep -c 'def _derotate_rect' pagefolio/page_ops.py` が 1）
    - `_crop_page` の単一・bulk 両分岐で `_derotate_rect` が呼ばれる
    - `pagefolio/constants.py` に `PT_PER_MM` が定義され、page_ops.py が import している
    - `pagefolio/page_ops.py` に `def _format_crop_info(` が存在し `_crop_drag_move` から呼ばれる
    - `pytest tests/test_page_polish.py -k "derotate or crop_info" -x` が exit 0（回転0で恒等・90/270 で座標変換を検証）
    - `ruff check pagefolio/page_ops.py pagefolio/constants.py tests/test_page_polish.py` が clean
  </acceptance_criteria>
  <done>回転ページでトリミング矩形が見たまま位置へ適用され、crop_info が mm＋％ 表示になる。derotate は単一定義で共通化され、テストがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: 矩形の矢印キー微調整（D-09）</name>
  <files>pagefolio/page_ops.py, pagefolio/ui_builder.py, tests/test_page_polish.py</files>
  <read_first>
    - pagefolio/page_ops.py（`_crop_drag_move`:279-329 のオーバーレイ描画（`crop_overlay_ids`/`crop_rect_id`/`coords` 更新）＝リサイズ後の再描画に再利用。`_canvas_rect_to_pdf` の `scale = self.zoom * 1.5`＝pt→canvas 換算スケール）
    - pagefolio/ui_builder.py（344-352＝preview_canvas への `<ButtonPress-1>`/`<B1-Motion>`/`<ButtonRelease-1>` バインド箇所。矢印キーバインドの追加先）
    - .planning/phases/03-v1-5-0/03-RESEARCH.md（Code Examples「矢印キー微調整」・バインド先は Claude's Discretion）
  </read_first>
  <action>
    `pagefolio/page_ops.py` に `_nudge_crop_rect(self, dx_pt, dy_pt, resize=False)` を追加する（RESEARCH Code Examples）: `self.crop_rect` が未確定なら return。`scale = self.zoom * 1.5`（`_canvas_rect_to_pdf` と同一）で `dx = dx_pt*scale, dy = dy_pt*scale`。`resize=True` は右下 (ex,ey) のみ加算、False は 4 点平行移動。`self.crop_rect` を更新し、既存オーバーレイ更新（`_crop_drag_move` のオーバーレイ再描画部分を小ヘルパーへ切り出すか、同等の `coords` 更新）で矩形を再描画する。`pagefolio/ui_builder.py` の 352 付近（preview_canvas ドラッグバインドの直後）に矢印キーバインドを追加: `<Left>/<Right>/<Up>/<Down>` を移動、`<Shift-Left>/<Shift-Right>/<Shift-Up>/<Shift-Down>` をリサイズへ結び、`_nudge_crop_rect(±1, 0)` 等 1pt 単位で呼ぶ。ハンドラ内で `self.crop_rect` 未確定または `not (self.crop_mode or self.redact_mode)` のとき無効化（早期 return）。canvas がキーイベントを受けるよう必要に応じ `preview_canvas.focus_set()` を drag_start 等で行う（Claude's Discretion）。tests/test_page_polish.py に `TestCropPolish` を追加: FakeApp（`self.zoom` を設定・`_crop_drag_move` のオーバーレイ Tk 依存は monkeypatch/簡易スタブ）で `crop_rect` をセットし `_nudge_crop_rect(1,0)`/`(0,1,resize=True)` 後の `crop_rect` 数値が期待どおり（scale 換算・移動 vs リサイズ）を検証する。オーバーレイ再描画の Tk 呼び出しはスタブ化してロジックのみ検証する。
  </action>
  <verify>
    <automated>pytest tests/test_page_polish.py -k "nudge or crop_polish" -x -q</automated>
    <human-check>アプリ起動 → トリミングモード ON → 矩形ドラッグで確定 → 矢印キーで 1pt 移動、Shift+矢印で右下辺リサイズがプレビューに反映されることを目視確認（VALIDATION.md Manual-Only「矩形ドラッグ操作感」）</human-check>
  </verify>
  <acceptance_criteria>
    - `pagefolio/page_ops.py` に `def _nudge_crop_rect(self, dx_pt, dy_pt, resize=False):` が存在する
    - `pagefolio/ui_builder.py` に `<Left>`/`<Shift-Left>` 等の矢印キーバインドが preview_canvas に追加されている
    - `_nudge_crop_rect` は `crop_rect` 未確定で早期 return する
    - `pytest tests/test_page_polish.py -k "nudge or crop_polish" -x` が exit 0（移動・リサイズの数値を検証）
    - `ruff check pagefolio/page_ops.py pagefolio/ui_builder.py` が clean
  </acceptance_criteria>
  <done>確定矩形が矢印で 1pt 移動・Shift+矢印でリサイズでき、移動/リサイズのロジックがテストで担保される。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 数値指定（mm）トリミング（D-10）</name>
  <files>pagefolio/page_ops.py, pagefolio/ui_builder.py, pagefolio/lang.py, tests/test_page_polish.py</files>
  <read_first>
    - pagefolio/page_ops.py（`_crop_page` の bulk 分岐:392-432＝クランプ/`set_cropbox`/undo（`bulk_crop`）作法。`_split_by_range`:682-743 の simpledialog 連続入力の型。冒頭 import の PT_PER_MM）
    - pagefolio/ui_builder.py（トリミングセクション f3:564-581＝`btn(f3, self._t("btn_crop"), self._crop_page, ...)` の並び。数値トリミングボタンの追加先）
    - pagefolio/lang.py（`btn_crop`:近傍・ja/en 両辞書。新規 mm 入力ラベル/タイトルキーの追加先）
    - .planning/phases/03-v1-5-0/03-RESEARCH.md（Code Examples「数値指定トリミング」・A2＝現在の cropbox 基準・Open Question 1）
  </read_first>
  <behavior>
    - `compute_margin_crop_rect(current_cropbox, top_pt, bottom_pt, left_pt, right_pt)`: 現在 cropbox の四辺から余白(pt)を差し引いた新 cropbox タプル (x0,y0,x1,y1) を返す純関数。差引結果の幅/高さが 1pt 未満なら None（安全側フォールバック・T-3-03）。基準は「現在の cropbox」（A2・今見えている範囲から削る）。
    - `_crop_by_margin`: mm 入力（simpledialog）を受け PT_PER_MM で pt 換算し、選択ページへ `compute_margin_crop_rect` の結果で `set_cropbox`。undo は既存 `bulk_crop` op（適用前 cropbox タプルをキャプチャ）を流用。負値/文字列/過大値は None フォールバックまたは入力バリデーションで安全に無視。
  </behavior>
  <action>
    `pagefolio/page_ops.py` に module-level 純関数 `compute_margin_crop_rect(current_cropbox, margin_top_pt, margin_bottom_pt, margin_left_pt, margin_right_pt)` を追加する（RESEARCH Code Examples）: `x0 = cb.x0 + left`, `y0 = cb.y0 + top`, `x1 = cb.x1 - right`, `y1 = cb.y1 - bottom`、`x1-x0 < 1 or y1-y0 < 1` で None を返す。`_crop_by_margin(self)` メソッドを追加: `_check_doc()` → `_get_targets()`、上下左右の mm を simpledialog（連続 askfloat・minvalue=0 か、小ダイアログ1枚のどちらかは Claude's Discretion）で受け、`PT_PER_MM` で pt 換算。各対象ページの現在 cropbox から `compute_margin_crop_rect` を計算し None ならスキップ、それ以外は既存 bulk 分岐と同じクランプ（mediabox 内・EPS）を経て `set_cropbox`。undo は `_save_undo("bulk_crop", crop_data=[(i,(cb...)),...])`（適用前 cropbox を全対象で1回キャプチャ・既存 op 流用）。末尾は `_invalidate_thumb_cache(targets)`→`_refresh_all()`→`_set_status(...)`。`pagefolio/lang.py` の ja/en 両辞書へ mm トリミング用キーを同一キーで追加（例: `btn_crop_margin`＝ボタン文言、`dlg_crop_margin_title`＝ダイアログタイトル、`crop_margin_top/bottom/left/right`＝各辺プロンプト。文言は既存トリミング系に合わせて自然に・test_lang_parity 監視のため両辞書必須）。`pagefolio/ui_builder.py` のトリミングセクション f3（564-581）へ `btn(f3, self._t("btn_crop_margin"), self._crop_by_margin, needs_doc=True, edit_only=True)` を追加。tests/test_page_polish.py に `TestMarginCrop` を追加: `compute_margin_crop_rect` を純関数として直接テスト（正常な余白差引・幅<1 で None・負/過大値でのフォールバック）し、FakeApp で `_crop_by_margin`（simpledialog を monkeypatch で固定 mm 値へ）適用後の cropbox 変化と undo 復元を検証する。
  </action>
  <verify>
    <automated>pytest tests/test_page_polish.py -k "margin_crop" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/page_ops.py` に `def compute_margin_crop_rect(` と `def _crop_by_margin(self):` が存在する
    - `compute_margin_crop_rect` は幅/高さ<1 で None を返す（安全側・T-3-03）
    - `pagefolio/lang.py` の ja/en 両辞書へ mm トリミング用キーが追加されている（parity 維持）
    - `pagefolio/ui_builder.py` に `_crop_by_margin` を command とするボタンが存在する
    - `pytest tests/test_page_polish.py -k margin_crop -x` と `pytest tests/test_lang_parity.py` が exit 0
    - `ruff check pagefolio/page_ops.py pagefolio/ui_builder.py pagefolio/lang.py` が clean
  </acceptance_criteria>
  <done>「上下左右から何mm削るか」で選択ページを一括トリミングでき、不正値は安全にフォールバックし、undo で復元される。純関数と FakeApp のテストがグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mm 数値入力（simpledialog）→ cropbox 変更 | ユーザー入力の数値が cropbox 計算に流入する |
| 表示（回転後）座標 → 未回転ページ座標 | プレビュー座標が PDF の未回転座標系へ変換され set_cropbox へ渡る |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-03 | Tampering | `_crop_by_margin` / `compute_margin_crop_rect` の mm 入力 | medium | mitigate | `compute_margin_crop_rect` が幅/高さ<1 で None フォールバック（既存 `_crop_page` の EPS/is_empty と同型）。simpledialog は minvalue=0・型検証。CropBox は MediaBox 内へクランプしてから set_cropbox（CLAUDE.md 制約） |
| T-3-05 | Tampering | `_derotate_rect` の座標変換 | low | mitigate | 表示→未回転の変換のみ担当し mediabox 相対化は温存（Pitfall 2）。回転0で恒等・90/270 の入替をテストで担保 |
| T-3-SC | Tampering | パッケージインストール | low | accept | 本フェーズは新規パッケージを一切導入しない（RESEARCH.md Package Legitimacy Audit）。install タスクなし |
</threat_model>

<verification>
- `pytest tests/test_page_polish.py tests/test_lang_parity.py -q` グリーン
- `pytest`（フルスイート）で既存テストに回帰なし
- `ruff check . && ruff format --check .` clean
- 矩形ドラッグ/矢印微調整の操作感は end-of-phase human-verify で確認
</verification>

<success_criteria>
- 回転90/180/270 のページでトリミング矩形が見たまま位置へ適用される（成功基準3・D-08）
- 矢印キー微調整（D-09）・mm 指定トリミング（D-10）・crop_info mm＋％ 表示（D-11）が動作する
- 回転機能自体の追加改善は見送り（D-12）・座標対応のみ
- LANG ja/en パリティ維持・既存テスト回帰なし
</success_criteria>

<output>
Create `.planning/phases/03-v1-5-0/03-03-SUMMARY.md` when done
</output>
