---
phase: 03-v1-5-0
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - pagefolio/redact_ops.py
  - pagefolio/page_ops.py
  - pagefolio/ui_builder.py
  - pagefolio/lang.py
  - tests/test_page_polish.py
autonomous: true
requirements:
  - V171-PAGE-02
user_setup: []

must_haves:
  truths:
    - "黒塗り/モザイク適用後もモードが維持され、明示トグルでのみ OFF になる（D-05・連続適用）。トリミングとの相互排他は維持される"
    - "モザイク粒度スライダーで粗さを変更でき、値は pagefolio_settings.json に永続化される（D-06）"
    - "複数矩形を追加→一括適用でき、1回の Undo で全矩形がまとめて戻る（D-07）"
    - "回転90/180/270 のページで黒塗り/モザイク矩形が『見たままの位置』へ適用される（D-08・_derotate_rect 再利用）"
    - "複数矩形一括適用でも各矩形の下地コンテンツが個別に実削除され、一部矩形の下地が残らない（情報漏えい防止）"
  prohibitions:
    - statement: "モザイク粒度をグローバル定数 MOSAIC_BLOCK の書き換えで変更しない（設定キー経由で既定値として温存）"
      status: resolved
      verification: "MOSAIC_BLOCK は constants.py で不変・_apply_mosaic は settings.get('mosaic_block', MOSAIC_BLOCK) を渡す（grep で確認）"
    - statement: "複数矩形一括適用で _save_undo を矩形/ページごとに多重呼び出ししない（ループ外で1回のみ）"
      status: resolved
      verification: "_apply_page_edit 本体で _save_undo 出現回数が1（grep -c）"
  artifacts:
    - "pagefolio/redact_ops.py の _mosaic_page に block 引数、_apply_page_edit の複数矩形化・derotate 統合・連続適用化"
    - "pagefolio/redact_ops.py に _clear_redact_rects・_on_mosaic_block_release メソッド"
    - "pagefolio/page_ops.py の矩形ドラッグハンドラに redact モードの複数矩形蓄積分岐"
    - "pagefolio/ui_builder.py にモザイク粒度スライダー・クリアボタン"
    - "settings キー mosaic_block、LANG キー（ja/en）"
    - "tests/test_page_polish.py の TestRedactPolish"
  key_links:
    - "_apply_page_edit → _derotate_rect（03-03 で新設）→ _page_rect_from_rel の1本道"
    - "_apply_page_edit → 全矩形×全対象ページのループで _save_undo は外側1回のみ（一括 undo の要）"
    - "mosaic_block_var → _on_mosaic_block_release → settings 永続化 → _apply_mosaic の block 引数"
---

<objective>
黒塗り/モザイクの棚卸し 4 項目（D-05 連続適用・D-06 モザイク粒度スライダー・D-07 複数矩形一括適用・D-08 回転座標対応）を実装する（V171-PAGE-02）。D-08 は 03-03 が新設した共通ヘルパー `_derotate_rect` を `_apply_page_edit` へ統合して再利用し、黒塗り/モザイク/トリミングの 3 操作すべてで「見たままの位置」を一貫させる。破壊的削除（`apply_redactions` による実削除）は後退させず、複数矩形でも各矩形の下地を個別に実削除する。

Purpose: 複数箇所の黒塗りを毎回ボタンを押し直さずに塗り続けられ（連続適用）、粒度を調整でき、回転ページでも見たまま適用できる操作性へ引き上げる。
Output: `_apply_page_edit` の複数矩形化＋derotate 統合＋連続適用化、`_mosaic_page` の block 引数化、モザイク粒度スライダー・クリアボタン・settings 永続化、TestRedactPolish。
</objective>

<artifacts_produced>
## Artifacts this phase produces (このプラン新規シンボル)

- `_mosaic_page` の `block` 引数（`pagefolio/redact_ops.py`・既定 MOSAIC_BLOCK）
- `_apply_page_edit` の改修（複数矩形ループ・`_derotate_rect` 統合・`_redact_mode_off()` 削除による連続適用化）
- `_clear_redact_rects` — メソッド（`pagefolio/redact_ops.py`・クリアボタン）
- `_on_mosaic_block_release` — メソッド（`pagefolio/redact_ops.py` or `pagefolio/ui_builder.py`・スライダー永続化）
- `_redact_rects` — 状態属性（list・複数矩形蓄積）
- `mosaic_block_var` / `mosaic_block_scale` — ウィジェット（`pagefolio/ui_builder.py`）
- `mosaic_block` — settings キー（`pagefolio_settings.json`）
- モザイク粒度/クリア関連 LANG キー（`pagefolio/lang.py` ja/en）
- `TestRedactPolish` — テストクラス（`tests/test_page_polish.py`）
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-v1-5-0/03-CONTEXT.md
@.planning/phases/03-v1-5-0/03-RESEARCH.md
@.planning/phases/03-v1-5-0/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 連続適用（D-05）＋モザイク粒度スライダー（D-06）</name>
  <files>pagefolio/redact_ops.py, pagefolio/ui_builder.py, pagefolio/lang.py, tests/test_page_polish.py</files>
  <read_first>
    - pagefolio/redact_ops.py（`_apply_page_edit`:72-133・後片付けの `_redact_mode_off()`:127＝この呼び出しのみ削除。`_toggle_redact_mode`:37-52 と `_redact_mode_off`:54-62 の相互排他は不変。`_mosaic_page`:165-187 の MOSAIC_BLOCK:179 参照箇所。`_apply_mosaic`:68-70）
    - pagefolio/page_ops.py（`_toggle_crop_mode`:237-251＝相互排他ペア・不変対象）
    - pagefolio/ui_builder.py（`thumb_zoom_scale`:200-211＝スライダー永続化の雛形。ページ編集セクション f3b:583-612＝スライダー/ボタン追加先）
    - pagefolio/constants.py（`MOSAIC_BLOCK = 16`:28＝既定値として温存）
    - pagefolio/settings.py（`_save_settings` の永続化フロー・`_SENSITIVE_KEYS` ガードは無関係だが settings 保存の型を確認）
    - tests/test_pdf_ops.py（`_mosaic_page` 既存テスト:1446＝`_mosaic_page(page, rect)` 呼び出し・block 引数はデフォルトで後方互換）
    - .planning/phases/03-v1-5-0/03-RESEARCH.md（Pattern 3・Pitfall 3）
  </read_first>
  <behavior>
    - 連続適用（D-05）: `_apply_page_edit` 末尾の `self._redact_mode_off()` 呼び出しのみ削除。適用後も `redact_mode` が True のまま維持され、明示トグル（`_toggle_redact_mode`）でのみ OFF。相互排他コード（`_toggle_redact_mode`/`_toggle_crop_mode` 内）は一切変更しない（Pitfall 3）。
    - モザイク粒度（D-06）: `_mosaic_page(page, rect, block=MOSAIC_BLOCK)` へ引数追加。`_apply_mosaic` は `block = int(self.settings.get("mosaic_block", MOSAIC_BLOCK))` を `_apply_page_edit` 経由で渡す。MOSAIC_BLOCK は constants.py で不変（既定値として温存）。スライダー変更値は settings["mosaic_block"] へ永続化。
  </behavior>
  <action>
    D-05: `pagefolio/redact_ops.py` の `_apply_page_edit`（72-133）の後片付けから `self._redact_mode_off()`（127 行）の呼び出しだけを削除する。`self.crop_rect = None`・`crop_info_var.set(...)`・`_invalidate_thumb_cache`/`_refresh_all`/`_set_status` は残す。`_toggle_redact_mode`/`_toggle_crop_mode`/`_redact_mode_off` の相互排他ロジックには一切触れない（Pitfall 3）。D-06: `_mosaic_page` 静的メソッド（165-187）へ `block=MOSAIC_BLOCK` 引数を追加し、内部の `MOSAIC_BLOCK` 参照（179）を `block` へ置換。`_apply_page_edit` に kind 別の block 伝搬を通す（例: `_apply_page_edit(self, kind, block=None)` とし mosaic 時に `_mosaic_page(page, rect, block=block or MOSAIC_BLOCK)`）。`_apply_mosaic` は `self._apply_page_edit("mosaic", block=int(self.settings.get("mosaic_block", MOSAIC_BLOCK)))` を呼ぶ。`pagefolio/ui_builder.py` の f3b セクション（583-612・モザイク適用ボタン近傍）へ `thumb_zoom_scale`（200-211）と同型のモザイク粒度スライダーを追加: `self.mosaic_block_var = tk.IntVar(value=self.settings.get("mosaic_block", MOSAIC_BLOCK))`、`ttk.Scale(f3b, from_=4, to=32, variable=..., orient="horizontal")`、`bind("<ButtonRelease-1>", self._on_mosaic_block_release)`（値域は Claude's Discretion・4〜32 は一例）。`_on_mosaic_block_release` を追加し `self.settings["mosaic_block"] = int(self.mosaic_block_var.get()); self._save_settings()`。スライダーラベルの LANG キー（例 `mosaic_block_label`）を ja/en 両辞書へ追加（parity 維持）。tests/test_page_polish.py に `TestRedactPolish` を追加し、(1) FakeApp で `crop_rect` をセットし `_apply_page_edit("redact")` 適用後に `self.redact_mode` が True のまま（連続適用・FakeApp に redact_mode 属性と最小スタブを用意）、(2) `_mosaic_page(page, rect, block=8)` と `block=24` で出力画像の粗さ（縮小後サイズ）が変わることを検証、(3) `_mosaic_page(page, rect)`（デフォルト）が従来どおり動く後方互換を検証する。
  </action>
  <verify>
    <automated>pytest tests/test_page_polish.py -k "redact_mode_persist or mosaic_block" -x -q && pytest tests/test_pdf_ops.py -k mosaic -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/redact_ops.py` の `_apply_page_edit` 本体に `_redact_mode_off()` 呼び出しが無い（連続適用・grep で確認）
    - `_mosaic_page` のシグネチャに `block` 引数があり、内部で `MOSAIC_BLOCK` 直接参照が `block` へ置換されている
    - `_apply_mosaic` が `settings.get("mosaic_block", MOSAIC_BLOCK)` を渡す
    - `pagefolio/ui_builder.py` にモザイク粒度スライダーと `_on_mosaic_block_release`（settings 永続化）が存在する
    - `MOSAIC_BLOCK` は `constants.py` で不変（書き換えていない）
    - `pytest tests/test_page_polish.py -k "redact_mode_persist or mosaic_block" -x` と既存 `pytest tests/test_pdf_ops.py -k mosaic -x` が exit 0（後方互換）
    - `ruff check pagefolio/redact_ops.py pagefolio/ui_builder.py pagefolio/lang.py` が clean・`pytest tests/test_lang_parity.py` が exit 0
  </acceptance_criteria>
  <done>黒塗り/モザイクが連続適用になり（相互排他は維持）、粒度スライダーで粗さを変えて永続化でき、既存モザイクテストは後方互換で通る。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 複数矩形の一括適用（D-07）＋回転座標対応の統合（D-08）</name>
  <files>pagefolio/redact_ops.py, pagefolio/page_ops.py, pagefolio/ui_builder.py, pagefolio/lang.py, tests/test_page_polish.py</files>
  <read_first>
    - pagefolio/redact_ops.py（`_apply_page_edit`:72-133＝複数矩形ループ化の起点。`_page_rect_from_rel`:135-157＝矩形ごとに再利用。`_redact_page`:159-163・`_mosaic_page`:165-187＝各矩形へ個別適用）
    - pagefolio/page_ops.py（`_derotate_rect`（03-03 で新設）＝表示→未回転変換。`_canvas_rect_to_pdf`:253-267。矩形ドラッグ `_crop_drag_start`:269-277/`_crop_drag_end`:331-334＝redact モードの矩形蓄積分岐を足す箇所。`crop_overlay_ids`/`crop_rect_id` オーバーレイ描画:304-322）
    - pagefolio/file_ops.py（`_save_undo` の `page_edit` 分岐:150-155＝対象ページの適用前 bytes を1回キャプチャ。`_restore_state` page_edit:359-367＝ページ bytes 差替で一括復元。複数矩形でも「_save_undo を1回→全矩形適用」で一括 undo が成立する根拠）
    - .planning/phases/03-v1-5-0/03-RESEARCH.md（Pattern 4・Pitfall 4＝_save_undo は必ずループ外で1回・Pitfall 2＝derotate は mediabox 相対化の前・Security Domain の情報漏えい防止）
  </read_first>
  <behavior>
    - 複数矩形蓄積（D-07）: redact モードのドラッグ完了ごとに矩形を `self._redact_rects` リストへ追加しオーバーレイ表示。「適用」で全矩形を一括処理、「クリア」で全削除。個別クリック選択/削除は実装しない。
    - 一括 undo（D-07/Pitfall 4）: `_apply_page_edit` は対象ページ集合に対し `_save_undo("page_edit", targets)` を全矩形ループの外側で必ず1回だけ呼ぶ。その後、各対象ページ×各矩形へ `_redact_page`/`_mosaic_page` を個別適用。1回の undo で全矩形がまとめて戻る。
    - 回転対応（D-08）: 各矩形の表示座標を `_derotate_rect(base_page, ...)` で未回転座標へ変換してから rel 化（既存 mediabox 相対化は温存・Pitfall 2）。黒塗り/モザイク/トリミングで「見たまま」を一貫。
    - 情報漏えい防止: 複数矩形でも各矩形について `apply_redactions` による実削除を個別実行し、一部矩形の下地が残らないこと（T-3-04）。
  </behavior>
  <action>
    複数矩形状態は `_toggle_redact_mode` 進入時に `self._redact_rects = []` で初期化する（app.py __init__ を触らない・lazy 初期化）。`pagefolio/page_ops.py` の `_crop_drag_end`（331-334）に redact モード分岐を追加: `self.redact_mode` かつ `self.crop_rect` が確定したとき、その矩形（canvas 座標タプル）を `self._redact_rects` へ append し、確定矩形を消さずに残す持続オーバーレイを描画する（複数矩形時は各矩形を実線アウトラインのみで描画＝stipple 省略・RESEARCH Open Question 2 推奨）。crop（トリミング）モードの単一矩形挙動は不変。`pagefolio/redact_ops.py` の `_apply_page_edit`（72-133）を複数矩形対応へ改修: 適用対象矩形集合を「`self._redact_rects`（非空ならそれ）＋現在の `crop_rect`」から構築。各矩形について `x0..y1 = self._canvas_rect_to_pdf(*r)` → `self._derotate_rect(self.doc[self.current_page], x0,y0,x1,y1)`（D-08）→ 現在ページ mediabox で rel 化、の順で相対座標リストを作る。`_save_undo("page_edit", targets=targets)` を**ループ外で1回だけ**呼ぶ（Pitfall 4）。`for i in targets:` の内側で `for rel in rels:` を回し `rect = self._page_rect_from_rel(page, rel)`（None はスキップ）→ kind により `_redact_page`/`_mosaic_page(block=...)` を個別適用。適用後は `self._redact_rects = []`・`self.crop_rect = None`・オーバーレイクリア（D-05 の連続適用のためモードは維持）。`_clear_redact_rects(self)` を追加（`self._redact_rects = []` とオーバーレイ全消去）。`pagefolio/ui_builder.py` の f3b（583-612）へ「クリア」ボタン `btn(f3b, self._t("btn_redact_clear"), self._clear_redact_rects, "Danger.TButton", needs_doc=True, edit_only=True)` を追加。`btn_redact_clear` を ja/en 両辞書へ追加（parity）。tests/test_page_polish.py の `TestRedactPolish` へ (1) 複数矩形（`_redact_rects` に2矩形セット）を `_apply_page_edit("redact")` で適用後、1回の `_undo` で全矩形が戻る（適用前ページ bytes 復元・`page.get_text`/`get_images` で確認）、(2) 回転90 ページで derotate 経由の適用位置が期待矩形に一致する（小 fitz.Document・`_page_rect_from_rel` 出力の座標検証）、(3) `_apply_page_edit` 本体で `_save_undo` が1回のみ（多重呼び出しなし）を検証するテストを追加する。
  </action>
  <verify>
    <automated>pytest tests/test_page_polish.py -k "multi_rect or redact_derotate" -x -q</automated>
    <human-check>アプリ起動 → 黒塗りモード ON → 複数箇所を順にドラッグ（各矩形が残る）→「適用」で一括黒塗り → 1回の Undo で全矩形が戻ることを確認。回転90 表示のページで矩形をドラッグ→適用し「見たままの位置」に黒塗りされることを目視確認（VALIDATION.md Manual-Only「回転ページでの位置一致・連続適用」）</human-check>
  </verify>
  <acceptance_criteria>
    - `pagefolio/redact_ops.py` の `_apply_page_edit` 本体で `_save_undo` 出現回数が1（`grep` でループ外1回のみ・Pitfall 4）
    - `_apply_page_edit` が各矩形について `_derotate_rect` を経由し `_page_rect_from_rel` で個別適用する
    - `_crop_drag_end` に redact モードの `_redact_rects` 蓄積分岐がある／`_clear_redact_rects` が存在する
    - `pagefolio/ui_builder.py` にクリアボタン（`_clear_redact_rects` command）が存在し、`btn_redact_clear` が ja/en 両辞書にある
    - `pytest tests/test_page_polish.py -k "multi_rect or redact_derotate" -x` が exit 0（複数矩形1回 undo・回転位置一致・_save_undo 単一呼び出しを検証）
    - `ruff check pagefolio/redact_ops.py pagefolio/page_ops.py pagefolio/ui_builder.py pagefolio/lang.py` が clean・`pytest tests/test_lang_parity.py` が exit 0
  </acceptance_criteria>
  <done>複数矩形を追加→一括適用でき1回の Undo で全戻り、回転ページでも見たまま適用され、各矩形の下地は個別に実削除される。テストがグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 矩形選択座標 → 破壊的 redaction（apply_redactions） | ユーザー選択矩形が下地コンテンツの実削除を引き起こす |
| モザイク粒度設定 → _mosaic_page のブロック演算 | settings 由来の block 値が縮小演算に流入する |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Information Disclosure | 複数矩形一括適用（`_apply_page_edit`） | high | mitigate | 各矩形について既存 `_redact_page`/`_mosaic_page`（`apply_redactions` で下地実削除）を個別に呼ぶ設計を維持し「一部矩形だけ下地が残る」ことを防ぐ（RESEARCH Security Domain）。1回の undo で全戻りするが復元は明示操作 |
| T-3-05 | Denial of Service | `_mosaic_page` の block 値 | low | mitigate | `max(1, w // block)` で 0 除算/極小回避。スライダー範囲 from_=4 で 0/負値を UI 側で排除 |
| T-3-SC | Tampering | パッケージインストール | low | accept | 本フェーズは新規パッケージを一切導入しない（RESEARCH.md Package Legitimacy Audit）。install タスクなし |
</threat_model>

<verification>
- `pytest tests/test_page_polish.py tests/test_pdf_ops.py tests/test_lang_parity.py -q` グリーン
- `pytest`（フルスイート）で既存テストに回帰なし（黒塗り/モザイク・undo・並列系）
- `ruff check . && ruff format --check .` clean
- 連続適用・複数矩形・回転位置一致の操作感は end-of-phase human-verify で確認
- 実装完了後、CLAUDE.md §既知の制限「矩形は未回転のページ座標系で適用」の記述を D-08 解消後の内容へ更新する（CONTEXT.md canonical_refs 指示）
</verification>

<success_criteria>
- 黒塗り/モザイクの棚卸し 4 項目（D-05 連続適用・D-06 粒度・D-07 複数矩形・D-08 回転座標）が反映され、改善前より操作しやすい（成功基準2）
- 複数矩形一括適用でも下地コンテンツが各矩形で実削除され情報漏えいがない
- MOSAIC_BLOCK は既定値として温存・LANG ja/en パリティ維持・既存テスト回帰なし
</success_criteria>

<output>
Create `.planning/phases/03-v1-5-0/03-04-SUMMARY.md` when done
</output>
