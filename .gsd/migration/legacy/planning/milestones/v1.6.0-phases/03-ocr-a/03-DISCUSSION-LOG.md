# Phase 3: 体感品質・回転プレビュー & OCR 堅牢性（プランA） - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-19
**Phase:** 3-ocr-a
**Areas discussed:** H5 実機検証の方針, M1 エラー UX の出し方, H1 回転即時反映の範囲, H2 キー秘匿監査の証跡

---

## H5 実機検証の方針（V16-QUAL-03）

### Q1: max_tokens クランプ・429 リトライの検証を実 API で叩くか？

| Option | Description | Selected |
|--------|-------------|----------|
| 実機相当の手順書中心（推奨） | クランプ/リトライは自動テストで担保し、実 API は手順書+チェックリストをユーザーが任意実行 | ✓ |
| 実 API で実際に叩く | 実 API キー保有・課金許容で実際に途切れ/429 を誘発し検証 | |
| 自動テストのみで担保 | 実 API 検証なし・手順書なし・テストグリーンで代替 | |

**User's choice:** 実機相当の手順書中心
**Notes:** —

### Q2: 検証手順書・結果記録の置き場所

| Option | Description | Selected |
|--------|-------------|----------|
| フェーズ内に検証手順書（推奨） | .planning/phases/03-ocr-a/ にチェックリスト形式で作成・GSD 検証フロー整合 | ✓ |
| 開発履歴.md に記録 | プロジェクト恒久ドキュメントに記載 | |
| テストの docstring/コメント | 独立文書を作らず最軽量 | |

**User's choice:** フェーズ内に検証手順書

### Q3: 追加する自動テストの範囲

| Option | Description | Selected |
|--------|-------------|----------|
| ギャップのみ埋める（推奨） | max_tokens クランプ実在確認＋未テスト分のみ追加・既テストと重複させない | ✓ |
| 包括的に棚卸しして補完 | 全振る舞いを網羅的に整理・補完 | |
| 新規テストは追加しない | 既存グリーンで十分 | |

**User's choice:** ギャップのみ埋める

---

## M1 エラー UX の出し方（V16-QUAL-04）

### Q1: 「状況+次アクション」の提示スタイル

| Option | Description | Selected |
|--------|-------------|----------|
| 既存のパネル内提示を強化（推奨） | OCRDialog 結果欄/進捗ラベルを踏襲・モーダル乱立回避 | ✓ |
| messagebox で能動通知 | 重要エラーをポップアップで能動通知 | |
| 両方を使い分け | ページ単位=パネル / 致命的=messagebox | |

**User's choice:** 既存のパネル内提示を強化

### Q2: トークン超過で応答途切れ（stop_reason/finishReason=max_tokens）の扱い

| Option | Description | Selected |
|--------|-------------|----------|
| 検出して専用文言+次アクション（推奨） | 途切れ検出＋「max_tokens を増やして再実行」案内・部分テキスト保持 | ✓ |
| 検出のみ（汎用注記） | 末尾に汎用の途切れ注記のみ | |
| 今回はスコープ外 | 途切れ検出は将来フェーズ | |

**User's choice:** 検出して専用文言+次アクション

### Q3: レート制限待機中の表示をどこまで磨くか

| Option | Description | Selected |
|--------|-------------|----------|
| 待ち時間を併記（推奨） | 「約 N 秒待機」（Retry-After/バックオフ・CAP クランプ済）を併記 | ✓ |
| 現状の文言で十分 | 「待機中（リトライ n/max）」のまま | |

**User's choice:** 待ち時間を併記

---

## H1 回転即時反映の範囲（V16-QUAL-01）

### Q1: 現行ビルドでプレビューは即時に回るか？

| Option | Description | Selected |
|--------|-------------|----------|
| 即時には回らない（バグあり） | 回転してもすぐ回らず、ページ切替/再読込で反映。原因調査と修正が中核 | ✓ |
| 即時に回る（検証中心） | 既に反映済み・回帰テストで担保 | |
| どちらか不確か（要確認） | researcher が実装トレースで判定 | |

**User's choice:** 即時には回らない（バグあり）
**Notes:** ユーザー実機確認による症状報告。

### Q2: 即時反映の対象範囲

| Option | Description | Selected |
|--------|-------------|----------|
| プレビュー+選択サムネイル両方（推奨） | 現在ページのプレビュー＋回転した選択全ページのサムネイルを即時 | ✓ |
| プレビューのみ即時 | 成功基準の文言どおりプレビューのみ | |

**User's choice:** プレビュー+選択サムネイル両方

### Q3: 修正の担保方法

| Option | Description | Selected |
|--------|-------------|----------|
| 検証可能な単位+手動UAT（推奨） | _render_preview_pixmap の回転反映を可能範囲でテスト＋見た目は手動UAT | ✓ |
| 手動 UAT のみ | GUI 即時反映は目視確認のみ | |
| You decide | researcher/planner 裁量（回帰担保は必須） | |

**User's choice:** 検証可能な単位+手動UAT

---

## H2 キー秘匿監査の証跡（V16-QUAL-02）

### Q1: 監査の再確認と証跡の残し方

| Option | Description | Selected |
|--------|-------------|----------|
| 回帰テスト+監査チェックリスト（推奨） | caplog/ソーススキャンの回帰テスト＋フェーズ内 3 経路チェックリスト文書 | ✓ |
| 回帰テストのみ | 自動化重視・独立文書なし | |
| 監査文書のみ | 手動確認・新規テストなし | |

**User's choice:** 回帰テスト+監査チェックリスト

### Q2: ソース埋め込み防止の実装

| Option | Description | Selected |
|--------|-------------|----------|
| 自動スキャンテスト（推奨） | ソース走査でキーパターン（sk-ant-/AIza/長 base64）不在を pytest アサート | ✓ |
| 監査チェックリストの手動確認 | 人間が grep して記録 | |
| You decide | 誤検知リスクと効果のバランスで裁量 | |

**User's choice:** 自動スキャンテスト

---

## Claude's Discretion

- 待機秒数の表示文言・桁丸め（LANG ja/en 規約に従う）
- モック 429/トークン超過統合ケースの要否最終判断（max_tokens クランプ実在確認後）
- caplog テストの関数/モジュール粒度（最低限 _save_settings + 全クラウドプロバイダ）
- ソーススキャンの検出パターン・除外（ダミーキー誤検知回避）の実装方式
- 検証手順書のファイル名・章立て（GSD 慣習）

## Deferred Ideas

- エラーの messagebox 能動通知（今回はパネル内強化を採用・将来検討余地）
- トークン超過時の自動 max_tokens 引き上げ再試行（今回は検出＋案内のみ）
- 実 API 検証の CI 自動化（課金・不安定のため不採用）
- OCR 出力の Markdown 整形・プロバイダ別プロンプト最適化（→ Phase 4 プランC）
