---
phase: 04-ai-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/md_render.py
  - tests/test_md_render.py
autonomous: true
requirements: [V16-AI-01]
must_haves:
  truths:
    - "parse_markdown が H1 行（'# '始まり）を line_kind 'md_h1' に分類する"
    - "parse_markdown が H2 行（'## '始まり）を line_kind 'md_h2' に分類する"
    - "parse_markdown が箇条書き行（'- '/'* '始まり）を line_kind 'md_bullet' に分類する"
    - "parse_markdown がコードフェンス（```）内の行を line_kind 'md_code' に分類し、フェンス内では見出し判定をしない"
    - "parse_markdown が通常段落を line_kind ''（空文字）に分類する"
    - "parse_markdown が **bold** をインライン span ('bold', 'md_bold') として抽出する"
    - "md_render は tkinter / fitz を一切 import しない純ロジック層である"
  artifacts:
    - path: "pagefolio/md_render.py"
      provides: "Markdown 文字列 → [(line_kind, [(span_text, inline_tag|None), ...]), ...] への変換純関数 parse_markdown"
      contains: "def parse_markdown"
      min_lines: 40
    - path: "tests/test_md_render.py"
      provides: "parse_markdown の行種別・インライン span unit テスト（Tk 非生成）"
      contains: "from pagefolio.md_render import parse_markdown"
  key_links:
    - from: "tests/test_md_render.py"
      to: "pagefolio/md_render.py"
      via: "from pagefolio.md_render import parse_markdown — 戻り値タプルを直接アサート"
      pattern: "from pagefolio\\.md_render import parse_markdown"
---

<objective>
V16-AI-01 の純ロジック層を新設する。OCR 結果の Markdown 文字列を「行種別 + インライン span」の構造データへ変換する Tk 非依存の純関数 `parse_markdown`（および内部ヘルパー `_split_inline`）を新規モジュール `pagefolio/md_render.py` に集約し、`tests/test_md_render.py` で網羅的に単体検証する。

Purpose: 描画ロジックを `ocr_dialog.py`（Tk 依存）にベタ書きするとテスト不能になり 582 件ベースラインに回帰を足せない（04-RESEARCH.md Pitfall 1）。`pagination.py` と同じ「純関数集約 + Tk 非生成 unit テスト」パターンに倣い、変換ロジックを 1 箇所に閉じ込める。本プランは Wave 2 の `ocr_dialog.py` 描画分岐（04-03）が import する契約を確定する。
Output: 新規 `pagefolio/md_render.py`（純関数 `parse_markdown` / 内部 `_split_inline`）・新規 `tests/test_md_render.py`。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-c/04-RESEARCH.md
@.planning/phases/04-ai-c/04-PATTERNS.md
@.planning/phases/04-ai-c/04-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
> 本プラン（04-01）が生成・確定するシンボル。後続プラン（04-03）が import する契約。

| 種別 | シンボル | 場所 | 契約 |
|------|----------|------|------|
| 新規ファイル | `pagefolio/md_render.py` | — | Tk/fitz 非依存純ロジック層 |
| 新規関数 | `parse_markdown(md: str)` | `md_render.py` | 戻り値 `list[tuple[str, list[tuple[str, str|None]]]]`。line_kind ∈ {`"md_h1"`,`"md_h2"`,`"md_bullet"`,`"md_code"`,`""`} |
| 新規内部関数 | `_split_inline(text: str)` | `md_render.py` | 戻り値 `list[tuple[str, str|None]]`。inline_tag ∈ {`"md_bold"`, `None`} |
| 新規ファイル | `tests/test_md_render.py` | — | parse_markdown の unit テスト |

新規 LANG キー: なし（本フェーズは rendered 固定・トグル UI なしのため UI 文言を追加しない／04-RESEARCH.md Open Question Q1 の最小実装方針）。
新規設定キー: なし。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: md_render.py の純関数 parse_markdown / _split_inline を新設</name>
  <files>pagefolio/md_render.py</files>
  <read_first>
    - .planning/phases/04-ai-c/04-RESEARCH.md（Code Examples 260-308 の parse_markdown/_split_inline シグネチャ、Security Domain 429 の ReDoS 回避方針）
    - .planning/phases/04-ai-c/04-PATTERNS.md（`pagefolio/md_render.py` 節 22-41: pagination.py の docstring 様式・例外型指定パターン）
    - pagefolio/pagination.py（アナログ: 1-17 ライセンスヘッダ+純ロジック層宣言 docstring、25-37 window_bounds の境界ケース docstring、133-137 clamp_page_size の `except (ValueError, TypeError)` 型指定）
    - CLAUDE.md（コーディング規約: 裸 except 禁止・`_` プレフィックス内部メソッド・新規 pip 依存ゼロ V14-D-01）
  </read_first>
  <behavior>
    - Test: `parse_markdown("# Title")[0][0] == "md_h1"`（H1 検出・'# ' プレフィックスは line_kind へ、本文からプレフィックス除去）
    - Test: `parse_markdown("## Sub")[0][0] == "md_h2"`（H2 検出）
    - Test: 箇条書き `"- item"` / `"* item"` → line_kind `"md_bullet"`、本文は `"• "` 始まりへ正規化
    - Test: コードフェンス `"```"` 行自体は出力に含めず、フェンス間の行は line_kind `"md_code"`、フェンス内では `"# "` を見出し化しない（in_code フラグ）
    - Test: 通常段落 `"hello"` → line_kind `""`
    - Test: `_split_inline("**bold** x")` が `("bold", "md_bold")` と `(" x", None)` を含む
    - Test: bold を含まない行 → `[(text, None)]`（span 1 個）
  </behavior>
  <action>
    新規ファイル `pagefolio/md_render.py` を作成する。冒頭 3 行は pagination.py と同一のライセンスヘッダ（`# PageFolio - PDF Page Organizer` / `# Copyright (c) 2026 mistyura` / `# Released under the MIT License`）。続くモジュール docstring に「OCR 結果 Markdown を (行種別, インライン span) へ変換する純ロジック層」「`tkinter` / `fitz` を一切 import しない」を明記（pagination.py:11 の作法）。import は標準 `re` のみ（V14-D-01 新規依存ゼロ）。

    モジュール定数として ReDoS 回避方針（非貪欲・文字クラス＝線形時間・量指定子のネスト不使用／04-RESEARCH.md:429）に従い `_BOLD = re.compile(r"\*\*(.+?)\*\*")` と `_CODE = re.compile(r"\`([^\`]+)\`")` を定義（_CODE は将来拡張用に定義のみでも可だが、本タスクでは少なくとも `_BOLD` を使用）。

    内部ヘルパー `_split_inline(text)` を実装: 1 行を `[(span_text, inline_tag|None), ...]` へ分解する純関数。`_BOLD.finditer` で `**…**` を `("...", "md_bold")` として抽出し、前後の非マッチ部は `(text, None)`。マッチが無ければ `[(text, None)]` を返す（空リストを返さない）。docstring に「`**bold**` のみ扱う（OCR Markdown の現実的サブセット・ネスト非対応）」を明記。

    純関数 `parse_markdown(md)` を実装: `md.splitlines()` で行走査し `in_code` フラグでコードフェンス（行頭 strip が ` ``` ` 始まり）をトグル。フェンス行自体は出力に含めない（continue）。判定優先順位は code > `"## "`(md_h2) > `"# "`(md_h1) > 箇条書き正規表現 `r"^\s*[-*]\s+"`(md_bullet・本文を `"• "` へ `re.sub`) > 通常段落（`""`）。見出しはプレフィックス除去後の本文を 1 span（`(body, None)`）として格納、箇条書き・通常段落は `_split_inline(body)` 適用。戻り値型 `list[tuple[str, list[tuple[str, str|None]]]]` と line_kind の取りうる値（`"md_h1"|"md_h2"|"md_bullet"|"md_code"|""`）を docstring へ明記（04-PATTERNS.md:30）。例外捕捉が必要な場合は `re.error`/`ValueError` を型指定（裸 except 禁止）。

    実装本体は 04-RESEARCH.md:261-308 のシグネチャ・ロジックをそのまま採用してよい。コードフェンス・量指定子のネストは導入しない（ReDoS 回避）。
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('pagefolio/md_render.py',encoding='utf-8').read()); print('OK')"</automated>
    <automated>python -c "import pagefolio.md_render as m; assert 'tkinter' not in m.__dict__ and 'fitz' not in m.__dict__; assert m.parse_markdown('# T')[0][0]=='md_h1'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ソース表明: `grep -c 'def parse_markdown' pagefolio/md_render.py` が 1 以上、`grep -c 'def _split_inline' pagefolio/md_render.py` が 1 以上
    - ソース表明: `pagefolio/md_render.py` に `import fitz` および `import tkinter` / `from tkinter` が一切現れない（純ロジック層／`grep -v '^#' pagefolio/md_render.py | grep -c 'import fitz'` が 0、同様に tkinter も 0）
    - 挙動表明: `python -c "from pagefolio.md_render import parse_markdown as p; assert p('## H')[0][0]=='md_h2'; assert p('- x')[0][0]=='md_bullet'; assert p('plain')[0][0]==''; print('OK')"` が `OK` を出力
    - 挙動表明: コードフェンス内で見出し化しない — `python -c "from pagefolio.md_render import parse_markdown as p; out=p('\`\`\`\n# nothead\n\`\`\`'); assert out[0][0]=='md_code'; print('OK')"` が `OK` を出力
    - CLI 出力: `ruff check pagefolio/md_render.py` がエラー 0 件で通過
  </acceptance_criteria>
  <done>
    `pagefolio/md_render.py` が存在し、`parse_markdown` が H1/H2/箇条書き/コード/通常段落を正しく分類し、`_split_inline` が `**bold**` を `md_bold` span として抽出する。tkinter/fitz を import しない。ruff 通過。
  </done>
</task>

<task type="auto">
  <name>Task 2: test_md_render.py で parse_markdown を unit 検証</name>
  <files>tests/test_md_render.py</files>
  <read_first>
    - tests/test_pagination.py（アナログ: Tk 非生成・`Test<FeatureName>` クラス + docstring で Req 明記・境界/不変条件のループ網羅・関数単位 import スタイル）
    - .planning/phases/04-ai-c/04-RESEARCH.md（最小テスト 310-322・Validation Architecture 398-416）
    - .planning/phases/04-ai-c/04-VALIDATION.md（Per-Task Verification Map: V16-AI-01 の automated command）
    - .planning/phases/04-ai-c/04-PATTERNS.md（`tests/test_md_render.py` 節 109-119）
    - pagefolio/md_render.py（Task 1 で確定した parse_markdown / _split_inline の戻り値型）
  </read_first>
  <action>
    新規ファイル `tests/test_md_render.py` を作成する。冒頭 3 行ライセンスヘッダ + モジュール docstring（「V16-AI-01: parse_markdown の行種別/インライン span 分類を検証。Tk 非生成」）。import は `from pagefolio.md_render import parse_markdown`（`_split_inline` も内部検証するなら追加 import 可）。`tests/**` は S101 除外のため `assert` 直書きでよい。`OCRDialog(...)` を一切インスタンス化しない（Pitfall 1 の Warning sign）。

    `class TestParseMarkdown:`（docstring に対応 Req `V16-AI-01` を明記・test_pagination.py:27 の作法）を作り、以下を網羅:
    - `test_h1_detected` / `test_h2_detected`: 見出し検出と本文プレフィックス除去（`parse_markdown("# Title")[0] == ("md_h1", [("Title", None)])` 等）
    - `test_bullet_detected`: `"- item"` / `"* item"` の双方で line_kind `"md_bullet"`、本文が `"• "` 始まり
    - `test_codeblock_no_heading`: コードフェンス内の `"# "` 行が `md_h1` ではなく `md_code` になること（in_code フラグ・Pitfall 2 の構造的担保）
    - `test_plain_paragraph`: 通常段落の line_kind が `""`
    - `test_bold_inline_span`: `"- **bold** item"` のパース結果に `("bold", "md_bold")` span が含まれること
    - `test_no_bold_single_span`: bold を含まない行は span 1 個（`[(text, None)]`）

    可能なら行種別をループ網羅する不変条件テスト（test_pagination.py:58-65 の `test_invariant_*` 作法）も 1 つ追加する。
  </action>
  <verify>
    <automated>python -m pytest tests/test_md_render.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ソース表明: `grep -c 'from pagefolio.md_render import parse_markdown' tests/test_md_render.py` が 1 以上
    - ソース表明: `tests/test_md_render.py` に `OCRDialog`・`tk.Tk(`・`tkinter` が一切現れない（`grep -c 'OCRDialog\|tk.Tk(\|import tkinter' tests/test_md_render.py` が 0）
    - テストコマンド: `python -m pytest tests/test_md_render.py -q` が全件 passed（少なくとも H1/H2/bullet/code-no-heading/plain/bold-span の 6 ケースを含む）
    - CLI 出力: `ruff check tests/test_md_render.py` がエラー 0 件で通過
    - 回帰: `python -m pytest -q` がフルスイート緑（582 件ベースライン + 本プランの新規テスト分のみ増加・既存に赤を出さない）
  </acceptance_criteria>
  <done>
    `tests/test_md_render.py` が parse_markdown の 6 行種別/span ケースを検証し全件パス。Tk を一切生成しない。フルスイート緑。ruff 通過。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部 LLM 出力 → `parse_markdown`（純関数） | OCR 結果（外部 LLM 由来文字列）が `re` パーサへ入力される。ただし本プランは UI へ描画せず構造データを返すのみ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-DOS | Denial of Service | `md_render.parse_markdown` の `re` パターン | mitigate | 正規表現は非貪欲 `.+?` ＋文字クラス `[^\`]+` のみ使用し量指定子のネストを禁止＝線形時間（ReDoS 回避・04-RESEARCH.md:429/437）。行単位処理で巨大入力でも線形 |
| T-04-IDISC | Information Disclosure | OCR 本文のログ露出 | accept | `md_render` は logger を持たず本文をログ出力しない。純関数は副作用なし（既存規約踏襲・新規露出経路を増やさない） |
</threat_model>

<verification>
- `python -m pytest tests/test_md_render.py -x -q` 全件 passed
- `python -m pytest -q` フルスイート緑（582 件ベースライン + 新規テスト分・既存に赤なし）
- `ruff check . && ruff format .` 通過
- `pagefolio/md_render.py` に tkinter/fitz import が無い（純ロジック層）
</verification>

<success_criteria>
- `parse_markdown` が H1/H2/箇条書き/コードフェンス/通常段落の 5 行種別を正しく分類する（V16-AI-01 の構造データ契約確定）
- `_split_inline` が `**bold**` を `md_bold` span として抽出する
- 全変換が Tk/fitz 非依存純関数で、`test_md_render.py` が Tk 非生成で検証可能
- 04-03 が import する `parse_markdown` の戻り値契約が確定する
</success_criteria>

<output>
Create `.planning/phases/04-ai-c/04-01-SUMMARY.md` when done
</output>
