---
phase: 05-claude-provider-ui
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - pagefolio/dialogs/llm_config.py
  - pagefolio/ui_builder.py
autonomous: false
requirements: [OCR-UI-01, OCR-UI-02, OCR-UI-04, OCR-API-03]
must_haves:
  truths:
    - "SettingsDialog（LLMConfigDialog）でプロバイダ（off/lmstudio/claude）を選択でき、選択が ocr_provider に保存される（成功基準4・OCR-UI-01）"
    - "プロバイダ選択に応じて下位欄（LM Studio URL欄・モデル・effort/temperature）が切り替わる（OCR-UI-04・D-07/D-17）"
    - "モデル更新ボタンで該当プロバイダの list_models を取得し選択肢へ反映、キー未設定でも静的推奨リストが出る（成功基準4・OCR-API-03・D-08）"
    - "effort 対応モデル選択時は effort 欄、非対応時は temperature 欄を表示する（OCR-UI-04・D-17）"
    - "ocr_provider が off のとき OCR ボタンが disabled 化される（成功基準6・OCR-UI-02・D-09）"
  artifacts:
    - path: "pagefolio/dialogs/llm_config.py"
      provides: "provider ドロップダウン・欄切替・モデル更新（claude 対応）"
      contains: "provider_var"
    - path: "pagefolio/ui_builder.py"
      provides: "off 時の OCR ボタン無効化（_ocr_buttons 管理）"
      contains: "_ocr_buttons"
  key_links:
    - from: "LLMConfigDialog.provider_combo"
      to: "_on_provider_change"
      via: "<<ComboboxSelected>> で欄の pack/pack_forget 切替"
      pattern: "_on_provider_change"
    - from: "_update_ocr_buttons_state"
      to: "self._ocr_buttons"
      via: "ocr_provider == off で disabled"
      pattern: "_ocr_buttons"
---

<objective>
プロバイダ選択 UI を `pagefolio/dialogs/llm_config.py`（LLMConfigDialog）に実装し、`pagefolio/ui_builder.py` に off 時の OCR ボタン無効化を追加する。provider ドロップダウン（off/lmstudio/claude）・プロバイダ別の下位欄切替・モデル更新ボタン（claude は ClaudeProvider.list_models・キー未設定時は静的リスト）・effort/temperature 欄の動的切替を行う。off 選択時は OCR ボタンを disabled にして外部送信・課金を防ぐ。

すべての記述・docstring・コメント・コミットメッセージは日本語で書くこと（変数名・関数名・コマンドは英語のまま）。

Purpose: 成功基準4（プロバイダ選択でモデル一覧が選択肢に反映）・6（off で OCR ボタン無効化）と OCR-UI-01/02/04・OCR-API-03 を満たす。UI 構築のためビジュアル確認が必要なので checkpoint:human-verify を含む（autonomous: false）。
Output: provider DD・欄切替ロジック・claude 対応モデル更新・effort 切替・OCR ボタン無効化。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-claude-provider-ui/05-01-SUMMARY.md
@.planning/phases/05-claude-provider-ui/05-02-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces（このプランで新規作成するシンボル）

- `LLMConfigDialog.provider_var` / `provider_combo` — `pagefolio/dialogs/llm_config.py`。provider ドロップダウン（values=["off","lmstudio","claude"]・state=readonly）。
- `LLMConfigDialog._on_provider_change(event=None)` — provider 変更時に下位欄を pack/pack_forget で切替。
- `LLMConfigDialog._on_model_change(event=None)` — モデル変更時に effort/temperature 欄を切替（D-17）。
- `LLMConfigDialog.claude_model_var` / `effort_var` — claude 用モデル・effort（low/medium/high/xhigh/max）の Var。
- `LLMConfigDialog._model_supports_effort(model)` — effort 対応判定（ClaudeProvider.EFFORT_MODELS / プレフィックス判定を流用）。
- `LLMConfigDialog` の各欄フレーム参照: `url_section_frame` / `effort_frame` / `temperature_frame`（pack_forget 対象）。
- モデル更新ボタンの claude 対応（`_refresh_models` がプロバイダ別に list_models を呼ぶ）。
- `_apply()` の保存拡張: `ocr_provider` / `claude_model` / `ocr_effort` を llm_settings に格納（**api_key は格納しない**）。
- `PDFEditorApp._ocr_buttons: list` — `pagefolio/ui_builder.py` で OCR ボタンを集める新規リスト。
- `PDFEditorApp._update_ocr_buttons_state()` — `pagefolio/ui_builder.py`（または app に置く・実装者裁量だが ui_builder の Mixin 内が自然）。off で disabled。
</artifacts_produced>

<read_first>
**触る前に必ず読むこと:**
- `pagefolio/dialogs/llm_config.py` — 変更対象。`__init__`/`_build`（URL/モデル/scale/timeout/max_tokens/temperature/concurrency 各行の Var + Spinbox パターン）・`_fetch_models`（LMStudioProvider.list_models 呼び出し）・`_set_lm_status`（ステータス表示・色分け）・`_apply`（クランプ保存パターン）。
- `.planning/phases/05-claude-provider-ui/05-PATTERNS.md` §`pagefolio/dialogs/llm_config.py` — provider DD・_on_provider_change の pack/pack_forget・effort 切替・モデル更新ステータスのパターン抜粋。
- `pagefolio/ocr_providers.py` — 05-01 の `ClaudeProvider`（list_models のキー未設定フォールバック・RECOMMENDED_MODELS・EFFORT_MODELS・_supports_effort）。
- `pagefolio/ui_builder.py` — 変更対象。`_build_tools`（行 344・`btn` ヘルパーの needs_doc/edit_only・`_doc_buttons` 管理）・OCR ボタン生成箇所（行 537-550・`f_ocr` セクション）。`_update_doc_buttons_state`（app.py 行 119）の構造をアナログにする。
- `pagefolio/lang.py` — 05-02 で追加した `ocr_provider_label` / `ocr_effort_label` / `ocr_model_refresh` / `ocr_provider_off_hint`。
- `pagefolio/constants.py` — `C` テーマ辞書（色は C["KEY"] 経由・ハードコード禁止）。
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: ui_builder.py に OCR ボタン無効化（_ocr_buttons・_update_ocr_buttons_state）を実装</name>
  <files>pagefolio/ui_builder.py</files>
  <read_first>
    - pagefolio/ui_builder.py（_build_tools の btn ヘルパー・f_ocr セクション 行 537-550・_doc_buttons の append パターン）
    - pagefolio/app.py（_update_doc_buttons_state 行 119-126 のアナログ・settings 参照）
    - pagefolio/lang.py（ocr_provider_off_hint・05-02 追加済み）
  </read_first>
  <action>
    `pagefolio/ui_builder.py` の `_build_tools` で `self._ocr_buttons = []` を初期化し、`f_ocr` セクションで生成する 2 つの OCR ボタン（`btn_ocr_current` / `btn_ocr_selected`）を `self._ocr_buttons` に append する（既存 `_doc_buttons` の append と同様）。
    `_update_ocr_buttons_state()` を追加する（UIBuilderMixin か app のどちらでもよいが、`_update_doc_buttons_state` のアナログとして app.py 側に置くのが自然・実装者裁量）。`is_ocr_on = self.settings.get("ocr_provider", "off") != "off"` で `state = ["!disabled"] if (is_ocr_on and self.doc) else ["disabled"]` とし `self._ocr_buttons` に適用する（off では doc があっても disabled・成功基準6・D-09）。
    `_build_tools` 末尾の `_update_doc_buttons_state()` 呼び出しの後に `_update_ocr_buttons_state()` を呼ぶ。設定変更後（`_apply_settings`/`_update_doc_buttons_state` が呼ばれる経路・app.py 行 342-373）でも OCR ボタン状態が更新されるよう、doc 状態更新箇所で `_update_ocr_buttons_state()` も呼ぶ（getattr で属性未定義に備える）。
    色・フォントは C[...] と self._font(...) を使う。新規ハードコード色禁止。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check pagefolio/ui_builder.py ; python -c "import ast; ast.parse(open('pagefolio/ui_builder.py', encoding='utf-8').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - source: `pagefolio/ui_builder.py` に `self._ocr_buttons` の初期化と 2 つの OCR ボタンの append が存在する
    - source: `_update_ocr_buttons_state` 相当のメソッドが存在し `ocr_provider` == "off" で disabled にする分岐を含む
    - test: `ruff check pagefolio/ui_builder.py` が exit 0
    - test: ast.parse が "OK" を出力（構文確認）
  </acceptance_criteria>
  <done>off 時に OCR ボタンが disabled になる仕組みが実装され、外部送信・課金を防ぐ（成功基準6・D-09）。</done>
</task>

<task type="auto">
  <name>Task 2: llm_config.py に provider DD・欄切替・claude モデル更新・effort 切替を実装</name>
  <files>pagefolio/dialogs/llm_config.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config.py（_build の各行 Var+Spinbox・_fetch_models・_set_lm_status・_apply クランプ保存）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §llm_config.py（provider DD・_on_provider_change・effort 切替）
    - pagefolio/ocr_providers.py（ClaudeProvider.list_models / EFFORT_MODELS / RECOMMENDED_MODELS / _supports_effort）
    - pagefolio/lang.py（ocr_provider_label / ocr_effort_label / ocr_model_refresh・05-02 追加済み）
  </read_first>
  <action>
    `_build` の先頭（見出しの直後）に provider 行を追加する。`self.provider_var = tk.StringVar(value=self.current_settings.get("ocr_provider", "off"))`・`self.provider_combo = ttk.Combobox(..., textvariable=self.provider_var, values=["off","lmstudio","claude"], state="readonly", ...)` を作り `<<ComboboxSelected>>` に `self._on_provider_change` をバインドする（D-07）。**values は静的リスト。コードに `# Phase 6: gemini / Phase 7: tesseract を追加予定` のコメントを付記し、成功基準4 の全5プロバイダ列挙は後続フェーズで追加される設計であることを明示する。**
    既存の LM Studio 固有欄（URL 行・既存モデル行・接続テスト/モデル取得ボタン行）を 1 つの親フレーム `self.url_section_frame` にまとめ、lmstudio 選択時のみ pack する。claude 選択時は `self.claude_model_var`（既定 `claude_model`）の Combobox + モデル更新ボタンを表示する `self.claude_section_frame` を pack する。
    effort 欄 `self.effort_frame`（`self.effort_var` の Combobox values=["low","medium","high","xhigh","max"]・ラベル `ocr_effort_label`）と既存 temperature 行を `self.temperature_frame` にまとめ、`_on_model_change` で切替える。`_model_supports_effort(model)` は ClaudeProvider.EFFORT_MODELS 参照またはプレフィックス判定（opus/sonnet かつ not haiku）で判定する（D-16/D-17）。
    `_on_provider_change(self, event=None)`: provider に応じて url_section_frame / claude_section_frame を pack/pack_forget。claude のとき選択中 claude_model で effort/temperature を切替（`_on_model_change` を呼ぶ）。lmstudio/off では effort 欄を隠し temperature 欄を出す（従来挙動）。
    モデル更新: claude のとき `ClaudeProvider(api_key=os.environ.get("ANTHROPIC_API_KEY",""), model="").list_models()` を呼ぶ（キー未設定でも RECOMMENDED_MODELS が返る・例外時は静的リストへフォールバックし `_set_lm_status` で「キー未設定のため静的リスト表示中」を表示・D-08）。lmstudio は既存 `_fetch_models` を流用。**api_key は settings に入れない・env 読み取りのみ**（D-01/D-05）。
    `_apply` を拡張し、`llm_settings["ocr_provider"] = self.provider_var.get()`・`llm_settings["claude_model"] = self.claude_model_var.get().strip() or "claude-sonnet-4-6"`・`llm_settings["ocr_effort"] = self.effort_var.get()`（許可値 low/medium/high/xhigh/max に検証・不正値は "low"）を格納する。**api_key 系キーは絶対に llm_settings に入れない**（成功基準1）。
    全ウィジェットの色は C[...]・フォントは self._font(...)。`<action>` 内に完全なコード本体は書かない。read_first の PATTERNS 抜粋を参照すること。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check pagefolio/dialogs/llm_config.py ; python -c "import ast; ast.parse(open('pagefolio/dialogs/llm_config.py', encoding='utf-8').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - source: `pagefolio/dialogs/llm_config.py` に `provider_var`・`provider_combo`・`_on_provider_change`・`effort_var`・`_model_supports_effort` が存在する
    - source: `_apply` が `ocr_provider` / `claude_model` / `ocr_effort` を llm_settings に格納し、api_key 系キーを格納しない（grep `api_key` が _apply 周辺で 0 件・成功基準1）
    - source: claude モデル更新が `ClaudeProvider` の list_models を使い、`os.environ` 読み取りのみでキーを settings へ書かない
    - test: `ruff check pagefolio/dialogs/llm_config.py` が exit 0・ast.parse が "OK"
  </acceptance_criteria>
  <done>provider 選択・欄切替・claude モデル更新（キー未設定で静的リスト）・effort/temperature 切替が実装され、OCR-UI-01/04・OCR-API-03 の UI が揃う。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    LLMConfigDialog にプロバイダ選択ドロップダウン（off/lmstudio/claude）・プロバイダ別欄切替・claude モデル更新ボタン・effort/temperature 切替を実装。ui_builder に off 時の OCR ボタン無効化を実装。`ruff check .` と `python -m pytest tests/ -q` は自動で通過済み。
  </what-built>
  <how-to-verify>
    1. `cd C:\Users\shdwf\work\project\PageFolio; python pagefolio.py` でアプリを起動する。
    2. 設定 → LLM 設定ダイアログを開く。プロバイダのドロップダウンに off / lmstudio / claude が表示されることを確認。
    3. 「claude」を選択 → LM Studio の URL 欄が消え、claude モデル欄が出ることを確認（D-07）。
    4. claude モデルで「opus」または「sonnet」系を選ぶと effort 欄（low〜max）が表示され、「haiku」系を選ぶと temperature 欄に切り替わることを確認（OCR-UI-04・D-17）。
    5. 「モデル更新」ボタンを押す。ANTHROPIC_API_KEY 未設定でもモデル候補（haiku/sonnet/opus）が表示され、ステータスに「静的リスト表示中」等が出ることを確認（成功基準4・D-08）。
    6. プロバイダを「off」にして適用 → メイン画面の OCR ボタン（ページ OCR / 選択 OCR）が PDF を開いていても disabled（押せない）ことを確認（成功基準6・D-09）。
    7. プロバイダを「lmstudio」に戻すと従来どおり URL/モデル欄が出て OCR ボタンが有効になることを確認（後方互換）。
  </how-to-verify>
  <resume-signal>問題なければ「approved」、不具合があれば具体的な症状を記載してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLMConfigDialog 入力 → settings（_apply 保存） | UI 入力値が settings 経由で永続化される境界（キー混入リスク） |
| モデル更新ボタン → 環境変数 / Anthropic API | キー読み取りと外部 API 呼び出しの境界 |
| ocr_provider 設定 → OCR ボタン状態 | off で外部送信を遮断する境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Information Disclosure | LLMConfigDialog._apply の保存 | mitigate | api_key 系キーを llm_settings に一切入れない（成功基準1）。設定ダイアログにキー入力欄を置かない（キー入力は実行時ダイアログのみ・D-03） |
| T-05-13 | Information Disclosure | モデル更新の env 読み取り | mitigate | os.environ.get での読み取りのみ・settings へ書かない（D-01/D-05）。キー値を _set_lm_status に出さない（D-04） |
| T-05-14 | Tampering / Cost | off 時の意図しないクラウド送信 | mitigate | off で OCR ボタンを disabled 化し外部送信・課金をゼロにする（成功基準6・OCR-UI-02・D-09） |
| T-05-15 | Spoofing | effort 非対応モデルへの effort 送信 | mitigate | UI が effort 対応モデル時のみ effort 欄を出す + Provider 側でも _supports_effort 判定（多層防御・OCR-UI-04・成功基準7） |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本プランは新規 pip 依存ゼロ（標準ライブラリ + 既存 ttk）。パッケージインストールなし |
</threat_model>

<verification>
- 成功基準4（プロバイダ選択でモデル一覧反映・キー未設定でも静的リスト）が provider DD + claude モデル更新で担保される（human-verify 手順5）。
- 成功基準6（off で OCR ボタン無効化・外部送信ゼロ）が _ocr_buttons + _update_ocr_buttons_state で担保される（human-verify 手順6）。
- OCR-UI-04（effort/temperature のモデル別防御）が UI 切替 + Provider 判定の多層で担保される（human-verify 手順4）。
- OCR-UI-01（プロバイダ選択）が provider_combo + _apply 保存で担保される。
</verification>

<success_criteria>
- LLMConfigDialog で off/lmstudio/claude を選択でき ocr_provider に保存される。
- プロバイダ・モデルに応じて下位欄（URL/モデル/effort/temperature）が切り替わる。
- モデル更新ボタンが claude の list_models を呼び、キー未設定でも静的推奨リストが出る。
- off 時に OCR ボタンが disabled になる（成功基準6）。
- `ruff check .` と `pytest tests/` が exit 0。human-verify が approved。
</success_criteria>

<output>
Create `.planning/phases/05-claude-provider-ui/05-04-SUMMARY.md` when done
</output>
