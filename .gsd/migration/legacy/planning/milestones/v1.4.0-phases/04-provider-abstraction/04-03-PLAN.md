---
phase: 04-provider-abstraction
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - pagefolio/ocr_dialog.py
  - pagefolio/lang.py
  - pagefolio/settings.py
autonomous: true
requirements:
  - OCR-PROV-02
  - OCR-PERF-01
user_setup: []

must_haves:
  truths:
    - "OCR ワーカースレッド関数（_worker）内に fitz.Document / get_pixmap / self.doc[ の直接呼び出しが一切存在しない（成功基準3・D-03）"
    - "レンダリング（page_to_png_b64）と has_embedded_text 判定がメインスレッド側（after() 小分け）で実行される（D-01/D-03/D-05）"
    - "テキスト埋め込みページは Vision API（provider.ocr_image）を呼ばず page.get_text() の結果を結果辞書へ投入し、UI にスキップが明示される（成功基準2・D-07/D-08）"
    - "OCRDialog が _worker のフェーズ2で run_parallel(provider, ...) を使い、LM Studio の OCR 挙動・UI 操作が v1.3.0 と一致する（成功基準1）"
    - "スキップ通知の日英文言が lang.py に追加され、ocr_provider のデフォルトが settings に追加される（D-09）"
  artifacts:
    - path: "pagefolio/ocr_dialog.py"
      provides: "スレッド境界リファクタ済み OCRDialog（provider 受け取り・メインスレッドレンダリング・スキップ統合）"
      contains: "run_parallel"
    - path: "pagefolio/lang.py"
      provides: "スキップ通知文言 ocr_text_skip_notice（日英）"
      contains: "ocr_text_skip_notice"
    - path: "pagefolio/settings.py"
      provides: "ocr_provider デフォルト値"
      contains: "ocr_provider"
  key_links:
    - from: "pagefolio/ocr_dialog.py _worker"
      to: "pagefolio.ocr.run_parallel"
      via: "フェーズ2 の API 並列送信を run_parallel(provider, ...) に置換"
      pattern: "run_parallel\\("
    - from: "pagefolio/ocr_dialog.py メインスレッド側"
      to: "pagefolio.ocr.has_embedded_text / page_to_png_b64"
      via: "レンダリング前にメインスレッドで埋め込み判定 + レンダリング"
      pattern: "has_embedded_text"
    - from: "pagefolio/ocr_dialog.py"
      to: "OCRProvider (provider 引数)"
      via: "__init__ で provider を受け取り list_models / run_parallel に使う"
      pattern: "self\\.provider"
---

<objective>
`pagefolio/ocr_dialog.py` の `_worker` のスレッド境界を再構成し、成功基準3「ワーカースレッド内に fitz.Document / get_pixmap() の直接呼び出しが一切存在しない」を必達する（D-03）。fitz レンダリング（`page_to_png_b64`）と埋め込みテキスト判定（`has_embedded_text`）をメインスレッド側へ移し（D-01: 事前レンダリング最小構成・`after()` 小分け）、ワーカースレッドは API 呼び出し（`run_parallel`）のみを担う。テキスト埋め込みページは Vision API を呼ばず `page.get_text()` の結果を結果辞書へ統合し、UI にスキップを明示する（成功基準2・D-07/D-08）。あわせてスキップ通知の日英文言（D-09）と `settings` の `ocr_provider` デフォルトを追加する。

Purpose: Phase 4 の3つの成功基準（1 後方互換 / 2 埋め込みスキップ / 3 スレッド境界）を UI 層で結実させる。Plan 01/02 で用意した `OCRProvider` / `run_parallel` / `has_embedded_text` / `build_provider` を OCRDialog に結線する（最終ワイヤリング）。
Output: 改修後 `pagefolio/ocr_dialog.py` / `pagefolio/lang.py` / `pagefolio/settings.py`。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-abstraction/04-CONTEXT.md
@.planning/phases/04-provider-abstraction/04-PATTERNS.md
@.planning/phases/04-provider-abstraction/04-01-SUMMARY.md
@.planning/phases/04-provider-abstraction/04-02-SUMMARY.md
@CLAUDE.md
@pagefolio/ocr_dialog.py
@pagefolio/lang.py
@pagefolio/settings.py
</context>

## Artifacts this plan produces / consumes

**Consumes:** `OCRProvider`/`LMStudioProvider`（Plan 01）、`run_parallel`/`has_embedded_text`/`build_provider`/`page_to_png_b64`（Plan 02）。

**Produces:**
- 改修後 `OCRDialog`（`provider` 引数・メインスレッドレンダリング/判定・スキップ統合・`run_parallel` 結線）— `pagefolio/ocr_dialog.py`
- 追加 lang キー `ocr_text_skip_notice`（日/英）他必要最小文言 — `pagefolio/lang.py`
- 追加 settings デフォルト `ocr_provider: "off"` — `pagefolio/settings.py`

## OCRDialog.__init__ 改修後の確定シグネチャ（フェーズ4の単一情報源・key_links）

> 04-02 と 04-03 は OCRDialog の改修後シグネチャを「相互参照」で済ませず、本表を唯一の確定仕様として参照すること（key_links_planned warning 対応）。04-02 の `_start_ocr`（呼び出し側）と 04-03 の `__init__`（定義側）の双方が、本表どおりの引数で一致しなければならない。

改修後の確定シグネチャ（既存引数を全保持し、`provider` を `concurrency` の後・`lang` の前に追加。`provider` のデフォルトは `None`）:

```
OCRDialog(
    parent,
    app,
    doc,
    page_indices,
    url,
    model,
    preset,
    scale,
    timeout,
    max_tokens=-1,
    temperature=0.1,
    concurrency=DEFAULT_OCR_CONCURRENCY,
    provider=None,        # ← 04-03 で新規追加（OCRProvider インスタンス）
    lang="ja",
    font_func=None,
)
```

| 引数 | 型 | 由来 | 用途 |
|------|----|------|------|
| `parent` | tk ウィジェット | 既存 | Toplevel 親 |
| `app` | PDFEditorApp | 既存 | アプリ参照 |
| `doc` | fitz.Document | 既存 | レンダリング元（**メインスレッド側でのみ触る**・D-03） |
| `page_indices` | list[int] | 既存 | OCR 対象ページ |
| `url` | str | 既存 | LM Studio 表示用（UI の URL 欄初期値・後方互換で保持） |
| `model` | str | 既存 | LM Studio 表示用（UI のモデル欄初期値・後方互換で保持） |
| `preset` | str | 既存 | プロンプトプリセット |
| `scale` | float | 既存 | レンダリング倍率 |
| `timeout` | int | 既存 | タイムアウト秒（UI 表示用） |
| `max_tokens` | int | 既存（=-1） | UI 表示用 |
| `temperature` | float | 既存（=0.1） | UI 表示用 |
| `concurrency` | int | 既存 | 並列度（`provider.max_concurrency` でクランプ） |
| `provider` | OCRProvider \| None | **04-03 新規** | `list_models()` / `run_parallel(provider, ...)` に使用。`None` の場合のフォールバックは 04-03 Task 2 の action 参照 |
| `lang` | str | 既存 | 言語 |
| `font_func` | callable \| None | 既存 | フォントヘルパ |

**整合ルール（両プラン必達）:**
- 04-02 Task 2 の `_start_ocr` は `provider = build_provider(self.settings)` の結果を **上表の `provider=provider` キーワード引数**で渡す。既存の `url`/`model`/`preset`/`scale`/`timeout`/`max_tokens`/`temperature`/`concurrency`/`lang`/`font_func` も従来どおり渡す（引数の削除・改名はしない）。
- 04-03 Task 2 の `__init__` は上表の順序・デフォルト値で `provider=None` を追加し、`self.provider = provider` を保持する。`url`/`model` 等は LM Studio 表示用に維持する。

<tasks>

<task type="auto">
  <name>Task 1: settings に ocr_provider デフォルトを追加し lang にスキップ通知文言を追加する</name>
  <files>pagefolio/settings.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/settings.py:29-55（_load_settings の defaults dict — 既存 OCR デフォルト値の並び）
    - pagefolio/lang.py:248-272（ja の OCR 文言キー群 — 追記位置とフォーマット）
    - pagefolio/lang.py:578-585（en の OCR 文言キー群 — 対応する英語キー）
    - .planning/phases/04-provider-abstraction/04-CONTEXT.md（D-09 スキップ通知の日英文言を最小追加 / V14-D-03 既定 ocr_provider "off"）
    - .planning/phases/04-provider-abstraction/04-PATTERNS.md:320-402（settings/lang の追記パターンと最小キー一覧）
    - .planning/research/ARCHITECTURE.md:316-338（settings/lang に追加すべき値・キー）
  </read_first>
  <action>
    `pagefolio/settings.py` の `_load_settings` の `defaults` dict（`pagefolio/settings.py:31-44`）に `"ocr_provider": "off"` を追加する（V14-D-03 安全デフォルト。既存キーの `data.setdefault(k, v)` ループにより既存ユーザー設定にも後方互換で注入される）。**Phase 4 では `ocr_provider` の値による UI 切替・OCR ボタン無効化は実装しない**（Phase 5 の OCR-UI 群）。本タスクはデフォルト値の追加のみ。
    `pagefolio/lang.py` の `LANG["ja"]` の OCR 文言群（`pagefolio/lang.py:258-265` 付近）に以下を最小追加する（D-09）:
      - `"ocr_text_skip_notice"`: `"p.{page}: テキスト埋め込み済みのためスキップしました"`
    `LANG["en"]` の対応位置（`pagefolio/lang.py:578-585` 付近）に:
      - `"ocr_text_skip_notice"`: `"p.{page}: Skipped (embedded text detected)"`
    スキップ結果を結果領域で由来区別表示するために必要なら、進行表示用キー（例 `ocr_progress_skip`: `"埋め込みテキスト抽出 ({done}/{total}) — p.{page}"` / `"Embedded text ({done}/{total}) — p.{page}"`）を追加してよい（Claude's Discretion・D-08 文言詳細は裁量）。プロバイダ名・コスト警告等の本格文言は Phase 7 のため追加しない。
    日英両辞書に同一キーが揃っていることを確認する（キー欠落は KeyError を招く）。
  </action>
  <verify>
    <automated>python -c "from pagefolio.constants import LANG; from pagefolio.settings import _load_settings; assert 'ocr_text_skip_notice' in LANG['ja'] and 'ocr_text_skip_notice' in LANG['en']; assert LANG['ja']['ocr_text_skip_notice'].format(page=3); d=_load_settings(); assert d.get('ocr_provider')=='off' or 'ocr_provider' in d; print('OK')" && ruff check pagefolio/settings.py pagefolio/lang.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ocr_provider" pagefolio/settings.py` が defaults dict 内に 1 件以上ヒットし、値が `"off"` である
    - `grep -c "ocr_text_skip_notice" pagefolio/lang.py` が 2（ja と en の両方）
    - `LANG['ja']['ocr_text_skip_notice']` と `LANG['en']['ocr_text_skip_notice']` がいずれも `{page}` プレースホルダで format 可能
    - `ruff check pagefolio/settings.py pagefolio/lang.py` が exit 0
  </acceptance_criteria>
  <done>settings の defaults に ocr_provider:"off" が追加され、lang.py の ja/en に ocr_text_skip_notice（+必要なら進行表示キー）が揃って追加されている。</done>
</task>

<task type="auto">
  <name>Task 2: _worker のスレッド境界を再構成し run_parallel / 埋め込みスキップを結線する</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py:1-22（import — call_lm_studio_parallel / fetch_lm_studio_models / page_to_png_b64 の import を新 API へ更新）
    - pagefolio/ocr_dialog.py:28-73（__init__ — provider 引数追加と既存 url/model/doc 保持の整合）
    - pagefolio/ocr_dialog.py:382-402（_fetch_models — provider.list_models() への切替）
    - pagefolio/ocr_dialog.py:423-522（_on_run / _worker — フェーズ1/2/3 構造。スレッド境界リファクタの主対象）
    - pagefolio/ocr_dialog.py:524-590（_on_progress_bar / _render_results_ordered / _finish_* — 結果描画とスキップ由来区別表示の結合点）
    - pagefolio/ocr.py（Plan 02: run_parallel / has_embedded_text / page_to_png_b64 のシグネチャ）
    - 本プラン冒頭「OCRDialog.__init__ 改修後の確定シグネチャ」表（provider 引数の位置・型・用途の唯一の確定仕様）
    - .planning/phases/04-provider-abstraction/04-PATTERNS.md:246-316, 408-451（self.after スレッド安全パターン / フェーズ構造 / fitz スレッド境界）
    - .planning/phases/04-provider-abstraction/04-CONTEXT.md（D-01 事前レンダリング最小構成・after 小分け / D-03 ワーカー内に fitz/get_pixmap ゼロ / D-05 has_embedded_text はメインスレッド / D-08 スキップを UI 明示・結果辞書統合 / D-02 逐次化は Phase 6 で踏み込まない）
    - .planning/research/PITFALLS.md:35-77（Pitfall #2 fitz スレッド非安全 / Pitfall #3 Tkinter スレッド非安全）
  </read_first>
  <action>
    **import 更新（`pagefolio/ocr_dialog.py:12-20`）:** `call_lm_studio_parallel` / `fetch_lm_studio_models` の import を削除し、`from pagefolio.ocr import (..., run_parallel, has_embedded_text, page_to_png_b64, OCR_PROMPTS, DEFAULT_OCR_CONCURRENCY, MAX_OCR_CONCURRENCY, MAX_OCR_MAX_TOKENS)` に整理する。
    **__init__ に provider 引数追加（`pagefolio/ocr_dialog.py:28-44`）:** 本プラン冒頭の「OCRDialog.__init__ 改修後の確定シグネチャ」表どおりに、`concurrency` の後・`lang` の前に `provider=None` を追加し、`self.provider = provider` を保持する。`url`/`model` 等の既存引数は LM Studio 表示用に維持する（04-02 Task 2 の `_start_ocr` が確定シグネチャ表に従い `provider=provider` と url/model 等の両方を渡す前提・後方互換）。`self.provider` が None の場合のフォールバックは Claude's Discretion（例: 既存 url/model から LMStudioProvider を生成）だが、04-02 で _start_ocr が常に provider を渡すため通常 None にはならない。
    **_fetch_models（`pagefolio/ocr_dialog.py:382-402`）:** `fetch_lm_studio_models(url, timeout=10)` を `self.provider.list_models()` に置換する。例外捕捉 `except (ConnectionError, TimeoutError, RuntimeError) as e:` と進行表示は現行を維持する。
    **_worker スレッド境界リファクタ（中核・成功基準3・D-03）:** 現行 `_worker`（`pagefolio/ocr_dialog.py:439-522`）はフェーズ1（`page_to_png_b64(self.doc[page_idx], ...)` を背景スレッドで実行）→フェーズ2（並列 API）→フェーズ3（UI）の構造。これを次の通り再構成する:
      - **fitz レンダリング + 埋め込み判定をメインスレッド側へ移す（D-01/D-03/D-05）。** `_on_run`（`pagefolio/ocr_dialog.py:423-437`）から、`_worker` スレッドを起動する前に、メインスレッドでページごとのレンダリング/判定を `self.after()` で小分け実行するコーディネータ（例 `_render_next_page(idx)` を `self.after(0, ...)` で連鎖、または `_prepare_images()` をメインスレッドで逐次実行し UI フリーズ回避）を実装する。各ページについて: (a) `has_embedded_text(self.doc[page_idx])` が True なら `self.doc[page_idx].get_text()` の結果を `self.results[page_idx]` に直接投入し、スキップ集合（例 `self._skipped_pages`）に記録し、Vision 経路の `images` には積まない（成功基準2・D-07/D-08）。(b) False なら `page_to_png_b64(self.doc[page_idx], scale)` でレンダリングして `images[page_idx]` に保持する。**この (a)/(b) はすべてメインスレッド（after コールバック内）で実行し、ワーカースレッドには文字列 `images`（と provider）のみを渡す。**
      - **レンダリング完了後にワーカースレッドを起動し、`_worker` は API 並列送信のみ担う。** `_worker` 内のフェーズ2を `results, errors, fatal_msg, fatal_kind = run_parallel(self.provider, images, [非スキップの page_indices], concurrency=self.concurrency, prompt=prompt, on_progress=on_progress, is_cancelled=self._cancel_flag.is_set)` に置換する（Plan 02 の run_parallel シグネチャに合わせる）。`run_parallel` に渡す `page_indices` はスキップ済みページを除いた対象のみとする。`on_progress` は現行の `self.after(0, lambda: ...)` パターンを維持する（Pitfall #3）。
      - **`_worker` 内に fitz/get_pixmap/self.doc[ が一切現れないこと（成功基準3 必達）。** レンダリング・判定・`self.doc` アクセスはすべてメインスレッド側コーディネータに存在させる。キャンセルチェック（`self._cancel_flag.is_set()`）・`self.after(0, self._finish_cancelled)` 等の制御フローは維持する。
      - **D-02 逐次レンダリング化（render→送信→破棄）は本プランで踏み込まない**（Phase 6）。事前レンダリング最小構成に留める。
    **スキップ結果の統合表示（D-08）:** `_render_results_ordered`（`pagefolio/ocr_dialog.py:529-542`）でスキップページの由来を区別表示する。スキップページは `self.results` に入っているため通常表示されるが、`self._skipped_pages` に含まれる page_idx は区切り行の後に `self._L["ocr_text_skip_notice"].format(page=page_idx+1)` を併記する等で「OCRスキップ（埋め込みテキスト）」を明示する（文言詳細は Claude's Discretion）。進行表示でもスキップ時に `ocr_progress_skip`（Task 1 で追加した場合）を使ってよい。
    **後方互換（成功基準1）:** 埋め込みテキストの無い通常 PDF（スキャン画像 PDF）では、全ページが Vision 経路に乗り `run_parallel(provider=LMStudioProvider, ...)` で v1.3.0 と同一の OCR 結果・UI 操作になること。`_finish_complete` / `_finish_cancelled` / `_finish_error` の状態遷移とボタン活性制御は現行を維持する。
    裸 except 禁止・`# type: ignore` 無断使用禁止・テーマ色は `C["KEY"]`・フォントは `self._font(delta)`・Python 3.8 互換型ヒント。
  </action>
  <verify>
    <automated>python -c "import ast,re; src=open('pagefolio/ocr_dialog.py',encoding='utf-8').read(); ast.parse(src); m=re.search(r'def _worker\(self.*?(?=\n    def |\Z)', src, re.S); assert m is not None, '_worker not found'; body=m.group(0); assert 'get_pixmap' not in body, 'get_pixmap in _worker'; assert 'self.doc[' not in body, 'self.doc[ in _worker'; assert 'page_to_png_b64' not in body, 'render in _worker'; assert 'run_parallel(' in src; assert 'has_embedded_text' in src; print('OK')" && ruff check pagefolio/ocr_dialog.py</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr_dialog.py` の `_worker` 関数本体に `get_pixmap`・`self.doc[`・`page_to_png_b64` が出現しない（成功基準3・D-03。上記 automated チェックで機械検証。`_worker` がファイル末尾最後のメソッドでも `(?=\n    def |\Z)` 終端 + `m is not None` ガードで堅牢に抽出する）
    - `grep -n "run_parallel(" pagefolio/ocr_dialog.py` が 1 件以上（フェーズ2 が run_parallel に置換済み）
    - `grep -n "has_embedded_text" pagefolio/ocr_dialog.py` が 1 件以上（埋め込み判定が結線済み）
    - `grep -n "call_lm_studio_parallel\|fetch_lm_studio_models" pagefolio/ocr_dialog.py` が空（旧 API への依存が除去されている）
    - `grep -n "self.provider" pagefolio/ocr_dialog.py` が 1 件以上（provider を受け取り使用）
    - `grep -n "ocr_text_skip_notice" pagefolio/ocr_dialog.py` が 1 件以上（スキップ UI 明示・D-08）
    - `python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py', encoding='utf-8').read())"` が成功
    - `ruff check pagefolio/ocr_dialog.py` が exit 0
    - `pytest` が exit 0（全テスト緑・後方互換）
  </acceptance_criteria>
  <done>_worker がメインスレッドでレンダリング/埋め込み判定を済ませた文字列のみを受け取り run_parallel(provider, ...) で API 並列送信のみを行い、_worker 内に fitz/get_pixmap/self.doc[ が存在せず、埋め込みページは get_text() 結果を採用して UI にスキップ明示し、LM Studio の通常 OCR が v1.3.0 と一致する。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| メインスレッド（fitz/Tkinter）↔ ワーカースレッド（HTTP IO） | fitz.Document / Tkinter ウィジェットはメインスレッドのみが触る。ワーカーへは base64 文字列と provider のみを渡す（Pitfall #2/#3） |
| アプリ → LM Studio (localhost) | run_parallel → provider.ocr_image 経由。本プランで新規接続先の追加なし |
| fitz.Page → has_embedded_text / get_text | 埋め込みテキスト抽出はローカル処理。外部送信なし。スキップ時は API へ画像を送らない（むしろ送信を減らす方向） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering / Crash | `_worker` のスレッド境界（fitz 並行アクセス） | mitigate | `_worker` 本体から fitz.Document / get_pixmap / page_to_png_b64 / self.doc[ を排除し、レンダリング・get_text 判定をメインスレッド（after 小分け）に集約。automated grep チェックで _worker 内ゼロを機械検証（成功基準3・Pitfall #2） |
| T-04-08 | Denial of Service / Crash | ワーカーからの Tkinter ウィジェット直接操作 | mitigate | 進捗・スキップ・結果更新はすべて `self.after(0, lambda: ...)` 経由。Provider/ocr モジュールは Tkinter シンボルを参照しない（Pitfall #3） |
| T-04-09 | Information Disclosure | スキップページの get_text() テキストの扱い | mitigate | 抽出テキストは結果辞書/結果テキスト領域への表示にのみ使用し、logger / 例外メッセージへ混入させない（security_threat_model 指示 (3)） |
| T-04-10 | Information Disclosure | settings の ocr_provider 永続化 | accept | 追加するのは `ocr_provider:"off"` のみで秘密情報ではない。APIキーは settings に書かない（V14-D-02 のガードは Phase 5 直前タスク。本プランでキーを settings に入れる処理を追加しない） |

**高 severity の新規脅威なし。** 本プランはスレッド境界の明確化により既存の潜在的 fitz 並行アクセスリスク（Pitfall #2）を低減する方向。新規外部接続先・APIキー取り扱いの導入はなし。
</threat_model>

<verification>
- 上記 Task 2 の automated チェックで `_worker` 本体に `get_pixmap` / `self.doc[` / `page_to_png_b64` が存在しないことを機械検証（成功基準3・D-03）。終端パターンは `(?=\n    def |\Z)` でファイル末尾も許容し、`m is not None` ガードで抽出失敗時に AttributeError ではなく明示エラーとなる
- `grep -n "run_parallel(" pagefolio/ocr_dialog.py` がヒット / `grep -n "call_lm_studio_parallel\|fetch_lm_studio_models" pagefolio/ocr_dialog.py` が空（新 API への移行完了）
- `grep -n "has_embedded_text\|ocr_text_skip_notice" pagefolio/ocr_dialog.py` がヒット（埋め込みスキップ + UI 明示の結線・成功基準2/D-08）
- `python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py', encoding='utf-8').read())"` が成功（構文確認）
- `pytest` が exit 0（全テスト緑 = 後方互換 = 成功基準1）
- `ruff check . && ruff format --check .` が exit 0
</verification>

<success_criteria>
- `_worker`（ワーカースレッド関数）内に `fitz.Document` / `get_pixmap()` / `self.doc[` / `page_to_png_b64` の直接呼び出しが一切存在しない（成功基準3・D-03。grep 機械検証可能）
- レンダリングと has_embedded_text 判定がメインスレッド側（after 小分け）で実行され、ワーカーは run_parallel(provider, ...) のみを担う（D-01/D-05）
- テキスト埋め込みページは provider.ocr_image を呼ばず page.get_text() の結果を結果辞書へ統合し、UI にスキップが明示される（成功基準2・D-07/D-08）
- LM Studio の通常 OCR 挙動・UI 操作が v1.3.0 と一致する（成功基準1。pytest 全緑）
- スキップ通知の日英文言が lang.py に、ocr_provider デフォルトが settings.py に追加されている（D-09）
</success_criteria>

<output>
Create `.planning/phases/04-provider-abstraction/04-03-SUMMARY.md` when done
</output>
