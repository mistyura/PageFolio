---
gsd_plan_version: 1.0
quick_id: 260812-a43
slug: claude-md
description: CLAUDE.md のリリースゲート記載を実測に合わせて更新
date: 2026-08-12
mode: quick
branch: quick/260812-a43-claude-md
---

# Quick Plan 260812-a43 — CLAUDE.md のリリースゲート記載を実測に合わせて更新

## 背景

`CLAUDE.md`「## リリースゲート（全テスト完走条件）」節は現在こう記載している。

> 過去に観測されていた `TclError` によるセットアップ ERROR・`STATUS_BREAKPOINT` プロセス
> クラッシュの2症状は、v1.9.0 Phase 3 の切り分け調査（10回連続実行）で現行環境では
> 再現しないことを確認済み（リサーチセッションの7回と合算し累計17回連続グリーン）。

**2026-08-12（quick 260812-9tv の検証中）に症状①が再現した**ため、この記載は実態と一致しなくなった。

## 観測した一次データ（2026-08-12）

| 実行 | コマンド | 結果 | 所要 |
|------|---------|------|------|
| 1 回目 | `pytest -q --basetemp=...pf_pytest_tmp` | **1396 passed, 8 errors** — `tests/test_toast.py` のセットアップ ERROR 8 件 | 37.71s |
| 2 回目 | `pytest -q tests/test_toast.py --basetemp=...pf_pytest_tmp2` | 33 passed | 0.61s |
| 3 回目 | `pytest -q --basetemp=...pf_pytest_tmp3` | **1404 passed** | 52.59s |

同日の別実行（quick 260812-9ev の検証時）は 1404 passed / 110.19s でグリーン。
ERROR が出たのは `tests/test_toast.py` の 8 件のみで、うち報告に出た 2 件は
`TestToastManagerShowDismiss::test_same_category_reshow_rebinds_retry_cb` と
`TestToastRegeneratedAfterRebuild::test_toast_manager_regenerated_after_root_children_destroyed`。

**変更内容はドキュメント・設定のみ**（`.planning/config.json` と `.md` ファイル）であり、
Tkinter テストに影響する製品コード経路は存在しない。したがって環境要因（症状①の再発）と判定する。

## 既存の調査結論との関係

`.planning/milestones/v1.9.0-phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` は
「結論①（TclError によるセットアップ ERROR）: 現行環境では再現しない」と記録する一方、
同ファイルの「未解明のまま残るもの」節で**根本原因はネイティブ層で未解明・非再現の因果は未証明**と
明記している。今回の再現はその留保が現実になったケースであり、調査ログ自体は当時の一次データとして
正しい。**アーカイブ済みのマイルストーン成果物は書き換えない。**

## 確定事項

1. **合格条件そのものは変更しない** — 単一プロセスのフルスイートが失敗 0 件・ERROR 0 件・
   クラッシュなしで完走すること。ERROR が出たまま「flaky だから」と通すのは不可
2. 「累計 17 回連続グリーン」「再現しないことを確認済み」の断定を、**症状ごとに現況を分けた記述**へ置換する
3. ERROR 発生時の切り分け手順（単体実行 → フルスイート再実行）を追記する。ただしこれは
   **原因調査のための手順**であり、再実行でグリーンになれば合格という意味ではないことを明記する
4. 「やってはいけないこと」節（削除・skip・`-k`/`--ignore` による静かな除外）は維持

## タスク

### Task 1: `CLAUDE.md` のリリースゲート節を更新

- **files**: `CLAUDE.md`
- **action**: 該当段落を、症状①＝2026-08-12 に再現・症状②＝未再現、という現況記述へ置換。
  ERROR 発生時の切り分け手順を追加し、合格条件は据え置きであることを明示。根拠リンクに本 quick タスクを追加
- **verify**: 「累計 17 回連続グリーン」「再現しないことを確認済み」の断定が残っていないこと。
  合格条件の文（失敗 0 件・ERROR 0 件・クラッシュなし）が保持されていること
- **done**: リリースゲート節が 2026-08-12 の実測と矛盾しない

### Task 2: 検証

- **files**: なし
- **action**: `ruff check .` と `pytest` を実行
- **verify**: ruff 失敗 0 件、pytest 失敗 0 件・ERROR 0 件
- **done**: リリースゲート合格

## スコープ外

- `03-TEST-ENV-INVESTIGATION.md` の書き換え（アーカイブ済み成果物・当時の一次データとして正しい）
- 症状①の根本原因調査（ネイティブ層・別タスク）
- `tests/test_toast.py` の修正（製品コードのバグとは判定していない）
