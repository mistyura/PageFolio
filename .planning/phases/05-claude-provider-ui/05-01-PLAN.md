---
phase: 05-claude-provider-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/ocr_providers.py
  - tests/test_ocr_providers.py
autonomous: true
requirements: [OCR-API-01, OCR-PERF-03, OCR-PERF-04, OCR-UI-04]
must_haves:
  truths:
    - "ClaudeProvider が base64 PNG を Anthropic messages API へ送り OCR テキストを返す（成功基準7 の payload 基盤）"
    - "Opus/Sonnet 系モデルでは effort を output_config に入れて送り temperature を送らない（成功基準7）"
    - "Haiku では effort を送らず temperature を送る（成功基準7・D-16）"
    - "429/5xx 応答時に ClaudeProvider が OCRRetryableError を retry_after 付きで送出する（成功基準8 の基盤・OCR-PERF-04）"
    - "ClaudeProvider の並列度は default=2 / max=2（成功基準8 関連・OCR-PERF-03 の Claude=2）"
    - "list_models はキー未設定でも例外を出さず静的推奨リストを返す（OCR-API-03 の基盤・D-08）"
  artifacts:
    - path: "pagefolio/ocr_providers.py"
      provides: "ClaudeProvider クラス・OCRRetryableError 例外"
      contains: "class ClaudeProvider"
    - path: "tests/test_ocr_providers.py"
      provides: "ClaudeProvider payload/レスポンス解析/effort 判定/429 変換のモックテスト"
      contains: "ClaudeProvider"
  key_links:
    - from: "ClaudeProvider.ocr_image"
      to: "https://api.anthropic.com/v1/messages"
      via: "urllib POST + x-api-key / anthropic-version ヘッダー"
      pattern: "api\\.anthropic\\.com/v1/messages"
    - from: "ClaudeProvider.ocr_image"
      to: "OCRRetryableError"
      via: "HTTP 429/5xx を retryable 例外へ変換"
      pattern: "OCRRetryableError"
---

<objective>
Anthropic Claude messages API を urllib 直叩きで呼び出す `ClaudeProvider` と、429/5xx リトライ可能を示す `OCRRetryableError` 例外を `pagefolio/ocr_providers.py` に追加する。後続プラン（バックオフ共通層・コスト確認 UI・キー解決）がすべて依存する基盤層であり、Phase 5 の wave 1 として最初に確定させる。

すべての記述・docstring・コメント・コミットメッセージは日本語で書くこと（変数名・関数名・ヘッダー名・モデル ID・コマンドは英語のまま）。

Purpose: クラウド OCR の中核となる Provider 実装。payload 構築・レスポンス解析・effort/temperature のモデル別防御・429/5xx の retryable 変換を Provider 内に集約する（D-14/D-16）。
Output: `ClaudeProvider` クラス・`OCRRetryableError` 例外・モデル能力判定ヘルパー・モックテスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces（このプランで新規作成するシンボル）

- `OCRRetryableError(RuntimeError)` — `pagefolio/ocr_providers.py`。属性 `retry_after: float | None` を保持。429/5xx リトライ可能を示す。
- `ClaudeProvider(OCRProvider)` — `pagefolio/ocr_providers.py`。
  - クラス属性: `default_concurrency = 2` / `max_concurrency = 2`（OCR-PERF-03 Claude=2）
  - クラス定数: `ANTHROPIC_VERSION = "2023-06-01"` / `MESSAGES_ENDPOINT = "https://api.anthropic.com/v1/messages"` / `MODELS_ENDPOINT = "https://api.anthropic.com/v1/models"`
  - 静的推奨モデルリスト定数: `RECOMMENDED_MODELS = ["claude-haiku-4-5", "claude-sonnet-4-6", "claude-opus-4-8"]`
  - effort 対応モデル集合: `EFFORT_MODELS`（sonnet/opus 系。haiku は非対応）
  - `__init__(self, api_key, model, timeout=120, max_tokens=4096, temperature=0.1, effort="low")`
  - メソッド: `_supports_effort()` / `_build_payload(b64_png, prompt)` / `ocr_image(b64_png, prompt, **kwargs)` / `list_models()`
- `tests/test_ocr_providers.py` に追記するテストクラス: `TestClaudeProvider` 系
</artifacts_produced>

<read_first>
**触る前に必ず読むこと:**
- `pagefolio/ocr_providers.py` — 変更対象。`OCRProvider` 抽象基底・`OCRAPIKeyError`・`LMStudioProvider`（payload/ocr_image/list_models/例外正規化のアナログ実装）。ClaudeProvider はこの LMStudioProvider と同じ構造で書く。
- `.planning/research/STACK.md` §Claude (Anthropic) API — エンドポイント・必須ヘッダー（`x-api-key` / `anthropic-version: 2023-06-01` / `content-type`）・base64 画像フォーマット（`type: image` / `source.type: base64` / `media_type: image/png`）・レスポンス解析（`content[]` の `type=="text"` 走査）・`temperature`/`effort` の正しい仕様（temperature 全モデル可・effort は `output_config.effort`・Haiku 非対応）・モデル ID・`/v1/models` の `capabilities.image_input.supported`。
- `.planning/phases/05-claude-provider-ui/05-PATTERNS.md` §`pagefolio/ocr_providers.py` — ClaudeProvider の payload/ocr_image/list_models のパターン抜粋・例外正規化（429/5xx → OCRRetryableError 変換の前段）。
- `.planning/research/PITFALLS.md` Pitfall 6（レスポンス解析の脆弱性: `content[0].text` 決め打ち禁止・`type=="text"` 走査）・Pitfall 7（API ドリフト: anthropic-version 必須・モデル別パラメータ）・Pitfall 5（429/529 バックオフ・Retry-After 尊重）。
- `tests/test_ocr_providers.py` — 既存テスト構造（io.BytesIO で urlopen をモックするパターン・`TestOCRProviderAbstract`）。同スタイルで ClaudeProvider テストを追記する。
- `tests/test_ocr.py` の `FakeProvider`（行 23 付近）— Provider テストダブルの書き方の参考。
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OCRRetryableError 例外と ClaudeProvider 骨格（payload 構築・effort 判定）を追加</name>
  <files>pagefolio/ocr_providers.py, tests/test_ocr_providers.py</files>
  <read_first>
    - pagefolio/ocr_providers.py（OCRAPIKeyError・LMStudioProvider の __init__/_build_payload のアナログ）
    - .planning/research/STACK.md §Claude（payload フォーマット・effort=output_config.effort・Haiku 非対応・モデル ID）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §ocr_providers.py（OCRRetryableError・_build_payload パターン抜粋）
  </read_first>
  <behavior>
    - Test: OCRRetryableError("msg", retry_after=5.0).retry_after == 5.0、retry_after 省略時は None
    - Test: ClaudeProvider("k","claude-haiku-4-5")._supports_effort() is False（Haiku 非対応・D-16）
    - Test: ClaudeProvider("k","claude-sonnet-4-6")._supports_effort() is True、ClaudeProvider("k","claude-opus-4-8")._supports_effort() is True
    - Test: opus/sonnet モデルの _build_payload は payload["output_config"]["effort"] を含み "temperature" キーを含まない（成功基準7）
    - Test: haiku モデルの _build_payload は "temperature" キーを含み "output_config" を含まない（成功基準7・D-16）
    - Test: _build_payload の messages[0].content[0] が {"type":"image","source":{"type":"base64","media_type":"image/png","data":b64}} 形式、content[1] が {"type":"text","text":prompt}
    - Test: _build_payload は "model" と "max_tokens" をトップレベルに持つ
  </behavior>
  <action>
    `OCRRetryableError(RuntimeError)` を `OCRAPIKeyError` の直後に追加する。`__init__(self, message, retry_after=None)` で `self.retry_after = retry_after` を保持し `super().__init__(message)` を呼ぶ。docstring は「429/5xx リトライ可能エラー。retry_after（秒）を保持する」。
    `ClaudeProvider(OCRProvider)` を `LMStudioProvider` の後に追加する。クラス属性 `default_concurrency = 2` / `max_concurrency = 2`（OCR-PERF-03 Claude=2）。クラス定数 `ANTHROPIC_VERSION = "2023-06-01"`・`MESSAGES_ENDPOINT = "https://api.anthropic.com/v1/messages"`・`MODELS_ENDPOINT = "https://api.anthropic.com/v1/models"`・`RECOMMENDED_MODELS = ["claude-haiku-4-5", "claude-sonnet-4-6", "claude-opus-4-8"]`・`EFFORT_MODELS = {"claude-sonnet-4-6", "claude-opus-4-8", "claude-opus-4-7", "claude-opus-4-6", "claude-opus-4-5"}`（STACK.md の effort 対応リスト・Haiku は含めない）。
    `__init__(self, api_key, model, timeout=120, max_tokens=4096, temperature=0.1, effort="low")` で全引数を self に保持する（max_tokens 既定 4096 は STACK.md 例示・D Claude's Discretion）。
    `_supports_effort(self)` はモデル ID が `EFFORT_MODELS` に含まれるか、または `"opus"`/`"sonnet"` を含み `"haiku"` を含まないかで判定する（前方互換のためプレフィックス判定併用・D-16）。Haiku は必ず False。
    `_build_payload(self, b64_png, prompt)` は STACK.md §Claude の base64 画像フォーマットに厳密に従う。トップレベルに `model`・`max_tokens`・`messages`。messages[0].content は画像ブロック（`type: image` / `source: {type: base64, media_type: image/png, data: b64_png}`）の後にテキストブロック（`type: text`, `text: prompt`）。`_supports_effort()` が True なら `payload["output_config"] = {"effort": self.effort}`（temperature は入れない）、False なら `payload["temperature"] = self.temperature`（output_config は入れない）。**effort は output_config の子フィールド・トップレベルの兄弟（generationConfig ではない）**。
    `os` を import に追加（後続タスクの env 解決でも使うが、ここでは未使用なら追加不要。判断は実装者）。`<action>` 内に完全なコード本体・fenced code block は書かない方針のため、read_first の PATTERNS/STACK のコード抜粋を参照すること。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; python -m pytest tests/test_ocr_providers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - source: `pagefolio/ocr_providers.py` に `class OCRRetryableError(RuntimeError)` と `class ClaudeProvider(OCRProvider)` が存在する
    - behavior: `ClaudeProvider("k","claude-haiku-4-5")._build_payload("AAA","p")` に "temperature" キーが存在し "output_config" が存在しない
    - behavior: `ClaudeProvider("k","claude-opus-4-8")._build_payload("AAA","p")` に `output_config["effort"]` が存在し "temperature" キーが存在しない
    - behavior: payload の content[0] の media_type が "image/png"・source.type が "base64"
    - test: `python -m pytest tests/test_ocr_providers.py -x -q` が exit 0
  </acceptance_criteria>
  <done>OCRRetryableError と ClaudeProvider 骨格（effort/temperature 防御を含む payload 構築）が実装され、モデル別 payload とexport構造のテストが通る。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClaudeProvider.ocr_image / list_models（429・5xx の OCRRetryableError 変換・レスポンス解析）を実装</name>
  <files>pagefolio/ocr_providers.py, tests/test_ocr_providers.py</files>
  <read_first>
    - pagefolio/ocr_providers.py（LMStudioProvider.ocr_image / list_models の urllib POST/GET・例外正規化の完成形）
    - .planning/research/STACK.md §Claude（レスポンス content[] 走査・/v1/models の capabilities.image_input.supported・必須ヘッダー）
    - .planning/research/PITFALLS.md Pitfall 5（429/529/5xx バックオフ・Retry-After）/ Pitfall 6（content type=="text" 走査）
    - tests/test_ocr_providers.py（urlopen を io.BytesIO でモックするパターン）
  </read_first>
  <behavior>
    - Test: 正常レスポンス（content に type=="text" ブロック）から ocr_image がそのテキストを返す
    - Test: content に type=="thinking" と type=="text" が混在しても type=="text" を結合して返す（content[0] 決め打ちしない・Pitfall 6）
    - Test: HTTP 429 応答で OCRRetryableError が送出され、Retry-After: 3 ヘッダーがあれば retry_after == 3.0 になる
    - Test: HTTP 503（5xx）応答で OCRRetryableError が送出される
    - Test: HTTP 400 応答（4xx・429 以外）は RuntimeError（retryable ではない）
    - Test: socket.timeout は TimeoutError、URLError は ConnectionError に正規化される
    - Test: list_models はキー未設定（api_key="")のとき例外を出さず RECOMMENDED_MODELS を返す（D-08）
    - Test: list_models はキー設定時に /v1/models を呼び capabilities.image_input.supported==True のモデル id のみ返す
  </behavior>
  <action>
    `ocr_image(self, b64_png, prompt, **kwargs)` を `LMStudioProvider.ocr_image` の構造を踏襲して実装する。エンドポイントは `MESSAGES_ENDPOINT`。`urllib.request.Request` のヘッダーに `Content-Type: application/json`・`x-api-key: self.api_key`・`anthropic-version: self.ANTHROPIC_VERSION`（= "2023-06-01"・必須・Pitfall 7）を付ける。`# noqa: S310` を urlopen/Request に付ける（既存 LMStudioProvider と同様）。
    例外処理: `urllib.error.HTTPError` で `e.code == 429` または `e.code >= 500` のとき、`Retry-After` ヘッダー（`e.headers.get("Retry-After")`）を float へ変換できれば retry_after に入れて `OCRRetryableError(f"HTTP {e.code}: ...", retry_after=...)` を raise する。それ以外の HTTPError は既存どおり `RuntimeError(f"HTTP {e.code}: {err_body or e.reason}")`。`socket.timeout`→TimeoutError、`urllib.error.URLError`→ConnectionError（reason が socket.timeout なら TimeoutError）は LMStudioProvider と同一規約。
    レスポンス解析: `json.loads(body)` 後に `data.get("content", [])` を走査し `block.get("type") == "text"` のブロックの `block["text"]` を集めて `"\n".join(...)` で返す。text ブロックが皆無なら `RuntimeError(f"Unexpected response format: {body[:500]}")`（Pitfall 6・content[0] 決め打ち禁止）。
    `list_models(self)` を実装する。`self.api_key` が空文字/None のときは API を呼ばず `list(self.RECOMMENDED_MODELS)` を返す（D-08・キー未設定・オフラインでも選択肢が出る）。キーがあれば `MODELS_ENDPOINT` を GET（`x-api-key`・`anthropic-version` ヘッダー付き・timeout=10）。レスポンス `data.get("data", [])` から `capabilities.image_input.supported` が True のモデルの `id` のみ返す。取得失敗（ConnectionError/TimeoutError/RuntimeError）はそのまま送出する（呼び出し側 UI が捕捉して静的リストへフォールバックする）。
    `<action>` 内に完全なコード本体・fenced code block は書かない方針のため、read_first の LMStudioProvider 実装と PATTERNS/STACK 抜粋を参照すること。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; python -m pytest tests/test_ocr_providers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - behavior: 429 応答モックで `pytest.raises(OCRRetryableError)` が成立し、Retry-After ヘッダー値が retry_after に反映される
    - behavior: 503 応答モックで OCRRetryableError、400 応答モックで RuntimeError が送出される
    - behavior: content に thinking+text 混在のレスポンスで text ブロックのみ結合して返る
    - behavior: `ClaudeProvider("", "claude-sonnet-4-6").list_models()` が API を呼ばず RECOMMENDED_MODELS を返す
    - source: `ocr_image` 内のヘッダー構築に `"anthropic-version"` と `"x-api-key"` の両方が含まれる（grep で確認）
    - test: `python -m pytest tests/test_ocr_providers.py -x -q` が exit 0
  </acceptance_criteria>
  <done>ocr_image / list_models が実装され、429/5xx→OCRRetryableError・4xx→RuntimeError の区別、content type=="text" 走査、キー未設定時の静的リストフォールバックがテストで検証される。</done>
</task>

<task type="auto">
  <name>Task 3: ruff・全テスト・構文確認でグリーンを確定</name>
  <files>pagefolio/ocr_providers.py, tests/test_ocr_providers.py</files>
  <read_first>
    - CLAUDE.md（コーディング規約・禁止事項: 裸 except 禁止・# type: ignore 無断使用禁止・ruff/pytest 必須）
    - pyproject.toml（ruff select ルール E/F/W/I/S/B・line-length 88。編集禁止）
  </read_first>
  <action>
    `ruff check . ; ruff format .` を実行し、ClaudeProvider 追加分の lint/format を通す。`urllib.request` 系には既存と同様に `# noqa: S310` を付与する（S ルール対策）。裸の except は使わず `except Exception as e:` の形にする。Phase 5 の他プランがこの Provider に依存するため、ここでテスト・構文・lint を完全にグリーンにして基盤を確定する。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check . ; python -m pytest tests/test_ocr_providers.py tests/test_ocr.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test: `ruff check .` が exit 0（エラーゼロ）
    - test: `python -m pytest tests/test_ocr_providers.py tests/test_ocr.py -q` が exit 0
    - source: `pagefolio/ocr_providers.py` に裸の `except:` が存在しない（grep `except\s*:` が 0 件）
  </acceptance_criteria>
  <done>ruff・pytest が完全グリーン。ClaudeProvider/OCRRetryableError が後続プランから利用可能な確定状態になる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| アプリ → Anthropic API (api.anthropic.com) | ページ画像 base64 と API キーが外部ホストへ送信される信頼境界 |
| API キー（self.api_key 引数） → ログ / 例外メッセージ / レスポンス | キー値が二次的に漏洩しうる経路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ClaudeProvider.ocr_image の例外メッセージ・ログ | mitigate | エラーメッセージに `self.api_key` を一切含めない。HTTPError 本文（err_body）はサーバ応答のみで、キーは含めない。logger 出力に api_key を渡さない（成功基準1・D-04） |
| T-05-02 | Information Disclosure | レスポンス解析（content 走査） | mitigate | レスポンス本文を `body[:500]` のみエラーに含める。OCR 結果テキストに API キーは構造的に混入しない（キーはヘッダーのみ） |
| T-05-03 | Tampering / DoS | 429/5xx の無制限リトライ | mitigate | Provider は retryable を例外で通知するのみ。回数管理・sleep は呼び出し側（run_parallel・05-03）が最大3回で制御（OCR-PERF-04） |
| T-05-04 | Spoofing / Information Disclosure | anthropic-version ヘッダー欠落による 400 / 想定外挙動 | mitigate | `ANTHROPIC_VERSION = "2023-06-01"` を定数化し全リクエストに付与（Pitfall 7・成功基準7 関連） |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本プランは新規 pip 依存ゼロ（urllib のみ・V14-D-01）。パッケージインストールなし |
</threat_model>

<verification>
- 成功基準7（Opus 系で temperature を送らず effort を使用・非対応パラメータで 400 が起きない）の payload 基盤が `_build_payload` のモデル別分岐テストで担保される。
- 成功基準8（429/5xx 指数バックオフ）の Provider 側基盤（OCRRetryableError + retry_after）が 429/5xx モックテストで担保される。
- OCR-PERF-03（Claude=2）が `default_concurrency=2 / max_concurrency=2` のクラス属性で担保される。
- OCR-API-03（モデル一覧取得）の基盤が list_models のキー有無分岐テストで担保される。
</verification>

<success_criteria>
- `pagefolio/ocr_providers.py` に `ClaudeProvider`・`OCRRetryableError` が存在し、Anthropic messages API の payload/ヘッダー/レスポンス解析を正しく実装している。
- effort/temperature のモデル別防御（Haiku=temperature・Opus/Sonnet=effort）が payload に反映される。
- 429/5xx が OCRRetryableError（retry_after 付き）に、4xx が RuntimeError に正規化される。
- `ruff check .` と `pytest tests/test_ocr_providers.py tests/test_ocr.py` が exit 0。
</success_criteria>

<output>
Create `.planning/phases/05-claude-provider-ui/05-01-SUMMARY.md` when done
</output>
