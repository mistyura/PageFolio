---
phase: 05-claude-provider-ui
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - pagefolio/ocr.py
  - pagefolio/app.py
  - tests/test_ocr.py
autonomous: true
requirements: [OCR-SEC-02, OCR-SEC-03, OCR-API-01, OCR-PERF-04, OCR-PERF-03]
must_haves:
  truths:
    - "セッションキーは self._session_api_keys（settings とは別オブジェクト）にのみ保持され os.environ にも書かれない（成功基準3・D-01/D-05）"
    - "キー解決は環境変数優先・未設定時のみセッションキー（D-02）"
    - "環境変数もセッションキーも無い状態で Claude OCR を開始しようとすると実行前に OCRAPIKeyError 由来の明示エラーが出て処理が始まらない（成功基準2）"
    - "build_provider が claude 分岐で ClaudeProvider を生成しキーを引数注入する（settings にキーを入れない・成功基準1/3）"
    - "run_parallel が OCRRetryableError を受けて指数バックオフ（最大3回・Retry-After 優先）でリトライし waiting 進捗を通知する（成功基準8・OCR-PERF-04）"
  artifacts:
    - path: "pagefolio/ocr.py"
      provides: "build_provider claude 分岐・_resolve_api_key・run_parallel バックオフ層"
      contains: "_resolve_api_key"
    - path: "pagefolio/app.py"
      provides: "_session_api_keys 属性（プロバイダ別 dict）"
      contains: "_session_api_keys"
    - path: "tests/test_ocr.py"
      provides: "バックオフ・キー解決・claude 分岐のモックテスト"
      contains: "OCRRetryableError"
  key_links:
    - from: "run_parallel"
      to: "OCRRetryableError.retry_after"
      via: "指数バックオフ + Retry-After 優先 sleep"
      pattern: "OCRRetryableError"
    - from: "build_provider"
      to: "ClaudeProvider"
      via: "claude 分岐でキー引数注入（settings には入れない）"
      pattern: "ClaudeProvider"
    - from: "OCRMixin._start_ocr"
      to: "self._session_api_keys"
      via: "環境変数優先のキー解決"
      pattern: "_session_api_keys"
---

<objective>
Phase 5 のセキュリティ結合層と並列/バックオフ層を `pagefolio/ocr.py` に実装し、セッションキー保持属性 `self._session_api_keys` を `pagefolio/app.py` に追加する。`build_provider` に claude 分岐を加え、キーは settings に入れず引数注入する。`run_parallel` に OCRRetryableError を受けた指数バックオフ（最大3回・Retry-After 優先）と waiting 進捗を組み込む。`_start_ocr` 前段でキー解決（環境変数優先）と未設定時の明示エラー（OCRAPIKeyError）を行う。

すべての記述・docstring・コメント・コミットメッセージは日本語で書くこと（変数名・関数名・コマンドは英語のまま）。

Purpose: 成功基準2（未設定で実行前明示エラー）・3（セッションキーで実行・os.environ/settings 非書き込み）・8（429/5xx 指数バックオフ・待機中表示）の中核ロジックを provider 非依存の共通層に集約し、Phase 6 Gemini でも再利用できる形にする（D-14）。
Output: `_resolve_api_key`・`build_provider` claude 分岐・`run_parallel` バックオフ・`_start_ocr` キー解決・`_session_api_keys` 属性・モックテスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-claude-provider-ui/05-01-SUMMARY.md
@.planning/phases/05-claude-provider-ui/05-02-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces（このプランで新規作成するシンボル）

- `self._session_api_keys: dict[str, str]` — `pagefolio/app.py` の `PDFEditorApp.__init__` で初期化する**プロバイダ別**セッションキー辞書（例: `{"claude": "sk-ant-..."}`）。**settings とは完全に別オブジェクト**でプロセス終了時に消滅（D-01）。
- `_resolve_api_key(provider_name, session_keys)` — `pagefolio/ocr.py` の新規関数。環境変数優先・未設定時のみセッションキー（D-02）。claude は `ANTHROPIC_API_KEY`、解決できなければ `OCRAPIKeyError("ANTHROPIC_API_KEY")` を raise。
- `build_provider(settings, api_key=None)` — 既存関数の改修。claude 分岐追加・api_key 引数追加（キーは settings に入れず注入・D-05）。
- `run_parallel(...)` — 既存関数の改修。`_call` の except に `OCRRetryableError` を追加し、`as_completed` ループで指数バックオフ（最大3回・初回 base から倍々・Retry-After 優先）+ `waiting` ステータスの on_progress 通知。
- 新規定数: `MAX_RETRIES = 3`・`RETRY_BASE_DELAY = 1.0`（ocr.py）。
- `tests/test_ocr.py` に追記するテスト: バックオフ・キー解決・claude 分岐。
</artifacts_produced>

<read_first>
**触る前に必ず読むこと:**
- `pagefolio/ocr.py` — 変更対象。`run_parallel`（83行・`_call` の例外区別・`as_completed` ループ・`on_progress(done, page_idx, status)` 契約）・`build_provider`（179行・lmstudio/off 分岐・ValueError）・`OCRMixin._start_ocr`（226行・provider 生成・concurrency クランプ・OCRDialog 生成）・定数群。
- `pagefolio/ocr_providers.py` — 05-01 で追加した `ClaudeProvider`・`OCRRetryableError`（retry_after 属性）・`OCRAPIKeyError`（env_var 属性）。run_parallel/build_provider が import する。
- `pagefolio/app.py` — 変更対象。`PDFEditorApp.__init__`（行 42-95・self.settings/self.doc 等の属性初期化箇所。ここに _session_api_keys を追加）。
- `.planning/phases/05-claude-provider-ui/05-PATTERNS.md` §`pagefolio/ocr.py`（build_provider claude 分岐・run_parallel バックオフ拡張点・_start_ocr キー解決パターン抜粋）。
- `.planning/research/PITFALLS.md` Pitfall 5（429/529/5xx・指数バックオフ初回1s→2s→4s 最大3回・Retry-After 尊重・on_progress で「待機中」表示）。
- `.planning/research/STACK.md` §環境変数規約（claude=ANTHROPIC_API_KEY・未設定時は明示エラー）。
- `tests/test_ocr.py` — `FakeProvider`（行 23）・`TestRunParallel`（行 256）・build_provider テスト（行 499 付近）。同スタイルで追記する。
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app.py に _session_api_keys 属性追加・ocr.py に _resolve_api_key と build_provider claude 分岐を実装</name>
  <files>pagefolio/app.py, pagefolio/ocr.py, tests/test_ocr.py</files>
  <read_first>
    - pagefolio/app.py（__init__ の属性初期化ブロック 行 69-90）
    - pagefolio/ocr.py（build_provider 行 179-205・関数内 import パターン）
    - pagefolio/ocr_providers.py（ClaudeProvider __init__ シグネチャ・OCRAPIKeyError）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §ocr.py（build_provider claude 分岐・_resolve_api_key）
    - .planning/research/STACK.md §環境変数規約
  </read_first>
  <behavior>
    - Test: _resolve_api_key("claude", {}) で環境変数 ANTHROPIC_API_KEY 設定時はその値を返す（monkeypatch で os.environ 設定）
    - Test: _resolve_api_key("claude", {"claude": "sess-key"}) で環境変数未設定時はセッションキー "sess-key" を返す（D-02 環境変数優先・未設定時セッション）
    - Test: 環境変数優先 — 環境変数があれば session_keys にキーがあっても環境変数を返す
    - Test: _resolve_api_key("claude", {}) で環境変数もセッションキーも無い場合 OCRAPIKeyError("ANTHROPIC_API_KEY") を raise（成功基準2）
    - Test: build_provider({"ocr_provider":"claude"}, api_key="k") が ClaudeProvider インスタンスを返し provider.api_key=="k"・provider.model がデフォルト（claude_model）
    - Test: build_provider が返す settings 辞書に api_key 相当キーが追加されていない（成功基準1/3・キーは引数のみ）
  </behavior>
  <action>
    `pagefolio/app.py` の `PDFEditorApp.__init__` の属性初期化ブロック（self.doc / self.filepath 付近）に `self._session_api_keys = {}` を追加する（D-01・プロバイダ別 dict・settings とは別オブジェクト）。コメントで「セッション中のみ保持・settings へは入れない・プロセス終了で消滅」を明記する。
    `pagefolio/ocr.py` に `_resolve_api_key(provider_name, session_keys)` を新規追加する。`import os` を関数内 import で行う（循環回避の前例に従う・モジュール先頭 import でもよいが既存スタイル踏襲）。claude のとき `os.environ.get("ANTHROPIC_API_KEY")` を優先し、なければ `session_keys.get("claude", "")` を使う（D-02）。どちらも空なら `from pagefolio.ocr_providers import OCRAPIKeyError; raise OCRAPIKeyError("ANTHROPIC_API_KEY")`（成功基準2）。**os.environ への書き込みは一切行わない**（D-05・読み取りのみ）。
    `build_provider(settings, api_key=None)` に api_key 引数を追加し、`elif name == "claude":` 分岐を加える。`from pagefolio.ocr_providers import ClaudeProvider` を関数内 import し、`ClaudeProvider(api_key=api_key or "", model=settings.get("claude_model", "claude-sonnet-4-6"), timeout=int(settings.get("ocr_timeout", DEFAULT_OCR_TIMEOUT)), max_tokens=int(settings.get("ocr_max_tokens", 4096)), temperature=float(settings.get("ocr_temperature", DEFAULT_OCR_TEMPERATURE)), effort=settings.get("ocr_effort", "low"))` を返す。**api_key は settings から読まず引数のみ・settings へ書き込まない**（D-01/D-05）。既存 lmstudio/off 分岐と最後の ValueError は維持する。
    `tests/test_ocr.py` に上記 behavior のテストを追記する（monkeypatch.setenv / delenv で os.environ を制御）。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; python -m pytest tests/test_ocr.py -x -q -k "resolve or build_provider or claude"</automated>
  </verify>
  <acceptance_criteria>
    - source: `pagefolio/app.py` の __init__ に `self._session_api_keys = {}` が存在する
    - source: `pagefolio/ocr.py` に `def _resolve_api_key` が存在し、関数本体に `os.environ.get("ANTHROPIC_API_KEY")` が含まれ、`os.environ[` への代入が存在しない（grep `os.environ\[` が _resolve_api_key 周辺で 0 件・D-05）
    - behavior: 環境変数未設定 + session_keys 空で `_resolve_api_key("claude", {})` が OCRAPIKeyError を raise（成功基準2）
    - behavior: `build_provider({"ocr_provider":"claude"}, api_key="k").api_key == "k"`
    - test: 該当 pytest が exit 0
  </acceptance_criteria>
  <done>セッションキー属性・キー解決（環境変数優先・未設定時明示エラー・os.environ 非書き込み）・claude 分岐（キー引数注入）が実装され、テストで成功基準2/3 の中核が担保される。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_parallel に OCRRetryableError 指数バックオフ（最大3回・Retry-After 優先・waiting 進捗）を組込</name>
  <files>pagefolio/ocr.py, tests/test_ocr.py</files>
  <read_first>
    - pagefolio/ocr.py（run_parallel 全体・_call の except 区別・as_completed ループ・on_progress 呼び出し）
    - pagefolio/ocr_providers.py（OCRRetryableError.retry_after）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §ocr.py（run_parallel バックオフ拡張点）
    - .planning/research/PITFALLS.md Pitfall 5（指数バックオフ 1s→2s→4s・最大3回・Retry-After 優先・waiting 表示）
  </read_first>
  <behavior>
    - Test: provider.ocr_image が 1 回 OCRRetryableError を投げた後に成功する場合、run_parallel は最終的にそのページを results に入れる（リトライ成功）
    - Test: OCRRetryableError が毎回投げられ続ける場合、最大 3 回リトライした後に errors に記録される（無限ループしない・最大3回）
    - Test: retry_after を持つ OCRRetryableError では sleep にその値が使われる（time.sleep を monkeypatch して引数を検証・Retry-After 優先）
    - Test: retry_after が None の場合は指数バックオフ（RETRY_BASE_DELAY * 2**(n-1) 等）が使われる
    - Test: リトライ中に on_progress が status=="waiting" で呼ばれ、retry 回数を伴う（D-15・waiting 進捗）
    - Test: 既存 TestRunParallel（正常・キャンセル・fatal）が全て引き続き通る（後方互換）
  </behavior>
  <action>
    `pagefolio/ocr.py` に定数 `MAX_RETRIES = 3` と `RETRY_BASE_DELAY = 1.0` を追加する。`import time` をモジュール先頭に追加する。`from pagefolio.ocr_providers import OCRRetryableError` を run_parallel が使えるよう関数内 import する（循環回避）。
    `run_parallel` の `_call(page_idx, b64)` を改修し、リトライループを内包させる。`for attempt in range(1, MAX_RETRIES + 1):` で `provider.ocr_image` を呼び、`OCRRetryableError` を捕捉したら: 最後の attempt なら `("err", page_idx, str(e))` を返す（errors 記録）。そうでなければ on_progress を `status="waiting"` で（リトライ回数を渡せる形で）呼び、`time.sleep(e.retry_after if e.retry_after else RETRY_BASE_DELAY * (2 ** (attempt - 1)))` してから次の attempt へ（Retry-After 優先・Pitfall 5）。`ConnectionError`/`TimeoutError`/`RuntimeError`/`Exception` の既存区別は維持する。キャンセル時は途中で `("cancel", ...)` を返す。
    `on_progress` の呼び出し契約は既存の `(done, page_idx, status)` を保つ。waiting 通知は `_call` 内から（メインスレッドへの委譲は呼び出し側 OCRDialog が after(0) で行う・05-05 が消費）on_progress を直接呼ぶ形にする。リトライ回数を status 文字列に埋めるか、on_progress のシグネチャを拡張するかは実装者裁量だが、**既存の正常/エラー/fatal フローを壊さないこと**（後方互換テストが通ること）。waiting の done カウントは進めない（完了ではないため）。
    `tests/test_ocr.py` に FakeProvider を OCRRetryableError 投げる side_effect で拡張したテストを追記する。`time.sleep` を monkeypatch して実時間待機を避け、呼び出し引数を検証する。
    `<action>` 内に完全なコード本体は書かない。read_first の PATTERNS 抜粋を参照すること。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; python -m pytest tests/test_ocr.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - source: `pagefolio/ocr.py` に `MAX_RETRIES = 3` と `OCRRetryableError` の捕捉が存在する
    - behavior: 毎回 retryable を投げる provider で run_parallel が高々 3 リトライ後に errors へ記録し、無限ループしない
    - behavior: retry_after=5 の OCRRetryableError で sleep に 5 が渡る（monkeypatch 検証・Retry-After 優先）
    - behavior: 1 回 retryable→2 回目成功で results にテキストが入る
    - behavior: 既存 TestRunParallel の正常/キャンセル/fatal テストが全て通る（後方互換）
    - test: `python -m pytest tests/test_ocr.py -x -q` が exit 0
  </acceptance_criteria>
  <done>run_parallel が provider 非依存の指数バックオフ（最大3回・Retry-After 優先・waiting 進捗）を持ち、後方互換を保ったまま成功基準8 の共通層が担保される。Phase 6 Gemini が再利用可能。</done>
</task>

<task type="auto">
  <name>Task 3: _start_ocr にキー解決ゲートを組込み・ruff/全テストでグリーン確定</name>
  <files>pagefolio/ocr.py, tests/test_ocr.py</files>
  <read_first>
    - pagefolio/ocr.py（_start_ocr 行 226-278・build_provider 呼び出しと try/except ValueError・OCRDialog 生成・concurrency クランプ）
    - pagefolio/ocr_providers.py（OCRAPIKeyError.env_var）
    - pagefolio/lang.py（ocr_api_key_missing キー・05-02 で追加済み）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §ocr.py（_start_ocr キー解決結合点）
    - CLAUDE.md（ruff/pytest 必須・裸 except 禁止）
  </read_first>
  <action>
    `OCRMixin._start_ocr` を改修し、build_provider 呼び出し前にキー解決ゲートを挟む。`name = self.settings.get("ocr_provider", "")` を取得し、claude（将来 gemini）のクラウドプロバイダなら `from pagefolio.ocr import _resolve_api_key` 相当でキーを解決する（同モジュール内のため直接呼べる）。`_resolve_api_key(name, self._session_api_keys)` を try で呼び、`OCRAPIKeyError as e` を捕捉したら `messagebox.showerror(self._t("err_title"), self._t("ocr_api_key_missing").format(env_var=e.env_var), parent=self.root)` を表示して return する（成功基準2・実行前明示エラー・処理を始めない）。解決できたキーを `build_provider(self.settings, api_key=api_key)` に渡す。off/lmstudio はキー解決をスキップし api_key=None のまま既存どおり。既存の `try/except ValueError`（CR-01）は維持する。
    `self._session_api_keys` がまだ無い経路（テスト等）に備え、`getattr(self, "_session_api_keys", {})` で安全に参照する。
    `ruff check . ; ruff format .` を実行し全体をグリーンにする。裸 except 禁止・`except Exception as e:` 維持。`# noqa: S310` は対象外（新規 urllib なし）。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check . ; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - source: `_start_ocr` 内に `_resolve_api_key` 呼び出しと `OCRAPIKeyError` の捕捉、`ocr_api_key_missing` を使う showerror が存在する
    - source: `_start_ocr` でクラウド時にキー未解決なら `return` し OCRDialog を生成しない（成功基準2・処理を始めない）
    - behavior: claude 設定 + キー未設定で _start_ocr を呼ぶと OCRDialog が生成されない（messagebox/showerror を monkeypatch してエラー表示が呼ばれることを確認）
    - test: `ruff check .` が exit 0
    - test: `python -m pytest tests/ -q` が exit 0（全テストグリーン）
  </acceptance_criteria>
  <done>_start_ocr がクラウド実行前にキーを解決し、未設定なら明示エラーで処理を止める（成功基準2）。ruff・全テストがグリーンで wave 3 UI プランが依存可能な確定状態になる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 環境変数 / セッションキー → build_provider → ClaudeProvider | キーがアプリ内部を流れ Provider へ注入される境界 |
| キー → settings 辞書 / os.environ | キーが永続化経路へ漏れうる境界（D-01/D-05） |
| run_parallel → 外部 API（リトライループ） | レート制限・過負荷への耐性境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | build_provider のキー扱い | mitigate | api_key は引数注入のみ。settings へ書かない・settings から読まない（成功基準1/3・D-01/D-05）。テストで build_provider 後の settings にキーが無いことを確認 |
| T-05-09 | Information Disclosure | _resolve_api_key の os.environ 操作 | mitigate | os.environ は読み取り（get）のみ・書き込み禁止（D-05）。grep で os.environ[ 代入が無いことを確認（成功基準3） |
| T-05-10 | DoS / Cost | run_parallel の 429/5xx リトライ | mitigate | 最大 MAX_RETRIES=3 回でリトライ打ち切り・Retry-After 優先 sleep（OCR-PERF-04）。無限ループしないことをテストで担保 |
| T-05-11 | Information Disclosure | キー未設定時のクラウド送信 | mitigate | _start_ocr がキー未解決時に return し OCRDialog を生成しない（外部送信ゼロ・成功基準2/6） |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本プランは新規 pip 依存ゼロ（標準ライブラリのみ）。パッケージインストールなし |
</threat_model>

<verification>
- 成功基準2（未設定で実行前明示エラー・処理開始せず）が _start_ocr キー解決ゲートと OCRAPIKeyError 表示で担保される。
- 成功基準3（セッションキーで実行・os.environ/settings 非書き込み）が _resolve_api_key と build_provider キー引数注入で担保される。
- 成功基準8（429/5xx 指数バックオフ・待機中）が run_parallel バックオフ層（最大3回・Retry-After 優先・waiting 進捗）で担保される。
- OCR-PERF-03（Claude=2）は 05-01 のクラス属性 + run_parallel のクランプ（既存）で機能する。
</verification>

<success_criteria>
- セッションキーが settings/os.environ と別オブジェクト（_session_api_keys）に保持され、両者へ書き込まれない（成功基準3）。
- キー未設定で Claude OCR 開始時に実行前明示エラーが出て OCRDialog が生成されない（成功基準2）。
- build_provider が claude 分岐でキーを引数注入し ClaudeProvider を返す（成功基準1）。
- run_parallel が最大3回・Retry-After 優先の指数バックオフと waiting 進捗を持つ（成功基準8）。
- `ruff check .` と `pytest tests/` が exit 0。
</success_criteria>

<output>
Create `.planning/phases/05-claude-provider-ui/05-03-SUMMARY.md` when done
</output>
