---
phase: 05-claude-provider-ui
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - pagefolio/ocr_dialog.py
autonomous: false
requirements: [OCR-UI-03, OCR-SEC-03, OCR-PERF-04]
must_haves:
  truths:
    - "クラウドプロバイダ（claude）実行時のみ、実行前に送信先ホスト・ページ数・概算コスト・プライバシー注記を含む確認ダイアログが出る（成功基準5・OCR-UI-03・D-12/D-13）"
    - "確認ダイアログをキャンセルすると OCR が始まらない（成功基準5）"
    - "lmstudio/off では確認ダイアログが出ない（OCR-UI-03・D-13）"
    - "環境変数未設定かつクラウド選択時のみ、保存されないマスク入力欄（show='*'）でセッションキーを入力でき、値は app._session_api_keys に入り settings には入らない（成功基準3・OCR-SEC-03・D-01/D-03/D-04）"
    - "run_parallel の waiting ステータスを受けて該当ページに「待機中（リトライ n/3）」が表示される（成功基準8・OCR-PERF-04・D-15）"
  artifacts:
    - path: "pagefolio/ocr_dialog.py"
      provides: "コスト確認ゲート・セッションキー入力欄・待機中進捗・provider 中立化"
      contains: "_confirm_cost"
  key_links:
    - from: "OCRDialog._on_run"
      to: "_confirm_cost"
      via: "クラウド時のみ実行前ゲート（キャンセルで中止）"
      pattern: "_confirm_cost"
    - from: "OCRDialog セッションキー入力"
      to: "app._session_api_keys"
      via: "クラウドかつ env 未設定時のみマスク入力欄の値を格納（settings には入れない）"
      pattern: "_session_api_keys"
    - from: "OCRDialog.on_progress"
      to: "ocr_waiting_retry 文言"
      via: "status=='waiting' で待機中表示"
      pattern: "waiting"
---

<objective>
クラウド実行ゲート層を `pagefolio/ocr_dialog.py`（OCRDialog）に実装する。クラウドプロバイダ（claude）実行時に、送信先ホスト・ページ数・概算コスト・プライバシー注記を含む確認ダイアログ（毎回・キャンセル可）を実行前に挟む。環境変数未設定かつクラウド選択時のみ、保存されないマスク入力欄でセッションキーを受け取り app._session_api_keys に格納する。run_parallel の waiting ステータスを「待機中（リトライ n/3）」として既存進捗 UI に表示する。OCRDialog を provider 中立化し、claude を build_provider 経由で扱えるようにする。

すべての記述・docstring・コメント・コミットメッセージは日本語で書くこと（変数名・関数名・コマンドは英語のまま）。

Purpose: 成功基準5（クラウドで確認ダイアログ・キャンセルで中止）・3（セッションキー入力欄・非永続化）・8（待機中表示）を満たす。UI ゲートのためビジュアル/機能確認が必要なので checkpoint:human-verify を含む（autonomous: false）。
Output: コスト確認ゲート・セッションキー入力欄・待機中進捗・provider 中立化。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-claude-provider-ui/05-01-SUMMARY.md
@.planning/phases/05-claude-provider-ui/05-02-SUMMARY.md
@.planning/phases/05-claude-provider-ui/05-03-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces（このプランで新規作成するシンボル）

- `OCRDialog._is_cloud_provider()` — 現在の provider がクラウド系（ClaudeProvider・将来 Gemini）か判定。
- `OCRDialog._confirm_cost()` — クラウド時の実行前確認ダイアログ（毎回・D-11）。送信先ホスト・ページ数・概算コスト・プライバシー注記（D-12 の3点）を表示。OK/キャンセルを bool で返す。
- `OCRDialog._estimate_cost(model, page_count)` — 概算コスト算出（D-10・ページ数 × モデル別概算単価の粗い範囲。係数は実装者裁量）。
- `OCRDialog._needs_session_key()` — クラウドかつ環境変数未設定かを判定。
- セッションキー入力欄（マスク `show="*"`・D-04）と `self.api_key_var` — `_needs_session_key()` 時のみ表示。値は `self.app._session_api_keys[provider_name]` に格納（settings には入れない・D-01）。
- `on_progress` の waiting 分岐 — `status == "waiting"` で `ocr_waiting_retry` を表示（D-15）。
- `_on_run` の provider 再生成を build_provider 経由（claude 対応）に変更（現状の LMStudioProvider 固定を中立化）。
</artifacts_produced>

<read_first>
**触る前に必ず読むこと:**
- `pagefolio/ocr_dialog.py` — 変更対象。`__init__`（provider 引数・self.app/self.provider 保持）・`_on_run`（行 433・現状 LMStudioProvider 固定再生成 行 481-489・ここを build_provider 中立化）・`_render_next_page`/`_start_worker_thread`/`_worker`（行 545・on_progress 定義 行 556-563・run_parallel 委譲 行 569）・`_finish_*`・grab_set モーダル作法。
- `.planning/phases/05-claude-provider-ui/05-PATTERNS.md` §`pagefolio/ocr_dialog.py` — コスト確認ダイアログ配置（_on_run の冒頭ゲート）・セッションキー入力欄（show="*" マスク）・on_progress の waiting 分岐・provider 再生成のパターン抜粋。
- `pagefolio/ocr.py` — 05-03 の `build_provider(settings, api_key=)`・`_resolve_api_key`・`run_parallel`（waiting ステータスを on_progress に渡す契約）。
- `pagefolio/ocr_providers.py` — `ClaudeProvider`（model/価格判定の手掛かり）・`OCRAPIKeyError`。
- `pagefolio/lang.py` — 05-02 の `ocr_cost_confirm_title` / `ocr_cost_confirm_msg`（{host}/{count}/{cost}）/ `ocr_session_key_label` / `ocr_waiting_retry`（{page}/{n}/{max}）/ `ocr_api_key_missing`。
- `.planning/research/STACK.md` 価格表（claude: haiku $1/$5・sonnet $3/$15・opus $5/$25 MTok・コスト見積もりの根拠）。
- `.planning/research/PITFALLS.md` Pitfall 8（プライバシー・コスト無確認送信）・Security Mistakes（キーをマスク・ログ/エラーに出さない・D-04）。
- `pagefolio/app.py` — `_session_api_keys`（05-03 で追加）・`self.settings`（ocr_provider 参照）。
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: OCRDialog の provider 中立化（_on_run を build_provider 経由・claude 対応）と待機中進捗表示</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（_on_run 行 433-494 の LMStudioProvider 固定再生成・_worker の on_progress 行 556-563・run_parallel 委譲）
    - pagefolio/ocr.py（build_provider・_resolve_api_key・run_parallel の waiting 契約・05-03）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §ocr_dialog.py（provider 再生成・on_progress waiting 分岐）
    - pagefolio/lang.py（ocr_waiting_retry・05-02）
  </read_first>
  <action>
    `_on_run` の provider 再生成部（現状 `LMStudioProvider(...)` 固定・行 481-489）を、provider 種別に応じた再生成に変更する。`name = self.app.settings.get("ocr_provider", "")` を取得し、lmstudio/off は従来どおりダイアログ live 値（url/model/max_tokens/temperature）で LMStudioProvider を再生成（CR-02 後方互換維持）。claude のときは `build_provider(self.app.settings, api_key=...)` 経由で ClaudeProvider を生成する（キーは Task 2 のセッションキー解決結果か env から `_resolve_api_key` で取得）。**api_key は settings に入れない・引数注入のみ**（D-01/D-05）。
    `_worker` の `on_progress(done, page_idx, status)` に `status == "waiting"` の分岐を追加する。waiting のときは `self.after(0, lambda p=page_idx, ...: self.progress_var.set(self._L["ocr_waiting_retry"].format(page=p+1, n=..., max=3)))` で待機中表示し、進捗バーは進めない（D-15）。リトライ回数 n の受け渡しは 05-03 の run_parallel が status に埋める形に合わせる（05-03 SUMMARY を確認・実装者がシグネチャに整合させる）。通常の ok/err は既存どおり。**バックグラウンドスレッドから直接ウィジェット操作しない・after(0) 経由**（Pitfall 3）。
    `<action>` 内に完全なコード本体は書かない。read_first の PATTERNS 抜粋・05-03 SUMMARY を参照すること。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check pagefolio/ocr_dialog.py ; python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py', encoding='utf-8').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - source: `_on_run` の provider 再生成が `ocr_provider` 値で分岐し、claude のとき build_provider/ClaudeProvider 経路を通る
    - source: `on_progress` に `status == "waiting"` の分岐があり `ocr_waiting_retry` を使う
    - source: provider 再生成・キー処理で `settings[` への api_key 系代入が存在しない（grep 0 件・成功基準1）
    - source: on_progress 内のウィジェット更新が全て `self.after(0, ...)` 経由（直接 set がない・Pitfall 3）
    - test: `ruff check pagefolio/ocr_dialog.py` が exit 0・ast.parse が "OK"
  </acceptance_criteria>
  <done>OCRDialog が provider 中立化され claude を扱え、run_parallel の waiting を「待機中（リトライ n/3）」として表示する（成功基準8 の UI 側）。</done>
</task>

<task type="auto">
  <name>Task 2: コスト確認ゲート・概算コスト算出・セッションキー入力欄（マスク・非永続化）を実装</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（_on_run 冒頭・_build の各欄構築・grab_set モーダル・_center）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §ocr_dialog.py（_confirm_cost 配置・show="*" マスク入力欄）
    - .planning/research/STACK.md 価格表（haiku/sonnet/opus MTok 単価）
    - .planning/research/PITFALLS.md Pitfall 8 / Security Mistakes（D-04 マスク・ログ非出力）
    - pagefolio/lang.py（ocr_cost_confirm_title/msg・ocr_session_key_label・ocr_api_key_missing・05-02）
    - pagefolio/app.py（_session_api_keys・05-03）
  </read_first>
  <action>
    `_is_cloud_provider()` を追加: `self.app.settings.get("ocr_provider","")` が claude（将来 gemini）なら True（D-13）。isinstance(self.provider, ClaudeProvider) 併用も可。
    `_estimate_cost(model, page_count)` を追加: STACK.md の MTok 単価表（haiku $1/$5・sonnet $3/$15・opus $5/$25）を基に、画像入力トークン + 想定出力トークンの粗い見積もりで「約 $X 程度」の範囲文字列を返す（D-10・係数は実装者裁量・正確性より警告の存在が重要）。model から単価を選び page_count を掛ける。
    `_confirm_cost()` を追加: `tk.Toplevel` のモーダル（`grab_set()`）または `messagebox.askyesno` で、送信先ホスト（claude は `api.anthropic.com`）・対象ページ数・概算コスト・「ページ画像が外部 API に送信されます」「従量課金が発生します」の3点（D-12）を `ocr_cost_confirm_msg.format(host=, count=, cost=)` で表示する。OK で True・キャンセルで False を返す（成功基準5・D-11 毎回確認・「今後表示しない」は設けない）。
    `_needs_session_key()` を追加: クラウドかつ `os.environ.get("ANTHROPIC_API_KEY")` が未設定なら True。
    セッションキー入力欄: `_needs_session_key()` のとき確認ダイアログ内またはダイアログ本体に `tk.Entry(show="*", textvariable=self.api_key_var, ...)`（マスク・D-04・ラベル `ocr_session_key_label`）を出す。入力値は OCR 実行直前に `self.app._session_api_keys["claude"] = self.api_key_var.get().strip()` に格納する（**settings には入れない**・D-01/D-03）。空のまま実行しようとした場合は `ocr_api_key_missing` を表示し実行しない（成功基準2 と整合）。キー値はログ・OCR結果・エラーに一切出さない（D-04）。
    `_on_run` の冒頭（`if self._started: return` の直後・self._started=True の前）にゲートを挟む: `if self._is_cloud_provider():` のとき、まずセッションキー入力（必要なら）→ `if not self._confirm_cost(): return`（キャンセルで中止）。lmstudio/off ではゲートをスキップ（D-13・確認なしで従来どおり）。
    色は C[...]・フォントは self._font(...)・モーダルは grab_set。`<action>` 内に完全なコード本体は書かない。read_first の PATTERNS 抜粋を参照すること。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check pagefolio/ocr_dialog.py ; python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py', encoding='utf-8').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - source: `_confirm_cost`・`_estimate_cost`・`_is_cloud_provider`・`_needs_session_key` が `pagefolio/ocr_dialog.py` に存在する
    - source: セッションキー入力欄が `show="*"` でマスクされ、値は `self.app._session_api_keys` に入り `settings` には入らない（grep で settings への api_key 代入が 0 件・成功基準1/3）
    - source: `_on_run` 冒頭にクラウド時のみのゲートがあり、`_confirm_cost()` が False を返したら return する（成功基準5）
    - source: `_confirm_cost` の本文が host・count・cost の3要素を含む（ocr_cost_confirm_msg を使う・D-12）
    - test: `ruff check pagefolio/ocr_dialog.py` が exit 0・ast.parse が "OK"
  </acceptance_criteria>
  <done>クラウド実行前の確認ゲート（毎回・キャンセル可）とマスク付きセッションキー入力欄（非永続化）が実装され、成功基準5/3 を満たす。</done>
</task>

<task type="auto">
  <name>Task 3: ruff・全テストでグリーン確定</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - CLAUDE.md（ruff/pytest 必須・裸 except 禁止・テーマ色 C 経由・self._font）
    - pyproject.toml（ruff ルール。編集禁止）
  </read_first>
  <action>
    `ruff check . ; ruff format .` を実行し ocr_dialog.py をグリーンにする。裸 except 禁止・`except Exception as e:` 維持。新規 urllib は無いため S310 対象なし。`python -m pytest tests/ -q` で全テストが通ることを確認する（UI ダイアログのロジック部はモックで検証されている既存テストを壊さないこと）。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check . ; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - test: `ruff check .` が exit 0
    - test: `python -m pytest tests/ -q` が exit 0（全テストグリーン）
    - source: `pagefolio/ocr_dialog.py` に裸の `except:` が存在しない
  </acceptance_criteria>
  <done>ruff・全テストがグリーン。クラウド実行ゲートが確定する。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    OCRDialog にクラウド実行前のコスト確認ダイアログ（送信先・ページ数・概算コスト・プライバシー注記3点・毎回・キャンセル可）、env 未設定時のマスク付きセッションキー入力欄（非永続化）、run_parallel の待機中表示を実装。provider を build_provider 経由で中立化し claude 対応。`ruff check .` と `python -m pytest tests/ -q` は自動で通過済み。
  </what-built>
  <how-to-verify>
    1. `cd C:\Users\shdwf\work\project\PageFolio; python pagefolio.py` でアプリを起動し、複数ページの PDF を開く。
    2. 設定でプロバイダを「claude」にする。`ANTHROPIC_API_KEY` を**未設定**のまま OCR（選択 OCR 等）を実行 → マスク（`*`）されたセッションキー入力欄が出ることを確認（成功基準3・D-04）。空のまま進めるとキー未設定エラーが出て実行が始まらないことを確認（成功基準2）。
    3. セッションキーに任意の文字列を入力し進める → コスト確認ダイアログに「送信先: api.anthropic.com」「対象 N ページ」「概算コスト 約 $X」「ページ画像が外部 API に送信されます」「従量課金が発生します」が表示されることを確認（成功基準5・D-12）。
    4. 確認ダイアログで「キャンセル」を押す → OCR が始まらない（進捗が動かない）ことを確認（成功基準5）。
    5. （任意・実キーがあれば）`ANTHROPIC_API_KEY` を設定して再起動 → claude 実行時にセッションキー欄が出ず、コスト確認のみ出ることを確認（D-02/D-03）。実行して 429 等が起きた場合「待機中（リトライ n/3）」が進捗に表示されることを確認（成功基準8・D-15）。
    6. プロバイダを「lmstudio」に戻す → コスト確認ダイアログもキー入力欄も出ず、従来どおり OCR が動くことを確認（成功基準1 後方互換・D-13）。
    7. 設定ファイル `pagefolio_settings.json` を開き、入力した API キー文字列が含まれていないことを確認（成功基準1・最優先）。
  </how-to-verify>
  <resume-signal>問題なければ「approved」、不具合があれば具体的な症状を記載してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| セッションキー入力欄 → app._session_api_keys / 画面表示 | キー入力値の保持と表示マスクの境界（D-04） |
| OCRDialog → Anthropic API（実行ゲート） | クラウド送信の最終ゲート（コスト確認・キャンセル） |
| セッションキー → settings / ログ / OCR結果 / エラー | キーの二次漏洩経路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information Disclosure | セッションキー入力欄の表示 | mitigate | `show="*"` でマスク表示しショルダーハックを防ぐ（D-04）。キー値はログ・OCR結果・エラーに出さない |
| T-05-17 | Information Disclosure | セッションキーの保持先 | mitigate | キーは app._session_api_keys（settings と別オブジェクト）にのみ格納。settings へ入れない（成功基準1/3・D-01）。設定ファイルにキーが無いことを human-verify 手順7 で確認 |
| T-05-18 | Tampering / Cost / Privacy | クラウドへの無確認・大量ページ送信 | mitigate | クラウド時のみ実行前にコスト確認ダイアログ（送信先・ページ数・概算コスト・プライバシー3点）を毎回表示しキャンセル可能にする（成功基準5・OCR-UI-03・Pitfall 8） |
| T-05-19 | Information Disclosure | キー未設定でのクラウド送信 | mitigate | env 未設定 + 入力欄空のとき ocr_api_key_missing を表示し実行しない（成功基準2 と整合・外部送信ゼロ） |
| T-05-20 | DoS | 待機中表示時のスレッド安全 | mitigate | waiting 進捗更新は after(0) 経由でメインスレッドへ委譲（Pitfall 3・直接ウィジェット操作禁止） |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本プランは新規 pip 依存ゼロ（標準ライブラリ + 既存 tkinter）。パッケージインストールなし |
</threat_model>

<verification>
- 成功基準5（クラウドで確認ダイアログ・送信先/ページ数/概算コスト/プライバシー注記・キャンセルで中止）が _confirm_cost ゲートで担保される（human-verify 手順3/4）。
- 成功基準3（セッションキー入力欄・非永続化）がマスク入力欄 + _session_api_keys 格納 + settings 非書き込みで担保される（human-verify 手順2/7）。
- 成功基準8（待機中表示）が on_progress waiting 分岐で担保される（human-verify 手順5）。
- 成功基準1 後方互換（lmstudio で従来どおり）が provider 中立化のゲートスキップで担保される（human-verify 手順6）。
- OCR-UI-03（ローカル/Tesseract には確認を出さない）が _is_cloud_provider ゲートで担保される。
</verification>

<success_criteria>
- claude 実行時のみコスト確認ダイアログ（毎回・キャンセル可・送信先/ページ数/概算/プライバシー3点）が出る。
- env 未設定時のみマスク付きセッションキー入力欄が出て、値が settings/os.environ に書かれない。
- waiting ステータスが「待機中（リトライ n/3）」として表示される。
- lmstudio/off では確認ダイアログ・キー入力欄が出ず後方互換が保たれる。
- `pagefolio_settings.json` にキー文字列が含まれない（成功基準1）。
- `ruff check .` と `pytest tests/` が exit 0。human-verify が approved。
</success_criteria>

<output>
Create `.planning/phases/05-claude-provider-ui/05-05-SUMMARY.md` when done
</output>
