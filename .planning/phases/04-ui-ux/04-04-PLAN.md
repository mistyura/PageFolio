---
phase: 04-ui-ux
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - pagefolio/viewer.py
  - pagefolio/page_ops.py
  - pagefolio/lang.py
  - tests/test_lang_parity.py
  - tests/test_pdf_ops.py
autonomous: true
requirements: [V171-UIUX-02]
must_haves:
  truths:
    - "viewer.py の _show_page_popup がポップアップタイトル・縮小/拡大/閉じるボタン文言を self._t() 経由で表示し、lang='en' で日本語が出ない（i18n 違反 C6 解消）"
    - "page_ops.py の _split_by_range で範囲未入力エラーが showerror + err_title で表示され、直後の範囲形式不正（showerror + err_title）と種別/タイトルが対称になる（C7 解消）"
    - "lang.py の確定未使用 9 キーが削除され、ja/en キー集合が一致したまま維持される"
    - "全 LANG キーが pagefolio/・tests/・plugins/ のどこかで参照されていることを検査する回帰テストが常設され、未使用キー混入を検出する（D-11）"
  artifacts:
    - "pagefolio/viewer.py（_show_page_popup のハードコード文言を LANG キー化）"
    - "pagefolio/page_ops.py（_split_by_range の messagebox 種別/タイトル統一）"
    - "pagefolio/lang.py（未使用 9 キー削除・viewer ポップアップ用新規キー追加・ja/en 同時）"
    - "tests/test_lang_parity.py（未使用キー検出テスト・動的参照許可リスト付き）"
    - "tests/test_pdf_ops.py（分割の範囲未入力エラーが showerror を使う回帰テスト）"
  key_links:
    - "viewer._show_page_popup → self._t('popup_*') 新規 LANG キー"
    - "page_ops._split_by_range → err_title / err_split_no_range（showerror へ統一）"
    - "test_lang_parity D-11 検査 → pagefolio/ tests/ plugins/ の全ソース走査（引用符付きキー literal 照合）"
---

<objective>
エラー表示・文言の一貫性を監査・修正する（V171-UIUX-02・D-09〜D-13／L-5 吸収）。新規発見のハードコード i18n 違反（棚卸し C6・viewer.py の `_show_page_popup`）を LANG キー化し、messagebox 種別/タイトルの非対称（棚卸し C7・page_ops.py の `_split_by_range`）を統一し、確定した未使用 LANG キー 9 件を削除し、未使用キー混入を検出する回帰テストを常設する（D-11）。棚卸しは 4 系統ソースから確定した活き残り（C6/C7 + 未使用キー 9 件）が対象（D-17）で、挙動バグ＋軽微な整理の基準で修正し（D-18）、テスト可能なものは回帰テスト必須・UI 実描画は VERIFICATION.md 手動項目とする（D-20）。本フェーズ（v1.7.1 最終）で棚卸しの文言系活き残りを全件解消し、次マイルストーンへ塩漬けを残さない（D-19）。

Purpose: `lang='en'` でも日本語が出る i18n 違反の除去・エラー種別の一貫性・辞書の未使用キー掃除と再発防止テスト。
Output: LANG キー化した viewer.py・種別統一した page_ops.py・9 キー削除と viewer 新規キーを反映した lang.py・D-11 検出テスト・C7 回帰テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux/04-CONTEXT.md
@.planning/phases/04-ui-ux/04-RESEARCH.md
@.planning/phases/04-ui-ux/04-PATTERNS.md
@.planning/phases/04-ui-ux/04-03-SUMMARY.md
</context>

## Artifacts this phase produces

このプラン（04-04）が新規に作成するシンボル:
- 新規 LANG キー（ja/en 同時）: viewer ポップアップ用（ポップアップタイトル・縮小/拡大/閉じるボタン。例 `popup_page_title`・`popup_zoom_out`・`popup_zoom_in`・`popup_close`）
- 削除 LANG キー（ja/en 双方）: 確定未使用 9 件（下記 read_first の監査表参照。`tesseract_not_installed` は使用中の `tesseract_not_installed_hint` と別キーである点に注意）
- テスト関数（`tests/test_lang_parity.py`）: 全 LANG キーがソース参照されることを検査する未使用キー検出テスト（動的参照許可リスト付き）
- テスト（`tests/test_pdf_ops.py`）: 分割の範囲未入力エラーが `showerror` を使う回帰テスト

<tasks>

<task type="auto">
  <name>Task 1: viewer.py の _show_page_popup ハードコード文言を LANG キー化（C6）</name>
  <files>pagefolio/viewer.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/viewer.py:388-514（_show_page_popup 全体。ハードコード箇所: :407 ポップアップタイトル、:494 縮小ボタン、:499 拡大ボタン、:503 閉じるボタン）
    - pagefolio/viewer.py 内の他 self._t() 使用箇所（LANG 経由ラベル生成の対称パターン）
    - pagefolio/lang.py:60-80 付近（ja/en 辞書の追加箇所・書式）
    - .planning/phases/04-ui-ux/04-RESEARCH.md「棚卸し 生き残り確定表」C6・「Anti-Patterns to Avoid」
    - tests/test_lang_parity.py:13-16（ja/en キー一致）
  </read_first>
  <action>
    `_show_page_popup`（viewer.py:388-514）の 4 箇所のハードコード日本語文言を LANG キー参照へ置き換える。(1) ポップアップタイトル（:407 の現在ページ / 総ページ数を示す文字列）を新規キー `popup_page_title` の `.format(...)`（現在ページ番号・総数のプレースホルダ）へ。(2) 縮小ボタン文言（:494）を `popup_zoom_out` へ。(3) 拡大ボタン文言（:499）を `popup_zoom_in` へ。(4) 閉じるボタン文言（:503・Danger.TButton）を `popup_close` へ。いずれも `self._t(...)` 経由（`_show_page_popup` は `self` を持ち `self._t()` を呼べる）。`lang.py` の ja/en 両辞書へ 4 キーを同一キー名で追加する（ja は絵文字含む従来トーン、en は英語）。`popup_page_title` のプレースホルダ名は ja/en で同一にする。ページラベル（:406 の `page_lbl` の "{i+1} / {n}" 数値のみ）は言語非依存なので変更不要（タイトルのみ LANG 化）。
  </action>
  <verify>
    <automated>python -c "s=open('pagefolio/viewer.py',encoding='utf-8').read(); import re; blk=s[s.index('_show_page_popup'):]; assert 'popup_zoom_out' in blk and 'popup_zoom_in' in blk and 'popup_close' in blk and 'popup_page_title' in blk" && python -c "from pagefolio.lang import LANG; ks={'popup_page_title','popup_zoom_out','popup_zoom_in','popup_close'}; assert ks<=set(LANG['ja']) and ks<=set(LANG['en']); assert set(LANG['ja'])==set(LANG['en'])"</automated>
  </verify>
  <acceptance_criteria>
    - `_show_page_popup` 内のポップアップタイトル・縮小・拡大・閉じるの 4 文言が `self._t("popup_...")` 経由になっている
    - `popup_page_title`/`popup_zoom_out`/`popup_zoom_in`/`popup_close` が ja/en 双方に存在する
    - `popup_page_title` が現在ページ・総数のプレースホルダを持ち、ja/en で同一プレースホルダ名を使う
    - `pytest tests/test_lang_parity.py -x -q` がグリーン（ja/en 一致）
    - `ruff check pagefolio/viewer.py pagefolio/lang.py` がクリーン
  </acceptance_criteria>
  <done>拡大ポップアップのタイトル・ボタン文言が LANG 経由になり、lang='en' で日本語が出ない。</done>
</task>

<task type="auto">
  <name>Task 2: page_ops.py の分割エラー messagebox 種別/タイトルを統一（C7）</name>
  <files>pagefolio/page_ops.py, tests/test_pdf_ops.py</files>
  <read_first>
    - pagefolio/page_ops.py:942-961（_split_by_range。:953-955 の範囲未入力は showinfo+info_title、:957-960 の範囲形式不正は showerror+err_title という非対称）
    - .planning/phases/04-ui-ux/04-RESEARCH.md「Pattern 4」messagebox 種別統一基準・「棚卸し 生き残り確定表」C7
    - tests/test_pdf_ops.py 内の _split_by_range / split 系既存テスト（monkeypatch で simpledialog.askstring / messagebox を差し替える型・test_v150_regression.py:189-215 の分割テストも参考）
  </read_first>
  <action>
    `_split_by_range`（page_ops.py:942-961）の範囲未入力分岐（現在 `messagebox.showinfo(self._t("info_title"), self._t("err_split_no_range"))`）を、直後の範囲形式不正分岐（`messagebox.showerror(self._t("err_title"), self._t("err_split_range").format(n=n))`）と対称になるよう `messagebox.showerror(self._t("err_title"), self._t("err_split_no_range"))` へ変更する（RESEARCH Pattern 4 基準: 入力を正すべきエラーは showerror + err_title）。`err_split_no_range` の文言キー自体は維持（エラー系キー名のまま）。tests/test_pdf_ops.py に回帰テストを追加する: `_split_by_range` を FakeApp/スタブで呼び、`po.simpledialog.askstring` が空文字を返すよう monkeypatch し、`po.messagebox.showerror` が呼ばれ `po.messagebox.showinfo` が呼ばれないことを検証する（呼び出しを記録するスタブ関数を monkeypatch し、showerror の呼び出し引数にエラータイトルキー相当が渡ることを確認）。既存の split 系テスト（test_v150_regression.py の TestTocPreservation 等）を壊さないこと。
  </action>
  <verify>
    <automated>python -c "s=open('pagefolio/page_ops.py',encoding='utf-8').read(); blk=s[s.index('def _split_by_range'):s.index('def _split_by_range')+900]; assert 'showinfo' not in blk; assert blk.count('showerror')>=2" && pytest tests/test_pdf_ops.py -k split -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_split_by_range` の範囲未入力分岐が `messagebox.showerror(self._t("err_title"), self._t("err_split_no_range"))` になり、同メソッド内に `showinfo` が残っていない
    - 範囲未入力・範囲形式不正の両分岐が showerror + err_title で対称になっている
    - tests/test_pdf_ops.py の追加テストが範囲未入力で showerror が呼ばれ showinfo が呼ばれないことを検証し `pytest tests/test_pdf_ops.py -k split -x -q` がグリーン
    - `ruff check pagefolio/page_ops.py tests/test_pdf_ops.py` がクリーン
  </acceptance_criteria>
  <done>分割の範囲未入力エラーが showerror + err_title に統一され、範囲形式不正と対称になり、回帰テストで固定される。</done>
</task>

<task type="auto">
  <name>Task 3: lang.py 確定未使用 9 キーを削除し D-11 未使用キー検出テストを常設</name>
  <files>pagefolio/lang.py, tests/test_lang_parity.py</files>
  <read_first>
    - .planning/phases/04-ui-ux/04-RESEARCH.md「lang.py 未使用キー監査（D-09/D-10・全 374 キー総当たり）」の 9 件の表（キー名と根拠行番号）
    - pagefolio/lang.py（削除対象 9 キーの ja/en 両ブロック該当行。RESEARCH の行番号を起点に該当キーを特定）
    - tests/test_lang_parity.py:1-26（既存 test_lang_keys_parity / format smoke。ここへ D-11 テストを追加）
    - .planning/phases/04-ui-ux/04-RESEARCH.md「Pitfall 3」（動的キー合成は現状ゼロ・許可リスト機構）
  </read_first>
  <action>
    RESEARCH「lang.py 未使用キー監査」表の確定未使用 9 件を ja/en 両辞書から削除する。対象は監査表の 9 キー（off ヒント・未インストール通知・Ollama 取得中・OCR モデル取得系 4 件・圧縮セクション見出し・汎用 warning タイトル）。**重要**: `tesseract_not_installed`（削除対象）は使用中の別キー `tesseract_not_installed_hint`（llm_config.py で使用）と混同しないこと——完全一致（引用符付き literal）で該当キー行のみを削除する。次に tests/test_lang_parity.py へ D-11 の未使用キー検出テストを追加する: `pagefolio/`（lang.py を除く）・`tests/`・`plugins/` の全 `.py` を読み込み、各 LANG キーについて引用符付き literal（`"<key>"` または `'<key>'`）がいずれかのソースに出現するかを検査し、出現しないキーがあれば失敗させる。プレフィックス衝突（`tesseract_not_installed` と `tesseract_not_installed_hint`）を誤判定しないよう、部分文字列一致ではなく引用符で括った完全一致で照合する。動的参照用の許可リスト（`_ALLOWLIST` 集合・現状は空でよいが機構は用意）で除外できるようにし、許可リストに入れた理由をコメントで残す。テスト失敗時メッセージに未使用キー名を含める。
  </action>
  <verify>
    <automated>python -c "from pagefolio.lang import LANG; assert set(LANG['ja'])==set(LANG['en'])" && pytest tests/test_lang_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - RESEARCH 監査表の確定未使用 9 キーが lang.py の ja/en 双方から削除されている
    - `tesseract_not_installed_hint`（使用中）は削除されていない（`python -c "from pagefolio.lang import LANG; assert 'tesseract_not_installed_hint' in LANG['ja']"` が成功）
    - tests/test_lang_parity.py に未使用キー検出テストが追加され、引用符付き完全一致で照合し、許可リスト機構を持つ
    - `pytest tests/test_lang_parity.py -x -q` がグリーン（削除後に未使用キーが 0 件・9 キー削除でこのテストが成立する）
    - `pytest` フルスイートがグリーン（削除キーを参照する箇所が残っていないことの担保）
    - `ruff check pagefolio/lang.py tests/test_lang_parity.py` がクリーン
  </acceptance_criteria>
  <done>確定未使用 9 キーが削除され、未使用キー混入を検出する回帰テストが常設され、フルスイートがグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LANG 辞書 → UI 表示文言 | ユーザーデータではなく表示専用の静的文言。キー削除は settings.json のユーザー値に影響しない |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Denial of Service (機能不全) | 使用中キーの誤削除 | medium | mitigate | 削除は引用符付き完全一致で該当キー行のみ。`tesseract_not_installed_hint` 等プレフィックス衝突を誤削除しないことをテストで固定。削除後フルスイートで KeyError 不在を担保 |
| T-04-02 | Information Disclosure | 文言の情報露出 | low | accept | 追加/変更する文言は UI ラベルのみで機密を含まない |

パッケージインストールなし（stdlib のみ）のため T-04-SC は非該当。
</threat_model>

<verification>
- `pytest tests/test_lang_parity.py tests/test_pdf_ops.py -x -q` がグリーン
- `pytest` フルスイートがグリーン（未使用キー 0・削除キー参照の残存なし）
- `ruff check . && ruff format .` がクリーン
- 拡大ポップアップの実描画（en 表示）は VERIFICATION.md の Manual-Only 項目
</verification>

<success_criteria>
- viewer.py の拡大ポップアップ文言が LANG 経由になり i18n 違反 C6 が解消される（V171-UIUX-02・D-12a）
- 分割エラーの messagebox 種別/タイトルが統一され C7 が解消される（V171-UIUX-02・D-12b/c）
- 確定未使用 9 キーが削除され、未使用キー検出テストが常設される（V171-UIUX-02・D-09/D-11・L-5 吸収）
- ja/en キー一致・フルスイートがグリーン
</success_criteria>

<output>
Create `.planning/phases/04-ui-ux/04-04-SUMMARY.md` when done
</output>
