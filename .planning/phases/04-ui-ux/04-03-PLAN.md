---
phase: 04-ui-ux
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - pagefolio/dialogs/llm_config.py
  - pagefolio/dialogs/settings.py
  - pagefolio/app.py
  - pagefolio/lang.py
  - tests/test_provider_ui.py
autonomous: true
requirements: [V171-UIUX-03, V171-TEST-03]
must_haves:
  truths:
    - "LLMConfigDialog に「全プロバイダ共通の設定」と「選択中プロバイダ固有の設定」を区別する見出しラベルが表示され、1 枚・プロバイダ選択でセクション切替という現行構造は維持される"
    - "LLMConfigDialog の適用が独立トランザクションとして app.settings（メモリ）へ即時反映され、外側 SettingsDialog をキャンセルしても LLM 設定は失われない（ディスクとメモリが食い違わない）"
    - "OCRDialog から開いた LLMConfigDialog の適用経路は本変更で壊れない（on_apply(llm_settings) の呼び出し規約は不変）"
    - "Ollama のモデル取得・接続テストが LM Studio と同型の単一共通ヘルパーへ統合され、従来どおり動作する（二重実装が解消される）"
  prohibitions:
    - statement: "LLMConfigDialog の適用で API キー系の値を llm_settings（永続化対象）へ入れない"
      status: resolved
      verification: "pytest tests/test_provider_ui.py の _apply/セッションキー系テスト、および llm_settings に api_key キーが含まれないことの assert"
  artifacts:
    - "pagefolio/app.py（_apply_llm_settings_live メソッド・_rebuild_ui を伴わない軽量反映）"
    - "pagefolio/dialogs/settings.py（SettingsDialog の on_llm_apply 任意引数と nested on_apply からの即時反映呼び出し）"
    - "pagefolio/dialogs/llm_config.py（共通/固有グルーピング見出し・_probe_ollama_provider 共通ヘルパー）"
    - "pagefolio/lang.py（グルーピング見出しキー・ja/en 同時）"
    - "tests/test_provider_ui.py（ネスト適用トランザクション回帰テスト・Ollama 共通ヘルパーテスト）"
  key_links:
    - "SettingsDialog nested on_apply → app._apply_llm_settings_live（メモリ即時反映・_rebuild_ui なし）"
    - "app._open_settings → SettingsDialog(on_llm_apply=self._apply_llm_settings_live)"
    - "llm_config._fetch_ollama_models / _test_ollama_connection → _probe_ollama_provider(update_combo)"
---

<objective>
SettingsDialog / LLMConfigDialog の項目配置・トランザクション整合を整理し（V171-UIUX-03・D-14/D-15）、Phase 2 から繰り越した Ollama モデル取得/接続テストの二重実装を解消する（V171-TEST-03・棚卸し C2）。LLMConfigDialog は「共通設定」と「プロバイダ固有設定」を見出しで明確に分離し（D-15）、ネストダイアログの適用を独立トランザクション化して外側キャンセル時のディスク/メモリ不整合（棚卸し C4）を解消する（D-14）。C2 は LM Studio 用 `_probe_lm_provider` と同型の共通ヘルパーへ Ollama を統合する（棚卸し対象は Phase 2 繰り越し・CONCERNS.md 由来・新規スキャンの 4 系統から確定・D-17。修正基準は挙動バグ＋軽微整理で大型構造分割は対象外・D-18。テスト可能な修正は回帰テスト必須で UI 実挙動は VERIFICATION.md 手動項目・D-20）。

Purpose: CONCERNS.md「Dialog-Level Settings Synchronization」fragile の解消（C4/C5）と OCR プロバイダ設定 UI の二重実装解消（C2）。
Output: `app.py` の軽量反映メソッド・`settings.py` の即時反映配線・`llm_config.py` のグルーピングと Ollama 共通化・`lang.py` 見出しキー・`test_provider_ui.py` の cascade/Ollama テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux/04-CONTEXT.md
@.planning/phases/04-ui-ux/04-RESEARCH.md
@.planning/phases/04-ui-ux/04-PATTERNS.md
@.planning/phases/04-ui-ux/04-02-SUMMARY.md
@.planning/codebase/CONCERNS.md
</context>

## Artifacts this phase produces

このプラン（04-03）が新規に作成するシンボル:
- メソッド（`PDFEditorApp`）: `_apply_llm_settings_live(self, llm_settings)`（メモリ `self.settings` 即時更新 + 永続化・`_rebuild_ui()` を呼ばない軽量反映）
- 引数（`SettingsDialog.__init__`）: 任意 `on_llm_apply=None`
- メソッド（`LLMConfigDialog`）: `_probe_ollama_provider(self, update_combo)`（`_probe_lm_provider` と同型の Ollama 共通ヘルパー）
- 新規 LANG キー（ja/en 同時）: 共通/固有グルーピングの見出し（例 `llm_config_common_section`・`llm_config_provider_section`）
- テストクラス（`tests/test_provider_ui.py`）: ネスト適用トランザクション回帰・Ollama 共通ヘルパーの 2 クラス

<tasks>

<task type="auto">
  <name>Task 1: LLMConfigDialog に共通/固有グルーピング見出しを追加（D-15）</name>
  <files>pagefolio/dialogs/llm_config.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config.py:94-160（_build 冒頭・プロバイダ選択・固有欄の開始）
    - pagefolio/dialogs/llm_config.py:658-760（effort/temperature/解像度倍率など共通設定の pack 開始位置・scale_row アンカー）
    - pagefolio/dialogs/llm_config.py:964-1048（_on_provider_change の before=self.scale_row 挿入ロジック — 見出し追加時に崩さないこと）
    - .planning/phases/04-ui-ux/04-RESEARCH.md D-15・「Architecture Responsibility Map」
    - tests/test_lang_parity.py:13-16（ja/en キー一致）
  </read_first>
  <action>
    `LLMConfigDialog._build` に 2 つのグループ見出しラベルを追加する。(1)「選択中プロバイダ固有の設定（URL・モデル・APIキー）」見出しを、プロバイダ選択行（llm_config.py:103-152）の後・プロバイダ別セクションフレーム（url_section_frame/ollama_section_frame/runpod/claude/gemini/tesseract の各 `*_section_frame`）が `before=self.scale_row` で挿入される領域の直前に配置する。(2)「全プロバイダ共通の設定（max_tokens・temperature・タイムアウト・プロンプト等）」見出しを、共通設定群（effort_frame/temperature_frame/scale_row 以降の max_tokens/timeout/prompt/concurrency）の先頭に配置する。見出しは既存のセクション区切り（`tk.Frame(..., height=1)` の区切り線 + `tk.Label(..., fg=C["WARNING"], font=self._font(0, "bold"))`）と同型で、新規 LANG キー（`llm_config_common_section` / `llm_config_provider_section`）を参照する。現行の「1 枚・プロバイダ選択でセクション切替」という構造（`_on_provider_change` の pack/pack_forget・`before=self.scale_row`）は一切変更しない（タブ化・スクロール化しない・D-15）。共通見出しは固定表示、固有見出しはプロバイダ別セクションのアンカーとして機能させ、`_on_provider_change` の `before=self.scale_row` 挿入が固有見出しより下に来るよう配置順を保つ。`lang.py` の ja/en 両辞書へ `llm_config_common_section`・`llm_config_provider_section` を同一キーで追加する。
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pagefolio/dialogs/llm_config.py', encoding='utf-8').read())" && python -c "from pagefolio.lang import LANG; assert 'llm_config_common_section' in LANG['ja'] and 'llm_config_common_section' in LANG['en']; assert 'llm_config_provider_section' in LANG['ja'] and 'llm_config_provider_section' in LANG['en']; assert set(LANG['ja'])==set(LANG['en'])"</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/llm_config.py` の `_build` が `llm_config_common_section` と `llm_config_provider_section` の 2 見出しラベルを参照する
    - `_on_provider_change` の `before=self.scale_row` 挿入ロジックが変更されておらず、`pytest tests/test_provider_ui.py -x -q` がグリーン（プロバイダ切替の既存テスト回帰なし）
    - `llm_config_common_section`/`llm_config_provider_section` が ja/en 双方に存在し `test_lang_parity.py` がグリーン
    - `ruff check pagefolio/dialogs/llm_config.py pagefolio/lang.py` がクリーン
  </acceptance_criteria>
  <done>共通設定と固有設定が見出しで区別され、現行のセクション切替構造は維持されている。</done>
</task>

<task type="auto">
  <name>Task 2: ネスト適用を独立トランザクション化（D-14・C4/C5）+ cascade 回帰テスト</name>
  <files>pagefolio/app.py, pagefolio/dialogs/settings.py, tests/test_provider_ui.py</files>
  <read_first>
    - pagefolio/dialogs/settings.py:173-214（_open_llm_config の nested on_apply・_apply・callback フロー）
    - pagefolio/app.py:473-505（_open_settings で SettingsDialog を生成・_apply_settings が self.settings 更新 + _rebuild_ui）
    - pagefolio/app.py:507-517 付近（_rebuild_ui が root.winfo_children を destroy する — nested on_apply から呼ぶと SettingsDialog Toplevel も破棄されるため呼べない）
    - .planning/codebase/CONCERNS.md:139-152, 264-276（Dialog-Level Settings Synchronization / Multi-Dialog Cascade）
    - .planning/phases/04-ui-ux/04-RESEARCH.md「Pattern 3」「Pitfall 5」「Open Questions」1
    - tests/test_provider_ui.py:189-234（SimpleNamespace スタブ + ソース assert の型）
  </read_first>
  <action>
    D-14 を実装し、LLMConfigDialog の適用を外側 SettingsDialog の Apply/Cancel から独立させる。(1) app.py に軽量メソッド `_apply_llm_settings_live(self, llm_settings)` を新設する: `self.settings.update(llm_settings)` でメモリ状態を即時更新し `_save_settings(self.settings)` で永続化するが、`_rebuild_ui()` は呼ばない（nested on_apply から呼ぶと `_rebuild_ui` が `self.root.winfo_children()` を destroy して開いている SettingsDialog Toplevel まで壊すため）。テーマ/フォントは LLM 設定に含まれないので UI 再構築は不要。(2) settings.py の `SettingsDialog.__init__` に後方互換を保つ任意引数 `on_llm_apply=None` を末尾に追加して `self._on_llm_apply` に保持する（既存の位置引数の並び順は変えない・Pitfall 5）。`_open_llm_config` の nested `on_apply(llm_settings)` 内で、現行の `self.current_settings.update(llm_settings)` + `_save_settings(self.current_settings)` に加えて、`self._on_llm_apply` が設定されていれば `self._on_llm_apply(dict(llm_settings))` を呼び、app.settings（メモリ）へ即時反映する。(3) app.py の `_open_settings`（app.py:487）の `SettingsDialog(...)` 呼び出しに `on_llm_apply=self._apply_llm_settings_live` を渡す。OCRDialog 経由の LLMConfigDialog 起動経路（別 on_apply）は変更しない（`on_apply(llm_settings)` の呼び出し規約は不変・Pitfall 5）。(4) test_provider_ui.py に cascade 回帰テストクラスを追加する: SimpleNamespace + `_apply_llm_settings_live` を unbound で呼ぶスタブで「nested on_apply が呼ばれた後に app.settings が新 LLM 値を含む」ことと、「その後に外側キャンセルしても app.settings が旧値へ戻らない」ことを検証する。加えて `_apply_llm_settings_live` が `llm_settings` に含まれない theme/font を破壊しないこと、api_key 系キーが settings へ流入しないことを assert する（成功基準の prohibition 固定）。実 Tk は生成しない。
  </action>
  <verify>
    <automated>python -c "s=open('pagefolio/app.py',encoding='utf-8').read(); assert 'def _apply_llm_settings_live' in s and 'on_llm_apply=self._apply_llm_settings_live' in s" && python -c "s=open('pagefolio/dialogs/settings.py',encoding='utf-8').read(); assert 'on_llm_apply' in s and '_on_llm_apply' in s" && pytest tests/test_provider_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/app.py` に `def _apply_llm_settings_live(self, llm_settings):` が存在し、`_rebuild_ui` を呼ばずに `self.settings` 更新 + `_save_settings` を行う
    - `_open_settings` の `SettingsDialog(...)` 呼び出しに `on_llm_apply=self._apply_llm_settings_live` が渡される
    - `SettingsDialog.__init__` が末尾任意引数 `on_llm_apply=None` を受け取り、nested `on_apply` から `self._on_llm_apply(...)` を呼ぶ
    - test_provider_ui.py に cascade トランザクションのテストクラスが追加され、「nested apply 後に app.settings 反映」「外側キャンセルで旧値へ戻らない」「api_key が settings に入らない」を検証する
    - `pytest tests/test_provider_ui.py -x -q` がグリーン（既存 OCRDialog/コスト系テスト回帰なし）
    - `ruff check pagefolio/app.py pagefolio/dialogs/settings.py tests/test_provider_ui.py` がクリーン
  </acceptance_criteria>
  <done>LLM 設定の適用が独立トランザクション化され、外側キャンセルでも失われず、cascade 回帰テストで固定される。</done>
</task>

<task type="auto">
  <name>Task 3: Ollama モデル取得/接続テストを共通ヘルパーへ統合（C2）</name>
  <files>pagefolio/dialogs/llm_config.py, tests/test_provider_ui.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config.py:1119-1158（_probe_lm_provider(update_combo) と _fetch_models/_test_connection — 統合の型）
    - pagefolio/dialogs/llm_config.py:1160-1206（_fetch_ollama_models / _test_ollama_connection — 重複解消対象。差分は Combobox 反映有無と OllamaProvider 使用のみ）
    - .planning/phases/04-ui-ux/04-RESEARCH.md「棚卸し 生き残り確定表」C2・「Pitfall 4」（未配線 lang キーの扱い）
    - .planning/phases/04-ui-ux/04-PATTERNS.md「pagefolio/dialogs/llm_config.py」
  </read_first>
  <action>
    C2 を解消する。`_probe_lm_provider(update_combo)`（llm_config.py:1120）と同型の共通ヘルパー `_probe_ollama_provider(self, update_combo)` を新設し、`self.ollama_url_var` から URL を取り、空なら `settings_lm_test_fail`、そうでなければ `settings_lm_testing` を表示、`OllamaProvider(url=url, model="").list_models()` を try/except（`ConnectionError, TimeoutError, RuntimeError`）で呼び、`update_combo` が True のとき `self.ollama_model_combo["values"]` へ反映、成功時 `settings_lm_test_ok` を表示する（既存 Ollama 実装のステータス文言 `settings_lm_testing`/`settings_lm_test_ok`/`settings_lm_test_fail` の使い回しをそのまま維持）。`_fetch_ollama_models` は `self._probe_ollama_provider(update_combo=True)`、`_test_ollama_connection` は `self._probe_ollama_provider(update_combo=False)` の 1 行ラッパーに置き換え、旧本体（1161-1206 の重複ロジック）を削除する。未配線の LANG キー `llm_fetching_ollama_models` は本統合では配線せず放置する（Pitfall 4 の設計判断: 削除。実際の削除は 04-04 の未使用キー一括削除で行うため、本プランでは lang.py を触らない）。test_provider_ui.py に `_probe_ollama_provider` の共通化を検証するテストクラスを追加する: OllamaProvider の list_models をモンキーパッチ（またはスタブ）して `update_combo=True/False` で Combobox 反映有無が切り替わること、URL 空でエラーステータスになることを SimpleNamespace スタブ + unbound 呼び出しで検証する（実 Tk 非生成・既存 `_make_dialog_stub` 型を参考にする）。
  </action>
  <verify>
    <automated>python -c "s=open('pagefolio/dialogs/llm_config.py',encoding='utf-8').read(); assert 'def _probe_ollama_provider' in s; assert s.count('def _test_ollama_connection')==1; assert 'self._probe_ollama_provider(update_combo=True)' in s and 'self._probe_ollama_provider(update_combo=False)' in s" && pytest tests/test_provider_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/llm_config.py` に `def _probe_ollama_provider(self, update_combo):` が存在する
    - `_fetch_ollama_models` と `_test_ollama_connection` が `_probe_ollama_provider(update_combo=...)` を呼ぶ 1 行ラッパーになり、旧重複本体が除去されている
    - test_provider_ui.py に `_probe_ollama_provider` を検証するテストクラスが追加され `pytest tests/test_provider_ui.py -x -q` がグリーン
    - lang.py は本プランで変更しない（`llm_fetching_ollama_models` の扱いは 04-04 に委譲）
    - `ruff check pagefolio/dialogs/llm_config.py tests/test_provider_ui.py` がクリーン
  </acceptance_criteria>
  <done>Ollama のモデル取得/接続テストが LM Studio と同型の単一共通ヘルパーへ統合され、二重実装が解消される。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLMConfigDialog の入力（APIキー・URL・モデル）→ app._session_api_keys / app.settings | APIキーはセッションメモリのみ、settings へは無害な設定値のみ。ここで機密分離を維持する |
| ネスト適用 → app.settings（メモリ）+ pagefolio_settings.json（ディスク） | D-14 の即時反映境界。ディスクとメモリの整合を保証する |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Information Disclosure | _apply_llm_settings_live / nested on_apply の settings 書き込み | high | mitigate | `_apply_llm_settings_live` は渡された `llm_settings`（API キーを含まない・LLMConfigDialog._apply が既に分離）のみを settings へ入れる。`_SENSITIVE_KEYS` ガード（settings.py）は不変。api_key が settings に流入しないことを Task 2 のテストで固定 |
| T-04-06 | Tampering | ディスク/メモリ不整合（C4） | medium | mitigate | nested on_apply でディスク保存とメモリ反映を同一トランザクションで実行し、外側キャンセルでも整合を保つ（cascade テストで固定） |
| T-04-07 | Denial of Service | OllamaProvider.list_models のネットワーク例外 | low | mitigate | `_probe_ollama_provider` は `ConnectionError/TimeoutError/RuntimeError` を捕捉しステータス表示に留める（既存挙動維持） |

パッケージインストールなし（stdlib + 既存依存のみ）のため T-04-SC は非該当。
</threat_model>

<verification>
- `pytest tests/test_provider_ui.py tests/test_lang_parity.py -x -q` がグリーン
- `pytest` フルスイートがグリーン
- `ruff check . && ruff format .` がクリーン
- LLMConfigDialog の共通/固有見出し表示・ネスト適用の実挙動目視は VERIFICATION.md の Manual-Only 項目
</verification>

<success_criteria>
- LLMConfigDialog に共通/固有の見出し分離が入り、現行のセクション切替構造は維持される（V171-UIUX-03・D-15）
- LLM 設定の適用が独立トランザクション化され、外側キャンセルでもディスク/メモリが食い違わない（V171-UIUX-03・D-14・C4/C5）
- Ollama のモデル取得/接続テストが単一共通ヘルパーに統合される（V171-TEST-03・C2）
- API キーが settings に流入しない prohibition が回帰テストで固定される
</success_criteria>

<output>
Create `.planning/phases/04-ui-ux/04-03-SUMMARY.md` when done
</output>
