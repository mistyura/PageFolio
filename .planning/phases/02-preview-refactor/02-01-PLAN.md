---
phase: 02-preview-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/viewer.py
  - tests/test_viewer.py
autonomous: true
requirements: [BUG-03, TEST-02]

must_haves:
  truths:
    - "ページ切り替え（_prev_page / _next_page / _zoom / _refresh_all）でプレビューが即時描画され、doc.tobytes() が一切呼ばれない"
    - "_render_preview_pixmap が Tk 非依存の純関数として self.doc[page_idx] から samples/size を返す"
    - "tests/test_viewer.py が monkeypatch で fitz.Document.tobytes 呼び出し 0 を検証し pytest が通る"
  artifacts:
    - path: "pagefolio/viewer.py"
      provides: "純関数 _render_preview_pixmap + 同期化された _show_preview"
      contains: "_render_preview_pixmap"
    - path: "tests/test_viewer.py"
      provides: "BUG-03 回帰テスト（tobytes スパイ + samples 妥当性）"
      contains: "class TestPreviewRender"
  key_links:
    - from: "pagefolio/viewer.py::_show_preview"
      to: "pagefolio/viewer.py::_render_preview_pixmap"
      via: "純関数呼び出し（同期・メインスレッド）"
      pattern: "_render_preview_pixmap\\("
    - from: "tests/test_viewer.py"
      to: "pagefolio/viewer.py::_render_preview_pixmap"
      via: "純関数を軽量スタブにバインドして直接呼び出し"
      pattern: "_render_preview_pixmap"
---

<objective>
BUG-03 を修正し、その回帰テスト（TEST-02）を整備する。

ページ切り替えのたびにドキュメント全体を `self.doc.tobytes()` で直列化してワーカースレッドで再オープンする現方式を撤廃し、メインスレッドで `self.doc[page_idx].get_pixmap()` を同期呼び出しする方式へ変更する（D-01）。レンダリングの中核を Tk 非依存の純関数ヘルパー `_render_preview_pixmap(page_idx, zoom)` に抽出し（D-08）、`tests/test_viewer.py` で `fitz.Document.tobytes` をスパイして呼び出し 0 を検証する（D-09）。

Purpose: 大きな PDF でページ切り替えのたびに走るフルシリアライズ（性能ボトルネック）を排除し、ROADMAP SC-1 / SC-4 を満たす。fitz の Document をスレッドへ渡せない制約も同期化で回避する。
Output: 同期化・純関数抽出済み `pagefolio/viewer.py`、新規 `tests/test_viewer.py`。
</objective>

<artifacts_produced>
## Artifacts this phase produces（このプランの生成物）

- 新規ヘルパー関数: `pagefolio/viewer.py::ViewerMixin._render_preview_pixmap(self, page_idx, zoom)` — Tk 非依存の純関数プレビューレンダリングヘルパー。`(samples: bytes, w: int, h: int)` を返す（戻り値型は executor 裁量・D-08）。
- 修正: `pagefolio/viewer.py::ViewerMixin._show_preview` — 同期化（worker スレッド / doc.tobytes 撤廃）。
- 新規テストファイル: `tests/test_viewer.py`（`class TestPreviewRender`）。
</artifacts_produced>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preview-refactor/02-CONTEXT.md
@.planning/phases/02-preview-refactor/02-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@pagefolio/viewer.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _render_preview_pixmap 純関数ヘルパーを抽出し _show_preview を同期化（BUG-03 / D-01 / D-08）</name>
  <files>pagefolio/viewer.py</files>
  <read_first>
    - pagefolio/viewer.py 全体（特に _show_preview: 41-127、_get_thumb_photo: 155-164）— 正解パターン `_get_thumb_photo` は既にメインスレッド同期 get_pixmap で tobytes 不使用。これをプレビュー用に拡張するのが本質。
    - .planning/phases/02-preview-refactor/02-PATTERNS.md §`pagefolio/viewer.py` — 撤廃対象行（69 doc.tobytes / 83-97 worker / 71-81 placeholder / 65-66 _preview_gen）と維持対象（_apply の矩形影・create_image・scrollregion=125）、例外処理パターン（94-95 と同形の `except Exception as e:` + logger）。
    - .planning/phases/02-preview-refactor/02-CONTEXT.md §decisions D-01/D-02/D-03 — 同期方式採用、単一ページ bytes 案・キャッシュ案は不採用、_preview_gen/プレースホルダー撤去可否は裁量（撤去時は _thumb_gen 等への波及確認必須）。
  </read_first>
  <behavior>
    - _render_preview_pixmap(page_idx, zoom): self.doc[page_idx] を直接参照し `fitz.Matrix(zoom * 1.5, zoom * 1.5)`（プレビュー規約スケール、サムネイルの 0.22 ではない）で get_pixmap(alpha=False) を呼び、(bytes(pix.samples), pix.width, pix.height) を返す。doc.tobytes() を一切呼ばない。self.doc と page_idx, zoom のみに依存し Tk Canvas を一切触らない（軽量スタブにバインドして呼べる）。
    - _show_preview: doc 未開時の空メッセージ描画（現 45-63）は維持。doc がある場合は worker スレッドを使わず、_render_preview_pixmap(page_idx, zoom) を同期呼び出しして得た samples/size を Image.frombytes → ImageTk.PhotoImage に変換し、矩形影・border・create_image・scrollregion 設定（現 _apply 105-125 相当）を実行する。例外時は `except Exception as e:` + logger.debug（裸 except 禁止）。
  </behavior>
  <action>
    `pagefolio/viewer.py` の `ViewerMixin` に純関数メソッド `_render_preview_pixmap(self, page_idx, zoom)` を新設する。中身は現 `worker()`（83-97）の中核から `fitz.open(stream=doc_bytes)` 再オープンを除去し、`page = self.doc[page_idx]` / `mat = fitz.Matrix(zoom * 1.5, zoom * 1.5)` / `pix = page.get_pixmap(matrix=mat, alpha=False)` として `(bytes(pix.samples), pix.width, pix.height)` を返す形に縮約する。`zoom * 1.5` は CLAUDE.md「プレビューは self.zoom * 1.5」規約（サムネイルの 0.22 と混同しない）。

    `_show_preview`（41-127）を同期化する: (1) 69 行 `doc_bytes = self.doc.tobytes()` を削除、(2) 83-97 の `worker()` 定義と 127 行 `threading.Thread(target=worker, daemon=True).start()` を撤廃、(3) `_apply` のロジック（102-125: Image.frombytes / PhotoImage / preview_img_ref 保持 / 矩形影 / border / create_image / scrollregion）を本体に展開し、samples/w/h は `self._render_preview_pixmap(page_idx, zoom)` の戻り値を使う。`page_idx = self.current_page` / `zoom = self.zoom` は維持。例外は `except Exception as e:` + `logger.debug("プレビュー描画例外: %s", e)`（現 94-95 と同形）でラップする。

    71-81 の `"..."` ローディングプレースホルダーと 65-66 の `self._preview_gen += 1` / `gen` / 100 行の `if self._preview_gen != gen` ガードは同期化により不要。撤去する場合は `pagefolio/` 全体で `_preview_gen` への他参照（特にサムネイル側 `_thumb_gen` と混同しないこと）が残らないことを grep で確認してから撤去する（D-03）。撤去判断と範囲は executor 裁量だが、撤去するなら波及確認を必ず行う。`threading` import が他で未使用になる場合は import 行も削除（Ruff F401 回避）。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; python -c "import ast,io; src=open('pagefolio/viewer.py',encoding='utf-8').read(); assert 'self.doc.tobytes()' not in src, 'tobytes still present'; assert '_render_preview_pixmap' in src; ast.parse(src); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('pagefolio/viewer.py',encoding='utf-8').read())"` が exit 0（構文 OK）。
    - `grep -c "self.doc.tobytes()" pagefolio/viewer.py` が 0（撤廃確認、SC-1 の核心）。
    - `grep "_render_preview_pixmap" pagefolio/viewer.py` が 2 件以上（定義 + _show_preview からの呼び出し）。
    - `grep "fitz.open(stream" pagefolio/viewer.py` が 0 件（ワーカー再オープン撤廃）。
    - `_render_preview_pixmap` 内に `zoom * 1.5` が存在し、`0.22` は存在しない（プレビュー規約スケール）。
    - `grep -n "except:" pagefolio/viewer.py` が 0 件（裸 except 禁止）。
    - `_preview_gen` を撤去した場合: `grep -rn "_preview_gen" pagefolio/` が 0 件。残した場合: 同期描画パスが `_preview_gen` ガードでブロックされないこと（戻り値を直接描画）。
  </acceptance_criteria>
  <done>
    _show_preview が doc.tobytes() / worker スレッド / fitz.open(stream=) を一切使わず、純関数 _render_preview_pixmap を同期呼び出しして即時描画する。scrollregion 後処理と矩形影描画は維持。例外は logger 付き except Exception で担保。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_viewer.py で tobytes 不使用と samples 妥当性を検証（TEST-02 / D-09）</name>
  <files>tests/test_viewer.py</files>
  <read_first>
    - tests/test_pdf_ops.py（1-60）— `Test<Feature>` クラス構成・命名・fitz 直接検証の方針（Tkinter 非依存）。
    - tests/conftest.py — `sample_pdf_doc`（43-53、3 ページ fitz.Document）フィクスチャを再利用する。
    - pagefolio/viewer.py の `_render_preview_pixmap`（Task 1 で新設）— テスト対象の純関数シグネチャと戻り値。
    - .planning/phases/02-preview-refactor/02-PATTERNS.md §`tests/test_viewer.py` — テスト構造パターン、monkeypatch スパイ、samples 長 == w*h*3 検証、軽量スタブにバインドして呼ぶ前提。
    - .planning/phases/02-preview-refactor/02-CONTEXT.md §decisions D-08/D-09 — 純関数を直接テスト（Tk root 不要）、tobytes スパイ + samples 妥当性の 2 点検証。
  </read_first>
  <behavior>
    - test_render_does_not_call_tobytes: `monkeypatch.setattr(fitz.Document, "tobytes", spy)` で呼び出し回数をカウントし、_render_preview_pixmap を sample_pdf_doc に対して呼んだ後 called == 0 を assert する（SC-1）。純関数を `doc` 属性だけ持つ軽量スタブ（または ViewerMixin インスタンスの __get__ バインド）にバインドして Tk root なしで呼ぶ。
    - test_render_returns_valid_samples: _render_preview_pixmap の戻り samples が bytes で、長さが w * h * 3（RGB・alpha=False）に等しいこと、w > 0 / h > 0 を assert する（D-09）。
  </behavior>
  <action>
    `tests/test_viewer.py` を新規作成する。`import fitz` / `import pytest` と、対象純関数を呼ぶための最小バインド（`types.SimpleNamespace(doc=sample_pdf_doc)` に `ViewerMixin._render_preview_pixmap.__get__(stub)` でバインドする等、Tk root を生成しない方法。具体的なバインド方法は executor 裁量）を用意する。

    `class TestPreviewRender` を定義し 2 メソッドを実装する: (1) `test_render_does_not_call_tobytes(self, sample_pdf_doc, monkeypatch)` — `orig = fitz.Document.tobytes` を退避し呼び出しカウント用 `spy` を `monkeypatch.setattr(fitz.Document, "tobytes", spy)` で差し込み、`_render_preview_pixmap(0, 1.0)` 相当を呼んだ後 `assert called["n"] == 0`。(2) `test_render_returns_valid_samples(self, sample_pdf_doc)` — 戻り値 `(samples, w, h)` を取り、`assert isinstance(samples, (bytes, bytearray))` / `assert len(samples) == w * h * 3` / `assert w > 0 and h > 0`。

    `sample_pdf_doc` フィクスチャ（conftest.py）をそのまま使う。Tk 依存（Canvas / root / PhotoImage）はテストに持ち込まない。`tests/**/*.py` は S101 免除のため `assert` を直接使ってよい。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; python -m pytest tests/test_viewer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_viewer.py -x` が exit 0（2 テスト pass）。
    - `tests/test_viewer.py` に `class TestPreviewRender` と `monkeypatch.setattr(fitz.Document, "tobytes"` が存在する。
    - `tests/test_viewer.py` に Tk import（`import tkinter` / `ImageTk` / `Canvas`）が存在しない（純関数テストのため不要）。
    - `len(samples) == w * h * 3` を検証する assert が存在する。
    - `python -m pytest -q` 全体が exit 0（既存テストも全通、回帰なし）。
  </acceptance_criteria>
  <done>
    tests/test_viewer.py が _render_preview_pixmap 経路で doc.tobytes() が呼ばれないことと、get_pixmap で妥当な samples（長さ w*h*3）が得られることを Tk 非依存で検証し、pytest 全体が緑。
  </done>
</task>

<task type="auto">
  <name>Task 3: Ruff・pytest 全通とプレビュー手動確認の最終ゲート</name>
  <files>pagefolio/viewer.py, tests/test_viewer.py</files>
  <read_first>
    - CLAUDE.md §変更時のチェックリスト・§作業フロー（Ruff 必須・pytest 必須・裸 except 禁止・# type: ignore 無断使用禁止）。
    - pagefolio/viewer.py（Task 1 の結果）, tests/test_viewer.py（Task 2 の結果）。
  </read_first>
  <action>
    `ruff check . && ruff format .` を実行し、Task 1/2 で生じた未使用 import（同期化で不要になった `threading` 等）や整形差分を解消する。`pyproject.toml` / `ruff.toml` は編集しない（CLAUDE.md 禁止事項）。続けて `python -m pytest -q` を実行し全テストが通ることを確認する。`# type: ignore` を新規追加していないこと、裸 `except:` がないことを確認する。最後に `python -c "import ast; ast.parse(open('pagefolio/viewer.py',encoding='utf-8').read())"` で構文確認する（CLAUDE.md チェックリスト準拠）。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; ruff check . ; if ($LASTEXITCODE -eq 0) { ruff format --check . ; if ($LASTEXITCODE -eq 0) { python -m pytest -q } }</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check .` が exit 0（lint エラーなし）。
    - `ruff format --check .` が exit 0（フォーマット済み）。
    - `python -m pytest -q` が exit 0（test_viewer.py 含む全テスト pass）。
    - `grep -rn "# type: ignore" pagefolio/viewer.py tests/test_viewer.py` が 0 件（無断使用なし）。
  </acceptance_criteria>
  <done>
    Ruff lint/format が通り、pytest 全体が緑。新規 lint 違反・型無視・裸 except なし。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （新規なし） | 本プランはデスクトップ PDF ツールの内部レンダリング経路の同期化リファクタであり、新規の外部入力・ネットワーク・認証境界を一切追加しない。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | viewer.py プレビュー描画パスの後方互換 | mitigate | 純関数抽出後も scrollregion・矩形影・例外 logger を維持し、ページ遷移（_prev/_next/_zoom/_refresh_all）から見た描画挙動を保つ。TEST-02 で tobytes 不使用と samples 妥当性を回帰検証。 |
| T-02-02 | Denial of Service | _preview_gen 撤去時の stale 結果 / スレッド競合 | mitigate | 同期化により stale 結果問題自体が消滅（ワーカースレッド廃止）。撤去する場合は `_thumb_gen` への波及がないことを grep 確認（D-03）。 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし（既存 fitz/Pillow のみ使用）。Package Legitimacy Gate 不要。 |
</threat_model>

<verification>
- `grep -c "self.doc.tobytes()" pagefolio/viewer.py` == 0（SC-1）
- `python -m pytest tests/test_viewer.py -x` exit 0（TEST-02・SC-4）
- `python -m pytest -q` 全通（回帰なし・SC-4）
- `ruff check . && ruff format --check .` exit 0
</verification>

<success_criteria>
- ページ切り替え時に doc.tobytes() が呼ばれず page.get_pixmap() 直接呼び出しに変更済み（ROADMAP SC-1 / BUG-03）
- _render_preview_pixmap が Tk 非依存の純関数として抽出され、_show_preview が同期呼び出しする
- tests/test_viewer.py（TEST-02）が tobytes 不使用と samples 妥当性を検証し pytest 全通（ROADMAP SC-4）
</success_criteria>

<output>
Create `.planning/phases/02-preview-refactor/02-01-SUMMARY.md` when done
</output>
