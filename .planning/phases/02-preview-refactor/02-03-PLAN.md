---
phase: 02-preview-refactor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/dialogs/__init__.py
  - pagefolio/dialogs/about.py
  - pagefolio/dialogs/settings.py
  - pagefolio/dialogs/llm_config.py
  - pagefolio/dialogs/plugin.py
  - pagefolio/dialogs/merge.py
  - pagefolio/dialogs.py
autonomous: true
requirements: [REFAC-01]

must_haves:
  truths:
    - "pagefolio/dialogs/ サブパッケージが存在し、from pagefolio.dialogs import AboutDialog, SettingsDialog, PluginDialog, MergeOrderDialog, MergeResizeDialog, LLMConfigDialog がすべて動作する"
    - "旧 dialogs.py（単一ファイル 1191 行）が削除され、6 クラスが責務別ファイルに移設されている"
    - "各 dialog ファイルは自分が使う import のみを持ち、テーマ色 C[...] とフォント font_func 参照を壊さない"
  artifacts:
    - path: "pagefolio/dialogs/__init__.py"
      provides: "後方互換の再エクスポート集約"
      contains: "from pagefolio.dialogs.about import AboutDialog"
    - path: "pagefolio/dialogs/about.py"
      provides: "AboutDialog"
      contains: "class AboutDialog"
    - path: "pagefolio/dialogs/merge.py"
      provides: "MergeOrderDialog + MergeResizeDialog（同居）"
      contains: "class MergeResizeDialog"
  key_links:
    - from: "pagefolio/__init__.py"
      to: "pagefolio/dialogs/__init__.py"
      via: "from pagefolio.dialogs import (AboutDialog, MergeOrderDialog, MergeResizeDialog, PluginDialog, SettingsDialog)"
      pattern: "from pagefolio.dialogs import"
    - from: "pagefolio/dialogs/__init__.py"
      to: "pagefolio/dialogs/{about,settings,llm_config,plugin,merge}.py"
      via: "サブモジュールからの再エクスポート"
      pattern: "from pagefolio.dialogs\\.\\w+ import"
---

<objective>
REFAC-01: 1,191 行・6 クラスが混在する `pagefolio/dialogs.py` を `pagefolio/dialogs/` サブパッケージに分割する。

D-06 の分割案をそのまま採用: `about.py`（AboutDialog）, `settings.py`（SettingsDialog）, `llm_config.py`（LLMConfigDialog）, `plugin.py`（PluginDialog）, `merge.py`（MergeOrderDialog + MergeResizeDialog 同居）, `__init__.py`（後方互換の再エクスポート集約）。最重要制約は `pagefolio/__init__.py:12-18` の `from pagefolio.dialogs import (...)` 公開表面を一切壊さないこと（D-07）。コードは原則そのまま移設しロジック変更はしない。

Purpose: 1,191 行の混在モジュールを責務別に分割し主要モジュールを管理可能な行数にする（ROADMAP SC-2）。サブパッケージ `__init__.py` の再エクスポートで既存 import を吸収する。
Output: 新規 `pagefolio/dialogs/` サブパッケージ（6 ファイル）、旧 `pagefolio/dialogs.py` の削除。
</objective>

<artifacts_produced>
## Artifacts this phase produces（このプランの生成物）

- 新規サブパッケージ: `pagefolio/dialogs/__init__.py` — AboutDialog / SettingsDialog / LLMConfigDialog / PluginDialog / MergeOrderDialog / MergeResizeDialog の後方互換再エクスポート。
- 新規モジュール: `pagefolio/dialogs/about.py`（AboutDialog）, `settings.py`（SettingsDialog）, `llm_config.py`（LLMConfigDialog）, `plugin.py`（PluginDialog）, `merge.py`（MergeOrderDialog + MergeResizeDialog）。
- 削除: 旧 `pagefolio/dialogs.py`（単一ファイル）。
</artifacts_produced>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preview-refactor/02-CONTEXT.md
@.planning/phases/02-preview-refactor/02-PATTERNS.md
@pagefolio/dialogs.py
@pagefolio/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: dialogs/ サブパッケージへ 6 クラスを責務別ファイルに移設（REFAC-01 / D-06）</name>
  <files>pagefolio/dialogs/about.py, pagefolio/dialogs/settings.py, pagefolio/dialogs/llm_config.py, pagefolio/dialogs/plugin.py, pagefolio/dialogs/merge.py</files>
  <read_first>
    - pagefolio/dialogs.py 全体（1191 行）— クラス位置: AboutDialog(24-97) / SettingsDialog(98-263) / LLMConfigDialog(264-670) / PluginDialog(671-866) / MergeOrderDialog(867-1044) / MergeResizeDialog(1045-1191)、共通 import（1-16 行: logging, os, tkinter, messagebox/ttk, fitz, constants の APP_VERSION/LANG/PLUGINS_DIR/C, ocr の MAX_OCR_MAX_TOKENS/fetch_lm_studio_models, plugins の _get_plugins_dir, settings の _current_font_size, logger）。
    - .planning/phases/02-preview-refactor/02-PATTERNS.md §`pagefolio/dialogs/{...}.py` — クラス→ファイル対応表、各ファイルが使う import の抜粋指針（about=APP_VERSION/LANG/C のみ・messagebox/fitz 不要、llm_config=MAX_OCR_MAX_TOKENS/fetch_lm_studio_models 集約、plugin=PLUGINS_DIR/_get_plugins_dir、merge=fitz 使用）、ダイアログ共通パターン（grab_set モーダル・親基準センタリング・C[...] / font_func 参照維持）。
    - .planning/phases/02-preview-refactor/02-CONTEXT.md §decisions D-06/D-07 — 分割案そのまま採用、MergeOrder+MergeResize 同居、公開 import 表面維持必須、シンボル割り当て細部は executor 裁量。
  </read_first>
  <action>
    `pagefolio/dialogs/` ディレクトリを作成し、以下 5 ファイルへ `dialogs.py` の各クラスを コードそのまま移設する（ロジック変更なし）:
    - `about.py` ← AboutDialog（元 24-97）
    - `settings.py` ← SettingsDialog（元 98-263）
    - `llm_config.py` ← LLMConfigDialog（元 264-670）
    - `plugin.py` ← PluginDialog（元 671-866）
    - `merge.py` ← MergeOrderDialog（元 867-1044）+ MergeResizeDialog（元 1045-1191）を同居

    各ファイル冒頭の import は そのファイルが実際に使う分だけ を持つ（元 dialogs.py 1-16 から抜粋）。指針: about.py は `from pagefolio.constants import APP_VERSION, LANG, C` のみ（messagebox/fitz 不要）。llm_config.py は `from pagefolio.ocr import MAX_OCR_MAX_TOKENS, fetch_lm_studio_models` を集約。plugin.py は `from pagefolio.constants import ... PLUGINS_DIR` と `from pagefolio.plugins import _get_plugins_dir`。merge.py は `import fitz`（リサイズ計算で使用）。各ファイルに `import logging` + `logger = logging.getLogger(__name__)` を置く。`tkinter as tk` / `from tkinter import messagebox, ttk` は使う側にのみ。不要 import は Ruff F401 で落ちるので各ファイルで実使用シンボルのみに絞る。

    ライセンスヘッダー（dialogs.py 冒頭 1-3 行と同形の Copyright / MIT License）を各ファイル先頭に付ける。テーマ色 `C["KEY"]` 参照とフォント `font_func`（`self._font` 相当）参照、`grab_set()` モーダル化・親基準センタリングのロジックは移設時に一切変更しない（CLAUDE.md 規約）。`__init__.py` はこの Task では作らない（Task 2 で作成）— ただしこの段階では旧 `dialogs.py` がまだ存在するため `pagefolio.dialogs` は旧ファイルを指す。新ファイルの import は `from pagefolio.dialogs.about import AboutDialog` のように直接サブモジュール指定で確認する。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; python -c "import ast; [ast.parse(open(f'pagefolio/dialogs/{m}.py',encoding='utf-8').read()) for m in ['about','settings','llm_config','plugin','merge']]; print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/about.py` に `class AboutDialog`、`settings.py` に `class SettingsDialog`、`llm_config.py` に `class LLMConfigDialog`、`plugin.py` に `class PluginDialog`、`merge.py` に `class MergeOrderDialog` と `class MergeResizeDialog` の両方が存在する。
    - 5 ファイルすべてが `python -c "import ast; ast.parse(...)"` で構文 OK（exit 0）。
    - about.py に `messagebox` の import が存在しない（PATTERNS の指針: about は messagebox/fitz 不要）。
    - merge.py に `import fitz` が存在する（リサイズ計算で使用）。
    - 各ファイルに裸 `except:` が存在しない（CLAUDE.md 禁止事項）。
    - 各ファイルでテーマ色がハードコード hex ではなく `C[` 経由で参照されている（移設で壊していない）。
  </acceptance_criteria>
  <done>
    6 クラスが 5 ファイルに責務別移設され、各ファイルは実使用 import のみを持ち構文 OK。テーマ色・フォント・モーダル化ロジックは無変更。
  </done>
</task>

<task type="auto">
  <name>Task 2: dialogs/__init__.py 再エクスポート作成と旧 dialogs.py 削除（REFAC-01 / D-07）</name>
  <files>pagefolio/dialogs/__init__.py, pagefolio/dialogs.py</files>
  <read_first>
    - pagefolio/__init__.py（12-18 行）— `from pagefolio.dialogs import (AboutDialog, MergeOrderDialog, MergeResizeDialog, PluginDialog, SettingsDialog)` の公開表面（この 5 つは必ず再エクスポートが必要、LLMConfigDialog も公開する）。
    - .planning/phases/02-preview-refactor/02-PATTERNS.md §`pagefolio/dialogs/__init__.py` — 再エクスポートパターン（pagefolio/__init__.py 12-18 の形を踏襲）、各行 `# noqa: F401` 付与、最低 5 クラス + LLMConfigDialog を公開。
    - Task 1 で作成した 5 ファイル（about/settings/llm_config/plugin/merge）— 再エクスポート元。
  </read_first>
  <action>
    旧 `pagefolio/dialogs.py`（単一ファイル 1191 行）を削除する（全クラスは Task 1 で移設済み）。続けて `pagefolio/dialogs/__init__.py` を新規作成し、5 サブモジュールから 6 クラスを再エクスポートする: `from pagefolio.dialogs.about import AboutDialog  # noqa: F401` / `from pagefolio.dialogs.settings import SettingsDialog  # noqa: F401` / `from pagefolio.dialogs.llm_config import LLMConfigDialog  # noqa: F401` / `from pagefolio.dialogs.plugin import PluginDialog  # noqa: F401` / `from pagefolio.dialogs.merge import MergeOrderDialog, MergeResizeDialog  # noqa: F401`。各行に `# noqa: F401`（未使用 import 抑止）を付ける（pagefolio/__init__.py と統一）。ライセンスヘッダーと簡潔な docstring を付ける。

    削除順序に注意: ディレクトリ `pagefolio/dialogs/` とファイル `pagefolio/dialogs.py` は同名のため、`__init__.py` を含むパッケージが優先されるよう、旧 `dialogs.py` を確実に削除してからパッケージ import を確認する。`pagefolio/__init__.py` の 12-18 行（`from pagefolio.dialogs import (...)` の 5 クラス要求）は再エクスポートで吸収されるため pagefolio/__init__.py は編集しない。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; python -c "from pagefolio.dialogs import AboutDialog, SettingsDialog, PluginDialog, MergeOrderDialog, MergeResizeDialog, LLMConfigDialog; import pagefolio.dialogs as d, os; assert os.path.isdir(os.path.dirname(d.__file__)); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from pagefolio.dialogs import AboutDialog, SettingsDialog, PluginDialog, MergeOrderDialog, MergeResizeDialog, LLMConfigDialog"` が exit 0（全 6 クラス再エクスポート・後方互換）。
    - `python -c "import pagefolio.dialogs as d; print(d.__file__)"` が `dialogs/__init__.py`（サブパッケージ）を指す。
    - 旧 `pagefolio/dialogs.py` ファイルが存在しない。
    - `pagefolio/dialogs/__init__.py` の各再エクスポート行に `# noqa: F401` が付いている。
    - `python -c "import pagefolio"` が exit 0（パッケージ全体 import で pagefolio/__init__.py の dialogs import が解決）。
  </acceptance_criteria>
  <done>
    旧 dialogs.py が削除され dialogs/ サブパッケージが優先され、__init__.py が 6 クラスを再エクスポート。`from pagefolio.dialogs import ...` の公開表面が無変更で動作。
  </done>
</task>

<task type="auto">
  <name>Task 3: 全体 import 解決・Ruff（F401 含む）・pytest 全通の最終ゲート</name>
  <files>pagefolio/dialogs/__init__.py, pagefolio/dialogs/about.py, pagefolio/dialogs/settings.py, pagefolio/dialogs/llm_config.py, pagefolio/dialogs/plugin.py, pagefolio/dialogs/merge.py</files>
  <read_first>
    - CLAUDE.md §変更時のチェックリスト（Ruff 必須・pytest 必須・裸 except 禁止・pyproject.toml/ruff.toml 編集禁止）。
    - Task 1/2 の結果（dialogs/ サブパッケージ 6 ファイル）。
  </read_first>
  <action>
    `ruff check . && ruff format .` を実行し、Task 1 の移設で各ファイルに残った未使用 import（F401）や import 順序（I）・整形差分を解消する。各 dialog ファイルは実使用シンボルのみを持つよう F401 違反を 0 にする。`# noqa: F401` は `dialogs/__init__.py` の再エクスポート行にのみ付与し、各 dialog 実装ファイルには付けない（実使用 import のみ）。`pyproject.toml` / `ruff.toml` は編集しない。続けて `python -c "import pagefolio"` および `from pagefolio import app, ui_builder, page_ops`（dialogs を利用する側）が解決することを確認し、`python -m pytest -q` で全テスト（特に既存の dialog/import 関連）が通り回帰がないことを確認する。`# type: ignore` を新規追加していないこと、裸 `except:` がないことを確認する。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; ruff check . ; if ($LASTEXITCODE -eq 0) { ruff format --check . ; if ($LASTEXITCODE -eq 0) { python -c "import pagefolio; from pagefolio import app, ui_builder, page_ops" ; if ($LASTEXITCODE -eq 0) { python -m pytest -q } } }</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check .` が exit 0（F401 未使用 import 違反 0・import 順序 OK）。
    - `ruff format --check .` が exit 0。
    - `python -c "import pagefolio; from pagefolio import app, ui_builder, page_ops"` が exit 0（dialogs 利用側も解決）。
    - `python -m pytest -q` が exit 0（既存テスト全通・回帰なし）。
    - `grep -rn "# type: ignore" pagefolio/dialogs/` が 0 件。各 dialog 実装ファイル（__init__.py 以外）に `# noqa: F401` が 0 件（実使用 import のみ）。
  </acceptance_criteria>
  <done>
    Ruff lint/format 通過（F401 含む）、pagefolio 全体および dialogs 利用側の import 解決、pytest 全通。型無視・裸 except なし。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （新規なし） | 本プランはダイアログクラスの物理ファイル分割（コード移設 + 再エクスポート）であり、新規の外部入力・ネットワーク・認証境界を一切追加しない。LLMConfigDialog 経由の LM Studio API 呼び出しは既存挙動を維持（新規攻撃面なし）。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | dialogs 後方互換 import 表面 | mitigate | dialogs/__init__.py で 6 クラスを再エクスポートし `from pagefolio.dialogs import ...` を維持。Task 3 で pagefolio 全体 import を検証し回帰防止。 |
| T-02-07 | Tampering | 旧 dialogs.py とサブパッケージの名前衝突 | mitigate | 旧 dialogs.py を確実に削除してから __init__.py を持つパッケージを優先させる。`d.__file__` が dialogs/__init__.py を指すことを検証。 |
| T-02-08 | Information Disclosure | LLMConfigDialog 移設時の API トークン/エンドポイント取り扱い変更 | accept | ロジック無変更でコードそのまま移設。既存の LM Studio ローカル API（localhost）挙動を保つため新規露出なし。 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし（純粋な内部分割）。Package Legitimacy Gate 不要。 |
</threat_model>

<verification>
- `python -c "from pagefolio.dialogs import AboutDialog, SettingsDialog, PluginDialog, MergeOrderDialog, MergeResizeDialog, LLMConfigDialog"` exit 0（SC-2・後方互換）
- `python -c "import pagefolio.dialogs as d; print(d.__file__)"` が dialogs/__init__.py を指す（サブパッケージ化）
- 旧 dialogs.py が存在しない
- `ruff check . && ruff format --check .` exit 0（F401 含む）
- `python -m pytest -q` 全通（回帰なし・SC-4）
</verification>

<success_criteria>
- pagefolio/dialogs/ サブパッケージが存在し、既存の `from pagefolio.dialogs import ...` import が動作する（ROADMAP SC-2 / REFAC-01）
- 6 クラスが責務別 5 ファイルに移設され旧 dialogs.py が削除されている
- テーマ色・フォント・モーダル化ロジックは無変更で、Ruff lint/format と pytest が全通
</success_criteria>

<output>
Create `.planning/phases/02-preview-refactor/02-03-SUMMARY.md` when done
</output>
