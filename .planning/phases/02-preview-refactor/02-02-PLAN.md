---
phase: 02-preview-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/themes.py
  - pagefolio/lang.py
  - pagefolio/constants.py
autonomous: true
requirements: [REFAC-02]

must_haves:
  truths:
    - "from pagefolio.constants import APP_VERSION, LANG, THEMES, C が分割後もそのまま動作する（後方互換 import 表面を維持）"
    - "C は themes.py に定義された単一オブジェクトであり、settings._apply_theme の C.update(THEMES[...]) による in-place 更新で識別子が保たれる"
    - "themes.py / lang.py は constants.py を import しない葉モジュール（依存方向は constants から themes/lang への一方向）"
  artifacts:
    - path: "pagefolio/themes.py"
      provides: "THEMES 辞書 + 可変 C dict（in-place 更新前提）"
      contains: "C = dict(THEMES"
    - path: "pagefolio/lang.py"
      provides: "LANG 辞書（純データ・依存なし）"
      contains: "LANG = {"
    - path: "pagefolio/constants.py"
      provides: "APP_VERSION/SETTINGS_FILE/PLUGINS_DIR/拡張子定数 + themes/lang からの再エクスポート"
      contains: "from pagefolio.themes import"
  key_links:
    - from: "pagefolio/constants.py"
      to: "pagefolio/themes.py"
      via: "再エクスポート import（識別子保持）"
      pattern: "from pagefolio.themes import .*C"
    - from: "pagefolio/constants.py"
      to: "pagefolio/lang.py"
      via: "再エクスポート import"
      pattern: "from pagefolio.lang import LANG"
    - from: "pagefolio/settings.py::_apply_theme"
      to: "pagefolio/themes.py::C"
      via: "C.update(THEMES[resolved]) による in-place 更新（再エクスポート経由で同一オブジェクト）"
      pattern: "C.update"
---

<objective>
REFAC-02: 711 行の `pagefolio/constants.py` を `themes.py`（THEMES / C）・`lang.py`（LANG 辞書）・`constants.py`（バージョン・拡張子定数 + 再エクスポート）に分割する。

最重要制約は「後方互換 import 表面を一切壊さない」こと（D-07）。全 11 モジュールが `from pagefolio.constants import ...` を使っており、特に `C` は `settings.py:95` の `C.update(THEMES[resolved])` で in-place 更新されるため、`constants.py` で `from pagefolio.themes import THEMES, C` / `from pagefolio.lang import LANG` を再エクスポートして同一オブジェクト識別子を保持する（D-04 / D-05）。

Purpose: 711 行の混在モジュールを責務別に分割し主要モジュールを管理可能な行数にする（ROADMAP SC-3）。再エクスポート方式により既存 import の物理変更をゼロに抑える。
Output: 新規 `pagefolio/themes.py`・`pagefolio/lang.py`、再エクスポート化された `pagefolio/constants.py`。
</objective>

<artifacts_produced>
## Artifacts this phase produces（このプランの生成物）

- 新規モジュール: `pagefolio/themes.py` — `THEMES` 辞書 + 可変 `C = dict(THEMES["dark"])`（in-place 更新前提・識別子保持）。
- 新規モジュール: `pagefolio/lang.py` — `LANG` 辞書（約 650 行・純データ・依存なし）。
- 修正: `pagefolio/constants.py` — `APP_VERSION` / `SETTINGS_FILE` / `PLUGINS_DIR` / `SUPPORTED_EXTENSIONS` / `IMAGE_EXTENSIONS` を保持し、`THEMES` / `C` / `LANG` を themes/lang から再エクスポート。
</artifacts_produced>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preview-refactor/02-CONTEXT.md
@.planning/phases/02-preview-refactor/02-PATTERNS.md
@pagefolio/constants.py
@pagefolio/__init__.py
@pagefolio/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: themes.py / lang.py を新設し THEMES・C・LANG を移設（REFAC-02 / D-04 / D-06）</name>
  <files>pagefolio/themes.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/constants.py 全体 — THEMES(8-43)、`C = dict(THEMES["dark"])`(44)、LANG(59-711) の正確な範囲。
    - pagefolio/settings.py の `_apply_theme`（C.update(THEMES[resolved]) の in-place 更新箇所）と import 行 `from pagefolio.constants import SETTINGS_FILE, THEMES, C`（10 行）。
    - .planning/phases/02-preview-refactor/02-PATTERNS.md §`pagefolio/themes.py` / §`pagefolio/lang.py` — そのまま移設・C の識別子保持・循環 import 禁止（themes/lang は constants を import しない葉モジュール）。
    - .planning/phases/02-preview-refactor/02-CONTEXT.md §decisions D-04/D-05/D-06 — C は themes.py に定義し再エクスポート、一斉書き換え案は不採用、分割案は REQUIREMENTS.md 通り採用。
  </read_first>
  <action>
    `pagefolio/themes.py` を新規作成し、`constants.py` の `THEMES`（8-43 行の dark/light テーマ定義）と `C = dict(THEMES["dark"])`（44 行）を コードそのまま移設する。コメント「実行時に _apply_theme() で更新」も維持する。`C` は再代入・再生成してはならない（`C = dict(THEMES["dark"])` の 1 回のみ生成し、以降は in-place 更新前提）。themes.py は `constants.py` を import しない（循環 import 禁止・葉モジュール）。

    `pagefolio/lang.py` を新規作成し、`constants.py` の `LANG` 辞書（59-711 行・約 650 行）を コードそのまま移設する。LANG は純データで他モジュールに依存しないため import 不要。lang.py も `constants.py` を import しない。

    ライセンスヘッダーコメント（constants.py 冒頭 1-3 行と同形の Copyright / MIT License）を両ファイル先頭に付ける（既存ファイル群と統一）。移設したシンボルは constants.py からはまだ削除しない（Task 2 で再エクスポート化する際にまとめて整理）。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; python -c "from pagefolio.themes import THEMES, C; from pagefolio.lang import LANG; assert isinstance(C, dict) and 'BG_DARK' in C; assert 'dark' in THEMES and 'light' in THEMES; assert isinstance(LANG, dict); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from pagefolio.themes import THEMES, C"` が exit 0。
    - `python -c "from pagefolio.lang import LANG"` が exit 0。
    - `python -c "from pagefolio.themes import C; assert C['BG_DARK'] == '#1a1a2e'"` が exit 0（dark テーマ既定値）。
    - themes.py / lang.py のどちらにも `from pagefolio.constants` を含む行が存在しない（循環 import なし・葉モジュール）。
    - themes.py に `C = dict(THEMES` を含む行がちょうど 1 行（C は 1 回のみ生成）。
  </acceptance_criteria>
  <done>
    themes.py に THEMES / C、lang.py に LANG が移設され、どちらも constants.py を import せず単体 import で成功する。C の生成は 1 箇所のみ。
  </done>
</task>

<task type="auto">
  <name>Task 2: constants.py を再エクスポート化し後方互換 import 表面を維持（REFAC-02 / D-04 / D-07）</name>
  <files>pagefolio/constants.py</files>
  <read_first>
    - pagefolio/constants.py 全体 — 残す定数（APP_VERSION:47 / SETTINGS_FILE:50 / PLUGINS_DIR:51 / SUPPORTED_EXTENSIONS:54 / IMAGE_EXTENSIONS:58）と、themes/lang へ移した部分（THEMES/C/LANG）。
    - pagefolio/__init__.py（9 行 `from pagefolio.constants import APP_VERSION, LANG, THEMES, C`）— 維持必須の公開表面。
    - 全モジュールの import 依存（app.py:12 LANG/SUPPORTED_EXTENSIONS/C、dnd.py/ui_builder.py/viewer.py C、file_ops.py/page_ops.py 拡張子、ocr_dialog.py LANG/C、plugins.py PLUGINS_DIR、settings.py:10 SETTINGS_FILE/THEMES/C、dialogs.py:13 APP_VERSION/LANG/PLUGINS_DIR/C）— これらが分割後も constants 経由で解決される必要がある。
    - .planning/phases/02-preview-refactor/02-PATTERNS.md §`pagefolio/constants.py`（再エクスポート化）— 再エクスポート + 残す定数のスケルトン、`# noqa: F401` 付与、循環 import 注意（一方向依存）。
  </read_first>
  <action>
    `pagefolio/constants.py` を再エクスポート構成に書き換える。冒頭で `from pagefolio.themes import THEMES, C  # noqa: F401` と `from pagefolio.lang import LANG  # noqa: F401` を import する（識別子保持・F401 抑止）。`constants.py` には `APP_VERSION = "v1.2.6"` / `SETTINGS_FILE` / `PLUGINS_DIR` / `SUPPORTED_EXTENSIONS` / `IMAGE_EXTENSIONS` を残す（現値そのまま）。Task 1 で themes.py / lang.py へ移したため、constants.py からは元の `THEMES` 定義ブロック・`C = dict(...)` 行・`LANG` 定義ブロックを削除する（再エクスポート import に置換）。

    依存方向は constants から themes/lang への一方向。`themes.py` / `lang.py` が `constants.py` を import していないこと（Task 1 で担保済み）を前提に循環を起こさない。ライセンスヘッダーと既存の docstring は維持する。`settings.py:10` の `from pagefolio.constants import SETTINGS_FILE, THEMES, C` は再エクスポートにより物理変更不要で動作する（D-04）— settings.py は編集しない。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; python -c "import pagefolio.themes as t; from pagefolio.constants import APP_VERSION, LANG, THEMES, C, SETTINGS_FILE, PLUGINS_DIR, SUPPORTED_EXTENSIONS, IMAGE_EXTENSIONS; assert C is t.C; print(APP_VERSION)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from pagefolio.constants import APP_VERSION, LANG, THEMES, C, SETTINGS_FILE, PLUGINS_DIR, SUPPORTED_EXTENSIONS, IMAGE_EXTENSIONS"` が exit 0（全シンボル解決・後方互換維持）。
    - `python -c "import pagefolio.themes as t; from pagefolio.constants import C; assert C is t.C"` が exit 0（C の識別子保持・D-04 の核心）。
    - constants.py に `from pagefolio.themes import` と `from pagefolio.lang import LANG` を含む行が存在する。
    - constants.py に `THEMES = {` を含む行が存在しない（定義は themes.py へ移設済み・再エクスポートのみ）。
    - `python -c "import pagefolio"` が exit 0（パッケージ全体 import で循環なし）。
  </acceptance_criteria>
  <done>
    constants.py が themes/lang から THEMES/C/LANG を再エクスポートし、残す定数を保持。`from pagefolio.constants import APP_VERSION, LANG, THEMES, C` が動作し C の識別子が themes.C と一致。
  </done>
</task>

<task type="auto">
  <name>Task 3: 全モジュール import 解決・テーマ更新の識別子保持・Ruff/pytest 全通の検証</name>
  <files>pagefolio/themes.py, pagefolio/lang.py, pagefolio/constants.py</files>
  <read_first>
    - pagefolio/settings.py の `_apply_theme`（C.update(THEMES[resolved]) の in-place 更新）— 分割後も C が同一オブジェクトであることを動作確認する対象。
    - CLAUDE.md §変更時のチェックリスト（Ruff 必須・pytest 必須・pyproject.toml/ruff.toml 編集禁止）。
    - Task 1/2 の結果（themes.py, lang.py, constants.py）。
  </read_first>
  <action>
    `python -c "import pagefolio"` および `python -c "from pagefolio import app, settings, dialogs, viewer, ui_builder, dnd, page_ops, file_ops, plugins, ocr_dialog"` 相当で全モジュールが分割後の constants 経由で import 解決されることを確認する。`settings._apply_theme` を呼んでも C が同一オブジェクトのまま更新される（再代入されない）ことを `id(C)` 比較で検証する。続けて `ruff check . && ruff format .` を実行し、移設に伴う import 順・未使用 import（F401）・整形差分を解消する（`# noqa: F401` は再エクスポート行に付与済み）。`pyproject.toml` / `ruff.toml` は編集しない。最後に `python -m pytest -q` で全テストが通る（既存の settings/constants 関連テストに回帰がない）ことを確認する。CLAUDE.md チェックリストに従い `開発履歴.md` への追記と APP_VERSION 同期は executor 裁量（本プランは定数値を変更しないため APP_VERSION は据え置き）。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio"; python -c "import pagefolio; from pagefolio.constants import C as c1; from pagefolio.settings import _apply_theme; before=id(c1); _apply_theme('light'); from pagefolio.constants import C as c2; assert id(c2)==before, 'C identity broke after theme apply'; print('ok')"; if ($LASTEXITCODE -eq 0) { ruff check . ; if ($LASTEXITCODE -eq 0) { ruff format --check . ; if ($LASTEXITCODE -eq 0) { python -m pytest -q } } }</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pagefolio"` が exit 0（全サブモジュール import で循環・解決エラーなし）。
    - `_apply_theme` 呼び出し前後で `id(C)` が一致（in-place 更新・識別子保持・D-04）。
    - `ruff check .` が exit 0、`ruff format --check .` が exit 0。
    - `python -m pytest -q` が exit 0（既存テスト全通・回帰なし）。
    - constants.py の物理行数が大幅に減少している（LANG/THEMES 移設により概ね 60 行未満）。
  </acceptance_criteria>
  <done>
    分割後も全モジュールの import が解決し、テーマ適用で C の識別子が保たれる。Ruff lint/format 通過、pytest 全通。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （新規なし） | 本プランは定数モジュールの物理分割（純データ移設 + 再エクスポート）であり、新規の外部入力・ネットワーク・認証境界を一切追加しない。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | constants 後方互換 import 表面 | mitigate | 再エクスポート方式（D-04/D-07）で `from pagefolio.constants import ...` を維持。Task 3 で全サブモジュール import 解決を検証し回帰を防止。 |
| T-02-04 | Tampering | C dict の識別子破壊（テーマ切替不能化） | mitigate | C を themes.py で 1 回のみ生成し再代入禁止。`id(C)` 一致を _apply_theme 前後で検証（anti-pattern「C を別オブジェクトに作り替え」を遮断）。 |
| T-02-05 | Denial of Service | themes/lang ↔ constants の循環 import | mitigate | 依存方向を constants → {themes, lang} の一方向に固定。葉モジュールが constants を import しないことを grep 相当で確認。 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし（純粋な内部分割）。Package Legitimacy Gate 不要。 |
</threat_model>

<verification>
- `python -c "from pagefolio.constants import APP_VERSION, LANG, THEMES, C"` exit 0（SC-3・後方互換）
- `id(C)` が _apply_theme 前後で一致（D-04・識別子保持）
- `python -c "import pagefolio"` exit 0（循環なし）
- `ruff check . && ruff format --check .` exit 0
- `python -m pytest -q` 全通（回帰なし・SC-4）
</verification>

<success_criteria>
- constants.py が lang.py / themes.py に分割され、既存の `C["BG_DARK"]` 等の参照が動作する（ROADMAP SC-3 / REFAC-02）
- C は単一オブジェクトを保持し _apply_theme の in-place 更新でテーマ切替が機能する
- 全モジュールの `from pagefolio.constants import ...` が物理変更なしで解決し pytest 全通
</success_criteria>

<output>
Create `.planning/phases/02-preview-refactor/02-02-SUMMARY.md` when done
</output>
