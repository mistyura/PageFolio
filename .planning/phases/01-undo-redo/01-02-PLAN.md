---
phase: 01-undo-redo
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pagefolio/app.py
  - pagefolio/file_ops.py
  - pagefolio/page_ops.py
autonomous: true
requirements: [REFAC-03]
must_haves:
  truths:
    - "_undo_stack と _redo_stack の両方が collections.deque(maxlen=MAX_UNDO) である"
    - "Undo スタックへの追加と上限超過時の削除が O(1) で完了する（手動 pop(0) が存在しない）"
    - "deque 化後も _do_insert の直近エントリ後追い書き込み（[-1] アクセス）が成立する"
  artifacts:
    - path: "pagefolio/app.py"
      provides: "_undo_stack/_redo_stack の deque(maxlen=MAX_UNDO) 初期化"
      contains: "deque(maxlen=self.MAX_UNDO)"
    - path: "pagefolio/file_ops.py"
      provides: "手動 pop(0) トリム削除後の _save_undo"
  key_links:
    - from: "pagefolio/app.py::__init__"
      to: "collections.deque"
      via: "_undo_stack/_redo_stack の初期化を deque へ"
      pattern: "deque\\(maxlen"
    - from: "pagefolio/page_ops.py::_do_insert"
      to: "_undo_stack[-1]"
      via: "deque でも O(1) な末尾参照で挿入総数を書き戻し"
      pattern: "_undo_stack\\[-1\\]"
---

<objective>
`_undo_stack` / `_redo_stack` の両方を `collections.deque(maxlen=MAX_UNDO)` 化し（REFAC-03 / D-06）、`_save_undo` の手動 `pop(0)` トリムおよび `_do_merge_resize` 側の手動トリムを削除する。これにより Undo スタックへの追加と上限超過時の削除が O(1) で完了し、メモリ上限が deque の `maxlen` で明示・一貫化される。

Purpose: 成功基準#2「Undo スタックへの追加と上限超過時の削除が O(1) で完了する（deque 使用）」を満たす。`list.pop(0)` の O(n) を排除。
Output: app.py の deque 初期化、file_ops.py の手動トリム削除、page_ops.py の手動トリム削除と `[-1]` 末尾参照の deque 互換確認。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-undo-redo/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/01-undo-redo/01-01-SUMMARY.md
</context>

<artifacts_produced>
## このプランが作成・変更するシンボル

**pagefolio/app.py（変更）**
- `__init__` 内 `self._undo_stack = []` / `self._redo_stack = []`（line 83-84）を `collections.deque(maxlen=self.MAX_UNDO)` へ置換。
- ファイル先頭に `from collections import deque`（または `import collections`）を追加。
- `MAX_UNDO = 20`（line 33）は維持（deque の maxlen ソースとして参照）。

**pagefolio/file_ops.py（変更）**
- `_save_undo` 内 `if len(self._undo_stack) > self.MAX_UNDO: self._undo_stack.pop(0)`（line 59-60）を削除（deque maxlen が自動トリム）。
- `_undo`/`_redo` の `.append`/`.pop()`/`.clear()` は deque でそのまま動作（変更不要、確認のみ）。

**pagefolio/page_ops.py（変更）**
- `_do_merge_resize` 内に残る手動トリム（01-01 で op 別デルタ化済みなら既に消えている可能性あり。残存する `if len(self._undo_stack) > self.MAX_UNDO: pop(0)` を削除）。
- `_do_insert` の `self._undo_stack[-1]["data"][1] = total`（line 344）は deque でも O(1) で動作することを確認（変更不要）。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: _undo_stack/_redo_stack を deque(maxlen=MAX_UNDO) 化し手動トリムを削除</name>
  <files>pagefolio/app.py, pagefolio/file_ops.py, pagefolio/page_ops.py</files>
  <read_first>
    - pagefolio/app.py:30-34（`MAX_UNDO = 20` クラス属性）, :82-84（`self._undo_stack = []` / `self._redo_stack = []` 初期化、import 群の位置）
    - pagefolio/file_ops.py:58-61（`_save_undo` 末尾の `append`→手動 `pop(0)` トリム→`_redo_stack.clear()`）
    - pagefolio/file_ops.py:63-93（`_undo`/`_redo` の `.append`/`.pop()`。deque で互換）
    - pagefolio/page_ops.py:332-360（`_do_insert` の `_undo_stack[-1]["data"][1] = total`。deque 末尾参照は O(1)）
    - pagefolio/page_ops.py:401-457（`_do_merge_resize`。01-01 で op 別化済みなら手動トリムは消えているはず。残存有無を確認）
    - .planning/phases/01-undo-redo/01-CONTEXT.md（D-06: 両スタックを deque(maxlen) 化、手動 pop(0) 削除）
    - .planning/phases/01-undo-redo/01-01-SUMMARY.md（01-01 後の file_ops.py/page_ops.py の最新形）
  </read_first>
  <action>
    `pagefolio/app.py` のトップレベル import に `from collections import deque` を追加し、`__init__` の `self._undo_stack = []` / `self._redo_stack = []` を `self._undo_stack = deque(maxlen=self.MAX_UNDO)` / `self._redo_stack = deque(maxlen=self.MAX_UNDO)` へ置換する（D-06、両スタック）。`pagefolio/file_ops.py` の `_save_undo` 末尾にある `if len(self._undo_stack) > self.MAX_UNDO: self._undo_stack.pop(0)` を削除する（deque の maxlen が自動で先頭を破棄）。`_redo_stack.clear()` と `.append`/`.pop()` は deque でそのまま動作するため変更しない。`pagefolio/page_ops.py` の `_do_merge_resize` に手動 `pop(0)` トリムが残存していれば削除する。`_do_insert` の `self._undo_stack[-1]["data"][1] = total` は deque でも O(1) で成立するため変更不要だが、末尾エントリへの後追い書き込みが deque で機能することを read_first で確認する。`_close_file`/`_open_pdf_path`/`_do_open_merged` などの `_undo_stack.clear()` / `_redo_stack.clear()` は deque でも有効なため変更不要。
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pagefolio/app.py',encoding='utf-8').read()); ast.parse(open('pagefolio/file_ops.py',encoding='utf-8').read()); ast.parse(open('pagefolio/page_ops.py',encoding='utf-8').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/app.py` に `from collections import deque`（または `import collections`）が存在する。
    - `pagefolio/app.py` の `_undo_stack`/`_redo_stack` 初期化が `deque(maxlen=` を用いている（grep `deque(maxlen=self.MAX_UNDO)` が 2 件ヒット）。
    - `pagefolio/file_ops.py` に `self._undo_stack.pop(0)` が存在しない（grep ヒット 0）。
    - `pagefolio/page_ops.py` に手動トリム `self._undo_stack.pop(0)` が存在しない（grep ヒット 0）。
    - 3 ファイルとも `ast.parse` が exit 0。
  </acceptance_criteria>
  <done>両スタックが deque(maxlen=MAX_UNDO) になり、全手動 pop(0) トリムが削除された状態。</done>
</task>

<task type="auto">
  <name>Task 2: lint・既存テスト確認と開発履歴・バージョン更新</name>
  <files>pagefolio/constants.py, 開発履歴.md</files>
  <read_first>
    - pagefolio/constants.py:47（`APP_VERSION`。01-01 で `v1.2.3` 済みなら `v1.2.4` へパッチ更新）
    - 開発履歴.md（最新エントリの記法）
    - CLAUDE.md「変更時のチェックリスト」「禁止事項」「言語ルール」
  </read_first>
  <action>
    `ruff check . && ruff format .` を通す。`APP_VERSION` を直前値からパッチ更新（01-01 後が `v1.2.3` なら `v1.2.4`）し、`開発履歴.md` に「Undo/Redo スタックの deque(maxlen=MAX_UNDO) 化による O(1) 化（REFAC-03）」を日本語で追記する。README.md にバージョンバッジがあれば同期する（無ければスキップ）。
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check .` がエラー 0、`ruff format --check .` が差分 0。
    - `pagefolio/constants.py` の `APP_VERSION` が直前値からパッチ番号 +1。
    - `開発履歴.md` に deque 化の日本語エントリが追加されている。
    - `python -m pytest tests/ -q` が exit 0。
  </acceptance_criteria>
  <done>lint クリーン・既存テスト全通・バージョンと履歴が更新された状態。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ローカルファイル → fitz パーサ | 唯一の外部入力。ネットワーク・認証・PII なし |
| Undo/Redo デルタ（deque 内 bytes） | プロセス内メモリに閉じる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | deque maxlen によるメモリ上限 | mitigate | `maxlen=MAX_UNDO` で上限を明示・有界化。従来の list より上限挙動が一貫し DoS 面が改善 |
| T-02-02 | Tampering | deque 末尾後追い書き込み（_do_insert の [-1]） | mitigate | deque の `[-1]` は O(1) かつ list と同義。書き戻しが破綻しないことを read_first と既存テストで確認 |
| T-02-SC | Tampering | pip パッケージ install | n/a | 新規 install なし（collections は標準ライブラリ）。Package Legitimacy Gate 不要 |

**No high-severity threats — local-only desktop app, no network/auth/PII.** ASVS L1 該当の高 severity 脅威なし。
</threat_model>

<verification>
- `_undo_stack`/`_redo_stack` が `collections.deque` のインスタンスとして初期化されている。
- 手動 `pop(0)` トリムがコードベース全体から消えている。
- `python -m pytest tests/ -q` 全通、`ruff check . && ruff format --check .` クリーン。
</verification>

<success_criteria>
- 成功基準#2: 両スタックが deque(maxlen=MAX_UNDO) で、追加・上限超過削除が O(1)。
- 手動トリムロジックが全廃され、deque の maxlen が単一の上限管理点になる。
- 既存テスト全通、lint クリーン、バージョン・履歴更新済み。
</success_criteria>

<output>
Create `.planning/phases/01-undo-redo/01-02-SUMMARY.md` when done
</output>
