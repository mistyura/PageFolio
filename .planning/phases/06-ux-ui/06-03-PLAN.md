---
phase: 06-ux-ui
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - pagefolio/file_ops.py
  - tests/test_pdf_ops.py
  - 開発履歴.md
  - .planning/PROJECT.md
  - .planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md
autonomous: true
requirements: [V180-QA-04]

must_haves:
  truths:
    - "insert→undo→redo→undo（2回目）でページ数と各ページ内容が正しく往復し、挿入ページが重複しない（D-17）"
    - "insert_redo 修正は _restore_state の insert_redo ブロックのみに限定され、他 op（delete/delete_redo/page_edit/insert/insert_undo 等）の対称性を壊さない（D-17）"
    - "insert→undo→redo→undo(2回目) の4手往復回帰テストが tests/test_pdf_ops.py に存在しグリーンである（D-17）"
    - "開発履歴.md の PageFolio 時代エントリ（v1.3.0〜v1.7.4）が git タグ履歴・APP_VERSION 変更履歴・MILESTONES.md と突合され、不一致があれば修正・なければ整合済みと記録される（D-14）"
    - "旧 PDF Editor 時代の同名バージョン見出し（新旧2回出現）は現状維持され、監査記録に意図的共存として明記される（D-15）"
    - "突合結果の監査記録（確認範囲・判定根拠）がフェーズ成果物として残り、PROJECT.md の V16-D-04 ステータスが解消済みへ更新される（D-16）"
  artifacts:
    - ".planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md（開発履歴突合の監査記録）"
    - "tests/test_pdf_ops.py の TestInsertUndoRedo へ追加する4手往復テストメソッド"
  key_links:
    - "file_ops._restore_state の insert_redo ブロックが delete_redo（354-358）と対称なアクション（削除）になっている"
    - "PROJECT.md Key Decisions の V16-D-04 が『⚠️ Revisit』から解消済みへ更新され 06-CHANGELOG-AUDIT.md を参照する"
  prohibitions:
    - "旧 PDF Editor 時代（リブランディング前）の開発履歴エントリを改変しない（歴史的記録の保全）"
    - "insert_redo 以外の undo/redo op（delete/delete_redo/page_edit/bulk_crop/insert/insert_undo/merge 系）の対称性・データ整合性を壊さない"
    - "_apply_inverse や _dispose_state の Blob 解放ロジックを変更しない（二重解放は発生しておらず修正範囲外）"
---

<objective>
開発履歴.md の版番・日付整合を完了させ（V180-QA-04・V16-D-04 残課題の解消）、あわせて Phase 5 から持ち越した Core Value 直撃バグ（insert→undo→redo→undo でページが重複する insert_redo 非対称復元バグ）を修正する（D-17）。突合先は git タグ履歴・APP_VERSION 変更履歴・MILESTONES.md（D-14）。旧 PDF Editor 時代エントリは現状維持（D-15）。既に整合済みなら監査記録を残して完了扱いとし PROJECT.md の V16-D-04 ステータスを解消済みへ更新する（D-16）。insert_redo 修正は delete_redo 対称パターンに倣い、修正範囲を _restore_state の insert_redo ブロックのみに限定する（D-17）。

Purpose: 「Undo/Redo が正しく動作する」という Core Value を守った状態で、版番表記も整合させて v1.8.0 を締められる状態にする。
Output: file_ops.py の insert_redo 修正・test_pdf_ops.py の4手往復テスト・06-CHANGELOG-AUDIT.md 監査記録・PROJECT.md の V16-D-04 更新。

このプランは Wave 2（06-01 完了後）。理由: pagefolio/file_ops.py を 06-01（保存系トースト化）と共有するため、同一ファイルの並行編集を避け 06-01 の後に実行する。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-ui/06-CONTEXT.md
@.planning/phases/06-ux-ui/06-RESEARCH.md
@.planning/phases/06-ux-ui/06-PATTERNS.md
@.planning/phases/05-blob-shortcutsdialog/deferred-items.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
本プランが新規作成するシンボル・ファイル:
- `.planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md`（新規監査記録ファイル）
- `tests/test_pdf_ops.py` の `TestInsertUndoRedo` クラスへ追加する新規テストメソッド（insert→undo→redo→undo 4手往復・命名は test_undo_stress.py の test_double_release_chain_delete_undo_redo_undo に倣った insert 版）
- 既存 `pagefolio/file_ops.py._restore_state()` の `insert_redo` ブロックの挙動変更（新規シンボルではなく既存ブロックの修正）
</artifacts_this_phase_produces>

<flagged_assumptions>
- 決定論的 edge 分類器は V180-QA-04 の日本語要件テキストに適用できず `unclassified/unresolved` 判定だった。本プランの受入基準は CONTEXT.md の D-14〜D-17 と ROADMAP Phase 6 Success Criteria #3 から導出した（flagged assumption・silent drop していない）。
- RESEARCH は git タグ・MILESTONES.md・開発履歴.md 本文の突合で「現時点で不一致ゼロ」と判定済み（D-16 の『既に整合済み』パスに該当する見込み）。ただし実装時に file_ops.py が他コミットで変更されていないか・突合結果が変わっていないかを再確認する（RESEARCH Valid until 注記）。
- D-17（要件外の折り込みバグ修正）は V180-QA 要件 ID を持たないが、ユーザー確定スコープとして本クローズアウトプラン（requirements: V180-QA-04）に同梱する。Core Value（Undo/Redo 正当性）直撃のため v1.8.0 出荷前に解消する。
</flagged_assumptions>

<review_dispositions>
06-REVIEWS.md（Antigravity・単独レビュー）— 本プランは Overall Risk = **LOW**・実行をブロックする HIGH/MEDIUM 懸念ゼロ。actionable findings（すべて Severity: LOW）の対応:

| # | Finding | 種別 | Disposition | 反映先/根拠 |
|---|---------|------|-------------|------------|
| R5 | insert_redo 修正時、`state["data"]` が `[(page_i, page_bytes), ...]` 形式である前提の型整合に注意（降順削除時はタプルからインデックスのみを展開し `doc.delete_page(page_i)` へ渡す） | Concern | **Incorporate**（既定計画で降順展開を指定済み・受入基準で型整合を明示固定） | Task 1: `targets = sorted([page_i for page_i, _ in state["data"]], reverse=True)` でタプルを (page_i, _) にアンパックしインデックスのみ抽出、タプルそのものを delete_page へ渡さないことを action と受入基準へ明示 |
| R6 | insert で 4 手目 undo バグが見落とされたのと同様に、`duplicate`/`merge`/`merge_resize` 等の他ページ構造変更 op でも do→undo→redo→undo の 4 手往復テストを水平展開する | Suggestion | **Defer（明示・非 silent）** | 本フェーズは「仕上げを軽く保つ」（Phase 3〜5 一貫方針・CONTEXT.md）クローズアウトであり、スコープは insert_redo バグ（D-17）に限定される。他 op への 4 手往復テスト水平展開は、insert_redo と同様に**新規の潜在バグを v1.8.0 締め直前に顕在化させるリスク**があり、クローズアウトの範囲を膨張させる。よって本フェーズでは実施せず、次マイルストーンの回帰テスト強化候補として繰り越す（下記 deferred_follow_up に記録。Core Value 直撃が判明した場合のみ前倒し） |
</review_dispositions>

<deferred_follow_up>
本フェーズでは実施せず次マイルストーンへ繰り越す項目（レビュー R6・明示的 defer）:
- `duplicate` / `merge` / `merge_resize` 等の他ページ構造変更 op に対する do→undo→redo→undo の 4 手往復回帰テストの水平展開。insert_redo と同型の非対称復元バグが潜在していないかを検証する。本フェーズ実行時、06-03-SUMMARY.md の「次セッションへの申し送り」または STATE.md の Deferred Items へこの項目を転記し、追跡可能な状態でクローズすること（silent drop 禁止）。
</deferred_follow_up>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: insert_redo 非対称復元バグ修正 + 4手往復回帰テスト（D-17）</name>
  <files>pagefolio/file_ops.py, tests/test_pdf_ops.py</files>
  <read_first>
    - .planning/phases/05-blob-shortcutsdialog/deferred-items.md（必読・再現コードと原因推定）
    - pagefolio/file_ops.py（_restore_state 内: delete_redo 354-358 の対称正実装・insert 391-394・insert_undo 395-400・insert_redo 401-407 のバグブロック。_apply_inverse の insert_redo 分岐 297-304 は変更不要）
    - tests/test_pdf_ops.py（TestInsertUndoRedo: 637-757 の _make_fake_app と既存 test_insert_undo_redo_roundtrip・_page_digest ヘルパー）
    - tests/test_undo_stress.py（test_double_release_chain_delete_undo_redo_undo の命名前例・_make_stress_app 57 行）
    - .planning/phases/06-ux-ui/06-RESEARCH.md（Pitfall 1・Code Examples）
  </read_first>
  <behavior>
    - insert（1ページ以上を位置Xに挿入）→ undo → redo → undo（2回目）の順で操作すると、最終的にページ数が元に戻り（重複しない）、各ページ digest が挿入前と一致する。修正前は2回目の undo でページが重複していた（例: 3→4→3→4→本来3のところ5）。
  </behavior>
  <action>
    まず tests/test_pdf_ops.py の TestInsertUndoRedo クラスに、insert→undo→redo→undo（4手）の往復正当性を検証する新規テストメソッドを追加する（既存 test_insert_undo_redo_roundtrip の3手を4手目まで延長した形。_make_fake_app と _page_digest を流用し、命名は test_undo_stress.py の test_double_release_chain_delete_undo_redo_undo に倣った insert 版とする）。挿入前の全ページ digest を記録し、4手後に len(doc) が元の枚数へ戻り after == before の digest 一致であることを assert する（修正前は5ページになり失敗する形にする＝先に RED を確認）。次に pagefolio/file_ops.py の _restore_state() 内 elif op == "insert_redo": ブロック（401-407）を、delete_redo（354-358）の対称実装に倣って「再挿入（doc.insert_pdf）」から「削除（doc.delete_page）」へ修正する: state["data"] の page_i を降順ソートして doc.delete_page(page_i) を反復する（昇順インデックスを降順削除しインデックスずれを防止）。修正は insert_redo ブロックのみに限定し、_apply_inverse（297-304）・_dispose_state・他 op（delete/delete_redo/page_edit/insert/insert_undo/merge 系）は変更しない（D-17・Blob 解放は正しく二重解放は発生しない）。裸の except を使わない。既存の insert 系テスト（test_insert_undo_restores_page_count / test_insert_undo_restores_content / test_insert_undo_redo_roundtrip）が引き続きグリーンであることを確認する。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py::TestInsertUndoRedo -x -q && pytest tests/test_undo_stress.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - file_ops.py の _restore_state の insert_redo ブロックが doc.delete_page（降順削除）へ変更され、doc.insert_pdf での再挿入をしていない
    - insert_redo ブロックが state["data"]（[(page_i, page_bytes), ...] 形式）を (page_i, _) にアンパックしてインデックスのみを抽出し（targets = sorted([page_i for page_i, _ in state["data"]], reverse=True)）、タプルそのものを delete_page へ渡していない（レビュー R5・型整合）
    - _apply_inverse の insert_redo 分岐（297-304）・_dispose_state は未変更（grep 差分で insert_redo ブロック以外の変更なし）
    - tests/test_pdf_ops.py::TestInsertUndoRedo に insert→undo→redo→undo 4手往復テストが追加され exit 0
    - 既存の insert 系3テストと test_undo_stress.py が引き続きグリーン
    - ruff check . && ruff format . が通る
  </acceptance_criteria>
  <done>insert の4手 undo/redo 往復でページが重複せず内容が一致する。修正範囲は insert_redo ブロックのみで他 op の対称性は不変。</done>
</task>

<task type="auto">
  <name>Task 2: 開発履歴.md 整合監査と監査記録の作成（D-14/D-15/D-16）</name>
  <files>開発履歴.md, .planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md</files>
  <read_first>
    - 開発履歴.md（PageFolio 時代エントリ v1.3.0〜v1.7.4・旧 PDF Editor 時代セクションの見出し）
    - .planning/MILESTONES.md（マイルストーン・ポイントリリースの正史）
    - .planning/phases/06-ux-ui/06-RESEARCH.md（D-14 監査結果サマリ・現時点で不一致ゼロの判定）
    - .planning/phases/06-ux-ui/06-CONTEXT.md（D-14/D-15/D-16）
  </read_first>
  <action>
    git タグ履歴（git tag --list）・pagefolio/constants.py の APP_VERSION 変更履歴（git log）・.planning/MILESTONES.md・開発履歴.md 本文の4者を突合する。PageFolio 時代の全エントリ（v1.3.0〜v1.7.4）について日付・版番・内容の不一致を検出する。V16-D-04 の痕跡（v1.6.0 期の一時 v1.7.0 バンプ由来の表記）を特定し、不一致があれば開発履歴.md を修正する。旧 PDF Editor 時代の同名バージョン見出し（v1.7.0/v1.6.0 が新旧2回出現）は現状維持し改変しない（D-15）。突合結果を .planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md に日本語で記録する: 確認範囲（突合した4ソースと対象バージョン範囲）・判定根拠（各バージョンのタグ/日付/内容の一致確認結果）・修正の有無（修正した場合は差分、整合済みなら『不一致なし』の根拠）・旧 PDF Editor 時代エントリを意図的共存として据え置いた旨（D-15）を明記する。RESEARCH の事前判定（不一致ゼロ見込み）を鵜呑みにせず、実際にコマンドで突合してから結論を書く。開発履歴.md を修正する場合、記載は日本語で行う（CLAUDE.md 言語ルール）。
  </action>
  <verify>
    <automated>test -f .planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md && grep -q "確認範囲" .planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md && grep -q "判定根拠" .planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 06-CHANGELOG-AUDIT.md が存在し「確認範囲」「判定根拠」セクションを含む（D-16）
    - git タグ・APP_VERSION 履歴・MILESTONES.md・開発履歴.md の突合結果と、不一致の有無・修正内容（または整合済みの根拠）が記録されている（D-14）
    - 旧 PDF Editor 時代の同名見出しが改変されておらず、監査記録に意図的共存として明記されている（D-15）
  </acceptance_criteria>
  <done>開発履歴.md の版番/日付が正史と突合され、監査記録に確認範囲・判定根拠が残る。V180-QA-04 の証跡が揃う。</done>
</task>

<task type="auto">
  <name>Task 3: PROJECT.md の V16-D-04 ステータス解消（D-16）</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md（Key Decisions テーブルの V16-D-04「⚠️ Revisit」行）
    - .planning/phases/06-ux-ui/06-CHANGELOG-AUDIT.md（Task 2 の監査結論を参照する）
    - .planning/phases/06-ux-ui/06-CONTEXT.md（D-16）
  </read_first>
  <action>
    .planning/PROJECT.md の Key Decisions テーブルの V16-D-04 行（234 行目）の第3列ステータスを、現在の警告付き残課題表記から解消済みステータス文字列「✅ 解消済み」へ置換し、括弧で根拠（06-CHANGELOG-AUDIT.md 参照・v1.8.0 Phase 6 で開発履歴.md 整合を確認）を添える。置換後の V16-D-04 行の第3列に旧ステータス語（Revisit・⚠️）を残さないこと（同一行に旧表記を残すと検証 grep が誤検出する）。Task 2 の監査で開発履歴.md の修正が発生した場合はその旨、整合済みだった場合は「監査により整合を確認」の旨を括弧内に反映する。V16-D-05 行（235 行目・実機目視の別残課題）は変更しない。PROJECT.md の他の行・他の決定は変更しない（scoped 編集）。日本語で記載する。
  </action>
  <verify>
    <automated>grep -q "V16-D-04.*✅ 解消済み" .planning/PROJECT.md && grep -q "V16-D-05.*⚠️ Revisit" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md の V16-D-04 行の第3列が「✅ 解消済み」を含み、同一行に旧ステータス語（Revisit・⚠️）を残していない（D-16）
    - V16-D-04 の更新根拠として 06-CHANGELOG-AUDIT.md（本フェーズの監査）が参照されている
    - V16-D-05 行の「⚠️ Revisit」は不変（別残課題・変更対象外）
    - PROJECT.md の他の決定行が変更されていない（git diff が V16-D-04 行周辺に限定）
  </acceptance_criteria>
  <done>V16-D-04 の「⚠️ Revisit」が解消され、開発履歴整合の残課題がクローズされる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ↔ Undo/Redo エンジン（ローカル・fitz.Document 操作） | insert_redo の復元アクション。永続化層・外部サービス・ネットワークは関与しない |
| ドキュメント記録（開発履歴.md / PROJECT.md） | 実行時コードに影響しない記録整合性のみ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering (データ整合性) | file_ops._restore_state の insert_redo デルタ復元 | low | mitigate | insert_redo を delete_redo 対称パターンへ修正し、insert→undo→redo→undo 4手往復のページ内容整合性を回帰テストで担保する。修正範囲を insert_redo ブロックのみに限定し他 op の対称性を保持（D-17） |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージを一切インストールしない（RESEARCH Package Legitimacy Audit: none） |
</threat_model>

<verification>
- pytest tests/test_pdf_ops.py tests/test_undo_stress.py -x -q がグリーン（insert 4手往復含む）
- ruff check . && ruff format . がクリーン
- 06-CHANGELOG-AUDIT.md に確認範囲・判定根拠が記載（D-14/D-16）
- PROJECT.md の V16-D-04 が解消済み・旧 PDF Editor 時代エントリ不変（D-15/D-16）
- insert_redo 以外の _restore_state ブロックが未変更（git diff 確認）
</verification>

<success_criteria>
- insert→undo→redo→undo でページが重複せず内容一致（D-17・Core Value 保全）
- 開発履歴.md 整合監査が完了し監査記録が残る（D-14/D-16・Success Criteria #3）
- 旧 PDF Editor 時代エントリ現状維持（D-15）・V16-D-04 解消（D-16）
- Full suite グリーン
</success_criteria>

<output>
Create `.planning/phases/06-ux-ui/06-03-SUMMARY.md` when done
</output>
