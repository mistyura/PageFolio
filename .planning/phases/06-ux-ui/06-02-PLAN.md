---
phase: 06-ux-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/dialogs/about.py
  - pagefolio/dialogs/plugin.py
  - pagefolio/ocr_dialog.py
  - tests/test_font_hardcode_guard.py
  - .planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md
autonomous: true
requirements: [V180-QA-03]

must_haves:
  truths:
    - "フォントサイズ数値ハードコード（about.py の見出し 16pt 固定）が _font ヘルパー経由へ是正され、font_size 設定（8〜16）に追従する（D-12）"
    - "フォントサイズ数値ハードコードを検出するソーススキャン型回帰テストが存在し、pagefolio/ 配下で検出ゼロを担保する（D-13）"
    - "plugin.py のスクロール Canvas に llm_config 基準の Enter/Leave 動的マウスホイール束縛が追加され、ホイールでスクロールできる（D-10）"
    - "ocr_dialog.py の _center() がダイアログ高さを winfo_screenheight() でクランプし、低解像度・大フォント環境で下端が画面外に出ない（D-10）"
    - "スクロール実装を持つ8ファイルが llm_config 基準と比較監査され、是正箇所と受容差分の判定根拠が監査記録に残る（D-11）"
  artifacts:
    - "tests/test_font_hardcode_guard.py"
    - ".planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md（スクロール/フォント監査記録）"
  key_links:
    - "test_font_hardcode_guard.py の正規表現が pagefolio/ の数値フォントタプルのみを検出し、_font/fs 変数連動箇所にはマッチしない（allowlist 不要）"
    - "plugin.py/ocr_dialog.py の是正が llm_config/dialog.py の _build_scrollable_area / _compute_dialog_height 基準パターンに一致する"
  prohibitions:
    - "make_scrollable_frame() 等のスクロール共通ヘルパーを新設せず、既存動作箇所を一斉移行しない（回帰面を狭く保つ）"
    - "フォントを _font ヘルパーへ全面一本化して意図的固定箇所（settings.py のフォントプレビューラベル）まで変更しない"
    - "ui_builder.py の fs 連動フォント（Segoe UI と fs 変数の組）を対象化しない（fs 連動済みで是正不要）"
---

<objective>
ダイアログ間の UI 一貫性を監査・是正する（V180-QA-03）。スクロールの統一基準は v1.7.2 の llm_config パターン（Canvas + Scrollbar + 高さクランプ + 下部ボタン固定 + マウスホイール対応）とし（D-10）、監査対象8ファイルの不一致を個別是正する（共通ヘルパー新設や一斉移行はしない・D-11）。フォント監査はサイズ数値ハードコードのみを対象とし（D-12）、about.py の見出し固定フォントを _font ベースへ是正、再発防止としてフォントハードコード検出のソーススキャン型回帰テストを新設する（D-13）。スクロールパターンは構造的にテスト化困難なため監査記録のみとする（D-13）。

Purpose: フォント設定追従とスクロール操作感をダイアログ間で揃え、低解像度環境での操作不能を防ぐ。
Output: about.py/plugin.py/ocr_dialog.py の是正・tests/test_font_hardcode_guard.py・06-SCROLL-FONT-AUDIT.md 監査記録。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-ui/06-CONTEXT.md
@.planning/phases/06-ux-ui/06-RESEARCH.md
@.planning/phases/06-ux-ui/06-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
本プランが新規作成するシンボル・ファイル:
- `tests/test_font_hardcode_guard.py`（新規ファイル・`test_no_hardcoded_font_sizes` 関数・`_FONT_HARDCODE_PATTERN` 定数・`_PAGEFOLIO_DIR` 定数）
- `.planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md`（新規監査記録ファイル）
- plugin.py に追加するマウスホイールハンドラのローカル関数群（`_on_mousewheel`/`_bind_wheel`/`_unbind_wheel` 相当）
- ocr_dialog.py の `_center()` へ追加する画面高クランプロジック
</artifacts_this_phase_produces>

<flagged_assumptions>
- 決定論的 edge 分類器は V180-QA-03 の日本語要件テキストに適用できず `unclassified/unresolved` 判定だった。本プランの受入基準は CONTEXT.md の D-10〜D-13 と ROADMAP Phase 6 Success Criteria #2 から導出した（flagged assumption・silent drop していない）。
- A1（RESEARCH Assumptions Log）: about.py の是正 delta 値（self._font(6, "bold") 相当）はベースフォント10前提の提案値。実装時に app のベースフォントサイズとの組合せで現状の見た目（約16pt）を再現する delta を確定する（視覚差異のみの軽微リスク）。
- ui_builder.py / ocr_dialog.py の「静的 bind 再帰付与」方式は llm_config の動的束縛と異なるが、いずれも現状機能する。回帰面を狭く保つ D-11 に従い、明確な低リスク是正（plugin.py のホイール未対応・ocr_dialog.py の高さクランプ欠如）のみ是正し、静的 bind 方式差分は監査記録へ「受容差分」として根拠付きで記録する。
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: フォントハードコード検出テスト新設 + about.py 見出しフォント是正</name>
  <files>tests/test_font_hardcode_guard.py, pagefolio/dialogs/about.py</files>
  <read_first>
    - tests/test_source_keyguard.py（grep 型ソーススキャンの直接複製元・_PAGEFOLIO_DIR の解決方法）
    - pagefolio/dialogs/about.py（36-43 の見出しラベル・49 の self._font(0) 正しい前例・27 の self._font=font_func）
    - pagefolio/app.py（_font: 550-555 の delta とベースサイズの関係）
    - .planning/phases/06-ux-ui/06-RESEARCH.md（Pitfall 5・Code Examples D-13）
  </read_first>
  <behavior>
    - test_no_hardcoded_font_sizes: pagefolio/ 配下の全 .py を走査し、数値リテラルのフォントタプル（Segoe UI に続く数値サイズ）を検出したら失敗する。about.py 是正後は検出ゼロ（offenders 空）で pass する。_font(delta) や変数 fs 連動箇所にはマッチしない。
  </behavior>
  <action>
    tests/test_font_hardcode_guard.py を新規作成する。tests/test_source_keyguard.py の構造をそのまま複製し、正規表現のみ差し替える: pathlib と re を使い、_FONT_HARDCODE_PATTERN を「font=( の直後に Segoe UI（シングル/ダブルクォート）とカンマと数値リテラル」を捉える正規表現とする（数値リテラルのみ検出し fs 等の変数連動にはマッチしない）。_PAGEFOLIO_DIR = pathlib.Path(__file__).resolve().parent.parent / "pagefolio" とし、rglob("*.py") 全件を走査して offenders が空であることを assert する。スキャン対象は pagefolio/ のみ（tests/ を含めない）。次に pagefolio/dialogs/about.py の見出しラベル（36-43 の text="PageFolio"）の数値フォントタプルを self._font(6, "bold") 相当へ是正する（about.py は 27 行で self._font=font_func を受け取り済み。delta は app のベースフォントサイズとの組合せで現状の約16pt を再現する値を _font(0) の実測から確定する。同 49 行の self._font(0) が既存の正しい前例）。他ラベル（VERSION/subtitle/copyright 等）は既に _font 準拠のため変更しない。
  </action>
  <verify>
    <automated>pytest tests/test_font_hardcode_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_font_hardcode_guard.py に def test_no_hardcoded_font_sizes と _FONT_HARDCODE_PATTERN/_PAGEFOLIO_DIR が定義されている
    - about.py の見出しラベルが self._font(...) 経由になり、数値フォントタプルが about.py から消えている
    - pytest tests/test_font_hardcode_guard.py -x -q が exit 0（pagefolio/ 全体で検出ゼロ・もし about.py 以外にも検出されたら同様に _font へ是正）
    - ruff check . && ruff format . が通る
  </acceptance_criteria>
  <done>フォントハードコード検出テストが存在し pagefolio/ 全体で検出ゼロ。about.py の見出しが font_size 設定に追従する。</done>
</task>

<task type="auto">
  <name>Task 2: スクロール個別是正（plugin.py ホイール束縛 + ocr_dialog.py 高さクランプ）</name>
  <files>pagefolio/dialogs/plugin.py, pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config/dialog.py（_build_scrollable_area: 101-156 の Enter/Leave 動的 bind_all/unbind_all パターン・_compute_dialog_height: 159-172 の画面高クランプ。基準実装）
    - pagefolio/dialogs/plugin.py（68-90 の Canvas/Scrollbar 生成・現状ホイール束縛なし）
    - pagefolio/ocr_dialog.py（_center: 197-207 の h = max(680, int(fs * 56)) にクランプなし・_font_size: 191-195）
    - .planning/phases/06-ux-ui/06-RESEARCH.md（Pitfall 3・Pitfall 4・Pattern 2）
  </read_first>
  <action>
    pagefolio/dialogs/plugin.py の Canvas（71 行付近で生成する canvas）へ、llm_config/dialog.py:135-153 と同型の動的マウスホイール束縛を追加する: _on_mousewheel（event.delta/120 で yview_scroll）・_on_mousewheel_linux（Button-4/5）・_bind_wheel（canvas.bind_all で MouseWheel/Button-4/Button-5 を束縛）・_unbind_wheel（unbind_all）を定義し、canvas.bind("<Enter>", _bind_wheel) / canvas.bind("<Leave>", _unbind_wheel) を付ける。ダイアログ破棄時の unbind も llm_config の self.bind("<Destroy>", ...) に倣って付与し、複数 Canvas 共存時のホイール横取りを防ぐ（D-10）。次に pagefolio/ocr_dialog.py の _center() の高さ算出 h = max(680, int(fs * 56)) に winfo_screenheight() クランプを追加する: 画面高から余白（例: 100px）を引いた上限で min クランプし、低解像度・大フォント環境で下端が画面外に出ないようにする（llm_config/dialog.py:159-172 の _compute_dialog_height と等価のロジック・Pitfall 4）。既存 minsize(960, 620) は維持。是正は plugin.py と ocr_dialog.py の2箇所に限定し、共通ヘルパー新設・他ファイルの一斉移行はしない（D-11）。裸の except は使わず、Tk 例外は except tk.TclError 等で個別に扱う。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py -q && ruff check pagefolio/dialogs/plugin.py pagefolio/ocr_dialog.py</automated>
  </verify>
  <acceptance_criteria>
    - plugin.py の Canvas に Enter/Leave による bind_all/unbind_all の動的マウスホイール束縛が追加されている（llm_config 基準・D-10）
    - ocr_dialog.py の _center() の高さ算出に winfo_screenheight() ベースの min クランプが追加されている
    - 共通スクロールヘルパー関数（make_scrollable_frame 等）が新設されていない（D-11・grep で不在確認）
    - ruff check . && ruff format . が通り、既存 pytest スイートがグリーン
  </acceptance_criteria>
  <done>plugin.py がホイールスクロール可能になり、ocr_dialog.py が画面高でクランプされる。共通ヘルパー新設なし。</done>
</task>

<task type="auto">
  <name>Task 3: スクロール/フォント監査記録の作成</name>
  <files>.planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md</files>
  <read_first>
    - pagefolio/dialogs/llm_config/dialog.py（基準実装・_build_scrollable_area/_compute_dialog_height）
    - pagefolio/dialogs/batch_ocr.py, pagefolio/dialogs/merge.py, pagefolio/dialogs/llm_config/sections.py, pagefolio/dialogs/plugin.py, pagefolio/ocr_dialog.py, pagefolio/ui_builder.py, pagefolio/viewer.py（D-10 監査対象8ファイルのスクロール実装）
    - .planning/phases/06-ux-ui/06-RESEARCH.md（State of the Art・Common Pitfalls 3/4）
    - .planning/phases/06-ux-ui/06-CONTEXT.md（D-10/D-11/D-13）
  </read_first>
  <action>
    .planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md を新規作成し、監査記録を日本語で残す。内容: (1) スクロール監査 — 基準実装（llm_config/dialog.py の Canvas+Scrollbar+Enter/Leave 動的束縛+高さクランプ+下部ボタン固定）を明記し、監査対象8ファイル（batch_ocr / llm_config dialog / llm_config sections / merge / plugin / ocr_dialog / ui_builder / viewer）ごとに基準との一致/不一致と判定を表で記録する。是正した箇所（plugin.py=ホイール束縛追加・ocr_dialog.py=高さクランプ追加）と、受容差分として据え置いた箇所（ui_builder.py と ocr_dialog.py の静的 bind 再帰付与方式は現状機能し、メインウィンドウ/OCR ダイアログのホイール束縛書き換えは回帰リスクが是正便益を上回るため D-11「個別是正・回帰面を狭く保つ」に従い据え置き）をその根拠とともに記載する。(2) フォント監査 — サイズ数値ハードコードの検出結果（是正前は about.py の見出しのみ・是正後はゼロ）と、除外対象（ui_builder.py の fs 連動・settings.py のフォントプレビュー意図的固定）を記録し、再発防止として tests/test_font_hardcode_guard.py を新設した旨を明記する。(3) スクロールパターンは構造的テスト化困難のため監査記録のみで完了とする旨（D-13）。
  </action>
  <verify>
    <automated>test -f .planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md && grep -q "plugin.py" .planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md && grep -q "ocr_dialog.py" .planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/06-ux-ui/06-SCROLL-FONT-AUDIT.md が存在し、監査対象8ファイルの一致/不一致判定表を含む
    - 是正箇所（plugin.py・ocr_dialog.py）と受容差分（ui_builder.py 等の静的 bind 据え置き）の根拠が記載されている（D-11）
    - フォント監査結果（about.py 是正・除外対象・回帰テスト新設）が記載されている（D-12/D-13）
  </acceptance_criteria>
  <done>スクロール/フォント監査の確認範囲・是正箇所・受容差分の根拠が監査記録として残り、QA-03 の証跡になる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ↔ ダイアログ UI（ローカル） | スクロール操作・フォント表示。信頼境界を越えるデータ経路はない（純粋な Tkinter ウィジェット配置） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | (N/A) | plugin.py/ocr_dialog.py/about.py のスクロール・フォント是正 | low | accept | スクロール/フォント是正は Tkinter ウィジェット配置のみで新規の入力面・ネットワーク経路・機密データ処理を持たない（RESEARCH Security Domain: 該当 STRIDE なし） |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージを一切インストールしない（RESEARCH Package Legitimacy Audit: none）。標準ライブラリ tkinter のみ |
</threat_model>

<verification>
- pytest tests/test_font_hardcode_guard.py tests/test_pdf_ops.py -x -q がグリーン
- ruff check . && ruff format . がクリーン
- 共通ヘルパー新設なし・意図的固定フォント（settings.py）不変（D-11/D-12・grep 確認）
- 06-SCROLL-FONT-AUDIT.md が監査対象8ファイル・是正/受容差分・フォント監査を記録
</verification>

<success_criteria>
- about.py の見出しが font_size 設定に追従し、フォントハードコード検出テストが pagefolio/ 全体でゼロ検出（D-12/D-13・Success Criteria #2）
- plugin.py がホイールスクロール可能・ocr_dialog.py が画面高クランプ（D-10）
- 監査記録に確認範囲と判定根拠が残る（D-11・Success Criteria #2）
- Full suite グリーン
</success_criteria>

<output>
Create `.planning/phases/06-ux-ui/06-02-SUMMARY.md` when done
</output>
