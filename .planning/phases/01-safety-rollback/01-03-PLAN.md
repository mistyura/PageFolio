---
phase: 01-safety-rollback
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pagefolio/dialogs/llm_config/sections.py
  - tests/test_provider_ui.py
autonomous: true
requirements:
  - V190-CFG-01
  - V190-CFG-02

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LLM 設定ダイアログでテンプレートを切り替えても、外部プロンプトファイル（ocr_custom_prompt.md / ocr_summary_prompt.md）は書き換わらない（V190-CFG-01・D-15）"
    - "LLM 設定ダイアログを Cancel（destroy）した場合、外部プロンプトファイルの内容はダイアログを開いたときのまま変化しない（V190-CFG-01）"
    - "Apply 押下時に外部プロンプトファイルへ書き込まれる内容は入力欄の現在値であり、アクティブテンプレートの保存済み値ではない（V190-CFG-01・D-16）"
    - "Apply 時に外部プロンプトファイルが存在しない場合、新規作成されない（V190-CFG-01・D-17）"
    - "アクティブテンプレート選択済みの状態で入力欄を編集して別テンプレートへ切り替えると、外部プロンプトファイルの有無にかかわらず未保存確認ダイアログが表示される（V190-CFG-02・D-18）"
    - "アクティブテンプレート未選択時は、入力欄に自由入力があるかどうかだけで未保存確認の要否が決まる（既存挙動の維持・D-18）"
    - "LLM 設定画面の外部ファイルに関する注記文言は変更されない（D-19・i18n 差分ゼロ）"
    - "LLM 設定ダイアログを開いて Cancel する操作を 2 回繰り返しても、外部プロンプトファイルの内容は 1 度も変化しない（probe: V190-CFG-01 / idempotency）"
    - "テンプレート切替の未保存確認で「いいえ」を選ぶと、選択が元のアクティブテンプレートへ戻り、入力欄の内容も保持される（probe: V190-CFG-02 / concurrency）"
    - statement: "ダイアログ表示中に外部エディタで ocr_custom_prompt.md が編集された場合、Apply は入力欄の現在値でファイルを上書きする（D-16 の WYSIWYG 契約により Apply が最後の書き手になる）"
      verification: backstop
  prohibitions:
    - statement: "外部プロンプトファイルを使っていないユーザーの作業ディレクトリに md ファイルを勝手に新規作成してはならない。外部ファイル連動は既存ファイルが存在する場合のみ有効なオプトイン仕様である"
      category: values
      requirement_id: V190-CFG-01
      status: resolved
      verification: test
    - statement: "外部プロンプトファイルへのライブ即時書き込み（ライブ連動 + Cancel 復元案）を維持・再導入してはならない。書き込みは Apply 押下時の 1 経路へ一本化する（REQUIREMENTS.md Out of Scope）"
      category: values
      requirement_id: V190-CFG-01
      status: resolved
      verification: test
  artifacts:
    - path: "pagefolio/dialogs/llm_config/sections.py"
      provides: "テンプレート切替の副作用除去と未保存判定の単一経路化"
      contains: "_has_unsaved_template_changes"
    - path: "tests/test_provider_ui.py"
      provides: "Apply 一本化契約と未保存確認の回帰テスト"
      contains: "TestApplyOnlyPromptFileWrite"
  key_links:
    - from: "pagefolio/dialogs/llm_config/dialog.py:_apply"
      to: "pagefolio/settings.py:save_prompt_file"
      via: "外部プロンプトファイルへの唯一の書き込み経路（prompt_file_exists ガード付き・本プランでは変更しない参照実装）"
      pattern: "_save_prompt_file\\(CUSTOM_PROMPT_FILE"
    - from: "pagefolio/dialogs/llm_config/sections.py:_on_template_change"
      to: "pagefolio/dialogs/llm_config/sections.py:_has_unsaved_template_changes"
      via: "切替前の未保存差分判定（ファイル存在による分岐を持たない単一経路）"
      pattern: "_has_unsaved_template_changes\\("
---

<objective>
LLM 設定ダイアログの Apply / Cancel 契約を整合させる。現状は `sections.py:_on_template_change` がテンプレート切替の都度 `save_prompt_file()` を呼んで外部プロンプトファイルを即時上書きするため、ユーザーが Cancel してもファイルは書き換わったまま残る。D-15 に従い外部ファイルへの書き込みを Apply 押下時（`dialog.py:_apply`・既に正しい実装）へ一本化し、D-18 に従い `_has_unsaved_template_changes` の `prompt_file_exists()` による分岐を削除して判定経路を 1 本にする。

Purpose: 「Cancel は何も変えない」という設定 UI の基本契約の回復。ユーザーが外部エディタで育てたプロンプト資産が、ダイアログ操作の副作用で無警告に破壊されるのを止める。あわせて外部ファイル非連動ユーザーで未保存確認が出ない同型バグ（V190-CFG-02）を同じ「分岐を残さない」方針で解消する。
Output: `sections.py` の副作用除去と判定経路の単一化、旧 D-07 挙動を検証していた既存テスト 4 件の更新、Apply 一本化契約の新規回帰テスト群。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-rollback/01-CONTEXT.md
@.planning/phases/01-safety-rollback/01-RESEARCH.md
@.planning/phases/01-safety-rollback/01-PATTERNS.md
@CLAUDE.md
@pagefolio/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: テンプレート切替の即時書き込み撤去と未保存判定の単一経路化（D-15・D-16・D-17・D-18）</name>
  <reversibility rating="reversible">1 ファイル内の 2 メソッドから分岐と副作用を取り除く変更で、公開シグネチャも呼び出し元も変わらない。戻す場合も同一ファイル内で完結する。</reversibility>
  <files>pagefolio/dialogs/llm_config/sections.py</files>
  <read_first>
    - `pagefolio/dialogs/llm_config/sections.py` の import ブロック（1-40 行目付近。`prompt_file_exists` / `save_prompt_file` / `CUSTOM_PROMPT_FILE` / `SUMMARY_PROMPT_FILE` / `get_template` の取り込み位置）
    - `pagefolio/dialogs/llm_config/sections.py` の `_has_unsaved_template_changes`（1158-1185 行目）— 未選択時分岐（1173-1174 行目）と、削除対象のファイル存在分岐（1175-1179 行目）と、保存済み値との比較（1180-1185 行目）
    - `pagefolio/dialogs/llm_config/sections.py` の `_on_template_change`（1207-1252 行目）— 未保存確認、入力欄への反映、削除対象の即時書き込み（1240-1245 行目）、アクティブ名の更新
    - `pagefolio/dialogs/llm_config/dialog.py` の `_apply`（445-469 行目付近）— 既に正しい Apply 時書き込み実装（本タスクでは変更しない参照実装）
    - `pagefolio/dialogs/llm_config/dialog.py:335` 付近の `_add_prompt_file_notice`（D-19 により文言は変更しない対象）
    - `.planning/phases/01-safety-rollback/01-RESEARCH.md` Common Pitfalls Pitfall 5（未選択時ロジックを壊さないための最小差分指示）
  </read_first>
  <action>
`pagefolio/dialogs/llm_config/sections.py` に 3 つの最小差分を入れる。関数の構造・引数・戻り値の型は変更しない。

(1) D-15: `_on_template_change` の末尾近くにある外部ファイルへの即時書き込み 2 ブロック（1240-1245 行目・ファイル存在チェックと書き込みの対）を削除する。削除後も、未保存差分の確認 → 選択テンプレート内容の入力欄への反映 → `_active_template_name` の更新 → `current_settings["prompt_templates"]["active"]` の更新 → `_refresh_template_delete_state()` という残りの処理順序は一切変えない。メソッドの docstring から旧 D-07 の説明（切替時に外部ファイルを上書きして「外部ファイル＝アクティブテンプレートのライブ編集内容」の不変条件を保つ、という記述）を削除し、代わりに「外部ファイルへの書き込みは Apply 押下時に `dialog.py:_apply` が入力欄の現在値で行う唯一の経路であり、切替はファイルに副作用を持たない（v1.9.0 D-15 / D-16）」という説明へ差し替える。

(2) D-18: `_has_unsaved_template_changes` から、外部ファイルが 1 つも存在しないときに早期 False を返すブロック（1175-1179 行目）だけを削除する。冒頭の未選択時分岐（`_active_template_name` が空のときに自由入力の有無だけを見る 1173-1174 行目）と、末尾の `get_template()` 保存済み値との比較（1180-1185 行目）は一切変更しない。この最小差分により、アクティブテンプレート選択済みの場合は外部ファイルの有無にかかわらず常に入力欄と保存済み値が比較される。docstring も、02-REVIEW WR-03 の経緯説明を残しつつ「選択済みの場合はファイル連動モード時のみ比較する」という記述を「選択済みの場合は常に保存済み内容と比較する（v1.9.0 D-18・判定経路を 1 本にする）」へ更新する。

(3) 未使用 import の除去: (1) と (2) の削除により、`sections.py` 内で `prompt_file_exists` と `save_prompt_file` の参照がゼロになる。ruff の未使用 import 検査に掛かるため、import ブロックから両名を取り除く。`CUSTOM_PROMPT_FILE` / `SUMMARY_PROMPT_FILE` が同ファイル内の他箇所から参照されていない場合はそれらも同様に取り除く（参照が残っている場合は残す）。この import 除去は既存テストの `monkeypatch.setattr("pagefolio.dialogs.llm_config.sections.prompt_file_exists", ...)` を壊すため、Task 2 で該当テストを更新するまでの間 `tests/test_provider_ui.py` は red になる。Task 1 と Task 2 は同一の作業単位として連続して実施する。

D-19 に従い i18n（`pagefolio/lang.py`）と注記文言は一切変更しない。`dialog.py` の Apply ハンドラも変更しない（既に D-15/D-16/D-17 を満たしている）。実装後に `ruff format .` と `ruff check .` を通す。
  </action>
  <verify>
    <automated>ruff check pagefolio/dialogs/llm_config/sections.py &amp;&amp; python -c "import pagefolio.dialogs.llm_config.sections as m; import inspect; src=inspect.getsource(m._has_unsaved_template_changes); assert 'prompt_file_exists' not in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'save_prompt_file' pagefolio/dialogs/llm_config/sections.py` が 0 を返す
    - Source: `grep -c 'prompt_file_exists' pagefolio/dialogs/llm_config/sections.py` が 0 を返す
    - Source: `grep -n 'save_prompt_file' pagefolio/dialogs/llm_config/dialog.py` が 3 件以上（関数内 import 1 件 + CUSTOM / SUMMARY への書き込み 2 件）を出力する ＝ Apply 経路は無改造で残っている
    - Source: `grep -n 'def _has_unsaved_template_changes\|if not self._active_template_name' pagefolio/dialogs/llm_config/sections.py` が両方 1 件ずつを出力する ＝ 未選択時分岐が保持されている
    - Source: `git diff --stat pagefolio/lang.py` が空 ＝ D-19 のとおり i18n 差分ゼロ
    - Source: `git diff --stat pagefolio/dialogs/llm_config/dialog.py` が空 ＝ Apply ハンドラは無改造
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>テンプレート切替が外部プロンプトファイルへ一切書き込まなくなり、未保存判定がファイル存在に依存しない単一経路になっている。Apply ハンドラと i18n は無改造。</done>
</task>

<task type="auto">
  <name>Task 2: 旧ライブ連動挙動を検証していた既存テスト 4 件の更新（D-15・D-16）</name>
  <files>tests/test_provider_ui.py</files>
  <read_first>
    - `tests/test_provider_ui.py` の `TestTemplateChangeFlow` クラス全体（2157 行目〜。`_make_template_dialog` ヘルパーは 2130-2154 行目付近、`_FakeTemplateText` / `_FakeCombo` / `_ButtonStub` スタブ群も同ファイル内）
    - `tests/test_provider_ui.py:2164-2203` `test_cancel_discards_switch_and_keeps_edited_content`（`sections.prompt_file_exists` と `sections.save_prompt_file` を monkeypatch している）
    - `tests/test_provider_ui.py:2205-2247` `test_confirmed_switch_overwrites_external_files_fake_capture`（旧 D-07 の「切替でファイルが上書きされる」ことを assert している ＝ D-15 で挙動が反転する）
    - `tests/test_provider_ui.py:2249-2297` `test_no_active_template_warns_on_unsaved_freeform_text`（未選択時ロジックの既存合格テスト。挙動は変わらないが monkeypatch 対象名が消える）
    - `tests/test_provider_ui.py:2299-` `test_change_overwrites_external_md_file`（`settings._get_base_dir` を tmp_path へ差し替えた実ファイル検証版。旧 D-07 の実ファイル上書きを assert している ＝ D-15 で挙動が反転する）
    - `tests/test_provider_ui.py:1240-1290` 付近の Apply 経路テスト（`llm_config_mod` の `prompt_file_exists` / `save_prompt_file` を monkeypatch する既存パターン。Apply 側は無改造なのでこれらは変更不要）
  </read_first>
  <action>
Task 1 の実装により `sections.py` から消えたシンボルを参照している既存テストと、旧ライブ連動挙動を仕様として固定している既存テストを更新する。

(1) `test_cancel_discards_switch_and_keeps_edited_content`: `sections.prompt_file_exists` の monkeypatch を削除する（D-18 により判定はファイル存在に依存しない）。`sections.save_prompt_file` の monkeypatch も削除し、代わりに `pagefolio.settings.save_prompt_file` をモジュール横断で監視する記録用スタブへ差し替えて「切替経路からファイル書き込みが一切発生しない」ことを検証する。`askyesno` の monkeypatch と既存の 3 つの assert（選択が A へ戻る・入力欄が保持される・書き込みゼロ）はそのまま維持する。

(2) `test_confirmed_switch_overwrites_external_files_fake_capture`: 挙動が D-15 で反転するため、テスト名を `test_confirmed_switch_does_not_touch_external_files` へ改名し、docstring を「未保存差分なしの切替確定後、選択テンプレートの内容は入力欄へ反映されるが、外部ファイルへの書き込みは発生しない（v1.9.0 D-15・書き込みは Apply のみ）」へ書き換える。assert を反転させ、記録用スタブの呼び出し履歴が空であること・`_active_template_name` が新テンプレート名になること・入力欄が新テンプレート内容へ更新されることを検証する。

(3) `test_no_active_template_warns_on_unsaved_freeform_text`: 挙動は変わらないため assert は維持し、消えたシンボルへの monkeypatch 2 件（`sections.prompt_file_exists` / `sections.save_prompt_file`）だけを削除する。書き込みゼロの検証は (1) と同じ `pagefolio.settings.save_prompt_file` の記録用スタブへ移す。

(4) `test_change_overwrites_external_md_file`: 実ファイル検証版も挙動が反転する。テスト名を `test_change_leaves_external_md_file_untouched` へ改名し、docstring を v1.9.0 D-15 の契約へ書き換える。`settings._get_base_dir` を tmp_path へ差し替えて実際に 2 つの md ファイルを作成し、テンプレート切替後にファイル内容が**作成時のまま**であることをファイル読み取りで検証する形へ変更する（`save_prompt_file` / `prompt_file_exists` / `load_prompt_file` は引き続き monkeypatch せず実関数のまま通す）。

クラスの docstring（`TestTemplateChangeFlow`）も、D-05 の切替中止検証は維持しつつ、旧 D-07 の外部ファイル上書き検証が v1.9.0 D-15 により「書き込みが発生しないことの検証」へ反転した旨を明記する。テスト更新後に `ruff format .` と `ruff check .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py -x -q -k "TemplateChangeFlow"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'pagefolio.dialogs.llm_config.sections.save_prompt_file' tests/test_provider_ui.py` が 0 を返す
    - Source: `grep -c 'pagefolio.dialogs.llm_config.sections.prompt_file_exists' tests/test_provider_ui.py` が 0 を返す
    - Source: `grep -n 'def test_confirmed_switch_does_not_touch_external_files\|def test_change_leaves_external_md_file_untouched' tests/test_provider_ui.py` が 2 件を出力する
    - Behavior: `_make_template_dialog` で構築したダイアログに対し `_on_template_change()` を呼んだ後、tmp_path 上の `ocr_custom_prompt.md` と `ocr_summary_prompt.md` の内容がテスト前に書き込んだ値のままである
    - Test: `pytest tests/test_provider_ui.py -q` が exit 0
    - Test: `pytest tests/test_prompt_templates.py -q` が exit 0（テンプレート CRUD 純関数の非回帰）
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>旧ライブ連動挙動を仕様として固定していた既存テストが v1.9.0 の Apply 一本化契約へ更新され、`tests/test_provider_ui.py` が green に戻っている。</done>
</task>

<task type="auto">
  <name>Task 3: Apply 一本化契約と未保存確認の新規回帰テスト整備（V190-CFG-01・V190-CFG-02）</name>
  <files>tests/test_provider_ui.py</files>
  <read_first>
    - `tests/test_provider_ui.py` の `TestTemplateChangeFlow`（Task 2 で更新済み）と `_make_template_dialog` ヘルパー
    - `tests/test_provider_ui.py:1240-1290` 付近の Apply 経路テスト（`llm_config_mod` の `prompt_file_exists` / `save_prompt_file` を monkeypatch し、入力欄の内容が書き込みへ渡ることと、ファイル非存在時に書き込まれないことを検証する既存 2 件）
    - `tests/test_provider_ui.py:2299-` `test_change_leaves_external_md_file_untouched`（Task 2 で作成した `settings._get_base_dir` 差し替え + 実ファイル読み取りパターン）
    - `pagefolio/dialogs/llm_config/dialog.py` の `_apply`（445-469 行目付近）と `destroy` へ紐づく Cancel ボタン（`sections.py` 1149-1151 行目付近）
    - `pagefolio/settings.py` の `prompt_file_exists` / `save_prompt_file` / `_get_base_dir`（75-100 行目付近）
  </read_first>
  <action>
`tests/test_provider_ui.py` に 2 つの新規クラスを追加し、V190-CFG-01 / V190-CFG-02 の契約を実ファイル検証を含めて固定する。既存の `_make_template_dialog` / `_FakeTemplateText` / `_FakeCombo` / `_ButtonStub` / `_SetGetVarStub` スタブ群を再利用し、新しいテスト基盤は導入しない。

`TestApplyOnlyPromptFileWrite`（V190-CFG-01）:
- `test_template_change_does_not_write_prompt_files`: `settings._get_base_dir` を tmp_path へ差し替え、2 つの md ファイルを既知の内容で作成。テンプレート切替を複数回行った後、両ファイルの内容が作成時のままであることを検証する。
- `test_cancel_leaves_prompt_files_unchanged`: 同じ tmp_path 構成で、入力欄を編集してからダイアログの Cancel（`destroy`）に相当する経路を通しても両ファイルの内容が変化しないことを検証する。Apply を呼ばないことが Cancel の定義であることを明示する。
- `test_apply_writes_input_field_content_not_active_template`（D-16 の契約テスト・assumption-delta の invariant test）: アクティブテンプレート A（保存済み内容 X）を選択した状態で入力欄を Y へ書き換え、Apply を実行する。書き込まれた内容が Y（入力欄の現在値）であり X（テンプレートの保存済み値）ではないことを検証する。
- `test_apply_does_not_create_missing_prompt_files`（D-17）: tmp_path に md ファイルを作らない状態で Apply を実行し、tmp_path 配下に md ファイルが 1 つも作られないことを検証する。
- `test_apply_overwrites_externally_edited_file_with_input_content`（probe: CFG-01 / concurrency）: md ファイル作成 → ダイアログ構築 → ファイルを外部から直接書き換え → Apply の順で操作し、Apply 後のファイル内容が入力欄の現在値になっていることを検証する。
- `test_open_cancel_twice_leaves_files_unchanged`（probe: CFG-01 / idempotency）: 「開く → 編集 → Cancel」を 2 回繰り返し、md ファイルの内容が 1 度も変化しないことを検証する。

`TestUnsavedTemplateChangesSinglePath`（V190-CFG-02）:
- `test_selected_template_edit_warns_without_prompt_files`: アクティブテンプレート選択済み・入力欄を保存済み値から編集済み・外部 md ファイルは**存在しない**状態で別テンプレートへ切り替え、`askyesno` が 1 回呼ばれることを検証する（D-18 の直接の回帰テスト。現状は早期 False で確認が出ない）。
- `test_selected_template_unedited_does_not_warn`: 同構成で入力欄が保存済み値と一致している場合は `askyesno` が呼ばれないことを検証する（過検知の防止）。
- `test_switch_cancel_restores_active_template_selection`（probe: CFG-02 / concurrency）: 未保存確認で「いいえ」を返したとき、`template_var` がアクティブテンプレート名へ戻り、入力欄の内容も編集後のまま保持されることを検証する。

Apply の実行は既存の Apply 経路テスト（1240-1290 行目付近）と同型に、`LLMConfigDialog` の `_apply` を bound method として直接呼ぶ形で駆動する。テスト追加後に `ruff format .` と `ruff check .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py tests/test_prompt_templates.py tests/test_settings_keyguard.py -q &amp;&amp; ruff check . &amp;&amp; ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - Test: `pytest tests/test_provider_ui.py::TestApplyOnlyPromptFileWrite -q` が exit 0 で 6 メソッド（`test_template_change_does_not_write_prompt_files` / `test_cancel_leaves_prompt_files_unchanged` / `test_apply_writes_input_field_content_not_active_template` / `test_apply_does_not_create_missing_prompt_files` / `test_apply_overwrites_externally_edited_file_with_input_content` / `test_open_cancel_twice_leaves_files_unchanged`）が収集・実行される
    - Test: `pytest tests/test_provider_ui.py::TestUnsavedTemplateChangesSinglePath -q` が exit 0 で 3 メソッド（`test_selected_template_edit_warns_without_prompt_files` / `test_selected_template_unedited_does_not_warn` / `test_switch_cancel_restores_active_template_selection`）が収集・実行される
    - Behavior: 外部 md ファイルが存在しない状態でアクティブテンプレートを編集して切り替えると `askyesno` が 1 回呼ばれる（D-18 の反転が実測されている）
    - Behavior: アクティブテンプレート A（保存済み X）を選び入力欄を Y にして Apply すると、md ファイルの内容が Y になる
    - Behavior: md ファイルが存在しない tmp_path で Apply を実行しても、tmp_path 配下に `.md` ファイルが作成されない
    - Test: `pytest tests/test_settings_keyguard.py tests/test_source_keyguard.py -q` が exit 0（API キー非永続化ガードの非回帰）
    - Test: `pytest -q` が exit 0（フルスイート非回帰。Tcl/Tk 由来のフレーキー ERROR が出た場合は該当ファイル単体で再実行し green を確認したうえで SUMMARY へ記録する）
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>「Cancel は外部ファイルを変えない」「Apply は入力欄の現在値を書く」「存在しないファイルは作らない」「選択済みテンプレートの編集は常に未保存確認を出す」の 4 契約が自動テストで固定されている。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

本フェーズ（Plan 01〜05）が**新規に作成**するシンボル一覧。plan-review-convergence の source-grounding パスはこの表を読み、新規シンボルを drift 検証の対象から除外する。

| 種別 | シンボル | 配置 | Plan |
|---|---|---|---|
| 関数 | `derive_pdf_has_password` | `pagefolio/file_ops.py`（モジュールレベル純関数） | 01 |
| 例外クラス | `OCRDisabledError` | `pagefolio/ocr_providers/errors.py`（`pagefolio/ocr_providers/__init__.py` から re-export） | 02 |
| メソッド | `_update_batch_menu_state` | `pagefolio/app.py`（`PDFEditorApp`） | 02 |
| 属性 | `self._tools_menu` / `self._batch_menu_index` | `pagefolio/app.py` | 02 |
| i18n キー | `ocr_disabled_msg` / `batch_menu_item_off` | `pagefolio/lang.py`（ja / en） | 02 |
| i18n キー | `warn_rollback_title` / `err_insert_rollback_failed` / `err_undo_restore_failed` / `err_redo_restore_failed` | `pagefolio/lang.py`（ja / en） | 04 |
| テストクラス | `TestSavePathsKeepEncryption` / `TestDerivePdfHasPassword` | `tests/test_password.py` | 01 |
| テストクラス | `TestOCRDisabledGuard` | `tests/test_ocr.py` | 02 |
| テストクラス | `TestApplyOnlyPromptFileWrite` / `TestUnsavedTemplateChangesSinglePath` | `tests/test_provider_ui.py` | 03 |
| テストメソッド | `test_template_change_does_not_write_prompt_files` / `test_cancel_leaves_prompt_files_unchanged` / `test_apply_writes_input_field_content_not_active_template` / `test_apply_does_not_create_missing_prompt_files` / `test_apply_overwrites_externally_edited_file_with_input_content` / `test_open_cancel_twice_leaves_files_unchanged` / `test_selected_template_edit_warns_without_prompt_files` / `test_selected_template_unedited_does_not_warn` / `test_switch_cancel_restores_active_template_selection` | `tests/test_provider_ui.py` | 03 |
| テストメソッド（改名） | `test_confirmed_switch_does_not_touch_external_files`（旧 `test_confirmed_switch_overwrites_external_files_fake_capture`）/ `test_change_leaves_external_md_file_untouched`（旧 `test_change_overwrites_external_md_file`） | `tests/test_provider_ui.py` | 03 |
| テストクラス | `TestInsertRollback` / `TestDuplicateUndoTiming` / `TestUndoRedoRestoreFailure` | `tests/test_pdf_ops.py` | 04 |
| テストメソッド | `test_duplicate_undo_redo_undo_roundtrip` / `test_merge_undo_redo_undo_roundtrip` / `test_merge_resize_undo_redo_undo_roundtrip` / `test_duplicate_single_page_doc_roundtrip` / `test_merge_head_and_tail_adjacent_roundtrip` | `tests/test_pdf_ops.py` | 05 |
| ドキュメント節 | 「Undo 記録が先置きのままの op 一覧」（D-12 の棚卸し） | `01-05-SUMMARY.md` | 05 |

Plan 01 の PLAN.md に完全版がある。上記以外のシンボルはすべて既存であり、本フェーズは内部実装のみを変更する（新規モジュール・新規ファイルはゼロ）。
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ダイアログの入力欄 → 作業ディレクトリの外部 md ファイル | ユーザーが外部エディタで管理しているプロンプト資産へアプリが書き込む境界。Apply 押下だけがこの境界を越えてよい |
| ダイアログの入力欄 → `pagefolio_settings.json` | API キーを含む LLM 設定が永続化される境界。`_SENSITIVE_KEYS` ガードが唯一の防波堤 |
| Cancel 操作 → 一切の永続化 | 「Cancel は何も変えない」という契約そのものが信頼境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | `pagefolio/dialogs/llm_config/sections.py:_on_template_change` の即時書き込み | medium | mitigate | 切替時の `save_prompt_file` 呼び出しを撤去し、書き込みを `dialog.py:_apply` の 1 経路へ一本化（Task 1）。`test_cancel_leaves_prompt_files_unchanged` / `test_change_leaves_external_md_file_untouched` で実ファイル読み取りにより機械検証（Task 2/3） |
| T-01-10 | Information Disclosure | LLM 設定の永続化経路（`_SENSITIVE_KEYS` ガード） | high | mitigate | 本プランは `sections.py` のテンプレート UI ハンドラのみを変更し、settings 保存経路（`_apply` の `llm_settings` 組み立て・`_save_settings`）には一切手を入れない。`tests/test_settings_keyguard.py` と `tests/test_source_keyguard.py` の green を Task 3 の acceptance criteria に含め、API キーが `pagefolio_settings.json` へ漏れないことを非回帰として担保する |
| T-01-11 | Tampering | 作業ディレクトリへの md ファイル自動生成 | low | mitigate | `dialog.py:_apply` の `prompt_file_exists` ガードを維持（D-17）し、存在するファイルのみ更新する。`test_apply_does_not_create_missing_prompt_files` で tmp_path 配下にファイルが作られないことを検証 |
| T-01-12 | Repudiation | 未保存差分の破棄が無警告で起こる | medium | mitigate | `_has_unsaved_template_changes` からファイル存在分岐を削除し、選択済みテンプレートの編集は常に確認ダイアログを出す（Task 1・D-18）。`test_selected_template_edit_warns_without_prompt_files` で検証 |
| T-01-SC | Tampering | npm / pip / cargo installs | low | accept | 本フェーズは新規パッケージを一切導入しない（RESEARCH.md「Package Legitimacy Audit」＝対象パッケージなし） |
</threat_model>

<verification>
- `pytest tests/test_provider_ui.py tests/test_prompt_templates.py tests/test_utils.py -q` が exit 0
- `pytest tests/test_settings_keyguard.py tests/test_source_keyguard.py -q` が exit 0
- `pytest -q`（フルスイート）が exit 0
- `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
- `grep -c 'save_prompt_file' pagefolio/dialogs/llm_config/sections.py` が 0
- `git diff --stat pagefolio/lang.py pagefolio/dialogs/llm_config/dialog.py` が空（D-19 の i18n 差分ゼロと Apply ハンドラ無改造）
</verification>

<success_criteria>
1. LLM 設定ダイアログを Cancel しても外部プロンプトファイル（`ocr_custom_prompt.md` / `ocr_summary_prompt.md`）が変更されず、書き込みは Apply 押下時のみ発生する（V190-CFG-01）
2. Apply が書き込む内容は入力欄の現在値であり、外部ファイルが存在しない場合は新規作成されない（D-16・D-17）
3. 選択済みテンプレートを編集した状態で別テンプレートへ切り替えると、外部ファイル連動の有無にかかわらず未保存確認が表示される（V190-CFG-02）
4. アクティブテンプレート未選択時の既存挙動が維持されている（既存テストが green）
5. i18n（`pagefolio/lang.py`）と `dialog.py` の Apply ハンドラに差分がない（D-19・変更面の最小化）
</success_criteria>

<output>
Create `.planning/phases/01-safety-rollback/01-03-SUMMARY.md` when done
</output>
