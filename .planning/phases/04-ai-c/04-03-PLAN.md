---
phase: 04-ai-c
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - pagefolio/ocr_dialog.py
autonomous: false
requirements: [V16-AI-01, V16-AI-02]
user_setup: []
must_haves:
  truths:
    - "OCRDialog の _on_run がプロンプト合成を resolve_ocr_prompt(preset, provider_name, custom_prompt) 呼び出しに置き換え、provider 名はプロンプト解決前に取得される"
    - "OCRDialog で markdown プリセット結果が tk.Text タグ（md_h1/md_h2/md_bullet/md_code/md_bold）で整形表示される"
    - "markdown 以外のプリセット（text/table）は従来どおり素朴 insert で表示される（後方互換）"
    - "カスタムプロンプトが設定済みのとき、_on_run は引き続きカスタムプロンプトを使用する（成功基準3 後方互換）"
    - "コピー/保存（_format_full_text）は raw テキストを返し、タグ/整形は表示専用に留まる"
  artifacts:
    - path: "pagefolio/ocr_dialog.py"
      provides: "_on_run の resolve_ocr_prompt 差し替え + _build の md_* タグ定義 + _render_results_ordered の markdown 整形分岐"
      contains: "resolve_ocr_prompt"
  key_links:
    - from: "pagefolio/ocr_dialog.py _on_run"
      to: "pagefolio/ocr.py resolve_ocr_prompt"
      via: "self._ocr_prompt = resolve_ocr_prompt(self.preset_var.get(), name, self.custom_prompt)"
      pattern: "resolve_ocr_prompt\\("
    - from: "pagefolio/ocr_dialog.py _render_results_ordered"
      to: "pagefolio/md_render.py parse_markdown"
      via: "preset == 'markdown' のとき parse_markdown(text) の戻り値を insert + tag_add"
      pattern: "parse_markdown\\("
    - from: "pagefolio/ocr_dialog.py _build タグ定義"
      to: "pagefolio/themes.py C"
      via: "tag_configure foreground/background を C['ACCENT']/C['BG_PANEL'] で指定（ハードコード禁止）"
      pattern: "tag_configure.*C\\["
---

<objective>
Wave 1 で確定した 2 つの純関数（`parse_markdown` / `resolve_ocr_prompt`）を `OCRDialog`（`pagefolio/ocr_dialog.py`）へ配線する。3 箇所を改修する: (1) `_on_run` のプロンプト合成を `resolve_ocr_prompt` 呼び出しへ置換（V16-AI-02）、(2) `_build` で markdown 整形用の `tk.Text` タグを定義（V16-AI-01）、(3) `_render_results_ordered` に `preset == "markdown"` ガード付きの整形描画分岐を追加（V16-AI-01）。描画層は「純関数の戻り値を insert/tag_add するだけ」の薄い層に保つ。

Purpose: 純ロジック層（Wave 1）を実際のユーザー体験へ接続する最終配線。これにより成功基準1（markdown 整形表示で読みやすく）・成功基準2（プロバイダ別プロンプト）・成功基準3（カスタム両立）の 3 つが OCRDialog 上で同時に満たされる。UI 目視確認が要るため human-verify チェックポイントで締める。
Output: `pagefolio/ocr_dialog.py` の 3 箇所改修（import 追加 + プロンプト解決差し替え + タグ定義 + 整形描画分岐）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-c/04-RESEARCH.md
@.planning/phases/04-ai-c/04-PATTERNS.md
@.planning/phases/04-ai-c/04-VALIDATION.md
@.planning/phases/04-ai-c/04-01-PLAN.md
@.planning/phases/04-ai-c/04-02-PLAN.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
> 本プラン（04-03）は Wave 1 の純関数を消費する配線層。新規シンボルは生成しない（既存メソッド内の改修のみ）。

| 種別 | 改修箇所 | 場所 | 内容 |
|------|----------|------|------|
| import 追加 | `from pagefolio.ocr import (... resolve_ocr_prompt)` / `from pagefolio.md_render import parse_markdown` | ocr_dialog.py:14-23 既存 import ブロック | Wave 1 純関数を import |
| メソッド改修 | `_on_run` プロンプト解決 | ocr_dialog.py:1085-1090 | resolve_ocr_prompt へ置換・name 前方移動 |
| メソッド改修 | `_build` タグ定義 | ocr_dialog.py:488-502 近傍（self.text 生成直後） | md_h1/md_h2/md_bullet/md_code/md_bold の tag_configure |
| メソッド改修 | `_render_results_ordered` 描画分岐 | ocr_dialog.py:1456-1479 | preset == markdown ガード付き整形描画ヘルパー |

新規 LANG キー: なし（rendered 固定・トグル UI なし／04-RESEARCH.md Q1 最小実装）。新規設定キー: なし。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: _on_run のプロンプト合成を resolve_ocr_prompt へ置換（name 前方移動）</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（既存 import 14-23、_on_run のプロンプト分岐 1085-1090、provider 名取得 `name = self.app.settings.get("ocr_provider","")` 1120、既存例外様式 `except (tk.TclError, ValueError):` 1078）
    - .planning/phases/04-ai-c/04-PATTERNS.md（改修1: _on_run 差し替え 72-83・name 前方移動の注意）
    - .planning/phases/04-ai-c/04-02-PLAN.md（resolve_ocr_prompt の契約・引数順 preset, provider_name, custom_prompt）
    - .planning/phases/04-ai-c/04-RESEARCH.md（Pitfall 3 カスタム上書き温存・純関数側で担保）
  </read_first>
  <action>
    既存 import ブロック（ocr_dialog.py:14-23 の `from pagefolio.ocr import (...)`）に `resolve_ocr_prompt` を追加する（アルファベット順を維持・既存 `OCR_PROMPTS` / `build_provider` と並べる）。

    `_on_run` 内のプロンプト合成（現状 1085-1090 の `if self.custom_prompt: ... else: preset = ...; OCR_PROMPTS.get(...)` ブロック）を `resolve_ocr_prompt` 呼び出し 1 行へ置換する。provider 名 `name = self.app.settings.get("ocr_provider", "")` は現状 1120 行（プロンプト解決より後）にあるため、**`name` 取得をプロンプト解決の前へ前方移動**する（04-PATTERNS.md:81 の指摘）。1120 行に残っていた `name` 取得は重複定義になるため、前方移動後は 1120 付近の再代入を削除し、以降の `if name == "claude":` 分岐がその前方移動した `name` を参照するようにする（変数スコープを壊さないこと）。

    置換後のプロンプト解決行: `self._ocr_prompt = resolve_ocr_prompt(self.preset_var.get(), name, self.custom_prompt)`。カスタム上書き温存（成功基準3）は純関数側で担保されるため UI 側の if 分岐は不要になる。`OCR_PROMPTS` の import は他で使われていなければ残す/削る を ruff の未使用警告に従って判断（未使用なら削除）。例外様式は既存どおり型指定を維持（裸 except 禁止）。
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py',encoding='utf-8').read()); print('OK')"</automated>
    <automated>python -m pytest tests/test_provider_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ソース表明: `grep -c 'resolve_ocr_prompt(' pagefolio/ocr_dialog.py` が 1 以上（呼び出し）かつ import 行にも `resolve_ocr_prompt` が存在
    - ソース表明: `_on_run` 内の旧プロンプト分岐 `if self.custom_prompt:` … `OCR_PROMPTS.get(preset` が除去されている（`grep -c 'OCR_PROMPTS.get(preset' pagefolio/ocr_dialog.py` が 0）
    - ソース表明: `name = self.app.settings.get("ocr_provider"` の代入が 1 箇所のみ（前方移動により重複していない・`grep -c 'name = self.app.settings.get(\"ocr_provider\"' pagefolio/ocr_dialog.py` が 1）
    - 挙動表明: `python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py',encoding='utf-8').read())"` が例外なく完了
    - CLI 出力: `ruff check pagefolio/ocr_dialog.py` がエラー 0 件（未使用 import 含む）で通過
  </acceptance_criteria>
  <done>
    `_on_run` がプロンプトを `resolve_ocr_prompt(preset, name, custom_prompt)` で解決し、provider 名は解決前に取得され重複定義がない。旧 if/else 分岐は除去。カスタム上書きは純関数側で温存。ruff 通過・既存テスト緑。
  </done>
</task>

<task type="auto">
  <name>Task 2: _build に md_* タグ定義 + _render_results_ordered に markdown 整形描画分岐</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（self.text 生成 488-502、_render_results_ordered 1456-1479、_font ヘルパー 144-148 と _font_size 150-154、preset_var 96、既存 C[] 利用 486/490-492）
    - pagefolio/md_render.py（04-01 で確定した parse_markdown の戻り値型 list[tuple[line_kind, list[tuple[span_text, inline_tag|None]]]]）
    - .planning/phases/04-ai-c/04-PATTERNS.md（改修2: タグ定義 85-95・改修3: 描画分岐 97-101・改修4: _format_full_text 不変 103）
    - .planning/phases/04-ai-c/04-RESEARCH.md（Pattern 1 121-148 のタグ定義/描画ループ・Pitfall 2/5 ガード条件と raw 維持）
    - CLAUDE.md（テーマ色は C[] 経由・フォントは self._font(delta, weight)・等幅は Consolas 許容だがサイズは _font_size() 追従・裸 except 禁止）
  </read_first>
  <action>
    **タグ定義（_build 内・self.text 生成直後の 502 付近）:** `tk.Text` のスタイルタグを `tag_configure` で 5 種定義する。テーマ色は `C[]` 経由・フォントは `self._font(delta, weight)`（ハードコード禁止・CLAUDE.md）。04-RESEARCH.md:130-134 / 04-PATTERNS.md:89-93 のとおり:
    - `md_h1`: `font=self._font(4, "bold")`, `foreground=C["ACCENT"]`
    - `md_h2`: `font=self._font(2, "bold")`, `foreground=C["ACCENT"]`
    - `md_bullet`: `lmargin1=20`, `lmargin2=36`
    - `md_code`: `background=C["BG_PANEL"]`（等幅にする場合 `font=("Consolas", self._font_size())` を許容・サイズは `_font_size()` 追従）
    - `md_bold`: `font=self._font(-1, "bold")`
    `C["ACCENT"]` / `C["BG_PANEL"]` は themes.py に存在するキー。

    **整形描画ヘルパー + 分岐（_render_results_ordered 1456-1479）:** 現状の素朴 `self.text.insert("end", self.results[page_idx] + "\n")`（1466/1468）の results 本文挿入部分について、`preset = self.preset_var.get()` を取得し **`preset == "markdown"` のときのみ**整形描画する分岐を追加する（Pitfall 2: text/table や LMStudio/Tesseract 素出力に Markdown パーサを当てない構造的ガード）。整形描画は薄いヘルパーメソッド（例 `_insert_markdown(text)`）に切り出し、04-RESEARCH.md:137-146 のループ: `for kind, spans in parse_markdown(text):` → 各 span を `insert` し `inline_tag` があれば `tag_add`、行末で行レベルタグ（kind）を行全体へ `tag_add`。`preset != "markdown"` の場合は現行の素朴 insert を**完全に温存**（後方互換）。スキップ通知・truncated 通知・page_separator・エラー表示の既存挿入（1459-1464/1469-1478）は変更しない（results 本文部分のみ条件分岐）。

    `from pagefolio.md_render import parse_markdown` を import に追加する。描画/パース例外を捕捉する場合は `tk.TclError` 等を型指定（裸 except 禁止・既存 1078 様式）。`_format_full_text`（1596-1604）は変更しない（raw 維持・Pitfall 5・コピー/保存は整形を反映しない）。
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py',encoding='utf-8').read()); print('OK')"</automated>
    <automated>python -m pytest tests/test_md_render.py tests/test_provider_ui.py tests/test_lang_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ソース表明: 5 種のタグが定義されている（`grep -c 'tag_configure("md_' pagefolio/ocr_dialog.py` が 5 以上、md_h1/md_h2/md_bullet/md_code/md_bold を含む）
    - ソース表明: タグ定義の foreground/background がハードコード 16 進でなく C[] 経由（`grep -c 'tag_configure("md_h1".*C\[' pagefolio/ocr_dialog.py` が 1 以上・少なくとも md_h1 が C["ACCENT"] を使用）
    - ソース表明: `grep -c 'parse_markdown' pagefolio/ocr_dialog.py` が 2 以上（import + 呼び出し）かつ `preset == "markdown"` ガードが描画分岐に存在（`grep -c '== "markdown"' pagefolio/ocr_dialog.py` が 1 以上）
    - ソース表明: `_format_full_text` が変更されず raw `self.results` を返す（`grep -c 'parse_markdown' pagefolio/ocr_dialog.py` の出現が `_format_full_text` 関数本体に無いこと＝整形は表示専用）
    - 挙動表明: `python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py',encoding='utf-8').read())"` が例外なく完了
    - CLI 出力: `ruff check pagefolio/ocr_dialog.py` がエラー 0 件で通過
    - 回帰: `python -m pytest -q` がフルスイート緑（582 件ベースライン + Wave 1 新規テスト分・既存に赤なし）
  </acceptance_criteria>
  <done>
    `_build` に md_h1/md_h2/md_bullet/md_code/md_bold の 5 タグが C[]/self._font で定義され、`_render_results_ordered` が `preset == "markdown"` のときのみ parse_markdown 戻り値を insert/tag_add で整形描画し、それ以外は素朴 insert を温存する。`_format_full_text` は raw 維持。フルスイート緑・ruff 通過。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    OCRDialog 上で (1) markdown プリセット OCR 結果が tk.Text タグで整形表示（見出し=ACCENT 色太字 / 箇条書き=字下げ / コード=背景色 / 強調=太字）、(2) プロバイダ別プロンプト（Claude=XML タグ / Gemini=明示指示）が適用、(3) カスタムプロンプト設定時は引き続きカスタムが効く、を自動実装した。文言効果（OCR 出力品質向上）は [ASSUMED]（04-RESEARCH.md A1/A2/A4）のため実 API での目視確認が必要。
  </what-built>
  <how-to-verify>
    1. `python pagefolio.py` で起動し、テキストを含む PDF を開く。
    2. ページを選択し OCR テキスト抽出を実行。プリセットを `Markdown` にする。
    3. LMStudio または実 API（Claude/Gemini・コスト確認ダイアログが課金前ゲート）で OCR 実行。結果ビューアで見出し（# / ##）が ACCENT 色・太字で、箇条書き（- / *）が字下げ「• 」で、`**強調**` が太字で表示され、プレーンテキストより読みやすいことを確認（成功基準1）。
    4. プリセットを `通常テキスト` / `表形式` に切り替えて再実行し、整形が当たらず従来どおり素直に表示されること（後方互換・Pitfall 2）を確認。
    5. コピー / 保存（.md）を実行し、保存ファイルに raw Markdown（タグ名や装飾文字の混入なし）が出力されること（Pitfall 5）を確認。
    6. （実 API 利用可能なら）LLM 設定でカスタムプロンプトを設定し OCR 実行 → カスタムプロンプトが効く（プロバイダ別テンプレに上書きされない）こと（成功基準3）を確認。
    7. （任意・実 API 利用可能なら）同一ページを「汎用 vs プロバイダ別テンプレ」で Claude/Gemini 実行し出力の構造化度を目視比較・記録（V16-AI-02 manual・04-VALIDATION.md Manual-Only）。
  </how-to-verify>
  <resume-signal>「approved」と入力、または見つかった問題（誤整形・後方互換崩れ・タグ漏れ等）を記述してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部 LLM 出力 → parse_markdown → tk.Text 描画 | OCR 結果（外部 LLM 由来文字列）が tk.Text に挿入される。`insert` はプレーンテキスト扱いで HTML/コード実行はない |
| ユーザーカスタムプロンプト → resolve_ocr_prompt → Provider | 既存設定機構経由のみ。本プランは新規入力経路を増やさない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-INJ | Tampering / Input Validation (V5) | tk.Text への LLM 出力挿入 | mitigate | `tk.Text.insert` はプレーンテキスト挿入でありマークアップ/コード実行はない。タグ適用は表示のみ。整形は表示専用でエクスポート（_format_full_text）は raw 維持（04-RESEARCH.md:429） |
| T-04-DOS | Denial of Service | 巨大/制御文字混入の LLM 出力描画 | mitigate | 行単位処理で線形。tk.Text は大量テキストに耐性・既存 see("end") でスクロール委譲。parse_markdown の re は線形（04-RESEARCH.md:436-437） |
| T-04-IDISC | Information Disclosure | OCR 本文/プロンプトのログ露出 | accept | 描画/解決経路で本文・プロンプトをログ出力しない（既存規約踏襲・has_embedded_text/_sync_param_vars_from_settings の前例・04-RESEARCH.md:438） |
</threat_model>

<verification>
- `python -m pytest -q` フルスイート緑（582 件ベースライン + Wave 1 新規テスト分・既存に赤なし）
- `ruff check . && ruff format .` 通過
- `python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py',encoding='utf-8').read())"` 構文確認通過
- human-verify チェックポイント承認（markdown 整形表示・後方互換・カスタム両立の目視確認）
- 開発履歴.md への追記（V16-AI-01/02 完了・APP_VERSION は変更しない＝中間フェーズ非バンプ方針）
</verification>

<success_criteria>
- markdown プリセット結果が tk.Text タグで整形表示され、見出し/箇条書き/コード/強調がプレーンより読みやすい（V16-AI-01・成功基準1）
- text/table プリセットは従来どおり素朴表示で後方互換が保たれる（Pitfall 2）
- _on_run が resolve_ocr_prompt でプロバイダ別プロンプトを適用し、カスタムプロンプトが引き続き効く（V16-AI-02・成功基準2/3）
- コピー/保存は raw テキストを返し整形は表示専用（Pitfall 5）
- フルスイート緑・ruff 通過・human-verify 承認
</success_criteria>

<output>
Create `.planning/phases/04-ai-c/04-03-SUMMARY.md` when done
</output>
