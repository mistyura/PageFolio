---
phase: 03-ocr-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/ocr_engine.py
  - pagefolio/ocr_dialog.py
  - tests/test_ocr_engine.py
autonomous: true
requirements:
  - V180-REFAC-03
must_haves:
  truths:
    - "OCRRunEngine が pagefolio.ocr_engine から単独 import できる（D-03）"
    - "単一ファイル OCR（run/rerun/resume）の進捗・キャンセル・リトライ・サーキットブレーカー挙動が抽出前と同一のまま維持される（D-04・回帰）"
    - "ocr_engine.py は Tk/fitz 非依存（トップレベル import は threading/queue/logging と pagefolio.ocr_pipeline のみ）で、バッチ OCR から再利用可能（D-01/D-02）"
    - "producer（OCRDialog._render_next_page）と consumer（Engine ワーカー）が同一の queue.Queue / PipelineState インスタンスを共有する（落とし穴10・Pitfall 1）"
  artifacts:
    - "pagefolio/ocr_engine.py"
    - "tests/test_ocr_engine.py（FakeProvider + TestOCRRunEngineUnit）"
    - "pagefolio/ocr_dialog.py（_start_worker_thread/_worker が Engine 委譲ラッパーへ）"
  key_links:
    - "OCRDialog._render_next_page → self._engine.queue（同一キュー・try_enqueue/send_sentinels の送出先を一本化）"
    - "Engine の on_success/on_page_error コールバック → OCRDialog._record_page_success/_record_page_error（results/errors 累積・resume 維持）"
    - "Engine の on_complete/on_cancelled/on_fatal（D-08）→ OCRDialog._on_engine_* アダプタ（ワーカースレッド側は世代ガード + self.after(0,...) 投函のみ・winfo_exists 非呼び出し）→ メインスレッド _safe_finish_*（winfo_exists 後に既存 _finish_* を実行・REVIEW MEDIUM 対応）"
---

<objective>
`ocr_dialog.py`（2520行・調査時点）から producer-consumer の consumer 駆動部（`_start_worker_thread`/`_worker` のロジック）を新設 `pagefolio/ocr_engine.py` の `OCRRunEngine` クラスへ抽出し、`OCRDialog` 側を薄い委譲ラッパー化する（V180-REFAC-03）。producer（fitz レンダリング連鎖 `_render_next_page`）は D-01 に従い `OCRDialog` に残す。抽出は既存の Tk/fitz 非依存純ロジック層 `pagefolio/ocr_pipeline.py`（`PipelineState`/`consume_one`/`try_enqueue`/`send_sentinels`）を変更せずそのまま呼び出す薄い consumer レイヤーの追加であり、新規ドメインロジックは持ち込まない。

Purpose: 単一ファイル OCR とバッチ OCR（Phase 4）で共用できる再利用可能な OCR 実行エンジンを確立する。Engine を Tk/fitz 非依存にすることで、Phase 4 の `BatchOCRDialog` がファイルごとに Engine を新規生成して再利用できる構造にする（D-01/D-11）。
Output: `pagefolio/ocr_engine.py`（`OCRRunEngine`）・`tests/test_ocr_engine.py`（unit スモーク）・委譲ラッパー化した `pagefolio/ocr_dialog.py`。

このフェーズは UI 変更を一切行わない内部リファクタリング + テスト整備であり、単一ファイル OCR の観測可能な挙動（進捗表示・キャンセル・リトライ・サーキットブレーカー・resume）は抽出前と完全に同一を維持する（同一ロジックの配置換えのみ）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ocr-e2e/03-CONTEXT.md
@.planning/phases/03-ocr-e2e/03-RESEARCH.md
@.planning/phases/03-ocr-e2e/03-PATTERNS.md
@.planning/phases/03-ocr-e2e/03-VALIDATION.md
@pagefolio/ocr_pipeline.py
@pagefolio/ocr_dialog.py
</context>

<artifacts_produced>
## Artifacts this phase produces

このフェーズが新規に導入するシンボル（drift 検証で新規作成分として扱うべきもの）:

**新規ファイル:**
- `pagefolio/ocr_engine.py`
- `tests/test_ocr_engine.py`

**新規クラス:**
- `OCRRunEngine`（`pagefolio/ocr_engine.py`）
- `TestOCRRunEngineUnit`（`tests/test_ocr_engine.py`）
- `FakeProvider`（`tests/test_ocr_engine.py` 内・既存 `tests/test_ocr_pipeline.py` の `FakeProvider` パターンの複製拡張。他ファイルの `FakeProvider` は変更しない。定義直前に「pipeline 用テストとのカプセル化のため意図的に複製・拡張した」旨の説明コメントを残す・REVIEW 提案対応）

**`OCRRunEngine` の公開シンボル（メソッド名/引数順は Claude's Discretion・下記は想定形）:**
- コンストラクタ引数: `provider` / `prompt` / `run_pages` / `concurrency` / `cancel_flag` / コールバック群（`on_success` / `on_page_error` / `on_retry_wait` / `on_progress` / `on_complete` / `on_cancelled` / `on_fatal`）
- メソッド: `start()`（consumer スレッド起動）・`note_skip(page_idx)`・`note_render_failed(page_idx)`・`progress_count()`
- プロパティ/属性: `queue`（producer が参照する共有 `queue.Queue`）・`results` / `errors` / `truncated_pages` / `skipped_pages` / `render_failed_pages`（per-run 内部状態・D-09）

**`OCRDialog`（`pagefolio/ocr_dialog.py`）へ追加する新規メソッド:**
- `_on_engine_complete` / `_on_engine_cancelled` / `_on_engine_fatal`（完了理由別アダプタ・ワーカースレッドから呼ばれるため世代ガード評価 + `self.after(0, ...)` 投函のみ・winfo_exists 非呼び出し・REVIEW MEDIUM 対応）
- `_safe_finish_complete` / `_safe_finish_cancelled` / `_safe_finish_error`（メインスレッド側 finalizer・冒頭で `winfo_exists()` チェック後に既存 `_render_results_ordered`/`_finish_*` を実行・REVIEW MEDIUM 対応）
- `_on_engine_progress`（または相当のクロージャ）・`_on_retry_wait_for(gen)`（リトライ待機メッセージ生成クロージャ）
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OCRRunEngine（consumer 駆動の軽量クラス）を新設し unit スモークで固定</name>
  <files>pagefolio/ocr_engine.py, tests/test_ocr_engine.py</files>
  <read_first>
    - pagefolio/ocr_pipeline.py（全文・変更禁止の呼び出し先。PipelineState/consume_one/try_enqueue/send_sentinels のシグネチャと docstring 作法）
    - pagefolio/ocr_dialog.py:1668-1786（_start_worker_thread/_worker の移植元ロジック）
    - pagefolio/ocr_dialog.py:765-804（_record_page_success/_record_page_error/_done_disp の移植先イメージ）
    - .planning/phases/03-ocr-e2e/03-PATTERNS.md（ocr_engine.py の analog 割当・consumer 起動パターン・結果辞書所有権）
    - .planning/phases/03-ocr-e2e/03-CONTEXT.md（D-01〜D-12 の locked decisions）
    - tests/test_ocr_pipeline.py:28-44（FakeProvider パターン・複製元）
    - tests/test_ocr_pipeline.py:200-279（_drive_pipeline 実スレッド駆動・タイムアウト設計の前例）
  </read_first>
  <behavior>
    TestOCRRunEngineUnit（tests/test_ocr_engine.py）で以下を先に固定してから実装する（RED→GREEN）:
    - test_engine_importable: `from pagefolio.ocr_engine import OCRRunEngine` が成功する
    - test_single_page_success_invokes_on_success: FakeProvider（ocr_image が固定テキストを返す）で run_pages=[0]・concurrency=1 の Engine を start() し、テスト側の薄い producer スタブが engine.queue へ (0, "b64-0") を try_enqueue → send_sentinels(engine.queue, 1) で終端。ワーカー join 後、注入した on_success が page_idx=0 で1回呼ばれ、engine.results[0] に FakeProvider の戻り値が入る
    - test_queue_is_single_shared_instance: start() 後に engine.queue が None でない同一 queue.Queue インスタンスであり、producer と consumer が同じオブジェクトを参照する（落とし穴10 の同一性検証。id() 一致で確認）
    - test_final_worker_calls_on_complete_once: concurrency=2 で全ページ成功後、on_complete がちょうど1回だけ呼ばれる（decrement_worker の is_last 契約・CR-01）
  </behavior>
  <action>
    `pagefolio/ocr_engine.py` を新設し `OCRRunEngine` クラスを実装する。D-03 に従い単一ファイル（サブパッケージ化しない）。`pagefolio/ocr_pipeline.py`（`pagination.py`/`undo_store.py` と同格の Tk/fitz 非依存純ロジック層系譜）の docstring 作法に倣い、モジュール冒頭 docstring に「`ocr_pipeline.py` と対になる consumer 駆動の軽量クラス（`PipelineState` と同格・完全な純関数ではない）」である旨を明記する。トップレベル import は `threading`・`queue`・`logging` と `pagefolio.ocr_pipeline`（`PipelineState`/`consume_one`/`try_enqueue`/`send_sentinels`）に限定し、Tk・fitz(PyMuPDF) のモジュールレベル依存を追加しない（D-01/D-02・落とし穴10 回避）。`pagefolio.ocr`/`pagefolio.ocr_providers` が必要になる場合のみ `ocr_pipeline.py` と同じく関数内 import で循環回避する。

    `OCRRunEngine.__init__` は D-02 の最小限の値渡しに限定する: `provider`・`prompt`・`run_pages`・`concurrency`・`cancel_flag`（`threading.Event`）と個別コールバック群（`on_success`/`on_page_error`/`on_retry_wait`/`on_progress`/`on_complete`/`on_cancelled`/`on_fatal`・D-05/D-06/D-08）。設定 dict（`self._active_ocr_settings` 相当）や API キー文字列は受け取らない（生成済み `provider` インスタンスのみ・T-03-01 情報漏洩防止）。per-run 内部状態 `self.results={}`・`self.errors={}`・`self.truncated_pages=set()`・`self.skipped_pages=set()`・`self.render_failed_pages=set()` を初期化する（D-09）。メソッド名・引数順・型ヒント・`PipelineState` の生成タイミング（`__init__` vs `start()`）は Claude's Discretion。

    落とし穴10（Pitfall 1）対策として、`queue.Queue(maxsize=concurrency + 1)` と `PipelineState(concurrency)` を Engine 内で**一度だけ**生成し（`start()` 内推奨）、`self.queue` プロパティで公開する。producer 側はこの単一インスタンスを参照する（Open Questions A1 の推奨解）。`start()` は `concurrency` 本の daemon consumer スレッドを起動して即座に返す（ブロッキングしない）。各 consumer スレッドは `pagefolio/ocr_dialog.py:1709-1754` の `_worker` ループ本体を移植する: `self.queue.get(timeout=1.0)` → `queue.Empty` 時は `cancel_flag.is_set()` で break/continue → `None` で完了シグナル break → `consume_one(provider, item, prompt, pstate, cancel_check=cancel_flag.is_set, breaker_threshold=CB_CONSECUTIVE_FAILURES 相当（ocr_pipeline.DEFAULT_CIRCUIT_BREAKER_THRESHOLD を既定に、`OCRDialog` 側と同値の 3 を渡す）, on_success=..., on_page_error=..., on_retry_wait=...)`。b64 は `finally` で `del` する（成功基準2・T-06-06）。

    consume_one へ渡す `on_success`/`on_page_error` は Engine 内部のラッパーにする: まず Engine の per-run 辞書（`self.results[page_idx]=text`・truncated 時 `self.truncated_pages.add`／`self.errors[page_idx]=msg`）を更新し（D-09）、その**後**に注入された外側コールバック（`OCRDialog` 由来）へ転送する二段構成（03-PATTERNS.md 結果辞書所有権節）。`on_retry_wait` は Engine 側では加工せず、`(page_idx, attempt, delay, exc)` をそのまま外側コールバックへ転送する（メッセージ生成は Tk/lang 依存のため `OCRDialog` に残す）。

    D-07/D-12 の統合進捗計算は Engine が持つ: `note_skip(page_idx)`/`note_render_failed(page_idx)` を producer から呼べるようにし、それぞれ `self.skipped_pages`/`self.render_failed_pages` へ追加する。`progress_count()` は `pstate.done_count + len(self.skipped_pages) + len(self.render_failed_pages)` を返す。Engine は D-11 により実行ごとに新規生成されるため、per-run のセットは常に空から始まり、`_skip_base`/`_render_failed_base` 相当のベースライン減算は構造的に不要（D-12 を new-instance で満たす）。各アイテム消費後は `on_progress(progress_count(), page_idx)` を呼ぶ。

    consumer ループ末尾は `pagefolio/ocr_dialog.py:1756-1786`（CR-01 最終ワーカー判定）を移植する: `is_last, fatal_msg, fatal_kind = pstate.decrement_worker()` → `is_last` でなければ return。最終ワーカーは `self.after()`（Tk）を持たないため、D-08 の理由別コールバックを同期的に**直接**呼ぶ: `fatal_msg is not None` → `on_fatal(fatal_msg, fatal_kind)`／`cancel_flag.is_set()` → `on_cancelled()`／それ以外 → `on_complete()`。Tk へのディスパッチ（`after(0, ...)`）は呼び出し側（`OCRDialog`）の責務（D-05）。

    同時に `tests/test_ocr_engine.py` を新設し、`tests/test_ocr_pipeline.py:28-44` の `FakeProvider` を複製（他ファイルは変更しない・D-14）した上で `TestOCRRunEngineUnit` に <behavior> の4テストを実装する。レビュー指摘 [提案] 対応: 複製した `FakeProvider` の定義直前（またはクラス docstring）に、『既存 `tests/test_ocr_pipeline.py` の pipeline 用テストとのカプセル化のため、意図的に複製・拡張したフェイク実装である（共有せず本ファイルに閉じる）』旨の説明コメントを日本語で残し、後から見た開発者がモッククラスの重複を混同しないようにする。テストの producer スタブは `engine.queue` へ `try_enqueue`（`pagefolio.ocr_pipeline` から import）で積み `send_sentinels(engine.queue, concurrency)` で終端し、ワーカー `thread.join(timeout=10.0)` のタイムアウト設計を踏襲する（Pitfall 3・flaky 回避）。Engine が保持するワーカースレッド参照を取得できる手段（属性 or start() の戻り値）を用意して join する。
  </action>
  <verify>
    <automated>python -m pytest tests/test_ocr_engine.py::TestOCRRunEngineUnit -x</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr_engine.py` に `class OCRRunEngine` が定義されている（grep: `class OCRRunEngine` が存在）
    - `python -c "from pagefolio.ocr_engine import OCRRunEngine"` が終了コード0で成功する
    - `ocr_engine.py` のトップレベル import が threading/queue/logging と pagefolio.ocr_pipeline のみ（Bash: `grep -nE '^import (tkinter|fitz)|^from (tkinter|fitz)' pagefolio/ocr_engine.py` が何も出力しない = 一致0件。Tk/fitz のモジュールレベル依存なし）
    - `ocr_engine.py` が `PipelineState`/`consume_one` を再実装せず import して使用（grep: `from pagefolio.ocr_pipeline import` が存在し、`consume_one(` の呼び出しがある）
    - `pytest tests/test_ocr_engine.py::TestOCRRunEngineUnit -x` が exit 0（4テスト green）
    - test_queue_is_single_shared_instance が id() 一致で producer/consumer のキュー同一性を検証している
    - 複製した `FakeProvider` に複製理由の説明コメントがある（Bash: `grep -nE '複製|カプセル化' tests/test_ocr_engine.py` が1件以上ヒット・REVIEW 提案対応）
    - `ruff check pagefolio/ocr_engine.py tests/test_ocr_engine.py` が clean
  </acceptance_criteria>
  <done>OCRRunEngine が単独 import 可能で Tk/fitz 非依存。単一ページ成功で on_success が呼ばれ engine.results に格納され、キュー同一性・最終ワーカー単一 on_complete が unit テストで green。</done>
</task>

<task type="auto">
  <name>Task 2: OCRDialog を Engine 委譲ラッパー化（producer 残留・キュー一本化・完了アダプタ）</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_engine.py（Task 1 で新設した OCRRunEngine のコンストラクタ引数とコールバック契約）
    - pagefolio/ocr_dialog.py:1311-1540（_on_run の provider 生成・concurrency 再クランプ・キュー初期化・起動順序）
    - pagefolio/ocr_dialog.py:1542-1786（_render_next_page/_retry_sentinels/_start_worker_thread/_worker の現状）
    - pagefolio/ocr_dialog.py:765-804（_record_page_success/_record_page_error/_done_disp）
    - pagefolio/ocr_dialog.py:1789-1930（_on_progress_bar/_finish_complete/_finish_cancelled/_finish_error）
    - pagefolio/ocr_dialog.py:877-905（_retry_wait_key/_build_retry_wait_message）
    - pagefolio/ocr_dialog.py:130-164（インスタンス状態初期化）
    - .planning/phases/03-ocr-e2e/03-PATTERNS.md（ocr_dialog.py 委譲後イメージ・完了理由別ハンドラ受け皿）
    - .planning/phases/03-ocr-e2e/03-CONTEXT.md（D-04/D-05/D-08/D-10/D-11）
  </read_first>
  <action>
    D-04 に従い `OCRDialog._start_worker_thread` を Engine 委譲ラッパーへ置き換える（メソッド名・シグネチャ `(self, gen=None)` は不変）: 内部で `self._engine = OCRRunEngine(provider=self.provider, prompt=self._ocr_prompt, run_pages=self._run_pages, concurrency=self.concurrency, cancel_flag=self._cancel_flag, on_success=lambda p, t, tr: self._record_page_success(p, t, truncated=tr), on_page_error=self._record_page_error, on_retry_wait=self._on_retry_wait_for(gen), on_progress=self._on_engine_progress_for(gen), on_complete=lambda: self._on_engine_complete(gen), on_cancelled=lambda: self._on_engine_cancelled(gen), on_fatal=lambda msg, kind: self._on_engine_fatal(gen, msg, kind))` を生成し `self._engine.start()` を呼ぶ（D-11: 実行ごとに新規生成）。`self._pstate`/`self._render_queue`/`self._worker_threads` の直接生成・保持は Engine へ移すため、`_on_run`（1532行のキュー生成 `self._render_queue = queue.Queue(...)`）は削除し、キューは Engine 生成後に `self._engine.queue` を参照する形へ改める。起動順序（consumer 先行起動 → producer 開始）は維持する: `_on_run` は `self._start_worker_thread(gen)`（Engine 生成 + start）→ `self._render_next_page(gen)` の順を保つ。

    `_worker` メソッド（1683-1786）はロジックが Engine へ移るため削除する（D-04 の委譲形により `OCRDialog` からは消える。`_start_worker_thread` がラッパーとして残るのが D-04 の「薄いラッパー維持」）。

    producer 側 `_render_next_page`（1542-1647）は D-01 により残すが、キュー参照先を `self._render_queue` から `self._engine.queue` に一本化する（落とし穴10・Pitfall 1: producer と consumer が同一キューを参照）。埋め込みテキストスキップ分岐では `self.results[page_idx]=extracted; self._skipped_pages.add(page_idx)` の後に `self._engine.note_skip(page_idx)` を呼び、進捗表示値を `self._engine.progress_count()` から取得する。レンダー失敗の except 分岐では `self.errors[page_idx]=...; self._render_failed_pages.add(page_idx)` の後に `self._engine.note_render_failed(page_idx)` を呼び、進捗値を `self._engine.progress_count()` から取得する。fatal 判定（1569行 `self._pstate.is_fatal()`）は `self._engine` 経由（`self._engine` が保持する PipelineState 状態）を参照する形へ改める（Engine が is_fatal 相当を委譲公開するか、Engine のプロパティ経由で fatal_msg/fatal_kind を取得できるようにする — Engine 側の公開手段は Task 1 の Discretion 範囲だが本タスクで参照が必要なため Engine に読み取り口を用意する）。`send_sentinels`/`try_enqueue` の送出先も `self._engine.queue` に統一する。`_retry_sentinels`（1649-1666）も送出先を `self._engine.queue` へ変更して残す。

    `_done_disp`（793-804）は `self._pstate`/`_skip_base`/`_render_failed_base` を参照しなくなるため、`return self._engine.progress_count() if self._engine is not None else 0` へ委譲する（D-07）。`_record_page_success`/`_record_page_error`（765-791）は `self.results`/`self.errors`/`self._truncated_pages` の累積更新（resume 跨ぎで結果を保持するため `OCRDialog` 側が正本・D-10）を維持し変更しない。レビュー指摘 [LOW]（データ競合への排他制御）は本フェーズでは**明示的に defer する**: これらは Engine ワーカースレッドからコールバック経由で呼ばれるが、現行 `ocr_dialog.py` でも元々ロックを用いておらず、本フェーズは「同一ロジックの配置換えのみ・観測挙動を変えない」方針（D-04）である。GIL 下で単一辞書代入はアトミックであり単一ファイル OCR では実害報告がないため、排他ロック導入は複数ファイルが真に並行しうる Phase 4（バッチ OCR）で扱う確認事項とする（根拠は threat_model T-03-03 参照）。

    完了理由別アダプタメソッドを新規追加する（D-08）: `_on_engine_complete(self, gen)`・`_on_engine_cancelled(self, gen)`・`_on_engine_fatal(self, gen, msg, kind)`。**これらは Engine のワーカースレッドから同期的に呼ばれる**（メインスレッドではない）。レビュー指摘 [MEDIUM] 対応（ワーカースレッドからの Tkinter メソッド直接実行の回避）: アダプタ本体（ワーカースレッド側）では Tkinter ウィジェットメソッド（`winfo_exists()` や `winfo_*` 系）を**一切呼ばず**、世代ガード評価（`gen is not None and gen != self._run_gen` なら return）と `self.after(0, ...)` への投函（`tk.TclError` 捕捉付き）のみを行う。ウィジェット存在チェック（`winfo_exists()`）と実際の UI 更新は、`after(0)` で投函されたあとメインスレッドで動く finalizer 内で行う（現行 `_worker` の CR-01 末尾 1762-1786 が worker スレッドから winfo_exists を呼ばず after 投函のみしているのと同じ規約を踏襲）。

    具体的にはメインスレッド finalizer `_safe_finish_complete(self)`・`_safe_finish_cancelled(self)`・`_safe_finish_error(self, msg, kind)` を新設し、各 finalizer 冒頭で `if not self.winfo_exists(): return` を実行したうえで既存の描画/終了処理を呼ぶ: `_safe_finish_complete` は `self._render_results_ordered()` → `self._finish_complete()`（現行 1783-1784 と同じ順序を再現）、`_safe_finish_cancelled` は `self._finish_cancelled()`、`_safe_finish_error` は `self._finish_error(msg, kind=kind)`。アダプタ側は世代ガード後に `_on_engine_complete` → `self.after(0, self._safe_finish_complete)`、`_on_engine_cancelled` → `self.after(0, self._safe_finish_cancelled)`、`_on_engine_fatal` → `self.after(0, lambda m=msg, k=kind: self._safe_finish_error(m, k))` を（`tk.TclError` 捕捉付きで）投函する。進捗クロージャ `_on_engine_progress_for(gen)` は `(done, page_idx)` を受け取り、ワーカースレッド側では世代ガードのみ評価して `self.after(0, progress_var 更新 + self._on_progress_bar(done))` を投函する（`winfo_exists()` は呼ばない・現行 _worker 1741-1754 と同じ tk.TclError 捕捉）。リトライ待機クロージャ `_on_retry_wait_for(gen)` は `(page_idx, attempt, delay, exc)` を受け取り、現行 `_worker` 内 `_on_retry_wait`（1699-1707）と同じく `_retry_wait_key`/`_build_retry_wait_message` でメッセージ生成し、ワーカースレッド側では世代ガードのみ評価して after 投函する（`winfo_exists()` 非呼び出し）。

    レビュー指摘 [LOW] 対応（不要変数のクリーンアップ明記）: D-11/D-12 により累積進捗計算が `OCRRunEngine` 内に閉じ、`_skip_base`/`_render_failed_base` を用いたベースライン差し引きは構造的に不要になる。以下を**明示的に削除**してコードベースに残骸を残さない: (a) コンストラクタの初期化 `self._skip_base = 0`（146行）と `self._render_failed_base = 0`（159行）、(b) `_on_run` 冒頭のベースライン確定代入 `self._skip_base = len(self._skipped_pages)` / `self._render_failed_base = len(self._render_failed_pages)`（1385-1386行）、(c) `_done_disp`（802-803行）内のこれら変数を用いた減算ロジック（`_done_disp` が `self._engine.progress_count()` 委譲へ置き換わることで同時に消える）。削除後 `grep -n '_skip_base\|_render_failed_base' pagefolio/ocr_dialog.py` が0件になることを確認する。

    抽出により `pagefolio.ocr_pipeline` からの `PipelineState`・`consume_one` の import（27-32行）が `ocr_dialog.py` で未使用になる場合は削除する（`try_enqueue`/`send_sentinels` は producer で継続使用のため残す）。ruff の F401 未使用 import 警告をゼロにする。`self._pstate` インスタンス属性が完全に不要になった場合は初期化（156行）を整理する（Engine が所有）。resume 判断ロジック `_pending_pages`/`_can_resume`（726-732）は D-10 により `OCRDialog` に残し変更しない。
  </action>
  <verify>
    <automated>python -m pytest tests/test_provider_ui.py tests/test_ocr_fallback.py tests/test_ocr.py tests/test_ocr_pipeline.py tests/test_ocr_engine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `_start_worker_thread` が `OCRRunEngine(` を生成し `.start()` を呼ぶ委譲ラッパーになっている（grep: `OCRRunEngine(` が pagefolio/ocr_dialog.py に存在）
    - `_worker` メソッド定義が pagefolio/ocr_dialog.py から削除されている（Bash: `grep -c 'def _worker(' pagefolio/ocr_dialog.py` が 0）
    - producer とアダプタが `self._engine.queue` を参照する（grep: `self._engine.queue` が存在）
    - 完了理由別アダプタ3種が追加されている（grep: `def _on_engine_complete`・`def _on_engine_cancelled`・`def _on_engine_fatal` がそれぞれ存在）
    - メインスレッド finalizer 3種が追加され `winfo_exists()` チェックを持つ（grep: `def _safe_finish_complete`・`def _safe_finish_cancelled`・`def _safe_finish_error` が存在・REVIEW MEDIUM 対応）
    - 完了アダプタ本体がワーカースレッドから `winfo_exists()` を直接呼ばない（コードレビュー確認: `_on_engine_complete`/`_on_engine_cancelled`/`_on_engine_fatal` および進捗/リトライクロージャの本体に `winfo_exists()` 呼び出しがなく、`winfo_exists()` は `_safe_finish_*` 内のみ・REVIEW MEDIUM 対応）
    - `_skip_base`/`_render_failed_base` が完全に除去されている（Bash: `grep -c '_skip_base\|_render_failed_base' pagefolio/ocr_dialog.py` が 0・REVIEW LOW 対応）
    - `python -c "import ast; ast.parse(open('pagefolio/ocr_dialog.py', encoding='utf-8').read())"` が成功する
    - OCRDialog 回帰テスト群が green: `pytest tests/test_provider_ui.py tests/test_ocr_fallback.py tests/test_ocr.py -x` が exit 0
    - `pagefolio/ocr_pipeline.py` は無変更（Bash: `git diff --stat pagefolio/ocr_pipeline.py` が空 = 差分なし）
    - フルスイート green: `pytest` が exit 0（ベースライン 987 件以上・回帰ゼロ）
    - `ruff check . && ruff format --check .` が clean（未使用 import F401 なし）
  </acceptance_criteria>
  <done>OCRDialog が Engine へ委譲し、producer/consumer が同一キューを共有。単一ファイル OCR の進捗・キャンセル・リトライ・fatal 挙動が抽出前と同一のまま既存回帰テスト・フルスイートが green、ruff clean。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCRDialog → OCRRunEngine | 設定・機密値の受け渡し境界。生の設定 dict / API キー文字列を Engine へ渡さず、構築済み provider インスタンスと最小限の値のみを渡す（D-02） |
| メインスレッド（producer）↔ consumer ワーカースレッド | 共有 `queue.Queue`/`PipelineState` の同一性境界。分割時に別インスタンスへ二重生成すると排他制御が壊れる（落とし穴10） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | OCRRunEngine.__init__ 引数 | low | mitigate | D-02 に従い設定 dict / 生 API キー文字列を Engine へ渡さない。引数は provider（構築済みインスタンス）・prompt・run_pages・concurrency・cancel_flag・コールバックのみ。acceptance で「設定 dict を渡さない」ことをコードレビューで確認 |
| T-03-02 | Tampering | 共有 queue/PipelineState の同一性 | high | mitigate | 落とし穴10 対策: queue.Queue/PipelineState を Engine 内で一度だけ生成し `self.queue` プロパティで公開。producer は同一インスタンスを参照。test_queue_is_single_shared_instance が id() 一致で検証 |
| T-03-03 | Tampering | `_record_page_success`/`_record_page_error` への並行アクセス | low | accept | レビュー指摘 [LOW] 対応（明示的 deferral）: これら結果/エラー辞書の更新はワーカースレッドからコールバック経由で呼ばれるが、現行 `ocr_dialog.py` でも元々ロックを用いておらず、本フェーズは「同一ロジックの配置換えのみ・観測挙動を変えない」方針（D-04）。GIL 下で単一辞書代入はアトミックであり単一ファイル OCR では実害報告がない。排他ロック導入は複数ファイルが真に並行しうる Phase 4（バッチ OCR）で扱う確認事項として明示的に defer（本フェーズではスコープ外・受容） |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規外部パッケージを一切導入しない（標準ライブラリ threading/queue/logging + 自社 ocr_pipeline のみ）。Package Legitimacy Gate 該当なし（03-RESEARCH.md Package Legitimacy Audit 参照）。install タスクなし |
</threat_model>

<verification>
- `pytest tests/test_ocr_engine.py::TestOCRRunEngineUnit -x`（Engine unit スモーク）
- `pytest tests/test_provider_ui.py tests/test_ocr_fallback.py tests/test_ocr.py -x`（OCRDialog 委譲後の回帰）
- `pytest`（フルスイート・ベースライン 987 件以上グリーン・回帰ゼロ）
- `ruff check . && ruff format --check .`（未使用 import なし・フォーマット準拠）
- `git diff --stat pagefolio/ocr_pipeline.py` が空（純ロジック層は無変更）
</verification>

<success_criteria>
- `OCRRunEngine` が `pagefolio.ocr_engine` から単独 import 可能で Tk/fitz 非依存（バッチ OCR 再利用可能構造・D-01/D-02/D-03）
- `OCRDialog._start_worker_thread` が Engine 委譲ラッパー化され、producer（`_render_next_page`）は残留（D-04）
- producer と consumer が同一 `queue.Queue`/`PipelineState` インスタンスを共有（落とし穴10 回避・T-03-02）
- 単一ファイル OCR の進捗・キャンセル・リトライ・サーキットブレーカー・resume 挙動が抽出前と同一（既存回帰テスト green）
- フルスイート green + ruff clean、`ocr_pipeline.py` 無変更
</success_criteria>

<output>
Create `.planning/phases/03-ocr-e2e/03-01-SUMMARY.md` when done
</output>
