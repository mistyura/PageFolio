---
phase: 03-qa-release-gate
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - pagefolio/constants.py
  - README.md
  - 開発履歴.md
autonomous: true
requirements: [V190-QA-03]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`pagefolio/constants.py` の `APP_VERSION` が `v1.9.0` である（D-16）"
    - "README.md のバージョンバッジが `APP_VERSION` と一致する（CLAUDE.md の 3 点同期ルール）"
    - "開発履歴.md の「最終更新」ブロック引用が v1.9.0 のマイルストーンエントリになっており、旧 v1.8.1 のエントリは下の履歴行として残っている"
    - "開発履歴.md の「バージョン索引」表の PageFolio セクション先頭に v1.9.0 の行がある"
    - "開発履歴.md の v1.9.0 エントリが、Phase 1（保存・編集・設定の安全性是正）・Phase 2（catalog 一元化 + OpenAI プロバイダ追加）・Phase 3（品質保証・リリースゲート）の実際の成果を記述している"
    - "バージョン更新後も `ruff check .` / `ruff format --check .` / フルテストスイートが失敗 0 件で完走する"
    - "PyInstaller リビルド・注釈付きタグ付与・GitHub Release 公開は本プランに含まれない（マイルストーンクローズ後のクイックタスク・D-16）"
  artifacts:
    - path: "pagefolio/constants.py"
      provides: "アプリバージョンの単一情報源"
      contains: "APP_VERSION"
    - path: "開発履歴.md"
      provides: "v1.9.0 のマイルストーンエントリとバージョン索引行"
      contains: "v1.9.0"
  key_links:
    - from: "pagefolio/constants.py"
      to: "README.md"
      via: "APP_VERSION を単一情報源としたバッジ文字列の同期"
      pattern: "version-v1\\.9\\.0-blue"
    - from: "pagefolio/constants.py"
      to: "開発履歴.md"
      via: "APP_VERSION を単一情報源とした最新エントリ・索引行の同期"
      pattern: "APP_VERSION = v1\\.9\\.0"
---

<objective>
v1.9.0 のリリース版数文書を 3 点同期させる（D-16）。`pagefolio/constants.py` の `APP_VERSION` を
`v1.9.0` へバンプし、README.md のバッジと `開発履歴.md` の最新エントリ・バージョン索引を
それに揃える。

**この作業を Phase 3 が引き受ける経緯:** Phase 1 の 01-05 / 01-06 / 01-07 と Phase 2 が、
「既存エントリはリリース単位の書式のためマイルストーン途中での追記は書式を崩す」という理由で
バージョン更新を明示的に Phase 3 へ委譲している（STATE.md の Accumulated Context に記録あり）。
v1.8.0 のマイルストーンクローズ時には `v1.7.4 → v1.8.0` のバンプ漏れが検出されており、
Phase 3 が引受け手になることで同じ漏れを構造的に防ぐ。

**含まれないもの（D-16）:** PyInstaller リビルド・注釈付きタグ付与・GitHub Release 公開。
これらは過去マイルストーン（260709-rel / 260722-rel）と同様、マイルストーンクローズ後の
クイックタスクで実施する。

**tracer について:** 本フェーズの tracer は 03-01 Task 1（保存経路の縦スライス）が担っている。
本プランはドキュメント整合のみのプランであり、「one shape per phase」に従って追加の
tracer タスクは置かない。

Purpose: `APP_VERSION` を真の情報源として、README バッジ・開発履歴.md の最新エントリが
実コード状態と食い違わない状態でリリース判定を下せるようにする。

Output: `pagefolio/constants.py` / `README.md` / `開発履歴.md` の 3 点同期
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-qa-release-gate/03-CONTEXT.md
@.planning/phases/01-safety-rollback/01-VERIFICATION.md
@.planning/phases/02-ocr-openai-chatgpt/02-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `APP_VERSION` の v1.9.0 バンプと README バッジの同期</name>
  <files>pagefolio/constants.py, README.md</files>
  <read_first>
    - `pagefolio/constants.py`（12 行目 `APP_VERSION = "v1.8.1"`）
    - `README.md`（7 行目 `![Version](https://img.shields.io/badge/version-v1.8.1-blue)` と、その周辺のバッジ行）
    - `CLAUDE.md`（「変更時のチェックリスト」の最終項目 — `APP_VERSION` を真の情報源として 3 者を同期させる規定）
  </read_first>
  <action>
`pagefolio/constants.py` の `APP_VERSION` を `"v1.8.1"` から `"v1.9.0"` へ変更する（D-16）。
定数の位置・前後のコメント・他の定数には触れない。

`README.md` のバージョンバッジ行を `![Version](https://img.shields.io/badge/version-v1.9.0-blue)`
へ更新する。同じバッジ群の `Stable` / `License` バッジには触れない。

README.md の本文にバージョン番号（`v1.8.1` など）を直書きしている箇所があれば、
`APP_VERSION` と食い違う記述だけを `v1.9.0` へ揃える。ただし過去バージョンの履歴・実績を
述べている箇所（「v1.6.2 で追加」等の説明文）は史実なので変更しない。

`ruff check . && ruff format .` を通す。`pyproject.toml` は編集しない（CLAUDE.md の禁止事項）。
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -c "from pagefolio.constants import APP_VERSION; assert APP_VERSION == 'v1.9.0', APP_VERSION; print(APP_VERSION)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'APP_VERSION = "v1.9.0"' pagefolio/constants.py` が `1`
    - `grep -c 'APP_VERSION = "v1.8.1"' pagefolio/constants.py` が `0`
    - `grep -c "version-v1.9.0-blue" README.md` が `1`
    - `grep -c "version-v1.8.1-blue" README.md` が `0`
    - `.\.venv\Scripts\python.exe -c "from pagefolio.constants import APP_VERSION; print(APP_VERSION)"` の出力が `v1.9.0`
    - `ruff check . && ruff format --check .` が通る
    - `git diff --stat -- pyproject.toml` の出力が空
    - `git diff --stat -- pagefolio/constants.py` の変更が 2 行以内（1 行の置換）
  </acceptance_criteria>
  <done>`APP_VERSION` が `v1.9.0` になり、README のバージョンバッジがそれと一致している。</done>
</task>

<task type="auto">
  <name>Task 2: 開発履歴.md への v1.9.0 マイルストーンエントリ追記とバージョン索引の更新</name>
  <files>開発履歴.md</files>
  <read_first>
    - `開発履歴.md`（1-32 行の「最終更新」ブロック引用と履歴行、34-50 行付近の「バージョン索引」表 PageFolio セクション。**v1.8.0 の書き方が直近のマイルストーンエントリの手本**）
    - `.planning/ROADMAP.md`（v1.9.0 の 3 フェーズ構成と各フェーズの Goal）
    - `.planning/REQUIREMENTS.md`（V190-* 全 27 要件 — エントリに書く「全 N 件」の根拠）
    - `.planning/phases/01-safety-rollback/01-VERIFICATION.md`（Phase 1 の実際の成果）
    - `.planning/phases/02-ocr-openai-chatgpt/02-VERIFICATION.md`（Phase 2 の実際の成果）
    - `.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md`（Phase 3 の UAT サマリ — エントリに書く品質保証の実測）
  </read_first>
  <action>
`開発履歴.md` の冒頭「最終更新」ブロック引用を v1.9.0 のマイルストーンエントリへ差し替え、
現在の `v1.8.1` エントリはその下の履歴行（`> v1.8.1 — 2026-07-22（…）` の形）へ降ろす。
**直近のマイルストーンである v1.8.0 のエントリの書き方をそのまま手本にする**
（`vX.Y.Z — YYYY-MM-DD（マイルストーン名・全 N フェーズ達成・出荷。①…②…③…。V190-* 全 N 件Complete。テスト N 件グリーン。`APP_VERSION = v1.9.0`）` の 1 段落形式）。
なお v1.8.0 / v1.8.1 は本文下部に詳細な `## vX.Y.Z` 節を持たないため、**v1.9.0 でも詳細節は
新設しない**（直近の運用に揃える）。

エントリ本文に含める内容（すべて実際の成果物から確認して書き、推測で書かない）:
  - **①Phase 1（保存・編集・設定の安全性是正）** — `01-VERIFICATION.md` から。保存経路の
    暗号化維持の全経路化・`off` プロバイダ生成不可化・設定ダイアログの書き込み一本化・
    ページ構造変更 op の部分失敗保護と `insert_redo` 系の対称復元・4 手往復回帰テストの水平展開
  - **②Phase 2（OCR プロバイダ基盤整理 + OpenAI 追加）** — `02-VERIFICATION.md` から。
    `catalog.py` 新設によるプロバイダ非機密メタデータの一元化、OpenAI(ChatGPT) プロバイダの
    追加（`urllib.request` 直叩き・新規 pip 依存ゼロ）、`max_completion_tokens` / `temperature`
    非互換の分岐、detail / reasoning effort / organization / project の設定 UI、
    API キーのセッションメモリ限定保持
  - **③Phase 3（品質保証・リリースゲート）** — 保存トースト再試行の確認スキップ（V190-QA-02）、
    テスト実行環境の切り分けとリリースゲート条件の文書化（V190-QA-01）、
    遡及 human-verify / UAT の正式消化（V190-QA-03）
  - **要件被覆** — `.planning/REQUIREMENTS.md` の V190-* の総数と Complete 数を実際に数えて書く
  - **テスト件数** — 当セッションで `pytest -q --collect-only` または `pytest -q` を実行して得た
    実測件数のみを書く。過去の値（1109 / 1387 等）を書き写さない
  - 末尾に `` `APP_VERSION = v1.9.0` `` を書く

「バージョン索引」表の `### PageFolio（リブランディング後）` セクションの**先頭行**（`v1.8.1` の
行の上）へ v1.9.0 の行を追加する。列は既存どおり `バージョン / 日付 / 種別 / 主な内容`。
`種別` は `マイルストーン`。日付は実行日を使う。

日本語で記述する（CLAUDE.md の言語ルール）。`Edit` による範囲限定の追記・置換で行い、
ファイル全体の書き換え（`Write`）は行わない。過去バージョンのエントリ・詳細節には触れない。
  </action>
  <verify>
    <automated>grep -c "APP_VERSION = v1.9.0" 開発履歴.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "最終更新.*v1.9.0" 開発履歴.md` が `1`
    - `grep -c "APP_VERSION = v1.9.0" 開発履歴.md` が `1` 以上
    - `grep -c "^| v1.9.0 " 開発履歴.md` が `1`（バージョン索引表に行が追加されている）
    - `grep -c "v1.8.1" 開発履歴.md` が着手前と同数以上（旧エントリを消していない）
    - v1.9.0 エントリ本文に Phase 1 / Phase 2 / Phase 3 の 3 つの成果が含まれている（`catalog` / `OpenAI` / `QA` 相当の語がいずれも出現する）
    - v1.9.0 エントリに書かれたテスト件数が、当セッションで実行した `pytest` の実測件数と一致する
    - `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件
    - `ruff check . && ruff format --check .` が通る
    - `python -c "import ast; ast.parse(open('pagefolio.py', encoding='utf-8').read())"` が成功する
    - `git diff --stat -- 開発履歴.md` の変更が冒頭ブロック引用とバージョン索引表の 2 箇所に収まっている
  </acceptance_criteria>
  <done>開発履歴.md の最新エントリが v1.9.0 のマイルストーンエントリになり、バージョン索引にも行が追加され、`APP_VERSION` / README バッジ / 開発履歴.md の 3 点が一致している。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `APP_VERSION`（単一情報源）→ README バッジ / 開発履歴.md / 配布物 | 版数が食い違うと、ユーザーが実際に使っているビルドと documentation が対応しなくなり、不具合報告の切り分けが不能になる |
| 過去マイルストーンの履歴記述 → 今回の追記 | 追記操作が既存エントリを壊すと、史実が失われる（v1.6.1 の日付誤記のような事故の再発面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Tampering | `APP_VERSION` と README バッジ / 開発履歴.md の版数不一致 | medium | mitigate | `pagefolio/constants.py` を単一情報源とし、受け入れ基準で 3 者の一致を grep と実インポート（`from pagefolio.constants import APP_VERSION`）の両方で検証する。旧版数文字列が残っていないことも 0 件 grep で確認する |
| T-03-04-02 | Tampering | 開発履歴.md の既存エントリ | medium | mitigate | `Edit` による範囲限定の追記・置換のみを行い `Write` を使わない。受け入れ基準で旧 `v1.8.1` の出現数が減っていないこと・差分が冒頭ブロック引用とバージョン索引表の 2 箇所に収まっていることを検証する |
| T-03-04-03 | Repudiation | v1.9.0 エントリに書くテスト件数・要件件数 | medium | mitigate | 件数は当セッションで実行した `pytest` / `REQUIREMENTS.md` の実測のみを出典とし、過去の値（1109 / 1387 等）を書き写さない。受け入れ基準で実測値との一致を検証する |
| T-03-04-04 | Tampering | ビルド設定（`pyproject.toml`） | low | mitigate | CLAUDE.md の禁止事項どおり `pyproject.toml` を編集しない。受け入れ基準で差分が空であることを検証する |
| T-03-04-SC | Tampering | npm/pip/cargo installs | low | accept | 本プランは新規パッケージのインストールを一切行わない（V14-D-01）。`03-RESEARCH.md` の Package Legitimacy Audit は「該当なし」と結論している |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**新規シンボル:** なし（既存定数 `APP_VERSION` の値変更のみ）

**新規ドキュメント行**
- `開発履歴.md` 冒頭「最終更新」ブロック引用の v1.9.0 エントリ
- `開発履歴.md`「バージョン索引 → PageFolio（リブランディング後）」表の `v1.9.0` 行

**変更される既存値**
- `pagefolio/constants.py` の `APP_VERSION`: `"v1.8.1"` → `"v1.9.0"`
- `README.md` のバージョンバッジ URL: `version-v1.8.1-blue` → `version-v1.9.0-blue`

**本プランに含まれないもの（D-16 で明示的に範囲外）**
- PyInstaller リビルド（`dist/PageFolio` の更新）
- 注釈付きタグ `v1.9.0` の付与
- GitHub Release の公開
</artifacts_this_phase_produces>

<verification>
- `.\.venv\Scripts\python.exe -c "from pagefolio.constants import APP_VERSION; print(APP_VERSION)"` の出力が `v1.9.0`
- `grep -c "version-v1.9.0-blue" README.md` が `1`
- `grep -c "APP_VERSION = v1.9.0" 開発履歴.md` が `1` 以上
- `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件
- `ruff check . && ruff format --check .` が通る
- `git diff --stat -- pyproject.toml` の出力が空
</verification>

<success_criteria>
- `APP_VERSION` が `v1.9.0` であり、README バッジ・開発履歴.md の最新エントリと 3 点一致している
- 開発履歴.md の v1.9.0 エントリが Phase 1 / 2 / 3 の実際の成果を記述しており、件数は実測に基づく
- バージョン索引表に v1.9.0 の行がある
- バンプ後もフルテストスイート・ruff が失敗 0 件
</success_criteria>

<output>
Create `.planning/phases/03-qa-release-gate/03-04-SUMMARY.md` when done
</output>
