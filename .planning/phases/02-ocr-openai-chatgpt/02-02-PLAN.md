---
phase: 02-ocr-openai-chatgpt
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pagefolio/ocr_dialog.py
  - pagefolio/dialogs/batch_ocr.py
  - pagefolio/lang.py
  - tests/test_provider_ui.py
  - tests/test_batch_ocr_dialog.py
autonomous: true
requirements:
  - V190-CAT-01
  - V190-OAI-04
  - V190-OAI-05
  - V190-OAI-06

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "OCR ダイアログのプロバイダ表示名解決が catalog の display_name_key を唯一の情報源とし、if 連鎖版と dict 版の 2 実装が同一の catalog 参照へ統合されている（V190-CAT-01・D-03）"
    - "OCR ダイアログとバッチ OCR ダイアログのクラウド判定が catalog.is_cloud_provider を経由し、既知クラウドプロバイダクラスの isinstance フォールバックは安全側として維持されている（D-04）"
    - "OCR ダイアログとバッチ OCR ダイアログの送信先ホスト分岐 5 箇所すべてが catalog.host_for を経由し、host 文字列リテラルが両ファイルから消えている（V190-CAT-01）"
    - "API キー欠落エラーの LANG キー dict が両ファイルから消え、catalog.api_key_missing_lang_key_for に置き換わっている（D-01）"
    - "ocr_provider が openai のとき、OCR 実行前に送信先ホスト api.openai.com を明示した確認ダイアログが表示される（V190-OAI-04）"
    - "ocr_provider が openai のとき、OCR 実行前に対象ページ数と概算コストを含む確認ダイアログが表示される（V190-OAI-05）"
    - "バッチ OCR で openai を使うとき、クラウド判定・集約コスト確認・送信先表示・APIキー欠落エラーが単発 OCR と同じ内容で機能する（V190-OAI-06）"
    - "確認ダイアログが表示する送信先ホストと OpenAIProvider が実際にリクエストするホストが一致することがテストで固定されている（T-02-04）"
    - "OpenAI が全ページ統合サマリのフォールバック候補として扱われる（_TEXT_CAPABLE_PROVIDERS に openai が含まれる）"
    - "lang.py の ja / en に ocr_provider_name_openai と ocr_api_key_missing_openai が同一キーで追加され、キー数の左右一致と未使用キー検出テストが green のまま"
    - "BatchOCRDialog は OCRDialog を継承せずメソッド import もせず、catalog 経由で共有するのはデータのみ（RESEARCH.md Anti-Patterns）"
  prohibitions:
    - statement: "MUST NOT 送信先確認・コスト確認ダイアログに「今後表示しない」等の抑止手段を設けてはならない。フォールバックで OpenAI へ切り替わる場合を含め、外部送信のたびにユーザーの明示同意を取り直す"
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "pagefolio/ocr_dialog.py"
      provides: "catalog 経由の表示名 / クラウド判定 / 送信先ホスト / APIキー欠落解決と openai 対応"
      contains: "catalog.is_cloud_provider"
    - path: "pagefolio/dialogs/batch_ocr.py"
      provides: "同型の独立実装（catalog 経由・ロジック非共有）"
      contains: "catalog.is_cloud_provider"
    - path: "pagefolio/lang.py"
      provides: "ocr_provider_name_openai / ocr_api_key_missing_openai（ja/en）"
      contains: "ocr_api_key_missing_openai"
  key_links:
    - from: "pagefolio/ocr_dialog.py"
      to: "pagefolio/ocr_providers/catalog.py"
      via: "表示名 / クラウド判定 / host / APIキー欠落 LANG キーの解決"
      pattern: "catalog\\.(is_cloud_provider|host_for|display_name_key_for|api_key_missing_lang_key_for)"
    - from: "pagefolio/dialogs/batch_ocr.py"
      to: "pagefolio/ocr_providers/catalog.py"
      via: "同型の独立実装から同じ catalog を参照（データのみ共有）"
      pattern: "catalog\\.(is_cloud_provider|host_for|api_key_missing_lang_key_for)"
    - from: "pagefolio/ocr_dialog.py"
      to: "pagefolio/lang.py"
      via: "ocr_api_key_missing_openai / ocr_provider_name_openai の参照"
      pattern: "ocr_(api_key_missing|provider_name)_openai"
---

<objective>
OCR ダイアログとバッチ OCR ダイアログに散在するプロバイダメタデータ参照（表示名 2 実装・
クラウド判定 2 箇所・送信先ホスト 5 箇所・APIキー欠落 dict 2 箇所）を `catalog` 経由へ移行し、
同時に OpenAI を既存 5 プロバイダと同じ安全境界（送信先確認・コスト確認・APIキー欠落エラー）に
組み込む。

Purpose: このプランが完了して初めて「`ocr_provider == "openai"` を選んだユーザーが、
ページ画像が `api.openai.com` へ送られる前に必ず送信先とコストの確認ダイアログを見る」という
安全境界が成立する。02-03（LLM 設定 UI への openai 追加）はこのゲートの完成を待つ。

Output: `ocr_dialog.py` 7 メソッド・`batch_ocr.py` 5 メソッドの catalog 移行と openai 対応、
`lang.py` へ ja/en 2 キー追加、価格表への OpenAI 単価、回帰テストとミューテーション検証。

**UI 設計契約について（記録）:** 本フェーズは `--skip-ui` で計画されており `-UI-SPEC.md` は存在しない。
UI 安全ゲートは本フェーズを frontend-touching と判定したが、PageFolio は Tkinter デスクトップアプリで
変更対象が既存 `messagebox` ダイアログの文言・分岐に限られ、02-PATTERNS.md が既存 Claude/Gemini 分岐との
一致度を「exact」と判定しているため override した。新規ウィジェットは追加しない。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr-openai-chatgpt/02-CONTEXT.md
@.planning/phases/02-ocr-openai-chatgpt/02-PATTERNS.md
@.planning/phases/02-ocr-openai-chatgpt/02-01-SUMMARY.md
@CLAUDE.md
@pagefolio/CLAUDE.md
</context>

<flagged_assumptions>
## Flagged Assumptions（edge probe — unresolved / unclassified）

| Requirement | Probe | 本プランでの扱い（未解決の前提） |
|---|---|---|
| V190-OAI-04 | unclassified — review manually | 「送信先ホストを明示した確認ダイアログが表示される」の自動検証は `messagebox.askyesno` をフェイクに差し替えたときの引数文字列アサーションまで。実際にモーダルが描画され人間が読める形で表示されることは 02-04 の human-verify に委ねる。また、フォールバック経路では `_fallback_resume` フラグにより `_confirm_cost` が二重表示されない既存仕様が働くため、「必ず 1 回は同意を取る」保証はフォールバック確認ダイアログとの合算で成立している前提を置いている |
| V190-OAI-05 | unclassified — review manually | 概算コストは `OCR_PRICE_TABLE` の静的単価 × 固定トークン見積り（入力 1600 / 出力 500 per page）に基づく既存方式をそのまま流用する。OpenAI の実課金額との乖離は検証しない（実コスト計測は v2 の Deferred 項目）。Task 1 の checkpoint で確定したモデル ID に対応する単価が公表値と異なっていた場合、表示額がずれる |
| V190-OAI-06 | unclassified — review manually | バッチ OCR の検証は `BatchOCRDialog` の該当メソッドを直接呼ぶ単体テストまで。複数ファイル × 複数ページの実バッチ実行で openai が完走することは 02-04 の human-verify に委ねる。`BatchOCRDialog` と `OCRDialog` のロジックは意図的に独立実装のままなので、両者の挙動一致はテストの対称性でのみ担保される |
</flagged_assumptions>

<artifacts>
## Artifacts this phase produces（本プランの分）

**新規 LANG キー（ja / en 両方に同一キー）**
- `ocr_provider_name_openai` — 表示名（例 ja: `"OpenAI (ChatGPT)"` / en: `"OpenAI (ChatGPT)"`）
- `ocr_api_key_missing_openai` — API キー未解決時のエラー本文（`{env_var}` プレースホルダを持つ。
  既存 `ocr_api_key_missing_gemini` / `ocr_api_key_missing_runpod` と同型）

**新規 `OCR_PRICE_TABLE` エントリ（`ocr_dialog.py` と `dialogs/batch_ocr.py` に同一内容で 2 箇所・D-02）**
- 02-01 Task 1 で確定した OpenAI モデル ID とそのモデルファミリ接頭辞に対する
  `(入力単価, 出力単価)` タプル

**既存シンボルの意味変更（削除される重複表現）**
- `ocr_dialog.py`: `_provider_display_name` / `_provider_key_to_display_name` の 2 実装が
  catalog 参照へ統合、`_is_cloud_provider` の集合リテラル、`_confirm_cost` /
  `_confirm_summary_cost` / `_fallback_candidate_host` の host リテラル、
  `_check_cloud_api_key` の LANG キー dict
- `dialogs/batch_ocr.py`: `_is_cloud_provider` の集合リテラル、`_build_provider_once` の
  `_cloud_providers` 集合、`_confirm_cost` / `_confirm_summary_cost` の host リテラル、
  `_check_cloud_api_key` の LANG キー dict
- `ocr_dialog.py`: `_TEXT_CAPABLE_PROVIDERS` に `"openai"` を追加
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: ocr_dialog.py の catalog 移行と OpenAI 安全境界</name>
  <files>pagefolio/ocr_dialog.py, pagefolio/lang.py</files>
  <read_first>
    - `pagefolio/ocr_providers/catalog.py`（02-01 で作成。公開関数のシグネチャと `PROVIDERS["openai"]` の値）
    - `pagefolio/ocr_dialog.py` 20-75 行（`OCR_PRICE_TABLE` / `_PRICE_FALLBACK` / `_TEXT_CAPABLE_PROVIDERS`）
    - `pagefolio/ocr_dialog.py` 810-935 行（`_provider_display_name` / `_provider_model_name` / `_is_cloud_provider`）
    - `pagefolio/ocr_dialog.py` 1214-1325 行（`_confirm_cost` / `_confirm_summary_cost` / `_check_cloud_api_key`）
    - `pagefolio/ocr_dialog.py` 2322-2375 行（`_active_provider_name` / `_provider_key_to_display_name` / `_fallback_candidate_host`）
    - `pagefolio/lang.py` 470-535 行（ja の `ocr_api_key_missing*` / `ocr_provider_name_*` 群）と 1215-1280 行（en の同群）
    - `.planning/phases/02-ocr-openai-chatgpt/02-PATTERNS.md`「移行対象の重複箇所（実在箇所・全列挙）」表
    - `.planning/phases/02-ocr-openai-chatgpt/02-CONTEXT.md`（D-01 / D-02 / D-03 / D-04）
  </read_first>
  <action>
`pagefolio/ocr_dialog.py` の 7 箇所を catalog 経由へ移行し、あわせて openai を対応させる。
既存の関数内 import 流儀に合わせ `from pagefolio.ocr_providers import catalog` は
モジュール冒頭ではなく各メソッド内で行う（`pagefolio.ocr_providers` 経由の重い import 連鎖と
循環 import を避ける既存方針。`_check_cloud_api_key` が `registry` をメソッド内 import しているのと同じ形）。
D-03 のとおり一括置換はせず、下記 (1)〜(7) を 1 つずつ適用してその都度テストを通すこと。

(1) `_provider_display_name`（812-839 行）: claude / gemini の isinstance 判定を伴う 2 分岐は
D-04 の安全側フォールバックとして残しつつ、名前による表示名解決を
`catalog.display_name_key_for(name)` → `self._L[key]` の 1 経路へ統合する。
`display_name_key` が None のプロバイダ（`off` / `ollama` / 未登録プラグイン）は
現行どおり `name` をそのまま返す。`lmstudio` と `""` が同じ表示名になる現行挙動を維持するため、
空文字は `lmstudio` として解決すること。

(2) `_provider_key_to_display_name`（2329-2351 行）: dict リテラルを削除し (1) と同じ
`catalog.display_name_key_for` 経由に置換する。isinstance 判定は行わない現行の性格を維持する
（フォールバック候補名の表示に使うため、まだ build されていない候補にも使える必要がある）。
`""` → LM Studio 表示名のマッピングは維持する。

(3) `_is_cloud_provider`（908-929 行）: 集合リテラル `("claude", "gemini", "runpod")` を
`catalog.is_cloud_provider(name)` に置換する。`ClaudeProvider` / `GeminiProvider` /
`RunPodProvider` の isinstance ガードは削除せずそのまま残す（D-04）。
`OpenAIProvider` を isinstance ガードのタプルへ追加すること
（catalog 登録済みなので通常経路では不要だが、プラグインが `OpenAIProvider` を継承した場合に
外部送信の同意ゲートが消えないようにする。既存 3 クラスと同じ安全側の扱いに揃える）。

(4) `_confirm_cost`（1216-1257 行）: host の if/elif/else を `catalog.host_for(name, s)` に置換する。
`runpod` の「URL 未設定なら `self._L["llm_runpod_host_unset"]`」という現行フォールバックは
`host_for` が空文字を返したときの分岐として呼び出し側に残す（catalog に LANG 依存を持ち込まない）。
モデル名の解決も `catalog.model_setting_key_for(name)` と `catalog.default_model_for(name)` を使い、
`s.get(model_key, "") or catalog.default_model_for(name)` の 1 経路へ統合する。
これにより `openai` は `s.get("openai_model", "")` → 空なら catalog 既定モデル、
host は `api.openai.com` として自動的に扱われる。
未登録プロバイダで `model_setting_key` が None の場合は現行の claude フォールバックではなく
空文字モデル + 空文字 host になるが、`_confirm_cost` は `_is_cloud_provider` が True の
ときにしか呼ばれないため到達しない。到達した場合に備え host が空なら
`self._L["llm_runpod_host_unset"]` ではなくプロバイダ表示名を出す分岐を置く。

(5) `_confirm_summary_cost`（1259-1287 行）: (4) と同じ host 解決へ置換する。

(6) `_fallback_candidate_host`（2353-2372 行）: 候補名ごとの個別 if を
`catalog.host_for(candidate, settings)` へ統合する。ローカルプロバイダ（`lmstudio` / `ollama`）は
catalog の host が None なので、呼び出し側で `settings.get("lm_studio_url", "http://localhost:1234")` /
`settings.get("ollama_url", "http://localhost:11434")` を返す現行分岐を残す。
`tesseract` はネットワーク送信を伴わないため表示名を返す現行挙動を維持する。
`openai` は catalog の host（`api.openai.com`）が返る。

(7) `_check_cloud_api_key`（1289-1324 行）: LANG キー dict リテラル（1310-1314 行）を削除し
`catalog.api_key_missing_lang_key_for(name) or "ocr_api_key_missing"` に置換する。
`primary_env_var(name)` による `{env_var}` 埋め込みと `messagebox.showerror` の外枠は変更しない。

(8) `OCR_PRICE_TABLE`（37-52 行）に OpenAI モデルの単価エントリを追加する。
キーは 02-01 Task 1 の checkpoint で確定したモデル ID と、そのモデルファミリを表す
接頭辞（既存の `claude-sonnet` / `claude-opus` と同じ粒度の部分一致キー）。
最低でも catalog の `default_model` と `RECOMMENDED_MODELS` の全モデルが
`_lookup_price` の完全一致または部分一致で解決できること。
未知モデルは既存 `_PRICE_FALLBACK` が受けるため新設しない。価格表を catalog へ移すことはしない（D-02）。

(9) `_TEXT_CAPABLE_PROVIDERS`（62-64 行）に `"openai"` を追加する
（`OpenAIProvider.supports_text_prompt = True` と整合させ、全ページ統合サマリの
フォールバック候補として扱えるようにする）。

(10) `pagefolio/lang.py` の ja / en 両方へ同一キーで 2 件追加する。
`ocr_provider_name_openai` は既存 `ocr_provider_name_claude`（`"Claude (Anthropic)"`）と
同じ体裁の 1 行文字列。`ocr_api_key_missing_openai` は既存 `ocr_api_key_missing_gemini` の
本文を雛形にし、`{env_var}` プレースホルダと「LLM 設定ダイアログでセッション限定キーを
入力する」旨を含める。ja / en でキー数が一致することと、追加キーが (1)〜(7) の
いずれかから実際に参照されることを確認する（`tests/test_lang_parity.py` の
`test_lang_keys_parity` と `test_no_unused_lang_keys` が常設）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_provider_ui.py tests/test_lang_parity.py tests/test_ocr.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr_dialog.py` に `catalog.is_cloud_provider` / `catalog.host_for` / `catalog.display_name_key_for` / `catalog.api_key_missing_lang_key_for` の 4 種の呼び出しがすべて出現する
    - `python -c "import re,pathlib; s=pathlib.Path('pagefolio/ocr_dialog.py').read_text(encoding='utf-8'); assert 'api.anthropic.com' not in s and 'generativelanguage.googleapis.com' not in s"` が終了コード 0（host リテラルの消滅）
    - `python -c "import re,pathlib; s=pathlib.Path('pagefolio/ocr_dialog.py').read_text(encoding='utf-8'); assert '\"ocr_api_key_missing_gemini\"' not in s"` が終了コード 0（LANG キー dict の消滅）
    - `python -c "import pathlib; s=pathlib.Path('pagefolio/ocr_dialog.py').read_text(encoding='utf-8'); assert 'OpenAIProvider' in s"` が終了コード 0（D-04 の isinstance ガード）
    - `python -c "from pagefolio.lang import LANG; assert set(LANG['ja']) == set(LANG['en']); assert 'ocr_provider_name_openai' in LANG['ja'] and 'ocr_api_key_missing_openai' in LANG['ja']"` が終了コード 0
    - `python -c "from pagefolio.ocr_dialog import _lookup_price, OCR_PRICE_TABLE; from pagefolio.ocr_providers import OpenAIProvider; assert all(_lookup_price(m) != (5.0, 25.0) for m in OpenAIProvider.RECOMMENDED_MODELS)"` が終了コード 0（全推奨モデルが `_PRICE_FALLBACK` ではなく実単価に解決される）
    - `python -c "from pagefolio.ocr_dialog import _TEXT_CAPABLE_PROVIDERS; assert 'openai' in _TEXT_CAPABLE_PROVIDERS"` が終了コード 0
    - 既存の `tests/test_provider_ui.py::TestIsCloudProvider` / `TestConfirmCost` / `TestCheckCloudApiKey` が無改修で green（catalog 移行が既存 3 プロバイダの挙動を変えていないことの担保）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>`ocr_provider == "openai"` のとき `_is_cloud_provider` が True、`_confirm_cost` / `_confirm_summary_cost` が `api.openai.com` を含む本文を返し、`_check_cloud_api_key` が `ocr_api_key_missing_openai` を表示する。既存 5 プロバイダの表示名・host・エラー文言は無変更。</done>
</task>

<task type="auto">
  <name>Task 2: batch_ocr.py の同型 catalog 移行と OpenAI 対応</name>
  <files>pagefolio/dialogs/batch_ocr.py</files>
  <read_first>
    - `pagefolio/dialogs/batch_ocr.py` 1-20 行（「OCRDialog からのコピペ移植・ロジックを共有しない」設計方針コメント）
    - `pagefolio/dialogs/batch_ocr.py` 60-115 行（`OCR_PRICE_TABLE` / `_lookup_price` の独立コピー）
    - `pagefolio/dialogs/batch_ocr.py` 485-570 行（`_is_cloud_provider` / `_estimate_cost` / `_confirm_cost` / `_check_cloud_api_key` / `_confirm_batch_cost`）
    - `pagefolio/dialogs/batch_ocr.py` 592-620 行（`_build_provider_once` の `_cloud_providers` 集合）
    - `pagefolio/dialogs/batch_ocr.py` 965-985 行（`_confirm_summary_cost`）
    - `pagefolio/ocr_dialog.py`（Task 1 で移行済みの対応メソッド — 同一挙動の独立実装として写経元にする）
    - `pagefolio/ocr_providers/catalog.py`
  </read_first>
  <action>
`pagefolio/dialogs/batch_ocr.py` の 5 箇所を、Task 1 で `ocr_dialog.py` に適用したものと
**同一挙動の独立実装**として移行する。継承・メソッド import による DRY 化は行わない
（ファイル冒頭コメントが明示的に否定している設計判断・RESEARCH.md Anti-Patterns）。
「`ocr_dialog.py:xxx` と同一挙動の独立実装。」という既存の docstring 規約を維持し、
参照している行番号が変わった場合は更新する。catalog は各メソッド内で import する。

(1) `_is_cloud_provider`（486-500 行）: 集合リテラルを `catalog.is_cloud_provider(name)` に置換し、
isinstance ガードのタプルへ `OpenAIProvider` を追加する（Task 1(3) と同一）。

(2) `_build_provider_once`（602 行）: `_cloud_providers = {"claude", "gemini", "runpod"}` の
集合リテラルと `if name in _cloud_providers:` を `if catalog.is_cloud_provider(name):` へ置換する。
API キー事前解決の外枠（`_resolve_api_key` / `OCRAPIKeyError` の捕捉）は変更しない。

(3) `_confirm_cost`（511-532 行）: host とモデル名の解決を Task 1(4) と同一の
`catalog.host_for` / `catalog.model_setting_key_for` / `catalog.default_model_for` 経由へ置換する。
`page_count` が None のときの `STATUS_ERROR` 除外集計は変更しない。

(4) `_confirm_summary_cost`（969-982 行）: Task 1(5) と同一の host 解決へ置換する。

(5) `_check_cloud_api_key`（534-560 行）: LANG キー dict リテラル（548-552 行）を
`catalog.api_key_missing_lang_key_for(name) or "ocr_api_key_missing"` へ置換する。

(6) `OCR_PRICE_TABLE`（71-103 行）へ Task 1(8) と**同一内容**の OpenAI 単価エントリを追加する
（D-02 により価格表の一元化は行わない。両ファイルの内容が一致していることを Task 3 のテストで固定する）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_batch_ocr_dialog.py tests/test_batch_ocr_state.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/batch_ocr.py` に `catalog.is_cloud_provider` / `catalog.host_for` / `catalog.api_key_missing_lang_key_for` の呼び出しがすべて出現する
    - `python -c "import pathlib; s=pathlib.Path('pagefolio/dialogs/batch_ocr.py').read_text(encoding='utf-8'); assert 'api.anthropic.com' not in s and 'generativelanguage.googleapis.com' not in s and '_cloud_providers' not in s"` が終了コード 0
    - `python -c "from pagefolio.ocr_dialog import OCR_PRICE_TABLE as A; from pagefolio.dialogs.batch_ocr import OCR_PRICE_TABLE as B; assert A == B"` が終了コード 0（2 箇所の価格表が同一内容）
    - `python -c "import pathlib; s=pathlib.Path('pagefolio/dialogs/batch_ocr.py').read_text(encoding='utf-8'); assert 'from pagefolio.ocr_dialog import OCRDialog' not in s and 'OCRDialog)' not in s"` が終了コード 0（継承・ロジック import が導入されていない）
    - 既存の `tests/test_batch_ocr_dialog.py` が無改修で green
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>バッチ OCR でも openai がクラウド扱いされ、集約コスト確認に `api.openai.com` が表示され、API キー未解決時に openai 専用のエラー文言が出る。`OCRDialog` への依存は増えていない。</done>
</task>

<task type="auto">
  <name>Task 3: 回帰テストと検知力（ミューテーション）検証</name>
  <files>tests/test_provider_ui.py, tests/test_batch_ocr_dialog.py</files>
  <read_first>
    - `tests/test_provider_ui.py` 348-730 行（`TestIsCloudProvider` / `TestEstimateCost` / `TestCheckCloudApiKey` / `TestConfirmCost` のフィクスチャとスタブ記法）
    - `tests/test_batch_ocr_dialog.py`（クラウド判定・コスト確認まわりの既存テストクラスとスタブ記法）
    - `pagefolio/ocr_dialog.py` / `pagefolio/dialogs/batch_ocr.py`（Task 1・Task 2 適用後の実体）
    - `.planning/phases/02-ocr-openai-chatgpt/02-RESEARCH.md`「Pitfall 1: カタログ移行時に monkeypatch 対象の名前空間が断絶する」
  </read_first>
  <action>
既存テストクラスへ openai ケースを追加し、catalog 移行が検知力を失っていないことを確認する。
新しいテスト作法は持ち込まず、既存クラスと同じスタブ（`_ButtonStub` / `_SetGetVarStub` /
`_VarStub` 等）を再利用する。

(A) `tests/test_provider_ui.py`:
- `TestIsCloudProvider` に openai ケース（`ocr_provider="openai"` で True）と、
  catalog 未登録のプラグイン名で False になるケース（D-04）を追加。
- `TestConfirmCost` に openai ケースを追加し、`messagebox.askyesno` へ渡る本文に
  `api.openai.com` が含まれることと、`openai_model` 未設定時に catalog 既定モデルが
  使われることをアサート。`_confirm_summary_cost` の openai ケースも同型で追加。
- `TestCheckCloudApiKey` に openai ケースを追加し、`_resolve_api_key` が
  `OCRAPIKeyError` を上げたとき `showerror` の本文が `ocr_api_key_missing_openai` 由来で
  `OPENAI_API_KEY` を含むことをアサート。
- 新規 `TestProviderDisplayNameCatalog` — `_provider_display_name` と
  `_provider_key_to_display_name` が 8 プロバイダすべてで同じ表示名を返すこと
  （2 実装が 1 情報源へ統合されたことの機械保証）。`""` は LM Studio 表示名、
  未登録名はそのまま返ること。
- 新規 `TestConfirmedHostMatchesProviderEndpoint` — T-02-04 の乖離検知。
  `catalog.host_for("openai", {})` と `urllib.parse.urlsplit(OpenAIProvider.CHAT_ENDPOINT).hostname`
  が一致すること。claude / gemini についても同様に、catalog の host と各 Provider の
  エンドポイント定数のホスト部が一致することを確認する。
- 新規 `TestFallbackCandidateHostCatalog` — `_fallback_candidate_host` が 8 プロバイダで
  移行前と同じ文字列を返すこと（openai は `api.openai.com`、lmstudio / ollama は
  settings の URL、tesseract は表示名）。

(B) `tests/test_batch_ocr_dialog.py`: (A) と対称に、バッチ側の
`_is_cloud_provider` / `_confirm_cost` / `_confirm_summary_cost` / `_check_cloud_api_key` /
`_build_provider_once` の openai ケースを追加する。加えて、単発 OCR とバッチで
同一 settings に対する `_confirm_cost` の本文（host 部分）が一致することを
アサートするテストを 1 本置き、独立実装の挙動一致を固定する。

(C) ミューテーション検証（RESEARCH.md Pitfall 1）: 本番コードへ意図的に 1 つバグを注入し、
テストが赤になることを確認してから revert する。最低 2 パターンを実施して SUMMARY に記録:
1. `catalog.PROVIDERS["openai"].host` を別ホスト文字列へ一時変更 →
   `TestConfirmedHostMatchesProviderEndpoint` と `TestConfirmCost` の openai ケースが赤になる
2. `ocr_dialog.py:_check_cloud_api_key` の catalog 参照を固定文字列
   `"ocr_api_key_missing"` へ一時変更 → `TestCheckCloudApiKey` の openai ケースが赤になる
どちらも「テストが green のまま通ってしまう」場合は monkeypatch 対象の名前空間が
断絶している（テストが実体を見ていない）ので、テスト側のパッチ対象を修正すること。
検証後は必ず revert し、`git status` がクリーンであることを確認する。
  </action>
  <verify>
    <automated>python -m pytest tests/test_provider_ui.py tests/test_batch_ocr_dialog.py tests/test_lang_parity.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_provider_ui.py -k "openai or Catalog or Endpoint" -q` が最低 6 テスト green
    - `python -m pytest tests/test_batch_ocr_dialog.py -k "openai" -q` が最低 4 テスト green
    - ミューテーション検証 2 パターンの「注入 → 赤 → revert → green」がそれぞれ実施され、赤になったテスト名が SUMMARY に記録されている
    - ミューテーション検証後に `git status --porcelain pagefolio/` が空
    - `python -m pytest -q --ignore=tests/test_ocr_pipeline.py` の失敗件数がプラン着手前と同数以下
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>openai を含む 8 プロバイダについて表示名・クラウド判定・送信先ホスト・APIキー欠落メッセージが単発 OCR / バッチ OCR の両方で回帰テストに固定され、意図的なバグ注入で確実に赤くなることを確認済み。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ローカルアプリ → `api.openai.com` | ページ画像が組織外へ出る地点。ユーザーの明示同意ゲート（送信先確認・コスト確認）がここに立つ |
| 表示（確認ダイアログ）→ 実際の送信先 | ユーザーが同意した対象と実際の通信先が一致していることが同意の前提 |
| サードパーティプラグイン → クラウド判定 | プラグインが `is_cloud_provider` を False に落として同意ゲートを迂回し得る地点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | `ocr_dialog.py:_is_cloud_provider` / `_confirm_cost`・`batch_ocr.py` 同型 | high | mitigate | `catalog.is_cloud_provider("openai") == True` により openai が同意ゲート対象になる（Task 1(3) / Task 2(1)）。「今後表示しない」オプションは設けない既存方針を維持（must_haves.prohibitions） |
| T-02-04 | Spoofing | 確認ダイアログの表示ホスト vs `OpenAIProvider.CHAT_ENDPOINT` | high | mitigate | 表示ホストの情報源を `catalog.host_for` の 1 箇所に統合（Task 1(4)(5)(6) / Task 2(3)(4)）し、`TestConfirmedHostMatchesProviderEndpoint` で catalog の host と Provider のエンドポイント定数のホスト部一致を機械保証（Task 3(A)） |
| T-02-07 | Elevation of Privilege | プラグインプロバイダによるクラウド判定回避 | medium | mitigate | catalog 未登録プロバイダは `is_cloud_provider` が False だが、`ClaudeProvider` / `GeminiProvider` / `RunPodProvider` / `OpenAIProvider` を継承したインスタンスは isinstance ガードで True に倒す（D-04・Task 1(3) / Task 2(1)）。判定経路が 2 本残ることは意図的な安全側の選択 |
| T-02-09 | Repudiation | 同意記録の欠如 | low | accept | PageFolio はローカル単一ユーザーアプリで監査ログ機構を持たず、同意は「毎回ダイアログを出す」ことで担保する設計。同意ログの永続化は本フェーズのスコープ外（外部送信のたびに再確認する方針で代替する） |
| T-02-10 | Information Disclosure | フォールバック連鎖での確認スキップ | high | mitigate | `_propose_fallback` は各段で `messagebox.askyesno` を必須化する既存実装（V180-D-02）。openai は `_fallback_candidate_host` から `api.openai.com` を表示できるようになる（Task 1(6)）。フォールバック候補としての実配線は 02-04 Task 2 |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本プランは新規パッケージを一切インストールしない（V190-OAI-11）。RESEARCH.md「Package Legitimacy Audit」該当なしの判定を継承 |
</threat_model>

<verification>
- `python -m pytest tests/test_provider_ui.py tests/test_batch_ocr_dialog.py tests/test_lang_parity.py tests/test_ocr.py -q` が全件 green
- `python -m pytest -q --ignore=tests/test_ocr_pipeline.py` と `python -m pytest -q tests/test_ocr_pipeline.py` の分割実行で失敗件数が着手前と同数以下
- `ruff check . && ruff format --check .` が終了コード 0
- `git diff pagefolio/ocr_providers/registry.py pagefolio/ocr_providers/errors.py` が空（D-06 / D-12）
- ミューテーション検証 2 パターンの結果が SUMMARY に記録され、作業ツリーがクリーン
</verification>

<success_criteria>
- V190-CAT-01: `ocr_dialog.py` 7 箇所 + `batch_ocr.py` 5 箇所が catalog 経由になり、host / 表示名 / APIキー欠落 LANG キーのリテラル重複が両ファイルから消えた（残る移行対象は `sections.py` の一覧リスト 2 箇所のみ）
- V190-OAI-04: openai 選択時、OCR 実行前に `api.openai.com` を明示した確認ダイアログが出る
- V190-OAI-05: openai 選択時、対象ページ数と概算コストの確認ダイアログが出る
- V190-OAI-06: バッチ OCR でも openai がクラウド判定・集約コスト確認・送信先表示・APIキー欠落エラーの対象になる
- 既存 5 プロバイダの表示名・送信先・エラー文言・コスト表示が無変更（既存テストが無改修で green）
</success_criteria>

<output>
Create `.planning/phases/02-ocr-openai-chatgpt/02-02-SUMMARY.md` when done
</output>
