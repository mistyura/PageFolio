---
phase: 01-foundation-split
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - pagefolio/settings.py
  - pagefolio/ocr.py
  - pagefolio/ocr_dialog.py
autonomous: true
requirements: [V180-ROBUST-02]
tags: [refactor, registry, sensitive-keys, api-key-resolution, security]

must_haves:
  truths:
    - "`settings._SENSITIVE_KEYS` が `registry.sensitive_keys()` から生成される（ハードコード集合を撤廃・ROBUST-02）"
    - "既存 keyguard 3経路テスト（claude/anthropic/gemini/api_key の JSON 非出力・ログ非出力・入力 dict 非破壊）が緑を維持する"
    - "`ocr._resolve_api_key` が `registry.env_vars_for()` 経由で解決し、セッションキー優先 → 環境変数フォールバックの2段構造と Gemini dual env var 優先順（GEMINI_API_KEY → GOOGLE_API_KEY）を不変で保持する"
    - "`ocr_dialog._check_cloud_api_key` のエラーメッセージ用 env var 参照が `registry.primary_env_var()` を使い、未知プロバイダで空文字フォールバック（現行 .get(name, \"\") 挙動）を保持する"
    - "test_provider_ui のキー解決・コスト確認・セッションキーテストが全緑を維持する"
  prohibitions:
    - statement: "os.environ への書き込みを行わない（キー解決は読み取り専用原則の継続）"
      status: resolved
      verification: "test_settings_keyguard.py / test_provider_ui.py のキー解決テストが緑（環境変数を書き換えない）"
  artifacts:
    - "registry 参照へ置換された settings.py / ocr.py / ocr_dialog.py"
  key_links:
    - "D-09 の4参照面のうち3面（settings._SENSITIVE_KEYS / ocr._resolve_api_key / ocr_dialog._check_cloud_api_key）が registry.py を Single Source of Truth として参照する（残り1面 llm_config は Plan 04）"
    - "settings.py は `from pagefolio.ocr_providers.registry import sensitive_keys` とサブモジュール直接指定で import し循環を回避（Pitfall 6）"
---

<objective>
D-09（プロバイダ→環境変数マッピングの全参照面統合）のうち、分割対象外の3ファイル面を Plan 02 で新設した `registry.py` への参照へ置換する: (1) `settings._SENSITIVE_KEYS` をハードコード集合から `registry.sensitive_keys()` 生成へ、(2) `ocr._resolve_api_key` の claude/gemini/runpod ハードコード分岐を `registry.env_vars_for()` 経由の優先順ループへ、(3) `ocr_dialog._check_cloud_api_key` のエラーメッセージ用 env var dict を `registry.primary_env_var()` へ。残る4番目の面（`llm_config` の環境変数チェック）は同ファイルが Plan 04 で分割されるため Plan 04 に同梱する。

これにより V180-ROBUST-02 の目標「新プロバイダ追加時に手動リストへの追加漏れが構造的に起きなくなる」を実現する。挙動は完全に不変に保つ（Gemini の dual env var 優先順・セッションキー優先・未知プロバイダのエラー挙動・os.environ 非書き込み原則をすべて維持）。安全網は既存 `test_settings_keyguard.py` の3経路テストと `test_provider_ui.py` のキー解決/コスト確認テスト。

Purpose: V180-ROBUST-02 を完成させ、API キー秘匿判定と env var 解決の重複マッピングを排除して単一の情報源に集約する。
Output: registry を参照する settings.py / ocr.py / ocr_dialog.py。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation-split/01-CONTEXT.md
@.planning/phases/01-foundation-split/01-RESEARCH.md
@.planning/phases/01-foundation-split/01-PATTERNS.md

@pagefolio/settings.py
@pagefolio/ocr.py
@pagefolio/ocr_dialog.py
@tests/test_settings_keyguard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: settings._SENSITIVE_KEYS を registry.sensitive_keys() 生成へ置換</name>
  <files>pagefolio/settings.py</files>
  <read_first>
    - pagefolio/settings.py（現行 _SENSITIVE_KEYS 定義 23-34 行・冒頭 import ブロック 6-16 行）
    - pagefolio/ocr_providers/registry.py（Plan 02 新設の sensitive_keys() 実装）
    - tests/test_settings_keyguard.py（既存 TestSensitiveKeysConstant と3経路ガードテスト）
    - .planning/phases/01-foundation-split/01-RESEARCH.md（Pitfall 6 循環 import 回避・レジストリ統合4箇所の (1)）
  </read_first>
  <action>
    pagefolio/settings.py の `_SENSITIVE_KEYS` ハードコード集合（23-34 行）を `registry.sensitive_keys()` 生成へ置換する（D-09 統合対象1・ROBUST-02）。import は循環回避のため **サブモジュール直接指定** で行う: `from pagefolio.ocr_providers.registry import sensitive_keys`（`pagefolio.ocr_providers` の `__init__.py` 経由＝全プロバイダ import する重い経路は使わない Pitfall 6）。`_SENSITIVE_KEYS = sensitive_keys()` としモジュールロード時に一度だけ生成する。`_SENSITIVE_KEYS` は引き続き `set` 型で、既存 `_save_settings` のガードロジック（`_SENSITIVE_KEYS` を参照してキーを除外する処理）は無変更のまま動作すること。既存の WR-03 コメント（dual env var フォールバックの意図）は registry.py 側に集約済みのため settings.py 側からは撤廃してよいが、`_SENSITIVE_KEYS` の意図を1行で説明するコメントは残す。
  </action>
  <verify>
    <automated>pytest tests/test_settings_keyguard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/settings.py` の `_SENSITIVE_KEYS` がハードコード集合ではなく `sensitive_keys()` 生成結果である
    - `python -c "from pagefolio.settings import _SENSITIVE_KEYS; req={'claude_api_key','gemini_api_key','google_api_key','anthropic_api_key','api_key','GEMINI_API_KEY','GOOGLE_API_KEY','ANTHROPIC_API_KEY','runpod_api_key','RUNPOD_API_KEY'}; assert isinstance(_SENSITIVE_KEYS, set) and _SENSITIVE_KEYS >= req"` が成功
    - `pytest tests/test_settings_keyguard.py -q`（既存 TestSensitiveKeysConstant + 3経路ガード）が全緑
    - `python -c "import time,importlib,pagefolio.settings"` が正常終了（循環 import・ロード異常なし）
    - `ruff check pagefolio/settings.py && ruff format --check pagefolio/settings.py` がクリーン
  </acceptance_criteria>
  <done>_SENSITIVE_KEYS が registry から生成され、キー漏れが構造的に起きなくなり、既存 keyguard テストが緑を維持する</done>
</task>

<task type="auto">
  <name>Task 2: ocr._resolve_api_key と ocr_dialog._check_cloud_api_key を registry 参照へ置換</name>
  <files>pagefolio/ocr.py, pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr.py（現行 _resolve_api_key 208-267 行・claude/gemini/runpod ハードコード分岐と未知プロバイダの最終 raise）
    - pagefolio/ocr_dialog.py（現行 _check_cloud_api_key の env var dict 1267-1271 行）
    - pagefolio/ocr_providers/registry.py（env_vars_for / primary_env_var の実装）
    - tests/test_provider_ui.py（キー解決テスト 402-435 行付近: session_key_resolves / env_var_resolves / runpod_session_key_does_not_use_claude_slot、コスト確認テスト・env_var メッセージ表示テスト）
    - .planning/phases/01-foundation-split/01-RESEARCH.md（レジストリ統合4箇所の (2)(3)・Gemini dual env var 不変保持）
  </read_first>
  <action>
    D-09 統合対象2・3 を registry 参照へ置換する（挙動完全不変）。

    (2) pagefolio/ocr.py `_resolve_api_key`（208-267 行）: claude/gemini/runpod の3つのハードコード分岐を `registry.env_vars_for(provider_name)` を使った統一ループへ置換する。解決順は現行どおり厳守すること: まずセッションキー（`session_keys.get(provider_name, "")`・キーはプロバイダ短縮名）を優先し、非空ならそれを返す。次に `env_vars_for(provider_name)` のタプルを **その順序で** `os.environ.get(var)` し最初の非空値を返す（Gemini は GEMINI_API_KEY → GOOGLE_API_KEY の順が保たれる）。いずれも未解決なら `OCRAPIKeyError(registry.primary_env_var(provider_name))` を raise（Gemini は主変数名 GEMINI_API_KEY でエラー）。`PROVIDER_ENV_KEYS` に無いプロバイダ名は現行どおり `OCRAPIKeyError(f"{provider_name.upper()}_API_KEY")` を raise してフォールバック挙動を保持する。`os.environ` への **書き込みは一切行わない**（読み取り専用原則の継続）。`OCRAPIKeyError` の import（現行は関数内 `from pagefolio.ocr_providers import OCRAPIKeyError`）は D-10 により旧パスのまま維持し、registry は `from pagefolio.ocr_providers.registry import env_vars_for, primary_env_var` で参照する。

    (3) pagefolio/ocr_dialog.py `_check_cloud_api_key` 内の env var dict（1267-1271 行 `{"claude": "ANTHROPIC_API_KEY", "gemini": "GEMINI_API_KEY", "runpod": "RUNPOD_API_KEY"}.get(name, "")`）を `registry.primary_env_var(name)` へ置換する。未知プロバイダ名で `primary_env_var` が `KeyError` を投げる場合は現行 `.get(name, "")` と同じく空文字へフォールバックする形にする（try/except KeyError で `""`、または registry 側に安全なアクセサがあればそれを使う）。エラーメッセージのフォーマット（`self._L[msg_key].format(env_var=env_var)`）は無変更。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py tests/test_ocr.py tests/test_ocr_providers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ocr._resolve_api_key` が claude/gemini/runpod の個別ハードコード分岐を持たず `registry.env_vars_for()` 経由で解決する
    - セッションキー優先 → 環境変数フォールバックの2段構造が保持される（test_provider_ui の session_key_resolves / env_var_resolves / runpod_session_key_does_not_use_claude_slot が緑）
    - Gemini の dual env var 優先順（GEMINI_API_KEY → GOOGLE_API_KEY）が保持される
    - `ocr_dialog._check_cloud_api_key` が `registry.primary_env_var()` を参照し、未知プロバイダで空文字フォールバックする
    - `pytest tests/test_provider_ui.py tests/test_ocr.py tests/test_ocr_providers.py -q` が全緑
    - `ruff check pagefolio/ocr.py pagefolio/ocr_dialog.py && ruff format --check pagefolio/ocr.py pagefolio/ocr_dialog.py` がクリーン
  </acceptance_criteria>
  <done>ocr / ocr_dialog の env var 参照が registry へ統合され、Gemini dual env var・セッションキー優先・未知プロバイダ挙動が不変のまま緑を維持する</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces（このプランが新規作成するシンボル/ファイル）

- 新規シンボルの作成はなし。本プランは既存 `pagefolio/settings.py` / `pagefolio/ocr.py` / `pagefolio/ocr_dialog.py` の内部実装を Plan 02 の `registry.py`（`sensitive_keys` / `env_vars_for` / `primary_env_var`）参照へ **置換** する（配線変更のみ）。
- `_SENSITIVE_KEYS`（settings.py）はシンボル名を維持したまま生成元が registry へ変わる（identity 名は不変）。
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| アプリ内部 → `pagefolio_settings.json`（ディスク永続化） | API キーがこの境界を越えて平文保存されてはならない。`_SENSITIVE_KEYS`（＝ registry.sensitive_keys()）が最後の砦 |
| `os.environ`（読み取り専用） → キー解決 | キー解決は environ から読むのみで書き込まない境界 |
| セッションメモリ（`_session_api_keys`）→ 外部 API | ユーザー入力キーの優先解決境界（挙動不変） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `settings._SENSITIVE_KEYS` の registry 生成置換 | high | mitigate | Task 1 で `_SENSITIVE_KEYS >= 現行10エントリ` を CLI/テストで機械検証。既存 keyguard 3経路（JSON 非出力・ログ非出力・入力 dict 非破壊）が回帰検知。キー漏れ = 平文保存の直接リスクを封じる |
| T-01-04 | Tampering | `ocr._resolve_api_key` の environ アクセス | medium | mitigate | os.environ への書き込みを一切追加しない（読み取り専用原則）。Gemini dual env var・セッション優先・未知プロバイダ raise を挙動不変で保持し test_provider_ui のキー解決テストで検証 |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージ導入ゼロ（RESEARCH「Package Legitimacy Audit: 該当なし」）。install タスクなし |
</threat_model>

<verification>
- `pytest -q`（全スイート）が全緑 — 特に `tests/test_settings_keyguard.py`（3経路ガード）・`tests/test_provider_ui.py`（キー解決/コスト確認）が緑
- `_SENSITIVE_KEYS >= 現行10エントリ` が保持される
- キー解決の挙動が不変（Gemini dual env var 優先順・セッション優先・os.environ 非書き込み）
- `ruff check . && ruff format --check .` がクリーン
</verification>

<success_criteria>
- `_SENSITIVE_KEYS` が registry 生成となり、新プロバイダ追加時の手動追加漏れが構造的に排除される（成功基準3・ROBUST-02）
- D-09 の3参照面（settings/ocr/ocr_dialog）が registry を単一情報源として参照する
- 全 keyguard・キー解決テストが緑を維持（成功基準4）
</success_criteria>

<output>
Create `.planning/phases/01-foundation-split/01-03-SUMMARY.md` when done
</output>
