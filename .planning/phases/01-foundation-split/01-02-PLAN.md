---
phase: 01-foundation-split
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - pagefolio/ocr_providers/__init__.py
  - pagefolio/ocr_providers/base.py
  - pagefolio/ocr_providers/errors.py
  - pagefolio/ocr_providers/registry.py
  - pagefolio/ocr_providers/lmstudio.py
  - pagefolio/ocr_providers/claude.py
  - pagefolio/ocr_providers/gemini.py
  - pagefolio/ocr_providers/tesseract.py
  - pagefolio/ocr_providers/ollama.py
  - pagefolio/ocr_providers/runpod.py
  - pagefolio/ocr_providers.py
  - tests/test_settings_keyguard.py
autonomous: true
requirements: [V180-REFAC-01, V180-ROBUST-02]
tags: [refactor, packaging, ocr-providers, registry, sensitive-keys]

must_haves:
  truths:
    - "`pagefolio/ocr_providers/` パッケージが存在し、旧 `pagefolio/ocr_providers.py` モジュールは削除されている（1プロバイダ=1ファイル D-01）"
    - "Wave 1 の TestOcrProvidersImports（package-level import 全シンボル）が緑を維持する（D-11 完全再エクスポート）"
    - "既存 `tests/test_ocr_providers.py` が無修正で全通過する（D-03・後方互換の検証）"
    - "`registry.sensitive_keys()` の出力が現行10エントリ（claude_api_key / gemini_api_key / google_api_key / anthropic_api_key / api_key / GEMINI_API_KEY / GOOGLE_API_KEY / ANTHROPIC_API_KEY / runpod_api_key / RUNPOD_API_KEY）を部分集合として含む（ROBUST-02・Pitfall 5）"
    - "`registry.py` は stdlib のみに依存し Tk/fitz を import しない（D-07・循環 import 回避）"
    - "各プロバイダファイル・base.py・errors.py が ruff（line-length 88・I ルール・S/B）を素通りする"
  artifacts:
    - "pagefolio/ocr_providers/__init__.py（全シンボル完全 re-export・# noqa: F401）"
    - "pagefolio/ocr_providers/base.py（OCRProvider ABC + _require_http_scheme + _ALLOWED_URL_SCHEMES）"
    - "pagefolio/ocr_providers/errors.py（OCRAPIKeyError / OCRRetryableError / OCRContextLengthError + リトライ/コンテキスト判定ヘルパー）"
    - "pagefolio/ocr_providers/registry.py（PROVIDER_ENV_KEYS 宣言的 dict + 導出関数群・新設）"
    - "pagefolio/ocr_providers/{lmstudio,claude,gemini,tesseract,ollama,runpod}.py（1プロバイダ1ファイル）"
    - "tests/test_settings_keyguard.py の registry.sensitive_keys() 網羅性テスト"
  key_links:
    - "__init__.py が private ヘルパー（_require_http_scheme / parse_retry_after / looks_like_context_error / _raise_mapped_http_error / _detect_tesseract）を含め全モジュールレベル名を re-export（Pitfall 1/2）"
    - "各プロバイダファイルは base.py / errors.py から必要シンボルを import（機械的移動・D-02 共通化しない）"
    - "settings.py から registry を参照する際はサブモジュール直接指定で循環回避（Pitfall 6・実配線は Plan 03）"
---

<objective>
`pagefolio/ocr_providers.py`（実測1537行・6プロバイダ + 共有基盤）を責務別パッケージ `pagefolio/ocr_providers/` へ **機械的移動のみ** で分割し（D-01: 1プロバイダ=1ファイル / D-02: 共通化・最適化・リネームを一切混ぜない）、同時にプロバイダ→環境変数の中央レジストリ `registry.py` を新設する（V180-ROBUST-02 の基盤）。

分割は DEBT-01（`pagefolio/dialogs/` パッケージ化）・DEBT-02（`constants.py`→`themes.py`/`lang.py` 分割）で確立済みの「サブパッケージ化 + `__init__.py` 完全 re-export（`# noqa: F401`）」パターンの複製であり、新規設計判断はほぼ不要。最大リスクは re-export 漏れ（Pitfall 1/2）— private ヘルパーまで漏れなく再エクスポートすることが後方互換の生命線。registry.py は stdlib のみ依存に保ち（D-07）、現行 `_SENSITIVE_KEYS` の10エントリを完全に生成できる `sensitive_keys()` を実装する（Pitfall 5: セッションキー形式と環境変数大小文字の2命名規則を両方導出）。

Purpose: V180-REFAC-01（ocr_providers 分割）を完了し、V180-ROBUST-02 の中央レジストリを新設して Plan 03/04 の参照統合の土台を作る。以降のフェーズでの新プロバイダ追加時に「触る場所が1箇所」になる。
Output: `pagefolio/ocr_providers/` パッケージ（10 ファイル）+ registry 網羅性テスト。旧 monolith モジュールは削除。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation-split/01-CONTEXT.md
@.planning/phases/01-foundation-split/01-RESEARCH.md
@.planning/phases/01-foundation-split/01-PATTERNS.md

@pagefolio/ocr_providers.py
@pagefolio/dialogs/__init__.py
@pagefolio/settings.py
@tests/test_settings_keyguard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ocr_providers.py を base/errors/6プロバイダへ機械的分割し、registry.py を新設、__init__.py で完全 re-export</name>
  <files>pagefolio/ocr_providers/__init__.py, pagefolio/ocr_providers/base.py, pagefolio/ocr_providers/errors.py, pagefolio/ocr_providers/registry.py, pagefolio/ocr_providers/lmstudio.py, pagefolio/ocr_providers/claude.py, pagefolio/ocr_providers/gemini.py, pagefolio/ocr_providers/tesseract.py, pagefolio/ocr_providers/ollama.py, pagefolio/ocr_providers/runpod.py, pagefolio/ocr_providers.py（削除）</files>
  <read_first>
    - pagefolio/ocr_providers.py（分割元・全1537行。移動する各シンボルの行範囲を実物で確認）
    - pagefolio/dialogs/__init__.py（DEBT-01 の完全 re-export パターン・そのまま踏襲）
    - pagefolio/constants.py（DEBT-02 の分割後 re-export パターン・# noqa: F401）
    - .planning/phases/01-foundation-split/01-PATTERNS.md（各ファイルへの移動元マッピング・__init__.py 適用形・registry.py 実装方針）
    - .planning/phases/01-foundation-split/01-RESEARCH.md（Pattern 1 完全シンボルリスト・Pattern 3 registry.py 実装例・Pitfall 1/2/5/6）
  </read_first>
  <action>
    `pagefolio/ocr_providers/` ディレクトリを新設し、旧 `pagefolio/ocr_providers.py` の内容を以下の配置で **行の移動のみ**（リネーム・共通化・最適化・コメント literal の改変を一切せず）に分割する（D-01/D-02）。分割元の実行範囲は分割前に grep で再確認すること（確認済みの module-level 定義位置: `_ALLOWED_URL_SCHEMES`=20 / `_require_http_scheme`=23 / `OCRProvider`=41 / `OCRAPIKeyError`=138 / `OCRRetryableError`=146 / `_retryable_http_message`=159 / `OCRContextLengthError`=170 / `_CONTEXT_ERROR_MARKERS`=182 / `parse_retry_after`=194 / `looks_like_context_error`=209 / `_raise_mapped_http_error`=221 / `LMStudioProvider`=249 / `ClaudeProvider`=425 / `GeminiProvider`=753 / `_detect_tesseract`=1023 / `TesseractProvider`=1062 / `OllamaProvider`=1169 / `RunPodProvider`=1346）。

    配置:
    - `base.py`: `_ALLOWED_URL_SCHEMES` + `_require_http_scheme` + `OCRProvider` ABC。冒頭に必要な stdlib import（`abc` / `base64` / `json` / `logging` / `socket` / `urllib.error` / `urllib.request` / `from urllib.parse import quote, urlsplit` のうち base.py が実際に使うもの）と `logger = logging.getLogger(__name__)` を配置。
    - `errors.py`: `OCRAPIKeyError` / `OCRRetryableError` / `_retryable_http_message` / `OCRContextLengthError` / `_CONTEXT_ERROR_MARKERS` / `parse_retry_after` / `looks_like_context_error` / `_raise_mapped_http_error`。errors.py は base.py の `OCRProvider` を参照しない独立関数群にできる（両者は相互 import 不要）。リトライ/コンテキスト判定ヘルパーを base/errors のどちらへ置くかは Claude's Discretion だが、上記の既定配置（errors.py 側）を推奨。
    - `registry.py`（**新設**）: `PROVIDER_ENV_KEYS = {"claude": ("ANTHROPIC_API_KEY",), "gemini": ("GEMINI_API_KEY", "GOOGLE_API_KEY"), "runpod": ("RUNPOD_API_KEY",)}`（タプル順序が解決優先順・Gemini は GEMINI_API_KEY 優先 → GOOGLE_API_KEY フォールバック D-08）と導出関数を実装。必須の公開関数は `env_vars_for(provider_name)`（Plan 03 の ocr.py / Plan 04 の llm_config が消費）・`primary_env_var(provider_name)`（Plan 03 の ocr_dialog が消費）・`sensitive_keys()`（Plan 03 の settings が消費）。`sensitive_keys()` は (1) セッションキー形式 `f"{provider_name}_api_key"`、(2) 各環境変数名そのものとその `.lower()`、(3) プロバイダ非依存の汎用 `"api_key"` の3系統を生成し現行10エントリを完全包含すること（Pitfall 5）。関数名・追加ヘルパー（`resolve_env_key` 等）の細目は Claude's Discretion。registry.py は **stdlib のみ依存**（`os` のみ・Tk/fitz/プロバイダクラスを import しない D-07）。
    - `lmstudio.py` / `claude.py` / `gemini.py` / `ollama.py` / `runpod.py`: 各 Provider クラスをそのまま移動。`tesseract.py` のみ `_detect_tesseract()` 関数を `TesseractProvider` と同居させる。各ファイルは `from pagefolio.ocr_providers.base import OCRProvider, _require_http_scheme`（実際に使うシンボルのみ）・`from pagefolio.ocr_providers.errors import OCRAPIKeyError, OCRRetryableError, OCRContextLengthError, _raise_mapped_http_error, parse_retry_after, looks_like_context_error`（実際に使うシンボルのみ）と必要な stdlib を import する。
    - `__init__.py`: PATTERNS の適用形どおり base/errors/6プロバイダの **全モジュールレベル名を完全 re-export**（`# noqa: F401`）。private ヘルパー（`_require_http_scheme` / `parse_retry_after` / `looks_like_context_error` / `_raise_mapped_http_error` / `_detect_tesseract` / `_ALLOWED_URL_SCHEMES` / `_CONTEXT_ERROR_MARKERS` / `_retryable_http_message`）を必ず含める。`registry` サブモジュールは `__init__.py` の re-export に含めない（settings.py 等はサブモジュール直接指定で import する Pitfall 6 の方針。ただし `import pagefolio.ocr_providers.registry` は当然可能）。

    旧 `pagefolio/ocr_providers.py` は削除する（`git rm pagefolio/ocr_providers.py`。パッケージディレクトリとモジュールファイルは共存不可）。内部コード（ocr.py / ocr_dialog.py / app.py / plugins.py / dialogs/llm_config.py 等）の `from pagefolio.ocr_providers import ...` は **一切変更しない**（D-10・package-level re-export が受ける）。`pagefolio/__init__.py` の公開面も広げない（Pitfall 4）。
  </action>
  <verify>
    <automated>pytest tests/test_imports.py -k OcrProviders tests/test_ocr_providers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr_providers/` に __init__.py / base.py / errors.py / registry.py / lmstudio.py / claude.py / gemini.py / tesseract.py / ollama.py / runpod.py の10ファイルが存在する
    - `pagefolio/ocr_providers.py`（モジュールファイル）が削除されている
    - `pytest tests/test_imports.py -k OcrProviders -q`（Plan 01 の package-level 後方互換テスト）が緑を維持
    - `pytest tests/test_ocr_providers.py -q`（既存2573行・D-03 凍結）が **無修正で** 全通過
    - `python -c "import pagefolio.ocr_providers.registry as r; assert callable(r.env_vars_for) and callable(r.primary_env_var) and callable(r.sensitive_keys)"` が成功
    - `python -c "import pagefolio.ocr_providers.registry as r; assert 'tkinter' not in r.__dict__ and 'fitz' not in r.__dict__"`（registry が Tk/fitz を名前空間へ持ち込まない簡易確認）が成功
    - `ruff check pagefolio/ocr_providers/ && ruff format --check pagefolio/ocr_providers/` がクリーン
  </acceptance_criteria>
  <done>ocr_providers が10ファイルのパッケージへ機械的分割され、後方互換 import テストと既存 ocr_providers テストが緑を維持し、registry.py が新設される</done>
</task>

<task type="auto">
  <name>Task 2: registry.sensitive_keys() の網羅性テストを追加し全体回帰を確認</name>
  <files>tests/test_settings_keyguard.py</files>
  <read_first>
    - tests/test_settings_keyguard.py（既存 TestSensitiveKeysConstant の記法・12-34 行付近）
    - pagefolio/ocr_providers/registry.py（Task 1 で新設した sensitive_keys() の実装）
    - pagefolio/settings.py（現行 _SENSITIVE_KEYS の10エントリ・置換は Plan 03 で行うため本プランでは未変更）
    - .planning/phases/01-foundation-split/01-PATTERNS.md（test_settings_keyguard.py 拡張の追加テスト方針）
  </read_first>
  <action>
    tests/test_settings_keyguard.py に `from pagefolio.ocr_providers.registry import sensitive_keys` を用いる新テストを追加する。`sensitive_keys()` の返り値が現行10エントリ（`claude_api_key` / `gemini_api_key` / `google_api_key` / `anthropic_api_key` / `api_key` / `GEMINI_API_KEY` / `GOOGLE_API_KEY` / `ANTHROPIC_API_KEY` / `runpod_api_key` / `RUNPOD_API_KEY`）を **部分集合として含む** ことを `set(sensitive_keys()) >= {...現行10エントリ...}` で検証する（Pitfall 5・ROBUST-02 の秘匿性劣化防止の中核ガード）。返り値が `set` 型であることも確認する。既存の `TestSensitiveKeysConstant`（`_SENSITIVE_KEYS` は Plan 03 まで monolith 定義のまま）は変更しない。

    追加後、フル回帰を実行し Wave 全体の緑を確認する。`_SENSITIVE_KEYS` の実配線置換は Plan 03 の担当であり、本プランでは registry.py の存在と生成網羅性のみを確定させる（settings.py は未変更）。
  </action>
  <verify>
    <automated>pytest tests/test_settings_keyguard.py -q && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `sensitive_keys()` 網羅性テストが緑（現行10エントリを部分集合として含むことを機械検証）
    - `pytest tests/test_settings_keyguard.py -q`（既存3経路ガード含む）が全緑
    - `pytest -q`（全スイート・約880件超）が全緑
    - `ruff check . && ruff format --check .` がクリーン
    - `pagefolio.spec` が存在する場合、`ocr_providers` / `llm_config` の個別モジュール参照（hiddenimports 等）が無いことを確認済み（存在しなければ確認不要）
  </acceptance_criteria>
  <done>registry の秘匿キー生成網羅性が緑で保証され、Wave 2 のフルスイートが全緑になる</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces（このプランが新規作成するシンボル/ファイル）

- **新規ファイル/モジュール**: `pagefolio/ocr_providers/__init__.py`, `base.py`, `errors.py`, `registry.py`, `lmstudio.py`, `claude.py`, `gemini.py`, `tesseract.py`, `ollama.py`, `runpod.py`
- **新規シンボル（registry.py）**: `PROVIDER_ENV_KEYS`（dict）, `env_vars_for(provider_name)`, `primary_env_var(provider_name)`, `sensitive_keys()`（および Claude's Discretion で追加しうる `resolve_env_key` 等）
- **新規テスト**: `tests/test_settings_keyguard.py` の `sensitive_keys()` 網羅性テスト（クラス/メソッド）
- **削除**: `pagefolio/ocr_providers.py`（モジュール → パッケージへ移行）

> `OCRProvider` / `ClaudeProvider` / `GeminiProvider` / `LMStudioProvider` / `OllamaProvider` / `RunPodProvider` / `TesseractProvider` / `OCRAPIKeyError` / `OCRRetryableError` / `OCRContextLengthError` / `parse_retry_after` / `looks_like_context_error` / `_require_http_scheme` / `_raise_mapped_http_error` / `_detect_tesseract` / `_ALLOWED_URL_SCHEMES` / `_CONTEXT_ERROR_MARKERS` / `_retryable_http_message` は **既存シンボルの物理移動**であり新規作成ではない（同一 identity を保つ・drift 検証の対象外）。
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| アプリ内部 → `pagefolio_settings.json`（ディスク永続化） | API キーがこの境界を越えて平文保存されてはならない。`_SENSITIVE_KEYS`（＝ registry.sensitive_keys()）がガード |
| アプリ内部 → `import pagefolio` 公開名前空間 | 分割の re-export で内部ヘルパーがトップレベル露出する境界 |
| ワーカースレッド → 外部 LLM API（https） | 各 Provider の HTTP 送信境界（本フェーズは機械的移動のみで挙動不変） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `registry.sensitive_keys()` | high | mitigate | Task 2 で `sensitive_keys()` 出力 ⊇ 現行10エントリを機械検証（Pitfall 5）。セッションキー形式 + 環境変数大小文字 + 汎用 api_key の3系統を導出し漏れをなくす。実配線（settings 置換）は Plan 03 で既存 keyguard 3経路が回帰検知 |
| T-01-02 | Information Disclosure | `ocr_providers/__init__.py` re-export 面 / `pagefolio/__init__.py` | low | mitigate | 完全 re-export は package-level 面のみ（`pagefolio.ocr_providers`）。`pagefolio` トップレベルは広げない（Pitfall 4・Plan 01 の負ガードが緑を維持） |
| T-01-03 | Tampering | 各プロバイダの HTTP エラー分類（OCRRetryableError / OCRContextLengthError） | medium | mitigate | D-02 機械的移動のみ・共通化しない。既存 `test_ocr_providers.py`（D-03 凍結・2573行）全通過でエラー分類/リトライ/レート制限の安全挙動が不変であることを保証 |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージ導入ゼロ（RESEARCH「Package Legitimacy Audit: 該当なし」）。install タスクなし |
</threat_model>

<verification>
- `pytest -q`（全スイート）が全緑 — 特に `tests/test_ocr_providers.py`（D-03 凍結）が無修正で通過
- `pytest tests/test_imports.py -k OcrProviders -q`（Plan 01 の後方互換安全網）が緑を維持
- `registry.sensitive_keys()` が現行10エントリを完全包含
- `ruff check . && ruff format --check .` がクリーン
- `pagefolio/ocr_providers.py` が削除され、パッケージのみが存在
</verification>

<success_criteria>
- ocr_providers が責務別10ファイルパッケージへ分割され、既存の `from pagefolio.ocr_providers import ...` が変更なく動作（成功基準1）
- registry.py が新設され sensitive_keys() が現行10エントリを生成（ROBUST-02 の基盤）
- D-03 凍結テスト・Plan 01 安全網・フルスイートが全緑（成功基準4）
</success_criteria>

<output>
Create `.planning/phases/01-foundation-split/01-02-SUMMARY.md` when done
</output>
