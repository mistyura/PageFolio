---
phase: 01-foundation-split
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - pagefolio/dialogs/llm_config/__init__.py
  - pagefolio/dialogs/llm_config/dialog.py
  - pagefolio/dialogs/llm_config/sections.py
  - pagefolio/dialogs/llm_config/model_fetch.py
  - pagefolio/dialogs/llm_config.py
  - tests/test_provider_ui.py
autonomous: true
requirements: [V180-REFAC-02, V180-ROBUST-02]
tags: [refactor, packaging, llm-config, mixin, tkinter, registry]

must_haves:
  truths:
    - "`pagefolio/dialogs/llm_config/` パッケージ（__init__ / dialog / sections / model_fetch）が存在し、旧 `pagefolio/dialogs/llm_config.py` モジュールは削除されている（D-04 Mixin 分割・D-05 責務別3層）"
    - "`from pagefolio.dialogs.llm_config import LLMConfigDialog` と `from pagefolio.dialogs import LLMConfigDialog` の両経路が動作する（D-11・Plan 01 の安全網が緑を維持）"
    - "`LLMConfigDialog(DialogMixin, SectionsMixin, ModelFetchMixin, tk.Toplevel)` の MRO で tk.Toplevel が末尾にあり、`__init__` は DialogMixin に集約されている（Pitfall 3）"
    - "test_provider_ui.py の3つのソーススキャンテスト（旧 llm_config.py をファイルパスで読む）が新パッケージ配下を走査するよう更新され緑を維持する"
    - "D-09 #4: sections.py の環境変数チェック（旧 512/612/714 行の os.environ 判定）が registry.env_vars_for を参照し、Gemini dual env var 表示（GEMINI_API_KEY / GOOGLE_API_KEY）の挙動が不変"
    - "既存 test_provider_ui.py 全件（プロバイダ状態・コスト・セッションキー・モデル取得ラッパー）が緑を維持する"
  artifacts:
    - "pagefolio/dialogs/llm_config/__init__.py（LLMConfigDialog を Mixin 統合し re-export）"
    - "pagefolio/dialogs/llm_config/dialog.py（DialogMixin: __init__ / _apply / _on_* / スクロール域構築等の共通部）"
    - "pagefolio/dialogs/llm_config/sections.py（SectionsMixin: _build UI セクション構築）"
    - "pagefolio/dialogs/llm_config/model_fetch.py（ModelFetchMixin: _fetch_models_async + probe/refresh 群）"
  key_links:
    - "tk.Toplevel を継承リスト末尾に配置し __init__ を DialogMixin に集約（Pitfall 3・MRO 破壊防止）"
    - "3 Mixin ファイルの `from pagefolio.ocr_providers import ...` / `from pagefolio.ocr import ...` / `from pagefolio.settings import ...` は旧パスのまま維持（D-10）"
    - "D-09 #4 の env var 参照は sections.py 内で registry を Single Source of Truth として参照（D-09 全参照面統合の4番目）"
---

<objective>
`pagefolio/dialogs/llm_config.py`（実測1659行・単一クラス `LLMConfigDialog`）を、PDFEditorApp の 8 Mixin 構成と同じパターンで責務別3層の Mixin パッケージ `pagefolio/dialogs/llm_config/` へ **機械的移動** で分割する（D-04/D-05）: `dialog.py`（DialogMixin: `__init__`/`_apply`/`_on_provider_change`/`_on_model_change`/`_model_supports_effort`/`_resize_to_fit`/`_add_prompt_file_notice`/`_set_lm_status`/スクロール域構築の共通部）・`sections.py`（SectionsMixin: `_build` の UI セクション構築約920行）・`model_fetch.py`（ModelFetchMixin: `_fetch_models_async` + `_probe_*`/`_refresh_*`/`_fetch_*`/`_test_*` の非同期取得群）。`__init__.py` が多重継承で `LLMConfigDialog` を統合・re-export する。

最大リスクは tk.Toplevel の MRO 破壊（Pitfall 3）— 継承リスト末尾に `tk.Toplevel` を置き `__init__` を DialogMixin に集約する。加えて **本フェーズ固有の落とし穴**: test_provider_ui.py の3つのテスト（`test_provider_combo_includes_gemini` / `test_gemini_section_frame_exists_in_source` / `test_fetch_and_test_are_thin_wrappers`）が旧 `pagefolio/dialogs/llm_config.py` を **リテラルファイルパスで `read_text`** しており、ファイル→パッケージ移行でこれらが FileNotFoundError で赤くなる。これらを新パッケージ配下走査へ更新する（D-03 が凍結するのは test_ocr_providers.py のみ・test_provider_ui.py は更新可）。同時に D-09 #4（sections.py 内の環境変数チェックの registry 統合）を適用する。

Purpose: V180-REFAC-02（llm_config 分割）を完了し、V180-ROBUST-02 の最後の参照面（D-09 #4）を registry へ統合する。Phase 2 のテンプレート/フォールバック UI 追加の土台となる Mixin 構造を確立する（D-06: 投機的な仕込みはしない・純粋分割のみ）。
Output: `pagefolio/dialogs/llm_config/` パッケージ（4ファイル）+ 更新された source-scan テスト。旧 monolith モジュールは削除。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation-split/01-CONTEXT.md
@.planning/phases/01-foundation-split/01-RESEARCH.md
@.planning/phases/01-foundation-split/01-PATTERNS.md

@pagefolio/dialogs/llm_config.py
@pagefolio/app.py
@pagefolio/dialogs/__init__.py
@tests/test_provider_ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: llm_config を Mixin 3層パッケージへ機械的分割し、source-scan テストと MRO 構造テストを更新</name>
  <files>pagefolio/dialogs/llm_config/__init__.py, pagefolio/dialogs/llm_config/dialog.py, pagefolio/dialogs/llm_config/sections.py, pagefolio/dialogs/llm_config/model_fetch.py, pagefolio/dialogs/llm_config.py（削除）, tests/test_provider_ui.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config.py（分割元・全1659行。メソッド境界を実物で確認: __init__=49 / _build_scrollable_area=104 / _compute_dialog_height=162 / _build=177 / _on_provider_change=1097 / _on_model_change=1196 / _model_supports_effort=1216 / _resize_to_fit=1230 / _add_prompt_file_notice=1248 / _set_lm_status=1282 / _probe_lm_provider=1301 / _fetch_models=1333 / _test_connection=1337 / _probe_ollama_provider=1342 / _fetch_ollama_models=1377 / _test_ollama_connection=1381 / _fetch_models_async=1386 / _refresh_runpod_models=1429 / _refresh_claude_models=1470 / _refresh_gemini_models=1512 / _apply=1554）
    - pagefolio/app.py（107-117 行: PDFEditorApp の Mixin 多重継承の確立パターン）
    - pagefolio/dialogs/__init__.py（LLMConfigDialog を re-export している行・分割後も無変更で動作すべき）
    - tests/test_provider_ui.py（215/228/1679 行の `pathlib.Path("pagefolio/dialogs/llm_config.py").read_text(...)` ソーススキャン3件）
    - .planning/phases/01-foundation-split/01-PATTERNS.md（Mixin 分割の移動元マッピング・__init__.py 適用形・tk.Toplevel MRO 注意）
    - .planning/phases/01-foundation-split/01-RESEARCH.md（Pattern 2 Mixin 分割・Pitfall 3 MRO）
  </read_first>
  <action>
    `pagefolio/dialogs/llm_config/` ディレクトリを新設し、旧 `pagefolio/dialogs/llm_config.py` の `LLMConfigDialog` を責務別3 Mixin へ **行の移動のみ**（self 参照メソッドをそのまま移す・リネーム/共通化/最適化/投機的拡張を一切しない D-05/D-06）で分割する:

    - `dialog.py` に `DialogMixin` を定義し、`__init__`（49-103）・`_build_scrollable_area`（104-161）・`_compute_dialog_height`（162-176）・`_on_provider_change`（1097-1195）・`_on_model_change`（1196-1215）・`_model_supports_effort`（1216-1229）・`_resize_to_fit`（1230-1247）・`_add_prompt_file_notice`（1248-1281）・`_set_lm_status`（1282-1300）・`_apply`（1554-1659）を移す。`__init__` は必ず DialogMixin に集約する（他 Mixin は `__init__` を持たない Pitfall 3）。スクロール域構築ヘルパーを sections.py 側に置くか dialog.py 側に置くかは切り出し線の都合で Claude's Discretion だが、`_build` から呼ばれる関係を壊さないこと（Mixin 越しに `self._build_scrollable_area()` が解決すれば配置はどちらでも可）。
    - `sections.py` に `SectionsMixin` を定義し、`_build`（177-1096・約920行）を移す。
    - `model_fetch.py` に `ModelFetchMixin` を定義し、`_probe_lm_provider`（1301）・`_fetch_models`（1333）・`_test_connection`（1337）・`_probe_ollama_provider`（1342）・`_fetch_ollama_models`（1377）・`_test_ollama_connection`（1381）・`_fetch_models_async`（1386）・`_refresh_runpod_models`（1429）・`_refresh_claude_models`（1470）・`_refresh_gemini_models`（1512）を移す。
    - `__init__.py` は PATTERNS 適用形どおり `class LLMConfigDialog(DialogMixin, SectionsMixin, ModelFetchMixin, tk.Toplevel):` として統合する。`tk.Toplevel` は継承リストの **末尾**（既存が `super().__init__(parent)` を呼ぶため MRO で tk.Toplevel を最後に）。

    各 Mixin ファイルは冒頭で必要なサブセットを個別 import する。import 元パス（`from pagefolio.ocr_providers import ClaudeProvider, GeminiProvider, LMStudioProvider, _detect_tesseract` / `from pagefolio.ocr import MAX_OCR_MAX_TOKENS` / `from pagefolio.constants import CUSTOM_PROMPT_FILE, LANG, SUMMARY_PROMPT_FILE, C` / `from pagefolio.settings import ...`）は **D-10 により旧パスのまま維持**する（ocr_providers は package-level re-export が受ける）。

    旧 `pagefolio/dialogs/llm_config.py` を削除する（`git rm pagefolio/dialogs/llm_config.py`）。`pagefolio/dialogs/__init__.py`（`from pagefolio.dialogs.llm_config import LLMConfigDialog`）と `pagefolio/dialogs/settings.py:220` の import は無変更のまま動作すること。

    **source-scan テスト更新（本フェーズ固有・必達）**: test_provider_ui.py の3テスト（`test_provider_combo_includes_gemini` 215 行付近・`test_gemini_section_frame_exists_in_source` 228 行付近・`test_fetch_and_test_are_thin_wrappers` 1679 行付近）は `pathlib.Path("pagefolio/dialogs/llm_config.py").read_text(...)` で旧ファイルを走査している。分割で旧ファイルが消えるため、これらを **`pagefolio/dialogs/llm_config/` 配下の全 `.py` を glob して連結した文字列** を走査する形へ更新する（各 `.py` の `read_text` 結果を sorted glob で連結し、既存の substring/count アサーションはそのまま維持）。連結対象は sections.py（`"gemini"`/`gemini_section_frame`/`gemini_model_var`）・dialog.py（`_on_provider_change`）・model_fetch.py（`self._probe_ollama_provider(update_combo=True/False)`・`def _test_ollama_connection` の 1 回出現）を含むため、パッケージ全体連結でアサーションの意図が保存される。テストの意図（該当シンボル/呼び出しが llm_config 実装に存在する）を変えないこと。

    **MRO 構造テスト追加（Pitfall 3 の headless ガード）**: test_provider_ui.py に、Tk をインスタンス化せずに `LLMConfigDialog.__mro__` を検査するテストを追加する。(a) MRO 内で `tk.Toplevel` が3 Mixin すべてより後ろにあること、(b) `LLMConfigDialog.__init__ is DialogMixin.__init__`（__init__ が DialogMixin に集約されていること）、(c) 主要メソッド（`_build`・`_apply`・`_on_provider_change`・`_fetch_models_async`）が `hasattr(LLMConfigDialog, ...)` で存在することを検証する。実機描画目視は v1.8.0 スコープ外（PROJECT.md）のため本テストが MRO 健全性の自動安全網となる。
  </action>
  <verify>
    <automated>pytest tests/test_imports.py -k "LlmConfig or llm_config" tests/test_provider_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/llm_config/` に __init__.py / dialog.py / sections.py / model_fetch.py の4ファイルが存在する
    - `pagefolio/dialogs/llm_config.py`（モジュールファイル）が削除されている
    - `python -c "from pagefolio.dialogs.llm_config import LLMConfigDialog; from pagefolio.dialogs import LLMConfigDialog"` が成功（両経路 import）
    - `python -c "import tkinter as tk; from pagefolio.dialogs.llm_config import LLMConfigDialog; from pagefolio.dialogs.llm_config.dialog import DialogMixin; m=LLMConfigDialog.__mro__; assert m.index(tk.Toplevel)>max(m.index(x) for x in LLMConfigDialog.__bases__ if x is not tk.Toplevel); assert LLMConfigDialog.__init__ is DialogMixin.__init__"` が成功（MRO 末尾 + __init__ 集約）
    - test_provider_ui.py の3ソーススキャンテストが新パッケージ配下を走査し緑
    - MRO 構造テストが追加され緑
    - `pytest tests/test_provider_ui.py -q` が全緑
    - `ruff check pagefolio/dialogs/llm_config/ tests/test_provider_ui.py && ruff format --check pagefolio/dialogs/llm_config/ tests/test_provider_ui.py` がクリーン
  </acceptance_criteria>
  <done>llm_config が Mixin 3層パッケージへ機械的分割され、両経路 import・MRO 健全性・更新済み source-scan テストが緑になり、tk.Toplevel の MRO 破壊が headless で自動検知される</done>
</task>

<task type="auto">
  <name>Task 2: sections.py の環境変数チェック（D-09 #4）を registry.env_vars_for 参照へ統合</name>
  <files>pagefolio/dialogs/llm_config/sections.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config/sections.py（Task 1 で移動した _build 内の環境変数チェック。旧 llm_config.py の 512 行 `os.environ.get("RUNPOD_API_KEY")` / 612 行 `os.environ.get("ANTHROPIC_API_KEY")` / 712-714 行 Gemini dual env var 判定に相当する箇所）
    - pagefolio/ocr_providers/registry.py（env_vars_for / primary_env_var の実装）
    - .planning/phases/01-foundation-split/01-RESEARCH.md（レジストリ統合4箇所の (4)・Gemini dual env var 表示ロジックの一般化）
    - tests/test_provider_ui.py（gemini/claude/runpod セクション関連テストが緑を保つべき対象）
  </read_first>
  <action>
    D-09 統合対象4（V180-ROBUST-02 の最後の参照面）を適用する。sections.py の `_build` 内にある「既に環境変数が設定済み」注記の判定（旧 llm_config.py 512/612/712-714 行相当: runpod は `os.environ.get("RUNPOD_API_KEY")`、claude は `os.environ.get("ANTHROPIC_API_KEY")`、gemini は `os.environ.get("GEMINI_API_KEY") or os.environ.get("GOOGLE_API_KEY")` および表示用変数名 `"GEMINI_API_KEY" if os.environ.get("GEMINI_API_KEY") else "GOOGLE_API_KEY"`）を、`registry.env_vars_for(provider)` のタプルを順にループして `os.environ.get()` する統一形へ置換する。import は `from pagefolio.ocr_providers.registry import env_vars_for`（必要なら primary_env_var も）を sections.py 冒頭へ追加する。

    Gemini の dual env var 挙動は不変で保持すること: 設定済み判定は「タプル内のいずれかが設定済みなら True」、表示用の env var 名は「タプル順で最初に設定済みのもの、いずれも未設定なら先頭（GEMINI_API_KEY）」とし、現行の GEMINI_API_KEY 優先 → GOOGLE_API_KEY フォールバックの表示ロジックを一般化して再現する。os.environ への **書き込みは行わない**（読み取り専用）。UI レイアウト・ラベル文言・注記の表示条件そのものは変えない（純粋に env var 参照元を registry へ寄せるのみ）。

    model_fetch.py 側のキー取得（旧 1437/1479/1522 行の `self.xxx_api_key_var.get().strip() or os.environ.get(...)`）は D-09 の明示対象外（RESEARCH の統合4箇所に含まれない）のため本タスクでは変更しない（スコープ厳守・D-06）。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py tests/test_imports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - sections.py の環境変数「設定済み」注記判定が claude/gemini/runpod のハードコード env var 名を直接持たず `registry.env_vars_for()` 経由で判定する
    - Gemini の dual env var 表示（設定済み判定・表示用変数名の GEMINI_API_KEY 優先）が不変
    - model_fetch.py のキー取得ロジックは未変更（D-09 スコープ外）
    - `pytest tests/test_provider_ui.py tests/test_imports.py -q` が全緑
    - `pytest -q`（全スイート）が全緑
    - `ruff check . && ruff format --check .` がクリーン
  </acceptance_criteria>
  <done>llm_config の環境変数チェックが registry へ統合され、D-09 の4参照面すべてが registry を単一情報源とする状態になり、全スイートが緑を維持する</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces（このプランが新規作成するシンボル/ファイル）

- **新規ファイル/モジュール**: `pagefolio/dialogs/llm_config/__init__.py`, `dialog.py`, `sections.py`, `model_fetch.py`
- **新規シンボル（Mixin クラス）**: `DialogMixin`（dialog.py）, `SectionsMixin`（sections.py）, `ModelFetchMixin`（model_fetch.py）
- **新規テスト**: test_provider_ui.py の MRO 構造検証テスト（メソッド）
- **削除**: `pagefolio/dialogs/llm_config.py`（モジュール → パッケージへ移行）

> `LLMConfigDialog` と全メソッド（`_build` / `_apply` / `_on_provider_change` / `_fetch_models_async` 等）は **既存シンボルの物理移動**であり新規作成ではない（同一 identity・drift 検証対象外）。新規なのは3つの Mixin クラスと4つのモジュールファイルのみ。
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| アプリ内部 → `import pagefolio` 公開名前空間 | llm_config 分割の re-export で LLMConfigDialog/内部メソッドがトップレベル露出する境界 |
| `os.environ`（読み取り専用）→ UI 注記表示 | sections.py が「設定済み」注記のため environ を読むのみで書き込まない境界 |
| Tk メインスレッド ↔ モデル取得ワーカースレッド | `_fetch_models_async` の after() ブリッジ境界（機械的移動で挙動不変・スレッド境界を再編しない） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Information Disclosure | `dialogs/llm_config/__init__.py` re-export 面 / `pagefolio` トップレベル | low | mitigate | 両経路 import は `pagefolio.dialogs` / `pagefolio.dialogs.llm_config` のみ。`pagefolio` トップレベルへ LLMConfigDialog を露出しない（Pitfall 4・Plan 01 の負ガードが緑を維持） |
| T-01-05 | Tampering | tk.Toplevel MRO / Mixin 統合の __init__ 集約 | medium | mitigate | Task 1 で MRO 構造テスト（tk.Toplevel 末尾・__init__ は DialogMixin・主要メソッド存在）を headless で自動検証。実機描画目視は v1.8.0 スコープ外（PROJECT.md）だが MRO 破壊は自動検知される |
| T-01-04 | Information Disclosure | sections.py の環境変数「設定済み」注記の env var 参照 | low | mitigate | registry.env_vars_for 参照へ統合するが os.environ は読み取りのみ・Gemini dual env var 表示挙動不変。既存 test_provider_ui のセクションテストが回帰検知 |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージ導入ゼロ（RESEARCH「Package Legitimacy Audit: 該当なし」）。install タスクなし |
</threat_model>

<verification>
- `pytest -q`（全スイート）が全緑 — 特に `tests/test_provider_ui.py`（更新済み source-scan + MRO 構造テスト含む）と `tests/test_imports.py`（両経路 import 安全網）が緑
- 旧 `pagefolio/dialogs/llm_config.py` が削除され、パッケージのみが存在
- `from pagefolio.dialogs import LLMConfigDialog` / `from pagefolio.dialogs.llm_config import LLMConfigDialog` が両方動作
- D-09 の4参照面すべてが registry を単一情報源とする
- `ruff check . && ruff format --check .` がクリーン

## Manual-Only（v1.8.0 スコープ外・参考）
- LLM 設定ダイアログの実表示・プロバイダ切替・モデル取得・適用の実機目視は v1.8.0 スコープ外（PROJECT.md）。MRO 構造テストが tk.Toplevel 初期化の健全性を headless で代替検証する。任意で `python pagefolio.py` 起動 →「⚙ LLM設定」開閉により分割前と同一挙動を確認できる。
</verification>

<success_criteria>
- llm_config が責務別3層 Mixin パッケージへ分割され、既存 import パスが変更なく動作（成功基準2）
- test_provider_ui の source-scan テストがパッケージ走査へ更新され緑（成功基準4）
- D-09 #4 が registry へ統合され V180-ROBUST-02 の全参照面統合が完了
- 全スイートが全緑（成功基準4）
</success_criteria>

<output>
Create `.planning/phases/01-foundation-split/01-04-SUMMARY.md` when done
</output>
