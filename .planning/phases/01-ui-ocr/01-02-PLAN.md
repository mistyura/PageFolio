---
phase: 01-ui-ocr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/ui_builder.py
  - pagefolio/constants.py
  - README.md
  - 開発履歴.md
autonomous: true
requirements:
  - V16-UI-02
user_setup: []

must_haves:
  truths:
    - "サムネイルサイズ変更スライダーが、全選択/解除ボタン行の下の独立した全幅行に配置される（D-08）"
    - "左ペインを縮小してもスライダーが潰れず操作できる（ボタンと幅を競合しない）（D-07）"
    - "スライダーの範囲 0.5〜2.5・thumb_zoom_var・<ButtonRelease-1>→_on_thumb_zoom_release の挙動は従来どおり（D-09）"
    - "viewer.py / settings.py は変更されない（thumb_zoom_var 属性名と倍率挙動が維持される）"
  artifacts:
    - path: "pagefolio/ui_builder.py"
      provides: "スライダー専用の全幅独立行 zoom_frame（sel_frame の直後・canvas_frame の前）"
      contains: "thumb_zoom_scale"
    - path: "pagefolio/constants.py"
      provides: "v1.6.0 へ更新された APP_VERSION"
      contains: "APP_VERSION"
  key_links:
    - from: "pagefolio/ui_builder.py（zoom_frame）"
      to: "pagefolio/viewer.py（_on_thumb_zoom_release）"
      via: "thumb_zoom_scale の <ButtonRelease-1> バインド（配置変更後も不変）"
      pattern: "_on_thumb_zoom_release"
    - from: "pagefolio/ui_builder.py（thumb_zoom_var）"
      to: "pagefolio/viewer.py（_get_thumb_photo 倍率参照）"
      via: "thumb_zoom_var 属性名を維持し倍率挙動を不変に保つ"
      pattern: "thumb_zoom_var"
---

<objective>
サムネイルサイズ変更スライダーを、全選択/解除ボタンと同一行（`sel_frame` 内 `side="right"`）で幅を奪い合う現状から、ボタン行の直後の独立した全幅行へ移設する。これにより左ペインを縮小してもスライダーが潰れず常に操作できる状態にする（V16-UI-02）。配置変更のみで挙動（範囲 0.5〜2.5・`thumb_zoom_var`・`<ButtonRelease-1>` → `_on_thumb_zoom_release` の設定保存と再描画）は一切変えない（D-09）。あわせて本マイルストーン（v1.6.0）の出荷準備として `APP_VERSION` を更新し、README バッジと開発履歴.md を同期する。

Purpose: 狭いペイン幅でもスライダーが確保され、サムネイルサイズ変更が常時可能になる。`pack` 構造変更を `_build_thumb_panel` 内に閉じ込め、`viewer.py` / `settings.py` には一切手を入れない。
Output: 独立行 `zoom_frame` に移設されたスライダーと、更新された APP_VERSION / README バッジ / 開発履歴.md。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ocr/01-CONTEXT.md
@.planning/phases/01-ui-ocr/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: サムネイルスライダーを独立全幅行へ移設</name>
  <files>pagefolio/ui_builder.py</files>
  <read_first>
    - pagefolio/ui_builder.py:173-212（`_build_thumb_panel`。hdr 独立行 174-189 / sel_frame ボタン行 191-198 / 現状スライダー 200-209 / canvas_frame 211-212）
    - pagefolio/ui_builder.py:191-198（手本となる `sel_frame` の全幅独立行パターン `tk.Frame(parent, bg=C["BG_PANEL"])` + `pack(fill="x", ...)`）
    - pagefolio/viewer.py:146-154（`_on_thumb_zoom_release` — このコールバックは変更しない・参照のみ）
    - pagefolio/viewer.py:137-139 付近（`_get_thumb_photo` の `fitz.Matrix(0.22 * z, 0.22 * z)` 倍率参照 — thumb_zoom_var 属性名維持の根拠・変更しない）
    - .planning/phases/01-ui-ocr/01-PATTERNS.md §`pagefolio/ui_builder.py`（移設前後の pack 構造と不変条件 D-09）
  </read_first>
  <action>
    `_build_thumb_panel`（173-212）内で、`thumb_zoom_var` / `thumb_zoom_scale` の生成と `<ButtonRelease-1>` バインド（現状 200-209、`sel_frame` 内 `side="right", fill="x", expand=True`）を、`sel_frame` の直後・`canvas_frame`（211-212）の前に新設する独立行へ移す。新設行は既存の `hdr` / `sel_frame` と同じ全幅独立行パターンに倣い `tk.Frame(parent, bg=C["BG_PANEL"])` を作り `pack(fill="x", padx=6, pady=(0, 4))` する（変数名は `zoom_frame` を推奨）。スライダーの親を `sel_frame` から `zoom_frame` に変更し、`pack` を `side="right"` 指定なしの `pack(fill="x", expand=True, padx=2)` にして全幅配置する（ボタンと幅を競合させない）。`zoom_frame` の `pack` は必ず `canvas_frame` の `pack` より前（パネル上部側）に置く。色は `C["BG_PANEL"]` 等テーマ辞書経由のみ、フォントが必要な場合は `self._font(delta)` を使い、いずれもハードコードしない。**不変条件（D-09・必達）:** `from_=0.5` / `to=2.5`、`textvariable=self.thumb_zoom_var`、`orient="horizontal"`、`<ButtonRelease-1>` → `self._on_thumb_zoom_release` バインド、`thumb_zoom_var` の初期値 `self.settings.get("thumb_zoom", 1.0)` は一切変更しない。sel_frame 内のボタン 2 つ（select_all / deselect、193-198）はそのまま残す。viewer.py / settings.py は変更しない。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio" && python -c "import ast,io; ast.parse(io.open('pagefolio/ui_builder.py',encoding='utf-8').read())" && git diff --name-only -- pagefolio/viewer.py pagefolio/settings.py | grep -c . | grep -qx 0 && echo "viewer/settings untouched"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `pagefolio/ui_builder.py` — `thumb_zoom_scale` が新設の独立 `tk.Frame`（zoom_frame）を親に生成され、その frame が `pack(fill="x", ...)` で全幅配置されている。
    - Source: `pagefolio/ui_builder.py` — `thumb_zoom_scale.pack(...)` に `side="right"` が含まれない（全幅・ボタンと競合しない）。
    - Source: `pagefolio/ui_builder.py` — スライダーは `from_=0.5` / `to=2.5` / `variable=self.thumb_zoom_var` / `orient="horizontal"` を維持し、`<ButtonRelease-1>` が `self._on_thumb_zoom_release` にバインドされている。
    - Source: `pagefolio/ui_builder.py` — zoom_frame の `pack` が canvas_frame の `pack` より前（行番号が小さい位置）に置かれている。
    - Behavior: `pagefolio/viewer.py` と `pagefolio/settings.py` が本タスクで変更されていない（git diff で未変更）。
    - Test: `python -c "...ast.parse..."` が構文エラーなく完了する。
  </acceptance_criteria>
  <done>スライダーが独立 zoom_frame の全幅行（ボタン行直後・canvas 前）へ移設され、範囲/変数/バインドが不変。viewer.py/settings.py 未変更。構文検証通過。</done>
</task>

<task type="auto">
  <name>Task 2: APP_VERSION 更新・README バッジ・開発履歴.md 同期</name>
  <files>pagefolio/constants.py, README.md, 開発履歴.md</files>
  <read_first>
    - pagefolio/constants.py:12 付近（`APP_VERSION = "v1.5.0"` — v1.6.0 へ更新）
    - README.md（バージョンバッジ箇所。`grep -n "v1.5" README.md` で位置特定）
    - 開発履歴.md（最新エントリのフォーマット。先頭付近の既存エントリに倣う）
    - CLAUDE.md §"変更時のチェックリスト" / §"バージョン番号を更新"（APP_VERSION を真の情報源とし README バッジ・開発履歴.md を同期する規約）
  </read_first>
  <action>
    `pagefolio/constants.py` の `APP_VERSION` を `"v1.5.0"` から `"v1.6.0"` へ更新する。README.md 内のバージョンバッジ・バージョン表記（`grep -n "v1.5" README.md` で特定した箇所）を v1.6.0 へ同期する。開発履歴.md の先頭付近に、既存エントリのフォーマットに倣って v1.6.0 Phase 1 の変更（OCR 数値パラメータの読み取り専用化・編集導線の LLM 設定への一元化・サムネイルスライダーの独立全幅行への移設）を日本語で追記する。記載は本フェーズ 2 プラン分（V16-UI-01 + V16-UI-02）の成果をまとめた 1 エントリでよい。CLAUDE.md の言語ルールに従い、開発履歴の記載・コミット文言は日本語で書く。`pyproject.toml` は編集しない。
  </action>
  <verify>
    <automated>cd "C:/Users/shdwf/work/project/PageFolio" && grep -q 'APP_VERSION = "v1.6.0"' pagefolio/constants.py && grep -c 'v1.6.0' README.md | grep -qv '^0$' && grep -c 'v1.6.0' 開発履歴.md | grep -qv '^0$' && echo "version synced"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `pagefolio/constants.py` — `APP_VERSION = "v1.6.0"` が存在する（`v1.5.0` は残っていない）。
    - Source: `README.md` — `v1.6.0` のバージョン表記/バッジが存在し、古い `v1.5.0` バッジが残っていない。
    - Source: `開発履歴.md` — v1.6.0 Phase 1 の変更内容（OCR パラメータ読み取り専用化・スライダー配置）を記した日本語エントリが追加されている。
    - Behavior: `pyproject.toml` が変更されていない。
    - Test: 上記 grep ベースの version-sync チェックが通過する。
  </acceptance_criteria>
  <done>APP_VERSION=v1.6.0、README バッジ同期、開発履歴.md に日本語エントリ追記、pyproject.toml 未編集。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| thumb_zoom_var → pagefolio_settings.json | スライダー値が `_on_thumb_zoom_release` 経由で settings へ保存される（既存経路・本フェーズで変更しない） |

> このフェーズは完全にローカルな Tkinter UI リファクタである。スライダーの親フレームと pack 引数を変更するのみで、新規ネットワーク I/O・新規ファイル/資格情報処理・新規外部アタックサーフェスを導入しない。バージョン更新は定数・ドキュメントのテキスト変更であり攻撃面を持たない。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | thumb_zoom 設定の保存経路 | accept | スライダー配置変更は `_build_thumb_panel` の pack 構造に閉じ、保存ロジック（`_on_thumb_zoom_release` → `_save_settings`）は変更しない。thumb_zoom は非機密の表示倍率値であり改ざんリスク増分なし |
| T-01-05 | Information Disclosure | README / 開発履歴.md / constants | accept | バージョン番号とフェーズ変更内容の公開テキストのみ。API キー等の機密情報を含まない |
</threat_model>

<verification>
- `python -c "import ast,io; ast.parse(io.open('pagefolio/ui_builder.py',encoding='utf-8').read())"` 構文検証通過
- `pagefolio/viewer.py` / `pagefolio/settings.py` が git diff で未変更
- `grep 'APP_VERSION = "v1.6.0"' pagefolio/constants.py` 一致、README / 開発履歴.md に v1.6.0 反映
- 手動確認（実行者裁量）: アプリ起動 → 左ペインを縮小し、スライダーが独立行で全幅・潰れず操作可能、サイズ変更が従来どおり反映されることを確認
</verification>

<success_criteria>
- ROADMAP Success Criteria 3: サムネイルサイズ変更スライダーが左ペイン縮小時も常に見える位置（ボタン行直後の独立全幅行）に表示される
- ROADMAP Success Criteria 4: スライダーでサムネイルサイズを変更でき、機能は従来どおり動作する（範囲・コールバック不変）
- V16-UI-02 充足、v1.6.0 出荷準備（バージョン同期）
</success_criteria>

<output>
Create `.planning/phases/01-ui-ocr/01-02-SUMMARY.md` when done
</output>
