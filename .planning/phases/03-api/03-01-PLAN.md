---
phase: 03-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/settings.py
  - pagefolio/__init__.py
  - pagefolio/app.py
  - pagefolio/dialogs/merge.py
  - pagefolio/dialogs/llm_config.py
autonomous: true
requirements: [REFAC-04]

must_haves:
  truths:
    - "settings.py に set_current_font_size(size) と get_current_font_size() が存在する (REFAC-04 / SC-1)"
    - "app.py が _current_font_size を直接書き換えていない (write 側が setter 経由) (REFAC-04 / SC-1)"
    - "dialogs/merge.py と dialogs/llm_config.py が _current_font_size を直接 import せず getter 経由で読む (REFAC-04 / D-01)"
    - "dialogs のフォントサイズが import 時点固定ではなく呼び出し時の最新値を反映する (D-02 stale binding 解消)"
    - "ruff check . と ruff format . がエラーなしで完了する (SC-3)"
    - "pytest が全通する (SC-3)"
  artifacts:
    - path: "pagefolio/settings.py"
      provides: "set_current_font_size / get_current_font_size 公開 API"
      contains: "def set_current_font_size("
    - path: "pagefolio/__init__.py"
      provides: "setter/getter のトップレベル再エクスポート (D-05 推奨)"
      contains: "set_current_font_size"
  key_links:
    - from: "pagefolio/app.py"
      to: "pagefolio.settings.set_current_font_size"
      via: "ファイル先頭 import + 呼び出し (write 側 2 箇所)"
      pattern: "set_current_font_size\\(self\\.font_size\\)"
    - from: "pagefolio/dialogs/merge.py"
      to: "pagefolio.settings.get_current_font_size"
      via: "メソッド内呼び出し (read 側 2 箇所)"
      pattern: "get_current_font_size\\(\\)"
    - from: "pagefolio/dialogs/llm_config.py"
      to: "pagefolio.settings.get_current_font_size"
      via: "フォールバック内呼び出し (read 側 1 箇所)"
      pattern: "get_current_font_size\\(\\)"
---

<objective>
REFAC-04 を完全に実装する。可変モジュール変数 `pagefolio/settings.py:_current_font_size` への外部からの「直接代入（write）」と「直接 import 参照（read）」を全廃し、公開 API（`set_current_font_size` / `get_current_font_size`）経由に一本化する。

Purpose: DEBT-04（プライベート変数への外部アクセス）を write/read 両面で解消し、import 時点で値を束縛する stale binding バグ（D-02）も同時に修正する。ROADMAP Phase 3 SC-1 を満たす。
Output: setter/getter を追加した `settings.py`、再エクスポートを追加した `__init__.py`、setter/getter 経由に書き換えた `app.py` / `dialogs/merge.py` / `dialogs/llm_config.py`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api/03-CONTEXT.md
@.planning/phases/03-api/03-RESEARCH.md
@.planning/phases/03-api/03-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_produced>
## このフェーズ（本プラン）が生成・変更するシンボル

- `pagefolio.settings.set_current_font_size(size: int) -> None` — 新規公開 setter（`global _current_font_size` 必須）
- `pagefolio.settings.get_current_font_size() -> int` — 新規公開 getter（呼び出し時に最新値を返す）
- `pagefolio.set_current_font_size` / `pagefolio.get_current_font_size` — `__init__.py` 経由のトップレベル再エクスポート（D-05 推奨）
- `app.py` の write 側 2 箇所: `_settings_mod._current_font_size = self.font_size` → `set_current_font_size(self.font_size)`（ローカル `import pagefolio.settings as _settings_mod` を削除）
- `dialogs/merge.py` / `dialogs/llm_config.py` の read 側 3 箇所: `_current_font_size` → `get_current_font_size()`

> 新規テストファイル `tests/test_imports.py` はプラン 03-02（TEST-03）で作成する。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: settings.py に setter/getter を追加し __init__.py で再エクスポートする</name>
  <files>pagefolio/settings.py, pagefolio/__init__.py</files>
  <read_first>
    - pagefolio/settings.py（全体。特に末尾 :98–107 の `_make_font` のスタイルと `_current_font_size = 12` 定義 :106–107）
    - pagefolio/__init__.py（全体。特に settings 再エクスポートブロック :33–42）
    - .planning/phases/03-api/03-RESEARCH.md §Architecture Patterns（setter/getter 実装パターン・global 宣言の必須性）
    - .planning/phases/03-api/03-PATTERNS.md §settings.py / §__init__.py（追加位置・括弧付き複数行 import スタイル）
    - CLAUDE.md §禁止事項（裸 except 禁止・型ヒント Python 3.8 互換）
  </read_first>
  <behavior>
    - Test 1: `from pagefolio.settings import set_current_font_size, get_current_font_size` が ImportError なく成功する
    - Test 2: `set_current_font_size(14)` 呼び出し後に `get_current_font_size()` が `14` を返す（roundtrip）
    - Test 3: `set_current_font_size` 内で `global _current_font_size` が宣言されており、呼び出しても `UnboundLocalError` が出ない
    - Test 4: `import pagefolio` 後に `hasattr(pagefolio, "set_current_font_size")` と `hasattr(pagefolio, "get_current_font_size")` が共に True（D-05 再エクスポート）
  </behavior>
  <action>
    `pagefolio/settings.py` 末尾の既存行 `_current_font_size = 12`（:107）の直後に、公開関数 `set_current_font_size(size: int) -> None` と `get_current_font_size() -> int` を追加する。`set_current_font_size` の本体は冒頭で `global _current_font_size` を宣言してから `_current_font_size = size` を代入する単純代入のみ（D-04: クランプ・バリデーションは入れない）。`get_current_font_size` は `return _current_font_size` のみ。型ヒントは Python 3.8 互換、docstring は日本語（既存 `_make_font` の注釈スタイルに倣う）。`global` 宣言の欠落は最大の実装トラップ（RESEARCH §Common Pitfalls 1）であり、欠落すると `UnboundLocalError` になる。

    次に `pagefolio/__init__.py` の settings 再エクスポートブロック（:34–42 の `from pagefolio.settings import (...)` 括弧内）に `get_current_font_size` と `set_current_font_size` を追加する（D-05 推奨: 既存の `_apply_theme` / `_make_font` 等 private 関数も再エクスポートしている前例に倣い、公開表面の一貫性を保つ）。括弧付き複数行 import スタイルを維持し、末尾カンマを付ける。`# noqa: F401` コメントは既存ブロックのものをそのまま維持する。
  </action>
  <verify>
    <automated>python -c "from pagefolio.settings import set_current_font_size, get_current_font_size; set_current_font_size(14); assert get_current_font_size()==14; set_current_font_size(12); import pagefolio; assert hasattr(pagefolio,'set_current_font_size') and hasattr(pagefolio,'get_current_font_size'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `settings.py` に `def set_current_font_size(` と `def get_current_font_size(` が存在する
    - `set_current_font_size` 関数本体に `global _current_font_size` が含まれる
    - `__init__.py` の settings ブロックに `set_current_font_size` と `get_current_font_size` の両方が含まれる
    - 上記 verify コマンドが `OK` を出力して exit 0
    - `ruff check pagefolio/settings.py pagefolio/__init__.py` が exit 0
  </acceptance_criteria>
  <done>setter/getter が settings.py に存在し、roundtrip が動作し、`import pagefolio` 経由でも両シンボルが解決する。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write 側（app.py）と read 側（merge.py / llm_config.py）を公開 API 経由に切り替える</name>
  <files>pagefolio/app.py, pagefolio/dialogs/merge.py, pagefolio/dialogs/llm_config.py</files>
  <read_first>
    - pagefolio/app.py（:6–26 の import ブロック、:49–51 の write 箇所 1、:346–348 の write 箇所 2）
    - pagefolio/dialogs/merge.py（:14 の import、:33 と :213 の read 箇所）
    - pagefolio/dialogs/llm_config.py（:12 の import、:49–51 のフォールバック内 read 箇所）
    - .planning/phases/03-api/03-RESEARCH.md §write 側の変更 / §read 側の変更（行番号対応表）と §Common Pitfalls 3（ローカル import 削除漏れ）
    - .planning/phases/03-api/03-PATTERNS.md §app.py / §merge.py / §llm_config.py（変更前後コードと括弧付き import スタイル）
  </read_first>
  <behavior>
    - Test 1: `app.py` に `_settings_mod._current_font_size =` という直接代入が 1 箇所も残っていない
    - Test 2: `app.py` のファイル先頭 import ブロックに `set_current_font_size` が含まれ、write 側 2 箇所が `set_current_font_size(self.font_size)` 呼び出しになっている
    - Test 3: `merge.py` / `llm_config.py` に `from pagefolio.settings import _current_font_size`（private import）が残っていない
    - Test 4: `merge.py` の read 2 箇所と `llm_config.py` の read 1 箇所が `get_current_font_size()` 呼び出しになっている（呼び出し時に最新値を読む = stale binding 解消）
    - Test 5: `app.py` に F401（未使用 import）が残らない（ローカル `import pagefolio.settings as _settings_mod` を削除）
  </behavior>
  <action>
    write 側（app.py）: ファイル先頭の `from pagefolio.settings import (...)` ブロック（:20–24）に `set_current_font_size` を追加する（括弧付き複数行・末尾カンマ・既存 `_apply_theme` 等と整列）。次に `__init__`（:49–51）の `import pagefolio.settings as _settings_mod` ローカル import 文を削除し `_settings_mod._current_font_size = self.font_size` を `set_current_font_size(self.font_size)` に置き換える。同様に `_apply_settings`（:346–348）のローカル import 文を削除し代入を `set_current_font_size(self.font_size)` に置き換える。ローカル import 削除は setter 化と同一コミットで行うこと（RESEARCH §Common Pitfalls 3: 残すと ruff F401）。

    read 側（merge.py）: import 行（:14）の `from pagefolio.settings import _current_font_size` を `from pagefolio.settings import get_current_font_size` に変更する。`MergeOrderDialog.__init__`（:33）と `MergeResizeDialog.__init__`（:213）の `self._font_size = _current_font_size` を `self._font_size = get_current_font_size()` に変更する（メソッド内＝呼び出し時参照で D-02 の stale binding を解消）。

    read 側（llm_config.py）: import 行（:12）の `from pagefolio.settings import _current_font_size` を `from pagefolio.settings import get_current_font_size` に変更する。フォールバック内（:51）の `fs = _current_font_size` を `fs = get_current_font_size()` に変更する。:48–51 の `try/except Exception` 構造は維持する（CLAUDE.md: 裸 except 禁止・既存 `except Exception:` を保持）。意味論は変わらない（getter は単純なモジュール変数参照で例外を投げない）。
  </action>
  <verify>
    <automated>python -c "import pagefolio.app, pagefolio.dialogs.merge, pagefolio.dialogs.llm_config; print('import OK')" && ruff check pagefolio/app.py pagefolio/dialogs/merge.py pagefolio/dialogs/llm_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' pagefolio/app.py | grep -c "_settings_mod._current_font_size ="` が 0（直接代入が残っていない）
    - `grep -v '^#' pagefolio/app.py | grep -c "import pagefolio.settings as _settings_mod"` が 0（ローカル import 削除済み）
    - `grep -v '^#' pagefolio/dialogs/merge.py | grep -c "import _current_font_size"` が 0
    - `grep -v '^#' pagefolio/dialogs/llm_config.py | grep -c "import _current_font_size"` が 0
    - `pagefolio/app.py` の import ブロックに `set_current_font_size` が含まれる
    - `merge.py` に `get_current_font_size()` が 2 箇所、`llm_config.py` に 1 箇所含まれる
    - 上記 verify コマンドが `import OK` を出力し ruff が exit 0
  </acceptance_criteria>
  <done>write/read すべての `_current_font_size` 直接アクセスが setter/getter 経由に置換され、未使用 import が残らず、対象 3 ファイルが例外なく import できる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| なし | このプランは純粋な内部リファクタリング（モジュール変数アクセサ追加 + 呼び出し置換）であり、新規の外部入力・ネットワーク・ファイル I/O・認証経路を一切追加しない。新しいトラスト境界は発生しない。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `set_current_font_size(size)` の入力値 | accept | 内部 UI（`app.py`）からのみ呼ばれる信頼済み呼び出し。D-04 によりバリデーション・クランプは意図的に省略（REFAC は挙動を変えない原則）。外部入力経路なし・低リスク。 |

> ASVS L1。本フェーズは新規攻撃面ゼロ（no new attack surface）。新規外部パッケージのインストールもないため、サプライチェーン脅威（T-{phase}-SC）は該当なし。RESEARCH §Security Domain で V2–V6 すべて「該当なし」と確認済み。
</threat_model>

<verification>
- `python -c "from pagefolio.settings import set_current_font_size, get_current_font_size"` が成功する
- `python -c "import pagefolio.app, pagefolio.dialogs.merge, pagefolio.dialogs.llm_config"` が成功する
- `ruff check . && ruff format .` が exit 0（SC-3）
- `pytest` が全通する（既存テストへの回帰なし。SC-3）
</verification>

<success_criteria>
- `settings.py` に `set_current_font_size(size)` と `get_current_font_size()` が存在する（SC-1 前半）
- `app.py` が `_current_font_size` を直接書き換えていない（write 側 setter 経由。SC-1 後半）
- `dialogs/merge.py` / `dialogs/llm_config.py` が `_current_font_size` を直接 import せず getter 経由で読む（D-01 完全 API 化）
- `ruff check . && ruff format .` がエラーなし・`pytest` 全通（SC-3）
</success_criteria>

<output>
Create `.planning/phases/03-api/03-01-SUMMARY.md` when done
</output>
