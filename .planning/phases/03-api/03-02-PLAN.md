---
phase: 03-api
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_imports.py
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "tests/test_imports.py が存在し、REFAC-01〜04 で変更/追加された全 import パスを明示 import + assert で検証する (TEST-03 / SC-2)"
    - "constants/lang/themes の後方互換 import が壊れていないことを検証する (REFAC-02)"
    - "dialogs サブパッケージと個別モジュールの import が壊れていないことを検証する (REFAC-01)"
    - "settings の新公開 API (set_current_font_size / get_current_font_size) の import と roundtrip を検証する (REFAC-04)"
    - "pagefolio トップレベル公開サーフェスのシンボル存在を検証する"
    - "pytest が全通し、test_imports.py が Tk root 無しで安全に実行できる (D-08)"
    - "import 回帰テストは単一ファイル tests/test_imports.py に集約する (D-09: 責務を 1 箇所にまとめ見通しを良くする)"
  artifacts:
    - path: "tests/test_imports.py"
      provides: "REFAC-01〜04 の import 回帰テスト（4 テストクラス）"
      contains: "class TestSettingsApiImports"
  key_links:
    - from: "tests/test_imports.py::TestSettingsApiImports"
      to: "pagefolio.settings.set_current_font_size / get_current_font_size"
      via: "明示 import + roundtrip assert"
      pattern: "from pagefolio.settings import (get_current_font_size|set_current_font_size)"
    - from: "tests/test_imports.py::TestDialogsImports"
      to: "pagefolio.dialogs.llm_config.LLMConfigDialog"
      via: "個別モジュール経由 import（トップレベルには非公開のため dialogs 経由）"
      pattern: "from pagefolio.dialogs.llm_config import LLMConfigDialog"
---

<objective>
TEST-03 を実装する。REFAC-01〜04 のリファクタリング（dialogs サブパッケージ化・constants 分割・settings 公開 API 化）で変更/追加された全 import パスが壊れていないことを、新規 `tests/test_imports.py` に「明示 import 文 + シンボル存在 assert」（D-06）で記録する。

Purpose: 後方互換の import 表面を機械的に保証し、将来のリファクタリングで import が壊れたら即座に検出できるようにする。ROADMAP Phase 3 SC-2 を満たす。
Output: 4 テストクラス（constants / dialogs / settings API / package surface）を含む `tests/test_imports.py`。Tk root 不要・import のみ（D-08）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api/03-CONTEXT.md
@.planning/phases/03-api/03-RESEARCH.md
@.planning/phases/03-api/03-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_produced>
## このプランが生成するシンボル

- `tests/test_imports.py`（新規）— 以下 4 テストクラスを含む:
  - `TestConstantsImports` — REFAC-02: `from pagefolio.constants import APP_VERSION, LANG, THEMES, C` / `from pagefolio.lang import LANG` / `from pagefolio.themes import THEMES, C` の検証
  - `TestDialogsImports` — REFAC-01: `from pagefolio.dialogs import (AboutDialog, SettingsDialog, PluginDialog, MergeOrderDialog, MergeResizeDialog)` および個別モジュール（`pagefolio.dialogs.about/.settings/.plugin/.merge/.llm_config`、`LLMConfigDialog` は dialogs 経由）の検証
  - `TestSettingsApiImports` — REFAC-04: `from pagefolio.settings import set_current_font_size, get_current_font_size` の import + roundtrip
  - `TestPackageSurface` — `import pagefolio` 後のトップレベルシンボル（`PDFEditorApp`, `APP_VERSION`, `LANG`, `THEMES`, `C`, `PluginManager`, `PDFEditorPlugin`, `set_current_font_size`, `get_current_font_size` 等）の `hasattr` 検証

> 依存: プラン 03-01（REFAC-04）が先に完了している必要がある（`TestSettingsApiImports` が `set_current_font_size` / `get_current_font_size` の存在を前提にするため）。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_imports.py を作成し REFAC-01〜04 の import 回帰テストを実装する</name>
  <files>tests/test_imports.py</files>
  <read_first>
    - tests/conftest.py（Tk 非依存方針・sys.path 設定。:10–11 のパス追加パターン）
    - tests/test_utils.py（:1–14 の sys.path + import パターン、クラス分割スタイル）
    - tests/test_plugins.py（クラス分割・シンボル存在 assert スタイル）
    - pagefolio/__init__.py（全体。トップレベル公開サーフェスの一次ソース。:8–9 constants/app、:12–18 dialogs、:33–42 settings 再エクスポート）
    - pagefolio/dialogs/__init__.py（:10–14。LLMConfigDialog は dialogs 経由で再エクスポート、トップレベル pagefolio には非公開）
    - .planning/phases/03-api/03-RESEARCH.md §TEST-03 の検証対象 import パス（完全リスト・1〜5）と §Common Pitfalls 4（LLMConfigDialog の帰属）と §Code Examples（assert パターン）
    - .planning/phases/03-api/03-PATTERNS.md §tests/test_imports.py（推奨クラス構成・sys.path パターン）
    - CLAUDE.md §言語ルール（docstring 日本語）・§禁止事項
  </read_first>
  <action>
    新規 `tests/test_imports.py` を作成する。ファイル先頭は既存テスト（`test_utils.py` :1–12）と同じ sys.path 設定パターン（`sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`）を置き、`import pagefolio` する。各テストは「明示 import 文 + シンボル存在 assert」のみとし、ダイアログ class のインスタンス化（`tk.Toplevel()` 等）・`fitz.open()` 等の I/O は一切行わない（D-08: Tk root 不要・ヘッドレス安全）。docstring は日本語で「どのリファクタリングの検証か」を明記する。

    以下 4 クラスを実装する（RESEARCH §テスト関数の推奨分割粒度準拠）:

    `TestConstantsImports`（REFAC-02）: (a) `from pagefolio.constants import APP_VERSION, LANG, THEMES, C` が成功し各シンボルが None でないこと（後方互換）、(b) `from pagefolio.lang import LANG` で `"ja" in LANG`、(c) `from pagefolio.themes import THEMES, C` で `"dark" in THEMES` かつ `isinstance(C, dict)` を検証する。

    `TestDialogsImports`（REFAC-01）: (a) `from pagefolio.dialogs import (AboutDialog, SettingsDialog, PluginDialog, MergeOrderDialog, MergeResizeDialog)` が成功し各シンボルが None でないこと、(b) 個別サブモジュール `pagefolio.dialogs.about` / `.settings` / `.plugin` / `.merge` / `.llm_config` からの import が成功すること。`LLMConfigDialog` は `from pagefolio.dialogs.llm_config import LLMConfigDialog`（または `from pagefolio.dialogs import LLMConfigDialog`）でテストし、`from pagefolio import LLMConfigDialog`（トップレベル）は書かない（RESEARCH §Common Pitfalls 4: トップレベルには非公開で失敗する）。

    `TestSettingsApiImports`（REFAC-04）: (a) `from pagefolio.settings import set_current_font_size` が成功し `callable` であること、(b) `from pagefolio.settings import get_current_font_size` が成功し `callable` であること、(c) roundtrip — `set_current_font_size(14)` 後 `get_current_font_size() == 14`、テスト後に `set_current_font_size(12)` で元値に戻す（他テストへの副作用を残さない）。

    `TestPackageSurface`: `import pagefolio` 後に `hasattr` で以下のトップレベルシンボル存在を検証する — `PDFEditorApp`, `APP_VERSION`, `LANG`, `THEMES`, `C`, `AboutDialog`, `SettingsDialog`, `PluginDialog`, `MergeOrderDialog`, `MergeResizeDialog`, `PluginManager`, `PDFEditorPlugin`, `OCRMixin`, `_load_settings`, `_save_settings`, `_apply_theme`, `_make_font`, `set_current_font_size`, `get_current_font_size`。トップレベルシンボルの一次ソースは `pagefolio/__init__.py` の再エクスポートブロックであり、列挙漏れがないか実コードと突き合わせること（D-07）。
  </action>
  <verify>
    <automated>pytest tests/test_imports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_imports.py` が存在する
    - `grep -c "class Test" tests/test_imports.py` が 4 以上（TestConstantsImports / TestDialogsImports / TestSettingsApiImports / TestPackageSurface）
    - `tests/test_imports.py` に `from pagefolio.settings import` で `set_current_font_size` と `get_current_font_size` の両方が現れる
    - `tests/test_imports.py` に `from pagefolio import LLMConfigDialog` が**含まれない**（帰属誤りの回避）
    - `tests/test_imports.py` に `tk.Toplevel(` や `fitz.open(` が**含まれない**（D-08 import-only）
    - `pytest tests/test_imports.py -x` が exit 0
    - `ruff check tests/test_imports.py` が exit 0
  </acceptance_criteria>
  <done>test_imports.py が 4 クラスで REFAC-01〜04 の全 import パスを検証し、Tk root 無しで pytest が全通する。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| なし | テストファイル 1 つの新規追加のみ。プロダクションコードの挙動・外部入力・ネットワーク・ファイル I/O を一切変更しない。新しいトラスト境界は発生しない。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | (該当なし) | tests/test_imports.py | accept | import-only の回帰テスト。外部入力・状態変更・I/O なし。`set_current_font_size` の roundtrip テストはテスト後に元値（12）へ復元し副作用を残さない。攻撃面ゼロ。 |

> ASVS L1。本プランは新規攻撃面ゼロ（no new attack surface）。新規外部パッケージのインストールもなし（pytest は既存）。RESEARCH §Security Domain で V2–V6 すべて「該当なし」と確認済み。
</threat_model>

<verification>
- `pytest tests/test_imports.py -x` が exit 0（4 クラス全通）
- `pytest` 全体が exit 0（既存テストへの回帰なし。SC-3）
- `ruff check . && ruff format .` が exit 0（SC-3）
- test_imports.py が Tk root 無しで実行完了する（D-08。CI/ヘッドレス安全）
</verification>

<success_criteria>
- `tests/` に import 回帰テスト（`tests/test_imports.py`）が存在し、REFAC-01〜04 で変更された全 import パスが壊れていないことを検証する（SC-2）
- `pytest` が全通し、`ruff check . && ruff format .` でエラーがない（SC-3）
</success_criteria>

<output>
Create `.planning/phases/03-api/03-02-SUMMARY.md` when done
</output>
