---
phase: 01-code-quality-responsive-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio.py
autonomous: true
requirements:
  - QUAL-01
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "ウィンドウを任意のサイズにリサイズしても右側ツールパネルが見切れない"
    - "サムネイル・プレビュー・ツールパネル間の境界をドラッグして比率を変更できる"
    - "ウィンドウを極端に狭くしてもサムネイルパネルが消えず最小幅が保たれる"
    - "既存機能（回転・削除・トリミング・結合・D&D並び替え・Undo/Redo）が正常動作する"
  artifacts:
    - path: "pagefolio.py"
      provides: "3ペイン PanedWindow レイアウトの _build_ui()"
      contains: "paned.add(right, minsize=220"
  key_links:
    - from: "_build_ui()"
      to: "_build_thumb_panel(), _build_preview(), _build_tools_scrollable()"
      via: "3ペイン PanedWindow の各ペインに配置"
      pattern: "paned\\.add\\("
    - from: "_rebuild_ui()"
      to: "_build_ui()"
      via: "全ウィジェット destroy 後に再呼び出し"
      pattern: "_build_ui\\(\\)"
---

<objective>
_build_ui() を 3ペイン PanedWindow に書き換え、右ペインの固定幅 pack を廃止する。同時にレイアウト関連のコードレビューを行い発見したバグを修正する。

Purpose: ウィンドウリサイズ時の右ペイン見切れを解消し、全ペインをユーザーが自由にリサイズできるようにする
Output: レスポンシブな 3ペインレイアウトを持つ pagefolio.py
</objective>

<execution_context>
@C:/Users/shdwf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shdwf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-quality-responsive-ui/01-CONTEXT.md
@.planning/phases/01-code-quality-responsive-ui/01-RESEARCH.md

<interfaces>
<!-- 現在の _build_ui() レイアウト構造（L720-755）- これを書き換える -->

From pagefolio.py L607-608:
```python
class PDFEditorApp:
    MAX_UNDO = 20
```

From pagefolio.py L614:
```python
self.root.minsize(900, 600)  # 800x600 に変更する
```

From pagefolio.py L720-755 (現在の _build_ui):
```python
def _build_ui(self):
    # header 構築 (L721-730) -- 変更なし
    # main Frame (L732-733) -- 廃止（PanedWindow に置き換え）
    # right Frame 固定幅 (L735-740) -- 廃止（3ペイン目に変更）
    # paned = PanedWindow 2ペイン (L742-755) -- 3ペインに拡張
```

From pagefolio.py L846-884 (_build_tools_scrollable):
```python
def _build_tools_scrollable(self, parent):
    # parent 引数を受け取るため、3ペイン化後もそのまま流用可能
    # L882-884 の多重 after() スクロールリセットは整理対象
```

From pagefolio.py L1907-1926 (_rebuild_ui):
```python
def _rebuild_ui(self):
    # 全 destroy → _build_styles() → _build_ui() の流れは変更不要
    # self._plugin_ui_frame のリセット漏れを確認
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: コードレビューとバグ修正（QUAL-01）</name>
  <files>pagefolio.py</files>
  <read_first>
    - pagefolio.py (全体 -- 特に L607-665 __init__, L720-755 _build_ui, L846-884 _build_tools_scrollable, L1907-1926 _rebuild_ui)
    - CLAUDE.md (コーディング規約)
  </read_first>
  <action>
pagefolio.py のレイアウト関連コードを中心にコードレビューを行い、以下の既知リスクを確認・修正する。

1. **_rebuild_ui() での _plugin_ui_frame リセット漏れ**: _rebuild_ui() (L1907付近) で `self._plugin_ui_frame = None` が設定されているか確認する。設定されていなければ、L1919 の `self.crop_rect_id = None` の後に `self._plugin_ui_frame = None` を追加する。_build_tools() 内で _plugin_ui_frame を作成する箇所を確認し、winfo_exists() チェックまたは None リセットが必要か判断する。

2. **_build_tools_scrollable() の多重 after() 整理**: L882-884 の3回の after() 呼び出しを確認する。
   - `inner.after_idle(lambda: canvas.yview_moveto(0))` (L882)
   - `inner.after(100, _after_build)` (L883) -- mousewheel バインド + yview_moveto
   - `inner.after(300, lambda: canvas.yview_moveto(0))` (L884)
   L882 の after_idle は _after_build 内の yview_moveto と重複するため削除できる。L884 の after(300) は _after_build (100ms) で十分であれば削除する。ただし動作に問題が出る場合は L883 の _after_build のみ残す形に統一する。

3. **minsize 変更**: L614 の `self.root.minsize(900, 600)` を `self.root.minsize(800, 600)` に変更する。

4. **その他のレイアウト関連バグ**: _build_ui, _build_thumb_panel, _build_preview, _build_tools_scrollable, _rebuild_ui を読み通し、以下をチェック:
   - 未使用変数や到達不能コード
   - テーマ色のハードコード（`C["KEY"]` 辞書を使うべき箇所）
   - self._doc_buttons への追加漏れ
   発見した問題は同時に修正する。発見がなければ「レビュー済み、問題なし」と記録する。
  </action>
  <verify>
    <automated>cd C:/Users/shdwf/work/project/PageFolio && python -c "import ast; ast.parse(open('pagefolio.py').read()); print('構文OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pagefolio.py L614 付近に `self.root.minsize(800, 600)` が存在する（900 ではない）
    - _rebuild_ui() 内に `_plugin_ui_frame` のリセット処理（None 代入または同等）が存在する
    - _build_tools_scrollable() 内の after() 呼び出しが2回以下に整理されている
    - `python -c "import ast; ast.parse(open('pagefolio.py').read())"` が正常終了する
  </acceptance_criteria>
  <done>minsize が 800x600 に変更され、_rebuild_ui の _plugin_ui_frame リセット漏れが修正され、_build_tools_scrollable の冗長な after() が整理され、構文エラーがない</done>
</task>

<task type="auto">
  <name>Task 2: _build_ui() を 3ペイン PanedWindow に書き換え（UI-01, UI-02, UI-03）</name>
  <files>pagefolio.py</files>
  <read_first>
    - pagefolio.py (L720-755 _build_ui -- Task 1 修正後の状態を確認)
    - .planning/phases/01-code-quality-responsive-ui/01-RESEARCH.md (Pattern 1, Pattern 2 のコード例)
  </read_first>
  <action>
pagefolio.py の `_build_ui()` メソッド (L720付近) を以下の通り書き換える。

**削除するコード:**
- `main = tk.Frame(self.root, bg=C["BG_DARK"])` と `main.pack(fill="both", expand=True)` の2行
- 右ペインの固定幅 Frame 生成: `right_width = max(260, ...)`, `right = tk.Frame(main, ...)`, `right.pack(side="right", fill="y")`, `right.pack_propagate(False)`, `self._build_tools_scrollable(right)` の5行
- 既存の2ペイン PanedWindow: `paned = tk.PanedWindow(main, ...)` の `main` 引数を変更

**新しい _build_ui() の構造:**

```python
def _build_ui(self):
    # ---- ヘッダー（既存コード L721-730 をそのまま維持）----
    header_h = max(56, int(self.font_size * 5))
    header = tk.Frame(self.root, bg=C["BG_PANEL"], height=header_h)
    header.pack(fill="x", side="top")
    header.pack_propagate(False)
    tk.Label(header, text="\u2726 PageFolio", bg=C["BG_PANEL"],
             fg=C["ACCENT"], font=self._font(6, "bold")).pack(side="left", padx=20, pady=12)
    self.status_var = tk.StringVar(value=self._t("status_initial"))
    tk.Label(header, textvariable=self.status_var,
             bg=C["BG_PANEL"], fg=C["SUCCESS"],
             font=self._font(-1)).pack(side="right", padx=20)

    # ---- 3ペイン PanedWindow（main Frame を廃止し直接 root に配置）----
    paned = tk.PanedWindow(self.root, orient="horizontal", bg=C["BG_DARK"],
                           sashwidth=5, sashrelief="flat",
                           opaqueresize=True, bd=0)
    paned.pack(fill="both", expand=True)

    # 左ペイン: サムネイル
    left_width = max(200, int(self.font_size * 18))
    left = tk.Frame(paned, bg=C["BG_PANEL"])
    self._build_thumb_panel(left)
    paned.add(left, minsize=150, width=left_width)

    # 中央ペイン: プレビュー
    center = tk.Frame(paned, bg=C["BG_DARK"])
    self._build_preview(center)
    paned.add(center, minsize=300)

    # 右ペイン: ツール（固定幅 → PanedWindow の3番目のペインに変更）
    right_width = max(260, int(self.font_size * 22))
    right = tk.Frame(paned, bg=C["BG_PANEL"])
    self._build_tools_scrollable(right)
    paned.add(right, minsize=220, width=right_width)

    # デフォルト比率 20:50:30 を描画後に設定
    def _set_sash():
        paned.update_idletasks()
        total = paned.winfo_width()
        if total > 100:
            paned.sash_place(0, int(total * 0.20), 0)
            paned.sash_place(1, int(total * 0.70), 0)
    self.root.after_idle(_set_sash)
```

**変更のポイント:**
- `main` Frame を削除し、`paned` を直接 `self.root` に `pack(fill="both", expand=True)` で配置
- 右ペインを `paned.add(right, minsize=220, width=right_width)` として PanedWindow の3番目のペインに追加
- `pack_propagate(False)` は不要（PanedWindow が幅を管理）
- `after_idle` で `_set_sash` を呼び出し、初期 sash 位置を 20:50:30 に設定
- header の構築コードは一切変更しない
  </action>
  <verify>
    <automated>cd C:/Users/shdwf/work/project/PageFolio && python -c "import ast; ast.parse(open('pagefolio.py').read()); print('構文OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pagefolio.py の _build_ui() 内に `paned = tk.PanedWindow(self.root,` が存在する（main ではなく self.root）
    - `paned.add(` が3回呼ばれている（left, center, right）
    - `paned.add(right, minsize=220` が存在する
    - `main = tk.Frame` が _build_ui() 内に存在しない
    - `pack_propagate(False)` が _build_ui() 内で header 以外に存在しない（右ペインの pack_propagate 削除）
    - `sash_place(0,` と `sash_place(1,` が存在する
    - `after_idle` で sash 位置設定が呼ばれている
    - `python -c "import ast; ast.parse(open('pagefolio.py').read())"` が正常終了する
  </acceptance_criteria>
  <done>_build_ui() が 3ペイン PanedWindow で構築され、右ペインが PanedWindow の3番目のペインとして配置され、sash ドラッグで全ペインのリサイズが可能で、構文エラーがない</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('pagefolio.py').read())"` で構文チェック
2. アプリ起動: `python pagefolio.py` で正常起動を確認
3. 手動確認: ウィンドウリサイズ時に右ペインが見切れないこと、sash ドラッグで3ペイン全ての比率変更が可能なこと、最小サイズ 800x600 でサムネイルパネルが表示されること
</verification>

<success_criteria>
- _build_ui() が 3ペイン PanedWindow を使用している
- 右ペインの固定幅 pack が廃止されている
- root.minsize が 800x600 に設定されている
- _rebuild_ui() の _plugin_ui_frame リセット漏れが修正されている
- 構文チェックが通る
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-quality-responsive-ui/01-01-SUMMARY.md`
</output>
