---
context: phase
phase: 01-基盤と画像対応
task: null
total_tasks: null
status: ready_to_plan
last_updated: 2026-05-04
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_今セッションで発見した失敗由来の制約はありません。_

<current_state>
フェーズ1「基盤と画像対応」のプランニング準備が整った状態。

- CONTEXT.md (D-01〜D-12): ✓ 完成
- UI-SPEC.md (6/6 PASS): ✓ 承認済み
- PLAN.md: 未作成（次のステップ）
- 実行: 未着手

次のアクション: `/gsd-plan-phase 1` を実行してプランを作成する。
</current_state>

<completed_work>

完了済みタスク:
- `/gsd-discuss-phase 1` — CONTEXT.md 作成 (D-01〜D-12: 12 の実装決定事項)
  - requirements.txt の範囲 (D-01〜D-04)
  - ファイルダイアログ・D&D (D-05〜D-08)
  - 複数画像の同時開封 (D-09〜D-10)
  - 上書き保存の挙動 (D-11〜D-12)
  - commit: d9024f0
- `/gsd-ui-phase 1` — UI-SPEC.md 作成・承認
  - gsd-ui-researcher → 01-UI-SPEC.md 生成（Tkinter コンテキスト適応）
  - gsd-ui-checker → BLOCK（Dimension 5: spacing 値が 4 の倍数でない）
  - リビジョン: Widget Geometry を「既存コード実測値（参照専用）」として再分類
  - gsd-ui-checker 再実行 → 全 6 ディメンション PASS
  - commit: 19d911a (UI-SPEC) + 29bee1a (revision) + ff80450 (draft)
</completed_work>

<remaining_work>

- `/gsd-plan-phase 1` — 実行プランの作成（要件: MAINT-02, IMG-01）
- `/gsd-execute-phase 1` — プランの実行（コード変更）
- `pytest && ruff check . && ruff format .` — テスト・リント確認
</remaining_work>

<decisions_made>

- **UI-SPEC Widget Geometry**: 既存の padx/pady 値は 4 の倍数でない値（2px, 6px, 10px 等）を含むが、これらは `ui_builder.py` の実測値。UI-SPEC では「参照専用・変更不可」として封印し、新規ウィジェットには 4/8/16px を要求する
- **requirements.txt**: プロジェクトルートに untracked で存在するが pip freeze 全出力（altgraph, numpy, opencv-python 等を含む）。D-04 の指示通り直接依存のみ（PyMuPDF, Pillow, tkinterdnd2, pyinstaller, pytest, pytest-cov, ruff）に書き直してから git add する必要がある
</decisions_made>

<blockers>
なし。
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-基盤と画像対応/01-CONTEXT.md` — 12 の実装決定事項（D-01〜D-12）。プランの全タスクはこれに基づく
2. `.planning/phases/01-基盤と画像対応/01-UI-SPEC.md` — UI デザインコントラクト。新規 LANG キー 8 件・既存 4 件更新の一覧あり
3. `.planning/ROADMAP.md` §Phase 1 — Goal と Success Criteria（4 項目）
4. `.planning/REQUIREMENTS.md` §IMG-01, §MAINT-02 — 要件の定義

## Critical Anti-Patterns (do NOT repeat these)

なし（今セッションでは実装作業未着手）。

## Infrastructure State

- Python 仮想環境: プロジェクトルートで確認要（venv or pip install済み）
- Tkinter: Windows 11 標準 Python に付属
- tkinterdnd2: pip install 済みのはず（既存動作アプリのため）

## 注意点: untracked ファイル

| ファイル | 状態 | 対処 |
|---------|------|------|
| `requirements.txt` | pip freeze 全出力（不正） | Phase 1 実行中に D-04 通り書き直す |
| `.planning/codebase/` | コードベース分析 5 ファイル（前セッション） | 必要に応じてコミットまたは .gitignore に追加 |

<context>
今セッションで `/gsd-ui-phase 1` を完了。Tkinter デスクトップアプリに web-centric な UI-SPEC テンプレートを適応させ、スペーシング問題（既存実測値 vs 設計トークン）を解決してチェッカーの承認を得た。

フェーズ1のスコープは明確: requirements.txt の整理（直接依存のみ）と画像ファイルのファイルダイアログ・D&D 対応。PyMuPDF が画像を直接 PDF として開ける（fitz.open）ため実装コストは低い。既存の `_open_pdf_path()`、`MergeOrderDialog`、`_save_as()` をほぼそのまま流用できる。
</context>

<next_action>
Start with: `/gsd-plan-phase 1` を実行してフェーズ1の実行プランを作成する。CONTEXT.md の D-01〜D-12 と UI-SPEC.md の Copywriting Contract（新規 LANG キー）がプランの主要インプット。
</next_action>
