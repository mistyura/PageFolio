---
phase: 02-ai
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [V180-TMPL-01, V180-TMPL-02, V180-TMPL-03]
files_modified:
  - pagefolio/dialogs/llm_config/dialog.py
  - pagefolio/dialogs/llm_config/sections.py
  - pagefolio/lang.py
  - tests/test_provider_ui.py

must_haves:
  truths:
    - "ダイアログ内でテンプレートを保存/削除/リネームした後『キャンセル』（destroy のみ・on_apply 非呼出）を押すと、app.settings['prompt_templates'] も pagefolio_settings.json も一切変化しない（Apply/Cancel 契約の回復・CR-02 解消・V180-TMPL-01/03）"
    - "ダイアログ内でテンプレートを変更した後 Apply（_apply）を押すと、変更が on_apply→app.settings 経由で一度だけ確定・永続化される（一括確定）"
    - "LLMConfigDialog.__init__ 後、self.current_settings['prompt_templates'] は app.settings['prompt_templates'] とネスト（items・各テンプレート dict）まで別オブジェクトである（ディープコピー分離）"
    - "_on_template_delete は削除前に messagebox.askyesno の確認を出し、No 応答で delete_template を呼ばずに中止する（誤削除防止・02-REVIEW Fix 案2）"
  artifacts:
    - "pagefolio/dialogs/llm_config/dialog.py（__init__: prompt_templates ディープコピー分離・_apply: items ディープコピー収集）"
    - "pagefolio/dialogs/llm_config/sections.py（_on_template_save/_delete/_rename: 即時 _save_settings 除去・_on_template_delete: askyesno 追加）"
    - "pagefolio/lang.py（tmpl_delete_confirm キー・ja/en 一致）"
    - "tests/test_provider_ui.py（CR-02 回帰テスト群）"
  key_links:
    - "__init__ のディープコピー分離 → CRUD ハンドラの in-place 変更が app.settings を汚染しない"
    - "CRUD ハンドラ（_save_settings 除去）→ _apply が prompt_templates を一括収集 → on_apply が単一経路で永続化"
    - "_on_template_delete → askyesno（No）→ delete_template 非呼出（早期 return）"
---

<objective>
Phase 2 検証（02-VERIFICATION.md status: gaps_found）のブロッカー **CR-02** を閉じる。
LLMConfigDialog のテンプレート CRUD 操作（保存/削除/リネーム）が「キャンセル」で
取り消せない設計欠陥（`dialog.py:46` の浅いコピー起因でネスト辞書 `prompt_templates`
が `app.settings` と同一参照を共有し、`sections.py` の各ハンドラが即座に
`_save_settings()` でディスクへ確定させる）を修正し、Apply/Cancel の一般的な
ダイアログ契約を回復する。

Purpose: テンプレート削除・リネームという本フェーズの中核機能（V180-TMPL-01/03・
ロードマップ Success Criteria 1）を「誤操作しても Cancel で戻せる」安全な状態にし、
データ消失リスク（Tampering/Repudiation 系欠陥）を解消する。

Output: dialog.py の分離修正・sections.py のハンドラ修正・lang.py の削除確認文言・
CR-02 回帰テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-VERIFICATION.md
@.planning/phases/02-ai/02-REVIEW.md
@.planning/phases/02-ai/02-CONTEXT.md
</context>

<gap_source>
本プランは 02-VERIFICATION.md frontmatter の `gaps`（1件・status: failed）を閉じる。

- **gap（BLOCKER・CR-02）:** `pagefolio/dialogs/llm_config/dialog.py:46` の
  `self.current_settings = dict(current_settings)` が浅いコピーのため、ネスト辞書
  `prompt_templates` が `app.settings` と同一参照を共有。`sections.py` の
  `_on_template_save`（1269/1275）・`_on_template_delete`（1293/1299）・
  `_on_template_rename`（1320/1328）が in-place 変更直後に `_save_settings()` で
  即時永続化するため、「キャンセル」で取り消せない（Apply/Cancel 契約違反・データ消失）。
- **修正方針（VERIFICATION.md missing 欄）:** ①`prompt_templates`（active/items）を
  ディープコピーしてダイアログ内 CRUD が `app.settings` を汚染しないようにする ②
  「即時確定」を Apply 経由の一括確定へ変更（`_on_template_*` から `_save_settings`
  を外し `_apply` で一括収集・永続化）③最低限の代替策として `_on_template_delete`
  に削除確認（askyesno）を追加。→ 本プランは①②③をすべて実施する（①②で契約回復・
  ③で誤操作の追加防御）。
</gap_source>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dialog.py で prompt_templates をディープコピー分離し _apply を一括確定へ整合させる</name>
  <files>pagefolio/dialogs/llm_config/dialog.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config/dialog.py（__init__ の 46 行目前後・_apply の 359-513 行。現状の浅いコピーと prompt_templates 収集コメントを確認）
    - .planning/phases/02-ai/02-REVIEW.md（CR-02 セクション・対処案1 の copy.deepcopy 推奨）
    - pagefolio/settings.py（save_template/delete_template/rename_template が settings["prompt_templates"]["items"] を in-place 変更することを確認・188-237 行）
  </read_first>
  <behavior>
    - Test（Task 3 で実装）: LLMConfigDialog.__init__ 後、`d.current_settings["prompt_templates"]` は入力 settings の `prompt_templates` と `is not`（別オブジェクト）で、内側の `items` および各テンプレート dict も別オブジェクト（ディープコピー）である
    - Test（Task 3 で実装）: `d.current_settings["prompt_templates"]["items"]` を変更しても入力 settings 側は不変
    - Test（既存 test_apply_collects_active_template_preserving_items が継続 pass）: _apply の prompt_templates 収集は items を等価（==）保持したまま active を _active_template_name で差し替える
  </behavior>
  <action>
    dialog.py の先頭 import 群に `import copy` を追加する（標準ライブラリ・tkinter import の前、モジュール規約順）。

    `__init__` の `self.current_settings = dict(current_settings)`（46 行目）の直後に、ネスト辞書 `prompt_templates` をディープコピーで分離する行を追加する。分離対象は `current_settings.get("prompt_templates", {"active": "", "items": {}})` を `copy.deepcopy(...)` した結果とし、`self.current_settings["prompt_templates"]` へ代入する。これにより `self.current_settings["prompt_templates"]` および内側の `items`・各テンプレート dict が `app.settings["prompt_templates"]` と完全に独立する（浅いコピーで残っていた共有参照を断つ）。既存の `self.current_settings = dict(current_settings)` 自体は残す（トップレベルの他キーは従来どおり浅いコピーで十分）。

    `_apply` の prompt_templates 収集（473-478 行）で items をエイリアスさせないよう、`"items": dict(existing_templates.get("items", {}))` を `"items": copy.deepcopy(existing_templates.get("items", {}))` に変更する。これで on_apply→app.settings.update 後の app.settings が完全に独立した構造を受け取る（各テンプレート dict の内側まで独立）。

    `_apply` の prompt_templates 収集コメント（461-469 行）が「CRUD 操作（保存/削除/リネーム）は sections.py 側で既に self.current_settings を直接変更し _save_settings 済みのため、_apply は active の最終確定のみを担う」と述べているが、Task 2 で即時 _save_settings を除去し Apply 一括確定へ変更するため、この記述は事実と食い違う。コメントを「CRUD 操作は sections.py 側で分離済みの self.current_settings（ディープコピー）のみを in-place 変更し永続化しない。_apply が active + items を一括収集し on_apply 経由で単一経路で確定・永続化する（V180-TMPL-01〜05・CR-02 修正）」の趣旨へ更新する（active フォールバックの getattr 後方互換の説明は維持する）。
  </action>
  <verify>
    <automated>python -c "import ast,io; ast.parse(io.open('pagefolio/dialogs/llm_config/dialog.py',encoding='utf-8').read())" && grep -c "copy.deepcopy" pagefolio/dialogs/llm_config/dialog.py</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: dialog.py に `import copy` が存在する（`grep -n "^import copy" pagefolio/dialogs/llm_config/dialog.py` が1件）
    - source assertion: `__init__` 内に `copy.deepcopy(` を用いた `prompt_templates` 分離行が存在する（`grep -n "copy.deepcopy" pagefolio/dialogs/llm_config/dialog.py` が2件以上＝__init__ と _apply）
    - behavior assertion: `dict(current_settings)` の行は残存する（トップレベル浅コピーは維持）
    - CLI: `python -c "import ast,io; ast.parse(...)"` が構文エラーなく完了する
    - behavior assertion: _apply の items 収集が `copy.deepcopy` になっている（_apply 内に deepcopy 出現）
    - source assertion: _apply の prompt_templates コメントに旧「_save_settings 済み」の記述が残っていない
  </acceptance_criteria>
  <done>
    dialog.py の __init__ が prompt_templates をディープコピーで分離し、_apply が items をディープコピー収集し、コメントが Apply 一括確定の新設計に整合している。構文パス。
  </done>
</task>

<task type="auto">
  <name>Task 2: sections.py の3 CRUD ハンドラから即時 _save_settings を除去し、_on_template_delete に askyesno 削除確認を追加する</name>
  <files>pagefolio/dialogs/llm_config/sections.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config/sections.py（import 群 1-26 行・_on_template_save 1244-1276・_on_template_delete 1278-1300・_on_template_rename 1302-1329）
    - pagefolio/lang.py（tmpl_ キー群 ja: 642-657 / en: 1315-1330・confirm_title/info_title/err_title の位置）
    - .planning/phases/02-ai/02-CONTEXT.md（D-03: アクティブテンプレート削除禁止・D-04: 重複名拒否）
  </read_first>
  <action>
    sections.py の `_on_template_save`（現 1275 行）・`_on_template_delete`（現 1299 行）・`_on_template_rename`（現 1328 行）の各末尾にある `_save_settings(self.current_settings)` の呼び出し行を3箇所とも削除する。これにより CRUD 操作は分離済み（Task 1）の `self.current_settings`（ディープコピー）のみを in-place 変更し、ディスク永続化は行わない。永続化は `_apply`（Apply ボタン）経由の一括確定に一本化される（Cancel では destroy のみで破棄される）。各ハンドラ末尾の `self._reload_template_combo(...)` 呼び出しは維持する（combobox の再読込は Apply/Cancel と独立した UI 反映のため残す）。

    3箇所の `_save_settings` を除去した結果、`_save_settings` は sections.py 内で未使用になる。冒頭の `from pagefolio.settings import (...)` ブロック（15-26 行）から `_save_settings,` の import 行を削除する（ruff F401 未使用 import を回避）。他のテンプレート CRUD import（delete_template/get_template/list_template_names/rename_template/save_template/template_name_exists）と load_prompt_file/prompt_file_exists/save_prompt_file は使用継続のため残す。

    `_on_template_delete`（1278-1300 行）に削除確認を追加する（02-REVIEW Fix 案2・誤操作の追加防御）。現在アクティブテンプレート拒否（`if name == self._active_template_name:` の showinfo→return）の後、`try: delete_template(...)` の直前に、`messagebox.askyesno(self._L["confirm_title"], self._L["tmpl_delete_confirm"], parent=self)` の確認を挟む。ユーザーが No（False）を返した場合は `delete_template` を呼ばずに早期 return する。Yes（True）の場合のみ従来どおり `delete_template`→`_reload_template_combo` を実行する。`messagebox` は sections.py で既に import 済み（8 行目）のため追加 import は不要。

    lang.py に `tmpl_delete_confirm` キーを ja/en 両方へ同一キーで追加する（test_lang_parity.py のキー数左右一致を維持）。ja は `tmpl_active_delete_blocked`（657 行）の直後に、en は対応する `tmpl_active_delete_blocked`（1330 行）の直後に挿入する。文言は削除確認の意図が伝わる自然文とする（ja 例: 「このテンプレートを削除しますか？この操作は元に戻せません。」／en 例: 「Delete this template? This action cannot be undone.」）。既存 tmpl_ キーと同じインデント・引用スタイルに合わせる。

    実装時に既存の `askyesno` 呼び出し（例: 黒塗り/モザイク等の適用確認ダイアログ）のキー構成を確認し、タイトル＋本文の2キー構成が慣例ならそれに合わせる（本プランは上記のとおりタイトルへ既存 `confirm_title` を流用し本文のみ `tmpl_delete_confirm` を新設する構成としているが、既存慣例が別タイトルキーを持つ場合は同型に揃える。文言不整合を避けるための確認で executor 裁量で可）。
  </action>
  <verify>
    <automated>python -c "import ast,io; ast.parse(io.open('pagefolio/dialogs/llm_config/sections.py',encoding='utf-8').read())" && pytest tests/test_lang_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: sections.py 全体に `_save_settings` の残存参照が無い（`! grep -q "_save_settings" pagefolio/dialogs/llm_config/sections.py` が真＝終了コード0。`grep -c` は0マッチ時に終了コード1を返し `&&` 連結すると必ず失敗するため使わない。import 行と3呼び出しがすべて除去済みで、コメント文字列を含め本トークンがソースに一切現れない前提を維持する）
    - source assertion: `_on_template_delete` メソッド本文に `askyesno` と `tmpl_delete_confirm` が出現する
    - source assertion: `_on_template_save`/`_on_template_delete`/`_on_template_rename` の末尾に `_reload_template_combo` 呼び出しが残っている
    - behavior assertion: lang.py の ja/en 双方に `"tmpl_delete_confirm"` キーが存在する（`grep -c "tmpl_delete_confirm" pagefolio/lang.py` が2）
    - test-command: `pytest tests/test_lang_parity.py -q` が pass（ja/en キー数一致を維持）
    - CLI: sections.py の構文パースがエラーなく完了する
  </acceptance_criteria>
  <done>
    3 CRUD ハンドラから即時 _save_settings が消え（import も除去）、_on_template_delete に askyesno 削除確認が入り、lang.py に tmpl_delete_confirm が ja/en 一致で追加され、lang parity テストが pass する。
  </done>
</task>

<task type="auto">
  <name>Task 3: CR-02 回帰テスト（Cancel 非永続化・deepcopy 分離・delete 確認 No 中止）を test_provider_ui.py に追加する</name>
  <files>tests/test_provider_ui.py</files>
  <read_first>
    - tests/test_provider_ui.py（_make_apply_key_stub 1169-1201・_GetVarStub/_GetTextStub 1147-1166・TestTemplateSection 1746-1817・_read_llm_config_package_source 208 付近）
    - tests/test_ocr_fallback.py（headless スタブ + 実 bound method 呼び出しパターン・_make_dialog 18-48・TestConfirmationGate/TestSettingsIsolation の monkeypatch スタイル）
    - pagefolio/dialogs/llm_config/dialog.py（Task 1 修正後の __init__ deepcopy・_apply）
    - pagefolio/dialogs/llm_config/sections.py（Task 2 修正後の _on_template_delete askyesno）
  </read_first>
  <action>
    tests/test_provider_ui.py に CR-02 回帰を検証する新テストクラス `TestTemplateCancelContract`（V180-TMPL-01/03・CR-02）を追加する。全テストは既存の headless スタイル（Tk 生成なし・実 bound method 直接呼び出し）を踏襲し、test_ocr_fallback.py の `_make_dialog` と同型に `LLMConfigDialog.__new__(LLMConfigDialog)` でインスタンスを作り必要属性のみ手動設定する（`__init__`/`_build` は経由しない）。

    テスト1（deepcopy 分離・__init__ 経路）: 実際の `LLMConfigDialog.__init__` の分離ロジックを検証するため、`app_settings` に `prompt_templates`（active + items 2件）を含む dict を用意し、`LLMConfigDialog.__new__` インスタンスの `current_settings` に対して Task 1 と同じ分離手順（`dict(app_settings)` 後に `prompt_templates` を `copy.deepcopy`）を模した最小再現ではなく、可能なら `_apply` を経由せず「分離不変条件」そのものをアサートする。具体的には、分離後の current_settings の `prompt_templates` が入力 app_settings の `prompt_templates` と `is not` であり、内側の `items` および任意のテンプレート dict も `is not` であること、かつ current_settings 側の items を変更しても app_settings 側が不変であることを確認する。__init__ を Tk なしで直接呼べない場合は、`copy.deepcopy` を用いた分離が dialog.py の実コードと一致することを `_read_llm_config_package_source()` の source assertion（`import copy` と `copy.deepcopy` が __init__ 相当箇所に存在）で補強する。

    テスト2（Cancel 非永続化・保存/削除/リネームの一括確定）: `_make_apply_key_stub` を土台に、分離済み `current_settings`（`prompt_templates` の active + items）を設定した stub を作り、CRUD 相当の in-place 変更（settings.py の save_template/delete_template を stub.current_settings に対して直接呼ぶ）を行った後、`stub.on_apply` が呼ばれなければ（＝Cancel 相当・destroy のみ）外部に渡した `app_settings` 参照が一切変化しないことをアサートする（deepcopy 分離により current_settings への変更が app_settings へ伝播しない）。次に `LLMConfigDialog._apply(stub)` を呼ぶと（＝Apply 相当）、`captured` に prompt_templates が active + items 込みで一度だけ渡り、その items がテンプレート変更後の内容を反映することを確認する。

    テスト3（sections.py に _save_settings が残っていない・source assertion）: `_read_llm_config_package_source()`（sections.py を含むパッケージソース）に対し、テンプレート CRUD ハンドラ本文で即時永続化していないことを担保するため、sections.py 単体ソースを読み `_save_settings` が0件であることをアサートする（Task 2 の除去を回帰防止）。

    テスト4（_on_template_delete の askyesno No で中止）: `TestTemplateCancelContract` 内に、`LLMConfigDialog.__new__` インスタンスへ `_L=LANG["ja"]`、`_active_template_name`（アクティブでない名前を選択状態にするため active とは別の名前）、`current_settings`（items にその名前を含む）、`template_var`（`.get()` が削除対象名を返す set/get スタブ）、`_reload_template_combo`（呼ばれたか記録する差し替え or 実メソッド）を設定する。`pagefolio.dialogs.llm_config.sections.messagebox.askyesno` を False 返却へ monkeypatch し、`d._on_template_delete()` を呼ぶ。呼び出し後、対象テンプレートが `current_settings["prompt_templates"]["items"]` に残存している（delete_template が呼ばれていない＝早期 return した）ことをアサートする。続いて askyesno を True へ monkeypatch し再度呼ぶと、対象が items から消える（delete 実行）ことをアサートする。

    必要に応じて set/get 可能な軽量 var スタブ（`.get()`/`.set()`）を新設する（既存 `_GetVarStub` は get のみのため）。既存ヘルパー名と衝突しない名前（例: `_SetGetVarStub`）を用いる。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py::TestTemplateCancelContract -q</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `pytest tests/test_provider_ui.py::TestTemplateCancelContract -q` が全 pass
    - behavior assertion: deepcopy 分離テストが「current_settings の prompt_templates/items/テンプレート dict が入力 settings と別オブジェクト」かつ「片方の変更が他方へ伝播しない」を実際にアサートしている
    - behavior assertion: Cancel テストが on_apply 非呼出時に app_settings 参照が不変であることをアサートしている
    - behavior assertion: Apply テストが `LLMConfigDialog._apply` の実 bound method 呼び出しで prompt_templates が一度だけ収集されることをアサートしている
    - behavior assertion: delete 確認テストが askyesno=False で items 残存・askyesno=True で items 削除を両方アサートしている
    - test-command: `pytest tests/test_provider_ui.py -q` が回帰なく pass
    - test-command: `pytest tests/test_provider_ui.py tests/test_prompt_templates.py -q` が全件 pass する。Task 2 の即時 `_save_settings` 除去は「CRUD 直後の即時永続化」→「Apply 経由の一括確定」という挙動変更であり、既存 `tests/test_provider_ui.py::TestTemplateSection`（特に `test_apply_collects_active_template_preserving_items`）が「CRUD 直後に settings が永続化されている」前提を持つ場合に破綻し得る影響圏にある。破綻した場合はテスト側を新契約（Apply 一括確定）に合わせて更新し、修正したケース名と理由を 02-05-SUMMARY.md の Deviations に記録する（挙動を再考するのではなくテスト契約の更新を既定とする）
  </acceptance_criteria>
  <done>
    TestTemplateCancelContract が deepcopy 分離・Cancel 非永続化・Apply 一括確定・delete askyesno No 中止をすべて headless bound-method テストで実証し、フルの test_provider_ui.py が pass する。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー UI 操作（テンプレート CRUD）→ app.settings / pagefolio_settings.json | ユーザーの明示承認（Apply）を経ずに、永続層（メモリ設定 + ディスク）への書き込みが起こり得る境界。CR-02 はこの境界で「Cancel でも確定してしまう」欠陥 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Repudiation | _on_template_save/_delete/_rename → _save_settings（即時永続化） | high | mitigate | __init__ で prompt_templates をディープコピー分離（Task 1）+ 3ハンドラから即時 _save_settings 除去（Task 2）→ _apply 一括確定に一本化。Cancel（destroy のみ）で変更が破棄され、承認なし確定を排除 |
| T-02-02 | Tampering | _on_template_delete（アクティブでないテンプレートの誤削除） | medium | mitigate | 削除前に messagebox.askyesno 確認を追加（Task 2）。No 応答で delete_template 非呼出・早期 return |
| T-02-03 | Tampering | _apply の prompt_templates items 収集がエイリアス参照になり app.settings と内部共有 | low | mitigate | _apply で copy.deepcopy(items) して app.settings へ完全独立構造を渡す（Task 1）|

パッケージ導入（npm/pip/cargo）: 本ギャップに新規パッケージ導入なし（標準ライブラリ `copy` のみ使用）。サプライチェーン脅威 T-02-SC は非該当。
</threat_model>

<verification>
- `pytest tests/test_provider_ui.py tests/test_lang_parity.py -q` が全 pass
- `pytest -q` がフルスイート回帰なし
- `ruff check . && ruff format --check .` がクリーン
- `! grep -q "_save_settings" pagefolio/dialogs/llm_config/sections.py` が真（終了コード0・残存参照なし。`grep -c` は0マッチで終了コード1のため使わない）
- `grep -c "copy.deepcopy" pagefolio/dialogs/llm_config/dialog.py` が2以上（__init__ + _apply）
- `grep -c "tmpl_delete_confirm" pagefolio/lang.py` が2（ja/en 一致）
</verification>

<success_criteria>
- CR-02 が解消: テンプレート保存/削除/リネームが Cancel で取り消せる（app.settings/ディスク非変更）・Apply で一括確定される
- 02-VERIFICATION.md gaps（truth 1・SC1）の failed 原因（浅いコピー + 即時 _save_settings）が両方除去されている
- 誤削除に askyesno 確認が入る
- lang.py ja/en キー一致維持・フルスイート回帰なし・ruff クリーン
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**変更（既存シンボルの挙動修正・新規シンボルなし）:**
- `pagefolio/dialogs/llm_config/dialog.py` — `__init__`（prompt_templates ディープコピー分離）・`_apply`（items ディープコピー収集 + コメント整合）。新規 import: `copy`
- `pagefolio/dialogs/llm_config/sections.py` — `_on_template_save`/`_on_template_delete`/`_on_template_rename`（即時 `_save_settings` 除去）・`_on_template_delete`（`messagebox.askyesno` 削除確認追加）。`_save_settings` import 削除

**新規シンボル:**
- `pagefolio/lang.py` — LANG["ja"]/LANG["en"] に `tmpl_delete_confirm` キー
- `tests/test_provider_ui.py` — テストクラス `TestTemplateCancelContract`（deepcopy 分離・Cancel 非永続化・Apply 一括確定・delete askyesno 中止）。必要に応じ軽量スタブ `_SetGetVarStub`
</artifacts_produced>

<output>
Create `.planning/phases/02-ai/02-05-SUMMARY.md` when done
</output>
