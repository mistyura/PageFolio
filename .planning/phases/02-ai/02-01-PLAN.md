---
phase: 02-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/settings.py
  - pagefolio/ocr_fallback.py
  - tests/test_prompt_templates.py
  - tests/test_ocr_fallback.py
autonomous: true
requirements:
  - V180-TMPL-01
  - V180-TMPL-03
  - V180-TMPL-04
  - V180-TMPL-05
  - V180-FALL-01
  - V180-FALL-03
must_haves:
  truths:
    - "テンプレート CRUD 純関数（save/delete/rename/list/get）が settings 辞書上で名前付きペア（custom_prompt+summary_prompt）を保存・列挙・削除・改名できる（D-01/D-02）"
    - "アクティブテンプレートの削除は ValueError で拒否される（D-03 の防御的実装・UI 側の無効化と二重防御）"
    - "テンプレート名の空文字・重複が保存/改名時に純関数で検出される（D-04）"
    - "load_custom_prompt / load_summary_prompt が『外部mdファイル > アクティブテンプレート > 設定欄直接値』の3段順で解決する（V180-TMPL-04/V180-TMPL-05）"
    - "next_fallback_candidate が chain から tried を除いた最初の候補を返し、空チェーン/全試行済みで None を返す（V180-FALL-01・D-10）"
    - "サマリ用フォールバック候補選択が text プロンプト非対応プロバイダ（tesseract）を除外する（D-12・Open Question 2）"
  artifacts:
    - pagefolio/ocr_fallback.py
    - tests/test_prompt_templates.py
    - tests/test_ocr_fallback.py
  key_links:
    - "settings._load_settings の defaults へ prompt_templates / ocr_fallback_enabled / ocr_fallback_chain を追加し setdefault ループで既存 pagefolio_settings.json へ後方互換注入される"
    - "load_custom_prompt / load_summary_prompt は全プロバイダ共通の解決経路のため、この3段化が全プロバイダへ横断適用される（V180-TMPL-05）"
  prohibitions:
    - statement: "テンプレート辞書・フォールバックチェーンのキー名が _SENSITIVE_KEYS パターン（*_api_key 等）と衝突してはならない（機密キーガード汚染防止）"
      status: resolved
      verification: "tests/test_prompt_templates.py で prompt_templates/ocr_fallback_enabled/ocr_fallback_chain が _SENSITIVE_KEYS に含まれないことを assert する"
---

<objective>
テンプレート管理とプロバイダーフォールバックの**純ロジック基盤層**を先に確立する。Tk/fitz を一切 import しない層に限定し、後続 UI/オーケストレーションプラン（02-02〜02-04）が乗る土台を作る。

具体的には (1) `settings.py` へテンプレート CRUD 純関数群と `prompt_templates`/`ocr_fallback_*` デフォルト値を追加（D-01/D-02/D-03/D-04）、(2) `load_custom_prompt`/`load_summary_prompt` を「外部ファイル > アクティブテンプレート > 設定欄」の3段解決へ拡張（V180-TMPL-04/V180-TMPL-05）、(3) 新規 `ocr_fallback.py` に次候補選択の純関数を追加（V180-FALL-01/V180-FALL-03・D-10・D-12）する。

Purpose: フェーズ最大の技術的難所（フォールバックのオーケストレーション）と UI 層を、テスト可能な純ロジックの上に構築できるようにする。Nyquist Rule に従い MISSING テスト2本（`test_prompt_templates.py`/`test_ocr_fallback.py`）を本プランで先行作成する（Wave 0 gap 解消）。
Output: `settings.py`（拡張）・`ocr_fallback.py`（新規）・純ロジックテスト2本。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-CONTEXT.md
@.planning/phases/02-ai/02-RESEARCH.md
@.planning/phases/02-ai/02-PATTERNS.md
@pagefolio/settings.py
@pagefolio/ocr.py
@pagefolio/ocr_pipeline.py
</context>

## Artifacts this phase produces (Plan 02-01)

新規シンボル（drift 検証で新規作成として扱うこと）:
- `pagefolio/ocr_fallback.py`（新規モジュール）: 関数 `next_fallback_candidate(chain, tried)`、`next_summary_candidate(chain, tried, text_capable)`
- `pagefolio/settings.py`（拡張）: 関数 `list_template_names(settings)`、`get_template(settings, name)`、`save_template(settings, name, custom_prompt, summary_prompt)`、`delete_template(settings, name)`、`rename_template(settings, old_name, new_name)`、`template_name_exists(settings, name)`
- `pagefolio/settings.py` の `_load_settings` defaults 新規キー: `prompt_templates`（`{"active": str, "items": {}}`）、`ocr_fallback_enabled`（bool）、`ocr_fallback_chain`（list[str]）
- `pagefolio/settings.py` の既存 `load_custom_prompt`/`load_summary_prompt` に「アクティブテンプレート」中間段を追加（シグネチャ不変・戻り値の解決元のみ拡張）
- 新規テスト: `tests/test_prompt_templates.py`（`TestSaveTemplate`/`TestListAndSelect`/`TestDeleteRename`/`TestExternalFileSync`）、`tests/test_ocr_fallback.py`（`TestDisabledByDefault`/`TestNextCandidate`/`TestSummaryCandidateFilter`）

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: settings.py テンプレート CRUD 純関数 + デフォルト値追加、test_prompt_templates.py を先行作成</name>
  <files>pagefolio/settings.py, tests/test_prompt_templates.py</files>
  <read_first>
    - pagefolio/settings.py（`_load_settings` の defaults 辞書・`save_prompt_file`/`load_custom_prompt` の関数型スタイル・`_save_settings` の `_SENSITIVE_KEYS` ガード・裸 except 禁止規約を確認）
    - tests/test_settings_keyguard.py（純ロジックテストの記法・クラス命名・`_load_settings`/`_save_settings` の使い方の踏襲元）
    - .planning/phases/02-ai/02-RESEARCH.md（Pattern 1「テンプレート永続化スキーマ」・Architectural Responsibility Map の「テンプレート名重複チェック・削除可否判定＝Pure Logic Layer」）
    - .planning/phases/02-ai/02-PATTERNS.md（settings.py セクションの関数型 CRUD ヘルパー・setdefault マイグレーション・機密キー除外ガードの写経元）
  </read_first>
  <behavior>
    - `_load_settings()` は既存 JSON に `prompt_templates`/`ocr_fallback_enabled`/`ocr_fallback_chain` が無くても setdefault で `{"active": "", "items": {}}` / `False` / `[]` を注入する
    - `save_template(settings, "A", "c1", "s1")` 後、`get_template(settings, "A")` は `{"custom_prompt": "c1", "summary_prompt": "s1"}` を返し、`list_template_names(settings)` は `"A"` を含む（sorted）
    - `template_name_exists(settings, "A")` は True、未登録名は False、空文字・空白のみ名は「無効」として弾ける純判定を提供する（D-04）
    - `delete_template(settings, name)` は items から除去する。ただし name が `prompt_templates["active"]` と一致する場合は ValueError を送出する（D-03 の防御的実装）
    - `rename_template(settings, old, new)` は old を new へ改名し、new が既存なら ValueError（D-04）。old が active だった場合 active も new へ更新する
    - `prompt_templates`/`ocr_fallback_enabled`/`ocr_fallback_chain` はいずれも `_SENSITIVE_KEYS` に含まれない（機密キーガードを汚染しない）
  </behavior>
  <action>
    `pagefolio/settings.py` の `_load_settings()` の `defaults` 辞書末尾（現行 `thumb_page_size` の後）に3キーを追加する: `prompt_templates` = `{"active": "", "items": {}}`（active は空文字＝テンプレート未選択で従来どおり設定欄直接編集と等価・D-02）、`ocr_fallback_enabled` = `False`（V180-FALL-01 安全側既定）、`ocr_fallback_chain` = `[]`。既存の `for k, v in defaults.items(): data.setdefault(k, v)` ループがそのまま後方互換マイグレーションを担うため追加コードは不要。

    同ファイルに関数型 CRUD ヘルパーを追加する（すべて `settings` 辞書を第1引数に取り、`_save_settings()` は呼び出し側の責務＝settings.py 内で自動保存しない。既存 `save_prompt_file`/`load_custom_prompt` の責務分離を踏襲・D-02）: `list_template_names(settings)`（`prompt_templates.items` のキーを sorted で返す）、`get_template(settings, name)`（items[name] を返す。無ければ None）、`template_name_exists(settings, name)`（正規化後の名前が items に存在するか）、`save_template(settings, name, custom_prompt, summary_prompt)`（`name.strip()` が空なら ValueError、`items[name] = {"custom_prompt": custom_prompt, "summary_prompt": summary_prompt}` で保存＝新規/更新共通・D-01 ペア保存）、`delete_template(settings, name)`（name が `prompt_templates["active"]` と一致するなら ValueError を送出＝D-03 アクティブ削除禁止の防御的実装、それ以外は `items.pop(name, None)`）、`rename_template(settings, old_name, new_name)`（new_name が空なら ValueError、new_name が既存なら ValueError＝D-04 重複拒否、`items[new] = items.pop(old)` で改名し old が active なら active も new へ更新）。`prompt_templates` キーが未初期化の settings でも安全に動くよう各関数冒頭で `settings.setdefault("prompt_templates", {"active": "", "items": {}})` を行う。裸 except は使わず、CRUD は例外を握りつぶさず ValueError を明示送出する。

    `tests/test_prompt_templates.py` を新規作成する（`test_settings_keyguard.py` の記法・sys.path 挿入・クラス命名を踏襲）。クラス `TestSaveTemplate`（save→get 往復・重複名 save は上書き・空名 ValueError）、`TestListAndSelect`（list_template_names の sorted・active 切替が settings に反映）、`TestDeleteRename`（delete で消える・active 削除は ValueError＝D-03・rename 正常系・rename 先重複は ValueError＝D-04・active の rename 追従）、`TestExternalFileSync`（後続 Task 2 で埋めるため空メソッド `pass` かスキップ可、または雛形コメントのみ）を用意する。さらに `prompt_templates`/`ocr_fallback_enabled`/`ocr_fallback_chain` が `_SENSITIVE_KEYS` に含まれないことを assert するテスト1件を含める（prohibition 検証）。
  </action>
  <verify>
    <automated>pytest tests/test_prompt_templates.py::TestSaveTemplate tests/test_prompt_templates.py::TestListAndSelect tests/test_prompt_templates.py::TestDeleteRename -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_prompt_templates.py::TestSaveTemplate tests/test_prompt_templates.py::TestListAndSelect tests/test_prompt_templates.py::TestDeleteRename -x` が全て pass する
    - `python -c "from pagefolio.settings import _load_settings; d=_load_settings(); assert 'prompt_templates' in d and d['prompt_templates']=={'active':'','items':{}}; assert d['ocr_fallback_enabled'] is False and d['ocr_fallback_chain']==[]"` が例外なく終了する
    - `pytest tests/test_prompt_templates.py::TestDeleteRename -x` が pass（active テンプレートへの delete_template が ValueError を送出する検証を含む）
    - `python -c "from pagefolio.settings import _SENSITIVE_KEYS; assert 'prompt_templates' not in _SENSITIVE_KEYS and 'ocr_fallback_chain' not in _SENSITIVE_KEYS and 'ocr_fallback_enabled' not in _SENSITIVE_KEYS"` が成功する
    - `ruff check pagefolio/settings.py tests/test_prompt_templates.py && ruff format --check pagefolio/settings.py tests/test_prompt_templates.py` がクリーン
  </acceptance_criteria>
  <done>テンプレート CRUD 純関数と3デフォルトキーが settings.py に存在し、test_prompt_templates.py の CRUD 3クラスと機密キー非汚染テストがグリーン。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: load_custom_prompt / load_summary_prompt をアクティブテンプレート段を含む3段解決へ拡張</name>
  <files>pagefolio/settings.py, tests/test_prompt_templates.py</files>
  <read_first>
    - pagefolio/settings.py（現行 `load_custom_prompt`/`load_summary_prompt` の2段実装・`load_prompt_file`/`prompt_file_exists`・`CUSTOM_PROMPT_FILE`/`SUMMARY_PROMPT_FILE` import）
    - pagefolio/ocr.py（`resolve_ocr_prompt`/`resolve_summary_prompt` は**変更禁止**であること・custom_prompt 引数の中身だけがテンプレート由来になる点を確認）
    - .planning/phases/02-ai/02-RESEARCH.md（Pattern 4「resolve_ocr_prompt へのテンプレート層挿入」・Anti-Patterns の「resolve_ocr_prompt のシグネチャ変更禁止」）
  </read_first>
  <behavior>
    - 外部 md ファイルが存在し非空なら、その内容が最優先で返る（従来どおり）
    - 外部ファイルが無い/空で、`prompt_templates["active"]` が非空かつ該当テンプレートに custom_prompt があれば、そのテンプレート値が返る
    - 外部ファイルも無く active も未設定なら、`settings["ocr_custom_prompt"]`（設定欄直接値）が返る（従来どおり）
    - `load_summary_prompt` も同型（summary_prompt を対象に3段解決）
    - `resolve_ocr_prompt`/`resolve_summary_prompt`（ocr.py）のシグネチャは一切変更しない
  </behavior>
  <action>
    `load_custom_prompt(settings)` を「外部ファイル > アクティブテンプレート > 設定欄」の3段解決へ拡張する（V180-TMPL-04/V180-TMPL-05・D-02）。手順: `load_prompt_file(CUSTOM_PROMPT_FILE)` が非空ならそれを返す（従来の最優先を維持）。無ければ `settings.get("prompt_templates", {})` の active 名を引き、`items[active]["custom_prompt"]` が非空ならそれを返す。いずれも該当しなければ従来どおり `settings.get("ocr_custom_prompt", "")` を返す。`load_summary_prompt(settings)` を同型に拡張（`SUMMARY_PROMPT_FILE` と `summary_prompt`/`ocr_summary_prompt` を対象）。`pagefolio/ocr.py` の `resolve_ocr_prompt`/`resolve_summary_prompt` は無改造で流用する（テンプレート層は custom_prompt の**解決元**を変えるだけ・シグネチャ変更は `test_provider_ui.py` を全滅させるため厳禁）。

    `tests/test_prompt_templates.py` の `TestExternalFileSync` を実装する。`monkeypatch` で `pagefolio.settings._get_base_dir` を `tmp_path` へ差し替え、(a) 外部ファイルあり→ファイル内容優先、(b) 外部ファイルなし+active テンプレートあり→テンプレート値、(c) 外部ファイルなし+active なし→`ocr_custom_prompt` 設定欄値、の3系統を custom と summary の両方について検証する。
  </action>
  <verify>
    <automated>pytest tests/test_prompt_templates.py::TestExternalFileSync tests/test_provider_ui.py -k resolve_prompt -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_prompt_templates.py::TestExternalFileSync -x` が pass（3段順の a/b/c が custom・summary 両方で検証される）
    - `pytest tests/test_provider_ui.py -k resolve_prompt -x` が pass（既存の resolve テストが回帰しない）
    - `python -c "import inspect; from pagefolio.ocr import resolve_ocr_prompt, resolve_summary_prompt; assert str(inspect.signature(resolve_ocr_prompt))=='(preset, provider_name, custom_prompt=\'\')'; assert str(inspect.signature(resolve_summary_prompt))=='(provider_name, custom_prompt=\'\')'"` が成功（シグネチャ不変）
    - `ruff check pagefolio/settings.py tests/test_prompt_templates.py && ruff format --check pagefolio/settings.py tests/test_prompt_templates.py` がクリーン
  </acceptance_criteria>
  <done>load_custom_prompt/load_summary_prompt が3段解決になり、TestExternalFileSync と既存 resolve テストがグリーン。ocr.py の解決関数シグネチャは不変。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ocr_fallback.py 新規（次候補選択の純関数）+ test_ocr_fallback.py を先行作成</name>
  <files>pagefolio/ocr_fallback.py, tests/test_ocr_fallback.py</files>
  <read_first>
    - pagefolio/ocr_pipeline.py（Tk/fitz を一切 import しない純ロジック層の作法・docstring スタイル・`PipelineState` の「オーケストレーションを持たせず状態は明示引数/戻り値でやり取り」設計）
    - tests/test_ocr_pipeline.py（純ロジック層テストの記法・sys.path 挿入・クラス命名の踏襲元）
    - .planning/phases/02-ai/02-RESEARCH.md（Pattern 6 の `next_fallback_candidate` コード例・Open Question 2 の「サマリ経路のみ supports_text_prompt フィルタを適用」推奨）
    - .planning/phases/02-ai/02-CONTEXT.md（D-10 連鎖は最後まで辿る・D-12 サマリにも同じ順を適用）
  </read_first>
  <behavior>
    - `next_fallback_candidate(chain, tried)`: chain（プロバイダ名リスト）の先頭から tried（set）に無い最初の名前を返す。全て試行済み or chain 空なら None（D-10）
    - `next_summary_candidate(chain, tried, text_capable)`: 上記に加え、text_capable（text プロンプト対応プロバイダ名の集合）に含まれない候補（例: tesseract）をスキップして次を探す（D-12・Open Question 2）
    - どちらも純関数: 引数を破壊的変更せず、fitz/tkinter を import しない
  </behavior>
  <action>
    `pagefolio/ocr_fallback.py` を新規作成する（`ocr_pipeline.py`/`pagination.py` と同格の Tk/fitz 非依存純ロジック層・ライセンスヘッダ + module docstring を既存純ロジック層に合わせる）。`next_fallback_candidate(chain, tried)` を実装: `for name in chain: if name not in tried: return name` / else `None`（D-10: 連鎖は最後まで辿る・1回限りに制限しない）。`next_summary_candidate(chain, tried, text_capable)` を実装: chain を走査し、tried に無くかつ `name in text_capable` を満たす最初の候補を返す（tesseract 等の text 非対応をスキップ・D-12）。引数は破壊的変更しない。標準ライブラリ以外・pagefolio 内部 UI/fitz モジュールを import しない。

    `tests/test_ocr_fallback.py` を新規作成する（`test_ocr_pipeline.py` の記法踏襲）。クラス `TestDisabledByDefault`（chain=[] で next_fallback_candidate が None＝V180-FALL-01 の安全側既定に対応する純ロジック部）、`TestNextCandidate`（tried による除外・chain 順の走査・全試行済みで None）、`TestSummaryCandidateFilter`（text_capable に無い候補=tesseract をスキップし次を返す・全て非対応なら None＝D-12）。`TestConfirmationGate`/`TestSettingsIsolation` は 02-04（オーケストレーション）で追加するため本タスクでは作らない。
  </action>
  <verify>
    <automated>pytest tests/test_ocr_fallback.py::TestDisabledByDefault tests/test_ocr_fallback.py::TestNextCandidate tests/test_ocr_fallback.py::TestSummaryCandidateFilter -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ocr_fallback.py::TestDisabledByDefault tests/test_ocr_fallback.py::TestNextCandidate tests/test_ocr_fallback.py::TestSummaryCandidateFilter -x` が全て pass
    - `python -c "from pagefolio.ocr_fallback import next_fallback_candidate, next_summary_candidate; assert next_fallback_candidate([], set()) is None; assert next_fallback_candidate(['claude','gemini'], {'claude'})=='gemini'; assert next_summary_candidate(['tesseract','claude'], set(), {'claude','gemini'})=='claude'"` が成功
    - `grep -v '^#' pagefolio/ocr_fallback.py | grep -c -E 'import (tkinter|fitz)'` が 0（純ロジック層の独立性）
    - `ruff check pagefolio/ocr_fallback.py tests/test_ocr_fallback.py && ruff format --check pagefolio/ocr_fallback.py tests/test_ocr_fallback.py` がクリーン
  </acceptance_criteria>
  <done>ocr_fallback.py が Tk/fitz 非依存で2純関数を提供し、test_ocr_fallback.py の3純ロジッククラスがグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/実行フロー → pagefolio_settings.json | テンプレート辞書・フォールバックチェーンが永続設定ファイルへ書き込まれる（本プランは書き込み対象データの構造を定義する層） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | settings.py 新規キー（prompt_templates/ocr_fallback_*） | low | mitigate | 新規キー名が `_SENSITIVE_KEYS` パターン（`*_api_key` 等）と衝突しないことを test_prompt_templates.py で assert。`_save_settings` の機密キー除外ガードは無改造で維持 |
| T-02-02 | Information Disclosure | テンプレート内容の平文保存 | low | accept | テンプレート内容（プロンプト文）は非機密前提。APIキーは既存 `_SENSITIVE_KEYS`/セッションメモリ方式で別管理され本プランで変更しない |
| T-02-03 | Tampering | テンプレート名の空文字・重複（Input Validation V5/ASVS L1） | low | mitigate | `save_template`/`rename_template` が空名・重複を ValueError で拒否する純関数バリデーション（D-04） |
</threat_model>

<verification>
- `pytest tests/test_prompt_templates.py tests/test_ocr_fallback.py -x`（本プラン新規テスト全件）
- `pytest`（フルスイート・回帰なし確認）
- `ruff check . && ruff format --check .`
</verification>

<success_criteria>
- テンプレート CRUD 純関数群（save/delete/rename/list/get/exists）が settings.py に存在し、D-01/D-02/D-03/D-04 の挙動を満たす
- `prompt_templates`/`ocr_fallback_enabled`/`ocr_fallback_chain` の3デフォルトキーが setdefault マイグレーションで既存 JSON へ後方互換注入される
- load_custom_prompt/load_summary_prompt が3段解決（外部ファイル>テンプレート>設定欄）になり、ocr.py の resolve 関数シグネチャは不変
- ocr_fallback.py が Tk/fitz 非依存で next_fallback_candidate/next_summary_candidate を提供
- 新規テスト2本 + フルスイートがグリーン、ruff クリーン
</success_criteria>

<output>
Create `.planning/phases/02-ai/02-01-SUMMARY.md` when done
</output>
