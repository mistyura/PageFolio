---
phase: 02-ai
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-03
files_modified:
  - pagefolio/ocr_dialog.py
  - pagefolio/lang.py
  - tests/test_ocr_fallback.py
autonomous: true
requirements:
  - V180-FALL-02
  - V180-FALL-03
must_haves:
  truths:
    - "OCR が fatal エラー（connection/timeout/circuit_breaker/api_key_missing）で停止すると _propose_fallback が呼ばれ、フォールバックが有効かつ次候補がある場合のみ提案する（V180-FALL-02・D-11）"
    - "フォールバック提案は毎回 messagebox.askyesno による送信先確認ダイアログ再提示を伴い、理由（reason）と切替先候補を明示する（D-10/D-11・自動送信なし）"
    - "ユーザー承認なしに別ベンダーへ自動送信されない（拒否/チェーン終端なら現状のエラー表示のみ）"
    - "フォールバック切替は self.app.settings を一切書き換えず、ダイアログローカルな _active_ocr_settings 上でのみプロバイダを差し替える（Pitfall 4・永続設定汚染防止）"
    - "フォールバック候補ごとに build_provider を独立呼び出しし、max_concurrency の再クランプ・APIキー解決・レート制限設定をプロバイダ単位で再評価する（V180-FALL-03・Pitfall 3）"
    - "APIキー未設定の候補は静かに握りつぶさず api_key_missing 理由を確認ダイアログに明示してから次候補へ進む（D-11/D-14）"
    - "フォールバック切替後は _pending_pages() の未処理ページのみ再実行する（D-09・成功済み結果は保持）"
    - "サマリ生成失敗（_on_summary_error）にも同じフォールバック連鎖を適用し、text 非対応候補は next_summary_candidate で除外する（D-12・Open Question 2）"
    - "フォールバック再開時、_on_run はプロバイダ再構築（build_provider を含む）の全設定参照をローカルスナップショット s（=settings/fb）から行い self.app.settings を読まないため、切替先候補プロバイダで実際に再実行される（レビュー HIGH 対応・_on_run 内 build_provider の app.settings 直参照残りを解消）"
    - "サマリ経路のフォールバック再開は _on_summary(settings=fb) でローカルスナップショットを引き継ぎ、_confirm_summary_cost/_check_cloud_api_key も settings 引数で一般化される（レビュー MEDIUM 対応・D-12）"
    - "フォールバック候補が実行不可（tesseract 未インストール／クラウドキー未解決）の場合、_validate_provider_readiness が build 前に検出し、理由を明示して次候補へ進む（静かな握りつぶし禁止・レビュー LOW 対応・D-11/D-14）"
  artifacts:
    - "pagefolio/ocr_dialog.py（_propose_fallback / _switch_to_fallback_provider / _active_ocr_settings）"
  key_links:
    - "_finish_error / _on_summary_error 末尾 → _propose_fallback フック（D-11/D-12）"
    - "_propose_fallback → ocr_fallback.next_fallback_candidate/next_summary_candidate（02-01）→ askyesno 再提示 → _switch_to_fallback_provider → build_provider（02-01 の ocr_fallback は純選択のみ）"
    - "_switch_to_fallback_provider → _validate_provider_readiness（tesseract 未インストール／クラウドキー未解決を build 前に検証）→ build_provider(fb) → OCR は _on_run(resume=True, settings=fb) / サマリは _on_summary(settings=fb)（レビュー HIGH/MEDIUM/LOW を貫通させる実行経路）"
  prohibitions:
    - statement: "フォールバック確認ダイアログに『今後表示しない』オプションを追加してはならない（毎回確認の同意方針を迂回する）"
      status: resolved
      verification: "tests/test_ocr_fallback.py::TestConfirmationGate が各連鎖段で askyesno が呼ばれることをアサート（呼び出し省略経路が無いこと）"
    - statement: "フォールバック中に self.app.settings['ocr_provider'] 等を書き換えてはならない（一時プロバイダ選択の永続化事故）"
      status: resolved
      verification: "tests/test_ocr_fallback.py::TestSettingsIsolation が _switch_to_fallback_provider 後も self.app.settings が不変であることをアサート"
---

<objective>
プロバイダーフォールバックの**実行時オーケストレーション**を `OCRDialog`（`pagefolio/ocr_dialog.py`）に実装する。02-01 の `ocr_fallback.py`（純選択）と 02-03 の設定 UI（`ocr_fallback_enabled`/`ocr_fallback_chain`）の上に、送信先確認ダイアログ再提示つきの手動フォールバックを構築する。

対象: V180-FALL-02（fatal 停止時に確認ダイアログ再提示つきで次候補を提案・自動送信なし）・V180-FALL-03（切替時に並列度・APIキー解決・レート制限をプロバイダ単位で再評価）。ロック済み決定 D-09/D-10/D-11/D-12 を実装し、RESEARCH の Pitfall 3（設定引き継ぎ）と Pitfall 4（`self.app.settings` 汚染）を回避する。

これはフェーズ最大の技術的難所。既存 `_is_cloud_provider`/`_confirm_cost`/`_check_cloud_api_key` が `self.app.settings.get("ocr_provider")` を直接参照する単一プロバイダ前提を、ダイアログローカルな設定スナップショット（`_active_ocr_settings`）を受け取れる形へ一般化することが核心（RESEARCH A3）。

Purpose: 「明示設定型・自動送信なし」方針（PROJECT.md 確定）を厳守した上で、プロバイダ障害から手動確認つきで処理を継続できるようにする。
Output: `ocr_dialog.py`（オーケストレーションメソッド + ヘルパー一般化 + フック）・`lang.py`（ja/en 確認文言）・`test_ocr_fallback.py`（スタブテスト追加）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-CONTEXT.md
@.planning/phases/02-ai/02-RESEARCH.md
@.planning/phases/02-ai/02-PATTERNS.md
@.planning/phases/02-ai/02-01-SUMMARY.md
@.planning/phases/02-ai/02-03-SUMMARY.md
@pagefolio/ocr_dialog.py
@pagefolio/ocr_fallback.py
@pagefolio/ocr.py
</context>

## Artifacts this phase produces (Plan 02-04)

新規シンボル（drift 検証で新規作成として扱うこと）:
- `ocr_dialog.py`（OCRDialog 拡張）: 属性 `self._active_ocr_settings`（ダイアログローカル設定スナップショット）、`self._fallback_tried`（set・試行済みプロバイダ）、`self._fallback_resume`（bool・再開中のコスト確認二重表示抑止フラグ）、メソッド `_propose_fallback(kind, msg, summary=False)`、`_switch_to_fallback_provider(candidate, summary=False)`、`_validate_provider_readiness(candidate, settings)`、`_active_provider_name()`
- `ocr_dialog.py`（既存メソッドのシグネチャ一般化・後方互換デフォルト維持）: `_is_cloud_provider(self, settings=None)`、`_confirm_cost(self, page_count=None, settings=None)`、`_check_cloud_api_key(self, settings=None)`、`_confirm_summary_cost(self, char_count, settings=None)`、`_on_run(self, resume=False, settings=None)`、`_on_summary(self, settings=None)`（レビュー HIGH/MEDIUM 対応でサマリ経路も settings 引数化）
- `ocr_dialog.py`（フック挿入）: `_finish_error` 末尾から `_propose_fallback(kind, msg)`（OCR 経路）、`_on_summary_error` 末尾から `_propose_fallback(kind or "generic", msg, summary=True)`（サマリ経路）
- `lang.py` 新規キー（ja/en 両方）: `fallback_confirm_title`、`fallback_confirm_msg`、`fallback_reason_connection`、`fallback_reason_timeout`、`fallback_reason_circuit_breaker`、`fallback_reason_api_key_missing`、`fallback_reason_provider_unavailable`（tesseract 未インストール等・レビュー LOW）、`fallback_reason_generic`、`fallback_exhausted`
- `tests/test_ocr_fallback.py`（拡張）: `TestConfirmationGate`、`TestSettingsIsolation`、`TestSummaryFallback`（レビュー MEDIUM）、`TestProviderReadiness`（レビュー LOW）

## Review Feedback Incorporation（02-REVIEWS.md / Antigravity）

本プランは Phase 2 のクロスAIレビュー（`02-REVIEWS.md`）の全 actionable 発見を反映する。レビュー3件はいずれも本プラン（02-04・フォールバック実行時オーケストレーション）を対象とし、02-01/02-02/02-03（純ロジック層・テンプレート UI・フォールバック設定 UI）はレビュー発見の対象外のため変更しない。

| 発見 | 重大度 | 反映先 | 反映内容 |
|------|--------|--------|----------|
| `_on_run` 再開時の `self.app.settings` 参照残りでフォールバック先がリセットされる（`build_provider(self.app.settings, ...)` 4箇所 + `name` 取得が元プロバイダを読み直す） | HIGH | Task 1 `<action>` / `<acceptance_criteria>` / must_haves | `_on_run` 内の全 `self.app.settings` 参照（現 1362 の `name` + 現 1417/1429/1450/1470 の `build_provider`）を `s = settings if settings is not None else self.app.settings` の `s` へ置換することを**明示タスク指示化**。回帰防止に `inspect.getsource(_on_run).count("self.app.settings")==1`（スナップショットフォールバック行のみ）と、承認後 build_provider が `ocr_provider==candidate` の fb で呼ばれる挙動テストを追加 |
| サマリ再開（`_on_summary_error`→再試行）のオーケストレーション記述不足。`_on_summary` は引数なしシグネチャで fb を引き継げない | MEDIUM | Task 1 `<action>`（`_on_summary(self, settings=None)`/`_confirm_summary_cost(self, char_count, settings=None)` 一般化）/ Task 2 `<action>`（`_switch_to_fallback_provider` サマリ経路が `_on_summary(settings=fb)` を呼ぶ・`_propose_fallback(..., summary=True)`）| サマリ経路も OCR 経路と対称に settings 引数化し、fb スナップショットを `_summary_worker` まで引き継ぐ。`TestSummaryFallback` で検証 |
| Tesseract 未インストール環境でフォールバック候補に tesseract が選ばれた際の例外遷移が未検証 | LOW | Task 2 `<action>`（`_validate_provider_readiness` 追加）/ threat_model T-02-14 / lang `fallback_reason_provider_unavailable` | build 前に `_detect_tesseract()[0]` とクラウドキー解決を検証し、不可なら `provider_unavailable`/`api_key_missing` 理由で次候補へ進む（reviewer 提案の `_validate_provider_readiness` 一般化を採用）。`TestProviderReadiness` で検証 |

いずれも新規発見でなく既存レビューの反映であり、却下・延期した actionable 発見はない。

<tasks>

<task type="auto">
  <name>Task 1: ダイアログローカル設定スナップショット導入とクラウド判定ヘルパーの一般化（Pitfall 4 回避）</name>
  <files>pagefolio/ocr_dialog.py, tests/test_ocr_fallback.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（`__init__` 付近の属性初期化・`_is_cloud_provider`(876)・`_confirm_cost`(1171)・`_confirm_summary_cost`(1214)・`_check_cloud_api_key`(1244)・`_on_run`(1280)・`_on_summary`(1947) の現行実装で `self.app.settings.get("ocr_provider"/"claude_model"/"gemini_model"/"runpod_url")` を直接参照する箇所・`_apply_llm_settings`(943) が settings 更新後に provider を再生成する経路。**特にレビュー HIGH の核心**＝`_on_run` 内の provider 再生成が `name = self.app.settings.get("ocr_provider")`(1362) と `build_provider(self.app.settings, ...)`(1417/1429/1450/1470) で元プロバイダを読み直す4+1箇所を確認する）
    - .planning/phases/02-ai/02-REVIEWS.md（Antigravity レビューの HIGH: `_on_run` の app.settings 参照残り／MEDIUM: `_on_summary` のシグネチャ拡張／Suggestions の `s` 置換・`_validate_provider_readiness` 一般化）
    - .planning/phases/02-ai/02-RESEARCH.md（Pitfall 4「一時プロバイダ選択が self.app.settings を汚染」・A3「ヘルパー一般化＝引数追加 vs ローカル状態属性」の設計判断）
    - .planning/phases/02-ai/02-PATTERNS.md（「self.app.settings を書き換えないローカルスナップショット方針」・確認ダイアログ毎回表示方針）
    - tests/test_provider_ui.py（`_is_cloud_provider`/`_check_cloud_api_key` の既存テストが `self.app.settings` を前提にする箇所・後方互換を壊さない確認）
  </read_first>
  <action>
    `_is_cloud_provider`/`_confirm_cost`/`_check_cloud_api_key`/`_confirm_summary_cost`/`_on_run`/`_on_summary` の**6メソッド**にオプション引数 `settings=None` を加え（`_on_run(self, resume=False, settings=None)`・`_on_summary(self, settings=None)`・`_confirm_summary_cost(self, char_count, settings=None)`）、内部でプロバイダ名・モデル名・ホスト・env var を `settings if settings is not None else self.app.settings` から取得する形へ一般化する（RESEARCH A3 の「引数追加」方式。引数なし呼び出しは従来どおり `self.app.settings` を読むため既存テスト・挙動は完全不変）。各メソッド冒頭で `s = settings if settings is not None else self.app.settings` を定義する。

    **【レビュー HIGH 対応・必達】`_on_run` 内の `self.app.settings` 参照は1つ残らず `s` へ置換する。** 具体的には (1) `name = self.app.settings.get("ocr_provider", "")`（現 1362）を `name = s.get("ocr_provider", "")` へ、(2) provider 再生成ブロックの**4箇所の `build_provider(self.app.settings, ...)`**（claude 現1417 / gemini 現1429 / runpod 現1450 / else=tesseract・プラグイン 現1470）を全て `build_provider(s, ...)` へ置換する。この置換を怠ると、`_switch_to_fallback_provider` が `settings=fb`（切替先候補）を渡して `_on_run(resume=True, settings=fb)` を呼んでも、`_on_run` 内の再生成ブロックが `self.app.settings`（＝元のエラープロバイダ）から `self.provider` を作り直してしまい、フォールバックが全く機能しなくなる（レビュー指摘の HIGH バグ）。url/model/max_tokens/temperature は tk 変数（`self.url_var` 等）由来のため置換不要。置換後、`_on_run` 内に残る `self.app.settings` は `s = ... else self.app.settings` の1箇所のみになる。

    同様に `_confirm_cost` の `claude_model`/`gemini_model`/`runpod_model`/`runpod_url`（現1186-1198）・`_check_cloud_api_key` の `name`（現1258）を `s.get(...)` へ置換する。**【レビュー MEDIUM 対応】`_on_summary`（現1947）の `name = self.app.settings.get("ocr_provider", "")`（現1974）と `resolve_summary_prompt(name, load_summary_prompt(self.app.settings))`（現1976 の `load_summary_prompt(self.app.settings)`）を `s` 経由へ、`_confirm_summary_cost`（現1214）の `name`（現1223）・`runpod_url`（現1228）を `s.get(...)` へ置換する。** `_on_summary` は内部のクラウドゲート `_is_cloud_provider()`/`_check_cloud_api_key()`/`_confirm_summary_cost()` へも `settings=s` を渡し、サマリ経路でもローカルスナップショットが最後まで貫通するようにする（置換後 `_on_summary` 内に残る `self.app.settings` も `s = ...` の1箇所のみ）。

    `OCRDialog.__init__` に `self._active_ocr_settings = None`（実行開始時に確定）・`self._fallback_tried = set()`・`self._fallback_resume = False` を追加する。`_on_run` の冒頭（`_started` を True にする前）で `self._active_ocr_settings = settings if settings is not None else dict(self.app.settings)` を確定し、以降のクラウドゲート（`_is_cloud_provider`/`_check_cloud_api_key`/`_confirm_cost`）へ `settings=self._active_ocr_settings` を渡す。`_on_summary` も同型に冒頭で `s`（=`settings` か `self.app.settings`）を確定し `settings=s` を各ゲートへ渡す。通常実行では `dict(self.app.settings)` のコピーになるため挙動不変。**`self.app.settings` は絶対に書き換えない**（Pitfall 4）。`_active_provider_name()` ヘルパーを追加し、`(self._active_ocr_settings or self.app.settings).get("ocr_provider", "")` を返す。

    さらに `_on_run`（現1306 の `_confirm_cost(len(run_pages))`）と `_on_summary`（現1982 の `_confirm_summary_cost(len(full_text))`）のコスト確認呼び出しを `if not getattr(self, "_fallback_resume", False):` でガードし、フォールバック再開中（`_switch_to_fallback_provider` が立てるフラグ）はコスト確認を二重表示しない（フォールバック確認 askyesno が送信先確認を兼ねる・D-10/D-11）。`_check_cloud_api_key` はフラグに関わらず実行してよい（未解決なら早期 return で安全側）。

    `tests/test_ocr_fallback.py` に `TestSettingsIsolation` を追加する（`OCRDialog` を Tk 生成なしのスタブ／軽量インスタンスで扱う既存テスト経路に合わせる。必要なら `SimpleNamespace` で `app.settings` を与える）: (a) 一般化した `_is_cloud_provider`/`_check_cloud_api_key` に `settings=` を渡すと渡した dict のプロバイダで判定されること、(b) 引数なし呼び出しは `self.app.settings` を読む後方互換、(c) ローカルスナップショット `_active_ocr_settings` を差し替えても `self.app.settings` が不変であること（Pitfall 4 の永続設定汚染防止）。
  </action>
  <verify>
    <automated>pytest tests/test_ocr_fallback.py::TestSettingsIsolation tests/test_provider_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import inspect,pagefolio.ocr_dialog as m; sig=str(inspect.signature(m.OCRDialog._is_cloud_provider)); assert 'settings' in sig; sig2=str(inspect.signature(m.OCRDialog._on_run)); assert 'settings' in sig2 and 'resume' in sig2; sig3=str(inspect.signature(m.OCRDialog._on_summary)); assert 'settings' in sig3; sig4=str(inspect.signature(m.OCRDialog._confirm_summary_cost)); assert 'settings' in sig4"` が成功（OCR/サマリ両経路のシグネチャ一般化・レビュー MEDIUM）
    - `python -c "import inspect,pagefolio.ocr_dialog as m; assert inspect.getsource(m.OCRDialog._on_run).count('self.app.settings')==1"` が成功（レビュー HIGH 回帰防止＝`_on_run` 内に残る app.settings 参照はスナップショットフォールバック `s = ... else self.app.settings` の1箇所のみ。build_provider/name の直参照が消えたことを証明）
    - `python -c "import inspect,pagefolio.ocr_dialog as m; assert inspect.getsource(m.OCRDialog._on_summary).count('self.app.settings')==1"` が成功（レビュー MEDIUM 回帰防止＝`_on_summary` も同様にスナップショット経由へ一般化）
    - `pytest tests/test_ocr_fallback.py::TestSettingsIsolation -x` が pass（app.settings 不変を検証）
    - `pytest tests/test_provider_ui.py tests/test_ocr.py -x -q` が pass（既存クラウド判定・キー解決テストが後方互換で回帰しない）
    - `grep -v '^#' pagefolio/ocr_dialog.py | grep -c "self.app.settings\[" ` の増加がない（フォールバック経路が app.settings へ代入しない・既存の代入箇所のみ）
    - `ruff check pagefolio/ocr_dialog.py tests/test_ocr_fallback.py && ruff format --check pagefolio/ocr_dialog.py tests/test_ocr_fallback.py` がクリーン
  </acceptance_criteria>
  <done>クラウド判定3ヘルパー・_confirm_summary_cost・_on_run・_on_summary が settings 引数で一般化され（レビュー HIGH/MEDIUM）、_on_run/_on_summary 内の app.settings 直参照がスナップショット `s` 経由へ全置換され、_active_ocr_settings/_fallback_tried/_fallback_resume が導入され、既存テストが後方互換で全緑。app.settings は書き換えられない。</done>
</task>

<task type="auto">
  <name>Task 2: _propose_fallback / _switch_to_fallback_provider を実装し、_finish_error/_on_summary_error にフックを追加</name>
  <files>pagefolio/ocr_dialog.py, pagefolio/lang.py, tests/test_ocr_fallback.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（`_finish_error`(1847) 末尾・`_on_summary_error`(2192) 末尾＝フック挿入点・`_apply_llm_settings`(970〜1058) の provider 再生成パターン＝build_provider+concurrency 再クランプの写経元・`_pending_pages`(712)/`_can_resume`(716)＝D-09 再開機構・`_confirm_cost`(1171)/`_confirm_summary_cost`(1214) の毎回表示方針・`_on_summary`(1947) の実行経路＝サマリ再開時の呼び出し先・Task 1 で `settings=None` に一般化済み）
    - pagefolio/ocr_fallback.py（02-01 の `next_fallback_candidate`/`next_summary_candidate`）
    - pagefolio/ocr.py（`build_provider(settings, api_key=..., plugin_manager=...)`・`_resolve_api_key(name, session_keys)`・`OCRAPIKeyError`。`build_provider("tesseract")` は未インストールでも例外を投げず TesseractProvider を返す＝実行時まで失敗が遅延する点を確認＝レビュー LOW の根拠）
    - pagefolio/ocr_providers/tesseract.py（`_detect_tesseract()` → `(available: bool, langs: frozenset)`。build 前の実行可能性検証に使う・レビュー LOW）
    - .planning/phases/02-ai/02-RESEARCH.md（Pattern 6 の `_propose_fallback` コード例・Pitfall 3 のプロバイダ別 max_concurrency（claude 2/gemini 1/runpod 4/lmstudio・ollama 8）・Open Question 2 のサマリ経路フィルタ）
    - .planning/phases/02-ai/02-CONTEXT.md（D-09 未処理ページのみ再開・D-10 連鎖は最後まで各段で確認再提示・D-11 発火条件と api_key_missing 明示・D-12 サマリにも同順）
  </read_first>
  <action>
    `OCRDialog` に `_propose_fallback(self, kind, msg, summary=False)` を追加する（D-11/D-12・`summary` フラグでサマリ経路を区別＝レビュー MEDIUM）。手順: (1) `self.app.settings.get("ocr_fallback_enabled", False)` が False なら即 return（D-16 既定OFF）。(2) `chain = self.app.settings.get("ocr_fallback_chain", [])`。`self._fallback_tried` が空なら `{self._active_provider_name()}` で初期化（元プロバイダを試行済みに含める）。(3) `summary` が False なら `next_fallback_candidate(chain, self._fallback_tried)`、True なら `next_summary_candidate(chain, self._fallback_tried, text_capable)` で候補を選ぶ（`text_capable` は `supports_text_prompt` を満たすプロバイダ名集合＝tesseract 除外・D-12・Open Question 2）。候補 None なら `fallback_exhausted` を表示して return（チェーン終端・現状のエラー表示のみ）。(4) 理由キーを `kind`（connection/timeout/circuit_breaker/api_key_missing/provider_unavailable）→`fallback_reason_*` へマップ（不明は `fallback_reason_generic`）。(5) **毎回** `messagebox.askyesno(self._L["fallback_confirm_title"], self._L["fallback_confirm_msg"].format(reason=..., candidate=candidate, host=<候補の送信先ホスト>), parent=self)` を表示する（D-10/D-11・送信先確認再提示・「今後表示しない」オプションは付けない）。No なら `self._fallback_tried.add(candidate)` して return（拒否候補も試行済み扱いで D-10 連鎖継続・ただし自動では次へ進まず現エラー表示に留める＝ユーザー承認なし送信なし）。Yes なら `self._fallback_tried.add(candidate)` して `self._switch_to_fallback_provider(candidate, summary=summary)` を呼ぶ。

    `_switch_to_fallback_provider(self, candidate, summary=False)` を追加する（V180-FALL-03・Pitfall 3/4・レビュー HIGH/MEDIUM/LOW を貫通させる中核）: (1) `fb = dict(self.app.settings); fb["ocr_provider"] = candidate` でローカルスナップショットを作り `self._active_ocr_settings = fb`（`self.app.settings` は書き換えない・Pitfall 4）。(2) **【レビュー LOW 対応】実行可能性を build 前に検証する**: `if not self._validate_provider_readiness(candidate, fb):` の場合、候補を `self._fallback_tried` に追加し、理由 `reason`（クラウドキー未解決→`"api_key_missing"`、tesseract 未インストール→`"provider_unavailable"`）で `self._propose_fallback(reason, msg, summary=summary)` を再帰呼び出しして次候補へ進み return する（静かな握りつぶし禁止・D-11/D-14）。(3) `_apply_llm_settings` の (f) provider 再生成ブロックと同型に `build_provider(fb, api_key=<_resolve_api_key(candidate, session_keys) を OCRAPIKeyError で "" フォールバック>, plugin_manager=...)` で `self.provider` を再構築する（**fb を渡す**＝候補プロバイダを生成）。(4) `self.concurrency = max(1, min(self.provider.max_concurrency, self.concurrency))` でプロバイダ別並列度を再クランプ（Pitfall 3・claude 2/gemini 1 等）。(5) `self._fallback_resume = True` を立てる（`_on_run`/`_on_summary` のコスト確認ゲートがこのフラグの間 `_confirm_cost`/`_confirm_summary_cost` をスキップする＝フォールバック確認 askyesno が送信先確認を兼ねる・D-10/D-11。**フラグはゲート通過直後に必ず `finally` 相当で False へ戻す**）。(6) OCR 経路（`summary=False`）は `self._done=False; self._started=False` に戻して `self._on_run(resume=True, settings=fb)`（D-09 未処理ページのみ再開・Task 1 で `_on_run` 内の build_provider が `s`=fb を使うよう修正済みなので候補プロバイダで実際に再実行される＝レビュー HIGH の解消経路）。**【レビュー MEDIUM 対応】サマリ経路（`summary=True`）は `self._summary_running=False` に戻して `self._on_summary(settings=fb)` を呼ぶ**（Task 1 で一般化した `_on_summary(self, settings=None)` が fb スナップショットを `_summary_worker` まで引き継ぎ、再構築 provider でサマリを再送信する）。

    `_validate_provider_readiness(self, candidate, settings)` を追加する（レビュー LOW・reviewer 提案の `_validate_provider_readiness` 一般化を採用）: (a) クラウド候補（`_is_cloud_provider(settings=settings)` が True）は `_check_cloud_api_key(settings=settings)` の結果をそのまま返す（未解決なら False）。(b) `candidate == "tesseract"` は `from pagefolio.ocr_providers import _detect_tesseract` の `_detect_tesseract()[0]`（インストール済み bool）を返す（未インストールなら False＝`build_provider` は例外を投げない代わりにここで検出する）。(c) それ以外のローカル（lmstudio/ollama）・プラグイン登録プロバイダは True を返す（URL 到達不能はランタイムの既存 connection fatal 経路が拾い、そこから再び `_propose_fallback` される）。裸 except は使わない。

    `_finish_error` の末尾（`self._after_run_ui_reset()` の後）に `self._propose_fallback(kind, msg)` を追加する（D-11 フック・OCR 経路 `summary=False`）。`_on_summary_error` の末尾（`self._summary_ui_reset()` の後）にサマリ経路の `self._propose_fallback(kind or "generic", msg, summary=True)` 呼び出しを追加する（D-12・`summary=True` により `next_summary_candidate` で text 非対応候補=tesseract を除外＝レビュー MEDIUM）。冪等ガード（`self._done`／`self._summary_running`）と整合するよう、フォールバックが提案・承認されて resume 実行に入る場合は `_done`/`_started`（サマリは `_summary_running`）の再初期化を `_switch_to_fallback_provider` 側で行う。

    `lang.py` の ja/en 両ブロックへ**同一キーで**追加: `fallback_confirm_title`、`fallback_confirm_msg`（reason/candidate/host を format する・「{reason} により停止しました。{candidate}（{host}）へ切り替えて未処理ページを再送信しますか？」相当）、`fallback_reason_connection`、`fallback_reason_timeout`、`fallback_reason_circuit_breaker`、`fallback_reason_api_key_missing`（「APIキー未設定」を明示・D-11）、`fallback_reason_provider_unavailable`（「プロバイダが利用できません（Tesseract 未インストール等）」相当・レビュー LOW）、`fallback_reason_generic`、`fallback_exhausted`（「フォールバック候補を使い切りました」相当）。

    `tests/test_ocr_fallback.py` に `TestConfirmationGate` を追加する: `monkeypatch` で `messagebox.askyesno` を差し替え、(a) `ocr_fallback_enabled=True`+chain 設定時に fatal で `_propose_fallback` が askyesno を呼ぶこと（承認ゲートが省略されないこと＝Pitfall 2）、(b) **【レビュー HIGH 回帰防止】承認（True）で `_switch_to_fallback_provider`→`_on_run(resume=True, settings=fb)` 経由で `build_provider` が `ocr_provider==candidate` の設定 dict で呼ばれ、`self.provider` が候補プロバイダになること**（`build_provider` をスパイし、呼び出し時の第1引数 settings の `ocr_provider` が元プロバイダでなく candidate であることを assert＝app.settings 直参照が残っていると失敗する）、(c) 拒否（False）で別ベンダー送信が起きないこと、(d) `ocr_fallback_enabled=False` では askyesno が一切呼ばれないこと（V180-FALL-01）、(e) api_key_missing 理由が確認文言に含まれること（D-11）。

    `TestSummaryFallback` を追加する（レビュー MEDIUM）: `_on_summary_error` 経由で `_propose_fallback(kind or "generic", msg, summary=True)` が呼ばれ、(a) `next_summary_candidate` で tesseract が候補から除外されること、(b) 承認時に `_switch_to_fallback_provider(candidate, summary=True)` → `self._on_summary(settings=fb)` が呼ばれること（`_on_summary` を monkeypatch でスパイし、渡された `settings` 引数が fb（`ocr_provider==candidate`）であることを assert）を検証する。

    `TestProviderReadiness` を追加する（レビュー LOW）: `_detect_tesseract` を monkeypatch で `(False, frozenset())` に固定し、chain に tesseract を含む状態でフォールバック承認時、(a) `_validate_provider_readiness("tesseract", fb)` が False を返すこと、(b) tesseract が `_fallback_tried` に追加され `provider_unavailable` 理由で次候補へ `_propose_fallback` が再帰されること（build_provider が tesseract で実行されず次候補で呼ばれること）を検証する。

    `OCRDialog` は Tk 生成を避けた軽量スタブ／`messagebox`・`build_provider`・`_detect_tesseract`・`_on_summary` の monkeypatch で検証する（`test_ocr.py`/`test_provider_ui.py` の既存スタブ流儀に合わせる）。
  </action>
  <verify>
    <automated>pytest tests/test_ocr_fallback.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pagefolio.ocr_dialog as m; assert hasattr(m.OCRDialog,'_propose_fallback') and hasattr(m.OCRDialog,'_switch_to_fallback_provider') and hasattr(m.OCRDialog,'_validate_provider_readiness')"` が成功
    - `python -c "import inspect,pagefolio.ocr_dialog as m; assert 'summary' in str(inspect.signature(m.OCRDialog._propose_fallback))"` が成功（サマリ経路分岐＝レビュー MEDIUM）
    - `pytest tests/test_ocr_fallback.py::TestConfirmationGate tests/test_ocr_fallback.py::TestSettingsIsolation -x` が pass（承認ゲート再提示・app.settings 不変・build_provider が candidate 設定で呼ばれる HIGH 回帰防止）
    - `pytest tests/test_ocr_fallback.py::TestSummaryFallback tests/test_ocr_fallback.py::TestProviderReadiness -x` が pass（レビュー MEDIUM: サマリ経路の fb 引き継ぎ／レビュー LOW: tesseract 未インストール候補のスキップ遷移）
    - `python -c "from pagefolio.lang import LANG; ja=set(LANG['ja']); en=set(LANG['en']); need={'fallback_confirm_title','fallback_confirm_msg','fallback_reason_api_key_missing','fallback_reason_provider_unavailable','fallback_exhausted'}; assert need<=ja and need<=en; assert ja==en"` が成功（`fallback_reason_provider_unavailable` を含む・ja/en 一致）
    - `grep -c '_propose_fallback' pagefolio/ocr_dialog.py` が 3 以上（定義 + _finish_error フック + _on_summary_error フック・readiness 再帰でさらに増える）
    - `pytest tests/test_ocr_fallback.py tests/test_ocr.py tests/test_provider_ui.py tests/test_lang_parity.py -x -q` が pass
    - `ruff check pagefolio/ocr_dialog.py pagefolio/lang.py tests/test_ocr_fallback.py && ruff format --check pagefolio/ocr_dialog.py pagefolio/lang.py tests/test_ocr_fallback.py` がクリーン
  </acceptance_criteria>
  <done>_propose_fallback（summary 分岐）/_switch_to_fallback_provider/_validate_provider_readiness が実装され、_finish_error/_on_summary_error からフックされ、毎回 askyesno 再提示・app.settings 非汚染・OCR/サマリ両経路の fb スナップショット引き継ぎ（レビュー HIGH/MEDIUM）・実行不可プロバイダの明示スキップ（レビュー LOW）・プロバイダ別 concurrency/APIキー再評価・未処理ページ再開を満たし、TestConfirmationGate/TestSettingsIsolation/TestSummaryFallback/TestProviderReadiness を含む全テストがグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCRDialog（クライアント） → 外部 LLM API | ページ画像 base64 が別ベンダーの https エンドポイントへ送信される（フォールバック切替は送信先ベンダーを変える操作） |
| フォールバックスナップショット → self.app.settings | 一時プロバイダ選択が永続設定へ漏れうる境界（Pitfall 4） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering / Repudiation | フォールバック発火時の同意迂回（無承認で別ベンダーへ送信） | high | mitigate | `_propose_fallback` が連鎖の各段で必ず `messagebox.askyesno` を再提示（「今後表示しない」不可）。No/終端は現状エラー表示のみで自動送信しない。`TestConfirmationGate` で askyesno 呼び出しをアサート（D-10/D-11） |
| T-02-11 | Tampering | 一時プロバイダ選択が self.app.settings を汚染し永続化（Pitfall 4） | high | mitigate | 切替は `self._active_ocr_settings = dict(self.app.settings)` のローカルコピー上のみ。`build_provider` にローカル dict を渡す。`self.app.settings` へ代入しない。`TestSettingsIsolation` で不変を検証 |
| T-02-12 | Repudiation | APIキー未設定候補の静かな握りつぶし | medium | mitigate | `_check_cloud_api_key(settings=fb)` False 時に `kind="api_key_missing"` を確認ダイアログへ明示し（D-11）、`logger` に記録してから次候補へ進む（D-14） |
| T-02-13 | Denial of Service | フォールバック先で並列度過大による 429 誘発（Pitfall 3） | medium | mitigate | 候補ごとに `build_provider` を独立呼び出しし `self.provider.max_concurrency`（gemini 1 等）で `self.concurrency` を再クランプ（V180-FALL-03） |
| T-02-14 | Repudiation / Denial of Service | 実行不可プロバイダ（tesseract 未インストール／クラウドキー未解決）へのフォールバックの静かな失敗（レビュー LOW） | medium | mitigate | `_validate_provider_readiness(candidate, fb)` が build 前に `_detect_tesseract()[0]`（都度検出）とクラウドキー解決を検証し、不可なら `provider_unavailable`/`api_key_missing` 理由を確認ダイアログへ明示して次候補へ進む（握りつぶし禁止・D-11/D-14）。`TestProviderReadiness` でスキップ遷移をアサート |
</threat_model>

<verification>
- `pytest tests/test_ocr_fallback.py tests/test_ocr.py tests/test_provider_ui.py tests/test_lang_parity.py -x -q`
- `pytest`（フルスイート・回帰なし）
- `ruff check . && ruff format --check .`
</verification>

<success_criteria>
- fatal 停止時（connection/timeout/circuit_breaker/api_key_missing）に _propose_fallback が呼ばれ、有効時のみ毎回 askyesno 再提示で次候補を提案する（V180-FALL-02・D-10/D-11）
- ユーザー承認なしに別ベンダーへ自動送信されない（拒否/終端は現状エラー表示のみ）
- self.app.settings を書き換えず、_active_ocr_settings ローカルスナップショット上でのみ切替する（Pitfall 4）
- **【レビュー HIGH】_on_run の provider 再構築（build_provider 4箇所 + name 取得）が全てローカルスナップショット s（=fb）から行われ、切替先候補プロバイダで実際に再実行される（app.settings 直参照残りゼロ）**
- **【レビュー MEDIUM】サマリ経路も _on_summary(settings=fb)/_confirm_summary_cost(settings=)/_check_cloud_api_key(settings=) で fb スナップショットを最後まで引き継ぐ**
- **【レビュー LOW】_validate_provider_readiness が tesseract 未インストール／クラウドキー未解決を build 前に検出し、provider_unavailable/api_key_missing 理由を明示して次候補へ進む（静かな握りつぶしなし）**
- 候補ごとに build_provider を再呼び出しし max_concurrency 再クランプ・APIキー解決・レート制限を再評価する（V180-FALL-03・Pitfall 3）
- api_key_missing を確認ダイアログに明示してから次候補へ進む（D-11/D-14）
- 切替後は _pending_pages() の未処理ページのみ再開（D-09）、サマリ失敗にも同順を適用し text 非対応候補を除外（D-12）
- TestConfirmationGate/TestSettingsIsolation/TestSummaryFallback/TestProviderReadiness を含む全テスト + フルスイートがグリーン、ruff クリーン
</success_criteria>

<output>
Create `.planning/phases/02-ai/02-04-SUMMARY.md` when done
</output>
