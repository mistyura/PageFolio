---
phase: 02-ai
plan: 06
type: execute
wave: 2
depends_on: ["02-05"]
gap_closure: true
autonomous: true
requirements: [V180-TMPL-02, V180-TMPL-03, V180-TMPL-04]
files_modified:
  - tests/test_provider_ui.py

must_haves:
  truths:
    - "_on_template_change は外部mdファイル連動モードで未保存差分がある場合 messagebox.askyesno を呼び、No 応答で切替を中止し template_var をアクティブテンプレートへ戻し入力欄を変化させない（D-05・V180-TMPL-04）— headless bound-method テストで実証"
    - "_on_template_change は切替確定後、外部mdファイル（ocr_custom_prompt.md/ocr_summary_prompt.md）を新アクティブテンプレートの custom_prompt/summary_prompt で上書きする（save_prompt_file 呼び出し・D-07・V180-TMPL-04）— headless bound-method テストで実証"
    - "_on_template_save/_on_template_rename は既存名入力時に messagebox.showerror を呼び save_template/rename_template を呼ばない（D-04・V180-TMPL-03・UI 経由）— headless bound-method テストで実証"
    - "_refresh_template_delete_state はアクティブテンプレート選択時に template_delete_btn へ 'disabled' を、非アクティブ選択時に '!disabled' を設定する（D-03・V180-TMPL-03・UI 経由）— headless bound-method テストで実証"
  artifacts:
    - "tests/test_provider_ui.py（TestTemplateUIHandlers 等の実 bound-method テストクラス + テンプレート UI 用ウィジェットスタブ + headless ダイアログスタブヘルパー）"
  key_links:
    - "LLMConfigDialog.__new__ ヘッドレスインスタンス + ウィジェットスタブ → 実 bound method（_on_template_change/_on_template_save/_on_template_rename/_refresh_template_delete_state）呼び出し"
    - "monkeypatch（sections.messagebox.askyesno/showerror・simpledialog.askstring・prompt_file_exists・save_prompt_file）→ UI ハンドラの分岐経路を駆動"
---

<objective>
Phase 2 検証（02-VERIFICATION.md）の `behavior_unverified_items`（4件・テンプレート
UI ハンドラ層の状態遷移）を、実 bound method を駆動する headless テストで閉じる。
これらは「実装はソース上存在するが、実ウィジェットの状態遷移を駆動する自動テストが
無い（02-02-SUMMARY.md 自身が human_judgment: true と記録）」という
PRESENT_BEHAVIOR_UNVERIFIED のギャップである。

フォールバック側（02-04）は headless スタブ + 実 bound method 呼び出しテスト
（tests/test_ocr_fallback.py）で実証済みという先例がある。テンプレート側の UI
ハンドラ層にも同型のカバレッジを与え、テストの質の非対称ギャップを解消する。

Purpose: D-03（削除ボタン無効化）・D-04（重複名 showerror 拒否）・D-05（切替時
未保存差分確認 → キャンセルで中止）・D-07（切替後の外部md上書き）の状態遷移を
回帰から保護する。

Output: tests/test_provider_ui.py への UI ハンドラ駆動テスト群と支援スタブ。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-VERIFICATION.md
@.planning/phases/02-ai/02-CONTEXT.md
</context>

<gap_source>
本プランは 02-VERIFICATION.md frontmatter の `behavior_unverified_items`（4件・
status: PRESENT_BEHAVIOR_UNVERIFIED）を閉じる。

- **D-05/D-07（truth 2・V180-TMPL-04）:** `_on_template_change`（sections.py:1197-1242）の
  未保存差分確認（askyesno）→キャンセルで切替中止→切替後の外部 md ファイル上書き。
  実 Tk 駆動テストが存在しない（source 確認のみ）。
- **D-04（truth 10・V180-TMPL-03）:** `_on_template_save`/`_on_template_rename` の重複名
  `messagebox.showerror` 拒否経路（UI 経由）。純ロジック層は test_prompt_templates.py で
  検証済みだが UI ハンドラの try/except→showerror 経路を駆動する実テストが無い。
- **D-03（truth 11・V180-TMPL-03）:** アクティブテンプレート選択時の `template_delete_btn`
  disabled 状態（`_refresh_template_delete_state`）。実ウィジェット state の駆動テストが無い。

**アプローチ（VERIFICATION.md why_human 欄が示す方針）:** headless Tk が使えない CI を
想定し、tests/test_ocr_fallback.py の先例（`OCRDialog.__new__` + 属性注入 + 実 bound
method 呼び出し + module-level monkeypatch）と同型で、`LLMConfigDialog.__new__` を用いた
ヘッドレスインスタンス + 軽量ウィジェットスタブで駆動する。実 Tk ウィジェット生成は
行わない（既存コードベースの headless テスト規約に統一）。

**依存理由（depends_on 02-05）:** 02-05 が `_on_template_delete` に askyesno を追加し
`_save_settings` を除去する。本プランは同じ tests/test_provider_ui.py を編集し、
テンプレート UI ハンドラの修正後挙動を前提とするため、02-05 の完了後（Wave 2）に実行する。
</gap_source>

<tasks>

<task type="auto">
  <name>Task 1: headless テンプレートダイアログスタブと支援ウィジェットスタブを追加し、D-05/D-07（_on_template_change 切替中止・外部md上書き）を検証する</name>
  <files>tests/test_provider_ui.py</files>
  <read_first>
    - tests/test_provider_ui.py（_GetVarStub/_GetTextStub 1147-1166・_make_apply_key_stub 1169-1201・TestTemplateSection 1746-1817・_read_llm_config_package_source 208 付近）
    - tests/test_ocr_fallback.py（_make_dialog 18-48・TestConfirmationGate/TestSettingsIsolation の monkeypatch スタイル 128-270）
    - pagefolio/dialogs/llm_config/sections.py（_has_unsaved_template_changes 1154-1175・_refresh_template_delete_state 1177-1183・_reload_template_combo 1185-1195・_on_template_change 1197-1242）
    - pagefolio/settings.py（get_template 167-173・save_template 188-200）
    - pagefolio/constants.py（CUSTOM_PROMPT_FILE / SUMMARY_PROMPT_FILE の値）
  </read_first>
  <action>
    tests/test_provider_ui.py に、テンプレート UI ハンドラを Tk 生成なしで駆動するための支援スタブと headless ダイアログヘルパーを追加する。

    ウィジェットスタブを新設する（既存 `_GetVarStub`（get のみ）・`_GetTextStub`（get のみ）では set/delete/insert/state/configure が足りないため）:
    - set/get 可能な var スタブ（`.get()` が現在値・`.set(v)` が値更新を行い最後にセットされた値を記録する）
    - tk.Text 相当スタブ（`.get(start, end)` が内部バッファ・`.delete(start, end)` がバッファ空化・`.insert(index, value)` がバッファ設定。現在の文字列内容を検査できるプロパティ/属性を持つ）
    - ttk.Button 相当スタブ（`.state(spec)` が最後に渡されたフラグ列（例: ["disabled"] / ["!disabled"]）を記録する）
    - ttk.Combobox 相当スタブ（`.configure(values=...)` が values を記録する。_reload_template_combo が呼ぶため）
    これらは既存ヘルパー名と衝突しない命名（例: `_SetGetVarStub`/`_FakeText`/`_FakeButton`/`_FakeCombo`）にする。

    headless ダイアログヘルパー `_make_template_dialog(...)` を追加する。test_ocr_fallback.py の `_make_dialog` と同型に `LLMConfigDialog.__new__(LLMConfigDialog)` でインスタンスを作り、`__init__`/`_build` を経由せず必要属性のみ設定する: `_L=LANG["ja"]`、`current_settings`（引数で受け取る・`prompt_templates` の active + items を含む）、`_active_template_name`、`template_var`（set/get スタブ）、`ocr_prompt_text`/`ocr_summary_prompt_text`（Text スタブ）、`template_combo`（Combobox スタブ）、`template_delete_btn`（Button スタブ）。`self=`（parent）は messagebox/simpledialog が monkeypatch されるため任意（None 可）。LLMConfigDialog の全 mixin メソッド（`_has_unsaved_template_changes`/`_refresh_template_delete_state`/`_reload_template_combo` 等）が実インスタンス上で使えるため、`_on_template_change` 内の `self._has_unsaved_template_changes(...)` 等の自己呼び出しも実コードで動く。

    テストクラス `TestTemplateChangeFlow`（D-05/D-07・V180-TMPL-04）を追加する:
    - D-05 テスト（未保存差分ありでキャンセル→切替中止）: `pagefolio.dialogs.llm_config.sections.prompt_file_exists` を True 返却へ monkeypatch（外部ファイル連動モードを再現）。current_settings に active テンプレート（例: "A"・custom_prompt="saved-A"）を設定し `_active_template_name="A"`。`ocr_prompt_text` を "saved-A" と異なる編集済み内容（例: "edited"）にして未保存差分を作る。`template_var.get()` は切替先（例: "B"）を返すよう設定。`sections.messagebox.askyesno` を False（No）へ monkeypatch。`d._on_template_change()` を呼び、(1) `template_var` の最終値が "A"（アクティブへ戻る）、(2) `ocr_prompt_text` の内容が編集済み "edited" のまま変化しない（切替先内容で上書きされない）、(3) `save_prompt_file` が呼ばれていない（D-07 の書き戻しに到達していない）ことをアサートする。`sections.save_prompt_file` は呼び出し記録用のフェイクへ monkeypatch する。
    - D-07 テスト（切替確定→外部md上書き）: `prompt_file_exists` を True、`askyesno` を True（Yes）へ monkeypatch（もしくは未保存差分が無い状態を作り確認をスキップさせる）。current_settings に切替先テンプレート "B"（custom_prompt="newC"・summary_prompt="newS"）を含め、`template_var.get()` が "B" を返すようにする。`sections.save_prompt_file` を (ファイル名, 内容) を記録するフェイクへ monkeypatch。`d._on_template_change()` を呼び、save_prompt_file が CUSTOM_PROMPT_FILE→"newC"・SUMMARY_PROMPT_FILE→"newS" で呼ばれたこと、`d._active_template_name` が "B" になったこと、`ocr_prompt_text` の内容が "newC" になったことをアサートする。

    monkeypatch 対象は sections.py にインポートされた名前（`pagefolio.dialogs.llm_config.sections.messagebox.askyesno`・`pagefolio.dialogs.llm_config.sections.prompt_file_exists`・`pagefolio.dialogs.llm_config.sections.save_prompt_file`）とする。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py::TestTemplateChangeFlow -q</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `pytest tests/test_provider_ui.py::TestTemplateChangeFlow -q` が全 pass
    - behavior assertion: D-05 テストが askyesno=False で template_var が元アクティブ名へ戻る・入力欄内容が不変・save_prompt_file 非呼出を全てアサートしている
    - behavior assertion: D-07 テストが save_prompt_file を CUSTOM_PROMPT_FILE/SUMMARY_PROMPT_FILE と新テンプレート内容で呼んだこと・_active_template_name 更新・入力欄反映をアサートしている
    - behavior assertion: テストが `LLMConfigDialog.__new__` + 実 bound method `_on_template_change` を呼び出しており、source-scan のみに依存していない
    - source assertion: 新設スタブ（set/get var・Text・Button・Combobox 相当）が追加されている
  </acceptance_criteria>
  <done>
    TestTemplateChangeFlow が D-05（キャンセルで切替中止）と D-07（切替後の外部md上書き）を実 bound-method 呼び出しで実証し、pass する。
  </done>
</task>

<task type="auto">
  <name>Task 2: D-04（重複名 showerror 拒否）と D-03（アクティブ削除ボタン無効化）の UI ハンドラテストを追加する</name>
  <files>tests/test_provider_ui.py</files>
  <read_first>
    - tests/test_provider_ui.py（Task 1 で追加したスタブ・_make_template_dialog ヘルパー）
    - pagefolio/dialogs/llm_config/sections.py（_on_template_save 1244-1276・_on_template_rename 1302-1329・_refresh_template_delete_state 1177-1183）
    - pagefolio/settings.py（template_name_exists 176-185・rename_template 219-237・delete_template 203-216）
  </read_first>
  <action>
    tests/test_provider_ui.py に、Task 1 の `_make_template_dialog` ヘルパーとスタブを再利用して D-04・D-03 の UI ハンドラ経路を駆動するテストを追加する。

    テストクラス `TestTemplateNameValidationUI`（D-04・V180-TMPL-03・UI 経由）:
    - save 重複拒否テスト: current_settings の items に既存名（例: "dup"）を含める。`sections.simpledialog.askstring` を "dup"（既存名）返却へ monkeypatch。`sections.messagebox.showerror` を呼び出し記録フェイクへ monkeypatch。`ocr_prompt_text`/`ocr_summary_prompt_text` に任意内容を設定。`d._on_template_save()` を呼び、(1) showerror が呼ばれたこと、(2) items の "dup" 内容が変化していない（save_template による上書きが起きていない＝重複拒否で return した）ことをアサートする。加えて空名（askstring→"" または空白のみ）ケースでも showerror が呼ばれ items が変化しないことをアサートする。
    - rename 重複拒否テスト: current_settings の items に "old" と "taken" を含める。`template_var.get()` が "old" を返すよう設定。`sections.simpledialog.askstring` を "taken"（別の既存名）返却へ monkeypatch。`sections.messagebox.showerror` をフェイクへ monkeypatch。`d._on_template_rename()` を呼び、(1) showerror が呼ばれたこと、(2) items のキー集合が変化しない（"old"/"taken" が両方残り rename が行われていない）ことをアサートする（rename_template の ValueError→showerror→return 経路を駆動）。

    テストクラス `TestTemplateDeleteButtonState`（D-03・V180-TMPL-03・UI 経由）:
    - アクティブ選択時 disabled テスト: `_active_template_name="A"`、`template_var.get()` が "A" を返す（アクティブ選択）状態で `template_delete_btn`（Button スタブ）を設定。`d._refresh_template_delete_state()` を呼び、`template_delete_btn` に記録された最後の state 引数が "disabled" フラグを含むことをアサートする。
    - 非アクティブ選択時 有効テスト: 同じ stub で `template_var.get()` が "B"（非アクティブ）を返すよう変更し `d._refresh_template_delete_state()` を呼び、記録された state 引数が "!disabled" フラグを含むことをアサートする。

    monkeypatch 対象は `pagefolio.dialogs.llm_config.sections.simpledialog.askstring`・`pagefolio.dialogs.llm_config.sections.messagebox.showerror`。simpledialog/messagebox は sections.py が `from tkinter import messagebox, simpledialog` で取り込んでいるため、この属性パスで差し替える。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py::TestTemplateNameValidationUI tests/test_provider_ui.py::TestTemplateDeleteButtonState -q</automated>
  </verify>
  <acceptance_criteria>
    - test-command: 上記2クラスの pytest が全 pass
    - behavior assertion: save 重複拒否テストが showerror 呼出 + items 非変化を、空名でも showerror + items 非変化をアサートしている
    - behavior assertion: rename 重複拒否テストが showerror 呼出 + items キー集合非変化をアサートしている
    - behavior assertion: 削除ボタン状態テストがアクティブ選択で "disabled"、非アクティブ選択で "!disabled" を Button スタブの記録から検証している
    - behavior assertion: 全テストが実 bound method（_on_template_save/_on_template_rename/_refresh_template_delete_state）を呼んでいる
    - test-command: `pytest tests/test_provider_ui.py -q` が回帰なく pass
  </acceptance_criteria>
  <done>
    TestTemplateNameValidationUI と TestTemplateDeleteButtonState が D-04（save/rename 重複名 showerror 拒否）と D-03（アクティブ削除ボタン無効化）を実 bound-method 呼び出しで実証し、pass する。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| テストハーネス → 本番トラスト境界を越えない | 本プランはテスト追加のみ。ランタイムのアタックサーフェスを新規に導入しない。テストは 02-05 で修正した CR-02 緩和（承認なし永続化の不在）およびテンプレート UI ハンドラの安全な状態遷移を検証する側に立つ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Repudiation | テンプレート UI ハンドラ（D-03/04/05/07）の状態遷移がテスト未カバーで、将来の回帰（誤削除・重複拒否漏れ・切替時の外部md競合）が検出されない | medium | mitigate | headless bound-method テストで4つの状態遷移を実証し、テストカバレッジの非対称ギャップ（フォールバック側は実証済み・テンプレート側は source-scan のみ）を解消して回帰を検出可能にする |
| T-02-06-02 | Tampering | D-07 の外部mdファイル上書き経路が未検証で、切替時に誤った内容がファイルへ書き込まれる回帰を見逃す | low | mitigate | save_prompt_file 呼び出しの引数（ファイル名・内容）をフェイクで捕捉しアサートする |

パッケージ導入（npm/pip/cargo）: 本ギャップに新規パッケージ導入なし（テスト追加のみ）。サプライチェーン脅威は非該当。
</threat_model>

<verification>
- `pytest tests/test_provider_ui.py -q` が全 pass（新テストクラス含む）
- `pytest -q` がフルスイート回帰なし
- `ruff check . && ruff format --check .` がクリーン
- 追加テストが `LLMConfigDialog.__new__` + 実 bound method 呼び出し（source-scan のみでない）で書かれている
</verification>

<success_criteria>
- 02-VERIFICATION.md behavior_unverified_items（4件・D-03/D-04/D-05/D-07）がすべて実 bound-method テストでカバーされ、human_verification の該当項目が自動テストへ移行する
- テンプレート UI ハンドラ層のカバレッジ非対称（フォールバック側との差）が解消される
- フルスイート回帰なし・ruff クリーン
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**新規シンボル（すべて tests/test_provider_ui.py）:**
- 支援ウィジェットスタブ: set/get 可能な var スタブ（例: `_SetGetVarStub`）・Text スタブ（例: `_FakeText`）・Button スタブ（例: `_FakeButton`）・Combobox スタブ（例: `_FakeCombo`）
- headless ヘルパー: `_make_template_dialog(...)`（`LLMConfigDialog.__new__` + 属性注入）
- テストクラス `TestTemplateChangeFlow`（D-05/D-07）
- テストクラス `TestTemplateNameValidationUI`（D-04・save/rename 重複拒否）
- テストクラス `TestTemplateDeleteButtonState`（D-03・削除ボタン無効化）

**変更なし:** プロダクションコードは一切変更しない（テスト追加のみ）。
</artifacts_produced>

<output>
Create `.planning/phases/02-ai/02-06-SUMMARY.md` when done
</output>
