---
phase: 04-ocr
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - pagefolio/dialogs/batch_ocr.py
  - pagefolio/dialogs/__init__.py
  - pagefolio/ui_builder.py
  - pagefolio/app.py
  - pagefolio/lang.py
  - tests/test_batch_ocr_dialog.py
autonomous: true
requirements:
  - V180-BATCH-05
must_haves:
  truths:
    - "バッチ完了後、完了済みファイルの OCR 結果はダイアログ内に保持され、ファイル選択で切り替えて閲覧できる（D-11・D-16）"
    - "統合サマリは手動トリガー（「📊 サマリ作成」ボタン）で生成され、バッチ完了時に自動生成されない（D-13・不要な送信コスト回避）"
    - "統合サマリ入力は各ファイル先頭に「=== ファイル名.pdf ===」見出しを挿入して連結される（D-15）"
    - "統合サマリ送信前に、連結後の合計文字数を渡した過大入力警告（_confirm_summary_cost 拡張）が表示される（D-14・V180-BATCH-05）"
    - "ファイル別結果ビューは _insert_markdown による Markdown 整形描画を流用し、コピー/保存は raw 維持・エクスポートはファイル単位（D-16）"
    - "_insert_markdown / _confirm_summary_cost / _format_pages_text は OCRDialog（ocr_dialog.py）の同名メソッドと同一挙動の BatchOCRDialog 自前実装（コピペ移植）であり、ocr_dialog.py は本フェーズで変更せず OCRDialog の継承やインスタンスメソッドの import 流用もしない（モジュール定数 SUMMARY_TOO_LONG_CHARS の import は可・レビュー懸念5・流用方式の明確化）"
    - "Treeview 用 ttk.Style は ui_builder._build_styles に追記され、テーマ切替時に _rebuild_ui が _build_styles() を再呼び出しする既存経路（app.py:613）で配色が C 辞書へ追随する（レビュー提案・テーマ追随の構造的担保）"
    - "ユーザーは新規メニュー項目「バッチOCR」から独立ダイアログを起動できる（D-01）"
    - "メニュー起動導線は self.doc / self.filepath を参照せず、開いているファイルと独立してダイアログを開く（D-04）"
    - "from pagefolio.dialogs import BatchOCRDialog が成功する（後方互換 re-export パターン）"
    - statement: "統合サマリで完了ファイルが0件の場合は送信せず no-op（何もしない・zero-completed エッジ）"
      verification: backstop
    - statement: "Treeview / メニューのテーマ配色（dark/light）が既存 C 辞書に整合する（実配色の目視は手動検証・Pitfall 4）"
      verification: backstop
    - statement: "メニュー項目にアクセラレータキーを設定せずクリック起動とし、既存 ShortcutsDialog の11コマンドと衝突しない（Pitfall 5・Open Question 1）"
      verification: backstop
  artifacts:
    - pagefolio/dialogs/batch_ocr.py
    - pagefolio/dialogs/__init__.py
    - pagefolio/ui_builder.py
    - pagefolio/app.py
    - pagefolio/lang.py
  key_links:
    - "_format_batch_summary_input（見出し連結）→ _confirm_summary_cost（合計文字数）→ complete_text_ex（既存プロバイダ層）"
    - "app メニューバー（tk.Menu）→ _open_batch_ocr → BatchOCRDialog（self.doc 非参照・D-04）"
    - "ui_builder._build_styles の Treeview/メニュー ttk.Style → C 辞書のテーマ色（Pitfall 4）→ _rebuild_ui→_build_styles() 再呼び出し（app.py:613）でテーマ追随（レビュー提案）"
    - "dialogs/__init__.py の re-export → from pagefolio.dialogs import BatchOCRDialog（既存 SettingsDialog 等と同記法）"
  prohibitions:
    - "統合サマリを、_confirm_summary_cost（D-14 過大入力警告）を経ずにクラウドAPIへ自動送信してはならない（明示同意なしの外部送信禁止・security-canon: threat_model T-04-01 で追跡）"
    - "統合サマリを、バッチ完了時に自動生成してはならない（手動トリガーのみ・D-13、不要な送信コストを避ける）"
    - "統合サマリで、新規のハード閾値・自動切り詰めロジックを追加してはならない（既存 _confirm_summary_cost の警告のみに留める・D-14）"
    - "新規メニュー項目にアクセラレータキーを設定してはならない（既存 ShortcutsDialog cmd_map の11コマンドと衝突しうる・Pitfall 5）"
    - "バッチOCRは、キュー内容・OCR結果をアプリ再起動を跨いで永続化してはならない（D-11・ダイアログを閉じたら破棄する）"
---

<objective>
`BatchOCRDialog` にファイル横断の統合サマリ生成とファイル別結果閲覧を追加し、新規メニューバー（`tk.Menu`）からの起動導線・Treeview/メニュー用の ttk.Style・後方互換 re-export を配線してバッチOCR機能を完成させる。

Purpose: V180-BATCH-05（複数ファイル横断の統合サマリ・入力過大時の事前警告）を、既存 `_on_summary`/`_summary_worker`/`_confirm_summary_cost` パターンをファイル横断連結（ファイル名見出し・D-15）へ拡張して満たす。加えて D-01 のメニュー起動導線（本プロジェクト初の `tk.Menu`）・Pitfall 4 の Treeview テーマ整合・Pitfall 5 のショートカット衝突回避を実装し、ユーザーが機能到達可能な状態にする（V180-BATCH-01〜05 の user-reachability を担保）。
Output: `pagefolio/dialogs/batch_ocr.py`（サマリ+結果閲覧拡張）・`pagefolio/ui_builder.py`（Treeview/メニュー Style）・`pagefolio/app.py`（メニューバー+起動）・`pagefolio/dialogs/__init__.py`（re-export）・`pagefolio/lang.py`（メニュー/サマリ用キー）・`tests/test_batch_ocr_dialog.py`（サマリ連結テスト）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ocr/04-CONTEXT.md
@.planning/phases/04-ocr/04-RESEARCH.md
@.planning/phases/04-ocr/04-PATTERNS.md

# 拡張対象・analog
@pagefolio/dialogs/batch_ocr.py
@pagefolio/ui_builder.py
@pagefolio/dialogs/__init__.py

# 04-02 SUMMARY（Task 1/2 で確定したダイアログ API）
@.planning/phases/04-ocr/04-02-SUMMARY.md
</context>

## Artifacts this phase produces

このプラン（04-03）が新規に作る/公開するシンボル。plan-review-convergence の source-grounding は本節を新規シンボルの許可リストとして読む。

- `BatchOCRDialog` メソッド（本プランで新規追加）:
  - サマリ: `_format_batch_summary_input`（ファイル名見出し連結・D-15）/ `_on_batch_summary`（手動トリガー・D-13）/ `_batch_summary_worker`（既存 _summary_worker 相当）
  - 結果閲覧: `_on_select_file`（ファイル選択切替）/ `_render_file_result`（_insert_markdown 流用）/ `_on_export_file`（ファイル単位エクスポート）
  - OCRDialog からのコピペ移植（レビュー懸念5・ocr_dialog.py 非変更）: `_insert_markdown`（ocr_dialog.py:1800-1819 と同一挙動）/ `_confirm_summary_cost`（ocr_dialog.py:1237-1265 と同一挙動）/ `_format_pages_text`（ocr_dialog.py:1980-1993 相当）を BatchOCRDialog へ同一シグネチャで自前実装
  - 属性: `self._summary_cancel_flag`（threading.Event・サマリ専用キャンセル。`_on_close`（04-02 実装）はこれも set するよう本プランで拡張）
  - ウィジェット: ファイル選択（ttk.Combobox もしくは tk.Listbox・D-16）/ 「📊 サマリ作成」ボタン
- `pagefolio/ui_builder.py`: Treeview 用 ttk.Style ブロック（`Treeview`/`Treeview.Heading` の configure + `style.map` の selected 色）を `_build_styles` へ追記（テーマ切替時に `_rebuild_ui`→`_build_styles()` 再呼び出しで追随・app.py:613）+ メニューバー構築（`_build_menubar` 相当・`tk.Menu` + `root.config(menu=...)`）
- `pagefolio/app.py`: `_open_batch_ocr`（メニューから BatchOCRDialog を起動・D-04 独立）
- `pagefolio/dialogs/__init__.py`: `from pagefolio.dialogs.batch_ocr import BatchOCRDialog` の re-export 行
- `pagefolio/lang.py` 新規キー: `batch_menu_tools`・`batch_menu_item`（Task 2）・`batch_summary_btn`・`batch_summary_file_header`（`{name}` プレースホルダ）・`batch_export_btn`・`batch_file_select_label`（Task 1）。各キーは追加タスク内で文字列リテラル参照する（`test_no_unused_lang_keys` 段階失敗の回避・レビュー懸念2）

> `_insert_markdown`/`_confirm_summary_cost`/`_format_pages_text` は既存 `OCRDialog`（`ocr_dialog.py`）の同名メソッドと同一挙動の**独立実装（コピペ移植）**であり、`ocr_dialog.py` は本フェーズの `files_modified` に含まれない（OCRDialog の継承やインスタンスメソッドの import 流用はしない。モジュール定数 `SUMMARY_TOO_LONG_CHARS` の import は可・レビュー懸念5）。plan-review-convergence の source-grounding はこれらを「本フェーズが移植する既知シンボル」として本節から許可する。

## Review Incorporation (Antigravity 04-REVIEWS.md)

本プランに対するクロスAIレビュー（Antigravity・04-REVIEWS.md）の actionable finding の反映状況。04-03 はリスク LOW 評価で、以下を反映した。

- **懸念5（MEDIUM）`OCRDialog` インスタンスメソッド流用方式の曖昧さ**: 反映済み。`_insert_markdown`（ocr_dialog.py:1800-1819）・`_confirm_summary_cost`（ocr_dialog.py:1237-1265）・`_format_pages_text`（ocr_dialog.py:1980-1993）を `BatchOCRDialog` へ**同一シグネチャ・同一挙動のコピペ移植**として自前実装することを Task 1 action・acceptance・Artifacts に明記。`ocr_dialog.py` は本フェーズで変更せず、OCRDialog の継承やインスタンスメソッドの import 流用もしない（モジュール定数 `SUMMARY_TOO_LONG_CHARS` の import は可・`git diff --name-only` に `ocr_dialog.py` を含めない）。
- **提案（Treeview 配色のテーマ追随確認）**: 反映済み。Treeview 用 ttk.Style を `ui_builder._build_styles` へ追記し、色は `C` 辞書（`C["BG_PANEL"]`/`C["TEXT_MAIN"]`/`C["ACCENT"]`）を参照する（ハードコード hex 禁止）。テーマ切替時に `_apply_settings`→`_rebuild_ui`→`self._build_styles()`（app.py:613）が再実行され配色が追随することを Task 2 acceptance に明記。
- **懸念2（MEDIUM・`test_no_unused_lang_keys` 段階実装競合）**: 反映済み。Task 1 追加キー（`batch_summary_btn` 等）は Task 1 のコードで、Task 2 追加キー（`batch_menu_tools`/`batch_menu_item`）は Task 2 のコードで、それぞれ同一タスク内で文字列リテラル参照する co-location 規約を明文化。
- **`_on_close` のサマリ連動（懸念1の延長）**: 反映済み。04-02 で実装した `_on_close`（WM_DELETE_WINDOW）を、本プランでサマリ生成中も安全に停止するよう `self._summary_cancel_flag.set()` を加える形へ拡張する（OCRDialog._on_close 1959-1978 が `_summary_cancel_flag` も set するのと同型）。
- 強みとして評価された「手動トリガー＋`_confirm_summary_cost` によるコスト制御（D-14）」「アクセラレータ非設定による ShortcutsDialog 11コマンドとの衝突回避（Pitfall 5）」は既存設計を維持する（構造変更なし）。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ファイル横断統合サマリ + ファイル別結果閲覧</name>
  <files>pagefolio/dialogs/batch_ocr.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/dialogs/batch_ocr.py（04-02 実装済み。self._entries / BatchFileEntry.results を持つ）
    - pagefolio/ocr_dialog.py（_format_pages_text 1980-1993行・_on_summary 2006-2074行・_confirm_summary_cost 1237-1265行・SUMMARY_TOO_LONG_CHARS 73行・_summary_worker 2162行台・_insert_markdown の Markdown 整形描画）
    - pagefolio/md_render.py（parse_markdown。_insert_markdown が消費する整形ロジック）
    - .planning/phases/04-ocr/04-RESEARCH.md（Pattern 7 ファイル横断連結・Code Examples の _confirm_cost/_confirm_summary_cost 拡張ポイント）
    - pagefolio/lang.py（ocr_summary_cost_confirm_msg・ocr_page_separator の再利用可否・batch_ 系キー命名規約）
    - tests/test_lang_parity.py（新規キーは ja/en 両方へ同一キーで追加）
  </read_first>
  <behavior>
    - _format_batch_summary_input(): 完了済み（STATUS_DONE）ファイルのみを対象に、各ファイル先頭へ batch_summary_file_header.format(name=display_name)（例「=== ファイル名.pdf ===」）を挿入し、そのファイルの全ページ本文（_format_pages_text 相当）を連結した1本の文字列を返す（D-15）。完了ファイル0件なら空文字。
    - _on_batch_summary(): 「📊 サマリ作成」手動トリガー（D-13）。完了ファイル0件（連結が空）なら no-op。provider.supports_text_prompt が False なら既存同様エラー表示。クラウド時は _check_cloud_api_key → _confirm_summary_cost(len(連結文字数)) が False なら送信しない（D-14）。len(full_text) > SUMMARY_TOO_LONG_CHARS で追加の過大警告 askyesno。承認後 _batch_summary_worker をワーカースレッド1本 + 世代ガード + サマリ専用キャンセルフラグで実行（既存 _summary_worker と同型）。
    - ファイル別結果閲覧: ファイル選択ウィジェット（ttk.Combobox もしくは tk.Listbox・D-16）で完了ファイルを切替えると _render_file_result が該当 BatchFileEntry.results を _insert_markdown で整形描画する。コピー/保存は raw 維持。_on_export_file はファイル単位で結果をエクスポート（既存エクスポートパターン流用）。
  </behavior>
  <action>
    `pagefolio/dialogs/batch_ocr.py` にファイル横断統合サマリとファイル別結果閲覧を追加する。まず、OCRDialog のインスタンスメソッドを `BatchOCRDialog` へ**コピペ移植**する（レビュー懸念5・`ocr_dialog.py` は本フェーズで変更しない・継承/cross-import しない）: `_format_pages_text`（`ocr_dialog.py:1980-1993` と同一挙動）・`_confirm_summary_cost`（`ocr_dialog.py:1237-1265` と同一挙動・既存 `ocr_summary_cost_confirm_msg` を再利用）・`_insert_markdown`（`ocr_dialog.py:1800-1819` と同一挙動・`md_render.parse_markdown` を消費）を同一シグネチャで自前定義する。過大入力の閾値は新規に定義せず、既存モジュール定数 `SUMMARY_TOO_LONG_CHARS`（`ocr_dialog.py:73`・状態を持たない定数のためモジュール import で再利用可・新規ハード閾値の追加ではない・D-14 順守）を用いる。

    `_format_batch_summary_input`（Pattern 7）は移植した `_format_pages_text` と同型の「区切り + 本文」ロジックをファイル軸へもう一段適用し、STATUS_DONE のファイルごとに `batch_summary_file_header.format(name=entry.display_name)` 見出しを先頭挿入して連結する（D-15）。完了ファイル0件なら空文字を返す。

    `_on_batch_summary`（「📊 サマリ作成」ボタン・D-13 手動トリガー）は `ocr_dialog.py:_on_summary`（2006-2074）のゲート構造を踏襲する: 連結が空なら no-op、`getattr(provider, "supports_text_prompt", False)` が False ならエラー表示、クラウド時は `_check_cloud_api_key`（04-02 で移植済み）→ 移植した `_confirm_summary_cost(len(full_text))`（D-14・合計文字数を渡すだけ・新規閾値ロジック追加禁止）→ `len(full_text) > SUMMARY_TOO_LONG_CHARS` で追加 askyesno。承認後、`_batch_summary_worker` をワーカースレッド1本 + 世代ガード + サマリ専用キャンセルフラグ `self._summary_cancel_flag`（`_summary_worker`/`_summary_cancel_flag` と同型）で実行し、`provider.complete_text_ex` を呼ぶ。バッチ完了時の自動生成はしない（D-13）。

    04-02 で実装した `_on_close`（WM_DELETE_WINDOW ハンドラ）を、サマリ生成中も安全に停止するよう拡張する: 既存の `_batch_cancel_flag.set()`/`_file_cancel_flag.set()` に加えて `self._summary_cancel_flag.set()` を呼ぶ（`OCRDialog._on_close` 1959-1978 が `_summary_cancel_flag` も set するのと同型・レビュー懸念1 の延長）。

    ファイル別結果閲覧（D-16）: ファイル選択ウィジェット（`ttk.Combobox` もしくは `tk.Listbox`・Claude's Discretion）を追加し、選択変更で `_render_file_result` が該当 `BatchFileEntry.results` を移植した `_insert_markdown` で整形描画する。コピー/保存は raw 維持。`_on_export_file` はファイル単位でエクスポートする（既存エクスポートパターン流用）。

    `pagefolio/lang.py` の LANG["ja"]/LANG["en"] **両方**へ `batch_summary_btn`・`batch_summary_file_header`（`{name}` プレースホルダ）・`batch_export_btn`・`batch_file_select_label` を同一キー・同一プレースホルダで追加する。各追加キーは本 Task 1 のコードで文字列リテラル参照する（消費タスクと同一タスクでキー追加・レビュー懸念2）。既存 `ocr_summary_cost_confirm_msg` は再利用する（新規サマリコストキーは追加しない）。`ruff check . && ruff format .` が通ることを確認する。fenced code は書かない。
  </action>
  <verify>
    <automated>pytest tests/test_lang_parity.py tests/test_batch_ocr_dialog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/batch_ocr.py` に `def _insert_markdown` / `def _confirm_summary_cost` / `def _format_pages_text` の自前定義があり（コピペ移植）、`class BatchOCRDialog(` が `OCRDialog` を基底クラスに持たない（インスタンスメソッドを継承・import して流用しない・レビュー懸念5）。ただしモジュールレベル定数 `SUMMARY_TOO_LONG_CHARS` の import は許可（インスタンスメソッドではなく状態を持たない定数のため・D-14 の同一閾値再利用）
    - `git diff --name-only` に `pagefolio/ocr_dialog.py` が含まれない（ocr_dialog.py 非変更・レビュー懸念5）
    - `pagefolio/dialogs/batch_ocr.py` に `_format_batch_summary_input` があり、STATUS_DONE ファイルごとに見出し（`batch_summary_file_header`）を挿入して連結する（D-15）
    - `_on_batch_summary` が `_confirm_summary_cost(` を連結後の合計文字数で呼び、完了0件は no-op・自動生成しない（D-13/D-14）
    - `pagefolio/dialogs/batch_ocr.py` に新規のハード閾値・自動切り詰めロジックが追加されていない（移植した `_confirm_summary_cost` + 既存定数 `SUMMARY_TOO_LONG_CHARS` のみ・D-14）
    - `_on_close`（04-02 実装）が `self._summary_cancel_flag.set()` も呼ぶよう拡張されている（レビュー懸念1 の延長・grep で確認）
    - ファイル別結果閲覧が移植した `_insert_markdown` を用い、エクスポートがファイル単位である（D-16）
    - `pagefolio/lang.py` の ja/en キー集合が一致し batch_summary_* 系キーが両方に存在（`pytest tests/test_lang_parity.py -x` green・未使用キー assertion なし）
    - `pytest tests/test_batch_ocr_dialog.py -x` green・`pytest` フルスイート green・`ruff check .` exit 0
  </acceptance_criteria>
  <done>ファイル横断統合サマリ（見出し連結・過大警告・手動トリガー）とファイル別結果閲覧（_insert_markdown 流用・ファイル単位エクスポート）が動作。lang ja/en 一致。</done>
</task>

<task type="auto">
  <name>Task 2: メニューバー起動導線 + Treeview/メニュー Style + 後方互換 re-export</name>
  <files>pagefolio/ui_builder.py, pagefolio/app.py, pagefolio/dialogs/__init__.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/ui_builder.py（_build_styles 15-93行。Treeview/メニュー用スタイルを新規ブロックとして追記する。既存スタイルは変更しない）
    - pagefolio/app.py（__init__・キーバインド・既存 OCR 起動導線。メニューバーは本プロジェクト初導入のため前例なし）
    - pagefolio/dialogs/__init__.py（既存 re-export 記法 10-17行。BatchOCRDialog を同記法で追加）
    - pagefolio/dialogs/shortcuts.py（ShortcutsDialog の cmd_map・11コマンドのキーバインド。アクセラレータ衝突回避の確認対象・Pitfall 5）
    - pagefolio/themes.py（C 辞書のテーマ色キー: BG_PANEL/TEXT_MAIN/ACCENT/WARNING）
    - .planning/phases/04-ocr/04-RESEARCH.md（Pitfall 4 Treeview テーマ整合・Pitfall 5 メニュー衝突・Open Question 1）
    - tests/test_lang_parity.py（menu 用 lang キーは ja/en 両方へ追加）
  </read_first>
  <action>
    `pagefolio/ui_builder.py` の `_build_styles` に、既存スタイルを一切変更せず Treeview 用の新規ブロックを追記する（Pitfall 4・レビュー提案）: `style.configure("Treeview", background=C["BG_PANEL"], foreground=C["TEXT_MAIN"], fieldbackground=C["BG_PANEL"])`・`style.configure("Treeview.Heading", ...)`（テーマ整合の見出し色）・`style.map("Treeview", background=[("selected", C["ACCENT"])])`。色は必ず `C` 辞書参照とし、ハードコード hex を使わない。D-06 の失敗/エラー行警告色は Treeview の `tag_configure("error", foreground=C["WARNING"])` 側（04-02 で行を tag 付け済み）に対応する配色をここで一貫させる。テーマ切替（dark/light）時は既存経路 `_apply_settings`→`_rebuild_ui`→`self._build_styles()`（`app.py:613` で `_rebuild_ui` が `_build_styles()` を再呼び出し）が新しい `C` 値で Treeview スタイルを再構成するため、配色がテーマへ追随する（新規メソッドではなく `_build_styles` へ追記することでこの再適用経路に自動的に乗る・レビュー提案の確認点）。

    メニューバーを新設する（D-01・本プロジェクト初の `tk.Menu`）。`app.py` の `__init__` 相当で `menubar = tk.Menu(root); root.config(menu=menubar)` を配線し、最小構成のトップレベルメニュー（`batch_menu_tools` ラベル・例「ツール」）に単一項目「バッチOCR」（`batch_menu_item`）を追加して `command=self._open_batch_ocr` へ束ねる。**アクセラレータキーは設定しない**（クリック起動のみ・Pitfall 5/Open Question 1: 既存 `ShortcutsDialog` cmd_map の11コマンドとの衝突を構造的に回避）。他機能のメニュー化は本フェーズのスコープ外とする（最小メニューバー）。

    `app.py` に `_open_batch_ocr` を追加する: `from pagefolio.dialogs import BatchOCRDialog` を関数内 import し、`BatchOCRDialog(self.root, app=self, lang=self.lang, font_func=self._font)` を起動する。`self.doc`/`self.filepath` は渡さない（D-04・独立起動）。

    `pagefolio/dialogs/__init__.py` に既存 re-export 記法（10-17行）と同じ形で `from pagefolio.dialogs.batch_ocr import BatchOCRDialog  # noqa: F401` を追加する。

    `pagefolio/lang.py` の LANG["ja"]/LANG["en"] **両方**へ `batch_menu_tools`・`batch_menu_item` を同一キーで追加する。`ruff check . && ruff format .` が通ることを確認する。fenced code は書かない。
  </action>
  <verify>
    <automated>pytest tests/test_lang_parity.py tests/test_imports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ui_builder.py` の `_build_styles` 内に `style.configure("Treeview"` と `style.map("Treeview"` が追加され、色に `C["BG_PANEL"]`/`C["TEXT_MAIN"]`/`C["ACCENT"]` を使う（ハードコード hex を使わない・Pitfall 4）
    - Treeview スタイルが `_build_styles` に置かれ、テーマ切替時に `_rebuild_ui`→`_build_styles()`（app.py:613）で再適用される経路に乗る（別メソッドへ隔離して再呼び出しから外れていないこと・レビュー提案の確認点）
    - `pagefolio/app.py` に `tk.Menu(` と `root.config(menu=` と `_open_batch_ocr` があり、`_open_batch_ocr` が `BatchOCRDialog(` を起動し `self.doc`/`self.filepath` を渡さない（D-04）
    - `pagefolio/app.py` のメニュー項目に `accelerator=` が設定されていない（Pitfall 5・クリック起動のみ）
    - `pagefolio/dialogs/__init__.py` に `from pagefolio.dialogs.batch_ocr import BatchOCRDialog` があり `from pagefolio.dialogs import BatchOCRDialog` が成功する
    - `pagefolio/lang.py` の ja/en キー集合が一致し batch_menu_* が両方に存在（`pytest tests/test_lang_parity.py -x` green）
    - `pytest tests/test_imports.py -x` green・`pytest` フルスイート green・`ruff check .` exit 0
  </acceptance_criteria>
  <done>メニューバー（tk.Menu・アクセラレータなし）から BatchOCRDialog を独立起動でき、Treeview/メニューのテーマ Style と後方互換 re-export が配線される。</done>
</task>

<task type="auto">
  <name>Task 3: test_batch_ocr_dialog.py — サマリ連結・過大警告・到達性テスト追加</name>
  <files>tests/test_batch_ocr_dialog.py</files>
  <read_first>
    - tests/test_batch_ocr_dialog.py（04-02 Task 3 の E2E テスト。ここへサマリ・到達性テストを追記する）
    - tests/test_ocr_engine.py（FakeProvider の complete_text_ex 67-72行・supports_text_prompt。サマリ生成の決定的偽実装）
    - pagefolio/dialogs/batch_ocr.py（Task 1 実装済み。_format_batch_summary_input / _on_batch_summary）
    - .planning/phases/04-ocr/04-RESEARCH.md（Test Map の test_batch_summary_concat）
    - .planning/phases/04-ocr/04-VALIDATION.md（Per-Task Verification Map・Manual-Only Verifications）
  </read_first>
  <action>
    `tests/test_batch_ocr_dialog.py` に統合サマリと到達性のテストを追記する（04-02 の FakeProvider・隠しルート・monkeypatch 方針を流用）。

    次のテストを定義する:
    - `test_batch_summary_concat`: 2ファイル分の完了 results を持つ状態で `_format_batch_summary_input()` が各ファイル先頭に `batch_summary_file_header`（`=== name ===`）見出しを挿入して連結する（D-15）。連結文字列に両ファイルの display_name 見出しと本文が含まれる。
    - `test_batch_summary_zero_completed_noop`（エッジ）: 完了ファイル0件で `_on_batch_summary` を呼んでも `complete_text_ex` が呼ばれず no-op（zero-completed）。
    - `test_batch_summary_oversized_warns`（エッジ）: 連結文字数が過大な場合に `_confirm_summary_cost`（+ SUMMARY_TOO_LONG_CHARS 警告）を経由し、承認しなければ送信しない（D-14）。FakeProvider の complete_text_ex 呼び出し有無で検証する。
    - `test_batch_dialog_reexport`（到達性）: `from pagefolio.dialogs import BatchOCRDialog` が成功する（後方互換 re-export・reachability smoke）。

    各テストは決定的・実 fitz/実ネットワーク非依存。実ウィンドウでの D&D・Treeview 配色・メニュー起動の目視は手動検証（04-VALIDATION.md）に委ねる。
  </action>
  <verify>
    <automated>pytest tests/test_batch_ocr_dialog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_batch_ocr_dialog.py` に test_batch_summary_concat / test_batch_summary_zero_completed_noop / test_batch_summary_oversized_warns / test_batch_dialog_reexport が追加されている
    - `test_batch_summary_concat` が見出し連結（`=== name ===` + 本文）を検証（D-15・V180-BATCH-05）
    - `test_batch_summary_oversized_warns` が過大入力時の警告ゲート（承認なしは非送信）を検証（D-14）
    - `test_batch_dialog_reexport` が `from pagefolio.dialogs import BatchOCRDialog` の成功を検証
    - `pytest tests/test_batch_ocr_dialog.py -x` green・`pytest` フルスイート green・`ruff check tests/test_batch_ocr_dialog.py` exit 0
  </acceptance_criteria>
  <done>サマリ連結（見出し）・過大警告ゲート・zero-completed no-op・後方互換 re-export の到達性テストが green。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BatchOCRDialog → クラウド OCR API（サマリ） | ファイル横断連結テキストが外部 API へ https 送信される境界。`_confirm_summary_cost`（D-14）が同意ゲート |
| メニューバー → BatchOCRDialog 起動 | メインアプリのメニュー操作が独立ダイアログを起動する。self.doc/filepath は渡さない（D-04 独立境界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | ファイル横断連結テキストの意図しないクラウド送信（大量連結でのコスト超過・情報漏洩） | high | mitigate | `_confirm_summary_cost`（D-14・連結後の合計文字数を表示）+ `SUMMARY_TOO_LONG_CHARS` 過大警告を送信前に表示し、手動トリガー（D-13）のみで実行。自動生成しない |
| T-04-02 | Information Disclosure | サマリ worker 経路・provider への APIキー混入 | medium | mitigate | 既存「キー名のみログ・値は非出力」規約を踏襲。構築済み provider のみを使用し生キーをログしない |
| T-04-05 | Denial of Service / UX | メニューアクセラレータと既存 ShortcutsDialog キーバインドの衝突（誤発火） | low | mitigate | メニュー項目にアクセラレータキーを設定せずクリック起動のみ（Pitfall 5・Open Question 1）。cmd_map 11コマンドとの重複を構造的に回避 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 新規パッケージ導入なし（RESEARCH Package Legitimacy Audit: 該当なし）。install タスクなし |
</threat_model>

<verification>
- `pytest tests/test_batch_ocr_dialog.py -x` が green
- `pytest tests/test_lang_parity.py tests/test_imports.py -x` が green
- `pytest` フルスイートが green
- `ruff check . && ruff format .` がクリーン
- 手動検証（/gsd-verify-work 時・04-VALIDATION.md）: メニュー「バッチOCR」起動・Treeview 配色（dark/light）・実サマリ出力品質
</verification>

<success_criteria>
- バッチ完了後、完了ファイル結果を閲覧でき、ファイル横断の統合サマリを見出し連結（D-15）で生成でき、過大入力時に事前警告が表示される（V180-BATCH-05・D-13/D-14）
- ユーザーは新規メニュー項目「バッチOCR」（アクセラレータなし・クリック起動）から独立ダイアログを起動できる（D-01・D-04）
- Treeview/メニューのテーマ Style が C 辞書に整合し（Pitfall 4）、`from pagefolio.dialogs import BatchOCRDialog` が成功する
- バッチOCR機能（V180-BATCH-01〜05）がユーザーから到達可能な状態で完成する
</success_criteria>

<output>
Create `.planning/phases/04-ocr/04-03-SUMMARY.md` when done
</output>
