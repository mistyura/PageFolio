---
phase: 04-ocr
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - pagefolio/dialogs/batch_ocr.py
  - pagefolio/lang.py
  - tests/test_batch_ocr_dialog.py
autonomous: true
requirements:
  - V180-BATCH-01
  - V180-BATCH-02
  - V180-BATCH-03
  - V180-BATCH-04
must_haves:
  truths:
    - "ユーザーは D&D と「+ ファイル追加」ボタン（askopenfilenames）の両方で複数PDFをキューへ投入できる（D-02）"
    - "非PDF（SUPPORTED_EXTENSIONS 外）のドロップは除外され、除外時のみ警告表示される（既存 dnd_pdf_only 流用）"
    - "同一パスの重複投入は enqueue_files により1件に集約される"
    - "キュー一覧は ファイル名/状態/ページ内進捗 の3列 Treeview で表示される（D-05）"
    - "全体進捗（ファイル x/合計）はダイアログ上部の進捗バー+ラベルで、キュー行の状態列（ページ軸）とは別軸で二段表示される（D-08）"
    - "ファイル単位の fatal（サーキットブレーカー等）発生時は自動スキップし、残りファイルの処理が継続する（D-09・V180-BATCH-03）"
    - "「バッチ中止」1ボタンでバッチ全体フラグとファイル内フラグの両方が set され、実行中ファイルが停止し次ファイルへ進まない（D-10・Pitfall 2）"
    - "ファイル間の fitz.open/get_pixmap はメインスレッド逐次で直列化され、ワーカーへ渡すのは b64 のみ（Pitfall 1・落とし穴3）"
    - "BatchOCRDialog は self.app.doc / self.app.filepath を一切参照しない独立設計である（D-04）"
    - "OCRRunEngine はファイルごとに新規生成され、次ファイルで使い回されない（Pattern 1・D-11 外挿）"
    - "OCR実行開始前に、対象ファイル数・総ページ数・概算コストの集約確認ダイアログが1回表示され、キャンセルで開始されない（D-03）"
    - statement: "壊れた/開けないPDFは事前ページ数スキャンで STATUS_ERROR 行として警告色表示され、削除のみ可能（実ウィンドウでの目視は手動検証）"
      verification: backstop
    - statement: "実ウィンドウでの tkinterdnd2 ネイティブ D&D 投入は手動検証（ヘッドレステスト不可）"
      verification: backstop
    - statement: "バッチ開始前にキューが空の場合は開始せず、案内表示のみ行う（空キュー始点）"
      verification: backstop
  artifacts:
    - pagefolio/dialogs/batch_ocr.py
    - tests/test_batch_ocr_dialog.py
  key_links:
    - "BatchOCRDialog のファイルループ → ファイルごとに OCRRunEngine 新規生成（provider は使い回し可・per-run 状態は持たない）"
    - "on_complete/on_fatal/on_cancelled 内で self._batch_cancel_flag.is_set() を確認してから次ファイルの _start_file_engine を呼ぶ（Pitfall 2 の一手）"
    - "BatchState.files_done()（ファイル軸）と OCRRunEngine.progress_count()（ページ軸）を別軸で更新（D-08・Pitfall 3）"
    - "D&D/ファイル選択 → SUPPORTED_EXTENSIONS フィルタ → enqueue_files（04-01 の純関数）"
  prohibitions:
    - "バッチOCRは、集約コスト確認（D-03）を経ずにクラウドAPIへページ画像を自動送信してはならない（明示同意なしの外部送信禁止・security-canon: threat_model T-04-01 で追跡）"
    - "バッチOCRは、ファイル並列化（ThreadPoolExecutor でファイルごとに fitz.open/get_pixmap を並列実行）してはならない（fitz スレッド間共有禁止・落とし穴3、ファイル間は必ず逐次）"
    - "バッチOCRは、ファイル失敗時に別プロバイダへ自動フォールバック切替してはならない（Phase 2 の送信先確認再提示フローと混同せず、D-09 の単純スキップに限定する）"
    - "「バッチ中止」をファイル内 _cancel_flag のみで実装してはならない（次ファイルが自動開始する・Pitfall 2）。必ずバッチ全体フラグを独立して持つ"
    - "OCRRunEngine を次ファイルで使い回してはならない（results/errors 等の per-run 状態がファイル間でリークする）"
    - "失敗ファイルの一括再試行機能（UI・ロジックとも）を本フェーズで実装してはならない（D-12: Deferred Idea として明示的にスコープ外。CONTEXT.md の Deferred Ideas 参照）"
---

<objective>
新設 `BatchOCRDialog`（`pagefolio/dialogs/batch_ocr.py`・`tk.Toplevel`）のコア機能を実装する: 複数ファイルのキュー投入（D&D + ファイル選択）、3列 Treeview キュー一覧と二段進捗表示、集約コスト確認、ファイル間逐次のファイルループコントローラ（ファイルごとに `OCRRunEngine` 新規生成）、2階層キャンセル、ファイル失敗の自動スキップ継続。

Purpose: Phase 3 で抽出済みの `OCRRunEngine` を「ファイルごとに新規生成して回す」外側ループでラップし、単一ファイル OCR の全ロジック（リトライ・サーキットブレーカー・埋め込みテキストスキップ）を流用する。V180-BATCH-01（複数ファイル投入）・V180-BATCH-02（キュー状態/全体進捗）・V180-BATCH-03（失敗分離継続）・V180-BATCH-04（全体/個別キャンセル）を満たす。落とし穴3（fitz スレッド共有）・4（キャンセルスコープ）・5（二軸進捗）の回避策を実装レベルで固定する。
Output: `pagefolio/dialogs/batch_ocr.py`（`BatchOCRDialog`）・`pagefolio/lang.py`（バッチ用キー ja/en 追加）・`tests/test_batch_ocr_dialog.py`（E2E モックテスト）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ocr/04-CONTEXT.md
@.planning/phases/04-ocr/04-RESEARCH.md
@.planning/phases/04-ocr/04-PATTERNS.md

# 直接の再利用資産・analog（実装の出発点）
@pagefolio/batch_ocr_state.py
@pagefolio/ocr_engine.py
@pagefolio/dialogs/merge.py
@tests/test_ocr_engine.py

# 04-01 SUMMARY（純ロジック層の確定 API）
@.planning/phases/04-ocr/04-01-SUMMARY.md
</context>

## Artifacts this phase produces

このプラン（04-02）が新規に作る/公開するシンボル。plan-review-convergence の source-grounding は本節を新規シンボルの許可リストとして読む。

- 新規ファイル: `pagefolio/dialogs/batch_ocr.py`
- 新規ファイル: `tests/test_batch_ocr_dialog.py`
- クラス: `BatchOCRDialog(tk.Toplevel)`
- `BatchOCRDialog` メソッド（本プランで新規実装）:
  - 投入/スキャン: `_on_batch_dnd_drop` / `_on_add_files` / `_enqueue_files` / `_scan_page_counts`
  - Treeview/進捗: `_build`（Treeview 3列構築）/ `_refresh_queue_row` / `_update_overall_progress`
  - 集約コスト確認: `_confirm_batch_cost`
  - ファイルループ: `_on_start_batch` / `_start_file_engine` / `_render_next_page_for` / `_advance_to_next_file`
  - 完了理由別アダプタ: `_on_file_complete` / `_on_file_fatal` / `_on_file_cancelled`（世代ガード + after(0) + tk.TclError 握り）
  - キャンセル: `_on_batch_cancel`
  - 属性: `self._batch_cancel_flag`（threading.Event・バッチ全体）/ `self._file_cancel_flag`（threading.Event・ファイル内）/ `self._batch_state`（BatchState）/ `self._entries`（BatchFileEntry のリスト）/ `self._run_gen`（世代カウンタ）
- `pagefolio/lang.py` 新規キー（ja/en 同時追加・下記 Task 1 で列挙）
- `pagefolio/dialogs/__init__.py` への `BatchOCRDialog` re-export は 04-03 で実施（本プランでは未追加）

> ファイル横断統合サマリ・ファイル別結果閲覧・メニュー起動導線は 04-03 の Artifacts 節を参照。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BatchOCRDialog 骨格・キュー投入・Treeview・二段進捗・集約コスト確認</name>
  <files>pagefolio/dialogs/batch_ocr.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/batch_ocr_state.py（04-01 実装済み。BatchFileEntry/BatchState/enqueue_files/STATUS 定数の確定 API）
    - pagefolio/dialogs/merge.py（MergeOrderDialog の中央配置 Toplevel 構築 22-55行・事前ページ数スキャン 35-43行の analog）
    - pagefolio/app.py（_on_dnd_drop 321-354行の tk.splitlist + SUPPORTED_EXTENSIONS フィルタパターン）
    - pagefolio/file_ops.py（_open_file 464-480行の askopenfilenames パターン）
    - pagefolio/ocr_dialog.py（_confirm_cost 1194-1235行・_estimate_cost・二段進捗 progress_bar/progress_var の参照元）
    - pagefolio/ui_builder.py（_build_styles・既存 ttk.Style 定義。Treeview スタイルは 04-03 で追加するため本プランは既定スタイルで構築してよい）
    - pagefolio/lang.py（LANG ja/en の既存キー・命名規約 batch_ 追加方針。ocr_cost_confirm_title/ocr_cost_confirm_msg/dnd_pdf_only/confirm_title は再利用）
    - tests/test_lang_parity.py（ja/en キー数一致 + 使用キーの存在チェック。新規キーは必ず ja/en 両方へ同一キーで追加）
  </read_first>
  <behavior>
    - 構築: BatchOCRDialog(parent, app, lang="ja", font_func=None) は tk.Toplevel を継承し、MergeOrderDialog と同じ中央配置・grab_set・configure(bg=C["BG_DARK"]) で構築される。self.app.doc / self.app.filepath を一切参照しない（D-04）。
    - キュー投入: _enqueue_files(paths) は SUPPORTED_EXTENSIONS でフィルタ → 04-01 の enqueue_files で dedup 追加 → 各新規パスに _scan_page_counts（fitz.open→len→close、失敗は page_count=0 かつ STATUS_ERROR）→ Treeview 行を追加。非PDF のみ/空は警告 or no-op。
    - Treeview: ファイル名/状態/ページ内進捗 の3列（D-05）。STATUS_ERROR 行は tag で警告色（tag 設定自体は本プランで行い、実配色スタイルは 04-03 と整合）。
    - 二段進捗: 上部に ttk.Progressbar + 「ファイル {done}/{total}」ラベル（BatchState.files_done() 由来・ファイル軸）。キュー行の状態列（ページ軸）とは別軸（D-08・Pitfall 3）。
    - 集約コスト確認: _confirm_batch_cost() は queued エントリの合計ページ数（sum of page_count、STATUS_ERROR 除外）を _confirm_cost 相当ロジック（ocr_cost_confirm_msg 再利用）へ渡し bool を返す。クラウドプロバイダ時のみ表示。
  </behavior>
  <action>
    `pagefolio/dialogs/batch_ocr.py` を新規作成する。先頭3行 MIT ヘッダー + モジュール docstring（バッチOCRダイアログ・独立設計 D-04 の旨）。`BatchOCRDialog(tk.Toplevel)` を定義し、`MergeOrderDialog.__init__`（merge.py:22-55）の中央配置パターン（super().__init__(parent)・self._L=LANG[lang]・self.configure(bg=C["BG_DARK"])・grab_set()・update_idletasks 後の中央 geometry・minsize）をそのまま踏襲する。`self.app` は保持するが `self.app.doc`/`self.app.filepath` は参照しない（D-04・独立性を構造的に担保）。

    状態属性を初期化する: `self._entries`（BatchFileEntry のリスト）・`self._batch_state`（後続 Task 2 の実行時に BatchState(total) を生成するため None 始点でよい）・`self._batch_cancel_flag`/`self._file_cancel_flag`（threading.Event）・`self._run_gen`（int, 0）。

    UI を `_build` で構築する: 上部に全体進捗（`ttk.Progressbar` + 「ファイル {done}/{total}」ラベル）、中央に3列 `ttk.Treeview`（columns=("file","status","progress")・見出しは lang キー）、下部に「+ ファイル追加」ボタン（`filedialog.askopenfilenames` → `_on_add_files` → `_enqueue_files`）・「削除」ボタン（D-07: 待機中行のみ有効・実行中/完了/失敗/エラー行は無効）・「▶ 実行」ボタン・「バッチ中止」ボタン（初期 disabled）。Treeview の Canvas/Frame を tkinterdnd2 の drop ターゲットへ登録し `_on_batch_dnd_drop`（app.py:321-354 の tk.splitlist + SUPPORTED_EXTENSIONS フィルタをそのまま複製・除外時のみ dnd_pdf_only 警告）へ配線する。

    `_enqueue_files(paths)` は SUPPORTED_EXTENSIONS フィルタ後、04-01 の `enqueue_files` で dedup 追加し、新規パスごとに `_scan_page_counts`（merge.py:35-43 の fitz.open→len→close パターン・try/except で開けない場合は page_count=0 かつ status=STATUS_ERROR）を実行して Treeview 行を追加する（STATUS_ERROR 行は tag=("error",) を付与）。`_update_overall_progress` は `BatchState.files_done()` から「ファイル {done}/{total}」を反映する（ページ軸のカウンタから逆算しない）。

    `_confirm_batch_cost` は STATUS_ERROR を除く queued エントリの合計 page_count を求め、`ocr_dialog.py:_confirm_cost` と同型のロジック（プロバイダ別ホスト表示・`_estimate_cost`・`messagebox.askyesno`）で集約確認を表示し bool を返す。新規 lang キーを追加せず既存 `ocr_cost_confirm_title`/`ocr_cost_confirm_msg` を再利用する（count に合計ページ数を渡す）。

    `pagefolio/lang.py` の LANG["ja"] と LANG["en"] の**両方**へ、次のバッチ用キーを同一キー名・同一プレースホルダで追加する（`test_lang_parity.py` のキー数一致・使用キー存在チェックを満たす）: `batch_dialog_title`・`batch_col_file`・`batch_col_status`・`batch_col_progress`・`batch_add_files_btn`・`batch_remove_btn`・`batch_start_btn`・`batch_cancel_btn`・`batch_overall_progress`（`{done}`/`{total}` プレースホルダ）・`batch_status_pending`・`batch_status_running`・`batch_status_done`・`batch_status_failed`・`batch_status_error`・`batch_empty_queue_msg`・`batch_scan_error`。メニュー項目文言（`batch_menu_*`）は 04-03 で追加するため本プランでは追加しない。

    `ruff check . && ruff format .` が通ることを確認する。fenced code は書かない。
  </action>
  <verify>
    <automated>pytest tests/test_lang_parity.py tests/test_imports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/batch_ocr.py` が存在し `from pagefolio.dialogs.batch_ocr import BatchOCRDialog` が成功する
    - `BatchOCRDialog` のソースに `self.app.doc` / `self.app.filepath` への参照が存在しない（D-04・grep で不在）
    - `pagefolio/dialogs/batch_ocr.py` に `ttk.Treeview(` と `askopenfilenames` と `tk.splitlist` と `enqueue_files(` が現れる（Treeview 3列・ファイル選択・D&D・dedup 投入の配線）
    - `pagefolio/lang.py` の LANG["ja"] と LANG["en"] のキー集合が一致し、上記 batch_ 系キーが両方に存在する（`pytest tests/test_lang_parity.py -x` が green）
    - `_confirm_batch_cost` が既存 `ocr_cost_confirm_msg` を再利用し、STATUS_ERROR を集計から除外する
    - `pytest` フルスイートが green（既存回帰なし）・`ruff check .` が exit 0
  </acceptance_criteria>
  <done>BatchOCRDialog 骨格が独立設計（D-04）で構築され、D&D/ファイル選択投入・3列 Treeview・二段進捗・集約コスト確認・lang キー（ja/en 一致）が揃う。lang_parity/imports green。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ファイルループコントローラ・per-file Engine 生成・2階層キャンセル・失敗自動スキップ</name>
  <files>pagefolio/dialogs/batch_ocr.py</files>
  <read_first>
    - pagefolio/dialogs/batch_ocr.py（Task 1 の骨格。ここへファイルループを追加する）
    - pagefolio/ocr_dialog.py（_start_worker_thread 1664-1694行・_render_next_page 1600-1642行・_on_engine_complete/_on_engine_fatal/_on_engine_cancelled の世代ガード+after(0)+tk.TclError パターン 1740-1769行）
    - pagefolio/ocr_engine.py（OCRRunEngine コンストラクタ契約 74-124行・on_complete/on_fatal/on_cancelled は最終ワーカーから同期的に呼ばれる 246-259行）
    - pagefolio/ocr.py（_start_ocr の provider/concurrency 構築 541-624行。バッチはこれをバッチ開始時に1回だけ構築し全ファイルで使い回す・Assumption A2）
    - .planning/phases/04-ocr/04-RESEARCH.md（Pattern 1 二層ループ・Pattern 3 2階層キャンセル・Pitfall 1/2/3）
  </read_first>
  <behavior>
    - 開始: _on_start_batch はキューが空なら開始せず案内（batch_empty_queue_msg）。クラウド時は _confirm_batch_cost が False なら開始しない（D-03）。BatchState(total=queued件数) を生成し _batch_cancel_flag.clear() 後、先頭ファイルから _start_file_engine を呼ぶ。
    - per-file Engine: _start_file_engine(entry) は _file_cancel_flag.clear() 後、OCRRunEngine を**ファイルごとに新規生成**（provider は使い回し、run_pages=そのファイルの全ページ、cancel_flag=self._file_cancel_flag、on_complete/on_fatal/on_cancelled=ファイル用アダプタ）。engine.start() 後 _render_next_page_for(entry) で producer 連鎖を開始（fitz.open→get_pixmap をメインスレッド逐次・b64 のみ enqueue）。
    - 前進判定: _on_file_complete/_on_file_fatal/_on_file_cancelled は世代ガード + after(0) ディスパッチ。BatchState を mark_completed/mark_failed/mark_cancelled で更新し、Treeview 行状態と全体進捗を更新後、_advance_to_next_file を呼ぶ。_advance_to_next_file は self._batch_cancel_flag.is_set() なら停止、そうでなければ次の待機ファイルの _start_file_engine を呼ぶ（Pitfall 2 の一手）。
    - 失敗自動スキップ: _on_file_fatal は該当ファイルを STATUS_FAILED にし、確認を挟まず次ファイルへ進む（D-09）。フォールバック機構（ocr_fallback.py）は一切使わない。
    - 2階層キャンセル: _on_batch_cancel は self._batch_cancel_flag.set() と self._file_cancel_flag.set() の両方を呼ぶ（D-10・Pattern 3）。実行中ファイルは停止し、_advance は次へ進まない。
    - fitz は全ファイルを通じてメインスレッド逐次（Pitfall 1・落とし穴3。ThreadPoolExecutor でのファイル並列化はしない）。
  </behavior>
  <action>
    Task 1 の `BatchOCRDialog` にファイルループコントローラを追加する。バッチ開始時に `ocr.py:_start_ocr`（541-624行）と同型のロジックで provider/concurrency を1回だけ構築し（Assumption A2: プロバイダ設定はファイル間で不変のため使い回す）、`self.provider`/`self.concurrency` に保持する。API キー解決は `_resolve_api_key` + `OCRAPIKeyError` の既存パターンを踏襲し、キー文字列は Engine へ渡さず構築済み provider のみを渡す（T-04-02 情報漏洩防止）。

    `_start_file_engine(entry)` は `ocr_dialog.py:_start_worker_thread`（1664-1694）の生成パターンを**ファイルごとに繰り返す**（Pattern 1）: `self._file_cancel_flag.clear()` 後に `OCRRunEngine(provider=self.provider, prompt=self._ocr_prompt, run_pages=entry の全ページ, concurrency=self.concurrency, cancel_flag=self._file_cancel_flag, on_success/on_page_error/on_progress=ファイル/ページ進捗更新, on_complete=lambda: self._on_file_complete(entry, gen), on_cancelled=..., on_fatal=lambda msg,kind: self._on_file_fatal(entry, msg, kind, gen), breaker_threshold=...)` を新規生成し、`engine.start()` → `_render_next_page_for(entry, gen)` で producer 連鎖を開始する。producer は `ocr_dialog.py:_render_next_page`（1600-1642）と同型に、メインスレッドで1ページずつ fitz レンダリング → `try_enqueue(self._engine.queue, (page_idx, b64))`（Full なら after(100) 再スケジュール）で積む。fitz.open/get_pixmap は**ファイル間も含めてメインスレッド逐次**にすること（Pitfall 1・落とし穴3。ThreadPoolExecutor でファイルを並列化しない）。

    完了理由別アダプタ `_on_file_complete`/`_on_file_fatal`/`_on_file_cancelled` は `ocr_dialog.py:1740-1769` の世代ガード（`gen != self._run_gen` なら return）+ `after(0, ...)` + `try/except tk.TclError` パターンを踏襲する。各アダプタは `BatchState` を該当メソッドで更新し、Treeview 行状態（STATUS_DONE/STATUS_FAILED）と全体進捗ラベルを更新した後、`_advance_to_next_file` を呼ぶ。`_advance_to_next_file` は必ず `self._batch_cancel_flag.is_set()` を先に確認し、set 済みなら停止（次ファイルを開始しない・Pitfall 2 の一手）、未設定なら次の STATUS_PENDING ファイルの `_start_file_engine` を呼ぶ。`_on_file_fatal` は確認を挟まず自動スキップして次へ進む（D-09）。フォールバック機構（`ocr_fallback.py`）は使わない（Anti-Pattern・D-09 の単純スキップに限定）。

    `_on_batch_cancel`（「バッチ中止」ボタン）は `self._batch_cancel_flag.set()` と `self._file_cancel_flag.set()` の両方を呼ぶ（D-10・Pattern 3）。`ruff check . && ruff format .` が通ることを確認する。fenced code は書かない。
  </action>
  <verify>
    <automated>pytest tests/test_batch_ocr_dialog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dialogs/batch_ocr.py` に `OCRRunEngine(` の生成が `_start_file_engine` 内にあり、ファイルループから繰り返し呼ばれる（per-file 新規生成）
    - `_on_batch_cancel` が `self._batch_cancel_flag.set()` と `self._file_cancel_flag.set()` の両方を呼ぶ（grep で2フラグの set 確認）
    - `_advance_to_next_file`（相当メソッド）が `self._batch_cancel_flag.is_set()` を確認してから次ファイルの Engine 生成を呼ぶ
    - `pagefolio/dialogs/batch_ocr.py` に `ThreadPoolExecutor` によるファイル並列 fitz レンダリングが存在しない（grep で ThreadPoolExecutor がファイルループに使われていない・Pitfall 1）
    - `pagefolio/dialogs/batch_ocr.py` に `ocr_fallback` の import/使用が存在しない（D-09・Anti-Pattern 回避）
    - `pytest tests/test_batch_ocr_dialog.py -x` が green・`pytest` フルスイートが green・`ruff check .` exit 0
  </acceptance_criteria>
  <done>ファイルループが per-file Engine 新規生成でファイル間逐次実行し、2階層キャンセル・失敗自動スキップ継続が動作。E2E テスト green。</done>
</task>

<task type="auto">
  <name>Task 3: test_batch_ocr_dialog.py — E2E モックテスト（失敗分離・キャンセル・エッジ）</name>
  <files>tests/test_batch_ocr_dialog.py</files>
  <read_first>
    - tests/test_ocr_engine.py（FakeProvider パターン 43-72行・薄い producer スタブ・send_sentinels/try_enqueue の使い方をそのまま流用）
    - pagefolio/dialogs/batch_ocr.py（Task 1/2 実装済み。テスト対象のファイルループ・キャンセル・進捗更新）
    - pagefolio/ocr_engine.py（OCRRunEngine の完了理由別コールバック契約）
    - .planning/phases/04-ocr/04-RESEARCH.md（Validation Architecture の Test Map・test_file_failure_continues / test_batch_cancel_stops_all）
    - .planning/phases/04-ocr/04-VALIDATION.md（Per-Task Verification Map・Manual-Only Verifications）
  </read_first>
  <action>
    `tests/test_batch_ocr_dialog.py` を新規作成する。`tests/test_ocr_engine.py` の `FakeProvider`（決定的・実API非依存で fatal を注入できる side_effect 対応）と薄い producer スタブ方針をそのまま流用する。実 tkinterdnd2 のネイティブ D&D と Treeview の実配色は手動検証（04-VALIDATION.md）に委ね、本テストはファイルループのオーケストレーション（複数ファイル分の OCRRunEngine 生成・失敗分離・キャンセル・進捗集計）を検証する。

    Tk 依存部は `tk.Tk()` の後 `root.withdraw()` した隠しルート上に `BatchOCRDialog` を構築して駆動する（この環境は Windows 実機で GUI 利用可能・既存フェーズと同じ前提）。fitz レンダリングは monkeypatch で決定的な canned b64 を返すよう差し替え、実 PDF/実ネットワークに依存させない。

    次のテストを定義する（RESEARCH の Test Map の関数名に合わせる）:
    - `test_file_failure_continues`: 2ファイルのうち先頭ファイルの provider が fatal（例: サーキットブレーカー閾値まで OCRRetryableError）を返す場合、先頭が STATUS_FAILED になり、2番目のファイルの OCRRunEngine が新規生成され処理が継続する（V180-BATCH-03・D-09）。BatchState.failed==1・completed==1。
    - `test_batch_cancel_stops_all`: 実行中に `_on_batch_cancel` を呼ぶと `_batch_cancel_flag` と `_file_cancel_flag` の両方が set され、実行中ファイルが停止し、次ファイルの Engine が新規生成されない（V180-BATCH-04・D-10・Pitfall 2）。
    - `test_cancel_before_start_noop`（エッジ）: 実行前に `_on_batch_cancel` を呼んでも例外にならず、開始しても新規 Engine を起動しない（cancel-before-start）。
    - `test_all_files_fail`（エッジ）: 全ファイルが fatal でも各ファイルが STATUS_FAILED になりバッチが停止せず全件処理される（BatchState.failed==総数）。
    - `test_progress_never_exceeds_total`（エッジ）: 進捗更新後 BatchState.files_done() が total_files を超えない。

    各テストは決定的（実 fitz/ネットワーク非依存）で、ワーカースレッドの join にタイムアウト（例: 10秒）を設ける。
  </action>
  <verify>
    <automated>pytest tests/test_batch_ocr_dialog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_batch_ocr_dialog.py` が存在し、test_file_failure_continues / test_batch_cancel_stops_all / test_cancel_before_start_noop / test_all_files_fail / test_progress_never_exceeds_total を定義する
    - テストは `test_ocr_engine.py` の FakeProvider を流用し、実 fitz/実ネットワークに依存しない（fitz レンダリングは monkeypatch）
    - `test_file_failure_continues` が「先頭失敗 → 2番目の OCRRunEngine 新規生成 → 継続」を検証している（V180-BATCH-03）
    - `test_batch_cancel_stops_all` が「2フラグ同時 set → 実行中停止 → 次ファイル未開始」を検証している（V180-BATCH-04）
    - `pytest tests/test_batch_ocr_dialog.py -x` が green・`pytest` フルスイートが green・`ruff check tests/test_batch_ocr_dialog.py` exit 0
  </acceptance_criteria>
  <done>E2E モックテスト5件が green。失敗分離継続・2階層キャンセル・エッジ（cancel-before-start/all-fail/progress上限）が実API非依存で検証される。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー選択/D&D パス → BatchOCRDialog | 未検証のローカルファイルパスが投入される。SUPPORTED_EXTENSIONS フィルタ + fitz.open の try/except を通す |
| BatchOCRDialog → クラウド OCR API | ページ画像（b64 PNG）が外部 API へ https 送信される境界。集約コスト確認（D-03）が同意ゲート |
| メインスレッド ↔ consumer ワーカースレッド | ワーカーへ渡すのは b64 のみ。fitz.Document/生設定/APIキーは渡さない（落とし穴3・T-04-02） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | ファイルループの意図しない大量クラウド送信（複数ファイル×大量ページ） | high | mitigate | `_confirm_batch_cost`（D-03）が対象ファイル数・総ページ数・概算コストを開始前に1回集約表示し、キャンセルで開始しない。フォールバックによる無断別ベンダー送信は本フェーズ対象外（Anti-Pattern で禁止・D-09） |
| T-04-02 | Information Disclosure | ファイル完了/失敗ログ・provider 構築経路への APIキー混入 | medium | mitigate | 既存「キー名のみログ・値は非出力」規約（`_resolve_api_key`/`OCRAPIKeyError`）をファイルループ経路へ適用。Engine へは構築済み provider のみ渡し、生キー文字列を渡さない（`ocr_engine.py` D-02 契約） |
| T-04-04 | Tampering/Input Validation | 壊れた/非PDF/悪意あるファイル名 | low | mitigate | SUPPORTED_EXTENSIONS フィルタ + 事前ページ数スキャンの try/except（STATUS_ERROR 表示）。ローカルパスのみ扱い外部（ネットワーク経由）入力は受けない |
| T-04-03 | Tampering | パストラバーサル的懸念 | low | accept | ローカル filedialog/D&D 経由パスのみ。既存 `fitz.open` のエラーハンドリングで十分。追加対策不要 |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 新規パッケージ導入なし（RESEARCH Package Legitimacy Audit: 該当なし）。install タスクなし |
</threat_model>

<verification>
- `pytest tests/test_batch_ocr_dialog.py -x` が green
- `pytest tests/test_lang_parity.py tests/test_imports.py -x` が green（ja/en キー一致・後方互換 import）
- `pytest` フルスイートが green
- `ruff check . && ruff format .` がクリーン
- 手動検証（/gsd-verify-work 時・04-VALIDATION.md）: 実ウィンドウでの D&D 投入・Treeview 失敗行の警告色（dark/light 両テーマ）
</verification>

<success_criteria>
- ユーザーは D&D + ファイル選択で複数PDFをキューへ投入でき、非PDF/重複が除外される（V180-BATCH-01）
- 3列 Treeview キュー一覧 + ファイル軸/ページ軸の二段進捗表示が動作する（V180-BATCH-02・D-08）
- ファイル失敗は自動スキップされ残りが継続する（V180-BATCH-03・D-09）
- 「バッチ中止」1ボタンで2階層フラグが同時 set され、実行中停止・次ファイル未開始（V180-BATCH-04・D-10）
- fitz はファイル間もメインスレッド逐次（落とし穴3）・Engine はファイルごと新規生成（D-11 外挿）
</success_criteria>

<output>
Create `.planning/phases/04-ocr/04-02-SUMMARY.md` when done
</output>
