# Phase 4: バッチ複数ファイルOCR - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-15
**Phase:** 4-バッチ複数ファイルOCR
**Areas discussed:** 起動導線・投入方式, キュー一覧の表示・操作, 失敗・キャンセルの挙動, 統合サマリ・結果閲覧

---

## 起動導線・投入方式

| Option | Description | Selected |
|--------|-------------|----------|
| 新規メニュー項目「バッチOCR」 | ツールバー/メニューに独立した入り口を新設し、専用ダイアログを開く | ✓ |
| ファイル→開く の隣に配置 | 「ファイルを開く」の近くに配置し、複数選択ファイルダイアログから直接キュー生成 | |

**User's choice:** 新規メニュー項目「バッチOCR」
**Notes:** 単一ファイルOCR（既存🔍ボタン）とは明確に別動線にする。

| Option | Description | Selected |
|--------|-------------|----------|
| D&D + 「追加」ボタン両方 | ダイアログ内キュー領域への直接D&Dと、複数選択filedialogの「+ファイル追加」ボタンを両方用意 | ✓ |
| D&D のみ | ドラッグ&ドロップのみでキュー追加 | |
| 「+ ファイル追加」ボタンのみ | filedialog.askopenfilenamesのみ | |

**User's choice:** D&D + 「追加」ボタン両方
**Notes:** 既存 file_drop.py の splitlist パターンを流用する。

| Option | Description | Selected |
|--------|-------------|----------|
| 一括確認ダイアログを出す | 実行前に全ファイルのページ数を事前取得し、ファイル数・総ページ数・概算コストを集約表示 | ✓ |
| 出さず、ファイルごとに既存の _confirm_cost をそのまま表示 | 事前スキャン不要でシンプルだが確認ダイアログがファイル数分連続する | |

**User's choice:** 一括確認ダイアログを出す
**Notes:** FEATURES.md Anti-Featuresの推奨方式。

| Option | Description | Selected |
|--------|-------------|----------|
| 完全に独立 | バッチはself.docを一切触らず、ユーザーが明示的にファイルパスで選ぶ | ✓ |
| 現在開いているファイルを自動でキューの先頭に追加 | 便利だが未保存変更との矛盾リスクがある | |

**User's choice:** 完全に独立
**Notes:** 編集中の未保存内容との衝突を構造的に避ける。

---

## キュー一覧の表示・操作

| Option | Description | Selected |
|--------|-------------|----------|
| ファイル名・状態・ページ内進捗の3列 | Treeviewで最小構成 | ✓ |
| 上記3列＋ファイルサイズ列 | 情報量は増えるが列も増える | |

**User's choice:** ファイル名・状態・ページ内進捗の3列

| Option | Description | Selected |
|--------|-------------|----------|
| 行の文字色を警告色（C["WARNING"]）にする | 既存テーマ辞書のWARNING色をTreeviewタグで適用 | ✓ |
| アイコン/絵文字（❌）を状態列に付与 | テーマ色に依存しない | |
| 両方 | 最も目立つが実装コストが一番高い | |

**User's choice:** 行の文字色を警告色（C["WARNING"]）にする

| Option | Description | Selected |
|--------|-------------|----------|
| 削除のみ（待機中ファイルのみ可） | 誤投入したファイルを実行前に除外できれば十分 | ✓ |
| 削除＋並び替え（MergeOrderDialogパターン流用） | 処理順序をユーザーが制御したい場合に対応 | |
| 何もなし（追加順のみ、削除不可） | 最小実装 | |

**User's choice:** 削除のみ（待機中ファイルのみ可）

| Option | Description | Selected |
|--------|-------------|----------|
| ダイアログ上部に固定の進捗バー + 「ファイル x/合計」ラベル | ページ内進捗とは別軸で常時可視化 | ✓ |
| キュー一覧の状態列だけで判断させる | 実装は簡単だが全体進捗の一目把握が困難 | |

**User's choice:** ダイアログ上部に固定の進捗バー + 「ファイル x/合計」ラベル
**Notes:** PITFALLS.md落とし穴5（ファイル/ページ二軸の進捗矛盾）への対応として二段表示を明示的に採用。

---

## 失敗・キャンセルの挙動

| Option | Description | Selected |
|--------|-------------|----------|
| 自動スキップして次ファイルへ進む | V180-BATCH-03要件文言に直接対応 | ✓ |
| 1件目の失敗で一時停止し確認 | 大量ファイル処理では不便 | |

**User's choice:** 自動スキップして次ファイルへ進む

| Option | Description | Selected |
|--------|-------------|----------|
| 「バッチ中止」ボタン1つのみ | 自動スキップ方針と整合。UIがシンプル | ✓ |
| 「このファイルをスキップ」と「バッチ中止」の2ボタン | 手動で今のファイルだけ飛ばすニーズに対応できるがUI要素が増える | |

**User's choice:** 「バッチ中止」ボタン1つのみ
**Notes:** 内部実装は2階層キャンセルフラグ（バッチ全体 + 既存ファイル内）を維持し、UI露出のみ1ボタンに絞る。

| Option | Description | Selected |
|--------|-------------|----------|
| 保持され、閲覧/サマリ生成に引き続き使える | 完了済み分の作業を無駄にしない | ✓ |
| ダイアログを閉じると破棄（保存・引き継ぎなし） | キュー永続化なし方針とは一致するがシンプルすぎる | |

**User's choice:** 保持され、閲覧/サマリ生成に引き続き使える

| Option | Description | Selected |
|--------|-------------|----------|
| 含めない | V180-BATCH-01〜05の要件文言に含まれない。スコープを最小に保つ | ✓ |
| 含める（失敗一覧から「再試行」ボタン） | 便利性は上がるが実装コスト増 | |

**User's choice:** 含めない
**Notes:** FEATURES.mdでDifferentiator/P2扱い。Deferred Ideasへ記録。

---

## 統合サマリ・結果閲覧

| Option | Description | Selected |
|--------|-------------|----------|
| 手動トリガー（「📊 サマリ作成」ボタン） | 既存単一ファイルOCRと同じUX。不要な送信コストを避ける | ✓ |
| バッチ完了時に自動でサマリ生成を開始 | 手間は減るが意図しない課金が発生しうる | |

**User's choice:** 手動トリガー（「📊 サマリ作成」ボタン）

| Option | Description | Selected |
|--------|-------------|----------|
| 既存 _confirm_summary_cost の文字数表示をもって代える | 新規閾値ロジックを作らず既存UXをそのまま拡張 | ✓ |
| 別途のハード閾値を設け、超えたら切り詰めて警告する | 新規切り詰めロジックの実装・テストコストが発生する | |

**User's choice:** 既存 _confirm_summary_cost の文字数表示をもって代える

| Option | Description | Selected |
|--------|-------------|----------|
| ファイル名見出しを挿入して連結 | LLMがファイル境界を認識しやすくなりサマリ品質が上がる | ✓ |
| 単純連結（区切りなし） | 実装は最小だがファイル境界が失われる | |

**User's choice:** ファイル名見出しを挿入して連結

| Option | Description | Selected |
|--------|-------------|----------|
| ファイル選択ドロップダウン/リストで切替え、既存のOCRDialog結果ビューを流用 | 既存表示ロジックをそのまま再利用できる | ✓ |
| 全ファイルを一本のロングテキストに連結表示 | 切替えなしで一度に見られるが大量ファイル時にスクロールが長大化する | |

**User's choice:** ファイル選択ドロップダウン/リストで切替え、既存のOCRDialog結果ビューを流用

---

## Claude's Discretion

- ページ内進捗表示の具体的なウィジェット（プログレスバー併記 vs テキストのみ）
- `PipelineState` の上位に置く薄い純関数層（`BatchState` 等）の内部データ構造・関数API設計
- キュー投入時の事前ページ数スキャンでファイルが壊れている場合のエラー表示文言
- ファイル選択ドロップダウン/リストのウィジェット種類（`ttk.Combobox` vs `tk.Listbox`）
- 新規メニュー項目「バッチOCR」のメニューバー上の配置位置

## Deferred Ideas

- 失敗ファイルの一括再試行機能 — FEATURES.md で Differentiator/P2 扱い。将来の別フェーズ/別タスク候補
