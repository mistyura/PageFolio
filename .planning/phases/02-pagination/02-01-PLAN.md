---
phase: 02-pagination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/pagination.py
  - tests/test_pagination.py
  - tests/conftest.py
autonomous: true
requirements:
  - V16-UI-03
must_haves:
  truths:
    - "window_bounds が件数で窓を区切り、最終窓の端数を実ページ数でクランプする（D-10）"
    - "to_global / to_local の往復変換が任意の窓 start で恒等になる"
    - "window_for_page が current_page を含む窓の先頭（page_size 倍数）を返す（D-11 の基礎）"
    - "clamp_window_start が削除・件数変更後に window_start を有効な窓先頭へ寄せる"
    - "to_global による D&D ドロップ先換算が窓末尾でも全ページ範囲にクランプされ正しい全ページ位置になる（SC3・D-06）"
    - "selected_pages が窓またぎでも全ページ index を保持し、窓ローカル照合でも全ページ index と整合する（SC4・D-07）"
    - "window_nav_state が単一窓で (False, False) を返す（D-09）"
    - "window_label が 1 始まり・端数最終窓で正しい範囲文字列を返す（D-09/D-10）"
  artifacts:
    - path: "pagefolio/pagination.py"
      provides: "Tk 非依存の窓計算・local↔global 変換・件数クランプの純関数群（件数クランプ関数 clamp_page_size をフェーズ内固定名で同モジュールに集約）"
      contains: "def window_bounds"
      min_lines: 40
    - path: "tests/test_pagination.py"
      provides: "純ロジック層の境界値・往復不変条件・D&D 換算・窓またぎ選択照合のヘッドレス unit テスト（RESEARCH L398-403 の named クラスを含む）"
      contains: "class TestWindowBounds"
      min_lines: 60
    - path: "tests/conftest.py"
      provides: "多ページ doc フィクスチャ（47 ページ）"
      contains: "def large_pdf_doc"
  key_links:
    - from: "tests/test_pagination.py"
      to: "pagefolio/pagination.py"
      via: "from pagefolio.pagination import window_bounds, to_global, to_local, window_for_page, clamp_window_start, window_label, window_nav_state, clamp_page_size"
      pattern: "from pagefolio.pagination import"
---

<objective>
本フェーズの中核である「表示窓のローカル位置 ↔ 全ページインデックス」変換を、Tkinter 非依存の純関数群として新規モジュール `pagefolio/pagination.py` に切り出し、`tests/test_pagination.py` で全境界値・往復不変条件をヘッドレス検証する。

これは 02-VALIDATION.md / 02-RESEARCH.md `## Validation Architecture` が指定する Wave 0（テスト基盤）に相当する。後続プラン（02・03）の描画窓化・D&D 換算・選択照合・窓追従はすべてこの純関数層を参照するため、最初に実装とテストを確定させる。

Purpose: index 変換ロジックを 1 箇所に集約し（02-CONTEXT.md integration point）、散在による窓またぎバグを構造的に防ぐ。描画から独立しているため、ウィジェットを起動せずに数学的正しさを徹底検証できる。
Output: 純関数 7 個（window_bounds / to_global / to_local / window_for_page / clamp_window_start / window_label / window_nav_state）＋件数クランプ純関数 1 個（フェーズ内で名称固定）と、それを検証する 8 テストクラス（RESEARCH の要件→テストマップ L398-403 の named クラス TestWindowBounds=SC1 / TestPageSizePersist=SC2 / TestDndIndexConvert=SC3 / TestSelectionAcrossWindows=SC4 / TestWindowFollow=D-11 / TestNavState=D-09 を含む）、および 47 ページの多ページフィクスチャ。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pagination/02-CONTEXT.md
@.planning/phases/02-pagination/02-RESEARCH.md
@.planning/phases/02-pagination/02-PATTERNS.md
@.planning/phases/02-pagination/02-VALIDATION.md
@pagefolio/viewer.py
@tests/test_viewer.py
@tests/conftest.py
</context>

<artifacts_this_phase_produces>
> 本フェーズ（Phase 2）全体が新規作成するシンボルの一覧。このプラン（02-01）が作るものは ★ で示す。

**新規ファイル:**
- ★ `pagefolio/pagination.py` — 純ロジック変換層モジュール
- ★ `tests/test_pagination.py` — 純ロジック層 unit テスト

**新規純関数（すべて Tk 非依存・引数→戻り値・状態非依存。最終名は実装裁量だが本一覧の名称を推奨）:**
- ★ `window_bounds(window_start, page_size, n_pages) -> (lo, hi)`
- ★ `to_global(local_pos, window_start) -> int`
- ★ `to_local(global_idx, window_start) -> int`
- ★ `window_for_page(page_idx, page_size) -> start`
- ★ `clamp_window_start(window_start, page_size, n_pages) -> start`
- ★ `window_label(window_start, page_size, n_pages) -> str`
- ★ `window_nav_state(window_start, page_size, n_pages) -> (prev_enabled, next_enabled)`
- ★ `clamp_page_size(value) -> int`（件数クランプ純関数。**フェーズ内で 1 つの確定名 `clamp_page_size` に固定**し、他の純関数と同じ `pagefolio/pagination.py` に集約。02-02 / 02-03 はこの確定名で import する・W1）

**新規 self.* 属性（02-02 で初期化）:** `self._page_window_start`（窓オフセット）、`self._page_size`（表示件数）、`self.page_size_var`（IntVar・02-03）
**新規設定キー（02-02）:** `thumb_page_size`（既定 20）
**新規ウィジェット参照（02-03）:** ナビフッター行の ◀ / ▶ ボタン・範囲ラベル・件数 Spinbox
**追加 LANG キー（02-03）:** 範囲ラベル雛形・件数ラベル等（ja/en 同一キー）

**新規テストフィクスチャ:**
- ★ `large_pdf_doc`（conftest.py・47 ページ）— 窓化境界値テスト用
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pagination.py 純関数層を実装し test_pagination.py で全境界値を検証</name>
  <files>pagefolio/pagination.py, tests/test_pagination.py</files>
  <read_first>
    - pagefolio/viewer.py:40-49（`_render_preview_pixmap`）— 倣うべき Tk 非依存純関数の作法（docstring で責務明記・引数のみ参照・戻り値で返す）
    - .planning/phases/02-pagination/02-RESEARCH.md `## Architecture Patterns` Pattern 1 と `## Code Examples`（7 関数のシグネチャと本体案）
    - .planning/phases/02-pagination/02-RESEARCH.md `## Validation Architecture` の「検証対象の純ロジックヘルパー」表・「検証すべき不変条件・境界値・プロパティ」節（全境界値の根拠）
    - .planning/phases/02-pagination/02-PATTERNS.md `### pagefolio/pagination.py`・`### tests/test_pagination.py`
    - tests/test_viewer.py:1-62（`_make_stub` 方式の純ロジック検証パターン。モジュール関数なら直接 import で更に軽量）
  </read_first>
  <behavior>
    - window_bounds(20, 20, 47) は (20, 40) を返す（中間窓）
    - window_bounds(40, 20, 47) は (40, 47) を返す（端数最終窓・D-10）
    - window_bounds(0, 20, 20) は (0, 20) を返す（件数 = 全ページ・単一窓）
    - window_bounds(0, 50, 20) は (0, 20) を返す（件数 > 全ページ）
    - window_bounds(0, 20, 0) は (0, 0) を返す（doc 未オープン）
    - 任意 g（0..n-1）と任意 start で to_global(to_local(g, start), start) == g（往復不変条件）
    - 窓内任意ローカル l（0..size-1）で to_local(to_global(l, start), start) == l
    - window_for_page(0, 20)==0, window_for_page(19, 20)==0, window_for_page(20, 20)==20, window_for_page(46, 20)==40, window_for_page(25, 10)==20
    - clamp_window_start(40, 20, 40)==20（n が 47→40 に減り最終窓 start=40 が無効 → 窓2 へ）、clamp_window_start(20, 20, 20)==0（n が 25→20 に減り → 窓1 へ）
    - window_nav_state(0, 20, 20) は (False, False)（単一窓・D-09）、中間窓は (True, True)、先頭窓は (False, True)、最終窓は (True, False)
    - window_label(40, 20, 47) は "41" と "47" と "47" を含む 1 始まりの範囲文字列（端数最終窓・D-10）、n_pages==0 では空ドキュメントを示す既定文字列
    - clamp_page_size(5)==10, clamp_page_size(200)==100, clamp_page_size(20)==20, clamp_page_size("30")==30, clamp_page_size("")==20, clamp_page_size(None)==20, clamp_page_size("abc")==20（件数クランプ純ロジック・W1。settings 永続化との結合は 02-02 で検証）
    - D&D 換算（SC3・D-06）: 窓2（window_start=20）でローカル dest=3 → to_global(3, 20)==23、窓末尾ドロップは window_bounds の hi（および n_pages）でクランプして全ページ範囲を超えない
    - 窓またぎ選択（SC4・D-07）: selected_pages={3, 25} を窓2（window_start=20）でローカル照合する場合も、to_global(local, 20) が 23 のとき g==23 が selected_pages に含まれ、各エントリは全ページ index（3 / 25）を保持して書き換わらない
  </behavior>
  <action>
    新規モジュール `pagefolio/pagination.py` を作成し、Tk 非依存の純関数を実装する: `window_bounds(window_start, page_size, n_pages)` は半開区間 (lo, hi) を返し最終窓を n_pages でクランプ（D-10）、`to_global(local_pos, window_start)` は `local_pos + window_start`（D-06）、`to_local(global_idx, window_start)` はその逆、`window_for_page(page_idx, page_size)` は page_idx を含む窓の先頭（page_size 倍数）を返し page_size<=0 では 0、`clamp_window_start(window_start, page_size, n_pages)` は `last_start = ((n_pages-1)//page_size)*page_size` で範囲クランプし n_pages<=0 or page_size<=0 では 0、`window_label(window_start, page_size, n_pages)` は 1 始まり `lo+1` から `hi` までの範囲文字列（n_pages==0 は空ドキュメント表示）、`window_nav_state(window_start, page_size, n_pages)` は `(lo > 0, hi < n_pages)` を返す。
    加えて件数クランプ純関数 `clamp_page_size(value)` を**同モジュールに集約**する（W1: フェーズ内で確定名 `clamp_page_size` に固定し、02-02 / 02-03 はこの名で import）: 引数を int 化して `max(10, min(100, value))` を返し、int 化失敗・None・空文字は `(ValueError, TypeError)` を捕捉して既定 20 を返す（裸 except は使わず必ず例外型を指定・CLAUDE.md）。Tk 値 `var.get()` が空文字で送出しうる TclError は呼び出し側 02-03 でハンドルするため、本純関数は tkinter 非依存のまま ValueError/TypeError のみ捕捉する。各関数は docstring で責務と参照する D-NN を明記し、`fitz` / `tkinter` を import しない（純ロジック・viewer.py:40-49 の作法に倣う）。
    同時に `tests/test_pagination.py` を作成し、`from pagefolio.pagination import ...` で直接 import（Mixin スタブ不要）。テストクラスは RESEARCH の要件→テストマップ L398-403 と 02-VALIDATION.md Per-Task Map に整合させ、named クラスを固定する: `TestWindowBounds`（SC1・端数・件数≧全ページ・n=0/n=1・D-10）、`TestPageSizePersist`（SC2・clamp_page_size の純ロジック 5/200/20/"30"/空/None/非数値。settings 永続化部分は 02-02 で結合検証）、`TestDndIndexConvert`（SC3・dest の to_global 換算と窓末尾の min クランプ・D-06）、`TestSelectionAcrossWindows`（SC4・selected_pages={3,25} を窓2 でローカル照合しても各エントリが全ページ index を保持・D-07）、`TestWindowFollow`（D-11・window_for_page・件数変更で別窓）、`TestNavState`（D-09 単一/中間/先頭/最終窓）に加え、`TestIndexConvert`（往復不変条件をループ網羅）、`TestClampWindowStart`（削除後の正規化）、`TestWindowLabel`（1 始まり・端数・n=0）を含める。境界値は behavior 列と 02-RESEARCH.md `## Validation Architecture` の列挙を網羅する。tests/ は S101 除外済みのため assert 使用可。LANG 文言を直接アサートせず、ラベルは数値部分（"41" / "47" / 区切り）の包含で検証して文言裁量と疎結合にする。
  </action>
  <verify>
    <automated>pytest tests/test_pagination.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_pagination.py -x -q` が exit 0 で全テスト green
    - `window_bounds(40, 20, 47)` が `(40, 47)` を返す（端数最終窓・D-10）
    - `window_bounds(0, 20, 0)` が `(0, 0)` を返す（doc 未オープン）
    - `window_for_page(46, 20)` が `40`、`window_for_page(25, 10)` が `20` を返す（D-11 追従基礎）
    - `clamp_window_start(40, 20, 40)` が `20` を返す（削除後正規化）
    - `clamp_page_size(5)`==`10`、`clamp_page_size(200)`==`100`、`clamp_page_size("")`==`20`、`clamp_page_size("30")`==`30`（件数クランプ純ロジック・W1。02-02 T2 / 02-03 が import する関数名 `clamp_page_size` と一致させること）
    - `window_nav_state(0, 20, 20)` が `(False, False)` を返す（D-09 単一窓）
    - 任意の `g in range(47)` と `start in {0, 20, 40}` で `to_global(to_local(g, start), start) == g`
    - RESEARCH L398-403 の named クラスが存在する: `grep -c 'class TestDndIndexConvert' tests/test_pagination.py` が 1、`grep -c 'class TestSelectionAcrossWindows' tests/test_pagination.py` が 1、`grep -c 'class TestPageSizePersist' tests/test_pagination.py` が 1（SC2/SC3/SC4 を named unit で固定）
    - `ruff check pagefolio/pagination.py tests/test_pagination.py` が exit 0（裸 except なし・未使用 import なし）
  </acceptance_criteria>
  <done>
    pagination.py に窓計算/変換の 7 純関数＋件数クランプ純関数 clamp_page_size が実装され、test_pagination.py の named テストクラス（TestWindowBounds=SC1 / TestPageSizePersist=SC2 / TestDndIndexConvert=SC3 / TestSelectionAcrossWindows=SC4 / TestWindowFollow=D-11 / TestNavState=D-09 ＋ TestIndexConvert / TestClampWindowStart / TestWindowLabel）が全 green。pagination.py は fitz/tkinter を import せず純ロジックのみ。clamp_page_size の名称はフェーズ内で固定（02-02/02-03 と一致）。ruff 通過。
  </done>
</task>

<task type="auto">
  <name>Task 2: conftest.py に 47 ページ多ページ doc フィクスチャを追加</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py:43-53（既存 `sample_pdf_doc` フィクスチャ・3 ページ。新規は同作法で 47 ページ）
    - .planning/phases/02-pagination/02-RESEARCH.md `## Validation Architecture` Wave 0 Gaps（「47 ページ等の多ページ doc フィクスチャを conftest に追加すると境界テストが書きやすい」）
    - .planning/phases/02-pagination/02-PATTERNS.md `### tests/test_pagination.py`（フィクスチャ節）
  </read_first>
  <action>
    `tests/conftest.py` に既存 `sample_pdf_doc`（43-53 行）と同じ作法で新規フィクスチャ `large_pdf_doc` を追加する。`fitz.open()` で 47 ページ（A4・595x842）の Document を生成し各ページに `Page {i+1}` を `insert_text` で描き、`yield doc` 後に `doc.close()` でクリーンアップする。47 ページは端数最終窓（件数 20 → 最終窓 41–47）の境界値検証に対応する。純関数テストの大半は doc 不要（n_pages を引数で渡す）ため、このフィクスチャは将来の窓化描画テスト用に用意する位置づけとする。
  </action>
  <verify>
    <automated>pytest tests/conftest.py tests/test_pagination.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --fixtures 2>/dev/null | grep -q large_pdf_doc` で新フィクスチャが認識される
    - `large_pdf_doc` を使うミニテスト（または既存 test 内）で `len(doc) == 47` が真
    - `pytest` 全スイートが exit 0（既存テストの回帰なし）
    - `ruff check tests/conftest.py` が exit 0
  </acceptance_criteria>
  <done>
    conftest.py に 47 ページの `large_pdf_doc` フィクスチャが追加され、既存フィクスチャと同じ generator + close 作法。`pytest` 全スイート green。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （なし） | 本プランは Tk 非依存の純ロジック + テストのみ。ネットワーク・認証・外部入力・永続化・機密の取り扱いを一切含まない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Denial of Service（ローカル・自己起因） | pagination.py 窓計算（page_size<=0 / n_pages<=0 / 範囲外 window_start） | mitigate | 全純関数で page_size<=0・n_pages<=0 をガードし安全な既定（0 や (0,0)）を返す。`clamp_window_start` が範囲外 start を有効窓先頭へ寄せる。境界値テストで n=0/負値/範囲外を網羅 |

> ASVS Level 1 / Block on: high。**high 相当の脅威は存在しない**。本フェーズはローカル UI のページネーション表示で、認証・セッション・ネットワーク送信・暗号・機密の取り扱いを含まない（02-RESEARCH.md `## Security Domain`）。件数の値域は 02-03 の Spinbox readonly と 02-02 の読み出しクランプで担保し、本プランの純関数は不正引数でも例外を投げず安全側に倒れることをテストで固定する（堅牢性）。
</threat_model>

<verification>
- `pytest tests/test_pagination.py -x -q` 全 green（6 テストクラス）
- `pytest` 全スイート green（既存 test_viewer / test_pdf_ops の回帰なし）
- `ruff check . && ruff format .` 通過
- pagination.py が fitz / tkinter を import していない（純ロジック・grep 確認可）
</verification>

<success_criteria>
- pagination.py に窓計算/変換の 7 純関数＋件数クランプ純関数 `clamp_page_size`（フェーズ内固定名）が実装され、すべて引数→戻り値・状態非依存・Tk 非依存
- test_pagination.py が RESEARCH L398-403 の named クラス（SC1〜SC4・D-09・D-11）を含む全境界値（端数最終窓・往復不変条件・D&D 換算・窓またぎ選択照合・単一窓ナビ・追従・件数クランプ）を網羅し green
- conftest.py に 47 ページ `large_pdf_doc` フィクスチャ
- Wave 0（テスト基盤）が満たされ、後続プラン 02/03 が pagination.py（`clamp_page_size` 含む）を import して窓化できる状態
</success_criteria>

<output>
Create `.planning/phases/02-pagination/02-01-SUMMARY.md` when done
</output>
