---
phase: 04-ui-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/app.py
  - tests/test_v150_regression.py
autonomous: true
requirements: [V171-UIUX-01]
must_haves:
  truths:
    - "build_keysym_from_event が Control ビット付き state と 'o' から '<Control-o>' を、修飾なし state と 'F5' から '<F5>' を返す"
    - "find_duplicate_binding が別コマンドの同一 keysym を検出して衝突コマンド名を返し、非衝突時と空 keysym 時は None を返す"
    - "keysym_to_display が '<Control-o>' を 'Ctrl+O'、'<Shift-Delete>' を 'Shift+Delete'、'<F5>' を 'F5'、空文字を '' に変換する"
    - "_bind_shortcuts() は再呼び出し時に前回バインドした keysym（shift variant 含む）を先に unbind してから新設定で再バインドする（旧キーが残らない）"
    - "アプリ起動時 __init__ から _bind_shortcuts() が呼ばれ、既定 8 種＋カスタム設定のショートカットが従来どおり動作する"
  artifacts:
    - "pagefolio/app.py（build_keysym_from_event / find_duplicate_binding / keysym_to_display 純関数・_bind_shortcuts メソッド・self._default_shortcuts / self._cmd_map / self._bound_keysyms 属性）"
    - "tests/test_v150_regression.py（keysym 変換・重複検出・表示変換の純関数テストクラス）"
  key_links:
    - "PDFEditorApp.__init__ → self._bind_shortcuts()（初回バインド経路）"
    - "self._cmd_map / self._default_shortcuts → _bind_shortcuts() が参照（後続 04-02 の ShortcutsDialog 保存時にも同メソッドを呼ぶ）"
---

<objective>
ショートカット GUI 編集（V171-UIUX-01）の基盤を作る。`app.py:146-186` の `__init__` 直書きバインドロジックを再実行可能な `_bind_shortcuts()` メソッドへ切り出し（D-05）、既存 `merge_shortcuts`/`shift_variant_keysym` と同居する Tk 非依存の純関数 3 種（keysym 組み立て・重複検出・人間可読表示変換）を追加する（D-04/D-07）。純関数は自動テストで固定する。

Purpose: 後続プラン 04-02 の ShortcutsDialog が「保存 → 再バインド」を呼べるようにし、キー変換・重複判定ロジックをテスト可能な純関数として先に確定させる。
Output: `app.py` の純関数 3 種・`_bind_shortcuts()` メソッド・関連インスタンス属性、`test_v150_regression.py` の純関数テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux/04-CONTEXT.md
@.planning/phases/04-ui-ux/04-RESEARCH.md
@.planning/phases/04-ui-ux/04-PATTERNS.md
</context>

## Artifacts this phase produces

このプラン（04-01）が新規に作成するシンボル:
- 純関数（`pagefolio/app.py` モジュールレベル）: `build_keysym_from_event(state, keysym, ...)`、`find_duplicate_binding(shortcuts, cmd_name, new_keysym)`、`keysym_to_display(keysym)`
- メソッド（`PDFEditorApp`）: `_bind_shortcuts(self)`
- インスタンス属性: `self._default_shortcuts`（dict）、`self._cmd_map`（dict）、`self._bound_keysyms`（list）
- テストクラス（`tests/test_v150_regression.py`）: keysym 組み立て・重複検出・表示変換の 3 クラス

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: keysym 組み立て・重複検出・表示変換の純関数を app.py に追加</name>
  <files>pagefolio/app.py, tests/test_v150_regression.py</files>
  <read_first>
    - pagefolio/app.py:35-50（既存 merge_shortcuts / shift_variant_keysym — 同レベルの Tk 非依存純関数。この隣に追加する）
    - .planning/phases/04-ui-ux/04-RESEARCH.md「Pattern 1」および「Common Pitfalls」Pitfall 2（推奨シグネチャと修飾キー単体の扱い）
    - .planning/phases/04-ui-ux/04-PATTERNS.md「pagefolio/app.py（純関数追加）」（推奨シグネチャ）
    - tests/test_v150_regression.py:61-95（既存 TestShortcutMerge — テストの書き方の型）
  </read_first>
  <behavior>
    - build_keysym_from_event: Control ビット(0x4)付き state + 'o' → '<Control-o>'。修飾なし state(0) + 'F5' → '<F5>'。Shift ビット(0x1) + 'Delete' → '<Shift-Delete>'。修飾順は Control, Alt, Shift の順で連結。
    - find_duplicate_binding: shortcuts={'open_file':'<Control-o>','save_file':'<Control-s>'} に対し cmd_name='undo', new_keysym='<Control-o>' → 'open_file' を返す。cmd_name='open_file'（自分自身）→ None。new_keysym='' → None。衝突なし → None。
    - keysym_to_display: '<Control-o>' → 'Ctrl+O'、'<Shift-Delete>' → 'Shift+Delete'、'<Control-S>' → 'Ctrl+S'、'<F5>' → 'F5'、'' → ''。
  </behavior>
  <action>
    `merge_shortcuts`/`shift_variant_keysym`（app.py:35-50）の直後に、Tk を import せず bool/str/dict/int の入出力のみで完結する純関数 3 種をモジュールレベルで追加する。(1) `build_keysym_from_event(state, keysym, shift_mask=0x1, control_mask=0x4, alt_mask=0x20000)`: state ビットマスクから Control/Alt/Shift を検出し、その順で '-' 連結して `<Control-Alt-Shift-KEY>` 形式の Tk keysym 文字列を組み立てる。修飾がなければ `<KEY>` を返す（RESEARCH.md Pattern 1 の推奨シグネチャに準拠。Alt ビットは A1 の想定値 0x20000 を既定に、キーワード引数で上書き可能にする）。(2) `find_duplicate_binding(shortcuts, cmd_name, new_keysym)`: `new_keysym` が空なら None、それ以外は shortcuts を走査し `cmd_name` 以外のコマンドが同一 keysym を持てばその他コマンド名（str）を、なければ None を返す（D-04 の後勝ち上書き防止の判定ロジック）。(3) `keysym_to_display(keysym)`: `<...>` を外して '-' 分割し、末尾をキー・先頭群を修飾子として、修飾子は Control→Ctrl / Alt→Alt / Shift→Shift へ写像、キーは 1 文字なら大文字化して '+' 連結（D-07 の人間可読表記）。空 keysym は空文字を返す。3 関数の docstring は日本語で、それぞれ D-04/D-07 のどの決定に対応するか 1 行で記す（禁止ワード grep 誤検知を避けるため keysym 例の生文字列を docstring 末尾に羅列しない）。
  </action>
  <verify>
    <automated>python -c "from pagefolio.app import build_keysym_from_event, find_duplicate_binding, keysym_to_display; assert build_keysym_from_event(0x4,'o')=='<Control-o>'; assert build_keysym_from_event(0,'F5')=='<F5>'; assert find_duplicate_binding({'open_file':'<Control-o>'}, 'undo', '<Control-o>')=='open_file'; assert find_duplicate_binding({'open_file':'<Control-o>'}, 'open_file', '<Control-o>') is None; assert keysym_to_display('<Control-o>')=='Ctrl+O'; assert keysym_to_display('')==''"</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/app.py` に `def build_keysym_from_event(`、`def find_duplicate_binding(`、`def keysym_to_display(` の 3 定義が存在する
    - 3 関数とも `tkinter` を import せず動作する（モジュールレベル純関数）
    - Task 1 の verify の python ワンライナーが例外なく終了する（全 assert 成立）
    - `ruff check pagefolio/app.py` がクリーン
  </acceptance_criteria>
  <done>keysym 組み立て・重複検出・人間可読表示変換の 3 純関数が app.py に存在し、代表入力に対し期待値を返す。</done>
</task>

<task type="auto">
  <name>Task 2: __init__ 直書きバインドを _bind_shortcuts() へ抽出し再実行可能化</name>
  <files>pagefolio/app.py</files>
  <read_first>
    - pagefolio/app.py:145-186（現状の __init__ 直書き default_shortcuts / cmd_map / バインドループ — 抽出対象そのもの）
    - .planning/phases/04-ui-ux/04-PATTERNS.md「抽出後の _bind_shortcuts()」（unbind してから再バインドする型・Pitfall 1）
    - .planning/phases/04-ui-ux/04-RESEARCH.md「Pattern 2」「Pitfall 1」（再バインド時の unbind 忘れ）
  </read_first>
  <action>
    `__init__`（app.py:145-186）の default_shortcuts 辞書・cmd_map 辞書・バインドループを新設メソッド `_bind_shortcuts()` へ移す。default_shortcuts は `self._default_shortcuts`（8 種: open_file/save_file/undo/redo/save_as/delete/toggle_mode/print_pdf の既定 keysym）、cmd_map は `self._cmd_map`（11 種: 上記 8 種のコールバック + rotate_right=`lambda: self._rotate_selected(90)` / rotate_left=`lambda: self._rotate_selected(270)` / rotate_180=`lambda: self._rotate_selected(180)`）としてインスタンス属性化し、両属性の設定は `_bind_shortcuts()` を呼ぶ前（`__init__` 内）に行う。`_bind_shortcuts()` 本体は、まず `getattr(self, "_bound_keysyms", [])` の各 keysym を try/except（`except Exception` + `logger.debug`）で unbind し（Pitfall 1: 旧キーが残るのを防ぐ）、次に `merge_shortcuts(self._default_shortcuts, self.settings.get("shortcuts", {}))` で有効 shortcuts を作り、各コマンドについて func と keysym があれば `self.root.bind` し（`shift_variant_keysym` の variant も同様にバインド）、実際にバインドした keysym を集めて `self._bound_keysyms` に保存する（変換不能な keysym は既存同様 try/except で `logger.warning`）。`__init__` の該当箇所は `self._bind_shortcuts()` の呼び出し 1 行に置き換える。既存の import（`merge_shortcuts`/`shift_variant_keysym` はモジュール内定義なので import 不要）や `test_v150_regression.py` の `from pagefolio.app import merge_shortcuts, shift_variant_keysym` を壊さないこと（両純関数はモジュールレベルのまま維持）。
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pagefolio/app.py', encoding='utf-8').read())" && python -c "import re,io; s=open('pagefolio/app.py',encoding='utf-8').read(); assert 'def _bind_shortcuts' in s; assert 'self._bind_shortcuts()' in s; assert 'self._cmd_map' in s; assert 'self._default_shortcuts' in s; assert 'self._bound_keysyms' in s"</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/app.py` に `def _bind_shortcuts(self):` が定義されている
    - `__init__` 内に `self._bind_shortcuts()` の呼び出しが存在し、直書きの bind ループが `__init__` から除去されている
    - `_bind_shortcuts` 本体で `self._bound_keysyms` の各要素を unbind してから再バインドしている（unbind → merge_shortcuts → bind の順）
    - `self._default_shortcuts`（8 キー）・`self._cmd_map`（11 キー: rotate_right/rotate_left/rotate_180 を含む）がインスタンス属性として設定される
    - `from pagefolio.app import merge_shortcuts, shift_variant_keysym` が引き続き解決する（`pytest tests/test_v150_regression.py -x -q` がグリーン）
    - `ruff check pagefolio/app.py` がクリーン
  </acceptance_criteria>
  <done>バインド処理が `_bind_shortcuts()` に集約され、`__init__` から初回呼び出しされ、再呼び出し時は旧バインドを解除してから張り直す。</done>
</task>

<task type="auto">
  <name>Task 3: 純関数の回帰テストを test_v150_regression.py へ追加</name>
  <files>tests/test_v150_regression.py</files>
  <read_first>
    - tests/test_v150_regression.py:61-95（既存 TestShortcutMerge — 同ファイル内の隣接テストの型。ここに追加する）
    - tests/test_v150_regression.py:15（`from pagefolio.app import merge_shortcuts, shift_variant_keysym` — import 追加箇所）
  </read_first>
  <action>
    `test_v150_regression.py` の import 行に `build_keysym_from_event, find_duplicate_binding, keysym_to_display` を追加し、`TestShortcutMerge` クラスの隣に 3 つのテストクラスを追加する。(1) `TestBuildKeysymFromEvent`: 修飾なし('F5' → '<F5>')・Control('o' → '<Control-o>')・Shift+Delete('Delete', state=0x1 → '<Shift-Delete>')・Control+Alt+Shift の連結順（Control-Alt-Shift の順序）を検証。(2) `TestFindDuplicateBinding`: 他コマンドとの衝突検出（衝突コマンド名を返す）・自分自身は None・空 keysym は None・衝突なしは None を検証。(3) `TestKeysymToDisplay`: '<Control-o>' → 'Ctrl+O'、'<Shift-Delete>' → 'Shift+Delete'、'<F5>' → 'F5'、'' → '' を検証。全テストは Tk 非依存で unbound function 呼び出し + assert のみ（実 Tk ウィジェット生成なし・既存 TestShortcutMerge と同じ方式）。テスト内では keysym の生文字列を assert の期待値としてのみ使い、コメントに禁止ワードを羅列しない。
  </action>
  <verify>
    <automated>pytest tests/test_v150_regression.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_v150_regression.py` に `TestBuildKeysymFromEvent`・`TestFindDuplicateBinding`・`TestKeysymToDisplay` の 3 クラスが存在する
    - `pytest tests/test_v150_regression.py -x -q` が全件グリーン（既存 TestShortcutMerge/TestDndDestIndex/TestTocPreservation を含む）
    - テストは実 Tk ウィジェットを生成しない（`tkinter` の Tk() 呼び出しがテスト内にない）
    - `ruff check tests/test_v150_regression.py` がクリーン
  </acceptance_criteria>
  <done>keysym 変換・重複検出・表示変換の純関数が回帰テストで固定され、フルにグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザーのキーボード入力 → Tk event → keysym 文字列 | 実キーキャプチャ由来の untrusted 文字列がここで keysym 表現に変換される。ただし本プランは純関数のみで、実際の event 受信は 04-02 の ShortcutsDialog 側 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | build_keysym_from_event / _bind_shortcuts（keysym → コマンド） | low | mitigate | keysym は `self._cmd_map` の固定 11 コマンドへのマッピングにのみ使い、eval/exec/シェル実行に一切渡さない。未知 keysym は bind の try/except で握るだけでコマンド化しない |
| T-04-02 | Tampering | 純関数への任意 state/keysym 入力 | low | accept | 出力は Tk bind 文字列のみで副作用なし。異常入力は Tk 側 bind 失敗として logger.warning に留まる |

パッケージインストールなし（stdlib のみ）のため T-04-SC（サプライチェーン）は非該当。
</threat_model>

<verification>
- `pytest tests/test_v150_regression.py -x -q` がグリーン
- `pytest` フルスイートがグリーン（回帰なし）
- `ruff check . && ruff format .` がクリーン
- 3 純関数が `tkinter` 非依存で単体呼び出し可能
</verification>

<success_criteria>
- keysym 組み立て・重複検出・人間可読表示変換の 3 純関数が app.py に存在し自動テストで固定されている
- `_bind_shortcuts()` が抽出され、`__init__` から初回呼び出しされ、再呼び出しで旧バインドを解除してから張り直す
- 既存の `merge_shortcuts`/`shift_variant_keysym` import・v1.5.0 回帰テストが壊れていない
</success_criteria>

<output>
Create `.planning/phases/04-ui-ux/04-01-SUMMARY.md` when done
</output>
