---
phase: 03-v1-5-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/dnd.py
  - pagefolio/app.py
  - tests/test_v150_regression.py
  - tests/test_pdf_ops.py
autonomous: true
requirements:
  - V171-TEST-01
user_setup: []

must_haves:
  truths:
    - "D&D の挿入位置計算が純関数 compute_dnd_dest_index として抽出され、合成データで直接テストできる（D-13/D-16）"
    - "ショートカットのマージと Shift 大文字補完判定が純関数 merge_shortcuts / shift_variant_keysym として抽出され直接テストできる（D-13）"
    - "削除/結合/分割時の TOC 保持・再採番が FakeApp で回帰検証される（D-13）"
    - "既存 undo 往復テスト（白紙挿入・テキスト透かし・ページ番号）へ内容検証（ページサイズ一致・get_text 抽出）が追加される（D-14）"
    - "抽出リファクタ前後で root.bind / D&D 挙動が不変で、既存テスト群がグリーン"
  artifacts:
    - "pagefolio/dnd.py に compute_dnd_dest_index 純関数"
    - "pagefolio/app.py に merge_shortcuts / shift_variant_keysym 純関数"
    - "tests/test_v150_regression.py（新規・TOC/D&D/ショートカット回帰）"
    - "tests/test_pdf_ops.py の TestContentOpsUndoFix へ内容検証追加"
  key_links:
    - "_dnd_dest_index → compute_dnd_dest_index への委譲（Tk 依存の薄いラッパー化）"
    - "app.py __init__ のショートカットループ → merge_shortcuts / shift_variant_keysym への委譲"
---

<objective>
v1.5.0 で未テストだった 3 機能（TOC 保持・D&D 指定位置挿入・ショートカット動的読込）の回帰テストを整備し、既存 undo 往復テストへ内容検証を足す（V171-TEST-01・D-13〜D-16）。テストゼロの機能は `pagination.py` で確立済みの「純ロジック抽出＋直接テスト」と `TestContentOpsUndoFix` の「FakeApp mixin」を機能ごとに使い分ける（D-13）。追加テストは新規ファイル `tests/test_v150_regression.py` へ分離する（D-15・test_pdf_ops.py 肥大化防止）。本プランは他のページ操作機能プラン（03-01/03-03/03-04）と独立したファイル群を触るため Wave 1 で並行実行できる。

Purpose: v1.5.0 新機能の回帰網を張り、以降のページ操作磨き込みの安全網を先に固める（ROADMAP R-04）。
Output: `compute_dnd_dest_index`（dnd.py）、`merge_shortcuts`/`shift_variant_keysym`（app.py）、`tests/test_v150_regression.py`（新規）、`TestContentOpsUndoFix` への内容検証追加。
</objective>

<artifacts_produced>
## Artifacts this phase produces (このプラン新規シンボル)

- `compute_dnd_dest_index` — module-level 純関数（`pagefolio/dnd.py`・cursor_y + frame_bounds → 挿入 index）
- `merge_shortcuts` — module-level 純関数（`pagefolio/app.py`・default+custom 後勝ちマージ）
- `shift_variant_keysym` — module-level 純関数（`pagefolio/app.py`・Control-小文字 → Shift 大文字版 keysym・対象外 None）
- `tests/test_v150_regression.py` — 新規テストファイル（D-15）
- `TestDndDestIndex` / `TestShortcutMerge` / `TestTocPreservation` — テストクラス（`tests/test_v150_regression.py`）
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-v1-5-0/03-CONTEXT.md
@.planning/phases/03-v1-5-0/03-RESEARCH.md
@.planning/phases/03-v1-5-0/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 純ロジック抽出（compute_dnd_dest_index / merge_shortcuts / shift_variant_keysym）</name>
  <files>pagefolio/dnd.py, pagefolio/app.py</files>
  <read_first>
    - pagefolio/dnd.py（`_dnd_dest_index`:74-93＝抽出元。`winfo_y`/`winfo_height`/`canvasy`/`event` の Tk 依存部と、cy と (fy,fh) 列の比較ロジックの境界）
    - pagefolio/app.py（`__init__` のショートカット読込:127-174。`default_shortcuts` 辞書:128-137・`{**default, **custom}` マージ:140・Shift 補完判定:162-166・`root.bind`:160/167）
    - pagefolio/pagination.py（純ロジック層の設計原則＝module-level 関数・引数→戻り値のみ・self/グローバル非依存。V16-D-01/D-02）
    - .planning/phases/03-v1-5-0/03-RESEARCH.md（Pattern 5・Pattern 6 のコード例）
  </read_first>
  <action>
    `pagefolio/dnd.py` に module-level 純関数 `compute_dnd_dest_index(cursor_y, frame_bounds)` を追加する（RESEARCH Pattern 5 の実装）: `frame_bounds` は `[(y, height), ...]`（窓ローカル順）。空なら None、cursor_y が先頭 y 未満なら 0、末尾 (y+height) 超過なら len、それ以外は各フレーム中点 `fy + fh/2` との比較で index を返す。既存 `_dnd_dest_index(self, event)` は Tk 依存（`winfo_*`/`canvasy`/`event`）を残す薄いラッパーへ改修し、`frame_bounds = [(fr.winfo_y(), fr.winfo_height()) for fr in frames]` を組み立てて `compute_dnd_dest_index(cy, frame_bounds)` へ委譲する。cy 算出（`event.y_root - winfo_rooty()` → `canvasy`）は従来どおり。`pagefolio/app.py` に module-level 純関数 `merge_shortcuts(default_shortcuts, custom_shortcuts)`（後勝ち `{**default_shortcuts, **custom_shortcuts}` を返す）と `shift_variant_keysym(keysym)`（`keysym.startswith("<Control-") and len(keysym) == 11 and keysym[-2].islower()` のとき `keysym[:-2] + keysym[-2].upper() + ">"` を返し、対象外は None）を追加する（RESEARCH Pattern 6）。`__init__` の該当箇所を `shortcuts = merge_shortcuts(default_shortcuts, custom_shortcuts)` に置換し、Shift 補完は `variant = shift_variant_keysym(keysym)` が None でないとき `root.bind(variant, ...)` する薄いループへ変える。`root.bind` の実挙動（既存の try/except・lambda f=func 束縛）は不変に保つ（抽出はロジックのみ・A4）。
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pagefolio/dnd.py',encoding='utf-8').read()); ast.parse(open('pagefolio/app.py',encoding='utf-8').read())" && ruff check pagefolio/dnd.py pagefolio/app.py</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/dnd.py` に `def compute_dnd_dest_index(cursor_y, frame_bounds):` が module-level に存在する
    - `pagefolio/app.py` に `def merge_shortcuts(` と `def shift_variant_keysym(` が module-level に存在する
    - `_dnd_dest_index` が `compute_dnd_dest_index` を呼ぶ（委譲）
    - `__init__` のショートカットループが `merge_shortcuts` と `shift_variant_keysym` を呼ぶ
    - `ruff check pagefolio/dnd.py pagefolio/app.py` が clean・両ファイルが構文エラーなし
    - `pytest`（フルスイート）で既存テストに回帰なし（抽出前後で挙動不変）
  </acceptance_criteria>
  <done>3 純関数が抽出され、既存メソッドは薄いラッパーへ改修。Tk 依存を切り離しつつ実挙動は不変で既存スイートがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: 回帰テスト新規ファイル（D&D 位置・ショートカット・TOC 保持）</name>
  <files>tests/test_v150_regression.py</files>
  <read_first>
    - pagefolio/dnd.py（Task 1 で追加した `compute_dnd_dest_index` のシグネチャ）
    - pagefolio/app.py（Task 1 で追加した `merge_shortcuts` / `shift_variant_keysym` のシグネチャ）
    - pagefolio/page_ops.py（TOC 処理: `_delete_selected` の TOC クリーンアップ:124-128・`_do_merge` の TOC 連結:643-650・`_split_by_range` の TOC 切出し:726-734・`_split_each_page`:770-778）
    - tests/test_pdf_ops.py（`TestContentOpsUndoFix._make_app`:1477-1518 の FakeApp 雛形＝TOC 削除/結合テストに流用）
    - tests/test_pagination.py（純関数テストの記法・合成データで直接呼び出し・Tk/fitz 生成なし）
    - tests/conftest.py（`sample_pdf_doc` / `multi_pdf_files` フィクスチャ）
  </read_first>
  <action>
    `tests/test_v150_regression.py` を新規作成する（D-15）。3 系統を収容: (1) `TestDndDestIndex` — `compute_dnd_dest_index` を合成データで直接テスト（Tk 不要・D-16 の「ドロップ座標→挿入位置計算」範囲。例: 先頭上=0、末尾下=len、各フレーム中点前後で境界。tkinterdnd2 のイベント発火自体は対象外＝手動 QA）。(2) `TestShortcutMerge` — `merge_shortcuts`（custom が default を上書き・後勝ち）と `shift_variant_keysym`（`<Control-o>` → `<Control-O>`、`<Delete>`/`<F5>`/`<Control-S>`（既大文字）等は None）を検証。(3) `TestTocPreservation` — `_make_app`（tests/test_pdf_ops.py の FakeApp をコピー流用・PageOpsMixin/FileOpsMixin/RedactOpsMixin 合成）に、TOC を set_toc したドキュメントを渡し、削除（deleted ページ指す item が除去・残存 item は保持）・結合（`_do_merge` で TOC がページオフセット付きで連結）・分割（`_split_by_range`/`_split_each_page` で範囲内 TOC がページ再採番されて出力）を `doc.get_toc()` で検証する。結合/分割はファイル出力を伴うため `tmp_path` と `multi_pdf_files` を活用（filedialog/simpledialog は monkeypatch で固定入力へ差し替え、または対象メソッドの純粋部分を直接呼ぶ）。D&D 経路は `_do_insert(paths, insert_at)` まで（D-16）を FakeApp で1件検証しても良い。
  </action>
  <verify>
    <automated>pytest tests/test_v150_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_v150_regression.py` が存在し `TestDndDestIndex` `TestShortcutMerge` `TestTocPreservation` を含む
    - `compute_dnd_dest_index([...])` の境界（先頭上=0・末尾下=len・中点境界）を検証している
    - `shift_variant_keysym("<Control-o>")` が `"<Control-O>"`、`shift_variant_keysym("<Delete>")` が None を検証している
    - 削除後に deleted ページを指す TOC item が消え、他 item が残ることを `doc.get_toc()` で検証している
    - `pytest tests/test_v150_regression.py -q` が exit 0
    - `ruff check tests/test_v150_regression.py` が clean
  </acceptance_criteria>
  <done>TOC 保持・D&D 位置・ショートカットの 3 系統回帰テストが新規ファイルでグリーン。</done>
</task>

<task type="auto">
  <name>Task 3: 既存 undo 往復テストへ内容検証を追加（D-14）</name>
  <files>tests/test_pdf_ops.py</files>
  <read_first>
    - tests/test_pdf_ops.py（`TestContentOpsUndoFix`:1469-1571・`test_insert_blank_roundtrip`:1520・`test_watermark_roundtrip`:1536・`test_page_numbers_roundtrip`:1558。既存の assert 構造を壊さず内容検証を追記する）
    - pagefolio/page_ops.py（`_insert_blank_page`:155-178＝白紙は元ページ w/h を継承。`_add_watermark_text`:180-215＝挿入テキスト）
    - tests/conftest.py（`sample_pdf_doc`＝595×842 A4）
  </read_first>
  <action>
    `tests/test_pdf_ops.py` の `TestContentOpsUndoFix` 既存メソッドへ内容検証 assert を追記する（テスト構造は変えず assert 追加のみ・D-14）。`test_insert_blank_roundtrip`: 白紙挿入後の `app.doc[1]` のページサイズ（`rect.width`/`rect.height`）が元ページ（595×842）と一致することを assert 追加。`test_watermark_roundtrip`: 既存の `get_text` 抽出確認に加え、undo 後に元テキスト（"Page 1" 等）が保持されていることの確認を厚くする（既存の "Page 1" assert があれば補強）。`test_page_numbers_roundtrip`: 既存の "1 / 3"/"3 / 3" 確認は維持しつつ、undo 後に元コンテンツ "Page 1" が残ることを assert（既存にあれば維持）。いずれも新規テストメソッドは追加せず、既存メソッドへ正常系の内容 assert を足すに留める。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py -k "roundtrip" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_insert_blank_roundtrip` に白紙ページのサイズ一致 assert（`rect.width` と `rect.height`）が追加されている
    - `test_watermark_roundtrip` / `test_page_numbers_roundtrip` に undo 後の元コンテンツ保持 assert が含まれる
    - `pytest tests/test_pdf_ops.py -k roundtrip -x` が exit 0
    - 既存テストメソッド数は減っていない（追記のみ・構造非破壊）
    - `ruff check tests/test_pdf_ops.py` が clean
  </acceptance_criteria>
  <done>白紙挿入・テキスト透かし・ページ番号の undo 往復テストに内容検証が加わり、正常系がグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| テストコード ↔ インメモリ PDF | 外部入力なし・テスト専用の合成 PDF/データのみを扱う |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-02 | Tampering | 純関数抽出リファクタ（dnd.py / app.py） | low | mitigate | 抽出はロジックのみで `root.bind`/D&D の実挙動を不変に保つ（A4）。既存フルスイートグリーン維持を回帰ゲートとする |
| T-3-SC | Tampering | パッケージインストール | low | accept | 本フェーズは新規パッケージを一切導入しない（RESEARCH.md Package Legitimacy Audit）。install タスクなし |
</threat_model>

<verification>
- `pytest tests/test_v150_regression.py tests/test_pdf_ops.py -q` グリーン
- `pytest`（フルスイート）で既存テストに回帰なし（抽出リファクタの安全網）
- `ruff check . && ruff format --check .` clean
</verification>

<success_criteria>
- v1.5.0 新機能（TOC 保持・D&D 指定位置挿入・ショートカット動的読込）の回帰テストが pytest でグリーン（成功基準4・V171-TEST-01）
- 既存 undo 往復テストへ内容検証が追加され正常系が担保される（D-14）
- 純関数抽出前後で既存挙動が不変
</success_criteria>

<output>
Create `.planning/phases/03-v1-5-0/03-02-SUMMARY.md` when done
</output>
