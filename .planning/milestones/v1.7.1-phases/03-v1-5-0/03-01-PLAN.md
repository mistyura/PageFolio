---
phase: 03-v1-5-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/page_ops.py
  - pagefolio/ui_builder.py
  - pagefolio/lang.py
  - tests/test_page_polish.py
autonomous: true
requirements:
  - V171-PAGE-01
user_setup: []

must_haves:
  truths:
    - "ユーザーは PNG/JPEG 画像を選択すると、選択ページ中央にページ幅の約50%・50%透過で焼き込まれる（D-01/D-02/D-03/D-04）"
    - "画像透かし追加後に Undo すると、適用前のページ内容に完全復元される（page_edit op・v1.5.0 テキストのみ制限の解除）"
    - "PNG の既存アルファは 0.5 乗算で尊重され、JPEG は RGBA 変換後に均一 50% 透過が付与される"
    - "破損/非対応画像を選んでもクラッシュせず messagebox.showerror で通知される（DoS 耐性）"
  artifacts:
    - "pagefolio/page_ops.py に _add_watermark_image メソッド"
    - "pagefolio/page_ops.py に _watermark_image_rect 静的ヘルパー"
    - "pagefolio/lang.py に btn_watermark_image キー（ja/en 両辞書）"
    - "tests/test_page_polish.py の TestImageWatermark"
  key_links:
    - "_add_watermark_image → _save_undo('page_edit', targets) を適用ループの外側で1回だけ呼ぶ（undo 一括復元の要）"
    - "ui_builder.py の透かしセクション → _add_watermark_image への command 結線"
---

<objective>
画像（ロゴ等）を透かしとしてページへ追加できるようにする（V171-PAGE-01・D-01〜D-04）。既存のテキスト透かし（`_add_watermark_text`）と同一の「ボタン→ファイル選択→即適用→page_edit undo」フローを踏襲し、`page.insert_image` に不透明度引数が無いため Pillow でアルファチャンネルを事前合成して 50% 透過を実現する。設定/プレビューダイアログは設けない（D-01 ミニマムパターン）。

Purpose: v1.5.0 の「テキストのみ」制限を解除し、Undo で戻せる非破壊的な画像透かしを提供する。
Output: `_add_watermark_image` / `_watermark_image_rect`（page_ops.py）、透かしボタン（ui_builder.py）、LANG キー、TestImageWatermark（test_page_polish.py）。
</objective>

<artifacts_produced>
## Artifacts this phase produces (このプラン新規シンボル)

- `_add_watermark_image` — メソッド（`pagefolio/page_ops.py` の `PageOpsMixin`）
- `_watermark_image_rect` — 静的メソッド（`pagefolio/page_ops.py`・純関数・配置矩形計算）
- `btn_watermark_image` — LANG キー（`pagefolio/lang.py` ja/en）
- `tests/test_page_polish.py` — 新規テストファイル（D-15・機能別テストを収容）
- `TestImageWatermark` — テストクラス（`tests/test_page_polish.py`）
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-v1-5-0/03-CONTEXT.md
@.planning/phases/03-v1-5-0/03-RESEARCH.md
@.planning/phases/03-v1-5-0/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 画像透かしの適用ロジック（_add_watermark_image / _watermark_image_rect）</name>
  <files>pagefolio/page_ops.py, tests/test_page_polish.py</files>
  <read_first>
    - pagefolio/page_ops.py（`_add_watermark_text`:180-215 が雛形。`_check_doc`→`_get_targets`→`_save_undo("page_edit", targets=targets)`→適用ループ→`_invalidate_thumb_cache(targets)`→`_refresh_all`→`_set_status` の並びを完全踏襲。冒頭 import の `filedialog, messagebox, simpledialog` を確認）
    - pagefolio/redact_ops.py（`_mosaic_page`:166-187 の Pillow/`insert_image` 使用例。`from PIL import Image` / `import io` パターン）
    - pagefolio/file_ops.py（`_save_undo`:132・`page_edit` 分岐:150-155＝対象ページの適用前 bytes を `_capture_page_blob` で1回キャプチャ。`_restore_state`:359-367＝page_edit の対称 undo）
    - tests/test_pdf_ops.py（`TestContentOpsUndoFix`:1469-1571・`_make_app` FakeApp 雛形と `test_watermark_roundtrip` の monkeypatch 型）
    - tests/conftest.py（`sample_pdf_doc` 3ページ A4 フィクスチャ）
    - .planning/phases/03-v1-5-0/03-RESEARCH.md（Pattern 1・Pitfall 1/5・Code Examples）
  </read_first>
  <behavior>
    - `_watermark_image_rect(page_rect, img_w, img_h)`: 目標幅 = page_rect.width * 0.5、アスペクト比保持で縮小し、ページ中央配置の fitz.Rect を返す（D-02）。縦長で target_h が page_rect.height*0.9 を超える場合は高さ基準へクランプ（Claude's Discretion）。
    - `_add_watermark_image`: PNG は既存アルファを 0.5 乗算、JPEG（アルファ無し）は convert("RGBA") 後に均一 128 を putalpha（D-03・Pitfall 1）。`insert_image` は rotate 引数なし＝水平のまま（D-03）。
    - undo: `_save_undo("page_edit", targets=targets)` を適用ループの外側で1回だけ呼ぶ。適用後 Undo で全対象ページが適用前へ復元される。
    - 破損/非対応画像: `Image.open` を try/except Exception で保護し `messagebox.showerror(self._t("err_title"), str(e))` で return（T-3-01）。
  </behavior>
  <action>
    `pagefolio/page_ops.py` の `PageOpsMixin` に `_add_watermark_image(self)` と静的メソッド `_watermark_image_rect(page_rect, img_w, img_h)` を追加する。冒頭に `from PIL import Image` と `import io` を追加（redact_ops.py と同型）。制御フローは `_add_watermark_text`（180-215）と同一: `_check_doc()` → `_get_targets()` → `filedialog.askopenfilename(title=self._t("btn_watermark_image"), filetypes=[(self._t("filetypes_image"), "*.png *.jpg *.jpeg")])`（D-04・`filetypes_image` は既存キー）。パス未選択なら return。`Image.open(path)` を try/except Exception で保護し `convert("RGBA")` する（失敗時 `messagebox.showerror(self._t("err_title"), str(e))` で return・T-3-01）。アルファ処理は D-03: `img.split()` の a チャンネルを `a.point(lambda v: int(v * 0.5))` で乗算して `putalpha`（PNG の既存アルファ尊重・JPEG は RGBA 変換で a=255 のため均一 50% になる）。`io.BytesIO` に PNG 保存して bytes 化。`_save_undo("page_edit", targets=targets)` をループ外で1回呼び、各 `i in targets` について `page = self.doc[i]`、`rect = self._watermark_image_rect(page.rect, iw, ih)`、`page.insert_image(rect, stream=img_bytes)`。末尾は `_invalidate_thumb_cache(targets)` → `_refresh_all()` → `_set_status(...)`。`_watermark_image_rect` は D-02 の中央配置＋幅50%縮小＋高さ0.9クランプで fitz.Rect を返す純ロジック（RESEARCH Pattern 1 の式）。`page.insert_image` に不透明度引数を探さないこと（存在しない・RESEARCH Anti-Patterns）。tests/test_page_polish.py を新規作成し（D-15）、`TestImageWatermark` を追加: `_make_app` は tests/test_pdf_ops.py:1477 の FakeApp をコピー流用（PageOpsMixin/RedactOpsMixin/FileOpsMixin 合成）。テストは (1) `_watermark_image_rect` の中央配置・幅約50%を数値検証（純関数・fitz 不要）、(2) FakeApp で PNG/JPEG バイトを生成し monkeypatch で filedialog.askopenfilename を差し替え、`_add_watermark_image` 適用後に `page.get_images()` が非空（画像埋め込み確認・`get_text` では検出不能・Pitfall 5）、(3) Undo でページの `get_images()` が空へ戻ることを検証。
  </action>
  <verify>
    <automated>pytest tests/test_page_polish.py -k watermark_image -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/page_ops.py` に `def _add_watermark_image(self):` と `def _watermark_image_rect(` が存在する
    - `_add_watermark_image` は `_save_undo("page_edit", targets=targets)` を1回だけ呼ぶ（`page_ops.py` の当該メソッド本体で `_save_undo` 出現回数が1）
    - `pytest tests/test_page_polish.py -k watermark_image -x` が exit 0
    - テストで画像透かし適用後 `page.get_images()` が非空、Undo 後に空へ戻ることを検証している
    - 破損画像パスで `_add_watermark_image` を呼んでも例外送出せず showerror 経路に入る（monkeypatch でのテスト or try/except の存在を確認）
    - `ruff check pagefolio/page_ops.py tests/test_page_polish.py` が clean
  </acceptance_criteria>
  <done>PNG/JPEG が中央・幅50%・50%透過で焼き込まれ、Undo で復元でき、破損画像でクラッシュしない。純関数と埋め込み・undo 往復のテストがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: 透かしボタンの UI 結線と LANG キー追加</name>
  <files>pagefolio/ui_builder.py, pagefolio/lang.py, tests/test_page_polish.py</files>
  <read_first>
    - pagefolio/ui_builder.py（既存テキスト透かしボタンの結線箇所。`section(...)` と `btn(...)` ヘルパーの呼び出しパターン。透かし/ページ番号ボタンが属するセクションを特定）
    - pagefolio/lang.py（`btn_watermark`:100/690・`filetypes_image`:221/811 の ja/en 対応行。新規キーは ja/en 両辞書へ同一キーで追加）
    - tests/test_lang_parity.py（ja/en キー一致・プレースホルダ整合の回帰テスト）
  </read_first>
  <action>
    `pagefolio/lang.py` の ja / en 両辞書へ新規キー `btn_watermark_image` を同一キーで追加する（ja 例: "🖼 画像透かしを追加…"、en 例: "🖼 Add Image Watermark..."・絵文字/文言は既存 `btn_watermark` に合わせて自然に）。`test_lang_parity.py` が左右キー数一致を監視するため必ず両方へ。`pagefolio/ui_builder.py` の既存テキスト透かしボタン（`self._t("btn_watermark")` を command=`_add_watermark_text` で生成している `btn(...)` 呼び出し）の直後に、同じ `btn(...)` ヘルパーで `self._t("btn_watermark_image")` を command=`self._add_watermark_image`・`needs_doc=True, edit_only=True` として追加する（既存ボタンと同一の引数パターン・破壊的でないため通常 "TButton"）。tests/test_page_polish.py に `TestImageWatermarkLang` を追加し、`from pagefolio.lang import LANG` で `"btn_watermark_image" in LANG["ja"]` と `in LANG["en"]` を検証する（UI 生成は Tk 必要なため grep 代替の存在検証）。
  </action>
  <verify>
    <automated>pytest tests/test_lang_parity.py tests/test_page_polish.py -k "watermark_image or lang" -q</automated>
    <human-check>アプリ起動 → ファイルを開く → 「画像透かしを追加…」ボタンで PNG/JPEG を選択 → プレビューで中央・約50%幅・半透明で表示されることを確認 → Undo で消えることを確認（VALIDATION.md Manual-Only「画像透かしの見た目」・A1 の SMask 半透明表示の実機確認）</human-check>
  </verify>
  <acceptance_criteria>
    - `pagefolio/lang.py` の ja/en 両辞書に `btn_watermark_image` キーが存在する（`grep -c '"btn_watermark_image"' pagefolio/lang.py` が 2）
    - `pagefolio/ui_builder.py` に `self._add_watermark_image` を command とするボタン結線が存在する
    - `pytest tests/test_lang_parity.py` が exit 0（ja/en パリティ維持）
    - `pytest tests/test_page_polish.py -k lang` が exit 0
    - `ruff check pagefolio/ui_builder.py pagefolio/lang.py` が clean
  </acceptance_criteria>
  <done>透かしボタンが UI に追加され `_add_watermark_image` に結線、LANG は ja/en パリティを維持。見た目は end-of-phase human-check で確認。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ローカルファイル（ユーザー選択画像）→ Pillow デコード → PDF ページ | 信頼できない画像バイトが Pillow を通り PDF ページへ焼き込まれる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Denial of Service | `_add_watermark_image` の `Image.open` | medium | mitigate | `try/except Exception` で保護し `messagebox.showerror` へフォールバック（既存 `_do_export_images` の例外作法）。filedialog フィルタで PNG/JPEG に限定（D-04） |
| T-3-SC | Tampering | パッケージインストール | low | accept | 本フェーズは新規パッケージを一切導入しない（RESEARCH.md Package Legitimacy Audit・Pillow/PyMuPDF は既存依存）。install タスクなし |
</threat_model>

<verification>
- `pytest tests/test_page_polish.py tests/test_lang_parity.py -q` グリーン
- `pytest`（フルスイート）で既存 707+ 件に回帰なし
- `ruff check . && ruff format --check .` clean
- 画像透かしの見た目・半透明（A1）は end-of-phase human-verify で確認
</verification>

<success_criteria>
- 画像（PNG/JPEG）を選択ページへ中央・幅50%・50%透過で追加でき、Undo で復元できる（成功基準1・D-01〜D-04）
- 破損画像でアプリがクラッシュしない
- LANG ja/en パリティ維持・既存テスト回帰なし
</success_criteria>

<output>
Create `.planning/phases/03-v1-5-0/03-01-SUMMARY.md` when done
</output>
