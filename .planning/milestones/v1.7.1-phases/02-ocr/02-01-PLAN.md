---
phase: 02-ocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/plugins.py
  - pagefolio/ocr.py
  - pagefolio/dialogs/llm_config.py
  - tests/test_plugins.py
  - .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md
autonomous: true
requirements:
  - V171-OCR-03
must_haves:
  truths:
    - "組み込み名（claude/gemini/lmstudio/tesseract/ollama/runpod/off）と衝突するプラグイン登録は logger.warning のうえ拒否される"
    - "プラグイン同士の重複名は logger.warning 付きで後勝ち上書きされる"
    - "プラグインを unload すると、そのプラグインが登録した OCR プロバイダが registry から解除される"
    - "登録済み OCR プロバイダへ PluginManager.get_ocr_provider / list_ocr_providers の公開アクセサで到達できる"
    - "ocr.py と llm_config.py が _provider_registry へ直接アクセスせず公開アクセサ経由になる"
  artifacts:
    - "pagefolio/plugins.py（register_ocr_provider 堅牢化・unload 解除・公開アクセサ）"
    - "tests/test_plugins.py（重複名警告・unload 解除・公開アクセサの新規テスト）"
  key_links:
    - "load_plugin/enable_plugin の on_load 呼び出し → register_ocr_provider の name→plugin 追跡"
    - "unload_plugin → registry からの owner ベース解除"
    - "ocr.py:720 / llm_config.py:127 の私有アクセス → 公開アクセサ置換"
---

<objective>
V171-OCR-03（L-2/L-3）を解消する。プラグイン OCR provider registry を堅牢化し、
(a) 組み込み名との重複登録を可視化して拒否・プラグイン同士の重複は後勝ち上書き（D-08）、
(b) プラグイン unload 時に registry から登録解除（D-09）、
(c) `get_ocr_provider(name)` / `list_ocr_providers()` の公開アクセサを追加して `_provider_registry` 私有属性への直接アクセス（`ocr.py:720`・`dialogs/llm_config.py:127`）を置換する（D-10）。

Purpose: プラグイン OCR プロバイダの登録/解除/参照面を安定化し、私有属性依存を排して将来の内部実装変更を非破壊にする。フェーズ内で最も独立した plugins.py 面から着手し、後続プラン（02-02/02-03/02-04）が乗る土台を固める。
Output: 堅牢化された `plugins.py`・公開アクセサ経由に置換された `ocr.py`/`llm_config.py`・新規テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr/02-CONTEXT.md
@.planning/phases/02-ocr/02-RESEARCH.md
@.planning/phases/02-ocr/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
本プランが新規に作成するシンボル（drift 検証時に既存コード扱いしないこと）:
- `PluginManager.get_ocr_provider(self, name)` メソッド（plugins.py）
- `PluginManager.list_ocr_providers(self)` メソッド（plugins.py）
- 組み込みプロバイダ名の定数（例 `_BUILTIN_PROVIDER_NAMES`・plugins.py。既存に無ければ新設）
- name→plugin 対応を追跡する内部辞書（例 `_provider_owners`・plugins.py）
- ロード中プラグイン ID を保持する内部コンテキスト属性（例 `_loading_plugin_id`・plugins.py）
- tests/test_plugins.py の新規テストクラス/関数（重複名警告・unload 解除・公開アクセサ）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: register_ocr_provider 堅牢化・unload 解除・公開アクセサ追加（plugins.py）</name>
  <files>pagefolio/plugins.py, tests/test_plugins.py</files>
  <read_first>
    - pagefolio/plugins.py（PluginManager 全体。特に __init__:84-88 の `_plugins`/`_plugin_modules`/`_disabled`/`_provider_registry`、load_plugin:116-146、unload_plugin:152-161、enable_plugin/disable_plugin:165-179、register_ocr_provider:200-218、get_disabled_ids:221-223）
    - pagefolio/ocr.py:627-721（build_provider — 組み込み名の分岐と登録プロバイダ参照。組み込み名の正の一覧を確認）
    - tests/test_plugins.py:546-601（既存の `_provider_registry` 直接参照テスト。破壊的変更はせず追加型で書く）
    - .planning/phases/02-ocr/02-PATTERNS.md §plugins.py（get_disabled_ids と同型の薄いラッパー・重複検証・unload 解除イメージ）
  </read_first>
  <behavior>
    - register_ocr_provider("tesseract", X): 組み込み名衝突 → logger.warning で記録し registry へ登録せず拒否（既存の「組み込み勝ち」を維持・D-08）
    - register_ocr_provider("myprov", A) 後に register_ocr_provider("myprov", B): プラグイン同士の重複 → logger.warning のうえ B で後勝ち上書き（D-08）
    - register_ocr_provider("myprov", NotAProvider): OCRProvider サブクラス以外は現行どおり TypeError（既存挙動維持）
    - load_plugin 経由で登録 → get_ocr_provider("myprov") がそのクラスを返す
    - unload_plugin(plugin_id): その plugin が登録した名前が registry から消え、get_ocr_provider は None を返す（D-09）
    - list_ocr_providers(): 登録済みプロバイダ名の list を返す（順不同で内容一致）
  </behavior>
  <action>
    register_ocr_provider に重複名ポリシーを追加する（D-08）。組み込み名の正の一覧を定数化する（例 `_BUILTIN_PROVIDER_NAMES = frozenset({"claude","gemini","lmstudio","tesseract","ollama","runpod","off"})`。ocr.py:627-721 の build_provider 分岐と一致させること）。登録名が組み込み名に含まれる場合は logger.warning を出して登録を行わず return（拒否）。既に登録済みのプラグイン名を再登録する場合は logger.warning を出したうえで後勝ち上書きする。OCRProvider サブクラス検証（既存 TypeError 経路）は維持する。

    D-09 の unload 解除のため name→plugin owner の対応を registry 側で追跡する。register_ocr_provider は plugin_id を明示引数に取らない（プラグイン作者向け公開 API のシグネチャは変更しない）ため、load_plugin と enable_plugin が on_load(app) を呼ぶ前後（load_plugin:145-146・enable_plugin:166-168）に「現在ロード中の plugin_id」を保持する内部コンテキスト属性（例 `self._loading_plugin_id`）を設定/クリアし、register_ocr_provider がその値を読んで owner 辞書（例 `self._provider_owners[name] = plugin_id`）へ記録する方式を採る（機構の詳細は Claude's Discretion。RESEARCH.md Architecture Patterns 準拠）。owner が不明（None）な登録は owner 追跡なしで登録可とする（後方互換）。

    unload_plugin（152-161）に、その plugin_id が owner の登録名を registry と owner 辞書から削除する処理を追加する（D-09）。settings の `ocr_provider` は触らない（副作用なし・D-09）。

    get_disabled_ids（221-223・読み取り専用の薄いラッパー）と同型で公開アクセサ 2 つを新設する（D-10）: `get_ocr_provider(self, name)` は `self._provider_registry.get(name)`（未登録は None）、`list_ocr_providers(self)` は `list(self._provider_registry.keys())`。`_provider_registry` はアンダースコア付きの私有属性のまま維持し、リネーム公開はしない（RESEARCH.md Anti-Patterns）。両アクセサにプラグイン作者向け docstring を付す。

    裸の except は使わず `except Exception as e:` + logger 経由で記録する（CLAUDE.md 規約）。

    tests/test_plugins.py に追加型でテストを新設する（既存 546-601 は変更しない）: 重複組み込み名の拒否+警告（caplog で warning 確認）、プラグイン同士の後勝ち上書き+警告、unload 後の登録解除（get_ocr_provider が None）、get_ocr_provider/list_ocr_providers の正常動作。
  </action>
  <verify>
    <automated>pytest tests/test_plugins.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_plugins.py -q` がグリーン（既存 546-601 テストも回帰なし）
    - `pytest tests/test_plugins.py -k "duplicate_builtin or unload_deregisters or public_accessor" -x` が新規テストを実行しグリーン
    - `grep -n "def get_ocr_provider" pagefolio/plugins.py` と `grep -n "def list_ocr_providers" pagefolio/plugins.py` が各 1 件マッチ
    - 組み込み名 tesseract を register_ocr_provider へ渡すと登録されず（get_ocr_provider("tesseract") 相当の直接検証で registry 非追加）、caplog に WARNING が 1 件出る
    - unload_plugin 後に対象プラグインの登録名が list_ocr_providers() に含まれない
    - `ruff check pagefolio/plugins.py tests/test_plugins.py` がクリーン
  </acceptance_criteria>
  <done>D-08/D-09/D-10 に沿って register_ocr_provider・unload_plugin・公開アクセサが実装され、新旧テストがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: 私有アクセスの公開アクセサ置換（ocr.py:720 / llm_config.py:127）+ REVIEW.md 完了追記</name>
  <files>pagefolio/ocr.py, pagefolio/dialogs/llm_config.py, tests/test_plugins.py, .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md</files>
  <read_first>
    - pagefolio/ocr.py:627-721（build_provider の `if plugin_manager is not None and name in plugin_manager._provider_registry:` :720 と `cls = plugin_manager._provider_registry[name]` :721。docstring :635 も `_provider_registry` に言及するが本タスクの置換対象は :720-721 の実アクセスのみ）
    - pagefolio/dialogs/llm_config.py:120-135（`self._plugin_manager._provider_registry.keys()` :127 の使用箇所と周辺の Combobox values 生成）
    - .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md §低優先度（L）の L-2/L-3 該当行（完了マーク追記の慣行を確認・D-12）
  </read_first>
  <action>
    ocr.py:720-721 の `_provider_registry` 私有アクセスを Task 1 の公開アクセサ経由へ置換する（D-10）。`cls = plugin_manager.get_ocr_provider(name)` を用い、`if plugin_manager is not None and cls is not None:` の形で分岐して cls を使用する（None 判定で未登録を扱う）。docstring :635 の説明文言はそのまま残してよい（実アクセスの置換のみが対象）。

    llm_config.py:127 の `self._plugin_manager._provider_registry.keys()` を `self._plugin_manager.list_ocr_providers()` へ置換する（D-10）。周辺の Combobox values 生成ロジックは戻り値が list であることを前提にそのまま動作させる。

    tests/test_plugins.py に、build_provider が公開アクセサ経由で登録プロバイダを解決することを検証するテストを追加する（ダミー OCRProvider を register→build_provider(settings with ocr_provider=そのプラグイン名) がそのインスタンスを返す。既存 build_provider テストパターンに倣う）。

    D-12: 解消した L-2/L-3 について、260610-aaa-REVIEW.md の該当項目行へ ✅ と本プランの解消コミットハッシュを追記する（同文書の既存慣行に従う）。コミットハッシュは実行時に確定するため、コミット直後の追記または追記コミットとして残す。

    裸の except 禁止・logger 経由記録（CLAUDE.md 規約）を維持する。
  </action>
  <verify>
    <automated>pytest tests/test_plugins.py tests/test_ocr.py tests/test_provider_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "get_ocr_provider(" pagefolio/ocr.py` が 1 件以上マッチ（公開アクセサ使用）
    - `grep -n "list_ocr_providers()" pagefolio/dialogs/llm_config.py` が 1 件マッチ
    - `grep -n "_provider_registry" pagefolio/dialogs/llm_config.py` が 0 件（llm_config は私有アクセスを持たない）
    - `pytest tests/test_plugins.py tests/test_ocr.py tests/test_provider_ui.py -q` がグリーン
    - 260610-aaa-REVIEW.md の L-2/L-3 行に ✅ が追記されている（`grep -n "L-2\|L-3" ...REVIEW.md` で確認）
    - `ruff check pagefolio/ocr.py pagefolio/dialogs/llm_config.py` がクリーン
  </acceptance_criteria>
  <done>ocr.py/llm_config.py の私有アクセスが公開アクセサへ置換され、build_provider の解決テストがグリーン、REVIEW.md に完了マークが追記された。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| プラグインコード → アプリ registry | サードパーティ製プラグインが on_load 内で任意の provider 名/クラスを registry へ登録する（信頼できない入力が越境） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-P01 | Spoofing/Tampering | PluginManager.register_ocr_provider | medium | mitigate | 組み込み名（claude/gemini/lmstudio/tesseract/ollama/runpod/off）との衝突を logger.warning のうえ拒否し、正規プロバイダのなりすまし上書きを防ぐ（D-08） |
| T-2-P02 | Tampering | PluginManager 内部 registry | low | mitigate | unload 時に owner ベースで登録解除し、無効化済みプラグインのプロバイダが残存参照されないようにする（D-09） |

本プランはパッケージインストールを伴わない（サプライチェーン脅威 T-{phase}-SC は非該当）。
</threat_model>

<verification>
- `pytest tests/test_plugins.py tests/test_ocr.py tests/test_provider_ui.py -q` グリーン
- `pytest`（フルスイート）で 707 件超のグリーン維持（回帰なし）
- `ruff check . && ruff format .` クリーン
- 手動確認不要（すべて caplog/monkeypatch で自動化。実プラグインインストール不要）
</verification>

<success_criteria>
- ROADMAP 成功基準 3（プラグイン OCR プロバイダの重複名警告・unload 登録解除・公開アクセサ到達）が満たされる
- `_provider_registry` 私有属性への外部直接アクセスが ocr.py/llm_config.py から排除される
- 既存 test_plugins.py の破壊的変更なしで新規テストが追加されグリーン
</success_criteria>

<output>
Create `.planning/phases/02-ocr/02-01-SUMMARY.md` when done
</output>
