---
phase: 02-ocr
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pagefolio/ocr_providers.py
  - pagefolio/ocr.py
  - pagefolio/dialogs/llm_config.py
  - pagefolio/lang.py
  - pagefolio/ocr_dialog.py
  - tests/test_ocr_providers.py
  - tests/test_provider_ui.py
  - .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md
autonomous: true
requirements:
  - V171-OCR-02
must_haves:
  truths:
    - "TesseractProvider が tesseract_lang 設定（self.lang）の言語で OCR を実行する"
    - "指定言語のうち利用可能な部分集合が優先され、全滅時のみ現行の自動決定（jpn有→jpn+eng / なし→eng）へ落ちる"
    - "指定言語が一部/全部利用不可でもエラー中止せず必ず何かしらの言語で実行される"
    - "言語パック検出はプロバイダ生成時に再評価され、追加した言語パックが再起動なしで反映される"
    - "フォールバック発生時に OCRDialog へ WARNING 色の非モーダル注記が 1 回表示され、OCR 結果テキストには混入しない"
  artifacts:
    - "pagefolio/ocr_providers.py（TesseractProvider の段階的縮退・再検出可能な言語検出）"
    - "pagefolio/lang.py（フォールバック注記キー・ja/en 対）"
    - "tests/test_ocr_providers.py（段階的縮退テスト・更新済みフォールバックテスト）"
  key_links:
    - "ocr.py:711-714 build_provider → TesseractProvider へ再検出言語を渡す配線"
    - "llm_config.py の _TESSERACT_LANGS 参照 → 同一再検出関数への統一（Pitfall 2）"
    - "TesseractProvider のフォールバック情報 → OCRDialog の非モーダル注記表示"
---

<objective>
V171-OCR-02（L-4）を解消する。TesseractProvider が `tesseract_lang` 設定（`self.lang`）を尊重し、
指定言語のうち利用可能な部分集合を優先する段階的縮退（D-06）で実行する。言語パック検出は import 時固定をやめプロバイダ生成時に再評価する（D-05）。
フォールバック（指定言語の一部/全部が利用不可）が起きたときは OCRDialog 内の非モーダル WARNING 注記で 1 回通知する（D-07・OCR 結果 raw には混入させない）。

Purpose: 「設定した言語が無視される」既知の齟齬を解消し、言語パック追加を再起動なしで反映しつつ、常に何かしらの言語で実行できる堅牢性を保つ。
Output: 段階的縮退を実装した `ocr_providers.py`・再検出配線を通した `ocr.py`/`llm_config.py`・注記文言 `lang.py`・注記表示 `ocr_dialog.py`・更新/新規テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr/02-CONTEXT.md
@.planning/phases/02-ocr/02-RESEARCH.md
@.planning/phases/02-ocr/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
本プランが新規に作成/変更するシンボル:
- TesseractProvider の実効言語属性（例 `self.effective_lang`）とフォールバック情報属性（例 `self.lang_fallback_note` / `(requested, effective)` タプル）
- TesseractProvider.__init__ の追加引数（例 `available_langs=None`・既定 None は内部再検出で後方互換）
- 再検出を公開呼び出しできる関数（`_detect_tesseract` を build 時から都度呼べる形。必要なら公開ラッパー `detect_tesseract()` を新設）
- lang.py のフォールバック注記キー（既存 `tesseract_lang_fallback` の再利用/拡張。新規なら ja/en 同一キーで追加）
- tests/test_ocr_providers.py の段階的縮退テスト（部分集合縮退・再検出）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tesseract 言語の段階的縮退 + プロバイダ生成時再検出（ocr_providers.py / ocr.py / llm_config.py）</name>
  <files>pagefolio/ocr_providers.py, pagefolio/ocr.py, pagefolio/dialogs/llm_config.py, tests/test_ocr_providers.py</files>
  <read_first>
    - pagefolio/ocr_providers.py:960-994（`_detect_tesseract()` と `_TESSERACT_AVAILABLE, _TESSERACT_LANGS = _detect_tesseract()` の import 時固定）
    - pagefolio/ocr_providers.py:997-1057（TesseractProvider の __init__ :1011-1022 と ocr_image :1024-1057、特に :1039 の `lang = "jpn+eng" if "jpn" in _TESSERACT_LANGS else "eng"` の自動決定と `-l` 引数渡し）
    - pagefolio/ocr.py:627-721（build_provider の tesseract 分岐 :711-714 `TesseractProvider(lang=settings.get("tesseract_lang", "jpn+eng"))`）
    - pagefolio/dialogs/llm_config.py:14-15, 142, 646, 970, 1364-1366（`_TESSERACT_LANGS`/`_TESSERACT_AVAILABLE` のモジュール定数参照 4 箇所・Pitfall 2）
    - tests/test_ocr_providers.py:1131-1157（`test_lang_fallback_to_eng_when_jpn_not_available` — 旧ロジック前提。新ロジックに合わせ書き換え・Pitfall 3）
    - .planning/phases/02-ocr/02-RESEARCH.md Common Pitfalls 2/3 と Security Domain（`-l` へ渡す値は検出済み集合との積のみ・injection 面維持）
  </read_first>
  <behavior>
    - self.lang="deu+jpn+eng", 利用可能 {jpn,eng}: 実効言語は "jpn+eng"（順序は self.lang の指定順を保つ）・fallback 発生フラグ True
    - self.lang="deu+fra", 利用可能 {jpn,eng}: 部分集合が空 → 自動決定へ縮退（jpn有り想定なら "jpn+eng"、無しなら "eng"）・fallback 発生フラグ True
    - self.lang="jpn+eng", 利用可能 {jpn,eng}: 実効言語 "jpn+eng"・fallback 発生フラグ False
    - self.lang 未指定/空: 現行の自動決定に一致・fallback False
    - build_provider を 2 回呼ぶ間に言語パックが増えた場合、2 回目のプロバイダは新しい検出結果を反映する（都度再検出・D-05）
    - subprocess の `-l` 引数へ渡すのは常に検出済み集合との積の結果のみ（未検証の生 self.lang を渡さない・injection 面維持）
  </behavior>
  <action>
    D-05: 言語検出を import 時固定から「プロバイダ生成時再評価」へ変える。`_detect_tesseract()` は都度呼び出し可能な現形のまま活かし、build_provider（ocr.py:711-714）が呼び出し毎に検出結果を取得して TesseractProvider へ渡す。TesseractProvider.__init__ に検出済み言語を受け取る引数（例 `available_langs=None`）を追加し、None のときのみ内部で `_detect_tesseract()` を呼ぶ（後方互換）。モジュール定数 `_TESSERACT_AVAILABLE, _TESSERACT_LANGS` は llm_config.py が UI 表示に参照しているため直ちに撤去せず、両ファイルが同一の検出関数を都度呼ぶよう統一する（Pitfall 2）。キャッシュ戦略（都度 subprocess か短期キャッシュか）は Claude's Discretion だが、`ocr_image` 呼び出しの都度 subprocess を起動しないこと（RESEARCH.md Anti-Patterns・並列 OCR 中の多重起動防止）。

    D-06: TesseractProvider の言語解決を段階的縮退へ書き換える。self.lang を "+" で分割し、検出済み集合に含まれる要素だけを指定順を保って残す（部分集合）。部分集合が非空ならそれを実効言語とする。空なら現行の自動決定（jpn 有→"jpn+eng" / なし→"eng"）へ落とす。実効言語は __init__ で算出して属性（例 `self.effective_lang`）に保持し、ocr_image:1039 は self.effective_lang を使う（都度計算しない）。フォールバックが起きたか（要求と実効の差）を判定できる情報（例 `(requested, effective)` と bool、または整形済み注記文字列）を属性として公開し、Task 2 の OCRDialog 表示が読めるようにする。エラー中止はしない（必ず何かしらで実行）。

    ocr.py build_provider:711-714 を、再検出した言語集合を TesseractProvider へ渡す形に更新する（tesseract_lang 設定は既存どおり settings から取得・配線済み）。

    tests/test_ocr_providers.py:1131-1157 の既存フォールバックテストを新ロジックへ書き換える（削除ではなく更新・Pitfall 3。「意図的な仕様変更」であることをコミットメッセージに明記）。あわせて部分集合縮退ケース（self.lang="deu+jpn+eng" × 利用可能 {jpn,eng} → "jpn+eng"）と再検出ケース（build_provider 2 回で検出結果差が反映される・`tesseract_redetect` 相当）を新規追加する。subprocess は monkeypatch でスタブする（実機不要）。

    裸の except 禁止・logger 経由記録（CLAUDE.md 規約）を維持する。
  </action>
  <verify>
    <automated>pytest tests/test_ocr_providers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ocr_providers.py::TestTesseractProviderOcrImage -x` がグリーン（新ロジック）
    - `pytest tests/test_ocr_providers.py -k "tesseract_redetect" -x` が再検出テストを実行しグリーン
    - self.lang="deu+jpn+eng" / 利用可能 {jpn,eng} で subprocess の `-l` 引数が "jpn+eng"（指定順保持）になる（monkeypatch 捕捉で検証）
    - self.lang="deu+fra" / 利用可能 {jpn,eng} で自動決定へ縮退し、いずれかの言語で実行される（例外送出しない）
    - `grep -n "effective_lang" pagefolio/ocr_providers.py` が 1 件以上マッチ
    - `ruff check pagefolio/ocr_providers.py pagefolio/ocr.py pagefolio/dialogs/llm_config.py tests/test_ocr_providers.py` がクリーン
  </acceptance_criteria>
  <done>TesseractProvider が self.lang を尊重し段階的縮退で常に実行でき、検出はプロバイダ生成時に再評価され、更新/新規テストがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: フォールバック注記文言（lang.py）+ OCRDialog 非モーダル通知（ocr_dialog.py）+ REVIEW.md 追記</name>
  <files>pagefolio/lang.py, pagefolio/ocr_dialog.py, tests/test_provider_ui.py, .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md</files>
  <read_first>
    - pagefolio/lang.py の既存 `tesseract_lang_fallback` キー（ja 側 :449 付近 / en 側 :1025 付近）— D-07 の注記文言に再利用/拡張候補
    - pagefolio/ocr_dialog.py:466-476（progress_var / 進捗ラベル定義）と :842-957（_apply_llm_settings：provider 再生成の流れ・注記表示の起点候補）、:1125-1190（_on_run：OCR 開始前の provider 参照）
    - pagefolio/ocr_dialog.py の既存 WARNING 色表示パターン（`C["WARNING"]` 使用箇所）と self._L 参照
    - tests/test_lang_parity.py（ja/en キー数一致監視 — 新規キーは必ず両辞書へペアで追加）
    - .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md L-4 該当行（完了マーク追記・D-12）
  </read_first>
  <action>
    D-07: フォールバック発生時に OCRDialog 内で WARNING 色の非モーダル注記を 1 回表示する。Task 1 で TesseractProvider が公開するフォールバック情報（要求言語・実効言語）を、provider 生成/選択後（`_apply_llm_settings` の provider 再生成後、または `_on_run` の OCR 開始前）に読み取り、「指定言語 xxx は利用不可のため yyy で実行」の趣旨を progress ラベル/結果ヘッダ部へ表示する。表示は実行を止めない・OCR 結果テキスト（コピー/保存 raw）には混入させない（V16-D-02 整合）。同一実行内で複数ページあっても注記は 1 回のみ（重複表示ガード）。

    注記文言は lang.py の既存 `tesseract_lang_fallback` キーの再利用/拡張を第一候補とする。要求言語・実効言語を差し込むプレースホルダが必要なら、ja/en 両辞書へ同一キーで追加/更新する（`test_lang_parity.py` のキー数一致を保つ）。文言詳細と新設/再利用の判断は Claude's Discretion。

    tests/test_provider_ui.py（または該当 UI テスト）に、フォールバック情報を持つスタブ provider を与えたとき OCRDialog が注記を 1 回だけ表示する（progress_var/ラベルにフォールバック趣旨の文言が入る）ことの回帰テストを追加する。フォールバックなしのときは注記が出ないことも確認する。

    D-12: 解消した L-4 について 260610-aaa-REVIEW.md の該当行へ ✅ + 解消コミットハッシュを追記する。

    裸の except 禁止・テーマ色は `C[...]`・フォントは `self._font(delta)` 経由（CLAUDE.md 規約）。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py tests/test_lang_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_lang_parity.py -q` がグリーン（ja/en キー数一致・プレースホルダ整合）
    - `pytest tests/test_provider_ui.py -q` がグリーン（フォールバック注記が 1 回表示・非発生時は無表示）
    - フォールバック注記が OCR 結果テキスト（raw）へ混入しない（テストで結果テキストに注記文言が含まれないことを確認）
    - 260610-aaa-REVIEW.md の L-4 行に ✅ が追記されている
    - `ruff check pagefolio/lang.py pagefolio/ocr_dialog.py tests/test_provider_ui.py` がクリーン
  </acceptance_criteria>
  <done>フォールバック時に OCRDialog が非モーダル注記を 1 回表示し、lang parity を保ちつつ結果 raw を汚さず、REVIEW.md に完了マークが追記された。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー設定（tesseract_lang）→ Tesseract subprocess 引数 | 設定値が `-l` 引数として外部プロセスへ渡る |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-T01 | Tampering/Injection | TesseractProvider.ocr_image の `-l` 引数 | low | mitigate | `-l` へ渡すのは検出済み言語集合との積を取った結果のみ。生の self.lang を直接渡さない。既存の `shell=True` 不使用・引数リスト渡しを維持（RESEARCH.md Security Domain） |

本プランはパッケージインストールを伴わない（T-{phase}-SC 非該当）。subprocess は既存 `_detect_tesseract` パターンを踏襲。
</threat_model>

<verification>
- `pytest tests/test_ocr_providers.py tests/test_provider_ui.py tests/test_lang_parity.py -q` グリーン
- `pytest`（フルスイート）で回帰なし
- `ruff check . && ruff format .` クリーン
- 実機 Tesseract での言語フォールバック目視は任意（テストは monkeypatch でカバー・実機確認は将来の human-verify 候補）
</verification>

<success_criteria>
- ROADMAP 成功基準 2（tesseract_lang 設定の言語で実行・利用不可時は自動フォールバックしユーザーへ伝達）が満たされる
- 言語パック追加が再起動なしで反映される（D-05）
- 段階的縮退により常に何かしらの言語で実行でき、エラー中止しない（D-06）
</success_criteria>

<output>
Create `.planning/phases/02-ocr/02-02-SUMMARY.md` when done
</output>
