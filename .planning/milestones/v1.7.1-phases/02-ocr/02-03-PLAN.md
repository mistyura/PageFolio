---
phase: 02-ocr
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - pagefolio/ocr_providers.py
  - pagefolio/dialogs/llm_config.py
  - pagefolio/ocr_dialog.py
  - tests/test_ocr_providers.py
  - tests/test_provider_ui.py
  - .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md
autonomous: true
requirements:
  - V171-OCR-01
must_haves:
  truths:
    - "LM Studio / Ollama / RunPod のユーザー入力 URL は http/https 以外のスキームで RuntimeError になる"
    - "Gemini のモデル名が URL パスセグメントとしてエスケープされてエンドポイントに埋め込まれる"
    - "HTTP エラーメッセージの body が一定長で切り詰められる（全プロバイダ共通ヘルパー経由）"
    - "ClaudeProvider.list_models が has_more/last_id カーソルを辿って全モデルを取得する"
    - "LLMConfigDialog の _fetch_models と _test_connection の重複ロジックが共通ヘルパーへ集約される（呼び出し元は不変）"
    - "OCR ダイアログで provider を 'off' に切り替えるとメイン画面ツールバーの OCR ボタンが disabled になる"
  artifacts:
    - "pagefolio/ocr_providers.py（URL スキーム検証ヘルパー・Gemini quote・エラー body 切り詰め・Claude list_models ページネーション）"
    - "pagefolio/dialogs/llm_config.py（_fetch_models/_test_connection 共通ヘルパー）"
    - "tests/test_ocr_providers.py / tests/test_provider_ui.py（新規回帰テスト）"
  key_links:
    - "各プロバイダの _post_chat/list_models → _require_http_scheme(url) 呼び出し（6 箇所）"
    - "_apply_llm_settings 末尾 → app._update_ocr_buttons_state()（try/except の外側）"
---

<objective>
V171-OCR-01（L-6 小物）を現行コード照合の上で一括解消する。対象は生き残り表で確定した:
L-6d（エラー body 切り詰め・共通ヘルパー1箇所）、L-6e（URL スキーム検証・LM Studio/Ollama/RunPod へ共通ヘルパーで統一適用・D-13）、L-6f（Gemini モデル名 URL エスケープ）、L-6i（`_fetch_models`/`_test_connection` 重複解消・LM Studio ペアのみ）、L-6j（"off" 切替時のツールバー OCR ボタン状態同期）、L-6b（ClaudeProvider `list_models` ページネーション）。

> パイプライン系小物（L-6a 進捗100%・L-6g fatal 後継続・L-6h sentinel 不変条件）は D-03 により 02-04（L-1 プラン）へ吸収済みのため本プラン対象外。L-6c（stop_reason）は解消済み・L-6k（CLAUDE.md）は 02-04 で対応。

Purpose: 実行・接続テスト・モデル取得を従来どおり動かしつつ、入力検証・重複解消・状態同期の小物を一掃してプロバイダ層/設定ダイアログの品質を底上げする。
Output: 修正済み `ocr_providers.py`/`llm_config.py`/`ocr_dialog.py` と新規回帰テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr/02-CONTEXT.md
@.planning/phases/02-ocr/02-RESEARCH.md
@.planning/phases/02-ocr/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
本プランが新規に作成するシンボル:
- `_require_http_scheme(url)` ヘルパー関数（ocr_providers.py）
- `_ALLOWED_URL_SCHEMES = ("http", "https")` 定数（ocr_providers.py）
- LLMConfigDialog の共通プローブヘルパー（例 `_probe_lm_provider(update_combo: bool)`・llm_config.py）
- tests/test_ocr_providers.py の新規テスト（url_scheme / gemini_model_escape / error_body_truncat / claude list_models ページネーション）
- tests/test_provider_ui.py の新規テスト（off_toggle_buttons）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: プロバイダ層 L-6 修正（URL スキーム検証・Gemini quote・エラー body 切り詰め・Claude list_models ページネーション）</name>
  <files>pagefolio/ocr_providers.py, tests/test_ocr_providers.py</files>
  <read_first>
    - pagefolio/ocr_providers.py:12-13（`import urllib.error` / `import urllib.request`。`from urllib.parse import quote, urlsplit` の追加先）
    - pagefolio/ocr_providers.py:193-215（`_raise_mapped_http_error` — `err_body` 生成 :209-211 と `message = f"HTTP {e.code}: {err_body or e.reason}"` :212。全プロバイダ共有・Pitfall 4）
    - pagefolio/ocr_providers.py:290-308, 359-370（LMStudioProvider `_post_chat` の endpoint :296 / `list_models` の endpoint :370）
    - pagefolio/ocr_providers.py:1139-1158, 1209-1220（OllamaProvider `_post_chat` :1145 / `list_models` :1220）
    - pagefolio/ocr_providers.py:1361-1372, 1397-1424（RunPodProvider `_post_chat` :1372 / `list_models` :1403）
    - pagefolio/ocr_providers.py:642-691（ClaudeProvider `list_models` — 単発 GET・has_more/after_id 未処理・L-6b）
    - pagefolio/ocr_providers.py:706-708, 811（Gemini `GENERATE_CONTENT_ENDPOINT` と `.format(model=self.model)`・L-6f）
    - tests/test_ocr_providers.py（既存の各プロバイダ HTTPError/エラーメッセージ assert。Pitfall 4：切り詰めで文字列が変わる箇所を横断確認）
    - .planning/phases/02-ocr/02-RESEARCH.md Patterns 4/5・Common Pitfalls 4
  </read_first>
  <behavior>
    - _require_http_scheme("file:///etc/passwd") → RuntimeError（http/https 以外を拒否）
    - _require_http_scheme("http://localhost:1234") / "https://api.example.com" → 例外なし
    - Gemini endpoint: self.model="models/foo?x=1" 等 URL 予約文字を含むモデル名が quote(safe="") でパスセグメント化され、意図しないクエリ/パスにならない
    - _raise_mapped_http_error: 長大な err_body（例 2000 文字）が 500 文字程度に切り詰められて message に載る（非リトライ系メッセージ）
    - ClaudeProvider.list_models: API 応答が has_more=true + last_id を返すページを複数ページ辿り、全モデル ID を連結して返す（1 ページで完結する場合は 1 回で終了）
  </behavior>
  <action>
    L-6e/D-13: `ocr_providers.py` に URL スキーム検証の共通ヘルパー `_require_http_scheme(url)` と定数 `_ALLOWED_URL_SCHEMES = ("http", "https")` を追加する（`urllib.parse.urlsplit(url).scheme.lower()` で判定し、許可外は `RuntimeError` を送出。RESEARCH.md Pattern 4 準拠）。リクエスト送信直前で呼ぶ（`__init__` での eager 検証はしない・A2）。呼び出し箇所は LM Studio（_post_chat :296 の endpoint 生成直後・list_models :370）、Ollama（_post_chat :1145・list_models :1220）、RunPod（_post_chat :1372・list_models :1403）の計 6 箇所。例外種別は既存規約（ConnectionError/TimeoutError/RuntimeError）に沿い RuntimeError を採用。

    L-6f: Gemini エンドポイント生成（:811 ほか `GENERATE_CONTENT_ENDPOINT.format(model=...)` の全箇所）で `urllib.parse.quote(self.model, safe="")` を適用してモデル名をパスセグメントとしてエスケープする。

    L-6d: `_raise_mapped_http_error`（:212 付近）の `err_body` を一定長（500 文字程度）で切り詰めてから message に載せる。この関数は 5 プロバイダ（LMStudio/Claude/Gemini/Ollama/RunPod）から共有呼び出しされるため、既存の各プロバイダのエラーメッセージ assert を横断的に確認し、切り詰めで壊れるテストがあれば併せて更新する（Pitfall 4）。

    L-6b（計画時判断＝対応する）: ClaudeProvider.list_models（:642-691）を `has_more`/`last_id`（Anthropic /v1/models のカーソル）を辿るループへ拡張し、複数ページのモデルを連結して返す。1 ページで完結する応答では現行と同じ結果になる後方互換を保つ。scope 縮小（v1 として据え置き 等）はしない。

    tests/test_ocr_providers.py に新規テストを追加する: `url_scheme`（file:// で RuntimeError・http/https は通過）、`gemini_model_escape`（予約文字入りモデル名が quote される）、`error_body_truncat`（長大 body が切り詰められる）、Claude list_models のページネーション（monkeypatch で has_more→last_id 2 ページを返し全件連結を確認）。ネットワークは monkeypatch でスタブ。

    裸の except 禁止・logger 経由記録（CLAUDE.md 規約）。`import urllib` 系の追加は既存 import ブロックに揃える。
  </action>
  <verify>
    <automated>pytest tests/test_ocr_providers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ocr_providers.py -k "url_scheme or gemini_model_escape or error_body_truncat" -x` がグリーン
    - Claude list_models のページネーションテストがグリーン（2 ページ連結）
    - `grep -n "def _require_http_scheme" pagefolio/ocr_providers.py` が 1 件、`_require_http_scheme(` の呼び出しが 6 箇所（`grep -c` ではなく `grep -n "_require_http_scheme(" pagefolio/ocr_providers.py` の関数定義を除く呼び出し行が 6 行）
    - `grep -n "quote(" pagefolio/ocr_providers.py` が Gemini endpoint 箇所でマッチ（リポジトリ従来ゼロ→1 件以上）
    - `pytest tests/test_ocr_providers.py -q` 全体がグリーン（Pitfall 4 の横断影響なし）
    - `ruff check pagefolio/ocr_providers.py tests/test_ocr_providers.py` がクリーン
  </acceptance_criteria>
  <done>URL スキーム検証・Gemini エスケープ・エラー body 切り詰め・Claude ページネーションが実装され、新旧プロバイダテストがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: 設定ダイアログ重複解消（L-6i）+ "off" 切替ボタン状態同期（L-6j）+ REVIEW.md 追記</name>
  <files>pagefolio/dialogs/llm_config.py, pagefolio/ocr_dialog.py, tests/test_provider_ui.py, .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md</files>
  <read_first>
    - pagefolio/dialogs/llm_config.py:1120-1141（`_fetch_models`）と :1142-1161（`_test_connection`）— 差分は Combobox への values 代入有無のみ
    - pagefolio/dialogs/llm_config.py:1164-1209（`_fetch_ollama_models`/`_test_ollama_connection` — 同型重複だが D-11 により本プラン対象外・Pitfall 5）
    - pagefolio/ocr_dialog.py:842-957（`_apply_llm_settings` — 正常系末尾 :957 `self._update_summary_btn_state()` と例外分岐 :952-955。Pitfall 6）
    - pagefolio/app.py:190-202（`_update_ocr_buttons_state` — ツールバー OCR ボタン活性の唯一の同期経路）
    - tests/test_provider_ui.py（既存 UI テストパターン・SimpleNamespace スタブ）
    - .planning/quick/260610-aaa-v140-review-fixplan/260610-aaa-REVIEW.md L-6i/L-6j 該当行（D-12）
  </read_first>
  <action>
    L-6i: `_fetch_models`(:1120-1141) と `_test_connection`(:1142-1161) の重複ロジックを共通ヘルパー（例 `_probe_lm_provider(update_combo: bool)`）へ集約し、両メソッドはそのヘルパーを呼ぶ薄いラッパーとして残す（ボタンの `command=` 呼び出し元は不変・RESEARCH.md Open Question 2 推奨）。差分は「取得モデルを Combobox へ反映するか」のみなので bool フラグでパラメータ化する。**スコープ:** LM Studio ペアのみ（L-6i 原文の明記範囲）。Ollama ペア（`_fetch_ollama_models`/`_test_ollama_connection`）は D-11 に従い Phase 4（V171-TEST-03）へ繰り越し、本プランでは変更しない（Pitfall 5）。共通ヘルパーが Ollama へ自然に波及する設計を採る場合は、その旨を SUMMARY にスコープ拡張として明記した上でのみ実施する（暗黙拡張は禁止）。

    L-6j: `_apply_llm_settings`(842-957) に `self.app._update_ocr_buttons_state()` の呼び出しを追加し、"off" 切替後にメイン画面ツールバーの OCR ボタンが disabled になるよう同期する。呼び出しは try/except の外側（関数末尾・:957 `_update_summary_btn_state()` の近傍）に置き、provider 再生成が例外で失敗した場合（:952-955 分岐）でも必ず実行されるようにする（Pitfall 6）。app 属性は既存の防御的パターン（getattr フォールバック）に倣い、SimpleNamespace スタブとの後方互換を保つ。

    tests/test_provider_ui.py に `off_toggle_buttons` 回帰テストを追加する: OCRDialog で provider を "off" に切り替えて _apply_llm_settings を通すと app._update_ocr_buttons_state が呼ばれる（またはボタン状態が disabled になる）ことを確認。provider 再生成が例外を投げるケースでも呼ばれることを確認。

    D-12: 解消した L-6d/L-6e/L-6f/L-6i/L-6j/L-6b について 260610-aaa-REVIEW.md の該当行へ ✅ + 解消コミットハッシュを追記する（Task 1 分と本タスク分をまとめて追記可）。

    裸の except 禁止・logger 経由記録（CLAUDE.md 規約）。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_provider_ui.py -k "off_toggle_buttons" -x` がグリーン
    - `pytest tests/test_provider_ui.py -q` 全体がグリーン
    - `grep -n "_probe_lm_provider\|def _fetch_models\|def _test_connection" pagefolio/dialogs/llm_config.py` で共通ヘルパー導入と両ラッパー存置を確認
    - `grep -n "_update_ocr_buttons_state" pagefolio/ocr_dialog.py` が 1 件以上マッチ（_apply_llm_settings 内）
    - Ollama ペア（_fetch_ollama_models/_test_ollama_connection）が本プランで変更されていない（明示拡張時は SUMMARY に記載）
    - 260610-aaa-REVIEW.md の L-6d/L-6e/L-6f/L-6i/L-6j/L-6b 行に ✅ が追記されている
    - `ruff check pagefolio/dialogs/llm_config.py pagefolio/ocr_dialog.py tests/test_provider_ui.py` がクリーン
  </acceptance_criteria>
  <done>LM Studio ペアの重複が共通ヘルパーへ集約され（呼び出し元不変）、"off" 切替でツールバー OCR ボタンが同期し、REVIEW.md に完了マークが追記された。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー入力 URL/モデル名 → HTTP リクエスト | 設定ダイアログ入力の URL・モデル名がそのまま urllib.request のエンドポイント/パスへ載る |
| 外部 API レスポンス body → エラーメッセージ表示 | プロバイダ応答 body が例外メッセージとして UI へ伝播 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | LM Studio/Ollama/RunPod の URL エンドポイント生成 | medium | mitigate | `_require_http_scheme(url)` で http/https のみ許可し file:///ftp:// 等を RuntimeError で拒否（L-6e/D-13） |
| T-2-02 | Tampering | Gemini `GENERATE_CONTENT_ENDPOINT.format(model=...)` | medium | mitigate | `urllib.parse.quote(self.model, safe="")` でモデル名をパスセグメントとしてエスケープし意図しないエンドポイントへの逸脱を防ぐ（L-6f） |
| T-2-03 | Information Disclosure/DoS | `_raise_mapped_http_error` のエラーメッセージ | low | mitigate | err_body を一定長で切り詰め、巨大レスポンスによる UI/ログ肥大を抑止（L-6d） |

本プランはパッケージインストールを伴わない（T-{phase}-SC 非該当）。URL 検証は `urllib.parse` 標準関数で実装（Don't Hand-Roll）。
</threat_model>

<verification>
- `pytest tests/test_ocr_providers.py tests/test_provider_ui.py -q` グリーン
- `pytest`（フルスイート）で回帰なし（Pitfall 4 の横断影響を含む）
- `ruff check . && ruff format .` クリーン
- 手動確認不要（すべて monkeypatch/スタブで自動化）
</verification>

<success_criteria>
- ROADMAP 成功基準 1（L-6 小物が現行照合の上で一括解消され、OCR 実行・接続テスト・モデル取得が従来どおり動作）が満たされる
- URL スキーム検証が 3 プロバイダへ統一適用される（D-13）
- LM Studio ペアの重複が解消され、Ollama ペアは D-11 に従い Phase 4 へ明示繰り越し
</success_criteria>

<output>
Create `.planning/phases/02-ocr/02-03-SUMMARY.md` when done
</output>
