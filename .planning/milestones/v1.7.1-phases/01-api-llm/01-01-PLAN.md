---
phase: 01-api-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/ocr.py
  - tests/test_ocr.py
  - pagefolio/lang.py
autonomous: true
requirements:
  - V171-KEY-02
  - V171-KEY-04
  - V171-TEST-02
must_haves:
  truths:
    - "_resolve_api_key は session_keys に値があればそれを返し、無ければ環境変数を返す（claude/gemini/runpod の3分岐すべて・D-08 系の優先順反転）"
    - "session_keys にも環境変数にもキーが無ければ OCRAPIKeyError(env_var) を raise する"
    - "_resolve_api_key は os.environ へ一切書き込まない（読み取り専用・Phase 05-03 原則の継続）"
    - "gemini の dual env var 内部順序（GEMINI_API_KEY → GOOGLE_API_KEY）は反転後も維持される"
    - "新規 LANG キーが ja/en 両辞書へ同一キー名で追加され、test_lang_parity がグリーン"
  artifacts:
    - "pagefolio/ocr.py（反転後の _resolve_api_key）"
    - "tests/test_ocr.py::TestResolveApiKeyRunPod（新設）"
    - "pagefolio/lang.py（llm_api_key_label / llm_key_toggle_show / llm_key_toggle_hide / llm_key_session_note / llm_key_env_set_note / ocr_api_key_missing_runpod 追加・ocr_api_key_missing[_gemini] 文言更新）"
  key_links:
    - "_resolve_api_key ← build_provider（pagefolio/ocr.py:625）・ocr_dialog._check_cloud_api_key（Plan 03）：全呼び出し経路が単一関数の反転を継承する"
    - "新 LANG キー ← LLMConfigDialog 入力欄（Plan 02）・OCRDialog エラー文言（Plan 03）"
---

<objective>
キー解決ロジックの優先順を「環境変数 → セッションキー」から「セッションキー(入力値) → 環境変数」へ反転し（V171-KEY-02 の核心・D-08 系の土台）、本フェーズの UI/ゲートが参照する新規 LANG 文言（ja/en）を先行整備する。あわせて反転仕様を固定する解決系テスト（claude/gemini の書き換え + RunPod 新設）を整備する（V171-KEY-04 の解決層・V171-TEST-02 の一部）。

Purpose: `_resolve_api_key` は build_provider・OCRDialog 実行ゲートの双方から呼ばれる単一の真実源であり、この1関数の反転で全経路が一括反転する（RESEARCH.md Pattern 1）。LANG を先行整備することで後続プラン（Plan 02 の入力欄・Plan 03 のエラー文言）の参照先を確定させ、実行中の KeyError を防ぐ。
Output: 反転後の `_resolve_api_key`・新規/更新 LANG キー・入力値優先を検証するテスト群（claude/gemini 書き換え + RunPod 新設）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-llm/01-CONTEXT.md
@.planning/phases/01-api-llm/01-RESEARCH.md
@.planning/phases/01-api-llm/01-PATTERNS.md
@.planning/phases/01-api-llm/01-UI-SPEC.md
@.planning/phases/01-api-llm/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 解決系テストを入力値優先へ書き換え + RunPod 版を新設</name>
  <files>tests/test_ocr.py</files>
  <read_first>
    - tests/test_ocr.py（既存 TestResolveApiKey :528-599 / TestResolveApiKeyGemini :1080-1133 の構造・フィクスチャ・monkeypatch パターンを踏襲）
    - pagefolio/ocr.py（_resolve_api_key :209 の現行シグネチャ `(provider_name, session_keys)` と OCRAPIKeyError の raise 形）
    - .planning/phases/01-api-llm/01-RESEARCH.md（Pitfall 4：既存テストは新規追加でなく書き換え。Validation Architecture → Test Map）
  </read_first>
  <behavior>
    - claude: session_keys={"claude":"K"} かつ環境変数 ANTHROPIC_API_KEY 設定時 → "K"（入力値優先）
    - claude: session_keys 空・環境変数のみ設定 → 環境変数値（フォールバック）
    - claude: 両方未設定 → OCRAPIKeyError を raise（env_var 属性が "ANTHROPIC_API_KEY"）
    - gemini: session_keys 優先。環境変数のみの場合 GEMINI_API_KEY を GOOGLE_API_KEY より優先（dual env の内部順序は不変）
    - runpod: 入力値優先 / 環境変数 RUNPOD_API_KEY フォールバック / 両方未設定で OCRAPIKeyError の3ケース
    - 全ケースで _resolve_api_key 呼び出し前後に os.environ が書き換わっていないこと（読み取り専用の回帰防止）
  </behavior>
  <action>
    既存の TestResolveApiKey / TestResolveApiKeyGemini を「入力値(session_keys)優先・環境変数フォールバック」の新仕様へ書き換える。env 優先を固定していたテスト（例 `test_env_var_takes_priority_over_session_key`）は `test_session_key_takes_priority_over_env_var` 相当へリネームし、アサーションと docstring を反転後の期待値へ更新する。両方未設定で OCRAPIKeyError を raise するテストは維持する。新規に TestResolveApiKeyRunPod クラスを追加し、claude/gemini と同型の3ケース（入力優先 / 環境変数フォールバック / raise）に加え、os.environ 非書き込みを検証するケースを含める。環境変数の設定/除去は既存テストと同じ monkeypatch.setenv/delenv パターンで行い、テスト間の環境汚染を避ける。この時点では Task 2 未実施のため入力値優先ケースは RED でよい（TDD の RED フェーズ）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_ocr.py -k "ResolveApiKey" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_ocr.py -k "TestResolveApiKeyRunPod" --collect-only -q` が3ケース以上を収集する（新設クラスが存在）
    - TestResolveApiKey / TestResolveApiKeyGemini / TestResolveApiKeyRunPod に「session_keys が環境変数より優先される」を主張するテストが各1件以上存在する
    - 少なくとも1件のテストが「_resolve_api_key 呼び出し前後で os.environ の該当キーが不変」を assert する
    - Task 2 前は入力値優先ケースが FAIL（RED）、両方未設定→raise ケースは PASS
  </acceptance_criteria>
  <done>解決系3クラスが反転仕様を固定し、RunPod 版が新設され、os.environ 非書き込みが検証されている（入力値優先ケースは Task 2 実施までは RED）</done>
</task>

<task type="auto">
  <name>Task 2: _resolve_api_key の優先順を反転（session_keys → 環境変数）</name>
  <files>pagefolio/ocr.py</files>
  <read_first>
    - pagefolio/ocr.py（_resolve_api_key :209-266 現行実装・3分岐すべて。build_provider :625 が呼び出し元であることを確認）
    - .planning/phases/01-api-llm/01-PATTERNS.md（"pagefolio/ocr.py — _resolve_api_key 優先順反転" 節：各分岐で env チェックと session チェックの順序を入替えるのみ）
    - .planning/phases/01-api-llm/01-RESEARCH.md（Code Examples の反転案・gemini dual env 内部順序維持）
  </read_first>
  <action>
    _resolve_api_key の claude/gemini/runpod 各分岐について、現行の「環境変数を先に os.environ.get で読み、無ければ session_keys.get」の順序を「session_keys.get を先に読み、非空なら返す。無ければ環境変数を os.environ.get で読む。どちらも無ければ OCRAPIKeyError(env_var) を raise」へ入れ替える。gemini 分岐の環境変数解決は `os.environ.get("GEMINI_API_KEY") or os.environ.get("GOOGLE_API_KEY")` の内部順序をそのまま維持する（反転対象は「セッションキー群 vs 環境変数群」の優劣のみ）。docstring の優先順記述を「セッションキー(入力値) > 環境変数（V171-KEY-02・優先順反転）」へ更新する。関数シグネチャ `(provider_name, session_keys)` は変更しない。os.environ への書き込みコードパスは一切追加しない（読み取り専用原則の維持・Phase 05-03 決定の継続）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_ocr.py -k "ResolveApiKey" -q && ruff check pagefolio/ocr.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_ocr.py -k "ResolveApiKey" -q` が全ケース PASS（Task 1 の RED が GREEN 化）
    - _resolve_api_key のシグネチャは `(provider_name, session_keys)` のまま（build_provider / ocr_dialog 呼び出し元は無変更で動作）
    - 関数本体に os.environ への代入（`os.environ[...] =` や `os.environ.setdefault` 等）が存在しない
    - `ruff check pagefolio/ocr.py` がクリーン
  </acceptance_criteria>
  <done>3プロバイダすべてで session_keys 優先・環境変数フォールバック・両方未設定で raise が成立し、gemini dual env 内部順序が保たれ、os.environ 非書き込みが維持されている</done>
</task>

<task type="auto">
  <name>Task 3: 新規/更新 LANG キーを ja/en 両辞書へ整備</name>
  <files>pagefolio/lang.py</files>
  <read_first>
    - pagefolio/lang.py（既存 ocr_api_key_missing :397 / ocr_api_key_missing_gemini :425、en 側 :952 / :980。既存 llm_env_key_unset_static[_gemini/_runpod] :482,:562,:565 は既に存在＝新規追加不要）
    - .planning/phases/01-api-llm/01-UI-SPEC.md（Copywriting Contract の LANG キー契約表：新規キーと更新キーの ja/en 確定文言）
    - tests/test_lang_parity.py（ja/en キー集合一致・プレースホルダ整合の検証内容）
  </read_first>
  <action>
    UI-SPEC.md の LANG キー契約表に従い、ja/en 両辞書へ同一キー名で以下を追加する（キー数の左右一致を維持）：`llm_api_key_label`（「APIキー:」/「API Key:」）、`llm_key_toggle_show`（「👁 表示」/「👁 Show」）、`llm_key_toggle_hide`（「🙈 隠す」/「🙈 Hide」）、`llm_key_session_note`（D-03 のセッション限定注記）、`llm_key_env_set_note`（D-07・`{env_var}` プレースホルダ付き）、`ocr_api_key_missing_runpod`（D-08・RunPod 用の導線案内文言）。加えて既存キー `ocr_api_key_missing` / `ocr_api_key_missing_gemini` の文言を D-08 の「LLM設定ダイアログで APIキーを入力するか、環境変数を設定してください」導線案内へ更新する（キー名は変更しない・`{env_var}` プレースホルダは維持）。既存 `llm_env_key_unset_static[_gemini/_runpod]` は D-11 のヒント文言として既に存在するため、UI-SPEC の更新後文言と差異があれば ja/en 同時に text のみ調整する（キー追加はしない）。`ocr_session_key_label` は本プランでは削除しない（参照する OCRDialog UI を撤去する Plan 03 と同一プランで削除し、実行時 KeyError の窓を作らない）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_lang_parity.py -q && ruff check pagefolio/lang.py</automated>
  </verify>
  <acceptance_criteria>
    - ja/en 両辞書に `llm_api_key_label` `llm_key_toggle_show` `llm_key_toggle_hide` `llm_key_session_note` `llm_key_env_set_note` `ocr_api_key_missing_runpod` が存在する
    - `llm_key_env_set_note` と `ocr_api_key_missing[_gemini|_runpod]` はいずれも `{env_var}` プレースホルダを ja/en 双方で同数含む
    - `ocr_api_key_missing` / `ocr_api_key_missing_gemini` の ja 文言に「LLM設定ダイアログ」の導線案内が含まれる
    - `ocr_session_key_label` は ja/en とも依然存在する（本プランでは未削除）
    - `python -m pytest tests/test_lang_parity.py -q` がグリーン（ja/en キー数一致）
  </acceptance_criteria>
  <done>後続プランが参照する新規文言が ja/en 同一キーで揃い、既存エラー文言が導線案内へ更新され、lang parity がグリーン</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 呼び出し元 → _resolve_api_key | プロバイダ名とセッションキー dict が渡る。関数は解決結果（キー文字列）を返すか OCRAPIKeyError を raise する。機密値（APIキー）がこの境界を通過する |
| _resolve_api_key → os.environ | 環境変数の読み取りのみ（書き込み禁止の境界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Tampering | pagefolio/ocr.py::_resolve_api_key | medium | mitigate | 反転実装は os.environ を read-only 参照のみ。代入コードパスを追加しない（acceptance_criteria で os.environ 非書き込みを assert・Task 1 のテストで回帰防止） |
| T-1-01a | Information Disclosure | _resolve_api_key / lang 文言 | high | mitigate | 解決関数はキー値を返すのみで settings/ログへ流さない。LANG エラー文言は env_var 名のみ埋め込み、キー値は文言に一切含めない（`{env_var}` プレースホルダのみ） |
| T-1-02 | Information Disclosure | テスト内のキー値 | low | mitigate | テストのダミーキーは架空値のみ使用。実キーをテストへ埋め込まない |
| T-1-SC | Tampering | pip インストール | low | accept | 本フェーズは新規パッケージ導入なし（RESEARCH.md「Package Legitimacy Audit: 該当なし」）。install タスクなし・受容 |
</threat_model>

<verification>
- Task 単位: `python -m pytest tests/test_ocr.py tests/test_provider_ui.py tests/test_settings_keyguard.py -q`
- プラン完了時: `python -m pytest -q`（フルスイート・707 件ベースライン）+ `ruff check . && ruff format --check .`
- 反転の全経路波及確認: `python -m pytest tests/test_ocr.py -k "ResolveApiKey" -q` が claude/gemini/runpod すべてグリーン
</verification>

<success_criteria>
- _resolve_api_key が3プロバイダで「入力値 → 環境変数 → OCRAPIKeyError」の順に解決する
- os.environ への書き込みが一切ない（読み取り専用維持）
- 新規/更新 LANG キーが ja/en 同一キーで整備され lang parity がグリーン
- 解決系テスト（claude 書き換え・gemini 書き換え・RunPod 新設）がグリーン
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

新規シンボル（drift 検証の除外対象）:
- `tests/test_ocr.py::TestResolveApiKeyRunPod`（新規テストクラス）
- LANG 新規キー: `llm_api_key_label`, `llm_key_toggle_show`, `llm_key_toggle_hide`, `llm_key_session_note`, `llm_key_env_set_note`, `ocr_api_key_missing_runpod`（ja/en 両辞書）

変更（新規シンボルではないが挙動反転）:
- `pagefolio/ocr.py::_resolve_api_key`（優先順反転・シグネチャ不変）
- LANG 更新キー: `ocr_api_key_missing`, `ocr_api_key_missing_gemini`（文言更新・キー名不変）
</artifacts_produced>

<output>
Create `.planning/phases/01-api-llm/01-01-SUMMARY.md` when done
</output>
