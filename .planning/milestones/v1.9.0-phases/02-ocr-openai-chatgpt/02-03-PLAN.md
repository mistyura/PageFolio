---
phase: 02-ocr-openai-chatgpt
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - pagefolio/ocr_providers/openai_provider.py
  - pagefolio/dialogs/llm_config/sections.py
  - pagefolio/dialogs/llm_config/dialog.py
  - pagefolio/dialogs/llm_config/model_fetch.py
  - pagefolio/lang.py
  - tests/test_ocr_providers.py
  - tests/test_provider_ui.py
autonomous: true
requirements:
  - V190-CAT-01
  - V190-OAI-01
  - V190-OAI-02
  - V190-OAI-03

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ユーザーは LLM 設定ダイアログのプロバイダ combobox から OpenAI(ChatGPT) を選択できる（V190-OAI-01）"
    - "sections.py の _base_providers と _base_fallback_providers がリテラルリストではなく catalog.provider_names() / catalog.fallback_candidate_names() を情報源とし、CONTEXT.md D-03 が列挙した 6 参照面の catalog 移行が完走している（V190-CAT-01・D-03）"
    - "ユーザーは OpenAI セクションでマスク表示付きの API キーを入力でき、その値は app._session_api_keys へのみ保持され pagefolio_settings.json へ書き出されない（V190-OAI-02・D-18）"
    - "ユーザーは OpenAI セクションのモデル更新ボタンで GET /v1/models からモデル一覧を取得でき、非チャットモデルがヒューリスティックフィルタで除外される（V190-OAI-03・D-07）"
    - "モデル一覧は能力マトリクスで画像入力を確認済みのモデル（VERIFIED_VISION_MODELS）が先頭に並び、未確認のモデルはその後に並ぶ。既定選択値が未確認グループから選ばれることはなく、未確認モデルが含まれる旨の注記が UI に常時表示される（レビュー HIGH 02-03-1）"
    - "モデル ID フィルタの除外マーカーに単独の image は含まれず、画像生成モデルは gpt-image のような限定的なマーカーで除外される（将来の vision モデル過剰除外の回避・レビュー MEDIUM 02-03-3）"
    - "モデル一覧の取得に失敗した場合と、フィルタ結果が 0 件の場合は、Combobox に入る一覧とステータス表示の LANG キーが完全に同一である（D-08 の「同一経路」を観測可能な結末の同一性として定義・レビュー MEDIUM-11）"
    - "ヒューリスティックフィルタは Tk / ネットワーク非依存の純関数として切り出され、除外パターンが単体テストで固定されている（D-07）"
    - "モデル一覧取得中にモデル更新ボタンを再クリックしても取得スレッドは二重起動せず（_fetch_models_async の _model_fetch_running ガード）、ダイアログ破棄後に結果が届いても Combobox 更新は静かに破棄される。_refresh_openai_models は独自スレッドを起動しない（edge: V190-OAI-03 concurrency）"
    - "モデル一覧取得のコールバックは combobox の values のみを書き換え、ユーザーが選択中のモデル変数を上書きしない（遅延到達した結果が新しい選択を壊さない・edge: V190-OAI-03 ordering）"
    - "OpenAI 選択時に openai_section_frame が表示され、他プロバイダ選択時には pack_forget される（既存 claude / gemini / tesseract と同じ排他制御）"
    - "推論系モデルを選んだとき temperature 欄が非表示になり、非推論系では表示される。この判定は is_reasoning_model 純関数のみが行う（D-13）"
    - "openai から claude / gemini を経て openai へ戻る往復操作で temperature_frame / effort_frame / openai_section_frame の表示状態が毎回正しく復元される（レビュー MEDIUM-13）"
    - "lang.py の ja / en に llm_fetching_openai_models・llm_env_key_unset_static_openai・llm_openai_model_unverified_note が同一キーで追加され、キー数の左右一致と未使用キー検出テストが green のまま。各キーはそれを参照するタスクと同一コミットで追加される（レビュー LOW 02-03-6）"
    - "既存 6 プロバイダのセクション表示・切替・_apply 収集内容が無変更である（edge: V190-OAI-01 regression）"
    - "フルテストスイート（tests/test_ocr_pipeline.py を含む全件）が失敗 0 件で完走する（レビュー HIGH-6）"
  prohibitions:
    - statement: "MUST NOT OpenAI の API キーを llm_settings や pagefolio_settings.json へ到達させてはならない。セッションメモリ（app._session_api_keys）のみに保持する"
      category: secrets
    - statement: "MUST NOT 送信先確認ダイアログの同意を経ずに外部ホストへ OCR リクエストを送ってはならない。本プランで openai を選択可能にできるのは 02-02 の同意ゲートが完成しているためである"
      category: transparency
    - statement: "MUST NOT 新規 pip 依存を追加してはならない"
      category: dependencies
    - statement: "MUST NOT registry.py に pagefolio 内部モジュールの import を追加してはならない"
      category: architecture
    - statement: "MUST NOT プロバイダ combobox を自由入力にしてはならない。state=readonly と catalog 由来 values の制約を外すと未知プロバイダが安全境界を素通りする"
      category: input-validation
  artifacts:
    - path: "pagefolio/dialogs/llm_config/sections.py"
      provides: "OpenAI セクション UI + プロバイダ一覧の catalog 化"
      contains: "openai_section_frame"
    - path: "pagefolio/dialogs/llm_config/model_fetch.py"
      provides: "_refresh_openai_models（_fetch_models_async 経由の非同期取得）"
      contains: "_refresh_openai_models"
    - path: "pagefolio/ocr_providers/openai_provider.py"
      provides: "list_models（GET /v1/models）+ filter_selectable_models 純関数 + order_models_for_display"
      contains: "def filter_selectable_models"
  key_links:
    - from: "pagefolio/dialogs/llm_config/sections.py"
      to: "pagefolio/ocr_providers/catalog.py"
      via: "プロバイダ combobox の values とフォールバック候補一覧の解決（D-03 移行 6/6）"
      pattern: "catalog\\.(provider_names|fallback_candidate_names)"
    - from: "pagefolio/dialogs/llm_config/model_fetch.py"
      to: "pagefolio/ocr_providers/openai_provider.py"
      via: "_refresh_openai_models が OpenAIProvider(...).list_models を _fetch_models_async へ渡す"
      pattern: "_fetch_models_async\\(provider\\.list_models"
    - from: "pagefolio/dialogs/llm_config/dialog.py"
      to: "pagefolio/ocr_providers/openai_provider.py"
      via: "_on_openai_model_change が is_reasoning_model を呼ぶ（D-13 の単一判定源）"
      pattern: "is_reasoning_model\\("
    - from: "pagefolio/dialogs/llm_config/dialog.py"
      to: "app._session_api_keys"
      via: "_apply のセッション API キー同期ループへ openai を追加（settings へは入れない）"
      pattern: "\\(\"openai\", self\\.openai_api_key_var\\)"
---

<objective>
LLM 設定ダイアログで OpenAI(ChatGPT) をプロバイダとして選択できるようにし、セッション限定
API キーの入力欄とモデル一覧取得（API 取得 + ヒューリスティックフィルタ + 能力マトリクス
由来の並び替え + 静的フォールバック）を提供する。あわせて `sections.py` に残っていた
プロバイダ一覧リテラル 2 箇所を catalog 化し、D-03 の 6 参照面移行を完走させる。

Purpose: 02-02 で送信先確認・コスト確認・APIキー欠落ゲートが openai に対応したので、
ここで初めて openai をユーザーが選択できる状態にしても安全境界が空かない。
このプランの完了で「ユーザーが OpenAI を選び、キーを入れ、モデルを選び、OCR を実行して
確認ダイアログを経て結果を得る」という主経路が通る。

02-REVIEWS.md の HIGH（除外型フィルタでは OCR 利用可能性を保証できない）に対しては、
D-07/D-08 の locked decision を維持したまま **能力マトリクスで確認済みのモデルを
先頭・既定に据え、未確認モデルには注記を出す**という構造で応える。

Output: `openai_provider.list_models` + `filter_selectable_models` / `order_models_for_display`
純関数、`sections.py` の OpenAI セクションと一覧 catalog 化、`dialog.py` の openai 分岐と
`_apply` 収集、`model_fetch._refresh_openai_models`、lang ja/en 3 キー、回帰テスト。

**UI 設計契約について（記録）:** 本フェーズは `--skip-ui` で計画されており `-UI-SPEC.md` は存在しない。
UI 安全ゲートは本フェーズを frontend-touching と判定したが、02-PATTERNS.md が OpenAI セクションと
既存 Claude セクション（`sections.py:411-510`）の一致度を「exact」と判定しており、
ウィジェット構成・配置・スタイル規約が既存アナログから決定できるため override した。
新規ウィジェットのスタイルは CLAUDE.md に従う（通常操作は `"TButton"`、フォントは
`self._font(delta)`、色は `C["BG_DARK"]` / `C["BG_CARD"]` / `C["TEXT_MAIN"]` / `C["TEXT_SUB"]` の
テーマ辞書参照。ハードコードした色・フォントサイズは使わない）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr-openai-chatgpt/02-CONTEXT.md
@.planning/phases/02-ocr-openai-chatgpt/02-PATTERNS.md
@.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md
@.planning/phases/02-ocr-openai-chatgpt/02-REVIEWS.md
@.planning/phases/02-ocr-openai-chatgpt/02-01-SUMMARY.md
@CLAUDE.md
@pagefolio/CLAUDE.md
</context>

<review_dispositions>
## 02-REVIEWS.md の指摘に対する本プランの処置

| # | 指摘（要約） | severity | 処置 | 根拠 / 反映先 |
|---|---|---|---|---|
| 3 | 除外型フィルタでは OCR 利用可能性を保証できない | HIGH | **採用（レビュー提案の第 2 案）** | D-07/D-08 は locked のためフィルタ方式自体は変えない。代わりに Task 1 で `order_models_for_display()` を追加し、`VERIFIED_VISION_MODELS`（02-01 の能力マトリクス由来）を先頭に、未確認 ID をその後に並べる。既定選択値は常に確認済みグループから取る。UI には `llm_openai_model_unverified_note` を常時表示する。関数名も `filter_chat_models` ではなく **`filter_selectable_models`** とし、docstring に「能力保証をしない」ことを明記する（レビュー Suggestion のとおり） |
| 11 | 取得失敗時のフォールバック経路が二重で D-08 と一致しない | MEDIUM | **採用（D-08 の解釈を明文化 + 機械保証）** | `list_models()` が HTTP 失敗を例外化する構造は既存 Claude/Gemini（`pagefolio/ocr_providers/claude.py:259-341`・`model_fetch.py:230-243`）と同型であり、ここだけ握り潰すと UI がエラーを示せなくなる。したがって **D-08 の「同一経路」を「観測可能な結末の同一性」と定義**し、`_on_error` 経路と 0 件経路が **同じ combobox values と同じステータス LANG キー**を生むことを Task 3(5) のテストで機械保証する。この解釈を `list_models` の docstring と本プランに記録する |
| 02-03-3 | `image` を除外マーカーに含めるのは過剰除外 | MEDIUM | **採用** | 単独の `image` を除外マーカーから外し、画像生成モデルは `gpt-image` / `dall-e` のような限定的なマーカーで落とす。Task 1(1) と Task 1(4) のテストで固定 |
| 10 | モデル一覧取得のエラー処理が OCR 実行時と非対称（`_raise_mapped_http_error` 不使用で 429/`Retry-After` が効かない） | MEDIUM | **却下（スコープを明文化）** | V190-OAI-13 のリトライ基盤は `ocr_pipeline.consume_one`（`pagefolio/ocr_pipeline.py:223-269`）の実行ループが実装しており、モデル一覧取得はそのループを通らない。`list_models` を `OCRRetryableError` にしても `_fetch_models_async._worker` が汎用 `Exception` で捕まえて静的一覧へ落とす（`model_fetch.py:156-162`）だけで、**再試行は 1 回も起きない**。例外型が変わるだけで挙動は不変であり、既存 4 プロバイダの `list_models` との対称性も壊れる。よって OCR 実行経路に限定する旨を `list_models` の docstring と本プランに明記して据え置く |
| 13 | `temperature_frame` の往復テストが必要 | MEDIUM | **採用** | Task 3(5) に `TestProviderRoundTripFrameState` を追加（openai → claude → gemini → openai） |
| 02-03-6 | LANG キー追加を Task 2 と Task 3 の同一コミットへまとめる指示が「1 タスクずつ完了」と緊張する | LOW | **採用（タスク境界を組み替え）** | 各 LANG キーを**それを参照するタスクと同じタスク**で追加する。`llm_openai_model_unverified_note` は Task 2（sections.py が参照）、`llm_fetching_openai_models` と `llm_env_key_unset_static_openai` は Task 3（model_fetch.py が参照）。これで各タスク単独で `test_no_unused_lang_keys` を通せる |
| S5 | `_fetch_models_async` に世代ガードがあるか実コード確認が必要 | 🔍 | **却下（実コード照合済み・防御不要）** | `pagefolio/dialogs/llm_config/model_fetch.py:136-138` の `_model_fetch_running` ガードにより取得は同時 1 本に直列化され、in-flight が 2 本になることがない。さらに完了コールバックは `combo["values"]` にしか書かず（同 219 / 235 / 259 / 275 行）、`*_model_var.set(...)` を呼ばないため、**遅延到達した結果がユーザーの新しい選択を上書きする経路が存在しない**。世代カウンタは不要。この性質を Task 3(5) の `TestRefreshOpenaiModels` で回帰固定する（コールバックが `openai_model_var` に触れないことのアサーション） |
| 全体-2 | `ast.parse` 構文チェックが無い | MEDIUM | **採用** | 各タスクの `<verify>` と `<verification>` に追加 |
| 全体-3 | 全テストゲートの扱いが不整合 | HIGH | **採用（例外を撤廃）** | 02-01-PLAN.md「テストゲートの確定」節に従い、フルスイート失敗 0 件を絶対ゲートにする |
| 全体-5 | `grep` が Windows で使えない可能性 | LOW | **採用** | 全検証コマンドを `python -c` ワンライナーへ統一（`git grep` も使わない） |
| 6（LOW） | 構造アサーションが実装詳細へ強く結合 | LOW | **採用（方式変更）** | 純関数性・スレッド非起動・単一判定源の検査を `ast.parse` ベースへ変更（コメント・docstring に影響されない） |
</review_dispositions>

<flagged_assumptions>
## Flagged Assumptions（edge probe — unresolved / unclassified）

| Requirement | Probe | 本プランでの扱い（未解決の前提） |
|---|---|---|
| V190-OAI-01 | unclassified — review manually | 「ユーザーが OpenAI を選択できる」の自動検証は `provider_combo["values"]` に `"openai"` が含まれることと `_on_provider_change("openai")` でセクションが pack されることまで。実際に combobox を操作したときのレイアウト崩れ・ダイアログ高さ追従（`_resize_to_fit`）は 02-04 の human-verify に委ねる。`ocr_provider` の既定は引き続き `"off"`（V14-D-03）で OpenAI が既定になることはない前提 |
| V190-OAI-02 | unclassified — review manually | 「永続化されない」の自動検証は `_apply` の戻り値 `llm_settings` にキーが含まれないことと `_save_settings` のキーガードまで。プロセスメモリのダンプやスワップ経由の露出は対象外（OS キーストア連携は v2 の Deferred 項目） |
| V190-OAI-03 | unclassified — review manually | D-07 のヒューリスティックフィルタは「OpenAI が将来もモデル ID の命名規則を保つ」という RESEARCH.md Assumptions Log A1 の仮定に依存する。命名規則が変わればフィルタが陳腐化するが、D-08 により 0 件時は静的一覧へ合流するため「何も選べない」状態にはならない。また、一覧に残った未確認 ID が実際に画像入力へ対応するかは検証しない（`order_models_for_display` の並び替えと UI 注記でリスクを可視化するに留める） |
| V190-CAT-01 | unclassified — review manually | 本プラン完了をもって D-03 の 6 参照面がすべて移行済みになる。ただし「新プロバイダ追加時の変更面が 1 箇所」は 02-01 で定義した「軸ごとに 1 箇所」（catalog 1 エントリ + registry 1 エントリ）の意味であり、Provider クラス本体と `build_provider` 分岐の追加は別途必要という前提 |
</flagged_assumptions>

<artifacts>
## Artifacts this phase produces（本プランの分）

**新規シンボル（`pagefolio/ocr_providers/openai_provider.py`）**
- `filter_selectable_models(model_ids)` — Tk / ネットワーク非依存の純関数（D-07）。
  **能力を保証しない**除外型フィルタであることを docstring に明記する
- `order_models_for_display(model_ids)` — Tk 非依存の純関数。
  `VERIFIED_VISION_MODELS` に含まれる ID をその宣言順で先頭に、残りを入力順で後ろに並べる
- `_EXCLUDED_MODEL_MARKERS`（フィルタの除外部分文字列タプル・02-01 の能力マトリクス由来）
- `OpenAIProvider.list_models()` の本実装（`GET /v1/models` + フィルタ + 並び替え + 静的フォールバック）

**新規シンボル（`pagefolio/dialogs/llm_config/sections.py`）**
- `self.openai_section_frame` / `self.openai_model_var` / `self.openai_model_combo` /
  `self.openai_api_key_var` / `self.openai_api_key_entry` / `self._openai_key_shown`

**新規シンボル（`pagefolio/dialogs/llm_config/dialog.py`）**
- `_on_openai_model_change(_event=None)`

**新規シンボル（`pagefolio/dialogs/llm_config/model_fetch.py`）**
- `_refresh_openai_models()`

**新規 LANG キー（ja / en 両方に同一キー・追加するタスクを明示）**
- `llm_openai_model_unverified_note`（**Task 2** で追加。sections.py が参照）—
  「一覧には画像入力対応が未確認のモデルが含まれる場合があります」の趣旨
- `llm_fetching_openai_models`（**Task 3** で追加。model_fetch.py が参照）—
  既存 `llm_fetching_claude_models` と同型
- `llm_env_key_unset_static_openai`（**Task 3** で追加。model_fetch.py が参照）—
  既存 `llm_env_key_unset_static_gemini` と同型

**新規セッションキースロット**
- `app._session_api_keys["openai"]`（メモリのみ・永続化しない）
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: OpenAI モデル一覧取得・除外フィルタ純関数・確認済み優先の並び替え</name>
  <files>pagefolio/ocr_providers/openai_provider.py, tests/test_ocr_providers.py</files>
  <read_first>
    - `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md`（導出結果 (2) の `RECOMMENDED_MODELS` と (4) の除外対象実例）
    - `pagefolio/ocr_providers/openai_provider.py`（02-01 で作成した実体。`MODELS_ENDPOINT` / `RECOMMENDED_MODELS` / `VERIFIED_VISION_MODELS` / `_headers` / `model_list_timeout` / 暫定 `list_models`）
    - `pagefolio/ocr_providers/claude.py` 259-341 行（`list_models` の「キー未設定 → RECOMMENDED_MODELS」分岐と `_fetch_models_page` の HTTP 例外規約。`capabilities.image_input.supported` で絞れる Anthropic との非対称の確認）
    - `pagefolio/ocr_providers/gemini.py` の `list_models`（`supportedGenerationMethods` によるフィルタ — OpenAI に相当フィールドが無いことの対比）
    - `pagefolio/ocr_providers/lmstudio.py` 157-188 行（`/v1/models` レスポンスの `data[].id` 取り出しと例外規約）
    - `pagefolio/ocr_pipeline.py` 223-269 行（`consume_one` のリトライループ — V190-OAI-13 の適用範囲がここに限られることの確認）
    - `tests/test_ocr_providers.py` の `TestClaudeProviderListModels` / `TestLMStudioProviderListModels` / `TestModelListTimeout`
  </read_first>
  <action>
`pagefolio/ocr_providers/openai_provider.py` にモデル一覧取得を実装する。

(1) モジュールレベルに `_EXCLUDED_MODEL_MARKERS` を定義する。02-01 の能力マトリクス
導出結果 (4) に記録された実在モデル ID を根拠に、OCR / チャット用途でないモデルを表す
部分文字列のタプルとする。最低限 `embedding` / `tts` / `whisper` / `audio` / `dall-e` /
`gpt-image` / `moderation` / `realtime` / `transcribe` / `search` を含めること。
**単独の `image` は含めない**（将来 ID に `image` を含む vision 対応モデルが出た場合に
過剰除外になるため。画像生成モデルは `gpt-image` / `dall-e` で落とす。レビュー MEDIUM 02-03-3）。
判断根拠（どの実モデル ID を除外したいか）を docstring に書く。

(2) モジュールレベル純関数 `filter_selectable_models(model_ids)` を定義する（D-07）。
Tk / HTTP / インスタンス状態に一切触れない純関数として、入力のモデル ID 文字列
イテラブルから `_EXCLUDED_MODEL_MARKERS` のいずれかを小文字化後の ID に含むものを除外し、
入力順を保ったリストを返す。空・None 要素は落とす。
docstring に次を明記する（レビュー HIGH 02-03-1 の Suggestion）:
「本関数は**選択肢として提示してよいか**を判定するだけで、**画像入力に対応するかは
保証しない**。OpenAI の `GET /v1/models` には Anthropic の `capabilities.image_input`
に相当するフィールドが存在しないため（RESEARCH.md Pitfall 2）、能力の確認は
`02-CAPABILITY-MATRIX.md` と `VERIFIED_VISION_MODELS` が担う」。

(3) モジュールレベル純関数 `order_models_for_display(model_ids)` を定義する
（レビュー HIGH 02-03-1）。`VERIFIED_VISION_MODELS` に含まれる ID をその宣言順で先頭へ、
残りを入力順のまま後ろへ並べたリストを返す。重複は先勝ちで 1 回だけ出す。
これにより Combobox の先頭（＝ユーザーが最初に目にする候補と、値未設定時に
選ばれる候補）が常に画像入力確認済みのモデルになる。

(4) `OpenAIProvider.list_models()` を本実装に差し替える。
`self.api_key` が偽なら `list(self.RECOMMENDED_MODELS)` を返す（`claude.py:320-322` と同型・D-08）。
キーがあれば `MODELS_ENDPOINT` へ `urllib.request.Request(..., headers=self._headers(),
method="GET")` で GET し、`timeout=self.model_list_timeout`（クラス属性 30）を使う。
例外処理は `claude.py:_fetch_models_page` と同じ規約（`socket.timeout` → `TimeoutError`、
`HTTPError` → `RuntimeError(f"HTTP {e.code}: {e.reason}")`、`URLError` → reason が timeout なら
`TimeoutError` そうでなければ `ConnectionError`、JSON 不正は `RuntimeError`）。
`data[].id` を集めて `filter_selectable_models` → `order_models_for_display` の順に通し、
結果が空リストなら `list(self.RECOMMENDED_MODELS)` を返す。
`# noqa: S310` は `urlopen` / `Request` の位置に付ける。`errors.py` は変更しない。
独自の待機・再送は書かない。

docstring に 2 つの設計判断を記録する:
- **D-08 の「同一経路」の解釈（レビュー MEDIUM-11）:** 本メソッドは HTTP 失敗を例外化し、
  0 件のみ静的一覧へ合流させる。既存 Claude / Gemini と同じ構造であり、UI 側
  （`model_fetch.py` の `_on_error`）が例外を受けて同じ静的一覧と同じステータスへ
  合流させる。D-08 が求める「失敗時パスの 1 本化」は**観測可能な結末の同一性**
  （Combobox に入る一覧とステータス表示が完全に一致する）として満たす。
  この同一性は `tests/test_provider_ui.py` の回帰テストで固定されている。
- **V190-OAI-13 の適用範囲（レビュー MEDIUM-10）:** 429/5xx の指数バックオフと
  `Retry-After` 尊重は OCR 実行ループ（`ocr_pipeline.consume_one`）の責務であり、
  モデル一覧取得はそのループを通らないため適用対象外。既存 4 プロバイダの
  `list_models` も同様で、ここだけ非対称にはしない。

(5) `tests/test_ocr_providers.py` に以下を追加する。
`TestOpenAIFilterSelectableModels` — 純関数の単体テスト。02-01 の能力マトリクスに
記録した実在の除外対象 ID がすべて落ち、チャット / vision 系 ID が入力順のまま残ること。
空入力で空リスト、全件除外で空リストになること。
**`image` を含むが `gpt-image` ではない架空 ID（例: `gpt-5-image-reasoning`）が
除外されないこと**を明示的にアサートする（レビュー MEDIUM 02-03-3 の回帰固定）。
`TestOpenAIOrderModelsForDisplay` — `VERIFIED_VISION_MODELS` の全 ID が
宣言順で先頭に来ること、未確認 ID が入力順で後ろに来ること、重複が 1 回だけになること、
空入力で空リストになること。
`TestOpenAIProviderListModels` — `_FakeResponse` で `/v1/models` 応答を差し替え、
(a) キー未設定で HTTP を叩かず `RECOMMENDED_MODELS` を返す、
(b) キーありでフィルタ + 並び替え後の一覧を返し先頭が確認済みモデルである、
(c) フィルタ結果が 0 件になる応答で `RECOMMENDED_MODELS` へ合流する、
(d) `HTTPError` / `URLError` / `socket.timeout` がそれぞれ `RuntimeError` / `ConnectionError` /
`TimeoutError` になる、(e) `Authorization: Bearer` ヘッダが GET にも付く、
(f) `urlopen` に渡る `timeout` が 30 であることを検証する。
`TestOpenAIFilterIsPure` — **AST ベース**の純度アサーション（レビュー LOW-17 の方式変更）。
`ast.parse` で `filter_selectable_models` と `order_models_for_display` の関数ノードを取り出し、
引数に `self` が無いこと、ボディ内に `urllib` / `tkinter` を参照する `ast.Attribute` /
`ast.Name` が無いこと、`ast.Call` の呼び先に I/O 系（`urlopen` / `Request` / `open`）が
無いことを検証する。部分文字列検査は使わない。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('pagefolio/ocr_providers/openai_provider.py').read_text(encoding='utf-8'))" && python -m pytest tests/test_ocr_providers.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from pagefolio.ocr_providers.openai_provider import filter_selectable_models as f; assert f(['gpt-x','text-embedding-3-large','tts-1','whisper-1','dall-e-3','omni-moderation-latest','gpt-image-1']) == ['gpt-x']"` が終了コード 0
    - `python -c "from pagefolio.ocr_providers.openai_provider import filter_selectable_models as f; assert f([]) == [] and f(['tts-1']) == [] and f(['gpt-5-image-reasoning']) == ['gpt-5-image-reasoning']"` が終了コード 0（過剰除外の回避・レビュー MEDIUM 02-03-3）
    - `python -c "from pagefolio.ocr_providers.openai_provider import order_models_for_display as o, OpenAIProvider as P; v=list(P.VERIFIED_VISION_MODELS); r=o(['zzz-unverified']+v); assert r[:len(v)]==v and r[-1]=='zzz-unverified'"` が終了コード 0
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/ocr_providers/openai_provider.py').read_text(encoding='utf-8')); fns={n.name:n for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; f=fns['filter_selectable_models']; args=[a.arg for a in f.args.args]; sys.exit(1 if 'self' in args else 0)"` が終了コード 0（純関数であることの AST アサーション）
    - `python -m pytest tests/test_ocr_providers.py -k "OpenAIFilterSelectableModels or OpenAIOrderModelsForDisplay or OpenAIProviderListModels or OpenAIFilterIsPure" -q` が最低 12 テスト green
    - 既存の `TestModelListTimeout` が無改修で green（既存プロバイダの期待値は不変）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>`OpenAIProvider.list_models()` が実 API 取得 → 除外フィルタ → 確認済み優先の並び替え → 0 件/失敗時の静的フォールバック、の全経路を持ち、フィルタと並び替えは Tk・ネットワーク非依存の純関数として AST アサーションつきの単体テストで固定されている。D-08 の解釈と V190-OAI-13 の適用範囲が docstring に記録されている。</done>
</task>

<task type="auto">
  <name>Task 2: LLM 設定ダイアログの OpenAI セクション新設とプロバイダ一覧の catalog 化</name>
  <files>pagefolio/dialogs/llm_config/sections.py, pagefolio/lang.py</files>
  <read_first>
    - `pagefolio/dialogs/llm_config/sections.py` 1-120 行（import 群・`_configured_env_var`・`_base_providers` リスト 84-96 行と `provider_combo` 構築）
    - `pagefolio/dialogs/llm_config/sections.py` 411-512 行（Claude セクション全体 — 雛形）
    - `pagefolio/dialogs/llm_config/sections.py` 512-560 行（Gemini セクション冒頭 — セクション同士の並び順）
    - `pagefolio/dialogs/llm_config/sections.py` 1015-1045 行（`_base_fallback_providers` 1021-1029 行と `_fallback_known_providers` のホワイトリスト検証）
    - `pagefolio/ocr_providers/catalog.py`（`provider_names` / `fallback_candidate_names` の宣言順）
    - `pagefolio/ocr_providers/openai_provider.py`（`RECOMMENDED_MODELS` / `VERIFIED_VISION_MODELS` / `order_models_for_display`）
    - `pagefolio/lang.py` の `llm_fetching_claude_models` / `llm_env_key_unset_static_gemini` / `llm_key_session_note` 周辺（ja / en 両方）
    - `CLAUDE.md`「ボタンスタイル」「フォントサイズ」「テーマ色の参照」
  </read_first>
  <action>
`pagefolio/dialogs/llm_config/sections.py` を 3 点変更する。D-03 のとおり 1 箇所ずつ適用し、
その都度 `tests/test_provider_ui.py::TestLLMConfigProviderValues` を通すこと。

(1) `_base_providers`（84-96 行）のリストリテラルを `catalog.provider_names()` に置換する。
`catalog` はファイル冒頭で `from pagefolio.ocr_providers import catalog` として import してよい
（`sections.py` は既に `from pagefolio.ocr_providers import ClaudeProvider, GeminiProvider` と
`from pagefolio.ocr_providers.registry import env_vars_for` を冒頭 import しており、
循環 import は発生しない）。`values=_base_providers + _plugin_extras` の構造と
プラグイン追加分の連結、`state="readonly"` は変更しない。catalog の宣言順が現行リストの表示順
（`off` / `lmstudio` / `ollama` / `runpod` / `claude` / `gemini` / `tesseract`）と一致し、
`openai` が末尾に来ることは 02-01 Task 3(A) の `TestCatalogContents` が固定済み。

(2) `_base_fallback_providers`（1021-1029 行）のリストリテラルを
`catalog.fallback_candidate_names()` に置換する。`_fallback_known_providers` の
ホワイトリスト検証（入力検証・ASVS L1）と `_fallback_chain` の読み込み時フィルタは
変更しない。これにより openai がフォールバック候補一覧へ自動的に現れる（V190-OAI-07 の前提）。

(3) Gemini セクションの直後（Tesseract セクションの前）に OpenAI 固有欄を新設する。
Claude セクション（411-510 行）を雛形とし、以下 6 要素をこの順で構築する。
色は `C[...]` の辞書参照、フォントは `self._font(-1)` / `self._font(-2)`、
ボタンは `ttk.Button`（既定の `"TButton"`）を使い、ハードコードした色・フォントサイズは書かない。

  a. `self.openai_section_frame = tk.Frame(body, bg=C["BG_DARK"])`
  b. モデル行 — ラベルは既存の汎用キー `self._L["settings_lm_model"]`（`width=20, anchor="w"`）。
     `self.openai_model_var = tk.StringVar(value=self.current_settings.get("openai_model", "") or catalog.default_model_for("openai"))`。
     `self.openai_model_combo = ttk.Combobox(..., values=order_models_for_display(OpenAIProvider.RECOMMENDED_MODELS))` を
     `side="left", fill="x", expand=True, padx=4` で配置し、`<<ComboboxSelected>>` を
     `self._on_openai_model_change` にバインドする（Claude が `self._on_model_change` に
     バインドしているのと同じ位置づけ。ハンドラは Task 3 で dialog.py に追加する）。
  c. **モデル注記ラベル（新規・レビュー HIGH 02-03-1）** — `self._L["llm_openai_model_unverified_note"]`
     を `fg=C["TEXT_SUB"]`, `font=self._font(-2)`, `wraplength=460`, `justify="left"` で
     モデル行の直下に常時表示する。一覧の後半に画像入力未確認のモデルが含まれ得ることを
     ユーザーへ伝える唯一の UI である。
  d. API キー行 — ラベルは既存の汎用キー `self._L["llm_api_key_label"]`。
     `self.openai_api_key_var = tk.StringVar(value=self._session_api_keys.get("openai", ""))`、
     `self.openai_api_key_entry = tk.Entry(..., show="*", bg=C["BG_CARD"], fg=C["TEXT_MAIN"], insertbackground=C["TEXT_MAIN"], relief="flat")`、
     `self._openai_key_shown = False`、およびトグル関数と
     `ttk.Button(..., text=self._L["llm_key_toggle_show"], width=4)`。
     Claude と同じく **トグルボタンを先に `side="right"` でパックしてから** Entry を
     `side="left", fill="x", expand=True` でパックする（幅不足時に Entry 側が縮む）。
  e. 環境変数注記ラベル — `self._L["llm_key_session_note"]` に、
     `_configured_env_var("openai")` が True を返したとき
     `self._L["llm_key_env_set_note"].format(env_var=...)` を連結する。
     `_configured_env_var("openai")` は `registry.env_vars_for("openai")` が
     `("OPENAI_API_KEY",)` を返すことで自動的に機能する（`registry.py` の追加変更は不要・D-06/D-18）。
  f. モデル更新ボタン行 — `ttk.Button(..., text=self._L["ocr_model_refresh"], command=self._refresh_openai_models)`。
     ハンドラは Task 3 で `model_fetch.py` に追加する。

  detail / reasoning effort / organization / project の各欄は本タスクでは追加しない
  （V190-OAI-08/09/10 は 02-04 の責務）。折りたたみ UI は導入しない（D-17）。

(4) `pagefolio/lang.py` の ja / en 両方へ `llm_openai_model_unverified_note` を
**1 件だけ**追加する（レビュー LOW 02-03-6 — キーは参照タスクと同一コミットで追加する）。
本文は「モデル一覧には画像入力への対応が未確認のモデルが含まれる場合があります。
推奨モデル（一覧の上部）以外を選ぶ場合はご注意ください」の趣旨。
`llm_fetching_openai_models` と `llm_env_key_unset_static_openai` は Task 3 で追加する
（それらを参照するのは `model_fetch.py` であり Task 3 の担当のため。
本タスク単独で `tests/test_lang_parity.py::test_no_unused_lang_keys` が通る）。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['pagefolio/dialogs/llm_config/sections.py','pagefolio/lang.py']]" && python -m pytest tests/test_provider_ui.py tests/test_lang_parity.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pathlib; s=pathlib.Path('pagefolio/dialogs/llm_config/sections.py').read_text(encoding='utf-8'); [__import__('sys').exit('missing: '+t) for t in ['catalog.provider_names()','catalog.fallback_candidate_names()','openai_section_frame','openai_api_key_var','openai_model_combo','order_models_for_display','llm_openai_model_unverified_note'] if t not in s]"` が終了コード 0
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/dialogs/llm_config/sections.py').read_text(encoding='utf-8')); lists=[n for n in ast.walk(t) if isinstance(n,ast.List)]; bad=[l for l in lists if [e.value for e in l.elts if isinstance(e,ast.Constant)]==['off','lmstudio','ollama','runpod','claude','gemini','tesseract']]; sys.exit(1 if bad else 0)"` が終了コード 0（プロバイダ一覧リテラルの消滅）
    - 既存の `tests/test_provider_ui.py::TestLLMConfigProviderValues` が、combobox の values に `openai` が含まれるよう更新されたうえで green（既存 7 プロバイダの並び順は不変）
    - `python -c "from pagefolio.lang import LANG; assert set(LANG['ja']) == set(LANG['en']); assert 'llm_openai_model_unverified_note' in LANG['ja'] and 'llm_openai_model_unverified_note' in LANG['en']"` が終了コード 0
    - `python -m pytest tests/test_lang_parity.py -q` が green（本タスク単独で未使用キー 0 件）
    - `python -m pytest tests/test_font_hardcode_guard.py -q` が green（`self._font(delta)` 使用の遵守）
    - `python -c "import pathlib,re,sys; s=pathlib.Path('pagefolio/dialogs/llm_config/sections.py').read_text(encoding='utf-8'); i=s.index('openai_section_frame'); seg=s[i:i+7000]; sys.exit(1 if (re.search(r'bg=\"#', seg) or re.search(r'fg=\"#', seg)) else 0)"` が終了コード 0（OpenAI セクション内で色の直書きが無い）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>プロバイダ combobox に openai が現れ、OpenAI セクションのモデル欄（確認済み優先の並び）・未確認モデル注記・APIキー欄・マスクトグル・環境変数注記・モデル更新ボタンが構築される。プロバイダ一覧とフォールバック候補一覧が catalog 由来になり、D-03 の 6 参照面移行が完走。</done>
</task>

<task type="auto">
  <name>Task 3: dialog.py の openai 分岐・セッションキー同期と非同期モデル取得の配線</name>
  <files>pagefolio/dialogs/llm_config/dialog.py, pagefolio/dialogs/llm_config/model_fetch.py, pagefolio/lang.py, tests/test_provider_ui.py</files>
  <read_first>
    - `pagefolio/dialogs/llm_config/dialog.py` 174-306 行（`_on_provider_change` の全分岐・`_on_model_change`・`_model_supports_effort`）
    - `pagefolio/dialogs/llm_config/dialog.py` 378-534 行（`_apply` の収集順序とセッション API キー同期ループ 518-529 行）
    - `pagefolio/dialogs/llm_config/model_fetch.py` 1-35 行（`_env_fallback` と冒頭 import）、124-164 行（`_fetch_models_async` の `_model_fetch_running` ガードと `winfo_exists` チェック）、206-243 行（`_refresh_claude_models` 全文）
    - `pagefolio/dialogs/llm_config/sections.py`（Task 2 で作成した `openai_*` ウィジェット名と注記ラベル）
    - `pagefolio/ocr_providers/openai_provider.py`（`is_reasoning_model` / `RECOMMENDED_MODELS` / `order_models_for_display` / `list_models`）
    - `pagefolio/lang.py` の `llm_fetching_claude_models` / `llm_env_key_unset_static_gemini`（ja / en 両方）
    - `tests/test_provider_ui.py` 726-900 行（`_VarStub` / `TestSyncParamVarsFromSettings` / `TestApplyLlmSettings*`）と 1262-1430 行（`TestApiKeyNotInSettings` / `TestSessionKeyStoreAndClear` / `TestRunpodSessionKeySlot`）
  </read_first>
  <action>
(1) `pagefolio/dialogs/llm_config/dialog.py` の `_on_provider_change` に openai 分岐を追加する。
Claude 分岐（219-230 行）と同型で `self.openai_section_frame.pack(fill="x", padx=24, pady=(4, 2), before=self.scale_row)` を行い、
`claude_section_frame` / `gemini_section_frame` / `tesseract_section_frame` を `pack_forget()`、
`self._common_section_heading.pack(anchor="w", padx=24, pady=(6, 2), before=self.scale_row)` の後に
`self._on_openai_model_change()` を呼ぶ。
**既存の全分岐（lmstudio / ollama / runpod / claude / gemini / tesseract / else）にも
`self.openai_section_frame.pack_forget()` を追加する**（追加漏れがあると別プロバイダへ
切り替えても OpenAI 欄が残る。往復テストで検出する）。

(2) `_on_openai_model_change(self, _event=None)` を新設する。
`from pagefolio.ocr_providers import is_reasoning_model` を関数内 import し、
`is_reasoning_model(self.openai_model_var.get())` が真なら `self.temperature_frame.pack_forget()`、
偽なら `self.temperature_frame.pack(fill="x", padx=24, pady=2, before=self.scale_row)` とし、
どちらの場合も `self.effort_frame.pack_forget()`（Claude 専用欄なので OpenAI では常に隠す）を
行ってから `self._resize_to_fit()` を呼ぶ。
判定は `is_reasoning_model` のみを情報源とし、`dialog.py` 側に独自のモデル名判定を書かない
（D-13。`_model_supports_effort` が `ClaudeProvider.EFFORT_MODELS` を再実装している
既存の負債を OpenAI では繰り返さない）。OpenAI 専用の reasoning effort 欄は 02-04 で
この同じ関数に足す。

(3) `_apply` に openai の収集を追加する。Gemini 設定の直後に
`llm_settings["openai_model"] = self.openai_model_var.get().strip()` を置く
（空文字を許容する。空のときは `build_provider` が `catalog.default_model_for("openai")` へ
解決するため、モデル ID 文字列を `dialog.py` に重複させない）。
API キーは `llm_settings` に絶対に入れない。
セッション API キー同期ループ（520-524 行のタプル列）へ
`("openai", self.openai_api_key_var)` を 1 行追加する（V190-F-03 のとおり
ループの完全動的化はスコープ外・1 行追記に留める）。

(4) `pagefolio/dialogs/llm_config/model_fetch.py` に `_refresh_openai_models` を新設する。
`_refresh_claude_models`（206-243 行）を構造ごと複製し、
`self._set_lm_status(self._L["llm_fetching_openai_models"], kind="info")`、
`api_key = self.openai_api_key_var.get().strip() or _env_fallback("openai")`、
`provider = OpenAIProvider(api_key=api_key, model="")`、
`_on_success(models)` は `self.openai_model_combo["values"] = models` とキー有無での
ステータス出し分け（キー無しは `llm_env_key_unset_static_openai`）、
`_on_error(e)` は `logger.warning(self._L["llm_model_fetch_failed"].format(provider="OpenAI", e=e))` と
`self.openai_model_combo["values"] = order_models_for_display(OpenAIProvider.RECOMMENDED_MODELS)` と
`self._set_lm_status(self._L["llm_env_key_unset_static_openai"], kind="info")`、
最後に `self._fetch_models_async(provider.list_models, _on_success, _on_error)` を呼ぶ。

**D-08 の観測同一性（レビュー MEDIUM-11）:** `_on_error` が入れる values は、
`list_models` が 0 件合流で返す値（`RECOMMENDED_MODELS` を並び替えたもの）と
**完全に同一**でなければならず、`_set_lm_status` に渡す LANG キーも同一でなければならない。
両者が一致していることを Task 3(5) のテストで固定する。

`_on_success` / `_on_error` のいずれも `self.openai_model_var` を書き換えないこと
（`_fetch_models_async` は `_model_fetch_running` で取得を直列化しており、
コールバックが `combo["values"]` にしか触れないことで「遅延到達した結果が
ユーザーの新しい選択を上書きする」経路が構造的に存在しなくなる。
既存 Claude / Gemini / RunPod のコールバックも同じ性質を持つ）。
独自の `threading.Thread` は起動しない。
`OpenAIProvider` と `order_models_for_display` は冒頭 import 行
（`from pagefolio.ocr_providers import ClaudeProvider, GeminiProvider, LMStudioProvider`）へ追加する。

(5) `pagefolio/lang.py` の ja / en 両方へ `llm_fetching_openai_models` と
`llm_env_key_unset_static_openai` を追加する（本タスクが参照するキーのみ・
レビュー LOW 02-03-6）。前者は `llm_fetching_claude_models` と同型の 1 行、
後者は `llm_env_key_unset_static_gemini` を雛形に `OPENAI_API_KEY` 未設定時に
静的一覧を表示している旨を書く。

(6) `tests/test_provider_ui.py` に以下を追加する。既存の `_VarStub` / `_FakeStringVar` /
`_ButtonStub` を再利用し、新しいスタブ作法を持ち込まない。
- `TestOpenAiSessionKeySlot` — `TestRunpodSessionKeySlot` と同型。`_apply` 後に
  `_session_api_keys["openai"]` へ値が入り、空入力なら pop されること。
- `TestApiKeyNotInSettings` に openai ケースを追加 — `_apply` が返す `llm_settings` に
  `openai_api_key` / `OPENAI_API_KEY` が含まれないこと。
- `TestOnProviderChangeOpenai` — `_on_provider_change` で `openai` を選んだとき
  `openai_section_frame` が pack され、claude / gemini / tesseract が pack_forget されること。
  逆に `claude` を選んだとき `openai_section_frame` が pack_forget されること。
- `TestProviderRoundTripFrameState` — レビュー MEDIUM-13。
  `openai` → `claude` → `gemini` → `openai` の順に `_on_provider_change` を呼び、
  各段で `temperature_frame` / `effort_frame` / `openai_section_frame` の
  pack / pack_forget 状態が期待どおりになること。最後の `openai` で
  1 回目の `openai` と同じ状態に戻ることをアサートする（分岐の追加漏れ検出装置）。
- `TestOnOpenaiModelChange` — 推論系モデル ID（02-01 の能力マトリクスの真ケース実例）で
  `temperature_frame` が pack_forget され、非推論系で pack されること。
  **AST ベース**の構造アサーションで、`dialog.py` の `_on_openai_model_change` 関数ノードに
  正規表現呼び出し（`re.` 参照）・集合リテラル・文字列 prefix 比較が現れず、
  `is_reasoning_model` の呼び出しが存在することを固定する（D-13）。
- `TestRefreshOpenaiModels` — `_fetch_models_async` をフェイクに差し替え、
  (a) `_fetch_models_async` を 1 回だけ呼ぶ、
  (b) 渡す callable が `OpenAIProvider.list_models` である、
  (c) 入力欄が空のとき `_env_fallback("openai")` 経由で `OPENAI_API_KEY` が使われる、
  (d) **`_on_error` 実行後の combobox values と、`list_models` の 0 件合流が返す値が
  完全一致し、`_set_lm_status` に渡された LANG キーも一致すること**（D-08 の観測同一性・
  レビュー MEDIUM-11）、
  (e) `_on_success` / `_on_error` のどちらも `openai_model_var.set` を呼ばないこと
  （レビュー S5 に対する回帰固定）、
  (f) `model_fetch.py` の `_refresh_openai_models` 関数ノードに `threading` を参照する
  `ast.Attribute` が無いこと（AST ベース）、を検証する。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['pagefolio/dialogs/llm_config/dialog.py','pagefolio/dialogs/llm_config/model_fetch.py','pagefolio/lang.py','tests/test_provider_ui.py']]" && python -m pytest tests/test_provider_ui.py tests/test_lang_parity.py tests/test_imports.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pathlib,sys; s=pathlib.Path('pagefolio/dialogs/llm_config/dialog.py').read_text(encoding='utf-8'); sys.exit(0 if s.count('openai_section_frame.pack_forget()') >= 4 and 'is_reasoning_model' in s else 1)"` が終了コード 0
    - `python -c "import pathlib,sys; s=pathlib.Path('pagefolio/dialogs/llm_config/dialog.py').read_text(encoding='utf-8'); sys.exit(0 if '(\"openai\", self.openai_api_key_var)' in s else 1)"` が終了コード 0
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/dialogs/llm_config/model_fetch.py').read_text(encoding='utf-8')); fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='_refresh_openai_models'][0]; bad=[n for n in ast.walk(fn) if isinstance(n,ast.Name) and n.id=='threading']; sys.exit(1 if bad else 0)"` が終了コード 0（独自スレッド非起動・AST ベース）
    - `python -m pytest tests/test_provider_ui.py -k "Openai or OpenAI or RoundTrip" -q` が最低 10 テスト green
    - `TestRefreshOpenaiModels` の (d) が「`_on_error` 後の values」と「`list_models` の 0 件合流の戻り値」を同一オブジェクト比較でアサートしている（D-08 の観測同一性）
    - `python -c "from pagefolio.lang import LANG; assert set(LANG['ja']) == set(LANG['en']); [__import__('sys').exit('missing: '+k) for k in ['llm_fetching_openai_models','llm_env_key_unset_static_openai'] if k not in LANG['en']]"` が終了コード 0
    - `python -m pytest tests/test_lang_parity.py -q` が green（未使用キー 0 件）
    - `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が **失敗 0 件**（`--ignore` 例外を使わない・レビュー HIGH-6）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>LLM 設定ダイアログで openai を選ぶと OpenAI セクションが表示され、セッション限定キーの入力と保存（メモリのみ）、モデル一覧の非同期取得と静的フォールバックが機能する。取得失敗と 0 件が同一の観測結果へ合流することがテストで固定され、プロバイダ往復で各フレームの表示状態が正しく復元される。モデル種別による temperature 欄の切替は `is_reasoning_model` 単一判定で駆動されている。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー入力（API キー Entry）→ アプリメモリ / ディスク | 機密が入力される地点。`_apply` が `llm_settings` へ入れない契約が唯一のガード |
| アプリ → `api.openai.com/v1/models` | モデル一覧取得で API キーが外部へ出る地点（OCR とは別のリクエスト） |
| バックグラウンドスレッド → Tk メインスレッド | `_fetch_models_async` の `after(0)` 越境。ダイアログ破棄後の到達が UI 例外になり得る地点 |
| プロバイダ combobox の選択値 → `settings["ocr_provider"]` | catalog 由来の値のみが入るべき地点（自由入力を許すと未知プロバイダが安全境界を素通りする） |
| モデル一覧（未検証 ID を含む）→ ユーザーの選択 | 画像入力に対応しないモデルを選ばせると課金だけ発生する地点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `dialog.py:_apply` | critical | mitigate | `openai_api_key` を `llm_settings` に入れず、セッション同期ループでのみ `app._session_api_keys` へ書く（Task 3(3)）。`TestApiKeyNotInSettings` の openai ケースで固定（Task 3(6)）。二重の砦として `settings.py:_SENSITIVE_KEYS`（02-01 で導出済み） |
| T-02-02 | Information Disclosure | `model_fetch.py:_refresh_openai_models` の `_on_error` | high | mitigate | `logger.warning` に渡すのは `llm_model_fetch_failed` テンプレート + 例外オブジェクトのみで、API キー変数を渡さない（既存 Claude / Gemini と同型）。`_set_lm_status` には例外文字列ではなく定型 LANG メッセージを渡す |
| T-02-11 | Information Disclosure | `api.openai.com/v1/models` へのキー送信 | medium | accept | モデル一覧取得は OCR とは別に API キーを OpenAI へ送るが、これは V190-OAI-03 が要求する機能そのもの。ユーザーが「モデル更新」ボタンを明示的に押した場合にのみ発火し、`GET /v1/models` は課金対象外・ページ画像を含まない。ページ画像送信（T-02-03）とは別リスクとして受容する |
| T-02-12 | Denial of Service | モデル更新ボタン連打によるスレッド増殖 | low | mitigate | `_fetch_models_async` の `_model_fetch_running` ガードで二重起動しない（`model_fetch.py:136-138`）。`_refresh_openai_models` が独自 `threading.Thread` を起動しないことを AST アサーションで固定（Task 3(6)(f)） |
| T-02-13 | Tampering | プロバイダ combobox への未知プロバイダ名の混入 | medium | mitigate | `provider_combo` は `state="readonly"` を維持し、values は `catalog.provider_names() + plugin_extras` のみ（Task 2(1)）。フォールバック候補は `_fallback_known_providers` のホワイトリスト検証を維持（Task 2(2)） |
| T-02-19 | Tampering | 画像入力非対応モデルを選ばせて課金だけ発生させる | medium | mitigate | `order_models_for_display` で確認済みモデルを先頭・既定に据え（Task 1(3)）、`llm_openai_model_unverified_note` を常時表示する（Task 2(3)c）。実行直前の確認ダイアログでも 02-02 が未確認モデルの注記を出す。レビュー HIGH 02-03-1 |
| T-02-20 | Tampering | 遅延到達したモデル一覧がユーザーの新しい選択を上書きする | low | mitigate | `_fetch_models_async` の直列化ガードと、コールバックが `combo["values"]` にしか触れない設計で構造的に防ぐ。`TestRefreshOpenaiModels` (e) で `openai_model_var.set` の非呼び出しを固定（Task 3(6)）。レビュー S5 の実コード照合結果 |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本プランは新規パッケージを一切インストールしない（V190-OAI-11）。RESEARCH.md「Package Legitimacy Audit」該当なしの判定を継承 |
</threat_model>

<verification>
- `python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['pagefolio/ocr_providers/openai_provider.py','pagefolio/dialogs/llm_config/sections.py','pagefolio/dialogs/llm_config/dialog.py','pagefolio/dialogs/llm_config/model_fetch.py','pagefolio/lang.py']]"` が終了コード 0
- `python -m pytest tests/test_provider_ui.py tests/test_ocr_providers.py tests/test_lang_parity.py tests/test_imports.py tests/test_font_hardcode_guard.py -q` が全件 green
- `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が **失敗 0 件**
- `ruff check . && ruff format --check .` が終了コード 0
- `git diff pagefolio/ocr_providers/registry.py pagefolio/ocr_providers/errors.py` が空（D-06 / D-12）
- `git diff --stat requirements.txt pyproject.toml` が空（V190-OAI-11）
- `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/dialogs/llm_config/sections.py').read_text(encoding='utf-8')); lists=[[e.value for e in n.elts if isinstance(e,ast.Constant)] for n in ast.walk(t) if isinstance(n,ast.List)]; sys.exit(1 if ['off','lmstudio','ollama','runpod','claude','gemini','tesseract'] in lists else 0)"` が終了コード 0（一覧リテラルの消滅）
</verification>

<success_criteria>
- V190-OAI-01: プロバイダ combobox から OpenAI を選択でき、OpenAI 固有欄が表示される
- V190-OAI-02: セッション限定 API キーを入力でき、`pagefolio_settings.json` に書き出されない
- V190-OAI-03: モデル一覧を API から取得でき、非チャットモデルが除外され、確認済みモデルが先頭に並び、取得失敗・0 件時は静的一覧から選択できる
- V190-CAT-01: `sections.py` の一覧リテラル 2 箇所が catalog 化され、D-03 の 6 参照面移行が完走
- レビュー HIGH 02-03-1: 未確認モデルが既定にならず、UI に注記が常時出る
- レビュー MEDIUM-11: 取得失敗と 0 件が同一の観測結果へ合流することがテストで固定
- レビュー MEDIUM-13: プロバイダ往復でフレーム表示状態が復元される
- レビュー MEDIUM 02-03-3: 単独 `image` による過剰除外が無いことがテストで固定
- 既存 6 プロバイダのセクション表示・切替・`_apply` 収集内容が無変更
</success_criteria>

<output>
Create `.planning/phases/02-ocr-openai-chatgpt/02-03-SUMMARY.md` when done
</output>
</content>
