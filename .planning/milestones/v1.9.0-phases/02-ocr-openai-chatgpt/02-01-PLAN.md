---
phase: 02-ocr-openai-chatgpt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md
  - pagefolio/ocr_providers/catalog.py
  - pagefolio/ocr_providers/openai_provider.py
  - pagefolio/ocr_providers/registry.py
  - pagefolio/ocr_providers/__init__.py
  - pagefolio/ocr.py
  - pagefolio/settings.py
  - tests/test_ocr_provider_catalog.py
  - tests/test_ocr_providers.py
autonomous: false
requirements:
  - V190-CAT-01
  - V190-CAT-02
  - V190-OAI-02
  - V190-OAI-11
  - V190-OAI-12
  - V190-OAI-13

user_setup:
  - service: openai
    why: "D-09 + レビュー HIGH-1 — モデル ID の実在確認（Stage A）と能力確認（Stage B: vision 入力 / max_completion_tokens / temperature 可否 / reasoning_effort 値域）を実 API で行う場合に必要（Task 1 で option-a を選ぶ場合のみ）"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Platform -> Dashboard -> API keys（https://platform.openai.com/api-keys）"
    dashboard_config: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "02-CAPABILITY-MATRIX.md が存在し、採用する全 OpenAI モデルについて vision 入力 / max_completion_tokens / temperature 可否 / reasoning_effort 可否 / 許容 effort 値域 / 入力単価 / 出力単価 / 単位 / 通貨 / 出典 URL / 参照日 / 根拠種別が 1 行 1 モデルで埋まっている（レビュー HIGH-1・HIGH-2）"
    - "catalog.py がプロバイダのキー・表示名 LANG キー・クラウド種別・モデル設定キー・既定モデル・送信先ホスト・フォールバック可否・APIキー欠落 LANG キーを 1 エントリ / 1 プロバイダで解決する（V190-CAT-01・D-01）"
    - "ProviderMeta の 8 フィールドはフィールド単位の型表（本プラン artifacts 節）どおりに宣言され、nullable なフィールドは実際に None を取り得る値で登録されている（レビュー MEDIUM-7）"
    - "catalog.py の import は標準ライブラリと pagefolio.ocr_providers.registry のみで、pagefolio 内部の他モジュールを AST 上 1 件も import しない（D-05/D-06）"
    - "registry.py は PROVIDER_ENV_KEYS への openai 1 行追加以外に変更されておらず、AST 上 os 以外を import しない（V190-CAT-02・D-06）"
    - "環境変数名の単一情報源は registry.py であり catalog.py は環境変数名を再定義も re-export もしない。V190-CAT-01 の「1 箇所」は「軸ごとに 1 箇所（非機密メタデータ=catalog / 機密キー・環境変数=registry）」を意味する（レビュー MEDIUM-4）"
    - "registry.sensitive_keys() が openai_api_key / OPENAI_API_KEY / openai_api_key(lower) を含み、_save_settings がそれらを pagefolio_settings.json へ書き出さない（V190-OAI-02・D-18）"
    - "build_provider({\"ocr_provider\": \"openai\", ...}, api_key=...) が OpenAIProvider を返し、ocr_image() が https://api.openai.com/v1/chat/completions へ Authorization: Bearer 付きで POST して choices[0].message.content を返す（V190-OAI-11）"
    - "OpenAI ペイロードのトークン上限キーは常に max_completion_tokens であり、推論系モデルと判定された場合は temperature を送らない（V190-OAI-12・D-10/D-11）"
    - "推論系モデルか否かの判定は単一の公開純関数 is_reasoning_model(model) のみが行い、プロバイダ側と UI 側が同じ関数を参照する（D-13）"
    - "is_reasoning_model の真ケース・偽ケースの機械テストは 02-CAPABILITY-MATRIX.md で reasoning_effort=yes と確認したモデル ID を最低 1 件含む（o 系だけの命名規則テストで終わらせない・レビュー HIGH 02-01-2）"
    - "OpenAI の 429/5xx は既存の errors.py:_raise_mapped_http_error 経由で OCRRetryableError にマップされ、openai_provider.py は独自の待機・再送処理を持たない（V190-OAI-13・D-12）"
    - "openai_provider.py は AST 上 ssl を import せず、urlopen 呼び出しに context 引数を渡さない（既定 TLS 検証を使う）"
    - "catalog.host_for('openai', {}) と OpenAIProvider.CHAT_ENDPOINT のホスト部が一致し、claude / gemini についても同じ一致がテストで固定されている（T-02-04・レビュー 02-01 Suggestion 4 により 02-02 から前倒し）"
    - "OpenAI の API キーはセッションメモリのみに保持され、並列 OCR 実行中・実行中断後のいずれでも pagefolio_settings.json に openai_api_key / OPENAI_API_KEY が書き出されない（edge: V190-OAI-02 concurrency）"
    - "OpenAIProvider は並列 OCR 実行（concurrency=2）でインスタンス属性を変異させず、_build_payload を同一入力で複数回呼んでも生成 payload が同一である（edge: V190-OAI-11 concurrency / V190-OAI-13 idempotency）"
    - "429 応答の待機は既存の clamp_retry_after（60 秒上限）/ interruptible_sleep のみを経路とし、openai_provider.py に独自待機呼び出しが AST 上存在しない（edge: V190-OAI-13 concurrency）"
    - "本プラン完了時点で pagefolio_settings.json の既定値に openai_* 非機密 5 キーが追加され、API キー系キーは 1 件も追加されていない（edge: V190-OAI-02 default-state）"
    - "フルテストスイート（tests/test_ocr_pipeline.py を含む全件）が失敗 0 件で完走する（レビュー HIGH-6・実測ベースライン 1187 passed / 0 failed）"
  prohibitions:
    - statement: "MUST NOT OpenAI の API キー（openai_api_key / OPENAI_API_KEY / その小文字）を pagefolio_settings.json へ到達させてはならない"
      category: secrets
    - statement: "MUST NOT 新規 pip 依存を追加してはならない（requirements.txt / pyproject.toml を変更しない・urllib.request 標準ライブラリのみで実装する）"
      category: dependencies
    - statement: "MUST NOT registry.py に pagefolio 内部モジュールの import を追加してはならない（V180-ROBUST-02 の独立性制約）"
      category: architecture
    - statement: "MUST NOT 送信先確認ダイアログの同意を経ずに外部ホストへ OCR リクエストを送ってはならない。本プランでは openai をプロバイダ combobox へ追加しないことでこの窓を構造的に閉じる"
      category: transparency
    - statement: "MUST NOT 確認ダイアログに表示する送信先ホストと実際にリクエストを送るホストを乖離させてはならない。catalog の openai ホスト値と OpenAIProvider のエンドポイント定数のホスト部は常に一致しなければならない"
      category: transparency
  artifacts:
    - path: ".planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md"
      provides: "モデル能力マトリクス + 価格プロヴェナンス（02-02 / 02-03 / 02-04 が消費する単一情報源）"
      contains: "| model_id |"
    - path: "pagefolio/ocr_providers/catalog.py"
      provides: "ProviderMeta dataclass + PROVIDERS dict + 解決純関数群（V190-CAT-01）"
      contains: "class ProviderMeta"
    - path: "pagefolio/ocr_providers/openai_provider.py"
      provides: "OpenAIProvider（Chat Completions・urllib 直叩き）+ is_reasoning_model 純関数"
      contains: "class OpenAIProvider"
    - path: "tests/test_ocr_provider_catalog.py"
      provides: "catalog の不変条件・独立性制約・default_model 機械保証テスト（D-05/V190-CAT-02）"
      contains: "def test_"
  key_links:
    - from: "pagefolio/ocr_providers/catalog.py"
      to: "pagefolio/ocr_providers/registry.py"
      via: "環境変数名の解決を registry へ委譲する一方向 import（D-06）"
      pattern: "from pagefolio\\.ocr_providers\\.registry import"
    - from: "pagefolio/ocr.py"
      to: "pagefolio/ocr_providers/openai_provider.py"
      via: "build_provider の openai 分岐が関数内 import で OpenAIProvider を生成"
      pattern: "OpenAIProvider\\("
    - from: "pagefolio/ocr.py"
      to: "pagefolio/ocr_providers/catalog.py"
      via: "_start_ocr のクラウド判定を catalog.is_cloud_provider へ置換（D-03 移行 1/6）"
      pattern: "catalog\\.is_cloud_provider"
    - from: "pagefolio/settings.py"
      to: "pagefolio/ocr_providers/registry.py"
      via: "_SENSITIVE_KEYS = sensitive_keys() が openai_api_key を自動導出（既存配線・D-18）"
      pattern: "sensitive_keys\\(\\)"
---

<objective>
プロバイダメタデータの単一情報源（`pagefolio/ocr_providers/catalog.py`）を新設し、その上に
OpenAI(ChatGPT) プロバイダの縦スライスを 1 本通す。ユーザー設定 → `build_provider` →
`OpenAIProvider` → `api.openai.com` → OCR テキスト、という経路が実際に動作する状態を最初に作る。
あわせて、後続 3 プランが共通で参照する **モデル能力マトリクス**
（`02-CAPABILITY-MATRIX.md`）を確定させる。

Purpose: 本フェーズの未知は 3 点に集中している。(a) catalog のデータ形状が 6 参照面すべてを
賄えるか、(b) OpenAI 固有のパラメータ分岐（`max_completion_tokens` 常用・推論系の
`temperature` 省略）が既存のリトライ/エラーマッピング基盤の上で成立するか、
(c) **どのモデルが実際に画像入力・`reasoning_effort` に対応し、いくらかかるのか**。
(c) はレビュー（02-REVIEWS.md HIGH-1 / HIGH-2）が最大のブロッカーとして指摘した点で、
`GET /v1/models` はモデル ID の存在しか返さないため能力確認にはならない。
UI 層は 02-PATTERNS.md が既存 Claude セクションとの一致度を「exact」と判定しており
設計上の未知が無いため、本プランでは触らない。

Output: `02-CAPABILITY-MATRIX.md`（新設）・`catalog.py`（新設）・`openai_provider.py`（新設）・
`registry.py` 1 行追加・`__init__.py` re-export・`ocr.py` の openai 分岐とクラウド判定移行・
`settings.py` の既定値・catalog 契約テスト一式。

**安全境界の順序制約（重要）:** 本プランでは `openai` を LLM 設定ダイアログのプロバイダ
combobox へ追加しない。OCR ダイアログ側の送信先確認・コスト確認・APIキー欠落ゲートが
`openai` に対応する 02-02 の完了前に選択可能にすると、確認ダイアログを経ずにページ画像が
外部送信され得る窓が空くため。combobox への追加は 02-03 の責務。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr-openai-chatgpt/02-CONTEXT.md
@.planning/phases/02-ocr-openai-chatgpt/02-RESEARCH.md
@.planning/phases/02-ocr-openai-chatgpt/02-PATTERNS.md
@.planning/phases/02-ocr-openai-chatgpt/02-REVIEWS.md
@CLAUDE.md
@pagefolio/CLAUDE.md
</context>

<review_dispositions>
## 02-REVIEWS.md の指摘に対する本プランの処置

02-REVIEWS.md は `REVIEWED-WITHOUT-REPO-ACCESS` マーカー付き（codex のサンドボックスが
`CreateProcessAsUserW failed: 5` で起動できず実コードを 1 行も読めなかった）。
実コード依存の指摘は計画時に**実ソースで照合**し、結果を根拠つきで下表へ記録した。

| # | 指摘（要約） | severity | 処置 | 根拠 / 反映先 |
|---|---|---|---|---|
| 1 | `GET /v1/models` だけでは vision 対応を確定できない | HIGH | **採用** | Task 1 を Stage A（ID 実在）/ Stage B（能力・価格）へ分割し `02-CAPABILITY-MATRIX.md` を成果物化。`ClaudeProvider.list_models` が `capabilities.image_input.supported` で絞れる（`pagefolio/ocr_providers/claude.py:330-337`）のに対し OpenAI に同等フィールドが無いという非対称を計画上明示 |
| 2 | reasoning モデル判定の根拠不足（`^o\d` だけでは取りこぼす） | HIGH | **採用** | 判定パターンは能力マトリクスの `reasoning_effort` 列から導出する。Task 3(F) は matrix で yes と確認した非 o 系モデルを最低 1 件含むことを必須化 |
| 3 | `ProviderMeta` の nullable 型契約が矛盾 | MEDIUM | **採用** | artifacts 節にフィールド単位の型表とアクセサ戻り型表を追加。`off` / `ollama` は LANG キーが実在しない（`pagefolio/lang.py:498-531` に `ocr_provider_name_off` / `_ollama` は無い）ため `display_name_key=None` が正しい |
| 4 | catalog の「環境変数名の一元化」の定義が曖昧 | MEDIUM | **採用** | `must_haves.truths` と catalog docstring に「軸ごとに 1 箇所」の明文化を追加（非機密メタデータ=catalog / 機密キー・環境変数=registry） |
| 5 | `list_models()` の暫定実装が公開される | MEDIUM | **記録のうえ受容** | 本プランの `list_models` は「常に静的推奨一覧を返す」= D-08 の最終挙動の部分集合であり、誤った結果を返す暫定実装ではない。UI 非公開かつ package API としても安全側。02-03 Task 1 が HTTP 取得を追加する |
| 6 | 構造アサーションが実装詳細へ強く結合（`inspect.getsource` の文字列禁止はコメントで壊れる） | LOW | **採用（方式変更）** | 全ての「〜が存在しないこと」アサーションを **AST ベース**へ変更（コメント・docstring の影響を受けない）。文字列リテラル検査が必要な箇所も `ast.Constant` 走査に限定する |
| S4 | endpoint host 一致テストを 02-02 ではなく 02-01 で導入 | — | **採用** | Task 3(G) として本プランへ前倒し |
| 全体-2 | 必須構文チェック（`ast.parse`）が計画に無い | MEDIUM | **採用** | 各タスクの `<verify>` と `<verification>` へ追加 |
| 全体-3 | 全テストゲートの扱いが CLAUDE.md と不整合 | HIGH | **採用（例外を撤廃）** | 計画時に実測した結果、`--ignore` 例外も「着手前と同数以下」も不要と判明。下記「テストゲートの確定」参照 |
| 全体-5 | 検証コマンドの `grep` が Windows で使えない可能性 | LOW | **採用** | 全検証コマンドを `python -c` ワンライナーへ統一（`grep` / `rg` を使わない） |

## テストゲートの確定（レビュー HIGH-6 の解消）

計画時に実機（Windows 11 / Python 3.14 / 本リポジトリ HEAD = 4b7f421）で実測した:

- `python -m pytest -q` → `1029 passed, 158 errors`。158 件はすべて
  `PermissionError: [WinError 5] ... C:\Users\shdwf\AppData\Local\Temp\pytest-of-shdwf` で、
  `tmp_path` フィクスチャがベーステンポラリを走査できないという **環境固有の ACL 事象**。
  製品コードの失敗ではない（テスト本体は 1 件も実行されず setup ERROR）。
- `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` →
  **`1187 passed`（失敗 0・error 0）**。連続 4 回すべて同結果。
  STATE.md Blockers に記録された `Windows fatal exception: code 0x80000003` による
  プロセスクラッシュも **本 HEAD では 4/4 回とも再発しなかった**。

したがって:

- **`--ignore=tests/test_ocr_pipeline.py` の例外を撤廃する。** 分割実行は不要。
- **「失敗件数が着手前と同数以下」という相対ゲートを撤廃する。** 絶対ゲート＝失敗 0 件。
- 本フェーズの全プランのフルスイートゲートは次の 1 本に統一する:
  `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が
  **失敗 0 件**（着手前ベースライン: 1187 passed / 0 failed。本プランでテストを追加するため
  合計件数は増える）。
- `--basetemp` は上記 ACL 事象の回避専用であり、テストを 1 件も除外しない。
  もし実行環境で `pytest-of-shdwf` の ACL 問題が起きないなら `--basetemp` は省略してよい。
- クラッシュが再発した場合のみ STATE.md の分割運用へ退避し、**その事実と件数を SUMMARY に
  記録する**（黙って例外にしない）。Phase 3（V190-QA-01）が引き取る blocking gate は
  「全件 1 プロセス完走・失敗 0」で変わらない。
</review_dispositions>

<assumption_delta_decision>
## Assumption-Delta: singular → plural（プロバイダメタデータの一般化）

**検出:** V190-CAT-01/02 が「プロバイダ 1 件ごとのメタデータ」を中央化し、その直後に
V190-OAI-* が 6 番目のプロバイダを載せる。プロバイダという名詞の同一性モデルを
再確認すべき seam。

**いま primary になる名詞:** `ProviderMeta` — プロバイダ名をキーとする
「非機密メタデータの 1 レコード」。

**決定: `promote`（新しい一般表現を primary に昇格させる）**

- `catalog.PROVIDERS` を**プロバイダ非機密メタデータの唯一の primary** とする。
  これまで散在していた表現（`ocr_dialog.py` の表示名 if 連鎖と dict の 2 実装、
  4 箇所のクラウド判定集合、5 箇所の host 分岐、2 箇所の APIキー欠落 dict、
  `sections.py` の 2 つの一覧リスト）は、すべて catalog の**消費者へ降格**する。
  add-alongside は採らない（D-03 が 6 参照面の移行完走を明示的に求めている）。
- **registry.py は昇格対象に含めない。** `registry.py` は「機密キー / 環境変数名」
  という直交する軸の primary であり続け、catalog の projection にはしない。
  したがって promote は**非機密メタデータ軸に限定**した 2 モジュール分割である（D-06）。
  レビュー MEDIUM-4 のとおり、V190-CAT-01 の「変更面が 1 箇所」は
  **「軸ごとに 1 箇所」**を意味する（新プロバイダ追加時に触るのは catalog 1 エントリ +
  registry 1 エントリの計 2 行であり、6 ファイルに散る現状とは質的に異なる）。
  この解釈を catalog の docstring と `must_haves.truths` に明文化する。
- **受容する残債:** catalog に未登録のプラグインプロバイダは `is_cloud_provider()` が
  False を返し、`ocr_dialog.py` の isinstance 判定が 2 本目の経路として残る（D-04）。
  外部送信の明示同意を落とさないことを経路の単純さより優先した意図的な選択。

**Reversibility:** `costly` — 6 参照面の呼び出し経路を catalog 経由へ切り替えるため、
戻すには全ファイルの再修正が必要。データ移行や公開契約の破壊は伴わないため `one-way` ではない。

**不変条件テスト（採用・Task 3(D)）:**
`registry.PROVIDER_ENV_KEYS` の全キーが `catalog.PROVIDERS` に存在し `is_cloud=True` であること、
および全 `ProviderMeta` の必須フィールドが解決可能であることをテストで固定する。
</assumption_delta_decision>

<flagged_assumptions>
## Flagged Assumptions（edge probe — unresolved / unclassified）

spec-less edge probe（`-SPEC.md` 不在のため fallback 実行）は 15 要件 16 エッジすべてを
`category: unclassified` で返し、1 件も自動解決していない（分類器が日本語要件文を扱えない
ための分類ミスであってエッジ不在ではない）。カテゴリが付かなかった行は
**flagged assumption としてここに surface** し、書ける基準は `must_haves.truths` へ
明示基準として author 済み。

| Requirement | Probe | 本プランでの扱い（未解決の前提） |
|---|---|---|
| V190-CAT-01 | unclassified — review manually | catalog の「単一の情報源」が満たされたと言えるのは D-03 が列挙した 6 参照面をすべて移行し終えた時点（02-01/02-02/02-03 にまたがる）。本プラン単体では `ocr.py:_start_ocr` の 1 面のみが移行済み。フェーズ全体での完走をもって判定する前提 |
| V190-CAT-02 | unclassified — review manually | 「循環 import が発生しない」の判定範囲を `registry.py` / `catalog.py` / `settings.py` の三者に限定している。`pagefolio` 全体の import グラフに循環が無いことは検証しない（既存の後方互換 re-export 構造を含む全体検証はスコープ外） |
| V190-OAI-11 | unclassified — review manually | 「新規 pip 依存ゼロ」の判定は `requirements.txt` / `pyproject.toml` の git diff が空であることまで。トランジティブ依存の変化は検証しない（本フェーズは標準ライブラリのみを追加使用するため実質的に非該当） |
| V190-OAI-12 | unclassified — review manually | 「パラメータ非互換が正しく分岐しエラーにならない」の確認はモック HTTP による payload 形状アサーションまで。実 API が新形式を受理することの実証は Task 1 Stage B（最小実リクエスト）または 02-04 の human-verify に委ねる |
| V190-OAI-13 | unclassified — review manually | 429/5xx のマップは `_FakeHTTPError` によるモック検証まで。OpenAI が実際に `Retry-After` を返すか（RESEARCH.md Assumptions Log A4）は未実証。返さない場合も既存の指数バックオフ分岐が代替として機能する前提 |

（02-02 / 02-03 / 02-04 の担当要件に対応する unclassified 行は、各プランの同名節に surface する。）
</flagged_assumptions>

<artifacts>
## Artifacts this phase produces（本プランの分）

**新規計画成果物**
- `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md`

**新規モジュール**
- `pagefolio/ocr_providers/catalog.py`
- `pagefolio/ocr_providers/openai_provider.py`
- `tests/test_ocr_provider_catalog.py`

**`ProviderMeta` フィールド型表（確定契約・レビュー MEDIUM-7）**

| field | 型注釈 | nullable | None を取るエントリ |
|---|---|---|---|
| `name` | `str` | いいえ | — |
| `display_name_key` | `str \| None` | **はい** | `off` / `ollama`（対応する LANG キーが `lang.py` に実在しない） |
| `is_cloud` | `bool` | いいえ | — |
| `model_setting_key` | `str \| None` | **はい** | `off` / `tesseract` |
| `default_model` | `str \| None` | **はい** | `off` / `lmstudio` / `ollama` / `runpod` / `tesseract` |
| `host` | `str \| None` | **はい** | `off` / `lmstudio` / `ollama` / `runpod`（URL 設定依存） / `tesseract` |
| `fallback_eligible` | `bool` | いいえ | — |
| `api_key_missing_lang_key` | `str \| None` | **はい** | `off` / `lmstudio` / `ollama` / `tesseract` |

**アクセサ戻り型表（呼び出し側が None を意識せずに済む境界）**

| accessor | 戻り型 | 未登録 / None のとき |
|---|---|---|
| `provider_names(include_off=True)` | `list[str]` | — |
| `fallback_candidate_names()` | `list[str]` | — |
| `is_cloud_provider(name)` | `bool` | `False`（D-04） |
| `host_for(name, settings)` | `str` | `""` |
| `default_model_for(name)` | `str` | `""` |
| `display_name_key_for(name)` | `str \| None` | `None` |
| `model_setting_key_for(name)` | `str \| None` | `None` |
| `api_key_missing_lang_key_for(name)` | `str \| None` | `None` |

**新規シンボル（catalog.py）**
- `ProviderMeta`（frozen dataclass・上表 8 フィールド）
- `PROVIDERS: dict[str, ProviderMeta]` — キー: `off` / `lmstudio` / `ollama` /
  `runpod` / `claude` / `gemini` / `tesseract` / `openai`（宣言順 = 表示順）
- 上のアクセサ表の 8 関数

**新規シンボル（openai_provider.py）**
- `OpenAIProvider`（`OCRProvider` サブクラス）
- クラス属性: `CHAT_ENDPOINT` / `MODELS_ENDPOINT` / `RECOMMENDED_MODELS` /
  `VERIFIED_VISION_MODELS` / `default_concurrency=2` / `max_concurrency=2` /
  `supports_text_prompt=True` / `model_list_timeout=30`
- モジュール純関数: `is_reasoning_model(model)`（D-13 の単一判定源）
- モジュール定数: `REASONING_MODEL_PREFIXES`（能力マトリクス由来の判定根拠）
- 内部メソッド: `_headers` / `_apply_gen_params` / `_build_payload` / `_build_text_payload` /
  `_post_chat` / `ocr_image` / `ocr_image_ex` / `complete_text_ex` / `list_models`

**新規 re-export（`pagefolio/ocr_providers/__init__.py`）**
- `OpenAIProvider` / `is_reasoning_model`（`catalog` サブモジュールは registry と同様 re-export しない）

**新規環境変数名**
- `OPENAI_API_KEY`（`registry.PROVIDER_ENV_KEYS["openai"]`）

**新規 settings キー（`pagefolio_settings.json` に永続化される非機密値）**
- `openai_model`（既定 `""` — 空のとき `catalog.default_model_for("openai")` へ解決）
- `openai_detail`（既定 `"high"`）
- `openai_reasoning_effort`（既定 `""`）
- `openai_organization`（既定 `""`）
- `openai_project`（既定 `""`）

**永続化されない機密キー（`_SENSITIVE_KEYS` 由来）**
- `openai_api_key` / `OPENAI_API_KEY` / `openai_api_key`（lower）
</artifacts>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: OpenAI モデル能力マトリクスと価格プロヴェナンスの確定（D-09 + レビュー HIGH-1/HIGH-2）</name>
  <decision>後続 3 プランが共通参照する `02-CAPABILITY-MATRIX.md` を、実 API（Stage A + Stage B）で埋めるか、OpenAI 公式ドキュメントを二次ソースとして埋めるか</decision>
  <context>
CONTEXT.md D-09 は「実装時に実 API キーで `GET /v1/models` を叩いて目視確認してから確定する」ことを
locked decision としており、プランに明示タスクとして含めることを求めている。
02-REVIEWS.md の HIGH-1 は、それだけでは不十分だと指摘した — `GET /v1/models` は
**モデル ID の存在しか返さない**。Anthropic の `/v1/models` が
`capabilities.image_input.supported` を返す（`pagefolio/ocr_providers/claude.py:330-337` が
実際にそれで絞っている）のに対し、OpenAI には同等フィールドが無い（RESEARCH.md Pitfall 2）。
したがって「一覧に出た＝画像入力できる」は成り立たない。

HIGH-2 は、コスト確認ダイアログに出す単価の根拠が計画されていないことを指摘した。
概算コストは外部送信への同意の質に直結するため、URL・参照日・単位・通貨を伴わない
「公表値」は受け入れられない。

また RESEARCH.md Assumptions Log A1（推論系モデルの命名規則）・A2（モデル名の陳腐化）・
Open Question 2（`reasoning_effort` の値域）はいずれも実在するモデルを見ないと確定できない。
本フェーズのどのソース成果物にも判断材料が無い（planner authority limit の
「missing information」）ため、実行前にユーザーの選択が必要。

この決定は `reversible`（マトリクスと定数の書き換えのみ・データ移行なし）。
  </context>
  <options>
    <option id="option-a">
      <name>実 API キーで Stage A + Stage B を実施して確定する</name>
      <pros>D-09 の第一希望。Stage A（`GET /v1/models` で ID 実在確認）に加え、Stage B で 1×1 ピクセルの PNG を使った最小の画像リクエストを候補モデルへ 1 回ずつ投げることで、vision 入力・`max_completion_tokens`・`temperature` 可否・`reasoning_effort` 可否を **実測** で確定できる。02-04 の実機確認にもキーを使い回せる</pros>
      <cons>ユーザーが OPENAI_API_KEY を用意する必要がある。Stage B は候補モデル数ぶんの微少な課金が発生する（1×1 画像 + 極小プロンプトのため 1 モデルあたり 1 円未満の想定だが、ゼロではない）。価格そのものは API から取れないため公式価格ページの参照は Stage B でも必要</cons>
    </option>
    <option id="option-b">
      <name>OpenAI 公式ドキュメント（モデルページ + 価格ページ）を一次ソースとして確定する</name>
      <pros>D-09 が明示している代替経路。キー不要・課金ゼロで即座に進行できる。vision 対応・パラメータ対応・`reasoning_effort` 値域・単価はいずれも公式モデルページ / 価格ページに記載があり、WebFetch で参照して URL と参照日を残せる。価格は結局この経路でしか取れない</pros>
      <cons>ドキュメント記載と実 API 応答がずれる可能性が残る。確定値の実証は 02-04 の human-verify まで持ち越しになる</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/02-ocr-openai-chatgpt/02-CONTEXT.md`（D-09・D-10〜D-16・Claude's Discretion）
    - `.planning/phases/02-ocr-openai-chatgpt/02-RESEARCH.md`（Pitfall 2・Pitfall 5 / Assumptions Log A1・A2 / Open Questions 1・2）
    - `.planning/phases/02-ocr-openai-chatgpt/02-REVIEWS.md`（HIGH-1 / HIGH-2 / 横断的な改善提案の能力マトリクス表）
    - `pagefolio/ocr_providers/claude.py` 29-41 行（`RECOMMENDED_MODELS` = 3 件・`EFFORT_MODELS` の粒度）と 300-341 行（`capabilities.image_input.supported` による絞り込み — OpenAI に無い機能の対比）
    - `pagefolio/ocr_dialog.py` 37-53 行（`OCR_PRICE_TABLE` の粒度と `_PRICE_FALLBACK`）
  </read_first>
  <action>
選択された option に従い `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md` を
新規作成する。これは 02-02 / 02-03 / 02-04 が参照する**唯一の情報源**であり、
以降のプランはこのファイルに書かれていない能力・価格を前提にしてはならない。

**Stage A — モデル ID の実在確認。**
option-a なら `OPENAI_API_KEY` を使い `GET https://api.openai.com/v1/models` を 1 回実行し、
返った `data[].id` の全リストを取得する。option-b なら OpenAI 公式モデル一覧ページを参照する。
どちらの場合も、取得元（実 API 応答 / URL）と実施日時を記録する。

**Stage B — 能力と価格の確認。Stage A とは別工程である。**
Stage A で得た ID のうち OCR 候補になるチャット/vision 系について、
公式モデルドキュメント（または option-a なら最小の実画像リクエスト）で次を確定する:
画像入力の可否、`max_completion_tokens` の受理、`temperature` の受理、
`reasoning_effort` の受理と**許容値の集合**。
さらに公式価格ページから入力単価・出力単価・単位・通貨を取得する。

**書式（この列構成を厳守。02-02〜02-04 のテストとタスクがこの列名を前提にする）:**

見出し `# OpenAI Capability & Price Matrix`、その下に取得元・実施日・採用 option を記した
メタ情報ブロック、その下に 1 行 1 モデルの markdown 表を置く。表の列は左から
`model_id` / `vision_input` / `max_completion_tokens` / `temperature` /
`reasoning_effort` / `allowed_effort_values` / `input_price` / `output_price` /
`price_unit` / `currency` / `source_url` / `retrieved_at` / `evidence`。
真偽値の列は `yes` / `no` / `unknown` の 3 値で書く（空欄禁止）。
`allowed_effort_values` は `reasoning_effort` が `yes` のときだけカンマ区切りで書き、
`no` のときは `-` を書く。`evidence` は `api-probe` / `official-doc` / `inferred` のいずれか。
`inferred` の行は既定モデルにも `RECOMMENDED_MODELS` にも採用してはならない。

表の下に次の 4 つの導出結果を明記する:
(1) `default_model` に採用する 1 件（`vision_input=yes` かつ `evidence` が `inferred` でないこと）
(2) `RECOMMENDED_MODELS` に採用するリスト（3〜6 件・`vision_input=yes` のみ・宣言順を確定）
(3) `is_reasoning_model()` が真を返すべきモデル ID の実例（最低 1 件）と偽を返すべき実例（最低 1 件）、
    および判定に使うプレフィックス/パターンの集合。`reasoning_effort=yes` と確認したモデルが
    o 系以外にも存在する場合は**必ずそれを真ケースの実例に含める**（レビュー HIGH 02-01-2）
(4) モデル一覧フィルタで除外すべき ID の実例（embedding / tts / whisper / dall-e /
    moderation / 画像生成系）。02-03 Task 1 のフィルタ純関数がこれを消費する

**API キーの取り扱い:** 本ファイルにも SUMMARY にもキー文字列を書かないこと。
実行ログ・スクリーンショットにも残さない。
  </action>
  <acceptance_criteria>
    - `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md` が存在する
    - `python -c "import pathlib,re; s=pathlib.Path('.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md').read_text(encoding='utf-8'); [__import__('sys').exit('missing col: '+c) for c in ['model_id','vision_input','max_completion_tokens','temperature','reasoning_effort','allowed_effort_values','input_price','output_price','price_unit','currency','source_url','retrieved_at','evidence'] if c not in s]"` が終了コード 0
    - `python -c "import pathlib,re; s=pathlib.Path('.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md').read_text(encoding='utf-8'); assert re.search(r'https://', s); assert re.search(r'20\d\d-\d\d-\d\d', s)"` が終了コード 0（出典 URL と参照日が実在する）
    - 表の全行で `vision_input` / `max_completion_tokens` / `temperature` / `reasoning_effort` の 4 列が `yes` / `no` / `unknown` のいずれかで埋まっており空欄が無い
    - 導出結果 (1)〜(4) が本文に列挙され、`default_model` と `RECOMMENDED_MODELS` の全モデルが `vision_input=yes` かつ `evidence != inferred` である
    - `is_reasoning_model()` の真ケース実例が最低 1 件・偽ケース実例が最低 1 件、判定パターンつきで記載されている
    - 単価は入力・出力ともに数値で記載され、`price_unit` と `currency` が全行同一である
    - 採用した option（a / b）と実施日が SUMMARY に明記され、API キー文字列はどこにも記録されていない
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: 縦スライス — catalog 新設 + OpenAIProvider + build_provider（1 経路が実際に通る）</name>
  <precondition>pagefolio/ocr_providers/errors.py に OCRDisabledError が定義され、pagefolio/ocr.py の build_provider が ocr_provider == "off" のときそれを raise する（Phase 1 / V190-SAFE-03 の完了物）</precondition>
  <reversibility rating="costly">catalog の `ProviderMeta` フィールド構成は以降 6 参照面と将来の全プロバイダが読む契約になるため、変更には全消費者の協調修正が必要（データ移行や公開契約の破壊は伴わないので one-way ではない）。</reversibility>
  <files>pagefolio/ocr_providers/catalog.py, pagefolio/ocr_providers/openai_provider.py, pagefolio/ocr_providers/registry.py, pagefolio/ocr_providers/__init__.py, pagefolio/ocr.py, pagefolio/settings.py, tests/test_ocr_providers.py</files>
  <read_first>
    - `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md`（Task 1 の成果物 — 採用モデル ID・推論系判定パターン・許容 effort 値域）
    - `pagefolio/ocr_providers/registry.py`（全 73 行 — 独立性制約の docstring・`PROVIDER_ENV_KEYS`・`sensitive_keys()` の自動導出）
    - `pagefolio/ocr_providers/lmstudio.py`（全 188 行 — OpenAI 互換 Vision API の一次実装。`_build_payload` / `_build_text_payload` / `_post_chat` / `ocr_image` / `complete_text_ex` の構造をそのまま土台にする）
    - `pagefolio/ocr_providers/claude.py` 16-60 行（クラス属性 `default_concurrency` / `max_concurrency` / `supports_text_prompt` / `model_list_timeout` とエンドポイント定数の置き方）と `_apply_gen_params` 相当の分岐
    - `pagefolio/ocr_providers/gemini.py`（`_model_generation` / `_is_legacy_gemini` — 「世代判定 → パラメータ省略」の安全側パターン）
    - `pagefolio/ocr_providers/errors.py`（全 131 行 — `_raise_mapped_http_error` / `_CONTEXT_ERROR_MARKERS` / `parse_retry_after`。本タスクでは変更しない）
    - `pagefolio/ocr_providers/base.py`（`OCRProvider` の抽象メソッド `ocr_image` / `list_models`、既定実装 `ocr_image_ex` / `complete_text_ex`、クラス属性の既定値）
    - `pagefolio/ocr_providers/__init__.py`（re-export の並び順とコメント規約）
    - `pagefolio/ocr.py` 429-546 行（`build_provider` の全分岐）と 567-600 行（`_start_ocr` の `_cloud_providers` 集合）
    - `pagefolio/settings.py` 254-303 行（`_load_settings` の defaults dict）と 1-30 行（`_SENSITIVE_KEYS = sensitive_keys()` の配線）
    - `pagefolio/lang.py` 490-535 行（ja の `ocr_provider_name_*` 群 — `off` と `ollama` のキーが存在しないことの確認）
    - `tests/test_ocr_providers.py`（`_FakeResponse` / `_FakeHTTPError` / `TestLMStudioProviderOcrImage` / `TestClaudeProviderBuildPayload` / `TestLMStudioRetrySymmetry` の記法）
    - `.planning/phases/02-ocr-openai-chatgpt/02-PATTERNS.md`（catalog / openai_provider / ocr.py の各セクション）
  </read_first>
  <action>
Task 1 で確定した能力マトリクスを前提に、以下 6 ファイルを実装する。ruff の
`line-length=88` と `select=["E","F","W","I","S","B"]` を満たすこと。裸の `except:` は禁止。

(1) `pagefolio/ocr_providers/catalog.py` を新設する（D-01/D-05/D-06）。
先頭に既存 3 行のライセンスヘッダを置く。docstring には 2 点を明記する:
「独立性制約: 本モジュールは標準ライブラリと `pagefolio.ocr_providers.registry` のみに依存し、
Provider クラスを import しない。registry への一方向 import のみで逆方向は作らない」と
「責務境界: 非機密メタデータの単一情報源は本モジュール、機密キーと環境変数名の単一情報源は
`registry.py`。V190-CAT-01 の『変更面が 1 箇所』は軸ごとに 1 箇所を意味する」。
`ProviderMeta` を `@dataclass(frozen=True)` で定義し、フィールドと型注釈は本プラン
artifacts 節の**フィールド型表どおり**にする（nullable な 5 フィールドは `str | None`）。
`PROVIDERS` に 8 エントリを登録する。各値は実コードの現行挙動と 1:1 で一致させること:

- `off`: display_name_key=None, is_cloud=False, model_setting_key=None, default_model=None,
  host=None, fallback_eligible=False, api_key_missing_lang_key=None
- `lmstudio`: `"ocr_provider_name_lmstudio"`, False, `"lm_studio_model"`, None, None, True, None
- `ollama`: display_name_key=None（対応 LANG キーが `lang.py` に存在しないため。
  未知キーを素通しする `_provider_display_name` の現行挙動と一致する）, False,
  `"ollama_model"`, None, None, True, None
- `runpod`: `"ocr_provider_name_runpod"`, True, `"runpod_model"`, None,
  None（`runpod_url` 設定依存）, True, `"ocr_api_key_missing_runpod"`
- `claude`: `"ocr_provider_name_claude"`, True, `"claude_model"`, `"claude-sonnet-4-6"`,
  Anthropic の既存送信先ホスト文字列, True, `"ocr_api_key_missing"`
- `gemini`: `"ocr_provider_name_gemini"`, True, `"gemini_model"`, `"gemini-2.5-flash"`,
  Google 生成 AI の既存送信先ホスト文字列, True, `"ocr_api_key_missing_gemini"`
- `tesseract`: `"ocr_provider_name_tesseract"`, False, None, None, None, True, None
- `openai`: `"ocr_provider_name_openai"`, True, `"openai_model"`,
  Task 1 で確定した default_model, `"api.openai.com"`, True, `"ocr_api_key_missing_openai"`

claude / gemini の host 値は `pagefolio/ocr_dialog.py` の `_confirm_cost`（1216-1257 行）に
現在ベタ書きされている文字列をそのまま移植すること（新しい値を考えない）。
`ocr_provider_name_openai` と `ocr_api_key_missing_openai` の LANG キー自体は 02-02 で追加するため、
本タスクでは catalog にキー名を持たせるだけで `lang.py` は変更しない。

公開関数は `registry.py` の「dict.get → 単純変換 → 返却」粒度に揃え、戻り型は
artifacts 節の**アクセサ戻り型表どおり**にする:
`provider_names(include_off=True)` は `PROVIDERS` の宣言順リスト、
`fallback_candidate_names()` は `fallback_eligible=True` の宣言順リスト、
`is_cloud_provider(name)` は未登録で False（D-04）、
`host_for(name, settings)` は `host` があればそれ、`runpod` は `settings.get("runpod_url", "")`、
それ以外と未登録は空文字、`default_model_for(name)` は None / 未登録で空文字、
`display_name_key_for` / `model_setting_key_for` / `api_key_missing_lang_key_for` は
該当値または None。環境変数名の関数は catalog に置かず re-export もしない（D-06）。

(2) `pagefolio/ocr_providers/registry.py` の `PROVIDER_ENV_KEYS` に
`"openai": ("OPENAI_API_KEY",)` を 1 エントリ追加する。それ以外の行は 1 文字も変更しない（D-06）。

(3) `pagefolio/ocr_providers/openai_provider.py` を新設する。
モジュール定数 `REASONING_MODEL_PREFIXES` を Task 1 の導出結果 (3) の判定パターン集合として
定義し、docstring に「値の根拠は 02-CAPABILITY-MATRIX.md の `reasoning_effort` 列」と記す。
モジュール純関数 `is_reasoning_model(model)` はこの定数だけを見てモデル ID が推論系かを
判定する唯一の関数（D-13）。プロバイダ側の `temperature` 省略（D-11）と UI 側の
reasoning effort 欄の表示切替（D-15・02-04）の両方をこの 1 関数が駆動する。
`ClaudeProvider._supports_effort` と `dialog.py:_model_supports_effort` が同じ判定を 2 箇所に
持っている既存の負債（RESEARCH.md Pitfall 3）を OpenAI では作らないこと。

`OpenAIProvider(OCRProvider)` のクラス属性は `default_concurrency = 2` /
`max_concurrency = 2`（D-14）/ `supports_text_prompt = True` / `model_list_timeout = 30` /
`CHAT_ENDPOINT = "https://api.openai.com/v1/chat/completions"` /
`MODELS_ENDPOINT = "https://api.openai.com/v1/models"` /
`RECOMMENDED_MODELS`（Task 1 導出結果 (2)）/
`VERIFIED_VISION_MODELS = tuple(RECOMMENDED_MODELS)`（能力マトリクスで
`vision_input=yes` を確認済みの集合。02-03 がモデル一覧の並び替えと注記に使う）。
`__init__` の引数は `api_key, model, timeout=120, max_tokens=4096, temperature=0.1,
detail="high", reasoning_effort=None, organization="", project=""`。

`_headers()` は `Content-Type` と `Authorization: Bearer {api_key}` を常に付け、
`organization` / `project` が真値のときだけ `OpenAI-Organization` / `OpenAI-Project` を足す（D-17）。
`_apply_gen_params(payload)` はトークン上限キーとして `max_completion_tokens` のみを設定し
（Chat Completions の非推奨キーは一切送らない・D-10）、`is_reasoning_model(self.model)` が
偽なら `temperature` を、真かつ `reasoning_effort` が真値なら `reasoning_effort` を設定する
（D-11/D-15）。
`_build_payload(b64_png, prompt)` は `lmstudio.py:42-63` と同じ `messages`/`content` 構造を使い、
`image_url` オブジェクトへ `url`（`data:image/png;base64,` 前置）と `detail`（D-16）を入れ、
`stream` は入れない。`_build_text_payload(text, prompt)` は `lmstudio.py:65-85` と同型で
画像ブロックのみ除く。
`_post_chat(payload)` はエンドポイントを固定定数にし（ユーザー入力 URL ではないので
`_require_http_scheme` は呼ばない）、`urllib.request.Request(..., headers=self._headers(),
method="POST")` と `urllib.request.urlopen(req, timeout=self.timeout)` を使う。
**`ssl` モジュールを import せず、`urlopen` に `context` 引数を渡さない**（既定の TLS 検証を使う）。
例外処理は `lmstudio.py:102-113` をそのまま複製する（`HTTPError` →
`_raise_mapped_http_error(e)`、`socket.timeout` → `TimeoutError`、`URLError` → reason が
timeout なら `TimeoutError` そうでなければ `ConnectionError`）。
`errors.py` は変更しない（D-12）。**独自の待機・再送ループを書かない**（V190-OAI-13）。
`ocr_image` / `complete_text_ex` は `lmstudio.py:115-155` と同じ本文
（`choices[0]["message"]["content"]` 取得、`finish_reason == "length"` で truncated 判定、
失敗時は `RuntimeError(f"Unexpected response format: {body[:500]}")`）。
`ocr_image_ex` は `finish_reason == "length"` を見て `(text, truncated)` を返す形で override する。

`list_models` はこのタスクでは HTTP を叩かず `list(self.RECOMMENDED_MODELS)` を返す実装に
とどめる。docstring に「D-08 の静的フォールバック一覧をそのまま返す。`GET /v1/models` からの
取得とヒューリスティックフィルタは 02-03 Task 1 が追加する」と記す。
これは誤った結果を返す暫定実装ではなく、D-08 の最終挙動の部分集合である
（レビュー MEDIUM 02-01-5 に対する記録つきの受容）。
`# noqa: S310` は `lmstudio.py` の 2 箇所と同じ位置に付ける。

(4) `pagefolio/ocr_providers/__init__.py` にアルファベット順の位置で
`from pagefolio.ocr_providers.openai_provider import OpenAIProvider, is_reasoning_model  # noqa: F401`
を追加する。`catalog` サブモジュールは registry と同じ理由で re-export しない。

(5) `pagefolio/ocr.py` を 2 箇所修正する。
`build_provider` に `elif name == "openai":` 分岐を `gemini` 分岐の直後へ追加する。
関数内 import（`from pagefolio.ocr_providers import OpenAIProvider`）と、
`mt = int(settings.get("ocr_max_tokens", DEFAULT_OCR_MAX_TOKENS))` → `mt = 4096 if mt <= 0 else mt`
のクランプは claude/gemini 分岐と同一（LM Studio の -1 委譲値対策）。渡す引数は
`api_key=api_key or ""`、
`model=settings.get("openai_model", "") or catalog.default_model_for("openai")`、
`timeout=int(settings.get("ocr_timeout", DEFAULT_OCR_TIMEOUT))`、`max_tokens=mt`、
`temperature=float(settings.get("ocr_temperature", DEFAULT_OCR_TEMPERATURE))`、
`detail=settings.get("openai_detail", "high")`、
`reasoning_effort=settings.get("openai_reasoning_effort") or None`、
`organization=settings.get("openai_organization", "")`、`project=settings.get("openai_project", "")`。
既定モデルの解決は catalog を唯一の情報源にする（V190-CAT-01）ため、モデル ID 文字列を
`ocr.py` 側に重複させないこと。catalog も関数内 import で参照する。
`_start_ocr`（592-597 行）の `_cloud_providers` 集合リテラルを `catalog.is_cloud_provider(name)` の
呼び出しへ置換し、**集合リテラルの代入文自体を削除する**（D-03 の段階移行 1/6。
これだけで openai が API キー事前解決の対象になる）。

(6) `pagefolio/settings.py` の `_load_settings` defaults へ 5 エントリを追加する:
`"openai_model": ""`、`"openai_detail": "high"`、`"openai_reasoning_effort": ""`、
`"openai_organization": ""`、`"openai_project": ""`。API キーは defaults に入れない。

(7) `tests/test_ocr_providers.py` に本スライスの end-to-end テストを追加する。
既存 `_FakeResponse` を使い `pagefolio.ocr_providers.openai_provider.urllib.request.urlopen` を
monkeypatch して、`build_provider({"ocr_provider": "openai", ...}, api_key="sk-test-e2e")` が
返すプロバイダの `ocr_image()` が、(a) `CHAT_ENDPOINT` へ POST し、(b) `Authorization` ヘッダが
`Bearer sk-test-e2e` で、(c) `OpenAI-Organization` / `OpenAI-Project` ヘッダが付かず、
(d) リクエスト body に `max_completion_tokens` があり、(e) `choices[0].message.content` の
文字列を返すことを 1 本のテストで通しで検証する。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['pagefolio/ocr_providers/catalog.py','pagefolio/ocr_providers/openai_provider.py','pagefolio/ocr.py','pagefolio/settings.py']]" && python -m pytest tests/test_ocr_providers.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr_providers/catalog.py` が存在し `class ProviderMeta` と `PROVIDERS` を含む
    - `python -c "from pagefolio.ocr_providers import catalog; assert set(catalog.PROVIDERS) == {'off','lmstudio','ollama','runpod','claude','gemini','tesseract','openai'}"` が終了コード 0
    - `python -c "from pagefolio.ocr_providers import catalog as c; m=c.PROVIDERS; assert m['off'].display_name_key is None and m['ollama'].display_name_key is None; assert m['tesseract'].model_setting_key is None and m['lmstudio'].default_model is None and m['claude'].api_key_missing_lang_key == 'ocr_api_key_missing'"` が終了コード 0（nullable 契約）
    - `python -c "from pagefolio.ocr_providers import catalog as c; assert c.default_model_for('lmstudio') == '' and c.host_for('lmstudio', {}) == '' and c.display_name_key_for('off') is None"` が終了コード 0（アクセサ戻り型表）
    - `python -c "from pagefolio.ocr_providers import catalog; assert catalog.is_cloud_provider('openai') and catalog.host_for('openai', {}) == 'api.openai.com' and not catalog.is_cloud_provider('unknown-plugin')"` が終了コード 0
    - `python -c "from pagefolio.ocr_providers.registry import sensitive_keys; s=sensitive_keys(); assert {'openai_api_key','OPENAI_API_KEY'} <= s"` が終了コード 0
    - `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('pagefolio/ocr_providers/catalog.py').read_text(encoding='utf-8')); mods=[n.module for n in ast.walk(t) if isinstance(n, ast.ImportFrom) and n.module] + [a.name for n in ast.walk(t) if isinstance(n, ast.Import) for a in n.names]; inner=[m for m in mods if m.startswith('pagefolio')]; assert inner == ['pagefolio.ocr_providers.registry'], inner"` が終了コード 0（D-05・AST ベースなのでコメントで壊れない）
    - `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('pagefolio/ocr.py').read_text(encoding='utf-8')); names=[x.id for n in ast.walk(t) if isinstance(n, ast.Assign) for x in n.targets if isinstance(x, ast.Name)]; assert '_cloud_providers' not in names"` が終了コード 0（集合リテラルの消滅）
    - `python -c "import pathlib; s=pathlib.Path('pagefolio/ocr.py').read_text(encoding='utf-8'); assert 'catalog.is_cloud_provider' in s and 'OpenAIProvider(' in s"` が終了コード 0
    - `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('pagefolio/ocr_providers/openai_provider.py').read_text(encoding='utf-8')); mods=[a.name for n in ast.walk(t) if isinstance(n, ast.Import) for a in n.names]+[n.module for n in ast.walk(t) if isinstance(n, ast.ImportFrom) and n.module]; assert not any(m.split('.')[0]=='ssl' for m in mods); assert not any(isinstance(n, ast.Call) and any(k.arg=='context' for k in n.keywords) for n in ast.walk(t))"` が終了コード 0（TLS 迂回の不在）
    - Task 2(7) の end-to-end テストが green で、そのテストが `Bearer sk-test-e2e` と `max_completion_tokens` の両方をアサートしている
    - `python -c "from pagefolio.settings import _load_settings" ` が import 可能で、defaults に 5 つの `openai_*` キーが存在し `openai_api_key` は存在しない
    - `python -c "import subprocess,sys; r=subprocess.run([sys.executable,'-c','import pagefolio.ocr_providers.catalog']); sys.exit(r.returncode)"` が終了コード 0
    - `git diff --stat requirements.txt pyproject.toml` が空（V190-OAI-11・新規 pip 依存ゼロ）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>設定 → `build_provider` → `OpenAIProvider` →（モック）`api.openai.com` → OCR テキスト、の 1 経路が自動テストで通しで検証されており、既存 5 プロバイダの挙動は無変更。`openai` はまだ LLM 設定 UI の選択肢に現れない。</done>
</task>

<task type="auto">
  <name>Task 3: catalog 契約テストと AST ベース構造アサーションの整備</name>
  <files>tests/test_ocr_provider_catalog.py, tests/test_ocr_providers.py</files>
  <read_first>
    - `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md`（推論系の真/偽ケース実例・許容 effort 値域）
    - `pagefolio/ocr_providers/catalog.py`（Task 2 で作成した実体）
    - `pagefolio/ocr_providers/openai_provider.py`（Task 2 で作成した実体）
    - `pagefolio/ocr_providers/registry.py`（`PROVIDER_ENV_KEYS` / `sensitive_keys()`）
    - `pagefolio/ocr_providers/claude.py` 30-31 行 / `pagefolio/ocr_providers/gemini.py` のエンドポイント定数（host 一致テストの対象）
    - `pagefolio/dialogs/llm_config/sections.py` 84-96 行（`_base_providers` の現行並び順）と 1021-1029 行（`_base_fallback_providers` の現行 6 件）
    - `tests/test_settings_keyguard.py`（`TestRegistrySensitiveKeysCoverage` / `TestSaveSettingsKeyGuard` の記法 — 同型で openai を足す）
    - `tests/test_ocr_providers.py`（`TestLMStudioRetrySymmetry` / `TestOllamaRetrySymmetry` / `TestProviderKeyNotLogged` / `TestGeminiProviderNewGenerationPayload` の記法）
    - `tests/test_imports.py` の `TestOcrProvidersImports`（個別 + 一括 import の 2 系統検証の記法）
  </read_first>
  <action>
新規 `tests/test_ocr_provider_catalog.py` を作成し、catalog の契約と独立性制約を機械保証する。
既存テストの記法（クラス単位のグルーピング・日本語 docstring・`assert` 直書き）に揃え、
新しいテスト作法を持ち込まないこと。

**構造アサーションの方式（レビュー LOW-17 の反映）:** 「〜が存在しないこと」を確かめる
テストは `inspect.getsource()` の部分文字列検査ではなく **`ast.parse` による構文木検査**で
書くこと。部分文字列検査はコメント・docstring・安全なリファクタで容易に壊れ、
逆にコメントに禁止語を書いただけで赤くなる（自己無効化する）ため。
文字列リテラルの不在を見たい場合も、`ast.walk` で `ast.Constant` の str 値だけを
集めて検査する（コメントは AST に残らないので影響を受けない）。

(A) `TestCatalogContents` — `PROVIDERS` の 8 キーが揃うこと、`provider_names()` が
`sections.py:_base_providers` の現行リスト（`off` / `lmstudio` / `ollama` / `runpod` / `claude` /
`gemini` / `tesseract`）を先頭順に含み末尾に `openai` が来ること、
`fallback_candidate_names()` が `sections.py:_base_fallback_providers` の現行 6 件（`off` を含まない）
＋ `openai` と一致すること。02-03 の一覧移行が表示順を変えないことをここで固定する。

(B) `TestProviderMetaFieldContract` — レビュー MEDIUM-7 の機械化。
`dataclasses.fields(ProviderMeta)` が本プラン artifacts 節のフィールド型表と同じ名前・
同じ順序であること、`str | None` 宣言の 5 フィールドが実際に None を取るエントリを
最低 1 件持つこと、`bool` 宣言の 2 フィールドが全エントリで `bool` 型であること。
さらにアクセサ戻り型表を固定する: `default_model_for` / `host_for` は未登録名でも
必ず `str` を返し、`display_name_key_for` / `model_setting_key_for` /
`api_key_missing_lang_key_for` は未登録名で None を返すこと。

(C) `TestCatalogDefaultModelMatchesProvider` — D-05 の機械保証。
`catalog.default_model_for("claude")` が `ClaudeProvider.RECOMMENDED_MODELS` に、
`catalog.default_model_for("gemini")` が `GeminiProvider.RECOMMENDED_MODELS` に、
`catalog.default_model_for("openai")` が `OpenAIProvider.RECOMMENDED_MODELS` に含まれること。
catalog 自身は Provider を import しないので、テスト側で両方を import して突き合わせる。

(D) `TestCatalogRegistryIndependence` — V190-CAT-02。`ast.parse` で `registry.py` の
全 import 文を列挙し、`os` 以外の import が無いこと（特に `pagefolio` で始まる import が
0 件であること）を検証する。あわせて `catalog.py` の内部 import が
`pagefolio.ocr_providers.registry` 1 件のみであることを検証する。
さらに `import pagefolio.settings` → `import pagefolio.ocr_providers.catalog` の順、
および逆順の両方で新規サブプロセス（`subprocess.run([sys.executable, "-c", ...])`）が
正常終了することを確認し、循環 import が発生しないことを固定する。

(E) `TestCatalogRegistryParity` — assumption-delta の不変条件テスト。
`registry.PROVIDER_ENV_KEYS` の全キーが `catalog.PROVIDERS` に存在し、かつ
`is_cloud=True` であること。および `catalog.PROVIDERS` の全エントリについて
8 フィールドすべてが属性として解決できること。
将来のフェーズがプロバイダを片方だけに追加した瞬間に赤になる装置。

`tests/test_ocr_providers.py` には以下を追加する。

(F) `TestOpenAIIsReasoningModel` — D-13 の判定源テスト。
02-CAPABILITY-MATRIX.md の導出結果 (3) に記録された真ケース実例（最低 1 件）で True、
偽ケース実例（最低 1 件）で False を返すこと。
**マトリクスが o 系以外に `reasoning_effort=yes` のモデルを記録している場合、
そのモデル ID を真ケースへ必ず含める**（レビュー HIGH 02-01-2。命名規則テストだけで
終わらせない）。空文字・None でも例外を投げず False を返すこと。

(G) `TestConfirmedHostMatchesProviderEndpoint` — T-02-04 の乖離検知（レビュー
02-01 Suggestion 4 により 02-02 から前倒し）。`catalog.host_for(name, {})` と
`urllib.parse.urlsplit(<Provider>.<エンドポイント定数>).hostname` が
`openai` / `claude` / `gemini` の 3 プロバイダで一致すること。
catalog の host を書き換えた瞬間に赤くなる装置。

(H) `TestOpenAIProviderBuildPayload` — `_build_payload` がトークン上限キーとして
`max_completion_tokens` を持ち Chat Completions の非推奨キーを持たないこと、
非推論系モデルでは `temperature` を持ち `reasoning_effort` を持たないこと、
推論系モデル（マトリクスの真ケース実例）では `temperature` を持たず
`reasoning_effort` 指定時のみ `reasoning_effort` を持つこと、
`image_url` に `detail` が入り既定が `high` であること（D-16）。

(I) `TestOpenAIProviderHeaders` — `organization` / `project` が空のときヘッダに
`OpenAI-Organization` / `OpenAI-Project` が含まれず、指定時のみ含まれること（D-17・V190-OAI-10）。

(J) `TestOpenAIRetrySymmetry` — 既存 `TestLMStudioRetrySymmetry` と同型。
`_FakeHTTPError` で 429 と 503 を返し `OCRRetryableError` にマップされること、
`Retry-After` ヘッダが `retry_after` として伝播すること、400 が `RuntimeError`、
`context_length_exceeded` を含む 400 body が `OCRContextLengthError` になること（D-12・V190-OAI-13）。

(K) `TestOpenAIProviderNoLocalRetryOrInsecureTLS` — **AST ベース**の構造アサーション。
`ast.parse(openai_provider のソース)` に対し、(1) import されるトップレベルモジュール名に
`ssl` が含まれないこと、(2) `ast.Call` のキーワード引数に `context` が現れないこと、
(3) `ast.Attribute` の `attr` に `sleep` を持つ呼び出しが 0 件であること、
(4) `while` ループによる再送構造が `_post_chat` 内に存在しないことを検証する。
リトライ待機は `ocr_pipeline.consume_one` の共通基盤にのみ存在するという構造を固定する。

(L) `TestOpenAIProviderIdempotentPayload` — 同一 `(b64_png, prompt)` で `_build_payload` を
2 回呼んだ結果が等しく、呼び出し前後で `vars(provider)` が変化しないこと（並列実行時に
インスタンス状態を共有しても安全であることの機械保証・edge probe V190-OAI-11/13）。

(M) `TestProviderKeyNotLogged` に openai ケースを追加 — 例外メッセージ・`str(provider)` に
API キー文字列が現れないこと。

(N) `tests/test_ocr_provider_catalog.py` の `TestCatalogSensitiveKeyGuard` として、
`_save_settings` に `openai_api_key` / `OPENAI_API_KEY` を混ぜても書き出された JSON に
現れないことを `tmp_path` + `monkeypatch` で検証する（既存 `TestSaveSettingsKeyGuard` の
claude/gemini ケースと同型）。`tests/test_settings_keyguard.py` は本プランの
`files_modified` に含めないため変更しない。

(O) `tests/test_imports.py` は変更しない。`OpenAIProvider` / `is_reasoning_model` の
package-level import は (C)/(F) が実質的に担保する。

**ミューテーション検証（RESEARCH.md Pitfall 1）:** 本タスクの最後に、
`registry.py` へ一時的に `from pagefolio.settings import _get_base_dir` を挿入して
(D) が赤になることを確認し、revert する。green のまま通ってしまう場合はテストが
実体を見ていないので (D) の実装を修正すること。結果を SUMMARY に記録し、
`git status --porcelain pagefolio/` が空であることを確認する。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['tests/test_ocr_provider_catalog.py','tests/test_ocr_providers.py']]" && python -m pytest tests/test_ocr_provider_catalog.py tests/test_ocr_providers.py tests/test_imports.py tests/test_settings_keyguard.py tests/test_lang_parity.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_ocr_provider_catalog.py` が存在し、`TestCatalogContents` / `TestProviderMetaFieldContract` / `TestCatalogDefaultModelMatchesProvider` / `TestCatalogRegistryIndependence` / `TestCatalogRegistryParity` / `TestCatalogSensitiveKeyGuard` の 6 クラスを含む
    - `python -m pytest tests/test_ocr_provider_catalog.py -q` が全件 green
    - `python -m pytest tests/test_ocr_providers.py -k "OpenAI" -q` が最低 8 テスト green（IsReasoningModel / ConfirmedHostMatchesProviderEndpoint / BuildPayload / Headers / RetrySymmetry / NoLocalRetryOrInsecureTLS / IdempotentPayload / KeyNotLogged）
    - `TestOpenAIIsReasoningModel` が 02-CAPABILITY-MATRIX.md の真ケース実例と偽ケース実例の**実モデル ID 文字列**を含む（架空の ID でテストしていない）
    - `TestOpenAIProviderNoLocalRetryOrInsecureTLS` が `ast` を使っており、`inspect.getsource` による部分文字列検査を使っていない
    - 独立性テストのミューテーション検証（`registry.py` への一時的な内部 import 挿入 → 赤 → revert → green）が実施され、赤になったテスト名が SUMMARY に記録されている
    - ミューテーション検証後に `python -c "import subprocess,sys; r=subprocess.run(['git','status','--porcelain','pagefolio/'],capture_output=True,text=True); sys.exit(1 if r.stdout.strip() else 0)"` が終了コード 0
    - `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が **失敗 0 件**（`--ignore` 例外を使わない・レビュー HIGH-6）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>catalog の内容・フィールド型契約・独立性・registry との整合・OpenAI ペイロード分岐・エラーマッピング・表示ホストと実送信先の一致・機密キーガードが自動テストで固定され、意図的なバグ注入でテストが赤になることを確認済み。フルテストスイートが失敗 0 件で完走する。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| アプリメモリ（`_session_api_keys`）→ ディスク（`pagefolio_settings.json`） | 機密（API キー）が永続化層を越える地点。`_SENSITIVE_KEYS` が唯一のガード |
| ローカルアプリ → `api.openai.com`（第三者・TLS） | ページ画像と API キーがプロセス外・組織外へ出る地点 |
| ユーザー入力（settings 値）→ HTTP リクエスト | `organization` / `project` / `reasoning_effort` / `detail` が API リクエストへ埋め込まれる地点 |
| サードパーティプラグイン → クラウド判定の安全境界 | `PluginManager.register_ocr_provider` 経由のコードがコスト確認・送信先確認を迂回し得る地点 |
| 能力マトリクス（計画成果物）→ 実装定数 | 誤ったモデル能力が既定値になると初回 OCR が失敗し、誤った単価が同意の前提を壊す地点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `settings.py:_save_settings` / `registry.sensitive_keys()` | critical | mitigate | `PROVIDER_ENV_KEYS` へ `"openai": ("OPENAI_API_KEY",)` を追加し `sensitive_keys()` が 3 バリアントを自動導出（Task 2(2)）。`TestCatalogSensitiveKeyGuard` で JSON 書き出しに現れないことを固定（Task 3(N)）。`_load_settings` defaults に API キーを入れない（Task 2(6)） |
| T-02-02 | Information Disclosure | `openai_provider.py:_headers` / `_post_chat` の例外パス | high | mitigate | 例外メッセージはレスポンス body の先頭 500 文字のみで、リクエストヘッダを含めない。`TestProviderKeyNotLogged` に openai ケースを追加（Task 3(M)）。Task 1 の checkpoint 本文にもキー非記録を明記 |
| T-02-03 | Information Disclosure | ページ画像の `api.openai.com` への送信 | high | mitigate | `catalog.PROVIDERS["openai"].is_cloud = True` により `ocr.py:_start_ocr` がクラウド扱いする（Task 2(5)）。**本プランでは `openai` をプロバイダ combobox に追加せず**、送信先確認・コスト確認ゲートが openai 対応する 02-02 完了後（02-03）に初めて選択可能にする |
| T-02-04 | Spoofing | 確認ダイアログの表示ホスト vs 実送信先 | high | mitigate | 送信先ホストは `catalog.host_for("openai")` の 1 箇所のみが情報源で、`OpenAIProvider.CHAT_ENDPOINT` も同ホストの固定定数。`TestConfirmedHostMatchesProviderEndpoint`（Task 3(G)）で openai / claude / gemini の 3 者について機械保証（02-02 から前倒し） |
| T-02-05 | Tampering | `organization` / `project` のヘッダ埋め込み | high | mitigate | `_headers()` は空文字のときヘッダを付与しない（D-17・Task 2(3)）。入力値の検証とヘッダ境界の制御文字除去は 02-04 Task 1 で 2 層に実装する |
| T-02-06 | Denial of Service（第三者への増幅） | リトライ・バックオフ | medium | mitigate | `openai_provider.py` に独自の待機・再送を書かず、既存 `clamp_retry_after`（60 秒上限）/ `interruptible_sleep` / `MAX_RETRIES` / サーキットブレーカーのみを経路とする。`TestOpenAIProviderNoLocalRetryOrInsecureTLS` の AST 検査で構造を固定（Task 3(K)） |
| T-02-07 | Elevation of Privilege | プラグインプロバイダのクラウド判定迂回 | medium | mitigate | `catalog.is_cloud_provider()` は未登録プロバイダに False を返すが、`ocr_dialog.py` の isinstance フォールバックを維持して安全側に倒す（D-04）。実装は 02-02 |
| T-02-08 | Information Disclosure | TLS 検証の無効化 | high | mitigate | `urllib.request.urlopen` を既定 SSL コンテキストで呼び、`ssl` の import と `context=` 指定を行わない。`TestOpenAIProviderNoLocalRetryOrInsecureTLS` の AST 検査（Task 3(K)） |
| T-02-16 | Tampering | 能力マトリクスの誤り（存在しないモデル / 誤った単価が既定値になる） | high | mitigate | Task 1 を Stage A（ID 実在）/ Stage B（能力・価格）へ分割し、`evidence` 列で根拠種別を明示。`inferred` 行を既定モデル・推奨一覧に採用することを禁止。単価は出典 URL と参照日を必須化し 02-02 が同じ表を消費する（レビュー HIGH-1 / HIGH-2） |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージを一切インストールしない（V190-OAI-11・`urllib.request` 標準ライブラリのみ）。RESEARCH.md「Package Legitimacy Audit」も該当なしと判定済みのため供給網リスクは現状維持で受容する。`git diff --stat requirements.txt pyproject.toml` が空であることを Task 2 の acceptance criteria で確認する |
</threat_model>

<verification>
- `python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['pagefolio/ocr_providers/catalog.py','pagefolio/ocr_providers/openai_provider.py','pagefolio/ocr_providers/registry.py','pagefolio/ocr.py','pagefolio/settings.py']]"` が終了コード 0（CLAUDE.md 変更時チェックリストの構文確認）
- `python -m pytest tests/test_ocr_provider_catalog.py tests/test_ocr_providers.py -q` が全件 green
- `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が **失敗 0 件**（着手前ベースライン 1187 passed / 0 failed。`--ignore` 例外は使わない）
- `ruff check . && ruff format --check .` が終了コード 0
- `git diff --stat pagefolio/ocr_providers/registry.py` が 1 行追加のみ（D-06）
- `git diff pagefolio/ocr_providers/errors.py` が空（D-12）
- `git diff --stat requirements.txt pyproject.toml` が空（V190-OAI-11）
- Task 3 のミューテーション検証（`registry.py` への一時的な内部 import 挿入）で独立性テストが赤になることを確認済み
- `02-CAPABILITY-MATRIX.md` の 13 列がすべて存在し、出典 URL と参照日が記載されている
</verification>

<success_criteria>
- レビュー HIGH-1: モデル能力マトリクスが成果物として存在し、`vision_input` を `GET /v1/models` とは別工程で確定している
- レビュー HIGH-2: 単価の出典 URL・参照日・単位・通貨がマトリクスに記録され 02-02 が消費できる
- レビュー HIGH-6: フルテストスイートが失敗 0 件で完走し、`--ignore` 例外も相対ゲートも使っていない
- V190-CAT-01 のうち `ocr.py:_start_ocr` のクラウド判定 1 面が catalog 経由へ移行済み（残り 5 面は 02-02 / 02-03）
- V190-CAT-02: `registry.py` の独立性が AST テストで固定され、`settings` ↔ `catalog` 双方向の import 順で循環しない
- V190-OAI-02: `openai_api_key` / `OPENAI_API_KEY` が `_SENSITIVE_KEYS` に含まれ JSON へ書き出されない
- V190-OAI-11: 新規 pip 依存ゼロで `urllib.request` 直叩き実装が動作
- V190-OAI-12: `max_completion_tokens` 常用と推論系モデルの `temperature` 省略が payload テストで固定
- V190-OAI-13: 429/5xx が `OCRRetryableError` へマップされ、独自の再送経路が AST 上存在しない
- `openai` はまだユーザーが UI から選択できない（安全境界の順序制約が守られている）
</success_criteria>

<output>
Create `.planning/phases/02-ocr-openai-chatgpt/02-01-SUMMARY.md` when done
</output>
</content>
</invoke>
