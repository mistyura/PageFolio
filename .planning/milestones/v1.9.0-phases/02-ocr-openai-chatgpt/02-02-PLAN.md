---
phase: 02-ocr-openai-chatgpt
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pagefolio/ocr_dialog.py
  - pagefolio/dialogs/batch_ocr.py
  - pagefolio/lang.py
  - tests/test_provider_ui.py
  - tests/test_batch_ocr_dialog.py
autonomous: true
requirements:
  - V190-CAT-01
  - V190-OAI-04
  - V190-OAI-05
  - V190-OAI-06

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "OCR ダイアログのプロバイダ表示名解決が catalog の display_name_key を唯一の情報源とし、if 連鎖版と dict 版の 2 実装が同一の catalog 参照へ統合されている（V190-CAT-01・D-03）"
    - "OCR ダイアログとバッチ OCR ダイアログのクラウド判定が catalog.is_cloud_provider を経由し、既知クラウドプロバイダクラスの isinstance フォールバックは安全側として維持されている（D-04）"
    - "OCR ダイアログとバッチ OCR ダイアログの送信先ホスト分岐 5 箇所すべてが catalog.host_for を経由し、host 文字列リテラルが両ファイルの AST 上の文字列定数から消えている（V190-CAT-01）"
    - "API キー欠落エラーの LANG キー dict が両ファイルから消え、catalog.api_key_missing_lang_key_for に置き換わっている（D-01）"
    - "ocr_provider が openai のとき、OCR 実行前に送信先ホスト api.openai.com を明示した確認ダイアログが表示される（V190-OAI-04）"
    - "ocr_provider が openai のとき、OCR 実行前に対象ページ数と概算コストを含む確認ダイアログが表示される（V190-OAI-05）"
    - "クラウド判定が真なのに送信先ホストを解決できないプロバイダでは、確認ダイアログ本文が送信先不明である旨を明示する（プロバイダ表示名だけを出して済ませない・レビュー MEDIUM-8）"
    - "選択中モデルが能力マトリクスの vision 確認済み集合に無い場合、確認ダイアログ本文に画像入力が未確認である旨の 1 行が加わる（レビュー HIGH 02-02-2）"
    - "OpenAI モデルの単価は入力/出力・単位・通貨・出典 URL・参照日を伴う定数として ocr_dialog.py と batch_ocr.py の両方に同一内容で定義され、その一致と形式妥当性がテストで固定されている（レビュー HIGH-2・MEDIUM-16）"
    - "確認ダイアログで「いいえ」を選んだとき build_provider にも HTTP 送信にも到達しないことが単発・バッチの両方でテストされている（レビュー 02-02 Suggestion 3）"
    - "バッチ OCR で openai を使うとき、クラウド判定・集約コスト確認・送信先表示・APIキー欠落エラーが単発 OCR と同じ内容で機能する（V190-OAI-06）"
    - "確認ダイアログが表示する送信先ホストと OpenAIProvider が実際にリクエストするホストが一致することが 02-01 Task 3(G) で固定済みであり、本プランはその情報源（catalog.host_for）だけを参照する（T-02-04）"
    - "OpenAI が全ページ統合サマリのフォールバック候補として扱われ、_TEXT_CAPABLE_PROVIDERS の集合が各 Provider クラスの supports_text_prompt と一致することがテストで固定されている（レビュー MEDIUM-9）"
    - "lang.py の ja / en に ocr_provider_name_openai・ocr_api_key_missing_openai・ocr_host_unknown・ocr_model_vision_unverified が同一キーで追加され、キー数の左右一致と未使用キー検出テストが green のまま"
    - "BatchOCRDialog は OCRDialog を継承せずメソッド import もせず、catalog 経由で共有するのはデータのみ（RESEARCH.md Anti-Patterns）"
    - "既存 5 プロバイダの表示名・送信先ホスト・APIキー欠落文言・コスト表示が無変更であり、既存テストが無改修で green のまま（edge: V190-CAT-01 regression）"
    - "フルテストスイート（tests/test_ocr_pipeline.py を含む全件）が失敗 0 件で完走する（レビュー HIGH-6）"
  prohibitions:
    - statement: "MUST NOT 送信先確認・コスト確認ダイアログに「今後表示しない」等の抑止手段を設けてはならない。フォールバックで OpenAI へ切り替わる場合を含め、外部送信のたびにユーザーの明示同意を取り直す"
      category: transparency
    - statement: "MUST NOT 送信先確認ダイアログの同意を経ずに外部ホストへ OCR リクエストを送ってはならない"
      category: transparency
    - statement: "MUST NOT OpenAI の API キーを pagefolio_settings.json へ到達させてはならない"
      category: secrets
    - statement: "MUST NOT 新規 pip 依存を追加してはならない"
      category: dependencies
    - statement: "MUST NOT registry.py に pagefolio 内部モジュールの import を追加してはならない"
      category: architecture
  artifacts:
    - path: "pagefolio/ocr_dialog.py"
      provides: "catalog 経由の表示名 / クラウド判定 / 送信先ホスト / APIキー欠落解決と openai 対応 + 価格プロヴェナンス定数"
      contains: "catalog.is_cloud_provider"
    - path: "pagefolio/dialogs/batch_ocr.py"
      provides: "同型の独立実装（catalog 経由・ロジック非共有）"
      contains: "catalog.is_cloud_provider"
    - path: "pagefolio/lang.py"
      provides: "ocr_provider_name_openai / ocr_api_key_missing_openai / ocr_host_unknown / ocr_model_vision_unverified（ja/en）"
      contains: "ocr_api_key_missing_openai"
  key_links:
    - from: "pagefolio/ocr_dialog.py"
      to: "pagefolio/ocr_providers/catalog.py"
      via: "表示名 / クラウド判定 / host / APIキー欠落 LANG キーの解決"
      pattern: "catalog\\.(is_cloud_provider|host_for|display_name_key_for|api_key_missing_lang_key_for)"
    - from: "pagefolio/dialogs/batch_ocr.py"
      to: "pagefolio/ocr_providers/catalog.py"
      via: "同型の独立実装から同じ catalog を参照（データのみ共有）"
      pattern: "catalog\\.(is_cloud_provider|host_for|api_key_missing_lang_key_for)"
    - from: "pagefolio/ocr_dialog.py"
      to: "pagefolio/lang.py"
      via: "ocr_api_key_missing_openai / ocr_provider_name_openai / ocr_host_unknown / ocr_model_vision_unverified の参照"
      pattern: "ocr_(api_key_missing|provider_name)_openai|ocr_host_unknown|ocr_model_vision_unverified"
    - from: "pagefolio/ocr_dialog.py"
      to: ".planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md"
      via: "OPENAI_PRICE_SOURCE の url / retrieved / unit / currency がマトリクスの記載と一致する"
      pattern: "OPENAI_PRICE_SOURCE"
---

<objective>
OCR ダイアログとバッチ OCR ダイアログに散在するプロバイダメタデータ参照（表示名 2 実装・
クラウド判定 2 箇所・送信先ホスト 5 箇所・APIキー欠落 dict 2 箇所）を `catalog` 経由へ移行し、
同時に OpenAI を既存 5 プロバイダと同じ安全境界（送信先確認・コスト確認・APIキー欠落エラー）に
組み込む。あわせて 02-REVIEWS.md が最大のブロッカーとした
**コスト表示の単価プロヴェナンス**を実装へ落とす。

Purpose: このプランが完了して初めて「`ocr_provider == "openai"` を選んだユーザーが、
ページ画像が `api.openai.com` へ送られる前に必ず送信先とコストの確認ダイアログを見る」という
安全境界が成立する。02-03（LLM 設定 UI への openai 追加）はこのゲートの完成を待つ。
同意の質を担保するため、表示する単価には出典 URL と参照日を持たせ、送信先を解決できない
ケースと画像入力が未確認のモデルについては本文で明示する。

Output: `ocr_dialog.py` 7 メソッド・`batch_ocr.py` 5 メソッドの catalog 移行と openai 対応、
`lang.py` へ ja/en 4 キー追加、価格表への OpenAI 単価とプロヴェナンス定数、
回帰テストとミューテーション検証。

**UI 設計契約について（記録）:** 本フェーズは `--skip-ui` で計画されており `-UI-SPEC.md` は存在しない。
UI 安全ゲートは本フェーズを frontend-touching と判定したが、PageFolio は Tkinter デスクトップアプリで
変更対象が既存 `messagebox` ダイアログの文言・分岐に限られ、02-PATTERNS.md が既存 Claude/Gemini 分岐との
一致度を「exact」と判定しているため override した。新規ウィジェットは追加しない。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ocr-openai-chatgpt/02-CONTEXT.md
@.planning/phases/02-ocr-openai-chatgpt/02-PATTERNS.md
@.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md
@.planning/phases/02-ocr-openai-chatgpt/02-REVIEWS.md
@.planning/phases/02-ocr-openai-chatgpt/02-01-SUMMARY.md
@CLAUDE.md
@pagefolio/CLAUDE.md
</context>

<review_dispositions>
## 02-REVIEWS.md の指摘に対する本プランの処置

| # | 指摘（要約） | severity | 処置 | 根拠 / 反映先 |
|---|---|---|---|---|
| 2 | コスト表示の単価ソースが無い（URL・参照日・単位・通貨が未定） | HIGH | **採用** | 02-01 Task 1 が `02-CAPABILITY-MATRIX.md` に単価と出典を確定済み。本プランは `OPENAI_PRICE_SOURCE` 定数（`url` / `retrieved` / `unit` / `currency`）を両ファイルへ追加し、Task 3(D) が形式妥当性と両ファイル一致を機械保証する |
| 16 | 価格表の検証が「辞書同士の一致」だけで「同じ誤値」を防げない | MEDIUM | **採用** | Task 3(D) を 4 層にした: (i) 両ファイルの表と `OPENAI_PRICE_SOURCE` が同一、(ii) `unit` / `currency` が固定値、(iii) `retrieved` が ISO 日付・`url` が https、(iv) 全推奨モデルが `_PRICE_FALLBACK` 以外へ解決し入力単価 < 出力単価かつ両方 > 0。加えて 02-04 の human-verify に「表示単価を公式価格ページと突き合わせる」項目を追加した |
| 02-02-2 | vision 非対応モデルもコスト確認を通過して選択され得る | HIGH | **採用** | `_confirm_cost` / `_confirm_summary_cost` は、選択モデルが `OpenAIProvider.VERIFIED_VISION_MODELS` に無いとき `ocr_model_vision_unverified` の 1 行を本文へ追加する。モデル一覧側の並び順と注記は 02-03 Task 1 が担当 |
| 8 | host が空のクラウド継承プラグインで「送信先を明示する」契約が満たされない | MEDIUM | **採用** | 新規 LANG キー `ocr_host_unknown` を追加し、`is_cloud_provider` が真なのに `host_for` が空文字を返す場合は本文に送信先不明である旨を出す。プロバイダ表示名だけを出す案は採らない |
| 9 | `_TEXT_CAPABLE_PROVIDERS` が catalog 外の新しいプロバイダ集合になっている | MEDIUM | **採用（D-01 のスコープは変えない）** | `ProviderMeta` へ能力フラグを足すのは D-01 の locked scope 変更になるため行わない。代わりに Task 3(E) で「`_TEXT_CAPABLE_PROVIDERS` == catalog 登録プロバイダのうち Provider クラスの `supports_text_prompt` が真の集合」というパリティテストを置き、追記漏れを機械的に検出する。この境界判断を `ocr_dialog.py` の該当定数コメントにも明記する |
| 10 | 「既存テストが無改修で green」は patch 断絶を見逃している可能性 | LOW | **却下（実コード照合済み）** | `tests/test_provider_ui.py` の該当テストは host 文字列を monkeypatch しておらず、`_confirm_cost` が組み立てた**本文文字列そのもの**をアサートしている（624 行で Claude ホストの包含を、678 / 697 / 718 行で非包含をアサート）。したがって catalog 移行で値が変われば必ず赤くなる。無改修 green には検知力がある |
| 20 | 各プランが大きい。02-02 はコミット単位を分けるべき | LOW | **採用** | Task 1 / Task 2 / Task 3 をそのまま 3 コミットに対応させ、各タスク末尾でコミットする（`feat(02-02): 単発 OCR の catalog 移行と OpenAI 対応` / `feat(02-02): バッチ OCR の同型移行` / `test(02-02): 安全境界の回帰とミューテーション検証`）。D-03 の段階移行方針と一致する |
| 全体-2 | `ast.parse` 構文チェックが無い | MEDIUM | **採用** | 各タスクの `<verify>` と `<verification>` に追加 |
| 全体-3 | 全テストゲートの扱いが CLAUDE.md と不整合 | HIGH | **採用（例外を撤廃）** | 02-01-PLAN.md「テストゲートの確定」節の結論に従う。フルスイート失敗 0 件が絶対ゲート（実測ベースライン 1187 passed / 0 failed）。`--ignore` 例外と相対ゲートは本プランでも使わない |
| 全体-5 | `grep` が Windows で使えない可能性 | LOW | **採用** | 全検証コマンドを `python -c` ワンライナーへ統一 |
| 6（LOW・02-01 由来） | 構造アサーションが実装詳細へ強く結合 | LOW | **採用（方式変更）** | 「〜が消えていること」の検査を `ast.Constant` 走査へ変更（コメント・docstring に影響されない） |
</review_dispositions>

<flagged_assumptions>
## Flagged Assumptions（edge probe — unresolved / unclassified）

| Requirement | Probe | 本プランでの扱い（未解決の前提） |
|---|---|---|
| V190-OAI-04 | unclassified — review manually | 「送信先ホストを明示した確認ダイアログが表示される」の自動検証は `messagebox.askyesno` をフェイクに差し替えたときの引数文字列アサーションまで。実際にモーダルが描画され人間が読める形で表示されることは 02-04 の human-verify に委ねる。また、フォールバック経路では `_fallback_resume` フラグにより `_confirm_cost` が二重表示されない既存仕様が働くため、「必ず 1 回は同意を取る」保証はフォールバック確認ダイアログとの合算で成立している前提を置いている |
| V190-OAI-05 | unclassified — review manually | 概算コストは `OCR_PRICE_TABLE` の静的単価 × 固定トークン見積り（入力 1600 / 出力 500 per page）に基づく既存方式をそのまま流用する。OpenAI の実課金額との乖離は検証しない（実コスト計測は v2 の Deferred 項目）。単価そのものの正しさは `02-CAPABILITY-MATRIX.md` の出典 URL と参照日、および 02-04 human-verify の突き合わせに依存する |
| V190-OAI-06 | unclassified — review manually | バッチ OCR の検証は `BatchOCRDialog` の該当メソッドを直接呼ぶ単体テストまで。複数ファイル × 複数ページの実バッチ実行で openai が完走することは 02-04 の human-verify に委ねる。`BatchOCRDialog` と `OCRDialog` のロジックは意図的に独立実装のままなので、両者の挙動一致はテストの対称性でのみ担保される |
| V190-CAT-01 | unclassified — review manually | 本プラン完了時点で移行済みは 6 参照面のうち 5 面（`ocr.py` 1 面は 02-01 で完了）。残る `sections.py` の一覧リスト 2 箇所は 02-03 が担当し、そこまで完走して初めて V190-CAT-01 の判定が可能になる前提 |
</flagged_assumptions>

<artifacts>
## Artifacts this phase produces（本プランの分）

**新規 LANG キー（ja / en 両方に同一キー・計 4 件）**
- `ocr_provider_name_openai` — 表示名（ja/en とも `"OpenAI (ChatGPT)"`）
- `ocr_api_key_missing_openai` — API キー未解決時のエラー本文（`{env_var}` プレースホルダを持つ。
  既存 `ocr_api_key_missing_gemini` / `ocr_api_key_missing_runpod` と同型）
- `ocr_host_unknown` — クラウド判定が真なのに送信先ホストを解決できないときの表示
  （レビュー MEDIUM-8。プレースホルダ `{provider}` にプロバイダ表示名を埋める）
- `ocr_model_vision_unverified` — 選択モデルが vision 確認済み集合に無いときの注意 1 行
  （レビュー HIGH 02-02-2。プレースホルダ `{model}`）

**新規モジュール定数（`ocr_dialog.py` と `dialogs/batch_ocr.py` に同一内容で 2 箇所・D-02）**
- `OCR_PRICE_TABLE` への OpenAI エントリ — 02-01 Task 1 で確定したモデル ID と
  そのモデルファミリ接頭辞に対する `(入力単価, 出力単価)` タプル
- `OPENAI_PRICE_SOURCE: dict[str, str]` — キーは `url` / `retrieved` / `unit` / `currency`。
  値は `02-CAPABILITY-MATRIX.md` の `source_url` / `retrieved_at` / `price_unit` / `currency` と一致させる

**既存シンボルの意味変更（削除される重複表現）**
- `ocr_dialog.py`: `_provider_display_name` / `_provider_key_to_display_name` の 2 実装が
  catalog 参照へ統合、`_is_cloud_provider` の集合リテラル、`_confirm_cost` /
  `_confirm_summary_cost` / `_fallback_candidate_host` の host リテラル、
  `_check_cloud_api_key` の LANG キー dict
- `dialogs/batch_ocr.py`: `_is_cloud_provider` の集合リテラル、`_build_provider_once` の
  `_cloud_providers` 集合、`_confirm_cost` / `_confirm_summary_cost` の host リテラル、
  `_check_cloud_api_key` の LANG キー dict
- `ocr_dialog.py`: `_TEXT_CAPABLE_PROVIDERS` に `"openai"` を追加（境界の根拠コメントつき）
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: ocr_dialog.py の catalog 移行と OpenAI 安全境界</name>
  <files>pagefolio/ocr_dialog.py, pagefolio/lang.py</files>
  <read_first>
    - `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md`（単価・単位・通貨・出典 URL・参照日・vision 確認済みモデル）
    - `pagefolio/ocr_providers/catalog.py`（02-01 で作成。公開関数のシグネチャと `PROVIDERS["openai"]` の値・アクセサ戻り型）
    - `pagefolio/ocr_providers/openai_provider.py`（02-01 で作成。`VERIFIED_VISION_MODELS`）
    - `pagefolio/ocr_dialog.py` 20-75 行（`OCR_PRICE_TABLE` / `_PRICE_FALLBACK` / `_TEXT_CAPABLE_PROVIDERS`）
    - `pagefolio/ocr_dialog.py` 810-935 行（`_provider_display_name` / `_provider_model_name` / `_is_cloud_provider`）
    - `pagefolio/ocr_dialog.py` 1214-1325 行（`_confirm_cost` / `_confirm_summary_cost` / `_check_cloud_api_key`）
    - `pagefolio/ocr_dialog.py` 2322-2375 行（`_active_provider_name` / `_provider_key_to_display_name` / `_fallback_candidate_host`）
    - `pagefolio/lang.py` 470-535 行（ja の `ocr_api_key_missing*` / `ocr_provider_name_*` 群）と 1215-1280 行（en の同群）
    - `.planning/phases/02-ocr-openai-chatgpt/02-PATTERNS.md`「移行対象の重複箇所（実在箇所・全列挙）」表
    - `.planning/phases/02-ocr-openai-chatgpt/02-CONTEXT.md`（D-01 / D-02 / D-03 / D-04）
  </read_first>
  <action>
`pagefolio/ocr_dialog.py` の 7 箇所を catalog 経由へ移行し、あわせて openai を対応させる。
既存の関数内 import 流儀に合わせ `from pagefolio.ocr_providers import catalog` は
モジュール冒頭ではなく各メソッド内で行う（`pagefolio.ocr_providers` 経由の重い import 連鎖と
循環 import を避ける既存方針。`_check_cloud_api_key` が `registry` をメソッド内 import しているのと同じ形）。
D-03 のとおり一括置換はせず、下記 (1)〜(11) を 1 つずつ適用してその都度テストを通すこと。
本タスク完了時に 1 コミット（`feat(02-02): 単発 OCR ダイアログの catalog 移行と OpenAI 安全境界`）。

**移行後の残留禁止（重要）:** 置換したホスト文字列や LANG キー文字列は、コメント・docstring を
含め当該ファイルに 1 箇所も残さないこと。Task 3 の検査は `ast.Constant` の文字列定数のみを
見るため docstring も対象になる。説明が必要な場合は「catalog が保持する送信先ホスト」の
ように値を書かずに記述する。

(1) `_provider_display_name`（812-839 行）: claude / gemini の isinstance 判定を伴う 2 分岐は
D-04 の安全側フォールバックとして残しつつ、名前による表示名解決を
`catalog.display_name_key_for(name)` → `self._L[key]` の 1 経路へ統合する。
`display_name_key` が None のプロバイダ（`off` / `ollama` / 未登録プラグイン）は
現行どおり `name` をそのまま返す。`lmstudio` と `""` が同じ表示名になる現行挙動を維持するため、
空文字は `lmstudio` として解決すること。

(2) `_provider_key_to_display_name`（2329-2351 行）: dict リテラルを削除し (1) と同じ
`catalog.display_name_key_for` 経由に置換する。isinstance 判定は行わない現行の性格を維持する
（フォールバック候補名の表示に使うため、まだ build されていない候補にも使える必要がある）。
`""` → LM Studio 表示名のマッピングは維持する。

(3) `_is_cloud_provider`（908-929 行）: 集合リテラルを `catalog.is_cloud_provider(name)` に置換する。
`ClaudeProvider` / `GeminiProvider` / `RunPodProvider` の isinstance ガードは削除せずそのまま残す（D-04）。
`OpenAIProvider` を isinstance ガードのタプルへ追加すること
（catalog 登録済みなので通常経路では不要だが、プラグインが `OpenAIProvider` を継承した場合に
外部送信の同意ゲートが消えないようにする。既存 3 クラスと同じ安全側の扱いに揃える）。

(4) 送信先ホスト解決の共通ヘルパー `_resolved_host_text(name, settings)` を新設する
（`ocr_dialog.py` 内のプライベートメソッド。`batch_ocr.py` へは Task 2 で同型の独立実装を置く）。
戻り値は確認ダイアログ本文へそのまま埋め込める文字列で、解決順は次のとおり:
`catalog.host_for(name, settings)` が真値ならそれ、
`runpod` で空なら既存の `self._L["llm_runpod_host_unset"]`、
それ以外で空なら `self._L["ocr_host_unknown"].format(provider=self._provider_key_to_display_name(name))`
（レビュー MEDIUM-8。プロバイダ表示名だけを出すのではなく「送信先が特定できない」ことを
明示する文言を LANG 側に持たせる）。

(5) `_confirm_cost`（1216-1257 行）: host の if/elif/else を (4) のヘルパー呼び出しへ置換する。
モデル名の解決も `catalog.model_setting_key_for(name)` と `catalog.default_model_for(name)` を使い、
`s.get(model_key, "") or catalog.default_model_for(name)` の 1 経路へ統合する
（`model_setting_key` が None のときはモデル名を空文字にする）。
これにより `openai` は `s.get("openai_model", "")` → 空なら catalog 既定モデル、
host は catalog の値として自動的に扱われる。
さらに、`name == "openai"` かつ解決したモデルが `OpenAIProvider.VERIFIED_VISION_MODELS` に
含まれないとき、本文の末尾へ `self._L["ocr_model_vision_unverified"].format(model=model)` を
1 行追加する（レビュー HIGH 02-02-2）。`OpenAIProvider` はメソッド内 import で参照する。

(6) `_confirm_summary_cost`（1259-1287 行）: (4) と同じヘルパーで host を解決する。
サマリ経路は画像を送らないため vision 未確認の注記は付けない。

(7) `_fallback_candidate_host`（2353-2372 行）: 候補名ごとの個別 if を
`catalog.host_for(candidate, settings)` へ統合する。ローカルプロバイダ（`lmstudio` / `ollama`）は
catalog の host が None なので、呼び出し側で `settings.get("lm_studio_url", "http://localhost:1234")` /
`settings.get("ollama_url", "http://localhost:11434")` を返す現行分岐を残す。
`tesseract` はネットワーク送信を伴わないため表示名を返す現行挙動を維持する。
`openai` は catalog の host が返る。

(8) `_check_cloud_api_key`（1289-1324 行）: LANG キー dict リテラル（1310-1314 行）を削除し
`catalog.api_key_missing_lang_key_for(name) or "ocr_api_key_missing"` に置換する。
`primary_env_var(name)` による `{env_var}` 埋め込みと `messagebox.showerror` の外枠は変更しない。

(9) `OCR_PRICE_TABLE`（37-52 行）に OpenAI モデルの単価エントリを追加する。
キーは 02-01 Task 1 で確定したモデル ID と、そのモデルファミリを表す接頭辞
（既存 `claude-sonnet` / `claude-opus` と同じ粒度の部分一致キー）。
`_lookup_price` は dict の宣言順で最初に部分一致したキーを採用するため、
**より具体的なキーを先に、汎用接頭辞を後に**並べること。
最低でも catalog の `default_model` と `RECOMMENDED_MODELS` の全モデルが解決できること。
未知モデルは既存 `_PRICE_FALLBACK` が受けるため新設しない。価格表を catalog へ移すことはしない（D-02）。

(10) `OPENAI_PRICE_SOURCE` をモジュールレベルへ新設する（レビュー HIGH-2）。
`url` / `retrieved` / `unit` / `currency` の 4 キーを持つ dict で、値は
`02-CAPABILITY-MATRIX.md` の `source_url` / `retrieved_at` / `price_unit` / `currency` を
そのまま転記する。直上のコメントに「単価の一次情報。変更時は
`02-CAPABILITY-MATRIX.md` と `dialogs/batch_ocr.py` の同名定数も同時に更新する」と書く。

(11) `_TEXT_CAPABLE_PROVIDERS`（62-64 行）に `"openai"` を追加する
（`OpenAIProvider.supports_text_prompt = True` と整合させ、全ページ統合サマリの
フォールバック候補として扱えるようにする）。直上のコメントへレビュー MEDIUM-9 に対する
境界判断を明記する: 「この集合はプロバイダ非機密メタデータではなく Provider クラスの
`supports_text_prompt` の射影であり、粒度が異なるため catalog（D-01 の 8 フィールド）には
含めない。追記漏れは `tests/test_provider_ui.py` のパリティテストが検出する」。

(12) `pagefolio/lang.py` の ja / en 両方へ同一キーで 4 件追加する
（`ocr_provider_name_openai` / `ocr_api_key_missing_openai` / `ocr_host_unknown` /
`ocr_model_vision_unverified`）。`ocr_provider_name_openai` は既存
`ocr_provider_name_claude` と同じ体裁の 1 行文字列。`ocr_api_key_missing_openai` は
既存の Gemini 版を雛形にし、`{env_var}` プレースホルダと「LLM 設定ダイアログで
セッション限定キーを入力する」旨を含める。`ocr_host_unknown` は `{provider}` を持ち
「送信先ホストを特定できません」の趣旨。`ocr_model_vision_unverified` は `{model}` を持ち
「このモデルの画像入力対応は未確認です」の趣旨。4 キーとも本タスク内の (4)(5)(8) から
実際に参照されるため `tests/test_lang_parity.py::test_no_unused_lang_keys` を単独で通せる。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['pagefolio/ocr_dialog.py','pagefolio/lang.py']]" && python -m pytest tests/test_provider_ui.py tests/test_lang_parity.py tests/test_ocr.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pathlib; s=pathlib.Path('pagefolio/ocr_dialog.py').read_text(encoding='utf-8'); [__import__('sys').exit('missing: '+t) for t in ['catalog.is_cloud_provider','catalog.host_for','catalog.display_name_key_for','catalog.api_key_missing_lang_key_for','OpenAIProvider','OPENAI_PRICE_SOURCE','_resolved_host_text'] if t not in s]"` が終了コード 0
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/ocr_dialog.py').read_text(encoding='utf-8')); lits=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str)]; bad=[h for h in ['api.anthropic.com','generativelanguage.googleapis.com'] if any(h in x for x in lits)]; sys.exit('host literal remains: '+repr(bad) if bad else 0)"` が終了コード 0（host リテラルの消滅・AST 文字列定数のみを検査）
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/ocr_dialog.py').read_text(encoding='utf-8')); lits=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str)]; sys.exit(1 if 'ocr_api_key_missing_gemini' in lits else 0)"` が終了コード 0（LANG キー dict の消滅）
    - `python -c "from pagefolio.lang import LANG; assert set(LANG['ja']) == set(LANG['en']); [__import__('sys').exit('missing key: '+k) for k in ['ocr_provider_name_openai','ocr_api_key_missing_openai','ocr_host_unknown','ocr_model_vision_unverified'] if k not in LANG['ja'] or k not in LANG['en']]"` が終了コード 0
    - `python -c "from pagefolio.ocr_dialog import _lookup_price, _PRICE_FALLBACK; from pagefolio.ocr_providers import OpenAIProvider; assert all(_lookup_price(m) != _PRICE_FALLBACK for m in OpenAIProvider.RECOMMENDED_MODELS)"` が終了コード 0（全推奨モデルが実単価に解決される）
    - `python -c "from pagefolio.ocr_dialog import OPENAI_PRICE_SOURCE as p; import re; assert set(p) == {'url','retrieved','unit','currency'}; assert p['url'].startswith('https://'); assert re.fullmatch(r'20\\d\\d-\\d\\d-\\d\\d', p['retrieved']); assert p['unit'] and p['currency']"` が終了コード 0
    - `python -c "from pagefolio.ocr_dialog import _TEXT_CAPABLE_PROVIDERS; assert 'openai' in _TEXT_CAPABLE_PROVIDERS"` が終了コード 0
    - 既存の `tests/test_provider_ui.py::TestIsCloudProvider` / `TestConfirmCost` / `TestCheckCloudApiKey` が**無改修で** green（624 / 678 / 697 / 718 行の host 本文アサーションが catalog 移行後も同じ文字列を出すことの担保）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>`ocr_provider == "openai"` のとき `_is_cloud_provider` が True、`_confirm_cost` / `_confirm_summary_cost` が catalog 由来の送信先ホストを含む本文を返し、vision 未確認モデルでは注記が 1 行付き、`_check_cloud_api_key` が `ocr_api_key_missing_openai` を表示する。単価には出典 URL と参照日が付いている。既存 5 プロバイダの表示名・host・エラー文言は無変更。</done>
</task>

<task type="auto">
  <name>Task 2: batch_ocr.py の同型 catalog 移行と OpenAI 対応</name>
  <files>pagefolio/dialogs/batch_ocr.py</files>
  <read_first>
    - `pagefolio/dialogs/batch_ocr.py` 1-20 行（「OCRDialog からのコピペ移植・ロジックを共有しない」設計方針コメント）
    - `pagefolio/dialogs/batch_ocr.py` 60-115 行（`OCR_PRICE_TABLE` / `_PRICE_FALLBACK` / `_lookup_price` の独立コピー）
    - `pagefolio/dialogs/batch_ocr.py` 485-570 行（`_is_cloud_provider` / `_estimate_cost` / `_confirm_cost` / `_check_cloud_api_key` / `_confirm_batch_cost`）
    - `pagefolio/dialogs/batch_ocr.py` 592-620 行（`_build_provider_once` の `_cloud_providers` 集合）
    - `pagefolio/dialogs/batch_ocr.py` 965-985 行（`_confirm_summary_cost`）
    - `pagefolio/ocr_dialog.py`（Task 1 で移行済みの対応メソッドと `_resolved_host_text` / `OPENAI_PRICE_SOURCE` — 同一挙動の独立実装として写経元にする）
    - `pagefolio/ocr_providers/catalog.py`
    - `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md`
  </read_first>
  <action>
`pagefolio/dialogs/batch_ocr.py` の 5 箇所を、Task 1 で `ocr_dialog.py` に適用したものと
**同一挙動の独立実装**として移行する。継承・メソッド import による DRY 化は行わない
（ファイル冒頭コメントが明示的に否定している設計判断・RESEARCH.md Anti-Patterns）。
「`ocr_dialog.py:xxx` と同一挙動の独立実装。」という既存の docstring 規約を維持し、
参照している行番号が変わった場合は更新する。catalog は各メソッド内で import する。
移行後にホスト文字列・LANG キー文字列を（コメント・docstring を含め）残さないこと。
本タスク完了時に 1 コミット（`feat(02-02): バッチ OCR ダイアログの同型 catalog 移行`）。

(1) `_is_cloud_provider`（486-500 行）: 集合リテラルを `catalog.is_cloud_provider(name)` に置換し、
isinstance ガードのタプルへ `OpenAIProvider` を追加する（Task 1(3) と同一）。

(2) `_build_provider_once`（602 行）: `_cloud_providers` 集合リテラルの代入文を削除し、
`if name in _cloud_providers:` を `if catalog.is_cloud_provider(name):` へ置換する。
API キー事前解決の外枠（`_resolve_api_key` / `OCRAPIKeyError` の捕捉）は変更しない。

(3) 送信先ホスト解決ヘルパー `_resolved_host_text(name, settings)` を Task 1(4) と
同一挙動の独立実装として新設する（`ocr_host_unknown` フォールバックを含む）。

(4) `_confirm_cost`（511-532 行）: host とモデル名の解決を Task 1(5) と同一の
`_resolved_host_text` / `catalog.model_setting_key_for` / `catalog.default_model_for` 経由へ置換し、
vision 未確認モデルの注記 1 行も同一条件で付与する。
`page_count` が None のときの `STATUS_ERROR` 除外集計は変更しない。

(5) `_confirm_summary_cost`（969-982 行）: Task 1(6) と同一の host 解決へ置換する
（vision 注記は付けない）。

(6) `_check_cloud_api_key`（534-560 行）: LANG キー dict リテラル（548-552 行）を
`catalog.api_key_missing_lang_key_for(name) or "ocr_api_key_missing"` へ置換する。

(7) `OCR_PRICE_TABLE`（71-103 行）へ Task 1(9) と**同一内容・同一宣言順**の OpenAI 単価
エントリを追加し、`OPENAI_PRICE_SOURCE` も Task 1(10) と同一内容で新設する
（D-02 により価格表の一元化は行わない。両ファイルの内容が一致していることを
Task 3(D) のテストで固定する）。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('pagefolio/dialogs/batch_ocr.py').read_text(encoding='utf-8'))" && python -m pytest tests/test_batch_ocr_dialog.py tests/test_batch_ocr_state.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pathlib; s=pathlib.Path('pagefolio/dialogs/batch_ocr.py').read_text(encoding='utf-8'); [__import__('sys').exit('missing: '+t) for t in ['catalog.is_cloud_provider','catalog.host_for','catalog.api_key_missing_lang_key_for','OPENAI_PRICE_SOURCE','_resolved_host_text'] if t not in s]"` が終了コード 0
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/dialogs/batch_ocr.py').read_text(encoding='utf-8')); lits=[n.value for n in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str)]; bad=[h for h in ['api.anthropic.com','generativelanguage.googleapis.com','ocr_api_key_missing_gemini'] if any(h in x for x in lits)]; sys.exit('literal remains: '+repr(bad) if bad else 0)"` が終了コード 0
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/dialogs/batch_ocr.py').read_text(encoding='utf-8')); names=[x.id for n in ast.walk(t) if isinstance(n,ast.Assign) for x in n.targets if isinstance(x,ast.Name)]; sys.exit(1 if '_cloud_providers' in names else 0)"` が終了コード 0
    - `python -c "from pagefolio.ocr_dialog import OCR_PRICE_TABLE as A, OPENAI_PRICE_SOURCE as SA; from pagefolio.dialogs.batch_ocr import OCR_PRICE_TABLE as B, OPENAI_PRICE_SOURCE as SB; assert A == B and list(A) == list(B) and SA == SB"` が終了コード 0（価格表・宣言順・プロヴェナンスが完全一致）
    - `python -c "import ast,pathlib,sys; t=ast.parse(pathlib.Path('pagefolio/dialogs/batch_ocr.py').read_text(encoding='utf-8')); mods=[n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module]; sys.exit(1 if 'pagefolio.ocr_dialog' in mods else 0)"` が終了コード 0（`OCRDialog` からのロジック import が導入されていない）
    - 既存の `tests/test_batch_ocr_dialog.py` が無改修で green
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>バッチ OCR でも openai がクラウド扱いされ、集約コスト確認に catalog 由来の送信先ホストが表示され、API キー未解決時に openai 専用のエラー文言が出る。価格表とプロヴェナンスが単発側と完全一致。`OCRDialog` への依存は増えていない。</done>
</task>

<task type="auto">
  <name>Task 3: 回帰テスト・価格プロヴェナンス検証・検知力（ミューテーション）検証</name>
  <files>tests/test_provider_ui.py, tests/test_batch_ocr_dialog.py</files>
  <read_first>
    - `tests/test_provider_ui.py` 348-730 行（`TestIsCloudProvider` / `TestEstimateCost` / `TestCheckCloudApiKey` / `TestConfirmCost` のフィクスチャとスタブ記法。624 / 678 / 697 / 718 行の host 本文アサーションが移行後も成立することを確認する）
    - `tests/test_batch_ocr_dialog.py`（クラウド判定・コスト確認まわりの既存テストクラスとスタブ記法）
    - `pagefolio/ocr_dialog.py` / `pagefolio/dialogs/batch_ocr.py`（Task 1・Task 2 適用後の実体）
    - `pagefolio/ocr_providers/base.py` と各 Provider の `supports_text_prompt` クラス属性（パリティテストの対象）
    - `.planning/phases/02-ocr-openai-chatgpt/02-CAPABILITY-MATRIX.md`（単価・単位・通貨・出典・vision 確認済み集合）
    - `.planning/phases/02-ocr-openai-chatgpt/02-RESEARCH.md`「Pitfall 1: カタログ移行時に monkeypatch 対象の名前空間が断絶する」
  </read_first>
  <action>
既存テストクラスへ openai ケースを追加し、catalog 移行が検知力を失っていないことを確認する。
新しいテスト作法は持ち込まず、既存クラスと同じスタブ（`_ButtonStub` / `_SetGetVarStub` /
`_VarStub` 等）を再利用する。本タスク完了時に 1 コミット
（`test(02-02): OpenAI 安全境界の回帰テストとミューテーション検証`）。

(A) `tests/test_provider_ui.py`:
- `TestIsCloudProvider` に openai ケース（`ocr_provider="openai"` で True）と、
  catalog 未登録のプラグイン名で False になるケース（D-04）を追加。
- `TestConfirmCost` に openai ケースを追加し、`messagebox.askyesno` へ渡る本文に
  `api.openai.com` が含まれることと、`openai_model` 未設定時に catalog 既定モデルが
  使われることをアサート。`_confirm_summary_cost` の openai ケースも同型で追加。
- `TestCheckCloudApiKey` に openai ケースを追加し、`_resolve_api_key` が
  `OCRAPIKeyError` を上げたとき `showerror` の本文が `ocr_api_key_missing_openai` 由来で
  `OPENAI_API_KEY` を含むことをアサート。
- 新規 `TestProviderDisplayNameCatalog` — `_provider_display_name` と
  `_provider_key_to_display_name` が 8 プロバイダすべてで同じ表示名を返すこと
  （2 実装が 1 情報源へ統合されたことの機械保証）。`""` は LM Studio 表示名、
  未登録名はそのまま返ること。
- 新規 `TestFallbackCandidateHostCatalog` — `_fallback_candidate_host` が 8 プロバイダで
  移行前と同じ文字列を返すこと（openai は catalog の host、lmstudio / ollama は
  settings の URL、tesseract は表示名）。
- 新規 `TestResolvedHostTextUnknown` — レビュー MEDIUM-8。`catalog.host_for` が空文字を
  返すクラウド判定プロバイダ（`ClaudeProvider` を継承した catalog 未登録のダミープラグイン）で
  `_confirm_cost` の本文に `ocr_host_unknown` 由来の文字列が現れ、
  プロバイダ表示名だけで済ませていないことをアサートする。
- 新規 `TestVisionUnverifiedNotice` — レビュー HIGH 02-02-2。
  `openai_model` に `VERIFIED_VISION_MODELS` 外の ID を入れたとき `_confirm_cost` の本文へ
  `ocr_model_vision_unverified` 由来の 1 行が追加され、`VERIFIED_VISION_MODELS` 内の ID では
  追加されないことをアサートする。
- 新規 `TestConfirmDenialStopsSend` — レビュー 02-02 Suggestion 3。
  `messagebox.askyesno` が False を返すフェイクで `_on_run` を駆動し、
  `pagefolio.ocr.build_provider` を呼び出し記録つきスタブに差し替えたとき
  **1 回も呼ばれない**こと、および `urllib.request.urlopen` を例外送出スタブに
  差し替えても例外が発生しない（＝ HTTP へ到達しない）ことをアサートする。
- 新規 `TestTextCapableProvidersParity` — レビュー MEDIUM-9。
  `_TEXT_CAPABLE_PROVIDERS` が「`catalog.PROVIDERS` のうち対応する Provider クラスの
  `supports_text_prompt` が真のプロバイダ名の集合」と一致すること。
  名前 → クラスの対応表はテスト側に置く（catalog は Provider を import しない・D-05）。
  将来 `supports_text_prompt=True` のプロバイダを catalog へ追加して
  `_TEXT_CAPABLE_PROVIDERS` を更新し忘れた瞬間に赤くなる装置。

(B) 新規 `TestOpenAIPriceProvenance`（`tests/test_provider_ui.py`）— レビュー HIGH-2 / MEDIUM-16。
辞書同士の一致だけでは「同じ誤値」を防げないため 4 層で検証する:
1. `ocr_dialog.OCR_PRICE_TABLE == batch_ocr.OCR_PRICE_TABLE` かつ宣言順も一致、
   `OPENAI_PRICE_SOURCE` も両ファイルで一致
2. `OPENAI_PRICE_SOURCE` のキー集合が `{url, retrieved, unit, currency}` で、
   `url` が `https://` 始まり、`retrieved` が `YYYY-MM-DD` 形式、
   `unit` と `currency` が空でないこと
3. `OpenAIProvider.RECOMMENDED_MODELS` の全モデルが `_lookup_price` で
   `_PRICE_FALLBACK` 以外へ解決されること（単発・バッチの両実装で）
4. 各 OpenAI エントリについて入力単価 > 0・出力単価 > 0・入力単価 < 出力単価
   （OpenAI / Anthropic の課金体系に共通する実世界不変条件。桁ずれ・入出力の
   取り違えを検出する）
実際の金額の正しさは `02-CAPABILITY-MATRIX.md` の出典 URL と参照日、および
02-04 Task 3B の human-verify（公式価格ページとの突き合わせ）が担保する旨を
テストの docstring に明記すること。

(C) `tests/test_batch_ocr_dialog.py`: (A) と対称に、バッチ側の
`_is_cloud_provider` / `_confirm_cost` / `_confirm_summary_cost` / `_check_cloud_api_key` /
`_build_provider_once` / `_resolved_host_text` の openai ケースを追加する。
加えて次の 2 本を置く:
- 単発 OCR とバッチで同一 settings に対する `_confirm_cost` の本文（host 部分）が
  一致することをアサートするテスト（独立実装の挙動一致の固定）
- バッチ側の `TestConfirmDenialStopsSend` 相当（「いいえ」で `_build_provider_once` に
  到達しないこと）

(D) ミューテーション検証（RESEARCH.md Pitfall 1）: 本番コードへ意図的に 1 つバグを注入し、
テストが赤になることを確認してから revert する。最低 3 パターンを実施して SUMMARY に記録:
1. `catalog.PROVIDERS["openai"].host` を別ホスト文字列へ一時変更 →
   `TestConfirmCost` の openai ケースと 02-01 の `TestConfirmedHostMatchesProviderEndpoint` が赤
2. `ocr_dialog.py:_check_cloud_api_key` の catalog 参照を固定 LANG キーへ一時変更 →
   `TestCheckCloudApiKey` の openai ケースが赤
3. `ocr_dialog.py` の OpenAI 単価エントリの出力単価を入力単価より小さい値へ一時変更 →
   `TestOpenAIPriceProvenance` の第 4 層が赤（価格検証に検知力があることの確認）
どれかが「green のまま通ってしまう」場合は monkeypatch 対象の名前空間が
断絶している（テストが実体を見ていない）ので、テスト側のパッチ対象を修正すること。
検証後は必ず revert し、作業ツリーがクリーンであることを確認する。
  </action>
  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['tests/test_provider_ui.py','tests/test_batch_ocr_dialog.py']]" && python -m pytest tests/test_provider_ui.py tests/test_batch_ocr_dialog.py tests/test_lang_parity.py -x -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_provider_ui.py -k "openai or Catalog or Provenance or Unverified or Denial or Parity" -q` が最低 10 テスト green
    - `python -m pytest tests/test_batch_ocr_dialog.py -k "openai or Denial or Host" -q` が最低 6 テスト green
    - `TestTextCapableProvidersParity` が存在し、`_TEXT_CAPABLE_PROVIDERS` を Provider クラスの `supports_text_prompt` から機械的に導出して突き合わせている
    - `TestOpenAIPriceProvenance` が上記 4 層すべてを実装している
    - ミューテーション検証 3 パターンの「注入 → 赤 → revert → green」がそれぞれ実施され、赤になったテスト名が SUMMARY に記録されている
    - `python -c "import subprocess,sys; r=subprocess.run(['git','status','--porcelain','pagefolio/'],capture_output=True,text=True); sys.exit(1 if r.stdout.strip() else 0)"` が終了コード 0（ミューテーション検証後に作業ツリーがクリーン）
    - `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が **失敗 0 件**（`--ignore` 例外を使わない・レビュー HIGH-6）
    - `ruff check . && ruff format --check .` が終了コード 0
  </acceptance_criteria>
  <done>openai を含む 8 プロバイダについて表示名・クラウド判定・送信先ホスト・APIキー欠落メッセージ・単価プロヴェナンスが単発 OCR / バッチ OCR の両方で回帰テストに固定され、「いいえ」で送信に到達しないことと、意図的なバグ注入で確実に赤くなることを確認済み。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ローカルアプリ → `api.openai.com` | ページ画像が組織外へ出る地点。ユーザーの明示同意ゲート（送信先確認・コスト確認）がここに立つ |
| 表示（確認ダイアログ）→ 実際の送信先 | ユーザーが同意した対象と実際の通信先が一致していることが同意の前提 |
| 表示（概算コスト）→ 実際の課金 | 同意の量的前提。単価の出所が不明だと同意の質が損なわれる |
| サードパーティプラグイン → クラウド判定 | プラグインが `is_cloud_provider` を False に落として同意ゲートを迂回し得る地点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | `ocr_dialog.py:_is_cloud_provider` / `_confirm_cost`・`batch_ocr.py` 同型 | high | mitigate | `catalog.is_cloud_provider("openai") == True` により openai が同意ゲート対象になる（Task 1(3) / Task 2(1)）。「今後表示しない」オプションは設けない既存方針を維持（must_haves.prohibitions）。「いいえ」で `build_provider` にも HTTP にも到達しないことを Task 3(A)/(C) の `TestConfirmDenialStopsSend` で固定 |
| T-02-04 | Spoofing | 確認ダイアログの表示ホスト vs `OpenAIProvider.CHAT_ENDPOINT` | high | mitigate | 表示ホストの情報源を `catalog.host_for` の 1 箇所に統合（Task 1(4)(5)(6)(7) / Task 2(3)(4)(5)）。catalog の host と Provider のエンドポイント定数のホスト部一致は 02-01 Task 3(G) で機械保証済み。Task 3(D) のミューテーション 1 でその検知力を再確認する |
| T-02-07 | Elevation of Privilege | プラグインプロバイダによるクラウド判定回避 | medium | mitigate | catalog 未登録プロバイダは `is_cloud_provider` が False だが、既知クラウド Provider クラス（`OpenAIProvider` を含む 4 クラス）を継承したインスタンスは isinstance ガードで True に倒す（D-04・Task 1(3) / Task 2(1)）。判定経路が 2 本残ることは意図的な安全側の選択 |
| T-02-09 | Repudiation | 同意記録の欠如 | low | accept | PageFolio はローカル単一ユーザーアプリで監査ログ機構を持たず、同意は「毎回ダイアログを出す」ことで担保する設計。同意ログの永続化は本フェーズのスコープ外 |
| T-02-10 | Information Disclosure | フォールバック連鎖での確認スキップ | high | mitigate | `_propose_fallback` は各段で `messagebox.askyesno` を必須化する既存実装（`pagefolio/ocr_dialog.py:2410-2426`・V180-D-02）。openai は `_fallback_candidate_host` から catalog の host を表示できるようになる（Task 1(7)）。フォールバック候補としての実配線は 02-04 Task 2 |
| T-02-17 | Spoofing | 送信先を解決できないクラウドプロバイダで送信先が不明のまま同意を取る | high | mitigate | `_resolved_host_text` が空ホストを検出したとき `ocr_host_unknown` を出す（Task 1(4) / Task 2(3)）。`TestResolvedHostTextUnknown` で固定（Task 3(A)）。レビュー MEDIUM-8 |
| T-02-18 | Tampering | 誤った単価による同意の質の毀損 | medium | mitigate | `OPENAI_PRICE_SOURCE`（url / retrieved / unit / currency）を両ファイルへ持たせ、`TestOpenAIPriceProvenance` の 4 層で形式妥当性・一致・実世界不変条件を検証（Task 3(B)）。金額そのものの突き合わせは 02-04 Task 3B の human-verify。レビュー HIGH-2 / MEDIUM-16 |
| T-02-19 | Tampering | 画像入力非対応モデルを選んだまま同意し課金だけ発生する | medium | mitigate | `VERIFIED_VISION_MODELS` 外のモデル選択時に確認ダイアログへ未確認である旨の 1 行を追加（Task 1(5) / Task 2(4)）。モデル一覧側の並び順と注記は 02-03。レビュー HIGH 02-02-2 |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本プランは新規パッケージを一切インストールしない（V190-OAI-11）。RESEARCH.md「Package Legitimacy Audit」該当なしの判定を継承 |
</threat_model>

<verification>
- `python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['pagefolio/ocr_dialog.py','pagefolio/dialogs/batch_ocr.py','pagefolio/lang.py']]"` が終了コード 0
- `python -m pytest tests/test_provider_ui.py tests/test_batch_ocr_dialog.py tests/test_lang_parity.py tests/test_ocr.py -q` が全件 green
- `python -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が **失敗 0 件**
- `ruff check . && ruff format --check .` が終了コード 0
- `git diff pagefolio/ocr_providers/registry.py pagefolio/ocr_providers/errors.py` が空（D-06 / D-12）
- `git diff --stat requirements.txt pyproject.toml` が空（V190-OAI-11）
- ミューテーション検証 3 パターンの結果が SUMMARY に記録され、作業ツリーがクリーン
- 3 コミットに分割されている（単発移行 / バッチ移行 / 回帰テスト）
</verification>

<success_criteria>
- V190-CAT-01: `ocr_dialog.py` 7 箇所 + `batch_ocr.py` 5 箇所が catalog 経由になり、host / 表示名 / APIキー欠落 LANG キーのリテラル重複が両ファイルの AST 上の文字列定数から消えた（残る移行対象は `sections.py` の一覧リスト 2 箇所のみ）
- V190-OAI-04: openai 選択時、OCR 実行前に catalog 由来の送信先ホストを明示した確認ダイアログが出る。ホストを解決できないクラウドプロバイダでは送信先不明である旨が出る
- V190-OAI-05: openai 選択時、対象ページ数と概算コストの確認ダイアログが出る。単価には出典 URL と参照日が付く
- V190-OAI-06: バッチ OCR でも openai がクラウド判定・集約コスト確認・送信先表示・APIキー欠落エラーの対象になる
- レビュー HIGH-2 / MEDIUM-16: 価格プロヴェナンスが定数化され、4 層のテストで検証されている
- レビュー HIGH 02-02-2: vision 未確認モデルの選択時に注記が出る
- レビュー MEDIUM-8 / MEDIUM-9: 送信先不明の明示と `_TEXT_CAPABLE_PROVIDERS` パリティテストが入っている
- 既存 5 プロバイダの表示名・送信先・エラー文言・コスト表示が無変更（既存テストが無改修で green）
</success_criteria>

<output>
Create `.planning/phases/02-ocr-openai-chatgpt/02-02-SUMMARY.md` when done
</output>
</content>
