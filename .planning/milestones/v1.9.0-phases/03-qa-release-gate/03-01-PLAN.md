---
phase: 03-qa-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/file_ops.py
  - tests/test_toast.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [V190-QA-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "保存に失敗してトーストの「再試行」を押すと、上書き確認ダイアログ（askyesno）も保存先ピッカー（asksaveasfilename）も再表示されずに、前回確定した対象へ保存が再実行される（D-09 / D-10）"
    - "初回の保存操作では、これまでどおり上書き確認ダイアログ／保存先ピッカーが表示される（確認スキップは再試行経路にのみ適用される）"
    - "再試行を 2 回以上連続で実行しても、確認ダイアログ・保存先ピッカーは一度も再表示されず、毎回同一の確定パスへ保存される"
    - "トースト表示中に別ファイルを開く・ファイルを閉じるなどアプリ状態が変化しても、retry_cb は確認・選択の時点で束縛された確定パスへのみ書き込み、切り替わった後の `self.filepath` へは書き込まない（D-11 / T-03-01-02。トーストは非モーダルで、表示中のアプリ状態変化と再試行の発火が並行しうる）"
    - "ファイルを閉じた後（`self.doc` が falsy）に古いトーストの再試行が発火しても、実保存層は書き込みも例外送出も行わずステータス通知だけで戻る（T-03-01-02）"
    - "実保存層が例外を送出した場合でも `self.doc` は有効なまま維持され、同じ確定パスでの再試行が引き続き可能である（失敗 → 再試行 → 成功でトーストが dismiss される）"
    - "暗号化 PDF を保存したとき、再試行経路を通っても暗号化が維持される（`tests/test_password.py` の暗号化維持テストが全件 green のまま）"
    - "`.planning/REQUIREMENTS.md` の V190-QA-02 と `.planning/ROADMAP.md` の Phase 3 Success Criteria #2 が、いずれも「再試行時は確認・保存先選択を再表示しない」挙動を記述している（D-12）"
    - "保存 3 経路すべて（`_save_file` / `_save_as` / `_save_compressed`）が同一の分離形（確認・パス選択層 + path 引数を取る実保存層）を持つ（D-10 / D-11）"
  prohibitions:
    - "再試行が、ユーザーが確認していない別のパスへ黙って書き込んではならない。確定パスは確認・選択の時点で束縛され、以降のアプリ状態変化（ファイルを閉じる・別ファイルを開く）で書き込み先が入れ替わってはならない"
    - "確認スキップを初回保存へ波及させてはならない。破壊的な上書き保存の初回実行は、常にユーザーの明示的な同意を経なければならない"
    - "再試行経路が暗号化維持の指定を落として、暗号化された PDF を平文で保存してはならない"
  artifacts:
    - path: "pagefolio/file_ops.py"
      provides: "保存 3 経路の「確認・パス選択層」と「path 引数を取る実保存層」の分離"
      contains: "def _do_save_file"
    - path: "tests/test_toast.py"
      provides: "再試行が確認ダイアログ／保存先ピッカーを経由しないことの振る舞い検証"
      contains: "retry_skips"
  key_links:
    - from: "pagefolio/file_ops.py"
      to: "pagefolio/toast.py"
      via: "_show_error_or_toast の retry_cb に、確定パスを束縛した実保存層 callable を渡す"
      pattern: "self\\._do_save_"
    - from: "pagefolio/file_ops.py"
      to: "pagefolio/file_ops.py"
      via: "_do_save_file / _do_save_compressed が _overwrite_current_file を暗号化 kwargs つきで呼ぶ"
      pattern: "_overwrite_current_file\\("
---

<objective>
保存トーストの「再試行」ボタンが、上書き確認ダイアログ・保存先ピッカーを再表示せず、
前回確定した対象へ黙って再保存するようにする（V190-QA-02 / D-09〜D-12）。

**要件文言の訂正が前提になっている点に注意（D-09 / D-12）:**
`.planning/REQUIREMENTS.md` と `.planning/ROADMAP.md` の現行文言は「上書き確認ダイアログが
再表示される」だが、これは v1.8.0 Phase 6 の `06-REVIEW.md` IN-01 が提起した Fix 提案（確認を
スキップする低レベル再試行入口）とは正反対で、**現状の挙動をそのまま書き写してしまった記述ミス**
として扱うことが D-09 で確定している。文言どおりに実装すると「何もしない」に帰着するため、
本プランは D-09 の意図（再試行時は確認をスキップする）を実装し、両ドキュメントを D-12 の
訂正文言へ揃える。

Purpose: 一度確認済みのユーザーが一過性の I/O 失敗で再試行する際に、毎回確認を求められる
のは「再試行」という語の普通の意味に反する。IN-01 が future polish として持ち越した項目の正式消化。

Output:
- `pagefolio/file_ops.py` — 保存 3 経路を「確認・パス選択層」と「path 引数を取る実保存層」へ分離
- `tests/test_toast.py` — 既存 3 アサーション（オブジェクト等価性）を振る舞いベースへ書き換え
- `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` — D-12 の文言訂正

**本プランがフェーズ唯一の tracer を担う。** Task 1 は `_save_file` 経路 1 本だけを
`file_ops.py`（確認層 → 実保存層）→ `ui_builder.py:_show_error_or_toast` → `toast.py:ToastManager`
→ 振る舞い検証テスト、という全レイヤを端から端まで通す縦スライスであり、production 品質で
コミットする（プロトタイプではない）。Task 2 はそこから `_save_as` / `_save_compressed` へ
水平展開する拡張タスク。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-qa-release-gate/03-CONTEXT.md
@.planning/phases/03-qa-release-gate/03-RESEARCH.md
@.planning/phases/03-qa-release-gate/03-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- 実行者が探索せずに済むよう、既存の契約をここに固定する。 -->

pagefolio/file_ops.py（現状・行番号は着手時点の実測）:
  1085: def _is_current_file(self, path)          -> bool
  1102: def _overwrite_current_file(self, path, **save_kwargs) -> None
        # save_kwargs.setdefault("encryption", fitz.PDF_ENCRYPT_KEEP)
        # メモリへ tobytes → doc.close → tmp へ書いて os.replace → 開き直し
        # 失敗時は bytes から doc を復元して再送出（ロールバック済み）
        # 保存成功後 self.pdf_has_password = derive_pdf_has_password(...)
  1134: def _save_file(self)                       # 確認層 + 実処理が同居（分離対象）
  1174: def _save_as(self)                         # 同上
  1221: def _save_compressed(self)                 # 同上

pagefolio/ui_builder.py（無改造・通過するだけ）:
  189: def _show_error_or_toast(self, category, title, msg, retry_cb) -> None
       # self._toast があれば toast.show(category, msg, retry_cb=retry_cb)
       # なければ messagebox.showerror(title, msg)

pagefolio/toast.py（無改造）:
  40: def show(self, category, message, retry_cb) -> None
      # 同一 category で表示中なら本文更新 + 再試行ボタンの command を retry_cb へ差し替え（WR-03）

file_ops.py の現行 import（`functools` は未 import — 追加が必要）:
  import logging / import os / from tkinter import filedialog, messagebox, simpledialog
  import fitz / from pagefolio.constants import IMAGE_EXTENSIONS, SUPPORTED_EXTENSIONS

tests/test_toast.py の既存スタブ（そのまま流用する）:
  _RecordingToast          # .shown: list[(category, msg, retry_cb)] / .dismissed: list[category]
  _FakeFileOpsApp(fo.FileOpsMixin, ui_builder_mod.UIBuilderMixin)
                           # doc / toast / filepath / overwrite_error / _t / _set_status
                           # _overwrite_current_file(path, **kw) / _is_current_file(path) -> False
  _RaisingThenOkDoc        # 1 回目の save で例外、2 回目は成功
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: tracer — 「保存失敗 → トースト再試行 → 確認なしで再保存」を `_save_file` 1 経路で端から端まで通す</name>
  <reversibility rating="costly">D-11 の関数境界の切り直しは保存 3 経路すべてに波及し、戻すには 3 経路の再修正が必要。既存の `_overwrite_current_file` / `ToastManager` 連携もこの境界の上に載る。</reversibility>
  <files>pagefolio/file_ops.py, tests/test_toast.py</files>
  <read_first>
    - `pagefolio/file_ops.py`（1085-1200 行 — `_is_current_file` / `_overwrite_current_file` / `_save_file` / `_save_as` の現状）
    - `pagefolio/ui_builder.py`（170-200 行 — `_show_error_or_toast` の契約。無改造で通過させる）
    - `pagefolio/toast.py`（40-84 行 — `ToastManager.show` / `_build_frame` の retry_cb 差し替え契約。無改造）
    - `tests/test_toast.py`（1-60 行のスタブ定義と 250-360 行の `TestSaveFilePathsUseSharedHelper`）
    - `.planning/phases/03-qa-release-gate/03-PATTERNS.md`（`_overwrite_current_file` 型の参照実装と分離方針）
  </read_first>
  <behavior>
    - Test 1（RED から開始）: `_save_file` が失敗してトーストが出た後、`toast.shown[0][2]`（retry_cb）を呼ぶと、`messagebox.askyesno` が **一度も呼ばれず**に保存が再実行される
    - Test 2: 初回の `_save_file()` では `messagebox.askyesno` が呼ばれ、`False` を返すと保存が実行されない（既存挙動の維持）
    - Test 3: retry_cb を 2 回連続で呼んでも `askyesno` は呼ばれず、保存先は毎回同一の確定パス（`_FakeFileOpsApp.filepath` 設定時の値）である
    - Test 4: 1 回目の retry_cb が失敗しても `app.doc` は None にならず、2 回目の retry_cb で成功して `toast.dismissed[-1] == "save_file"` になる
    - Test 5: `app.doc` を None にしてから retry_cb を呼ぶと、`doc.save` も `_overwrite_current_file` も呼ばれず、例外も送出されない
    - Test 6（並行性エッジ）: retry_cb を受け取った後に `app.filepath` を別のパスへ差し替えてから retry_cb を呼ぶと、書き込み先は**差し替え後のパスではなく初回確認時に束縛された元のパス**である（トースト表示中に別ファイルを開く操作が挟まっても、書き込み先が入れ替わらない）
  </behavior>
  <action>
`pagefolio/file_ops.py` に `import functools` を追加する（既存 import 群の標準ライブラリ側・`logging` / `os` と同じブロックへ、ruff の isort 順に従って挿入）。

**`_do_save_file(self, path)` を新設する**（配置は `_save_file` の直前・`_overwrite_current_file` の後）。責務は「確認ダイアログを一切含まない実保存層」で、内容は現行 `_save_file` の `try:` ブロック以降をそのまま移送し、`self.filepath` 参照をすべて引数 `path` へ置き換えたもの:
  1. 先頭ガード: `self.doc` が falsy なら `self._set_status(self._t("info_open_first"))` して return する（ファイルを閉じた後に古い再試行が発火しても書き込みが起きないようにする。T-03-01-02 の緩和策。新規 LANG キーは追加しない — `info_open_first` は既存キー）
  2. `self.doc.save(path, incremental=True, encryption=fitz.PDF_ENCRYPT_KEEP)` を試み、例外時は `logger.debug` の既存メッセージを保ったまま `self._overwrite_current_file(path)` へフォールバック
  3. 成功時: `self._set_status(self._t("status_saved").format(name=os.path.basename(path)))` → `self.plugin_manager.fire_event("on_file_save", self, path)` → `self._toast` があれば `dismiss("save_file")`
  4. 失敗時: `self._show_error_or_toast("save_file", self._t("err_save_title"), self._t("err_save_msg").format(e=e), functools.partial(self._do_save_file, path))`

`_save_file(self)` は「確認・パス選択層」だけを残す: `self.doc` 未オープン時の `messagebox.showinfo`、`self.filepath` 未設定時の `_save_as()` 委譲、`IMAGE_EXTENSIONS` 時の `_set_status` + `_save_as()` 委譲、`messagebox.askyesno` による上書き確認、を現行のまま維持し、確認が通ったら `self._do_save_file(self.filepath)` を呼んで終わる。`_save_file` 自身は `try/except` を持たない（例外処理は実保存層が引き受ける）。

`tests/test_toast.py` の `TestSaveFilePathsUseSharedHelper` に、上記 `<behavior>` の Test 1〜6 を満たすテストを追加する。Test 6 は「retry_cb を受け取ってから `app.filepath` を別値へ差し替え、その後に retry_cb を呼んで、実際に書き込まれたパスが差し替え前の値であること」をアサートする形にする（`_FakeFileOpsApp` の `_overwrite_current_file` / `doc.save` が受け取った path を記録して比較する）。テスト名は `retry_skips` を含む形にすること（例: `test_save_file_retry_skips_confirm_dialog`）。確認ダイアログが呼ばれないことの検証は、`monkeypatch.setattr(fo.messagebox, "askyesno", ...)` で呼び出し回数を数えるスパイに差し替え、retry_cb 呼び出しの前後でカウンタが変化しないことをアサートする方式にする（`retry_cb == app._save_file` のようなオブジェクト等価性は使わない）。既存の `_RecordingToast` / `_FakeFileOpsApp` / `_RaisingThenOkDoc` を流用し、新規スタブは追加しない。

**先にテストを書いて RED を確認してから実装する。** 実装前にテストを走らせて「確認ダイアログが再表示される」ことで失敗することを目視で確認し、その後に `_do_save_file` の分離を入れて GREEN にする。

既存の `test_save_file_failure_shows_toast_with_retry`（`assert retry_cb == app._save_file`・327 行付近）は本タスクで意図的に破壊されるため、同じテスト内のオブジェクト等価性アサーションを削除し、「retry_cb が callable であること」＋「呼ぶと `askyesno` を経由せず保存が走ること」の振る舞いアサーションへ置き換える。トースト表示そのもの（category / メッセージ本文）のアサーションは維持する。

`ruff check . && ruff format .` を通し、`pagefolio/ui_builder.py` と `pagefolio/toast.py` には一切手を入れないこと（D-11 で ToastManager 側の状態保持案は不採用）。
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_toast.py tests/test_password.py tests/test_save_overwrite.py --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _do_save_file" pagefolio/file_ops.py` が `1`
    - `grep -c "^import functools" pagefolio/file_ops.py` が `1`
    - `grep -n "functools.partial(self._do_save_file" pagefolio/file_ops.py` が 1 行以上ヒットする
    - `grep -v '^\s*#' pagefolio/file_ops.py | grep -c "askyesno"` が `1`（上書き確認は `_save_file` の確認層 1 箇所のみ。`_close_file` の確認は別の LANG キー `close_confirm` を使うため、この数え上げが 2 になる場合は `save_confirm_msg` を含む行が 1 行だけであることを `grep -c "save_confirm_msg" pagefolio/file_ops.py` == 1 で代替確認する）
    - `grep -c "retry_skips" tests/test_toast.py` が 1 以上
    - `.\.venv\Scripts\python.exe -m pytest -q tests/test_toast.py --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件
    - `.\.venv\Scripts\python.exe -m pytest -q tests/test_password.py --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件（暗号化維持の回帰なし・T-03-01-01）
    - 振る舞い: トーストの retry_cb を呼ぶと `messagebox.askyesno` が呼ばれずに保存が完了する
    - 振る舞い: `app.doc = None` の状態で retry_cb を呼んでも `doc.save` / `_overwrite_current_file` が呼ばれず例外も出ない
    - 振る舞い（並行性エッジ）: retry_cb を受け取った後に `app.filepath` を別パスへ差し替えても、retry_cb が実際に書き込むパスは束縛時の値のまま変わらない（Test 6）
    - `ruff check . && ruff format --check .` が通る
    - `pagefolio/ui_builder.py` と `pagefolio/toast.py` に差分がない（`git diff --stat -- pagefolio/ui_builder.py pagefolio/toast.py` の出力が空）
  </acceptance_criteria>
  <done>`_save_file` 経路が確認層と実保存層に分離され、トーストの再試行が上書き確認を経由せず保存を再実行することが自動テストで固定された。ページ 1 枚ぶんの縦スライスがコミット済み。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `_save_as` / `_save_compressed` への水平展開（D-10 の 3 経路一貫化）</name>
  <files>pagefolio/file_ops.py, tests/test_toast.py</files>
  <read_first>
    - `pagefolio/file_ops.py`（Task 1 適用後の `_do_save_file` / `_save_file`、および 1174-1195 行の `_save_as`・1221-1261 行の `_save_compressed`）
    - `tests/test_toast.py`（Task 1 で追加したテストと、`test_save_as_failure_then_success_dismisses`・`test_save_compressed_failure_shows_toast`）
    - `tests/test_password.py`（`test_save_as_keeps_encryption` / `test_save_file_fallback_keeps_encryption` / `test_save_compressed_*_keeps_encryption` — 暗号化維持の既存契約）
  </read_first>
  <behavior>
    - Test 1: `_save_as` 失敗後の retry_cb を呼ぶと `filedialog.asksaveasfilename` が呼ばれずに保存が再実行され、成功で `toast.dismissed[-1] == "save_as"` になる
    - Test 2: `_save_compressed` 失敗後の retry_cb を呼ぶと `filedialog.asksaveasfilename` が呼ばれずに保存が再実行され、`garbage=4` / `deflate=1` / `clean=1` / `encryption` を含む同一の `save_kwargs` が渡る
    - Test 3: `_save_compressed` の retry_cb を 2 回連続で呼んでも保存先パスは初回選択時のまま変わらない
    - Test 4: 初回の `_save_as()` / `_save_compressed()` では `asksaveasfilename` が呼ばれ、キャンセル（空文字返却）時は保存が実行されない（既存挙動の維持）
  </behavior>
  <action>
`_do_save_as(self, path)` を新設する。責務は現行 `_save_as` の `try:` ブロック以降そのまま:
  1. 先頭ガード: `self.doc` が falsy なら `self._set_status(self._t("info_open_first"))` して return
  2. `self.doc.save(path, encryption=fitz.PDF_ENCRYPT_KEEP)` — **`encryption` の明示指定を必ず残すこと**（Phase 1 / V190-SAFE-01 で確立した暗号化維持ロジック。落とすと `tests/test_password.py` が red になる）
  3. 成功時: `self.filepath = path` → `_set_status(status_saved)` → `fire_event("on_file_save", self, path)` → toast `dismiss("save_as")`
  4. 失敗時: `self._show_error_or_toast("save_as", self._t("err_title"), str(e), functools.partial(self._do_save_as, path))`

`_save_as(self)` は `self.doc` ガードと `filedialog.asksaveasfilename(defaultextension=".pdf", filetypes=[(self._t("filetypes_pdf"), "*.pdf")])`、空パス時の early return までを残し、パス確定後に `self._do_save_as(path)` を呼んで終わる。

`_do_save_compressed(self, path, save_kwargs)` を新設する。`save_kwargs` は dict を明示引数で受ける（`**kwargs` にしない — retry_cb で束縛したときに元の dict がそのまま再利用されることを型として明らかにするため）。責務:
  1. 先頭ガード: `self.doc` が falsy なら `self._set_status(self._t("info_open_first"))` して return
  2. `self._is_current_file(path)` が真なら `self._overwrite_current_file(path, **save_kwargs)` して `self.filepath = path`、偽なら `self.doc.save(path, **save_kwargs)`
  3. 成功時: `_set_status(self._t("status_compressed").format(name=os.path.basename(path)))` → toast `dismiss("save_compressed")`（現行どおり `fire_event` は呼ばない — 挙動を変えない）
  4. 失敗時: `self._show_error_or_toast("save_compressed", self._t("err_title"), str(e), functools.partial(self._do_save_compressed, path, save_kwargs))`

`_save_compressed(self)` は `self.doc` ガード（`messagebox.showinfo`）、`dialog_kwargs` の組み立て（`initialdir` / `initialfile` 付与）、`asksaveasfilename`、空パス時の early return、`save_kwargs` 辞書の構築（`garbage=4` / `deflate=1` / `clean=1` / `encryption=fitz.PDF_ENCRYPT_KEEP` と既存コメント）までを残し、最後に `self._do_save_compressed(path, save_kwargs)` を呼ぶ。

`tests/test_toast.py` の `test_save_as_failure_then_success_dismisses`（338 行付近の `assert toast.shown[-1][2] == app._save_as`）と `test_save_compressed_failure_shows_toast`（352 行付近の `assert toast.shown[-1][2] == app._save_compressed`）から、オブジェクト等価性アサーションを削除する。代わりに `<behavior>` の Test 1〜4 を満たす振る舞いアサーションを Task 1 と同じスパイ方式（`monkeypatch.setattr(fo.filedialog, "asksaveasfilename", <呼び出し回数カウンタ>)`）で追加する。テスト名には `retry_skips` を含める。

`ruff check . && ruff format .` を通し、`pagefolio/ui_builder.py` / `pagefolio/toast.py` は引き続き無改造のままにする。
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _do_save_as" pagefolio/file_ops.py` が `1`
    - `grep -c "def _do_save_compressed" pagefolio/file_ops.py` が `1`
    - `grep -c "functools.partial(self._do_save_" pagefolio/file_ops.py` が `3`（3 経路すべてが実保存層を束縛している・D-10）
    - `grep -c "PDF_ENCRYPT_KEEP" pagefolio/file_ops.py` が着手前と同数以上（暗号化指定を 1 箇所も落としていない）
    - `grep -c "retry_skips" tests/test_toast.py` が `3` 以上
    - `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` がフルスイートで失敗 0 件（`--ignore` 例外・`-k` 絞り込みを使わない）
    - `.\.venv\Scripts\python.exe -m pytest -q tests/test_password.py --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件
    - 振る舞い: `_save_as` / `_save_compressed` の retry_cb を呼ぶと `filedialog.asksaveasfilename` が呼ばれずに保存が再実行される
    - 振る舞い: 初回の `_save_as()` / `_save_compressed()` では保存先ピッカーが表示され、キャンセルで保存が実行されない
    - `ruff check . && ruff format --check .` が通る
    - `git diff --stat -- pagefolio/ui_builder.py pagefolio/toast.py` の出力が空
  </acceptance_criteria>
  <done>保存 3 経路すべてが同一の分離形を持ち、いずれの経路でも再試行が確認・保存先選択を経由しないことが自動テストで固定された。フルスイートが失敗 0 件で完走する。</done>
</task>

<task type="auto">
  <name>Task 3: D-12 の文言訂正（REQUIREMENTS.md / ROADMAP.md）</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`（60 行目付近の V190-QA-02 の行）
    - `.planning/ROADMAP.md`（Phase 3 セクションの Success Criteria #2）
    - `.planning/phases/03-qa-release-gate/03-CONTEXT.md`（D-09 / D-12 の訂正根拠と訂正後文言）
  </read_first>
  <action>
D-12 に従い、実装と判定基準が正反対になる状態を解消する。下流の検証エージェントは ROADMAP.md の
Success Criteria を基準に合否を判定するため、両方を訂正しないと本プランの成果が「不合格」と
判定されてしまう。

`.planning/REQUIREMENTS.md` の V190-QA-02 の行を、訂正後の文言へ置き換える:
`保存トーストの再試行を実行した際、上書き確認・保存先選択を再表示せず、前回確定した対象へ黙って再保存する（IN-01・v1.8.0 Phase 6 持ち越し / D-09・D-12 により文言訂正）`
チェックボックス（`- [ ] **V190-QA-02**:`）の形式・要件 ID の位置は既存フォーマットのまま維持する。

`.planning/ROADMAP.md` の Phase 3 Success Criteria #2 が、同じ趣旨の訂正文言
（`保存トーストの再試行を実行すると、上書き確認・保存先選択が再表示されず、前回確定した対象へ黙って再保存される（V190-QA-02・D-09/D-12 により文言訂正）`）
になっていることを確認する。計画時に既に訂正済みであれば内容を確認するだけでよく、二重に書き換えない。
未訂正または文言が異なる場合はこの task で訂正する。

いずれのファイルも `Edit` による範囲限定の置換で行い、ファイル全体の書き換え（`Write`）は行わない。
他の要件行・他フェーズの記述には一切触れないこと。
  </action>
  <verify>
    <automated>grep -c "前回確定した対象へ黙って再保存" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "V190-QA-02" .planning/REQUIREMENTS.md` の行に `再表示せず` が含まれる
    - `grep -c "前回確定した対象へ黙って再保存" .planning/REQUIREMENTS.md` が `1`
    - `grep -c "前回確定した対象へ黙って再保存" .planning/ROADMAP.md` が `1`
    - `grep -c "V190-QA-02" .planning/REQUIREMENTS.md` が着手前と同数（要件行を増減させていない）
    - `git diff --stat -- .planning/REQUIREMENTS.md` の変更行数が 2 行以内（1 行の置換）
  </acceptance_criteria>
  <done>REQUIREMENTS.md と ROADMAP.md の双方が、再試行時に確認・保存先選択を再表示しない挙動を記述しており、実装と判定基準が一致している（D-12）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー操作（上書き確認 / 保存先ピッカー）→ ファイルシステム書き込み | ユーザーの同意が「どのパスへ書くか」を決める唯一のゲート。再試行はこのゲートを迂回するため、確定パスの束縛が信頼境界そのものになる |
| アプリ内部状態（`self.doc` / `self.filepath`）→ 実保存層 | トーストは非モーダルで、表示中にファイルを閉じる・別ファイルを開く操作が可能。実保存層は自身が受け取った引数のみを信頼する |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Tampering | `pagefolio/file_ops.py` `_do_save_as` / `_do_save_compressed` | high | mitigate | 実保存層は `encryption=fitz.PDF_ENCRYPT_KEEP`（および `_save_compressed` の `save_kwargs` に含まれる `encryption`）を保持したまま受け渡す。`tests/test_password.py` の暗号化維持テスト全件 green を Task 1 / Task 2 双方の完了条件に含める（Phase 1 V190-SAFE-01 の退行防止） |
| T-03-01-02 | Tampering | `retry_cb` に束縛された確定パス | high | mitigate | `functools.partial` で確認・選択時のパスを束縛し、以降の `self.filepath` 変化から独立させる。加えて `_do_save_*` の先頭で `self.doc` の存在を確認し、falsy なら書き込みを一切行わずステータス通知のみで return する（ファイルクローズ後に古い再試行が発火しても書き込みが起きない） |
| T-03-01-03 | Elevation of Privilege | 上書き確認のスキップ範囲 | medium | mitigate | 確認スキップは `retry_cb` 経由の再実行にのみ適用する。`_save_file` / `_save_as` / `_save_compressed` の公開エントリは確認・パス選択層を保持し、初回保存では従来どおりユーザーの明示的同意を取る。振る舞いテストで「初回は確認が出る」ことを固定する |
| T-03-01-04 | Denial of Service | 再試行の無限ループ | low | accept | 再試行はユーザーのボタン押下ごとに 1 回だけ実行され、自動再試行ループは実装しない。失敗し続けても同じトーストが更新されるだけで、リソース枯渇に至らない |
| T-03-01-SC | Tampering | npm/pip/cargo installs | low | accept | 本プランは新規パッケージのインストールを一切行わない（`functools` は Python 標準ライブラリ・V14-D-01 の新規 pip 依存ゼロ方針を維持）。`03-RESEARCH.md` の Package Legitimacy Audit も「該当なし」と結論している |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

本プランが新規に作成するシンボル・ファイル（下流のドリフト検証で「既存からの逸脱」と誤判定しないための一覧）:

**新規関数（`pagefolio/file_ops.py`）**
- `_do_save_file(self, path)` — 上書き保存の実保存層（確認ダイアログを含まない）
- `_do_save_as(self, path)` — 名前を付けて保存の実保存層（保存先ピッカーを含まない）
- `_do_save_compressed(self, path, save_kwargs)` — 縮小保存の実保存層（保存先ピッカーを含まない）

**新規 import（`pagefolio/file_ops.py`）**
- `import functools` — `functools.partial` による確定パスの束縛にのみ使用

**新規テスト（`tests/test_toast.py` / 既存クラス `TestSaveFilePathsUseSharedHelper` 内）**
- `retry_skips` を名前に含む 3 件以上のテスト（保存 3 経路ぶん）

**新規ファイル:** なし（既存ファイルの改修のみ）

**変更されるドキュメント行:** `.planning/REQUIREMENTS.md` の V190-QA-02、`.planning/ROADMAP.md` の Phase 3 Success Criteria #2
</artifacts_this_phase_produces>

<verification>
- `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` がフルスイートで失敗 0 件（`--ignore` 例外・相対件数ゲートを使わない）
- `ruff check . && ruff format --check .` が通る
- `python -c "import ast; ast.parse(open('pagefolio.py', encoding='utf-8').read())"` が成功する
- `git diff --stat -- pagefolio/ui_builder.py pagefolio/toast.py` の出力が空（D-11 の「ToastManager 無改造」契約）
- `grep -c "functools.partial(self._do_save_" pagefolio/file_ops.py` が `3`
</verification>

<success_criteria>
- 保存 3 経路すべてで、トーストの「再試行」が上書き確認ダイアログ／保存先ピッカーを再表示せず、前回確定した対象へ再保存する
- 初回保存では従来どおり確認ダイアログ／保存先ピッカーが表示される
- 暗号化 PDF の保存で暗号化が維持される（`tests/test_password.py` 全件 green）
- `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` の V190-QA-02 関連文言が実装と一致している
- フルテストスイートが失敗 0 件で完走する
</success_criteria>

<output>
Create `.planning/phases/03-qa-release-gate/03-01-SUMMARY.md` when done
</output>
