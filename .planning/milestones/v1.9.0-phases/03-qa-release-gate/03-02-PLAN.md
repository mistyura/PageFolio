---
phase: 03-qa-release-gate
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md
  - CLAUDE.md
  - tests/conftest.py
autonomous: true
requirements: [V190-QA-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "03-01 適用後の HEAD に対しフルテストスイートを 10 回連続実行し、各回のコマンド・passed/failed/error 件数・クラッシュ有無が一次データとして `03-TEST-ENV-INVESTIGATION.md` に記録されている（D-08）"
    - "STATUS_BREAKPOINT クラッシュと `TclError` によるセットアップ ERROR が、同じ調査プランの中で並行して切り分けられつつ、別症状として別々に結論づけられ記録されている（D-04）"
    - "再現しなかった場合、`pagefolio/` にも `tests/` にもコード変更を入れずに「現行環境では解消済み」と一次データ付きで記録して閉じられている（D-05）"
    - "再現した場合、D-03 の 2 仮説（pytest assertion rewriting / `tests/test_pdf_ops.py` の二分探索）それぞれについて実行コマンド・結果・結論が記録され、2 本とも外れたら分割実行の受容へ確定している（D-02 / D-03）"
    - "リリースゲートの合格条件（単一プロセス完走 か、定義された分割実行手順）が `CLAUDE.md` から実行可能な形で読める（D-07）"
    - "分割実行手順を記録する場合、その各コマンドの実測件数の合計が単一プロセスでの収集件数と一致することが、当セッションで実行した `--collect-only` の出力で示されている"
    - "別マシン / CI / 別ユーザーでの確認は本プロジェクトに CI も別環境も存在しないため対象外であることが、理由付きで記録されている（D-08）"
  prohibitions:
    - "リリースゲートを、テストの削除・skip マーク付与・`-k` や `--ignore` による静かな除外で緑にしてはならない。除外を伴う運用を採る場合は、除外対象と件数と根拠を必ず明示し、合計が全件と一致することを示さなければならない"
    - "再現しなかったことを「解消」と報告する際に、実行回数・実行コマンド・出力の一次データを残さず結論だけを書いてはならない。反証データを残さない結論は、次マイルストーンに同じ調査をやり直させる"
  artifacts:
    - path: ".planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md"
      provides: "2 症状の切り分け実験ログ・反証データ・結論（D-07 の 2 系統記録のうち「詳細」側）"
      min_lines: 60
    - path: "CLAUDE.md"
      provides: "日常の実行手順としてのリリースゲート合格条件（D-07 の 2 系統記録のうち「実行者が必ず見る」側）"
      contains: "リリースゲート"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md"
      via: "チェックリストからの根拠参照リンク（なぜこの合格条件なのかの出典）"
      pattern: "03-TEST-ENV-INVESTIGATION"
  flagged_assumptions:
    - id: "EDGE-QA01-UNCLASSIFIED"
      source: "spec-less edge probe（V190-QA-01）"
      status: "unresolved"
      statement: "V190-QA-01 のエッジ分類が `unclassified` のままであり、自動分類器は検証すべき境界条件を提示できなかった。V190-QA-01 は『環境の切り分けと修復』という調査要件で、入力→出力の形を持たないため既存のエッジ分類（idempotency / concurrency / adjacency / empty / ordering）のいずれにも当てはまらない。本プランは代替として D-08 の「複数回連続実行」を統計的な再現性判定の代わりに置いているが、これは probe が求める形式的なエッジ検証ではない。次マイルストーンでこの要件を再訪する際は、ゲート条件そのものに対するエッジ（例: 実行順序の入れ替え・並行実行・部分実行）を改めて検討すること。**自動 dismiss はしていない — 未解決のまま繰り越す。**"
---

<objective>
Python 3.14.6 環境で単一プロセス `pytest -q` を実行したときの 2 症状
（① `TclError` によるセットアップ ERROR のフレーキー、② `tests/test_ocr_pipeline.py` 実行中の
`STATUS_BREAKPOINT` プロセスクラッシュ）を実機で切り分け、その結果に応じた修復または
「解消済み」の記録を行い、リリースゲートの合格条件を文書として確定させる（V190-QA-01）。

**「調べる前に直さない」がこのプランを貫く判断基準。** D-05（切り分け結果駆動）・D-06（修復は
`tests/` 配下に閉じる）・D-03（既知の切り分け結果の続きから入る）はいずれも「症状に対して
予防的にコードを足す」ことを退けている。Task 1 は必ず再現試行から始め、再現しなければ
コード変更ゼロで閉じる。

**重要な前提の変化（03-RESEARCH.md）:** STATE.md が記録する「HEAD 内容で約 10 回中 7 回
クラッシュ」は 01-07 実行時点（Phase 1 途中・1184 件）のデータであり、Phase 1 完了・Phase 2 完了を
経た現在（1387 件）ではリサーチセッションの 7 回連続実行で 7/7 グリーンだった。ただし
Assumptions Log A1 が明記するとおり **その 7 回だけで「解消済み」と断定してはならない**ため、
本プランで独立に 10 回連続実行し、リサーチの 7 回と合算して証拠とする。

**tracer について:** 本フェーズの tracer は 03-01 Task 1（保存経路の縦スライス）が担っている。
本プランはコードのレイヤスタックを縦に貫く対象を持たない調査・文書化プランであり、
「one shape per phase」に従って追加の tracer タスクは置かない。

Purpose: 全テスト完走をリリースの前提条件（ゲート）として扱えるようにする。原因未解明の
ネイティブ層調査で出荷判定をブロックさせないため、D-02 の打ち切り契約（仮説 2 本）を守る。

Output:
- `.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md`（新規・実験ログと反証データ）
- `CLAUDE.md`（リリースゲートの合格条件と実行手順）
- `tests/conftest.py`（**再現した場合のみ**・D-05/D-06）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-qa-release-gate/03-CONTEXT.md
@.planning/phases/03-qa-release-gate/03-RESEARCH.md
@.planning/phases/03-qa-release-gate/03-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<!-- planner-discipline-allow: TCL_LIBRARY -->
<!-- planner-discipline-allow: TK_LIBRARY -->

<tasks>

<task type="auto">
  <name>Task 1: 現行 HEAD での再現試行（フルスイート 10 回連続実行）と一次データの記録</name>
  <precondition>`.\.venv\Scripts\python.exe --version` が `Python 3.14.6` を出力し、`.\.venv\Scripts\python.exe -m pytest --collect-only -q` が収集エラー 0 件で完了する</precondition>
  <files>.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md</files>
  <read_first>
    - `.planning/STATE.md`（「Blockers/Concerns」セクション — **2 症状の一次情報がすべてここにある。着手前に必読**）
    - `.planning/phases/03-qa-release-gate/03-RESEARCH.md`（Pitfall 1 の検証データ・Assumptions Log A1/A2・Validation Architecture）
    - `.planning/phases/03-qa-release-gate/03-CONTEXT.md`（D-01〜D-08 と Deferred Ideas の `--basetemp` 運用回避）
    - `.planning/research/PITFALLS.md`（Pitfall 13 の 506-547 行 — 環境修復が「テスト環境だけ」の問題ではない理由）
  </read_first>
  <action>
まず現行 HEAD（03-01 適用後）の収集件数を実測する:
`.\.venv\Scripts\python.exe -m pytest --collect-only -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"`
出力末尾の収集件数を控える。この実測値だけを以降で使い、リサーチ時点の 1387 という数値を
そのまま書き写さないこと（03-01 でテストが増減している可能性がある）。

次に、単一プロセスのフルスイートを **10 回連続**実行する:
`.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"`
回数の根拠は D-08（フレーキーなので 1 回グリーンは証拠にならない）と既知の再現率
7/10 である。再現率 0.7 の事象が 10 回連続で一度も出ない確率は 0.3^10 ≒ 6e-6 であり、
リサーチセッションの 7 回（0.3^7 ≒ 2e-4）と合算すれば「現行 HEAD では成立しない」と
言い切れる水準になる。各回ごとに以下を記録する:
  - 実行番号 / 実行コマンド全文
  - `passed` / `failed` / `error` の件数（`-q` の末尾サマリ行をそのまま）
  - プロセスクラッシュの有無（`Windows fatal exception: code 0x80000003` の出現有無）
  - `_tkinter.TclError` を含む ERROR の有無と、あれば ERROR になったテスト ID

`%TEMP%\pytest-of-shdwf` に対する `PermissionError` が併発した場合は、CONTEXT.md の
Deferred Ideas に従い `--basetemp` 指定での運用回避で通し、根本対応は行わない（発生した
事実だけを記録する）。

`.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` を新規作成し、以下の
構成で記録する（`.planning/STATE.md`「Blockers/Concerns」の記述形式＝症状・検証手法・
反証データを踏襲する）:
  - `## 対象環境`（Python / Tcl-Tk / pytest / PyMuPDF の実測バージョン。すべてこのセッションで
    実行して得た値のみを書く）
  - `## 症状 ①: TclError によるセットアップ ERROR` — STATE.md の初出記述の要約と、今回の観測結果
  - `## 症状 ②: STATUS_BREAKPOINT クラッシュ` — 同上
  - `## 再現試行ログ` — 10 行の表（実行番号 / コマンド / passed / failed / error / クラッシュ /
    TclError）
  - `## 現時点の判定` — 「再現した」または「再現しなかった」のいずれかを明記し、Task 2 の
    どちらの分岐へ進むかを書く
D-04 に従い、2 症状は同じ実験（1 回のフルスイート実行）で並行観測するが、**節を分けて別々に
記録する**。片方の非再現をもう片方の非再現と読み替えないこと。

このタスクではコード（`pagefolio/` / `tests/`）に一切変更を入れない。
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` が真
    - `grep -c "^## 再現試行ログ" .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` が `1`
    - 再現試行ログの表に 10 行のデータ行がある（表ヘッダ・区切り行を除く）
    - `grep -c "^## 症状 ①" .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` が `1` かつ `grep -c "^## 症状 ②" ...` が `1`（D-04 の別症状分離）
    - `grep -c "^## 現時点の判定" .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` が `1`
    - `git diff --stat -- pagefolio/ tests/` の出力が空（このタスクではコード変更ゼロ）
    - 実測した収集件数がレポート本文に書かれており、その数値が `pytest --collect-only -q` の当セッション実行結果と一致する
  </acceptance_criteria>
  <done>現行 HEAD に対する 10 回連続実行の一次データが記録され、2 症状それぞれについて「再現した / しなかった」の判定が根拠付きで確定している。</done>
</task>

<task type="auto">
  <name>Task 2: 判定に応じた切り分け（D-03 の 2 仮説検証）または「解消済み」の確定（D-05）</name>
  <files>.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md, tests/conftest.py</files>
  <read_first>
    - `.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md`（Task 1 の判定結果）
    - `.planning/STATE.md`（「Blockers/Concerns」— `--deselect` で再現し `--ignore` で非再現＝import 時点で成立、モジュールサイズ模倣では非再現、孤児 daemon スレッド仮説は棄却済み）
    - `.planning/phases/03-qa-release-gate/03-RESEARCH.md`（Architecture Patterns Pattern 2 の検証コマンド・Open Questions #1 のタイムボックス運用）
    - `tests/conftest.py`（修復を入れる場合の唯一の置き場・既存 fixture の命名規則）
    - `tests/test_pdf_ops.py`（二分探索の対象。クラス構成を把握する）
  </read_first>
  <action>
Task 1 の判定によって分岐する。**どちらの分岐でも、結論と反証データを
`03-TEST-ENV-INVESTIGATION.md` へ追記する**（D-02 の「次マイルストーンに同じ地面を掛け直させない」
という契約はここで果たされる）。

**分岐 A — Task 1 で 2 症状のいずれも再現しなかった場合（D-05）:**
コード変更を一切入れずに閉じる。`03-TEST-ENV-INVESTIGATION.md` へ以下の節を追記する:
  - `## 結論 ①`（TclError 症状）/ `## 結論 ②`（STATUS_BREAKPOINT 症状）— それぞれ別々に
    「現行環境では再現しない」と、根拠（Task 1 の 10 回 + 03-RESEARCH.md の 7 回 = 累計 17 回
    連続グリーン）を書く
  - `## 未解明のまま残るもの` — 根本原因（ネイティブ層）は依然未解明であり、非再現の因果は
    未証明（03-RESEARCH.md Assumptions Log A2 の「Phase 2 で約 200 件テストが増えて収集順序 /
    メモリレイアウトが変わったため」は相関のみの推測）であることを明記する。次マイルストーンで
    再発した場合の入口として D-03 の 2 仮説コマンドをそのまま残す
  - `## 検証しなかった仮説と理由` — D-03 の 2 仮説は「再現していないため検証不能」として記録
    （検証していないものを「検証して外れた」と書かない）
  - `## 対象外とした確認項目` — 別マシン / CI / 別ユーザーでの再現確認は、本プロジェクトに
    CI も別環境も存在しないため対象外（D-08）。`.planning/research/PITFALLS.md` のチェック項目の
    うちこれに該当する行を引用して、対象外の理由を明示する

**分岐 B — Task 1 でいずれかの症状が再現した場合（D-02 / D-03）:**
再現した症状に対してのみ、D-03 で固定された 2 仮説を **この順で 1 本ずつ**検証する。
1 仮説 = 「コマンド実行 → 結果記録」の 1 サイクルをタイムボックスの単位とし、
2 本を消化したら結果にかかわらず追求を打ち切る（D-02）。
  - 仮説 1（pytest assertion rewriting）:
    `.\.venv\Scripts\python.exe -m pytest -q --assert=plain -p no:cacheprovider --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"`
    を、Task 1 と同じ観測項目で複数回実行する。再現するかどうかで白黒がつく。
  - 仮説 2（二分探索）: `tests/test_pdf_ops.py` を **クラス単位で半分ずつ** `--deselect` して
    実行し、どちらの半分が引き金かを絞り込む。既知の絞り込み結果（モジュールサイズ模倣・
    `import ast` / `pathlib` の追加だけでは非再現）を出発点とし、既に否定された方向へ戻らない。
    各段階で「対象クラス集合 / コマンド / 試行回数 / 再現有無」を記録する。
  2 本とも外れた場合は分割実行の受容へ確定させ、`## 結論` に「根本原因は未特定・分割実行を
  リリースゲートとして受容」と書く（D-01）。

**分岐 B で修復コードが必要になった場合（D-06）:**
修復は `tests/` 配下に閉じることを原則とする。`tests/conftest.py` に入れる場合は、既存 fixture と
同じ `@pytest.fixture()` デコレータ・`snake_case` 命名・docstring 冒頭一文要約の作法に揃える。
`TCL_LIBRARY` / `TK_LIBRARY` といった Tcl/Tk ライブラリパス環境変数の予防的なハードコードは
**入れない**（D-05 で不採用が確定済み。PITFALLS.md Pitfall 13 が、開発環境のテストは直るが
PyInstaller 配布 EXE の Tcl/Tk 探索ロジックと衝突して逆に壊れる機序を名指しで警告している）。
`pytest-forked` / `pytest-xdist` 等の新規 pip 依存も導入しない（V14-D-01・CONTEXT.md Deferred）。

**製品コード（`pagefolio/`）への変更が必要と判断した場合は、実装せずに停止する**（D-06）。
`checkpoint:human-verify` 形式で「なぜ製品コード変更が必要か」「実際にユーザーが遭遇し得る
不具合であることの根拠」を提示し、ユーザーの判断を仰いでから進むこと。A/B 検証で製品コード
無実は確認済みであり、リリース直前のフェーズで製品コードを広く触るのは回帰リスクが高い。

分岐 A / B のいずれでも、最後に `.\.venv\Scripts\python.exe -m pytest -q --basetemp=...` を
もう 1 回走らせ、追記・修復後も失敗 0 件であることを確認する。
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 結論" .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` が `2` 以上（2 症状ぶんの別々の結論・D-04）
    - `grep -c "^## 対象外とした確認項目" .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` が `1`（CI / 別環境の受容記録・D-08）
    - `grep -rl "TCL_LIBRARY" tests/ pagefolio/ | wc -l` が `0`（予防的ハードコードを入れていない・D-05）
    - `grep -rl "TK_LIBRARY" tests/ pagefolio/ | wc -l` が `0`
    - `git diff --stat -- pagefolio/` の出力が空（D-06 の「製品コードへは触れない」原則。触れる必要が生じた場合は実装せず checkpoint で停止していること）
    - `grep -c "pytest-xdist\|pytest-forked" requirements.txt` が `0`（新規 pip 依存を足していない・V14-D-01）
    - `git diff --stat -- requirements.txt pyproject.toml` の出力が空
    - 分岐 B を通った場合のみ: `grep -c "assert=plain" .planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` が `1` 以上、かつ二分探索の各段階（対象クラス集合 / コマンド / 試行回数 / 再現有無）が表で記録されている
    - `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件
  </acceptance_criteria>
  <done>2 症状それぞれについて結論と反証データが記録され、再現した症状には D-03 の 2 仮説が消化されている（または非再現のためコード変更ゼロで閉じられている）。追求の打ち切りが D-02 の契約どおりに行われた。</done>
</task>

<task type="auto">
  <name>Task 3: リリースゲートの合格条件と実行手順を CLAUDE.md へ記録（D-07）</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md`（104-111 行付近の「## 変更時のチェックリスト」— 追記先。既存 5 項目の書式に揃える）
    - `.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md`（Task 1/2 の判定結果と実測件数 — ここに書く数値の唯一の出典）
    - `.planning/phases/03-qa-release-gate/03-CONTEXT.md`（D-01 の合格条件・D-07 の記録先分割）
  </read_first>
  <action>
D-07 に従い、**日常の実行手順**（実行者が必ず見る場所）を `CLAUDE.md` へ記録する。
実験ログと反証データは `03-TEST-ENV-INVESTIGATION.md` 側にあるので重複させない。

`CLAUDE.md` の「## 変更時のチェックリスト」の直下に、`## リリースゲート（全テスト完走条件）`
という独立セクションを新設する（チェックリスト自体は 5 項目のまま増やさず、`pytest` 項目の
記述を「詳細は下記リリースゲート節」を指すよう最小限だけ調整する）。新設セクションの内容:

  - **合格条件** — Task 1/2 の判定に応じてどちらか一方だけを書く:
    - 単一プロセスで完走した場合:
      `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"`
      が失敗 0 件・ERROR 0 件・プロセスクラッシュなしで完走すること
    - 分割実行を受容した場合（D-01）: 2 本のコマンドと**それぞれの実測件数**、および
      両者の合計が単一プロセス収集件数と一致すること。件数は Task 1 で実測した値のみを
      書き、リサーチ時点の値（1370 / 17 / 1387）を書き写さない
  - **`--basetemp` について** — `%TEMP%\pytest-of-shdwf` のロック競合回避専用であり、テストを
    1 件も除外しないこと。実行環境でロック競合が起きないなら省略してよいこと
  - **やってはいけないこと** — テストの削除・skip マーク付与・`-k` / `--ignore` による静かな
    除外でゲートを通すこと。除外を伴う運用を採る場合は対象と件数と根拠を必ず明示し、
    合計が全件と一致することを示すこと
  - **根拠** — `.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` への相対リンク

日本語で記述し、既存 CLAUDE.md のトーン（見出しレベル・箇条書き・コードスパン）に揃える。
`Edit` による範囲限定の追記で行い、ファイル全体の書き換え（`Write`）は行わない。
CLAUDE.md の他セクション（コーディング規約・言語ルール・既知の制限）には触れないこと。
  </action>
  <verify>
    <automated>grep -c "リリースゲート" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## リリースゲート" CLAUDE.md` が `1`
    - `grep -c "03-TEST-ENV-INVESTIGATION" CLAUDE.md` が `1` 以上（根拠への参照リンク）
    - `grep -c "basetemp" CLAUDE.md` が `1` 以上
    - `grep -c "^## 変更時のチェックリスト" CLAUDE.md` が `1`（既存セクションを壊していない）
    - `grep -v '^\s*#' CLAUDE.md | grep -c "pyproject.toml"` が着手前と同数（禁止事項の記述を壊していない）
    - CLAUDE.md に書かれたゲートコマンドを実際に実行すると失敗 0 件で完走する
    - CLAUDE.md に書かれた件数が `03-TEST-ENV-INVESTIGATION.md` の実測値と一致する（数値の出典が 1 つ）
    - `git diff --stat -- CLAUDE.md` の変更が「変更時のチェックリスト」直下の追記に収まっている
  </acceptance_criteria>
  <done>リリースゲートの合格条件が CLAUDE.md から実行可能な形で読め、その根拠が調査レポートへリンクされている。数値はすべて当セッションの実測値に基づく。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| テスト実行環境（pytest プロセス / Tcl-Tk ネイティブ層）→ リリース判定 | 「全テスト完走」という主張が出荷可否を決める。ここで観測を歪めると、壊れたビルドがゲートを通る |
| 調査時の一時的な回避策（`--basetemp` / 除外フラグ）→ 恒久的な運用手順 | 実験中の便宜が、そのまま CLAUDE.md の日常手順へ昇格してしまう経路 |
| 開発環境の Tcl/Tk 探索 → PyInstaller 配布 EXE の Tcl/Tk 探索 | conftest.py での環境変数上書きは開発環境だけでなく凍結ビルドの探索ロジックへ波及しうる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Repudiation | リリースゲートの合格記録（`CLAUDE.md` / `03-TEST-ENV-INVESTIGATION.md`） | high | mitigate | ゲート判定は実行コマンド全文・実測件数・連続実行回数・出力サマリを一次データとしてレポートへ残す。CLAUDE.md に書く件数はレポートの実測値のみを出典とし、リサーチ時点の数値を書き写さない。除外フラグを使う運用を採る場合は対象と件数と根拠を明示し、合計が全件と一致することを示す |
| T-03-02-02 | Tampering | `tests/conftest.py` へ入れる修復コード | high | mitigate | 修復は切り分け結果駆動（D-05）で、再現しなければコード変更ゼロ。Tcl/Tk ライブラリパス環境変数の予防的ハードコードは入れない（PITFALLS.md Pitfall 13 — 配布 EXE の探索ロジックと衝突して逆に壊れる）。受け入れ基準で当該環境変数名がリポジトリ内に出現しないことを検証する |
| T-03-02-03 | Tampering | `pagefolio/` 製品コード | high | mitigate | D-06 により製品コード変更は原則対象外。必要と判断した場合は実装せず checkpoint で停止し、実際にユーザーが遭遇し得る不具合であることの根拠を提示して判断を仰ぐ。受け入れ基準で `git diff --stat -- pagefolio/` が空であることを検証する |
| T-03-02-04 | Denial of Service | pytest 一時ディレクトリ（`%TEMP%\pytest-of-shdwf`）のロック競合 | low | accept | `--basetemp` 指定での運用回避で通す。根本対応（Windows Defender 等のロック競合の解消）は CONTEXT.md の Deferred Ideas で明示的に範囲外とされている |
| T-03-02-05 | Tampering | 依存パッケージ構成（`requirements.txt` / `pyproject.toml`） | medium | mitigate | 調査目的でも `pytest-forked` / `pytest-xdist` 等を導入しない（V14-D-01 の新規 pip 依存ゼロ方針）。`pyproject.toml` の編集は CLAUDE.md の禁止事項。受け入れ基準で両ファイルの差分が空であることを検証する |
| T-03-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本プランは新規パッケージのインストールを一切行わない。`03-RESEARCH.md` の Package Legitimacy Audit は「該当なし」と結論している。導入が必要になった場合は明示的な決定として上げること |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**新規ファイル**
- `.planning/phases/03-qa-release-gate/03-TEST-ENV-INVESTIGATION.md` — テスト実行環境の切り分け実験ログと反証データ

**新規セクション（既存ファイル内）**
- `CLAUDE.md` の `## リリースゲート（全テスト完走条件）` — 合格条件・実行コマンド・件数・禁止事項・根拠リンク

**条件付きで新設される可能性のあるシンボル（分岐 B のみ）**
- `tests/conftest.py` 内の autouse fixture（名称は切り分け結果に依存するため実行時に確定。既存 fixture の `@pytest.fixture()` / `snake_case` / docstring 冒頭一文要約の作法に揃える）

**変更されないもの（契約として明示）**
- `pagefolio/` 配下の製品コード（D-06）
- `requirements.txt` / `pyproject.toml`
</artifacts_this_phase_produces>

<verification>
- `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件で完走する
- `ruff check . && ruff format --check .` が通る（`tests/conftest.py` へ変更を入れた場合）
- `03-TEST-ENV-INVESTIGATION.md` に 10 行の再現試行ログ・2 症状ぶんの別々の結論・対象外とした確認項目が存在する
- `CLAUDE.md` の `## リリースゲート` 節に書かれたコマンドを実行すると失敗 0 件で完走する
- `git diff --stat -- pagefolio/ requirements.txt pyproject.toml` の出力が空
</verification>

<success_criteria>
- Python 3.14.6 環境での 2 症状が切り分けられ、再現有無に応じた対応（修復 または 「解消済み」の根拠付き記録）が完了している
- 検証した仮説と反証データがフェーズ成果物に残り、次マイルストーンが同じ地面を掛け直さずに済む
- リリースゲートの合格条件が CLAUDE.md から実行可能な形で読める
- GUI テストを含む全テストスイートが、記録された合格条件のもとで完走する
</success_criteria>

<output>
Create `.planning/phases/03-qa-release-gate/03-02-SUMMARY.md` when done
</output>
