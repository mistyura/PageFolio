---
phase: 03-qa-release-gate
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md
autonomous: false
requirements: [V190-QA-03]

user_setup:
  - service: gemini
    why: "遡及 UAT のうち『プロバイダ別プロンプトで実 API の OCR 出力品質が引き出される』（v1.6.0 Phase 4 項目 2）の Gemini 分を実施するため"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio → API keys。未設定の場合は D-14 に従い当該項目を「未実施・理由付き」として記録し、リリース判定はブロックしない"
  - service: lmstudio
    why: "遡及 UAT のうち LM Studio のモデル切替反映・タイムアウト表示一致（v1.4.0 Phase 04 の human_needed 2 件）を実施するため"
    dashboard_config:
      - task: "LM Studio をローカルで起動し、モデルを 2 つ以上ロード可能にしてサーバを有効化する（送信された model フィールドをログで確認できる状態にする）"
        location: "LM Studio アプリ → Local Server"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "遡及 UAT の候補項目が現行コードと照合され、「活き残り」「除外（理由付き）」「未実施（理由付き）」へ仕分けられてから対象が確定している（D-13）"
    - "`03-UAT-RESULTS.md` に全対象項目が「項目・出典・手順・実施日・結果・根拠」の列で記録されている（D-15）"
    - "実 API・課金が必要でキーが用意できない項目は pass ではなく「未実施（理由）」として記録され、リリース判定をブロックしていない（D-14）"
    - "実機目視を伴う項目は、複数の human-verify チェックポイントへグループ分けして実施され、各グループの結果がそのまま記録に反映されている（D-15）"
    - "遡及項目と v1.9.0 項目が同一の現行挙動を指す場合は 1 行へ統合され、統合元の出典（マイルストーン・元の項番）が両方併記されている（重複計上しない）"
    - "現行照合の結果、活き残りが 0 件になった候補カテゴリは、表から黙って消さず「対象なし（理由）」の明示行として残っている"
    - "`03-UAT-RESULTS.md` の項目は出典マイルストーン昇順 → 同一マイルストーン内は元の項番昇順で並び、実施順序が変わっても表の並びは変わらない"
    - "v1.4.0 Phase 04 の LM Studio 関連項目は、v1.8.0 Phase 3 の OCRRunEngine 抽出後の現在地を実コードで再特定したうえで手順が書かれている（03-RESEARCH.md Assumptions Log A3）"
  prohibitions:
    - "実機で実施していない項目を pass として記録してはならない。コードを読んだだけの確認を「実機目視で確認した」と書くことは、v1.4.0 以降 3 マイルストーン続いた「一旦 pass」の再演であり、この要件が存在する理由そのものを無効化する"
    - "実施できない項目（実 API キーがない・課金が発生する）を pass へ丸めてはならない。「未実施」であることと、その理由を、記録に残さなければならない"
    - "遡及項目の文言を、現行実装に合わせて通りやすい形へ書き換えてはならない。仕様が変わって項目自体が意味を失ったなら、書き換えるのではなく「除外」と理由を明記する"
  artifacts:
    - path: ".planning/phases/03-qa-release-gate/03-UAT-RESULTS.md"
      provides: "遡及分と v1.9.0 分を一覧で追える UAT 実施記録（項目・出典・手順・実施日・結果・根拠）"
      min_lines: 50
  key_links:
    - from: ".planning/phases/03-qa-release-gate/03-UAT-RESULTS.md"
      to: ".planning/milestones/v1.7.1-phases/04-ui-ux/04-UAT.md"
      via: "遡及項目の出典参照（どのマイルストーンのどの項番を消化したか）"
      pattern: "v1\\.7\\.1"
  canon_referral_breadcrumbs:
    - "UAT 記録への API キー・個人情報の混入防止は canon（機密情報の取扱い）に属するため、prohibition としては mint せず threat register の T-03-03-01 として扱う"
---

<objective>
v1.4.0 / v1.6.0 / v1.7.1 で「一旦 pass」あるいは `human_needed` のまま残されてきた実機目視項目の
うち、**今も現行機能に未検証のまま生きているものだけ**を対象化して正式に消化し、v1.9.0 分と
あわせて `03-UAT-RESULTS.md` へ記録する（V190-QA-03 / D-13〜D-15）。

**「活き残り」を確定してから対象化する（D-13）。** v1.8.0 Phase 1 の肥大モジュール分割・
Phase 3 の OCRRunEngine 抽出でコード配置と実装詳細が変わっている項目があるため、候補を
そのまま転記すると「現行コードに存在しない機能」を検証しようとすることになる。Task 1 で
現行コードとの照合を行い、除外・未実施を理由付きで仕分けてから実機確認へ進む。

**タスク数について:** 本プランは 5 タスクだが、うち 3 つは `checkpoint:human-verify` で
実行エージェントのコンテキストをほとんど消費しない（自動タスクは 2 つ）。3 分割は D-15
（Phase 2 と同型のチェックリスト分割）と、Phase 2 レビュー MEDIUM-14（1 つの巨大な
human-verify では失敗時の原因切り分けが難しい）への対応であり、コンテキスト予算上の
分割ではない。

**tracer について:** 本フェーズの tracer は 03-01 Task 1（保存経路の縦スライス）が担っている。
本プランはコードのレイヤスタックを持たない実機検証・記録プランであり、「one shape per phase」に
従って追加の tracer タスクは置かない。

Purpose: v1.4.0 から 3 マイルストーン続いた「実機目視をスキップして一旦 pass」の運用を
正式に清算し、v1.9.0 のリリース判定を実測に基づいて下せるようにする。

Output: `.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md`（新規）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-qa-release-gate/03-CONTEXT.md
@.planning/phases/03-qa-release-gate/03-RESEARCH.md
@.planning/milestones/v1.7.1-phases/04-ui-ux/04-UAT.md
@.planning/milestones/v1.6.0-phases/04-ai-c/04-VERIFICATION.md
@.planning/milestones/v1.4.0-phases/04-provider-abstraction/04-VERIFICATION.md
@.planning/phases/01-safety-rollback/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 遡及 UAT 候補の現行照合と対象確定・`03-UAT-RESULTS.md` の骨組み作成</name>
  <files>.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md</files>
  <read_first>
    - `.planning/milestones/v1.7.1-phases/04-ui-ux/04-UAT.md`（7 件・すべて `reason: ユーザー判断で一旦パス`）
    - `.planning/milestones/v1.6.0-phases/04-ai-c/04-VERIFICATION.md`（frontmatter の `human_verification` 2 件）
    - `.planning/milestones/v1.4.0-phases/04-provider-abstraction/04-VERIFICATION.md`（frontmatter の `human_verification` 2 件）
    - `.planning/milestones/v1.6.0-REQUIREMENTS.md`（V16-QUAL-03 の定義 — max_tokens クランプと 429 リトライの実 API 検証）
    - `.planning/phases/01-safety-rollback/01-CONTEXT.md`（D-13 — Undo 復元失敗時の messagebox ブロック通知。v1.9.0 分の UAT 対象）
    - `pagefolio/dialogs/shortcuts.py` / `pagefolio/dialogs/settings.py`（75/155/172 行のセクション区切り）/ `pagefolio/dialogs/llm_config/sections.py` / `pagefolio/viewer.py`（拡大ポップアップの文言参照）
    - `pagefolio/ocr_engine.py` と `pagefolio/ocr_dialog.py`（`_apply_llm_settings` は 1031 行付近、`_on_run` は 1392 行付近、`build_provider` 呼び出しは 1062-1170 行付近。v1.4.0 の LM Studio 項目の現在地を再特定する対象）
    - `pagefolio/md_render.py` と `pagefolio/ocr_dialog.py`（md_* タグ定義と `_insert_markdown`。v1.6.0 Phase 4 項目 1 の現在地）
  </read_first>
  <action>
候補 14 項目を現行コードと照合し、「活き残り（実施対象）」「除外（理由付き）」「未実施（理由付き）」へ
仕分ける（D-13）。候補は以下:

  - v1.4.0 Phase 04（`human_needed`）: ①LM Studio でダイアログのモデル変更が実際に送信される
    `model` フィールドへ反映される ②タイムアウト変更が表示メッセージの秒数と実際の待機時間に一致する
  - v1.6.0 Phase 3（V16-QUAL-03）: ③max_tokens クランプと 429 リトライの実 API 検証
  - v1.6.0 Phase 4（`human_needed`）: ④OCRDialog の markdown 整形表示（md_h1 / md_h2 / md_bullet /
    md_code / md_bold）の見え方と読みやすさ ⑤Claude / Gemini 実 API でのプロバイダ別プロンプト出力品質
  - v1.7.1 Phase 4（7 件・全件「一旦 pass」）: ⑥ShortcutsDialog の実キーキャプチャ（修飾キー単体では
    確定せず待機継続） ⑦同一キー重複割り当て時のエラーダイアログと保存拒否 ⑧保存直後の即時反映
    ⑨SettingsDialog の 3 セクション表示（外観 / 操作 / AI・OCR・⚙ アイコン） ⑩LLMConfigDialog の
    「選択中プロバイダ固有の設定」/「全プロバイダ共通の設定」見出し順序とプロバイダ切替
    ⑪外側 SettingsDialog を Cancel しても LLM 設定の変更が保持される ⑫拡大ポップアップの
    `lang='en'` 表示（Page N / M・Zoom Out・Zoom In・Close）
  - v1.9.0 分: ⑬保存トーストの再試行が上書き確認・保存先ピッカーを再表示しない（03-01 の成果・
    V190-QA-02） ⑭Undo/Redo の復元に失敗したときに messagebox でブロック通知される（Phase 1 D-13）

照合時の判断:
  - **除外にするのは「仕様が変わって項目自体が意味を失った」場合のみ。** 項目の文言を現行実装に
    合わせて通りやすい形へ書き換えることはしない。除外する場合は該当コードの現在地（または
    不在の根拠）を挙げる。
  - **項目 ①② は v1.8.0 Phase 3 の OCRRunEngine 抽出により配置が変わっている。** 元の
    `ocr_dialog.py:_on_run`（旧行番号）という参照は現在では無効なので、`ocr_engine.py` /
    `ocr_dialog.py` を実際に読んで、モデル値・タイムアウト値が provider 生成へ流れる現在の
    経路を再特定してから手順を書く（03-RESEARCH.md Assumptions Log A3）。
  - **項目 ③⑤ は実 API・課金を伴う。** `GEMINI_API_KEY` は設定済みの想定だが `ANTHROPIC_API_KEY`
    は未設定である（03-RESEARCH.md Environment Availability）。実行時に環境変数の有無を確認し、
    キーがある分だけを実施対象、無い分は「未実施（理由）」へ仕分ける（D-14）。⑤ は Claude 分と
    Gemini 分を別行に分けること。
  - **Phase 2 の OpenAI 関連 UAT は 02-04 の実機確認 3 分割で実施済みのため対象外**とし、その旨を
    記録に残す（黙って落とさない）。

`.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` を新規作成する。構成:
  - `## 対象確定（現行照合）` — 候補 14 項目 + 対象外とした Phase 2 分について、
    `候補 / 出典 / 現行コードでの現在地 / 判定（実施対象・除外・未実施）/ 理由` の表
  - `## 実施結果` — 実施対象の各項目を `# / 項目 / 出典 / 手順 / 実施日 / 結果 / 根拠` の表で並べる。
    この時点では `実施日` `結果` `根拠` の欄を `—` にしておき、Task 2〜4 の checkpoint 承認後に
    Task 5 で埋める
  - `## 未実施（理由付き・D-14）` — 実施できない項目と理由、次に消化できる条件
  - `## グループ分け` — Task 2 / 3 / 4 のどの checkpoint でどの項目を確認するかの対応表

表の並び順の規則（この規則自体をレポート冒頭に明記する）:
  出典マイルストーン昇順（v1.4.0 → v1.6.0 → v1.7.1 → v1.9.0）→ 同一マイルストーン内は元の
  項番昇順。実施順序が変わっても表の並びは変えない。

重複の扱い: 遡及項目と v1.9.0 項目が同一の現行挙動を指す場合は 1 行へ統合し、`出典` 欄に
統合元の出典（マイルストーンと元の項番）を両方併記する。同じ挙動を 2 行に分けて重複計上しない。

空の扱い: 現行照合の結果、実施対象が 0 件になった候補カテゴリ（例: あるマイルストーンの分が
全件除外になった場合）は、表から黙って消さず `対象なし（理由）` の明示行として残す。

日本語で記述する。実 API キー文字列・個人のフルパスは一切書かない（パスはリポジトリ相対へ正規化する）。
  </action>
  <verify>
    <automated>grep -c "^## 対象確定（現行照合）" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が真
    - `grep -c "^## 対象確定（現行照合）" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が `1`
    - `grep -c "^## 実施結果" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が `1`
    - `grep -c "^## 未実施（理由付き" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が `1`
    - `grep -c "^## グループ分け" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が `1`
    - 対象確定表に候補 14 項目すべてと Phase 2 対象外の行があり、各行に「判定」と「理由」が入っている（黙って消えている候補が 1 件もない）
    - `grep -c "v1\.4\.0" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が 1 以上、`v1.6.0` / `v1.7.1` / `v1.9.0` も同様に 1 以上（4 マイルストーンすべての出典が現れる）
    - 並び順の規則がレポート冒頭に明記されており、実施結果表の行順がその規則どおりである
    - `grep -c "ANTHROPIC_API_KEY\|GEMINI_API_KEY" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が 1 以上（キーの有無に基づく仕分け根拠が書かれている）— ただしキーの**値**は 1 文字も含まれない
    - `git diff --stat -- pagefolio/ tests/` の出力が空（照合はコードを読むだけ・変更しない）
  </acceptance_criteria>
  <done>候補 14 項目の仕分けが根拠付きで完了し、`03-UAT-RESULTS.md` の骨組み（対象確定表・実施結果表の枠・未実施リスト・グループ分け）が存在する。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 実機確認 (1/3) — ショートカット設定と保存トースト再試行</name>
  <precondition>`.\.venv\Scripts\python.exe -m pagefolio` でアプリのメインウィンドウが起動する（Phase 1 / Phase 2 の全コード変更と 03-01 の保存経路変更が作業ツリーに反映済み）</precondition>
  <read_first>
    - `.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md`（Task 1 で確定した実施対象とグループ分け表のグループ 1）
    - `.planning/milestones/v1.7.1-phases/04-ui-ux/04-UAT.md`（項目 1〜3 の `expected` 原文 — 文言を書き換えずにそのまま検証する）
  </read_first>
  <what-built>
v1.7.1 Phase 4 で ShortcutsDialog（実キーキャプチャ編集・重複割り当て拒否・保存直後の即時反映）を
実装したが、実機目視はユーザー判断で一旦 pass とされ未検証のまま残っている。
v1.8.0 Phase 5（V180-ROBUST-03）でキー衝突・表示残留の不具合は解消済みで、自動テストは
`tests/test_shortcuts_dialog.py` が通っている。未確認なのは Tk 実描画とキーイベントの実挙動のみ。

あわせて 03-01 で、保存失敗トーストの「再試行」が上書き確認ダイアログ・保存先ピッカーを
再表示せず、前回確定した対象へ黙って再保存するようにした（V190-QA-02 / D-09〜D-11）。
`tests/test_toast.py` の振る舞いテストで「確認を経由しない」ことは自動検証済みだが、
実際のトースト UI 上で再試行ボタンを押したときの挙動は未確認。
  </what-built>
  <how-to-verify>
`.\.venv\Scripts\python.exe -m pagefolio` でアプリを起動し、以下を順に確認する。各項目の
結果（成功 / 失敗と、失敗時の実際の表示内容）をそのまま返信すること。

  1. **ショートカットのキーキャプチャ** — 設定ダイアログ →「⌨ ショートカット設定…」を開き、
     任意の行の「変更」を押して実キーを押下する。行のキー表示が押下したキーの人間可読表記
     （例: `Ctrl+O`）へ更新されること。**Ctrl や Shift などの修飾キーだけを押した場合は
     確定せず待機状態が続く**こと。
  2. **キー衝突の拒否** — 既に別コマンドへ割り当て済みのキーを割り当てて「保存」を押す。
     衝突しているコマンドの表示名を含むエラーダイアログが出て、保存が拒否され
     ダイアログが閉じないこと。
  3. **保存直後の即時反映** — ショートカットを保存した直後（ダイアログを閉じる前）に、
     新しいキーで実際にコマンドが起動すること。
  4. **保存トースト再試行の確認スキップ（v1.9.0 / V190-QA-02）** — PDF を開き、意図的に
     保存を失敗させる（例: 保存先ファイルを別アプリで排他オープンしておく、または
     書き込み権限のない場所を保存先に選ぶ）。保存失敗トーストが出たら「再試行」を押し、
     **上書き確認ダイアログも保存先ピッカーも表示されない**こと、そして失敗が解消していれば
     そのまま保存が完了しトーストが消えることを確認する。あわせて、**初回の保存操作では
     従来どおり上書き確認ダイアログが出る**ことも確認する。

返信にはファイルのフルパス（ユーザー名を含むパス）や API キーを含めないこと。
  </how-to-verify>
  <resume-signal>4 項目すべての結果を「項番: 成功 / 失敗（失敗時は実際の表示内容）」の形式で返信し、問題がなければ "approved" と書く</resume-signal>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"</automated>
  </verify>
  <acceptance_criteria>
    - 4 項目それぞれについて、ユーザーからの実機実施結果（成功 / 失敗）が返信として得られている
    - 失敗があった場合は、実際の表示内容が記録され、修正するか「未解消の課題」として明示的に扱われている（結果を書き換えて pass にしない）
    - 承認後（指摘に基づく修正を行った場合はその後）に `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件で完走する（checkpoint 間のサンプリング点）
    - ユーザー返信にフルパス・API キー文字列が含まれていない（含まれていた場合は記録へ転記する前に伏せ字化する）
  </acceptance_criteria>
  <done>グループ 1 の 4 項目について実機での確認結果が得られ、Task 5 の記録へ転記できる状態になっている。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 実機確認 (2/3) — 設定 / LLM 設定ダイアログの表示・保持と Undo 復元失敗通知</name>
  <precondition>Task 2 が承認済みであり、`.\.venv\Scripts\python.exe -m pagefolio` でアプリのメインウィンドウが起動する</precondition>
  <read_first>
    - `.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md`（グループ分け表のグループ 2）
    - `.planning/milestones/v1.7.1-phases/04-ui-ux/04-UAT.md`（項目 4〜7 の `expected` 原文）
    - `.planning/phases/01-safety-rollback/01-CONTEXT.md`（D-13 — Undo 復元失敗時の messagebox ブロック通知の設計意図）
  </read_first>
  <what-built>
v1.7.1 Phase 4 で SettingsDialog を「外観 / 操作 / AI・OCR」の 3 セクションへ再編し、
LLMConfigDialog に「選択中プロバイダ固有の設定」/「全プロバイダ共通の設定」の 2 見出しを導入、
外側 SettingsDialog を Cancel しても LLM 設定の適用が保持されるようにし、拡大ポップアップの
i18n 漏れを解消した。いずれも実機目視はユーザー判断で一旦 pass とされ未検証のまま残っている。
Phase 2（v1.9.0）で LLMConfigDialog に OpenAI セクションが追加されているため、プロバイダ切替の
確認対象は 7 種（LM Studio / Ollama / RunPod / Claude / Gemini / OpenAI / Tesseract / off）になっている。

あわせて Phase 1（v1.9.0）で、Undo / Redo の復元が途中で失敗したときに messagebox で
ブロック通知する挙動を実装した（01-CONTEXT.md D-13、01-07 で警告文言を
「二重化または欠落している可能性」へ確定）。自動テストは通っているが、実際にダイアログが
出る様子は未確認。
  </what-built>
  <how-to-verify>
`.\.venv\Scripts\python.exe -m pagefolio` でアプリを起動し、以下を順に確認する。各項目の
結果（成功 / 失敗と、失敗時の実際の表示内容）をそのまま返信すること。

  1. **SettingsDialog の 3 セクション** — 設定ダイアログを開き、「外観」「操作」「AI・OCR」の
     3 セクションが区切り線とともに順に表示され、見出しアイコンが ⚙ になっていること。
  2. **LLMConfigDialog の見出し順序とプロバイダ切替** —「⚙ LLM 設定…」を開き、
     `選択中プロバイダ固有の設定` → 固有フレーム → `全プロバイダ共通の設定` → 共通パラメータ
     の順で表示されること。プロバイダ combobox を **すべての選択肢**（LM Studio / Ollama /
     RunPod / Claude / Gemini / OpenAI / Tesseract / off）へ順に切り替え、固有セクションが
     正しく入れ替わり、見出し順序が崩れないこと。
  3. **外側 Cancel での LLM 設定保持** — 外側 SettingsDialog を開いた状態で「⚙ LLM 設定…」を
     開き、値（例: プロバイダ選択やタイムアウト値）を変更して「適用」を押す。その後
     **外側 SettingsDialog を「キャンセル」で閉じ**、再度設定を開くと LLM 設定の変更が
     保持されていること。
  4. **拡大ポップアップの英語表示** — 設定で `lang` を `en` にしてから、サムネイルまたは
     ページから拡大ポップアップを開く。タイトル・縮小 / 拡大 / 閉じるボタンがすべて英語
     （`Page N / M`・`Zoom Out`・`Zoom In`・`Close`）で表示され、日本語が一切出ないこと。
     確認後 `lang` を元に戻す。
  5. **Undo 復元失敗のブロック通知（v1.9.0 / Phase 1 D-13）** — 復元失敗を実機で意図的に
     起こすのが難しい場合は、**「再現できなかった」と返信してよい**（結果を推測で pass に
     しないこと）。再現できた場合は、messagebox が表示されて操作がブロックされること、
     文言が「二重化または欠落している可能性」を伝える内容であることを確認する。

返信にはファイルのフルパス（ユーザー名を含むパス）や API キーを含めないこと。
  </how-to-verify>
  <resume-signal>5 項目すべての結果を「項番: 成功 / 失敗 / 再現できず（失敗時は実際の表示内容）」の形式で返信し、問題がなければ "approved" と書く</resume-signal>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"</automated>
  </verify>
  <acceptance_criteria>
    - 5 項目それぞれについて、ユーザーからの実機実施結果（成功 / 失敗 / 再現できず）が返信として得られている
    - 「再現できず」の項目は pass ではなく「未実施（理由: 再現条件を実機で作れなかった）」として扱われている
    - 失敗があった場合は、実際の表示内容が記録され、修正するか「未解消の課題」として明示的に扱われている
    - 承認後（指摘に基づく修正を行った場合はその後）に `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件で完走する（checkpoint 間のサンプリング点）
  </acceptance_criteria>
  <done>グループ 2 の 5 項目について実機での確認結果が得られ、Task 5 の記録へ転記できる状態になっている。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 実機確認 (3/3) — OCR / AI 系（markdown 整形表示・実 API 出力品質・LM Studio 反映）</name>
  <precondition>Task 3 が承認済みであり、`.\.venv\Scripts\python.exe -m pagefolio` でアプリのメインウィンドウが起動する</precondition>
  <read_first>
    - `.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md`（グループ分け表のグループ 3 と、Task 1 で仕分けた「未実施（理由付き）」の行）
    - `.planning/milestones/v1.6.0-phases/04-ai-c/04-VERIFICATION.md`（`human_verification` 2 件の `test` / `expected` 原文）
    - `.planning/milestones/v1.4.0-phases/04-provider-abstraction/04-VERIFICATION.md`（`human_verification` 2 件の `test` / `expected` 原文）
  </read_first>
  <what-built>
v1.6.0 Phase 4 で OCRDialog の markdown 整形表示（md_h1 / md_h2 / md_bullet / md_code / md_bold の
5 タグ）と、プロバイダ別プロンプトテンプレート（Claude=XML 構造 / Gemini=明示指示）を実装したが、
実描画の見え方と実 API 出力品質は `human_needed` のまま残っている。

v1.4.0 Phase 04 で LM Studio のモデル・タイムアウトをダイアログから live 値で読み取って
provider を再生成する配線を入れたが、実際に送信される `model` フィールドと実待機時間の確認は
`human_needed` のまま残っている。**v1.8.0 Phase 3 の OCRRunEngine 抽出でこのロジックの配置は
変わっており、Task 1 で現在地を再特定済み**（`ocr_dialog.py` の `_apply_llm_settings` /
`_on_run` から `build_provider` へ流れる経路）。

v1.6.0 Phase 3 の V16-QUAL-03（max_tokens クランプ・429 リトライの実 API 検証）は
実 API と課金を伴うため、Task 1 の仕分け結果に従う。
  </what-built>
  <how-to-verify>
`.\.venv\Scripts\python.exe -m pagefolio` でアプリを起動し、Task 1 で「実施対象」と仕分けられた
項目だけを実施する。**「未実施」と仕分けられた項目は実施せず、そのまま未実施として返信する
（推測で pass にしない）。** 各項目の結果をそのまま返信すること。

  1. **markdown 整形表示** — 適当な PDF を開き、OCR ダイアログでプリセット `Markdown` を選んで
     OCR を実行する。結果テキストで、見出し（アクセント色・太字）・箇条書き（• インデント）・
     コードブロック（パネル背景・等幅フォント）・`**bold**` が視覚的に区別されて表示され、
     プレーンテキストより読みやすいことを目視する。
  2. **プロバイダ別プロンプトの実 API 出力品質（Gemini 分）** — プロバイダを Gemini にして
     同じページを Markdown プリセットで OCR し、構造化された Markdown が返ることを確認する。
     `GEMINI_API_KEY` が未設定の場合は実施せず「未実施（キー未設定）」と返信する。
  3. **プロバイダ別プロンプトの実 API 出力品質（Claude 分）** — `ANTHROPIC_API_KEY` が未設定の
     想定。設定されていなければ実施せず「未実施（キー未設定）」と返信する。
  4. **LM Studio のモデル切替反映** — LM Studio をローカルで起動しモデルを 2 つ以上用意する。
     LLM 設定ダイアログでモデルを変更してから「読み取り実行」を押し、**LM Studio 側のログで
     実際に送信された `model` フィールド**がダイアログで選んだモデル名になっていることを確認する。
     LM Studio を用意できない場合は「未実施（LM Studio 未起動）」と返信する。
  5. **タイムアウト表示と実待機時間の一致** — LLM 設定でタイムアウトを短い値（例: 15 秒）に変更し、
     接続を切った LM Studio に対して OCR を実行する。表示されるタイムアウトメッセージの秒数と、
     実際に待たされた時間がおおむね一致することを確認する。
  6. **max_tokens クランプ / 429 リトライ（V16-QUAL-03）** — Task 1 の仕分けが「実施対象」の
     場合のみ実施する。課金が発生する、またはレート制限を意図的に踏む必要がある場合は
     実施せず「未実施（課金 / レート制限誘発が必要）」と返信する。

**API キー文字列を返信・ログ・スクリーンショットへ残さないこと。** OCR 結果の全文ではなく、
構造が読み取れる程度の要旨だけを返信すること（個人情報を含む PDF を使った場合は特に注意）。
  </how-to-verify>
  <resume-signal>6 項目すべての結果を「項番: 成功 / 失敗 / 未実施（理由）」の形式で返信し、問題がなければ "approved" と書く</resume-signal>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"</automated>
  </verify>
  <acceptance_criteria>
    - 6 項目それぞれについて「成功 / 失敗 / 未実施（理由）」が返信として得られている
    - 未実施の項目には具体的な理由（キー未設定 / LM Studio 未起動 / 課金が必要 等）が付いており、pass へ丸められていない（D-14）
    - 返信に API キー文字列が含まれていない
    - 承認後（指摘に基づく修正を行った場合はその後）に `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件で完走する（checkpoint 間のサンプリング点）
  </acceptance_criteria>
  <done>グループ 3 の 6 項目について、実施結果または未実施理由が得られ、Task 5 の記録へ転記できる状態になっている。</done>
</task>

<task type="auto">
  <name>Task 5: `03-UAT-RESULTS.md` への結果確定と未実施項目の理由付き記録</name>
  <files>.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md</files>
  <read_first>
    - `.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md`（Task 1 の骨組みと、Task 2〜4 で得たユーザー返信を転記する先）
    - `.planning/phases/03-qa-release-gate/03-CONTEXT.md`（D-14 の Deferred 運用・D-15 の記録形式）
  </read_first>
  <action>
Task 2〜4 のユーザー返信を `03-UAT-RESULTS.md` の `## 実施結果` 表へ転記し、`実施日` `結果`
`根拠` の欄を埋めて確定させる。

  - `結果` に使う値は `pass` / `fail` / `未実施` の 3 種のみとする。「一旦 pass」は使わない。
  - `根拠` には、ユーザー返信の該当箇所を要約して書く（何を見て pass と判断したか）。
    コードを読んだだけの確認を実機目視の根拠として書かない。
  - `未実施` の行は `## 未実施（理由付き・D-14）` 節にも理由と「次に消化できる条件」を書く。
    実 API キーが必要なもの（V16-QUAL-03 の max_tokens クランプ / 429 リトライ、Claude 実 API の
    出力品質）は、次に実キーが用意できる機会に消化する旨を明記する。
  - `fail` があった場合は、その場で修正するのか次マイルストーンへ送るのかを明記し、
    送る場合は `## 未解消の課題` 節を新設して記録する。**結果を書き換えて pass にしない。**
  - 実 API キー文字列・ユーザー名を含むフルパスは 1 文字も書かない。パスはリポジトリ相対へ
    正規化する。OCR 結果は構造が読み取れる要旨のみとする。

最後に `## サマリ` 節を追加し、`対象確定件数 / pass / fail / 未実施 / 除外` の内訳を書く。
内訳の合計が対象確定表の行数と一致することを確認する（黙って消えた項目が無いことの検算）。
  </action>
  <verify>
    <automated>grep -c "^## サマリ" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## サマリ" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が `1`
    - `## 実施結果` 表のすべての行で `実施日` `結果` `根拠` が `—` ではなく埋まっている
    - `grep -c "一旦 pass\|一旦パス" .planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` が `0`（過去の運用の引用を除き、本フェーズの判定に「一旦 pass」を使っていない。引用として言及する場合は `## 対象確定（現行照合）` 節の出典欄に限る）
    - `## サマリ` の内訳（pass + fail + 未実施 + 除外）の合計が `## 対象確定（現行照合）` 表のデータ行数と一致する
    - `未実施` と記録された各行に、対応する理由が `## 未実施（理由付き・D-14）` 節に存在する
    - `03-UAT-RESULTS.md` に `C:\Users\` を含む文字列が 0 件（フルパスを書いていない）
    - `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件
  </acceptance_criteria>
  <done>遡及分と v1.9.0 分の UAT が一覧で追える形で記録され、未実施項目は理由と次の消化条件つきで残っている。リリース判定に必要な実測が揃った。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 実機確認のユーザー返信 → `03-UAT-RESULTS.md` の記録 | 人の観測が唯一のソース。転記の段階で結果を丸めると、記録がリリース判定を誤らせる |
| 実 API 実行（Gemini / LM Studio）→ UAT 記録・SUMMARY・git 履歴 | API キー・OCR 対象 PDF の内容・ユーザー名を含むパスが記録へ流れ込みうる経路 |
| 過去マイルストーンの UAT 記録 → 本フェーズの対象確定 | 遡及項目の文言を書き換えると、過去の検証意図が失われる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | `03-UAT-RESULTS.md` / checkpoint 返信 / SUMMARY | high | mitigate | 各 checkpoint の `how-to-verify` に「API キー文字列を返信・ログ・スクリーンショットへ残さない」を明記する。Task 5 の受け入れ基準で `C:\Users\` を含む文字列が 0 件であること・キー値が含まれないことを検証する。OCR 結果は全文ではなく構造が読み取れる要旨のみを記録する |
| T-03-03-02 | Repudiation | UAT 結果の記録 | high | mitigate | `## 実施結果` 表に `実施日` `結果` `根拠` を必須列として持たせ、`結果` は `pass` / `fail` / `未実施` の 3 値のみに限定する。`## サマリ` の内訳合計が対象確定表の行数と一致することを検算し、黙って消えた項目がないことを示す |
| T-03-03-03 | Tampering | 遡及項目の文言 | medium | mitigate | 過去 UAT の `expected` 原文を `read_first` で読み、書き換えずにそのまま検証する。仕様変更で意味を失った項目は書き換えではなく「除外」＋理由で扱う（Task 1 の action で明示） |
| T-03-03-04 | Spoofing | 実 API 実行時の送信先 | low | accept | 送信先は既存プロバイダ実装（Phase 2 以前で完成・本フェーズでは無改造）が決めており、本プランは新たな送信先を追加しない。Gemini / LM Studio ともに Phase 2 までのコスト確認ゲートを通る |
| T-03-03-SC | Tampering | npm/pip/cargo installs | low | accept | 本プランは新規パッケージのインストールを一切行わない（V14-D-01）。`03-RESEARCH.md` の Package Legitimacy Audit は「該当なし」と結論している |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**新規ファイル**
- `.planning/phases/03-qa-release-gate/03-UAT-RESULTS.md` — 遡及分と v1.9.0 分の UAT 実施記録

**`03-UAT-RESULTS.md` 内の新規セクション名（下流が参照する見出し）**
- `## 対象確定（現行照合）`
- `## 実施結果`
- `## 未実施（理由付き・D-14）`
- `## グループ分け`
- `## サマリ`
- `## 未解消の課題`（fail が出た場合のみ新設）

**新規コードシンボル:** なし（本プランはコードを一切変更しない）
</artifacts_this_phase_produces>

<verification>
- `03-UAT-RESULTS.md` に対象確定表・実施結果表・未実施リスト・グループ分け・サマリが揃っている
- 実施結果表の全行で `実施日` `結果` `根拠` が埋まっている
- サマリの内訳合計が対象確定表のデータ行数と一致する
- `.\.venv\Scripts\python.exe -m pytest -q --basetemp="$env:LOCALAPPDATA\Temp\pf_pytest_tmp"` が失敗 0 件
- `git diff --stat -- pagefolio/ tests/` の出力が空（本プランはコードを変更しない）
</verification>

<success_criteria>
- 遡及 UAT 候補が現行コードとの照合を経て対象確定され、除外・未実施が理由付きで記録されている
- 実機目視が 3 つの human-verify チェックポイントで実施され、結果がそのまま記録されている
- 実 API・課金が必要でキーが用意できない項目は「未実施（理由）」として残り、リリース判定をブロックしていない
- `03-UAT-RESULTS.md` から遡及分と v1.9.0 分の消化状況が一覧で追える
</success_criteria>

<output>
Create `.planning/phases/03-qa-release-gate/03-03-SUMMARY.md` when done
</output>
