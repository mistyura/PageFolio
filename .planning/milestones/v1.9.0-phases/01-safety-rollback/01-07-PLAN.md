---
phase: 01-safety-rollback
plan: 07
type: execute
wave: 6
depends_on:
  - 01-06
files_modified:
  - pagefolio/file_ops.py
  - pagefolio/lang.py
  - tests/test_pdf_ops.py
  - tests/test_undo_stress.py
autonomous: false
gap_closure: true
requirements:
  - V190-UNDO-01

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "page_edit の undo / redo で「片側の mutation（ページ削除または差し替えページ挿入）が成功した直後にもう片側が失敗」しても、対象ページの内容は doc か保持 Blob のいずれかに必ず存在し、恒久的に失われない（01-VERIFICATION.md gaps missing[1] / 01-REVIEW.md CR-02 / V190-UNDO-01・Success Criterion 5）"
    - "上記の失敗後に障害要因が解消してから undo を再試行すると、doc のページ数が操作前の値へ戻り、全ページの digest 列が操作前と完全一致する。当初 page_edit の対象でなかった隣接ページが巻き添えで削除されない（01-VERIFICATION.md Evidence B の再現手順で `Page 3` が残る。欠陥時は 3 ページ・`Page 3` 消失）（missing[2]）"
    - "ロールバックにも失敗して doc が部分変更のまま残る場合に限り、通常の部分失敗メッセージではなく専用の強い警告が messagebox でブロッキング表示される（`err_undo_restore_failed_content_at_risk` / `err_redo_restore_failed_content_at_risk`）。成功したように見えるサイレント完了は起きない（missing[1]・D-13）"
    - "`_restore_state` 内で `tmp` という名前で開かれる一時 `fitz.Document` は、`insert_pdf` / `delete_page` が例外を送出しても必ず `close()` される（delete / insert_undo / merge_undo / merge_resize / merge_resize_undo / duplicate_undo / `_capture_page_blob` の全 7 箇所）（missing[3]・WR-04）"
    - "`insert`（base op）の削除ループが途中で失敗した場合、実際に削除できた件数を差し引いた残り件数のみを表す state がスタックへ戻り、再試行で挿入対象外だった既存ページを過剰削除しない（missing[4]・WR-05）"
    - "上記いずれの経路でも undo / redo スタックへの直接 `append` / `clear` は行われず、保持された Blob は deque 溢れ（`_push_evicting`）・redo クリア（`_clear_redo_stack`）・消費時・クローズ時（`_clear_undo_stacks`）のいずれかで必ず一度だけ解放される（二重解放ゼロ）（CLAUDE.md Blob ライフサイクル規約・D-14）"
    - "01-06 が確立した `_pending_inverse` 蓄積方式と cb5344e の部分適用保護は退行しない（`TestUndoRedoRestoreFailure` の既存 11 件と `TestBlobLeakDetection` の既存 5 件が無変更のまま green）"
    - "Undo / Redo の復元失敗時に pop した state を `_push_evicting` 経由でスタックへ戻す挙動と、通知が messagebox（トースト／ステータスバーではない）である点は不変（D-13・D-14）"
  prohibitions:
    - "ユーザーへの通知なしにページ内容を失わせてはならない — 復元処理が doc を部分変更のまま確定させる場合は必ずブロッキング警告を出す"
    - "『再試行が成功したように見えるのに実際にはページが失われている』状態を作ってはならない — 再試行経路は doc の実ページ数と state の整合を前提にしてよい状態でのみ mutation を再実行する"
    - "ページ内容を保持した一時ファイル（`UndoBlobStore` の Blob）を、所有者不明のまま残置してはならない — 保持する場合は必ず単一の所有者を定め、既存の解放経路のいずれかで確実に解放されること"
  artifacts:
    - path: "pagefolio/file_ops.py"
      provides: "page_edit 復元ループの中間失敗ロールバック（CR-02）・一時 Document の finally 保護（WR-04）・insert base op の部分適用保護（WR-05）"
      contains: "content_at_risk"
    - path: "pagefolio/lang.py"
      provides: "復旧不能な部分失敗を伝える専用の強い警告文言（ja / en 各 2 キー）"
      contains: "err_undo_restore_failed_content_at_risk"
    - path: "tests/test_pdf_ops.py"
      provides: "『delete_page 成功後・insert_pdf 失敗』を狙い撃ちした失敗注入回帰テストと、一時 Document クローズの AST ガード"
      contains: "test_page_edit_insert_failure_rolls_back_and_retry_preserves_neighbors"
    - path: "tests/test_undo_stress.py"
      provides: "復旧不能経路で保持された Blob の解放・二重解放なしの機械検証"
      contains: "test_page_edit_unrecoverable_failure_blobs_released_on_clear"
  key_links:
    - from: "pagefolio/file_ops.py:_restore_state（page_edit 分岐の中間失敗）"
      to: "pagefolio/file_ops.py:_restore_partial_error"
      via: "ロールバック可否を判定し、復旧不能な場合のみ content_at_risk=True を立てて remaining_state を返す"
      pattern: "content_at_risk"
    - from: "pagefolio/file_ops.py:PartialRestoreError.content_at_risk"
      to: "pagefolio/file_ops.py:_undo / _redo の messagebox.showerror"
      via: "content_at_risk の真偽で通常の部分失敗文言と専用の強い警告文言を切り替える"
      pattern: "err_undo_restore_failed_content_at_risk"
    - from: "pagefolio/file_ops.py:_dispose_state"
      to: "pagefolio/file_ops.py:_release_blob"
      via: "復旧不能時に remaining_state へ保持した Blob も evict / clear / close 経路で解放する（D-14）"
      pattern: "_release_blob"
    - from: "pagefolio/file_ops.py:_restore_state（insert base op の削除ループ）"
      to: "pagefolio/file_ops.py:_merge_pending_inverse"
      via: "削除できたページ分の逆デルタを蓄積し、部分失敗時は残り件数のみの state を戻して過剰削除を防ぐ（WR-05）"
      pattern: "_merge_pending_inverse\\("
---

<objective>
`01-VERIFICATION.md`（status: `gaps_found` / score 4/5）が FAILED と判定した Truth 5（V190-UNDO-01 / ROADMAP Success Criterion 5）を閉じる。**01-01〜01-06 は完了済みであり、本プランはそれらに一切手を入れない追加プランである。**

01-06（`f9973ce`..`e41b7e3`）は旧欠陥（`_apply_inverse` が縮小 `state["data"]` を次段逆デルタに使い回す問題）を `_pending_inverse` / `_merge_pending_inverse` 方式で正しく解消した。しかしその後のコードレビュー（`01-REVIEW.md` / `5bc55ca`）が **同じ復元ループ群に別種の Critical 欠陥（CR-02）** を発見し、検証が独立再現でこれを確認した。

`_restore_state` の `op == "page_edit"` 分岐（`pagefolio/file_ops.py` 555-589 行目付近）は、1 ページにつき `delete_page(page_i)` → `insert_pdf(tmp, start_at=page_i)` という **2 段階 mutation** を行う唯一の op である。`delete_page` 成功直後に `insert_pdf` が失敗すると、doc から消えた内容の唯一のコピーである `captured` が例外ハンドラで解放され、ページ内容は doc にも Blob にも存在しない恒久喪失状態になる。さらに外側のハンドラは「doc は無変更」という前提で `remaining_state` を構築するため、doc の実ページ数（1 少ない）と矛盾したままスタックへ戻り、再試行時にインデックスがずれて **無関係な隣接ページを巻き添えで恒久削除する**。検証の独立再現では 4 ページ doc / `page_edit(targets=[0,1])` の undo が 1 回目失敗 → 2 回目（障害解消後）で **例外を出さず正常終了したように見えるのに** `Page 3` がエラー通知なく消失した（Evidence B）。

あわせて、検証が「同一の是正プランに含めること」を明示的に推奨した付随 Warning 2 件も閉じる。WR-04（`tmp = fitz.open()` → `insert_pdf` → `tmp.close()` が `finally` 未保護で一時 `fitz.Document` がリークする）と WR-05（`insert` base op の削除ループが部分適用保護から除外されたままで、page_edit と同型の過剰削除リスクが残る）は、いずれも `_restore_state` の同じ復元ループ群に属し脅威モデルと修正パターンが共通する。

Purpose: マイルストーン共通の受け入れ条件「操作が失敗した場合、Document・Undo 履歴はいずれも操作前の状態へ戻る」の最後の 1 件。現状は 1177 件のテストが全 green でありながら、CR-02 を検出できるテストが 1 件も存在せず、ユーザーの PDF が静かに壊れ得る。
Output: page_edit 復元ループの中間失敗ロールバック（+ 復旧不能時の保持と専用警告）、一時 Document の `finally` 保護（7 箇所）、`insert` base op の部分適用保護。狙い撃ちの失敗注入回帰テストと AST ガード、Blob ライフサイクルの機械検証。

**01-06 の `_pending_inverse` / `_merge_pending_inverse` 蓄積方式は revert しない。** 本プランはその上に「2 段階 mutation の中間失敗」という別の関心事を積み増す。`PartialRestoreError` / `_restore_partial_error` / `_handle_partial_restore` / `_push_evicting` 経由の state 復帰（cb5344e・01-04 の D-13/D-14 成果）も不変条件として維持する。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-rollback/01-VERIFICATION.md
@.planning/phases/01-safety-rollback/01-REVIEW.md
@.planning/phases/01-safety-rollback/01-CONTEXT.md
@.planning/phases/01-safety-rollback/01-PATTERNS.md
@.planning/phases/01-safety-rollback/01-06-SUMMARY.md
@CLAUDE.md
@pagefolio/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: page_edit 2 段階 mutation の中間失敗をどう封じるかの方式確定（one-way・REVERSIBILITY GATE）</name>
  <read_first>
    - `.planning/phases/01-safety-rollback/01-VERIFICATION.md` の `gaps[0].missing[]`（4 項目）・「Truth 5 の詳細 Evidence」の Evidence B・「根本原因（コードで再確認）」段落
    - `.planning/phases/01-safety-rollback/01-REVIEW.md` の CR-02 全文（Issue と Fix 案 1 / 2）
    - `pagefolio/file_ops.py` の `_restore_state` の `page_edit` 分岐（555-589 行目付近）と、比較対象として `merge_resize` 分岐の `_merged_page_deleted` フラグ（697-741 行目付近・「2 フェーズ mutation の再試行制御」の既存 precedent）
    - `pagefolio/file_ops.py` の `_dispose_state`（116-144 行目付近）・`_merge_pending_inverse`（146-161 行目付近）・`_restore_partial_error`（163-180 行目付近）
    - `CLAUDE.md` §「Architectural Constraints」の Undo Blob ライフサイクル規約（`_push_evicting` / `_clear_redo_stack` / `_clear_undo_stacks` 経由必須・スタックへの直接 append / clear 禁止）
  </read_first>
  <decision>`page_edit` の復元で「片側の mutation は成功したがもう片側が失敗した」中間状態を、どちらの方式で封じるか</decision>
  <context>
本決定は (1) undo / redo スタック上の state dict に新しい内部キーを追加するか、(2) ユーザーへ新しい失敗カテゴリ（「このページの内容が失われた可能性があります」級の強い警告）を提示するか、という 2 つの契約変更を含む。とくに option-a は「Blob をスタック上の state に保持し続ける」という **CLAUDE.md に文書化された Undo Blob ライフサイクル規約への追記**を伴い、以後の undo 系実装すべてがこの規約の上に載るため one-way である。option-b は保持を一切必要としないが、`page_edit` の mutation 順序（doc への書き込み順）を反転させる。どちらを選んでも `<acceptance_criteria>` の behavior 条件（内容喪失なし・隣接ページ巻き添えなし・専用警告）は同一であり、Task 1 の `<action>` は選択された option の枝だけを実行する。
  </context>
  <options>
    <option id="option-a">
      <name>option-a（01-REVIEW.md CR-02 Fix 案の文言どおり）: 現行の `delete_page` → `insert_pdf` 順を維持し、`insert_pdf` 失敗時に `captured` から即座にロールバックを試みる。ロールバックも失敗した場合のみ `captured` を解放せず `remaining_state` に保持し、専用の強い警告を出す</name>
      <pros>検証レポートの `missing[1]` とレビューの Fix 案 1 に文言レベルで一致するため、再検証時の解釈ぶれが最小。差分が `page_edit` 分岐 1 箇所と `_dispose_state` の拡張に閉じる。保持した内容を再試行の先頭で doc へ戻すことでインデックス整合も回復できる</pros>
      <cons>「doc からページ内容が消えている時間帯」が構造としては残り続ける（ロールバックが失敗すれば内容の唯一のコピーは Blob だけになる）。Blob 保持キーの追加により所有権・二重解放の管理点が 1 つ増え、CLAUDE.md の Blob ライフサイクル規約への追記が必要（one-way）</cons>
    </option>
    <option id="option-b">
      <name>option-b（推奨・構造的解消）: mutation 順序を反転し「差し替えページを `insert_pdf` で先に挿入 → 旧ページを `delete_page` で除去」にする。`delete_page` 失敗時は挿入済みページを削除してロールバックし、それも失敗した場合は『挿入済み』マーカーを `remaining_state` に残して再試行が挿入を二重に行わないようにする</name>
      <pros>doc がページ内容を失っている瞬間が構造的に存在しなくなる（最悪でも旧ページと新ページが両方 doc に残る＝内容喪失ゼロ・可視かつ回復可能）。Blob の保持が一切不要なため Blob ライフサイクル規約への追記が不要で、二重解放の新しい管理点も増えない。再試行制御マーカーは `merge_resize` の `_merged_page_deleted` と同型の既存 precedent に乗る</pros>
      <cons>検証レポートが例示した警告文言（『このページの内容が失われた可能性があります』）は本方式では事実に反するため、文言を「二重化または欠落の可能性」に置き換える必要がある（再検証時に文言差の説明が要る）。`page_edit` の doc 書き込み順序が変わるため、既存の page_edit 系テスト（`TestPageEditRedactMosaic` / `test_page_edit_stress_blob_bounded`）の非退行確認が必須</cons>
    </option>
    <option id="option-c">
      <name>option-c: ユーザーが別方式を指定する</name>
      <pros>プランナー・レビュー・検証のいずれの検討にもない制約（性能・将来設計・文言方針）を反映できる</pros>
      <cons>指定内容に応じて Task 1 の `<action>` を再設計する必要がある</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 選択された option ID が SUMMARY の「Decisions Made」に記録されている
    - option-c が選ばれた場合、Task 1 の `<action>` を選択方式へ読み替えたうえで、読み替え内容が SUMMARY に記録されている
    - option-a / option-b が選ばれた場合、Task 1 は本 PLAN の該当 option 枝の記述どおりに実行し、選ばなかった枝の変更は一切行わない
    - option-b が選ばれた場合、警告文言が「内容が失われた」ではなく「二重化または欠落の可能性」を表す表現になっている理由が SUMMARY に記録されている（再検証時の照合用）
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: page_edit の中間失敗ロールバックと専用警告を end-to-end で成立させる（トレーサー・CR-02）</name>
  <reversibility rating="one-way">undo / redo スタック上の state dict に新しい内部キー（option-a: Blob を保持する `_page_recovery` / option-b: 再試行制御の `_page_edit_inserted`）を追加し、`PartialRestoreError` と `_restore_partial_error` のシグネチャを拡張し、ユーザー向けに新しい失敗カテゴリの文言を 2 キー追加する。とくに option-a は CLAUDE.md に文書化された Blob ライフサイクル規約への追記を伴い、以後の undo 系実装がこの規約の上に載る。Task 0 の checkpoint:decision で確定してから着手する。</reversibility>
  <files>pagefolio/file_ops.py, pagefolio/lang.py, tests/test_pdf_ops.py</files>
  <read_first>
    - `.planning/phases/01-safety-rollback/01-VERIFICATION.md` の Evidence B（4 ページ doc・`targets=[0,1]`・`insert_pdf` 2 回目失敗・再試行後に `Page 3` が消える実測ログ）と `gaps[0].missing[]` の 1・2 項目
    - `.planning/phases/01-safety-rollback/01-REVIEW.md` の CR-02（Issue の 1〜3 の順序と Fix 案 1 / 2）
    - `pagefolio/file_ops.py` の `_restore_state` 全体（486-797 行目付近）— とくに `page_edit` 分岐（555-589）、比較用の `delete` 分岐（500-525）・`delete_redo` 分岐（526-554）、`merge_resize` 分岐の `_merged_page_deleted` フラグ運用（697-741）
    - `pagefolio/file_ops.py` の `PartialRestoreError`（21-36）・`_dispose_state`（116-144）・`_merge_pending_inverse`（146-161）・`_restore_partial_error`（163-180）
    - `pagefolio/file_ops.py` の `_undo`（271-309）・`_redo`（311-339）— `except PartialRestoreError as e:` ハンドラの `_push_evicting` / `_handle_partial_restore` / `messagebox.showerror` の順序（D-13・D-14 の不変条件）
    - `pagefolio/file_ops.py` の `_capture_page_blob`（90-100）・`_blob_bytes`（102-108）・`_release_blob`（110-114）
    - `pagefolio/lang.py` の `err_undo_restore_failed_partial` / `err_redo_restore_failed_partial`（ja: 242-251 行目付近 / en: 988-995 行目付近）— 追加位置と `{e}` プレースホルダの書式
    - `tests/test_lang_parity.py` の `test_lang_keys_parity` と `test_no_unused_lang_keys`（新キーは ja / en 両方に追加し、かつ `pagefolio/` 内で引用符付き完全一致で参照される必要がある）
    - `tests/test_pdf_ops.py` の `TestUndoRedoRestoreFailure`（1956 行目〜）— `_make_fake_app`（1960）・`_make_full_fake_app(n_pages=4)`（2636）・`test_page_edit_partial_retry_then_redo_undo_roundtrip`（2366、`_blob_bytes` 失敗＝`delete_page` 呼び出し「前」のみを模す既存テスト）
    - `tests/test_pdf_ops.py` の `_page_digest`（764）と `TestPageEditRedactMosaic`（3155 行目〜）の page_edit 駆動パターン
    - `CLAUDE.md` §「Architectural Constraints」および §「既知の制限・注意事項」の黒塗り／モザイク（page_edit op）の記述
  </read_first>
  <behavior>
    - `test_page_edit_insert_failure_rolls_back_and_retry_preserves_neighbors`: 4 ページ doc（`Page 1`〜`Page 4`）→ `targets=[0,1]` で page_edit（黒塗り）→ undo 1 回目は `app.doc` に対する 2 回目の `insert_pdf`（option-b では 2 回目の `delete_page`）で失敗 → 障害解除 → undo 2 回目は成功。最終的に `len(app.doc) == 4` かつ全ページ digest 列が編集前と一致し、index 2 の digest が `Page 3` に対応する（欠陥時は 3 ページ・`Page 3` 消失）
    - 同テスト中、1 回目の失敗直後に `messagebox.showerror` が 1 回だけ呼ばれ、その本文が通常の部分失敗キー `err_undo_restore_failed_partial` である（ロールバックが成功しているため強い警告ではない）
    - `test_page_edit_unrecoverable_failure_warns_and_preserves_all_pages`: 同じ 4 ページ doc・同じ page_edit で、ロールバックに使う fitz 呼び出し自体も失敗させる（option-a: `app.doc` に対する `insert_pdf` を 2 回目以降すべて失敗 / option-b: `app.doc` に対する `delete_page` を 2 回目以降すべて失敗）→ 1 回目の undo で `messagebox.showerror` が 1 回だけ呼ばれ、その本文が `err_undo_restore_failed_content_at_risk` である
    - 同テストで障害を解除して undo を再試行すると、`len(app.doc) == 4` かつ全ページ digest 列が編集前と一致する（内容喪失なし・隣接ページの巻き添えなし・ページ重複なし）
    - 同テストでさらに `app._redo()` すると digest 列が編集直後と一致し、続けて `app._undo()` すると再び編集前と一致する（復旧不能経路を経由しても逆デルタが完全に再構成される）
    - 同テストの全過程を通して、同一 Blob オブジェクトに対する `_release_blob` の呼び出しが 2 回以上発生しない
    - 既存の `test_page_edit_partial_retry_then_redo_undo_roundtrip`（`_blob_bytes` 失敗＝mutation 前の失敗）・`TestPageEditRedactMosaic` の全件・`tests/test_undo_stress.py::TestUndoRedoStress::test_page_edit_stress_blob_bounded` は無変更のまま green を維持する
  </behavior>
  <action>
Task 0 で確定した option の枝だけを実装する。**本タスクでは `page_edit` 分岐と、それが依存する共通部品（`PartialRestoreError` / `_restore_partial_error` / `_undo` / `_redo` の文言選択 / `_dispose_state`）だけを変更する。** WR-04（一時 Document の `finally` 保護）と WR-05（`insert` base op）は Task 2 / Task 3 で扱い、本タスクでは触らない。ほかの 6 op（delete / delete_redo / insert_undo / insert_redo / merge_resize / merge_resize_undo）の mutation ループも本タスクでは変更しない。

**(1) 共通: 失敗カテゴリの区別を型に持たせる。** `PartialRestoreError.__init__` へキーワード引数 `content_at_risk=False` を追加し、`self.content_at_risk` として保持する。docstring に「True は『復元ループが doc をページ内容の一意性が保証されない状態（喪失または二重化）で残した』ことを表し、中間 mutation のロールバックにも失敗した場合にのみ立つ」と明記する。既存の位置引数 `remaining_state` / `original_exception` の順序と意味は変えない。

**(2) 共通: 伝播経路。** `_restore_partial_error` へキーワード引数 `content_at_risk=False` を追加し、`PartialRestoreError` へそのまま渡す。既存の全呼び出し箇所（delete / delete_redo / insert_undo / insert_redo / merge_undo / merge_resize / merge_resize_undo の各分岐）は引数を追加せず既定値 False のままにする（挙動不変）。

**(3) 共通: 通知の切り替え。** `_undo` の `except PartialRestoreError as e:` ハンドラで、`e.content_at_risk` が真なら `err_undo_restore_failed_content_at_risk`、偽なら従来どおり `err_undo_restore_failed_partial` をキーとして `self._t(...).format(e=e.original_exception)` を組み立てる。`_redo` 側は `err_redo_restore_failed_content_at_risk` / `err_redo_restore_failed_partial` で同型にする。`_push_evicting(..., e.remaining_state)` → `_handle_partial_restore()` → `messagebox.showerror(self._t("err_title"), ...)` という呼び出し順序と、通知が messagebox であること（トースト・ステータスバーは不採用）は D-13 の確定事項として変更しない。スタックへの直接 `append` / `clear` は導入しない（D-14）。

**(4) 共通: i18n。** `pagefolio/lang.py` の `LANG["ja"]` と `LANG["en"]` の両方へ `err_undo_restore_failed_content_at_risk` と `err_redo_restore_failed_content_at_risk` を追加する。追加位置はそれぞれ既存の `err_redo_restore_failed_partial` の直後とし、既存キーの文言は変更しない。文言は `{e}` プレースホルダを含み、(i) 復元が途中で失敗したこと、(ii) このページの内容に問題が生じている可能性があること、(iii) ファイルを閉じずに内容を確認してほしいこと、の 3 点を必ず含める。option-a の場合は「このページの内容が失われた可能性があります」を明示する（01-REVIEW.md CR-02 Fix 案の文言）。option-b の場合は内容喪失が構造的に起こり得ないため「このページが二重化または欠落している可能性があります」とし、事実と異なる「失われた」断定は書かない。en 側は同じ 3 点を英語で表現する。

**(5-a) option-a を選んだ場合の `page_edit` 分岐。** `_restore_state` の `page_edit` 分岐を次の構造にする。

- 分岐の先頭で、`state` から保持済み復旧エントリを取り出す専用ヘルパー `_pop_page_recovery(self, state)` を呼ぶ。このヘルパーは `state.pop("_page_recovery", [])` を返すだけの薄いメソッドとして `FileOpsMixin` に追加し、`_merge_pending_inverse` と同じ「pop によって Blob 所有権をローカルへ移す」規約であることを docstring に書く。
- 取り出した復旧エントリが空でない場合、page_index 昇順に、保持していた Blob の内容を `self.doc.insert_pdf(...)` で当該 index へ再挿入して doc のページ数とインデックス整合を回復する。再挿入に成功したエントリはその場で `_release_blob` する（内容が doc へ戻ったので Blob は不要）。再挿入が失敗した場合は、まだ戻せていないエントリを保持したまま `_restore_partial_error` を `content_at_risk=True` と保持エントリ付きで呼ぶ（下記の受け渡し規約に従う）。
- 主ループは現行どおり `state["data"]` を昇順に回し、各ページで `tmp` を先に開き（Blob ロード失敗を mutation 前に検出する現行の意図を維持）、`captured = self._capture_page_blob(page_i)` を取ってから mutation に入る。
- mutation は `self.doc.delete_page(page_i)` を実行したあと、`self.doc.insert_pdf(tmp, start_at=page_i)` を内側の `try` で実行する。内側が失敗したら、保持している `captured` の内容から復元用 Document を開き、`self.doc.insert_pdf(...)` で `page_i` へ差し戻すロールバックを試みる（復元用 Document は `try` / `finally` で確実に閉じる）。ロールバックが成功した場合は doc が mutation 前へ戻っているので `captured` を解放し、元の例外を送出する（このとき `content_at_risk` は False のまま＝`remaining_state` の「doc 無変更」前提が真になる）。ロールバックも失敗した場合は `captured` を **解放せず** ローカルの復旧エントリリストへ `(page_i, captured)` として積み、元の例外を送出する。
- 外側の `except Exception as e:` では、現行どおり成功済み分の Blob 解放と `_merge_pending_inverse` による蓄積合流を行ったうえで、`_restore_partial_error` へ (i) 未適用分 `state["data"][applied:]`、(ii) `pending_inverse=` 合流済み蓄積、(iii) 復旧エントリが 1 件以上あるときのみ `content_at_risk=True` を渡す。復旧エントリは `remaining_state["_page_recovery"]` として引き継ぐ必要があるため、`_restore_partial_error` へキーワード引数 `page_recovery=None` も追加し、`None` 以外が渡されたときだけ `remaining["_page_recovery"]` を上書きする（`pending_inverse` と同型の実装）。
- `_dispose_state` を拡張し、`state.get("_page_recovery")` のエントリについても `_release_blob` を呼ぶ。所有権規約は「ローカル変数 → 解放（ロールバック成功時／再挿入で消費時）または `remaining_state["_page_recovery"]` へ移譲（単一所有者）→ evict / redo クリア / クローズ時に `_dispose_state` が解放」であり、`_pop_page_recovery` が pop するため再試行に取り込まれた後の state は所有権を持たず二重解放は発生しない。この所有権規約を `_pop_page_recovery` と `_dispose_state` の docstring に明記する。

**(5-b) option-b を選んだ場合の `page_edit` 分岐。** `_restore_state` の `page_edit` 分岐を次の構造にする。

- 分岐の先頭で、`state` から再試行制御マーカーを取り出す（`state.pop("_page_edit_inserted", set())` 相当。値は「差し替えページの挿入だけが済んでいるページ index の集合」）。`merge_resize` の `_merged_page_deleted` と同型の precedent であることをコメントに書く。
- 主ループは現行どおり `state["data"]` を昇順に回し、各ページで `tmp` を先に開く（Blob ロード失敗を mutation 前に検出する現行の意図を維持）。
- 当該 `page_i` がマーカー集合に含まれていない通常経路では、`captured = self._capture_page_blob(page_i)`（差し替え前＝現在の内容）を取り、`self.doc.insert_pdf(tmp, start_at=page_i)` で差し替えページを **先に挿入**し、続けて `self.doc.delete_page(page_i + 1)` で旧ページを除去する。挿入が失敗した場合は doc 無変更なので `captured` を解放して例外を送出する（`content_at_risk` は False）。挿入成功後に削除が失敗した場合は `self.doc.delete_page(page_i)` で挿入済みページを取り除くロールバックを試み、成功すれば `captured` を解放して例外を送出する（`content_at_risk` は False）。ロールバックも失敗した場合は、旧ページが `page_i + 1` に残っており内容喪失は起きていないため `captured` は解放してよく、マーカー集合へ `page_i` を追加して例外を送出する（`content_at_risk=True`）。
- 当該 `page_i` がマーカー集合に含まれる再試行経路では、差し替えページは既に `page_i` にあり旧ページが `page_i + 1` にある前提で、`captured = self._capture_page_blob(page_i + 1)`（旧＝差し替え前の内容）を取ってから `self.doc.delete_page(page_i + 1)` だけを実行する。成功したらマーカー集合から `page_i` を取り除く。失敗した場合は `captured` を解放し、マーカーを保ったまま例外を送出する（`content_at_risk=True`）。
- 成功したページは通常経路・再試行経路とも `(page_i, captured)` を蓄積リストへ積み、ループを抜けたら `inverse["data"] = self._merge_pending_inverse(state, 蓄積リスト)` を代入する（01-06 の規約は変更しない）。
- 外側の `except Exception as e:` では、現行どおり成功済み分の Blob 解放と `_merge_pending_inverse` による合流を行ったうえで、`_restore_partial_error` へ (i) 未適用分 `state["data"][applied:]`、(ii) `pending_inverse=` 合流済み蓄積、(iii) マーカー集合が空でないときのみ `content_at_risk=True` を渡す。マーカーを `remaining_state` へ引き継ぐため、`_restore_partial_error` へキーワード引数 `page_edit_inserted=None` を追加し、`None` 以外が渡されたときだけ `remaining["_page_edit_inserted"]` を上書きする（`pending_inverse` と同型の実装）。
- Blob を state に保持する経路は生じないため `_dispose_state` の拡張は不要だが、マーカーが `inverse` 側へ漏れないこと（`inverse` は `_apply_inverse` が作る別 dict であり、マーカーは `state` 側にしか存在しないこと）をコメントで明示する。

**(6) テスト（先に red を作る）。** `tests/test_pdf_ops.py` の既存クラス `TestUndoRedoRestoreFailure` へ 2 件を追加する。新しいテストクラスや新しい FakeApp は作らず、同クラスの `_make_full_fake_app(n_pages=4)` と `_page_digest`、`pagefolio.redact_ops.RedactOpsMixin._redact_page` による page_edit 駆動（`test_page_edit_partial_retry_then_redo_undo_roundtrip` と同じ手順）を再利用する。

- 失敗注入は `_blob_bytes` の差し替えでは到達できない（既存テストが `delete_page` 呼び出し前しか模していないのが本 gap の原因）。代わりに `monkeypatch.setattr(fitz.Document, "insert_pdf", ...)`（option-b では `fitz.Document.delete_page`）でクラスメソッドを差し替え、ラッパー内で第 1 引数が `app.doc` と同一オブジェクトのときだけ呼び出し回数を数え、指定回で `RuntimeError` を送出して、それ以外は事前に退避した実メソッドへ委譲する実装にする。`_capture_page_blob` 内の呼び出しは第 1 引数が `tmp` 側の Document なのでカウントされず、注入が復元ループの mutation だけを狙い撃ちできる。
- `test_page_edit_insert_failure_rolls_back_and_retry_preserves_neighbors` は「2 回目の呼び出しだけ失敗」させ、`test_page_edit_unrecoverable_failure_warns_and_preserves_all_pages` は「2 回目以降すべて失敗」させたうえで、再試行の前に `monkeypatch.undo()` 相当（フラグを寝かせる）で障害を解除する。
- 通知の検証は `monkeypatch.setattr(fo.messagebox, "showerror", ...)` で `(title, body)` を収集し、FakeApp の `_t` がキーをそのまま返す性質を使って body がどちらのキーかを assert する。
- 二重解放の検証は `monkeypatch.setattr(fo.FileOpsMixin, "_release_blob", staticmethod(spy))` で `id(blob)` ごとの呼び出し回数を数える（`tests/test_undo_stress.py::TestBlobLeakDetection::test_double_release_chain_delete_undo_redo_undo` の release スパイと同型）。スパイは退避した実装へ委譲する。

実装後に `ruff check .` と `ruff format .` を通す。`pagefolio/constants.py` の `APP_VERSION` と `開発履歴.md` は本タスクでは更新しない（Task 3 の (5) に判断根拠を記載）。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py::TestUndoRedoRestoreFailure tests/test_pdf_ops.py::TestPageEditRedactMosaic tests/test_lang_parity.py tests/test_undo_stress.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: `pytest "tests/test_pdf_ops.py::TestUndoRedoRestoreFailure::test_page_edit_insert_failure_rolls_back_and_retry_preserves_neighbors" -q` が exit 0
    - Test: `pytest "tests/test_pdf_ops.py::TestUndoRedoRestoreFailure::test_page_edit_unrecoverable_failure_warns_and_preserves_all_pages" -q` が exit 0
    - Behavior: 01-VERIFICATION.md Evidence B の再現手順（4 ページ doc・`targets=[0,1]`・`app.doc` に対する 2 回目の mutation 呼び出しで失敗注入）で、障害解除後の undo 再試行のあとに `len(app.doc) == 4` となり、`[_page_digest(app.doc[i]) for i in range(4)]` が page_edit 実行前の digest 列とリスト単位で一致する（欠陥時は 3 ページ・index 2 の `Page 3` が消失）
    - Behavior: ロールバック不能な注入下では 1 回目の undo で `messagebox.showerror` が 1 回だけ呼ばれ、その本文が `err_undo_restore_failed_content_at_risk` と等しい。ロールバック可能な注入下では同じ位置の本文が `err_undo_restore_failed_partial` と等しい
    - Behavior: ロールバック不能経路を経由した再試行完了後に `app._redo()` → digest 列が page_edit 直後と一致し、続けて `app._undo()` → digest 列が page_edit 実行前と一致する
    - Behavior: 上記 2 テストの全過程で、`_release_blob` に渡された同一 Blob オブジェクト（`id()` 単位）の呼び出し回数が 1 回を超えない
    - Source: `grep -c 'content_at_risk' pagefolio/file_ops.py` の値が 5 以上
    - Source: `grep -n 'err_undo_restore_failed_content_at_risk' pagefolio/file_ops.py pagefolio/lang.py` が `file_ops.py` から 1 件以上、`lang.py` から 2 件（ja / en）を出力する
    - Source: `grep -n 'err_redo_restore_failed_content_at_risk' pagefolio/file_ops.py pagefolio/lang.py` が `file_ops.py` から 1 件以上、`lang.py` から 2 件（ja / en）を出力する
    - Test: `pytest tests/test_lang_parity.py -q` が exit 0（ja / en キー集合一致と未使用キーゼロ）
    - Test: `pytest tests/test_pdf_ops.py::TestUndoRedoRestoreFailure -q` が exit 0（01-04〜01-06 由来の既存 11 件を含めて全件 green＝部分適用保護と蓄積方式の非退行）
    - Test: `pytest tests/test_pdf_ops.py::TestPageEditRedactMosaic tests/test_pdf_ops.py::TestContentOpsUndoFix -q` が exit 0（page_edit 既存挙動の非退行）
    - Test: `pytest tests/test_undo_stress.py -q` が exit 0（`test_page_edit_stress_blob_bounded` の Blob 上限不変条件を含む）
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>page_edit の 2 段階 mutation が中間で失敗しても、ページ内容が doc か保持 Blob のどちらかに必ず残り、障害解除後の再試行で doc が完全に元へ戻る。復旧不能な場合はユーザーへ専用の強い警告がブロッキングで届く。捕捉 → mutation → ロールバック → 保持または解放 → 再試行 → 通知という 1 本の実行経路が失敗注入テストで実証され、残りの Warning 2 件（WR-04 / WR-05）へ同型展開できる状態になっている。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 復元ループの一時 fitz.Document を finally で確実にクローズする（WR-04）</name>
  <reversibility rating="reversible">`try` / `finally` の追加のみで、mutation の順序・件数追跡・例外ハンドラの内容は変更しない。戻す場合は追加した `finally` を外すだけで済む。</reversibility>
  <files>pagefolio/file_ops.py, tests/test_pdf_ops.py</files>
  <read_first>
    - `.planning/phases/01-safety-rollback/01-VERIFICATION.md` の `gaps[0].missing[]` 第 3 項（WR-04）と「Anti-Patterns Found」表の該当行
    - `.planning/phases/01-safety-rollback/01-REVIEW.md` の WR-04 全文（対象 5 箇所の行番号と Fix 例）
    - `pagefolio/file_ops.py` の一時 Document を開く全 8 箇所 — `_capture_page_blob`（96 行目付近）、`delete` 分岐（513）、`page_edit` 分岐（570・Task 1 適用後の形。既に `finally` 保護済み）、`duplicate_undo`（609）、`insert_undo`（629）、`merge_undo`（689）、`merge_resize`（723）、`merge_resize_undo`（764）
    - `pagefolio/file_ops.py` の他の `fitz.open(` 使用箇所（835 / 839 / 840 / 867 / 987 / 992 行目付近）— これらは `tmp` という名前ではなく、本タスクの対象外であることを確認するため
    - `tests/test_font_hardcode_guard.py` と `tests/test_source_keyguard.py` — 本リポジトリ既存のソース走査ガードテストの書式（新規パターンを持ち込まないための雛形）
    - `tests/test_pdf_ops.py` の `TestUndoRedoRestoreFailure::_make_fake_app` / `_make_full_fake_app` と `_page_digest`
  </read_first>
  <behavior>
    - `test_temp_documents_are_finally_closed_guard`: `pagefolio/file_ops.py` を `ast` で解析し、`tmp` という名前へ `fitz.open(...)` の戻り値を代入している全ステートメントについて、同じブロック（文リスト）内のそれ以降に `finalbody` で `tmp.close()` を呼ぶ `Try` ノードが存在することを検証する。1 箇所でも欠けたらどの行かを示して fail する
    - `test_restore_failure_closes_temp_document`: `delete` の undo（複数ページ再挿入ループ）で `app.doc` に対する `insert_pdf` を失敗させたとき、その復元中に `pagefolio.file_ops` 名前空間の `fitz.open` が返した全 Document の `is_closed` が True である
    - 同型の検証を `merge_resize_undo` の復元（`merged_bytes` の挿入で失敗）についても行い、失敗時にも一時 Document が閉じられていることを確認する
    - 既存の部分失敗テスト（`TestUndoRedoRestoreFailure` の 13 件）と `TestAllOpsUndoRedoRoundtrip` の 27 件が無変更のまま green を維持する
  </behavior>
  <action>
`_restore_state` およびその周辺で `tmp` という名前で開かれる一時 `fitz.Document` を、例外経路でも必ず閉じるようにする。**mutation の順序・`pending` への蓄積・`applied` のカウント・`except` ハンドラの中身は一切変更しない**（変更するのは `tmp.close()` の到達保証だけ）。

(1) 次の 7 箇所を `try:` / `finally: tmp.close()` で保護する。`finally` の内側には `tmp.close()` だけを置き、`pending.append(...)` / `applied += 1` / `deleted.add(...)` などの後続処理は `finally` の外（`try` の後）に残す。

- `_capture_page_blob`（96-100 行目付近）: `tmp.insert_pdf(self.doc, ...)` と `tmp.tobytes()` を `try` に入れ、`finally` で閉じる。`data` はローカル変数として `try` 内で束縛し、`return self._get_undo_store().put(data)` は `try` / `finally` の後に置く
- `_restore_state` の `delete` 分岐（513-515 行目付近）
- `_restore_state` の `duplicate_undo`（609-612 行目付近）: `tmp.insert_pdf(self.doc, ...)` と `self.doc.insert_pdf(tmp, ...)` の両方を `try` に入れる
- `_restore_state` の `insert_undo` 分岐（629-631 行目付近）
- `_restore_state` の `merge_undo` 分岐（689-691 行目付近）
- `_restore_state` の `merge_resize` 分岐の元ページ再挿入ループ（723-725 行目付近）
- `_restore_state` の `merge_resize_undo` 分岐の結合ページ挿入（764-768 行目付近）: 既存の外側 `try` の内側に、`tmp` の生成から `insert_pdf` までを包む入れ子の `try` / `finally` を作る（外側の `except Exception as e:` は現行のまま残す）

`page_edit` 分岐は Task 1 適用後も `finally: tmp.close()` を持つ形を維持する（Task 1 の実装で失われていないことをここで確認する）。`fitz.open(` を使う他の箇所（`_open_path_as_pdf` の `img_doc` / `doc`、`_do_open_merged` の `merged`、`_open_pdf_path` の `self.doc` など）は所有権とライフサイクルが異なるため対象外とし、変更しない。

(2) `tests/test_pdf_ops.py` へ新規クラス `TestTempDocumentCloseGuard` を追加し、`test_temp_documents_are_finally_closed_guard` を実装する。`ast.parse` で `pagefolio/file_ops.py` を読み込み、`body` / `orelse` / `finalbody` を持つ全ノードの文リストを走査して、リスト内に「`tmp` への代入で右辺が `fitz.open` 呼び出し」の `ast.Assign` があれば、同じリストのそれ以降に「`finalbody` の中で `tmp.close()` を呼ぶ `ast.Try`」が存在することを assert する。違反時は `node.lineno` を含む assertion message を出す。既存の `tests/test_font_hardcode_guard.py` のファイル読み込み・assertion message の書式に揃え、新しいヘルパーモジュールは作らない。

(3) `tests/test_pdf_ops.py` の `TestUndoRedoRestoreFailure` へ `test_restore_failure_closes_temp_document` を追加する。`monkeypatch.setattr(fo.fitz, "open", spy)` で `pagefolio.file_ops` 名前空間の `fitz.open` を実装へ委譲するスパイに差し替えて戻り値の Document を収集し、Task 1 と同じ「第 1 引数が `app.doc` のときだけ数える」方式で `fitz.Document.insert_pdf` を指定回に失敗させる。`delete` の undo と `merge_resize_undo` の redo の 2 ケースについて、失敗後に収集した全 Document の `is_closed` が True であることを assert する（`merge_resize` 系の駆動には同クラスの `_make_full_fake_app(n_pages=4)` を使う）。

実装後に `ruff check .` と `ruff format .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py::TestTempDocumentCloseGuard tests/test_pdf_ops.py::TestUndoRedoRestoreFailure tests/test_pdf_ops.py::TestAllOpsUndoRedoRoundtrip -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: `pytest tests/test_pdf_ops.py::TestTempDocumentCloseGuard -q` が exit 0
    - Test: `pytest "tests/test_pdf_ops.py::TestUndoRedoRestoreFailure::test_restore_failure_closes_temp_document" -q` が exit 0
    - Behavior: `delete` の undo 中に `app.doc` の `insert_pdf` が例外を送出したあと、その復元処理で `pagefolio.file_ops` の `fitz.open` が返した Document の `is_closed` がすべて True である
    - Behavior: `merge_resize_undo` の復元中に `app.doc` の `insert_pdf` が例外を送出したあとも同じ条件が成り立つ
    - Source: `grep -c 'finally:' pagefolio/file_ops.py` の値が 8 以上（Task 2 適用前は 1）
    - Test: `pytest tests/test_pdf_ops.py::TestUndoRedoRestoreFailure -q` が exit 0（Task 1 で追加した 2 件を含む全件 green）
    - Test: `pytest tests/test_pdf_ops.py::TestAllOpsUndoRedoRoundtrip tests/test_pdf_ops.py::TestMergeResizeLogic tests/test_pdf_ops.py::TestInsertUndoRedo -q` が exit 0（往復・merge_resize・insert 既存挙動の非退行）
    - Test: `pytest tests/test_undo_stress.py -q` が exit 0
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>`_restore_state` 周辺で `tmp` として開かれる一時 `fitz.Document` は、mutation が例外を送出しても必ず閉じられる。この不変条件は AST ガードテストで恒久的に固定され、将来の新しい復元ループが `finally` を忘れると即座に fail する。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: insert（base op）の部分適用保護と最終ゲート（WR-05）</name>
  <reversibility rating="costly">`insert` の逆デルタ捕捉タイミングを「mutation 前の一括捕捉」から「削除の直前」へ移し、`_apply_inverse` の当該分岐を op 名決定のみに縮小する。01-06 が 7 op へ適用したのと同じ規約に乗せる変更であり、戻す場合は `_apply_inverse` 側の捕捉復活と `_pending_inverse` 経路の除去を同時に行う必要がある。</reversibility>
  <files>pagefolio/file_ops.py, tests/test_pdf_ops.py, tests/test_undo_stress.py</files>
  <read_first>
    - `.planning/phases/01-safety-rollback/01-VERIFICATION.md` の `gaps[0].missing[]` 第 4 項（WR-05）と「Anti-Patterns Found」表の該当行
    - `.planning/phases/01-safety-rollback/01-REVIEW.md` の WR-05 全文（`num` 回再実行による過剰削除の説明と Fix 例）
    - `pagefolio/file_ops.py` の `_apply_inverse` の `insert` 分岐（433-443 行目付近）— `range(insert_at, insert_at + num)` を mutation 前に一括捕捉し、コメントで「本 Task の変更対象ではない」と明示している現行実装
    - `pagefolio/file_ops.py` の `_restore_state` の `insert` 分岐（613-616 行目付近）— 保護のない `for _ in range(num): self.doc.delete_page(insert_at)`
    - `pagefolio/file_ops.py` の `delete_redo` 分岐（526-554 行目付近）と `insert_redo` 分岐（642-677 行目付近）— 「削除の直前に捕捉し、成功時のみ蓄積する」01-06 の参照実装
    - `pagefolio/file_ops.py` の `_merge_pending_inverse`（146-161）・`_restore_partial_error`（Task 1 適用後の形）・`_dispose_state`（116-144。`insert` op の `data` は `[insert_at, num]` のスカラー 2 要素であり blob 走査対象に含まれないこと）
    - `pagefolio/file_ops.py` の `_save_undo` の `insert` 分岐（240-241 行目付近・`state["data"] = [kwargs["insert_at"], 0]` の初期化）と `pagefolio/page_ops.py` の `_do_insert`（件数を後から確定する呼び出し側）
    - `tests/test_pdf_ops.py` の `TestInsertUndoRedo::test_insert_undo_redo_undo_roundtrip`（894-943 行目付近）— insert state を手組みで駆動する既存パターン
    - `tests/test_pdf_ops.py` の `TestUndoRedoRestoreFailure` 内の insert 系部分失敗テスト（`test_insert_undo_partial_retry_then_redo_undo_roundtrip` / `test_insert_redo_partial_retry_then_undo_redo_roundtrip`）
    - `tests/test_undo_stress.py` の `_make_stress_app`（57）・`stress_pdf_bytes`（88）・`_blob_files`（107）・`TestBlobLeakDetection`（231 行目〜、とくに `test_double_release_chain_delete_undo_redo_undo` と `test_pending_inverse_blobs_released_on_stack_clear`）
    - `CLAUDE.md` §「変更時のチェックリスト」（リント・テスト・開発履歴.md・バージョン番号）
  </read_first>
  <behavior>
    - `test_insert_partial_failure_preserves_remaining_and_retry_completes`: 3 ページ doc の index 1 へ 2 ページを挿入 → undo 1 回目は `app.doc` に対する 2 回目の `delete_page` で失敗し、doc は 4 ページ（1 件だけ削除済み）になり `messagebox.showerror` が 1 回呼ばれる。undo スタックの先頭は `op == "insert"` かつ `data == [1, 1]`（残り 1 件）である
    - 同テストで障害を解除して undo を再試行すると doc が 3 ページへ戻り、digest 列が挿入前と一致する（`num` 回ぶんの削除が再実行されて既存ページを巻き添えにしない）
    - 同テストでさらに `app._redo()` すると digest 列が挿入後と一致し（逆デルタが 2 件ぶん完全に再構成されている）、続けて `app._undo()` すると digest 列が挿入前と一致する
    - `test_insert_partial_retry_blobs_released_on_stack_clear`（`tests/test_undo_stress.py`）: `insert` の部分失敗で `_pending_inverse` を持つ state がスタックに残った状態から `_clear_undo_stacks()` を呼ぶと `_blob_files(app) == 0` になる
    - `test_page_edit_unrecoverable_failure_blobs_released_on_clear`（`tests/test_undo_stress.py`）: Task 1 の復旧不能経路を通した state がスタックに残った状態から `_clear_undo_stacks()` を呼ぶと `_blob_files(app) == 0` になり、その全過程で同一 Blob の `release()` 呼び出しが 1 回を超えない
    - 既存の `TestInsertRollback` / `TestInsertUndoRedo` / `TestAllOpsUndoRedoRoundtrip` / `TestUndoRedoRestoreFailure` / `TestBlobLeakDetection` が無変更のまま green を維持する
  </behavior>
  <action>
`insert`（base op）の削除ループを、01-06 が 7 op へ適用したのと同型の部分適用保護に乗せる。**page_edit / delete / delete_redo / insert_undo / insert_redo / merge_undo / merge_resize / merge_resize_undo の各分岐は本タスクでは変更しない。**

(1) `_apply_inverse` の `insert` 分岐から `inv["data"]` の構築（`range(insert_at, insert_at + num)` の一括 `_capture_page_blob`）を取り除き、`inv["op"] = "insert_undo"` の決定と `current_page` / `selected_pages` の記録だけを残す。現行コメントの「`insert` は `_restore_state` 側にリトライ機構がないため V190-UNDO-01 の変更対象ではない」という記述は、本タスクで対象化されたので削除し、「捕捉タイミングを削除の瞬間へ移した」旨へ書き換える。この変更は、削除ループが途中で失敗したときに一括捕捉済みの Blob が `inverse` ごと破棄されて解放されない潜在リーク（WR-05 の副産物）も同時に閉じる。

(2) `_restore_state` の `insert` 分岐を次の形にする。`state["data"]` から `insert_at` と `num` を取り出し、蓄積リストと削除済み件数を用意する。再試行時のインデックス基準を得るため、ループに入る前に `state.get("_pending_inverse")` の要素数を `already` として読む（`_merge_pending_inverse` は pop するため、必ず pop の前に読む）。`num` 回のループで、各回 `self._capture_page_blob(insert_at)` により「これから削除するページ」の内容を捕捉し、`self.doc.delete_page(insert_at)` を実行する。削除が成功したときだけ `(insert_at + already + k, 捕捉した blob)` を蓄積リストへ積む（`k` はこの呼び出し内の 0 起点カウンタ）。捕捉後に削除が失敗した場合は捕捉 blob を `_release_blob` してから例外処理へ進む。ループを抜けたら `inverse["data"] = self._merge_pending_inverse(state, 蓄積リスト)` を代入する。この index 算出により、部分失敗 → 再試行を経ても `insert_undo` が期待する「挿入位置の昇順連続レンジ」が正しく再構成される。

(3) 同分岐の例外処理では、`_merge_pending_inverse(state, 蓄積リスト)` で合流させたリストを `pending_inverse=` に渡しつつ、残り件数のみを表す `[insert_at, num - この呼び出しで削除できた件数]` を `remaining_data` として `_restore_partial_error` を呼ぶ。これにより、再試行時に `num` 回ではなく残り回数だけ `delete_page` が実行され、挿入対象外だった既存ページの過剰削除（WR-05 の本体）が構造的に起きなくなる。`content_at_risk` は渡さない（削除の失敗は doc からページが余分に消える方向ではなく、残る方向の部分適用であり、内容の一意性は失われないため既定値 False のままでよい）。

(4) テスト（先に red を作る）。`tests/test_pdf_ops.py` の `TestUndoRedoRestoreFailure` へ `test_insert_partial_failure_preserves_remaining_and_retry_completes` を 1 件追加する。state の手組みは `TestInsertUndoRedo::test_insert_undo_redo_undo_roundtrip` と同じ手順（`_save_undo("insert", insert_at=...)` → 実挿入 → `app._undo_stack[-1]["data"][1] = num` で件数確定）で行い、失敗注入は Task 1 と同じ「第 1 引数が `app.doc` のときだけ数える」方式で `fitz.Document.delete_page` を 2 回目に失敗させる。`tests/test_undo_stress.py` の `TestBlobLeakDetection` へ `test_insert_partial_retry_blobs_released_on_stack_clear` と `test_page_edit_unrecoverable_failure_blobs_released_on_clear` の 2 件を追加し、`_make_stress_app` / `stress_pdf_bytes` / `_blob_files` と既存の release スパイ実装を再利用する（新しいフィクスチャは作らない）。

(5) バージョン・ドキュメントの扱い（明示的な判断）。**本プランでは `pagefolio/constants.py` の `APP_VERSION`・`開発履歴.md`・README バッジを更新しない。** 理由は、01-05-SUMMARY で確定し 01-06 でも踏襲された「開発履歴.md はリリース単位で追記する書式であり、マイルストーン途中での追記は書式を崩すため Phase 3（V190-QA-03 リリースゲート）へ委譲する」という既存方針に従うためである（現行 `APP_VERSION` は `v1.8.1`、v1.9.0 への bump は Phase 3 が担当する）。この判断を SUMMARY の「Decisions Made」へ記録する。

(6) 最終ゲート。`ruff check .` / `ruff format --check .` / `pytest -q`（フルスイート）を通す。フルスイートで `tests/test_ocr_dialog_center.py` 等が Tcl/Tk 由来のセットアップ ERROR になった場合は、STATE.md Blockers に既知記載のフレーキー（Phase 3 / V190-QA-01 が引き取り予定）であり本プランの回帰ではないため、該当ファイル単体で再実行して green を確認したうえで SUMMARY へ記録する。pytest が `%TEMP%\pytest-of-*` に対して `PermissionError` を出す場合は、書き込み可能なディレクトリを `--basetemp` で明示して再実行する（環境要因であり失敗と解釈しない）。

(7) SUMMARY への追加記録。`merge`（base op）の `while len(self.doc) > old_count: self.doc.delete_page(old_count)` ループが `insert` と同型の構造（`_apply_inverse` 側での一括捕捉＋保護なしの削除ループ）を持つことを棚卸し結果として記録する。**本プランではコード変更を行わない**（01-VERIFICATION.md の `missing[]` は `insert` のみを対象としており、D-12 が定めた「対象 op を広げず棚卸し記録に留める」方針に従う）。次マイルストーン候補として SUMMARY に明記する。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py tests/test_undo_stress.py tests/test_lang_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: `pytest "tests/test_pdf_ops.py::TestUndoRedoRestoreFailure::test_insert_partial_failure_preserves_remaining_and_retry_completes" -q` が exit 0
    - Behavior: 3 ページ doc の index 1 へ 2 ページ挿入したのち、`app.doc` の 2 回目の `delete_page` で失敗注入して `app._undo()` を実行すると `len(app.doc) == 4` になり、`app._undo_stack[-1]["op"] == "insert"` かつ `app._undo_stack[-1]["data"] == [1, 1]` である
    - Behavior: 障害解除後に `app._undo()` を再試行すると `len(app.doc) == 3` かつ digest 列が挿入前と一致し、続けて `app._redo()` で digest 列が挿入後と一致、さらに `app._undo()` で挿入前と一致する
    - Test: `pytest "tests/test_undo_stress.py::TestBlobLeakDetection::test_insert_partial_retry_blobs_released_on_stack_clear" -q` が exit 0
    - Test: `pytest "tests/test_undo_stress.py::TestBlobLeakDetection::test_page_edit_unrecoverable_failure_blobs_released_on_clear" -q` が exit 0
    - Source: `grep -c '_merge_pending_inverse(' pagefolio/file_ops.py` の値が 17 以上（本プラン開始時点は 15。`insert` 分岐の正常系と例外系で 2 箇所増える）
    - Test: `pytest tests/test_pdf_ops.py::TestInsertRollback tests/test_pdf_ops.py::TestInsertUndoRedo tests/test_pdf_ops.py::TestInsertMergeUndoRedo -q` が exit 0（V190-SAFE-04 と insert 系既存挙動の非退行）
    - Test: `pytest tests/test_pdf_ops.py::TestAllOpsUndoRedoRoundtrip -q` が exit 0（V190-UNDO-02 の 4 手往復 27 件の非退行）
    - Test: `pytest tests/test_pdf_ops.py::TestUndoRedoRestoreFailure -q` が exit 0 で、`test_page_edit_insert_failure_rolls_back_and_retry_preserves_neighbors` / `test_page_edit_unrecoverable_failure_warns_and_preserves_all_pages` / `test_restore_failure_closes_temp_document` / `test_insert_partial_failure_preserves_remaining_and_retry_completes` の 4 メソッドが収集・実行される
    - CLI: `grep -c 'def test_' tests/test_pdf_ops.py` の値が 104 以上（本プラン開始時点は 99。Task 1 で 2 件・Task 2 で 2 件・Task 3 で 1 件を追加する）
    - CLI: `git diff --stat pagefolio/constants.py 開発履歴.md README.md` の出力が空（本プランはこれら 3 ファイルを変更しない）
    - Test: `pytest -q`（フルスイート）が exit 0（Tcl/Tk 既知フレーキーは単体再実行 green を SUMMARY へ記録すれば可）
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>`insert` base op の削除ループが部分適用保護に乗り、部分失敗 → 再試行で既存ページを過剰削除しない。01-VERIFICATION.md `missing[]` の 4 項目すべてが実装とテストで閉じ、フルスイートと lint が green で V190-UNDO-01 / Success Criterion 5 の再検証を受けられる状態になっている。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

本プラン（01-07）が**新規に作成**するシンボル一覧。plan-review-convergence の source-grounding パスはこの表を読み、新規シンボルを drift 検証の対象から除外する。

| 種別 | シンボル | 配置 | Task | 備考 |
|---|---|---|---|---|
| 例外属性 / ctor kwarg | `PartialRestoreError.content_at_risk`（`__init__(..., content_at_risk=False)`） | `pagefolio/file_ops.py` | 1 | 全 option 共通 |
| シグネチャ変更 | `_restore_partial_error(self, state, remaining_data, exc, pending_inverse=None, content_at_risk=False, ...)` | `pagefolio/file_ops.py`（`FileOpsMixin`） | 1 | 全 option 共通。option 別に `page_recovery=` または `page_edit_inserted=` が追加される |
| メソッド（option-a のみ） | `_pop_page_recovery(self, state)` | `pagefolio/file_ops.py`（`FileOpsMixin`） | 1 | option-a 選択時のみ新設 |
| state 内部キー（option-a のみ） | `_page_recovery`（`(page_index, blob)` のリスト） | undo / redo スタック上の state dict | 1 | option-a 選択時のみ |
| state 内部キー（option-b のみ） | `_page_edit_inserted`（差し替え挿入だけ済んだ page index の集合） | undo / redo スタック上の state dict | 1 | option-b 選択時のみ |
| i18n キー | `err_undo_restore_failed_content_at_risk` / `err_redo_restore_failed_content_at_risk` | `pagefolio/lang.py`（`LANG["ja"]` と `LANG["en"]` の両方） | 1 | 全 option 共通・文言は option 依存 |
| テストクラス | `TestTempDocumentCloseGuard` | `tests/test_pdf_ops.py` | 2 | AST ガード |
| テストメソッド | `test_page_edit_insert_failure_rolls_back_and_retry_preserves_neighbors` / `test_page_edit_unrecoverable_failure_warns_and_preserves_all_pages` | `tests/test_pdf_ops.py`（`TestUndoRedoRestoreFailure`） | 1 | |
| テストメソッド | `test_temp_documents_are_finally_closed_guard` | `tests/test_pdf_ops.py`（`TestTempDocumentCloseGuard`） | 2 | |
| テストメソッド | `test_restore_failure_closes_temp_document` | `tests/test_pdf_ops.py`（`TestUndoRedoRestoreFailure`） | 2 | |
| テストメソッド | `test_insert_partial_failure_preserves_remaining_and_retry_completes` | `tests/test_pdf_ops.py`（`TestUndoRedoRestoreFailure`） | 3 | |
| テストメソッド | `test_insert_partial_retry_blobs_released_on_stack_clear` / `test_page_edit_unrecoverable_failure_blobs_released_on_clear` | `tests/test_undo_stress.py`（`TestBlobLeakDetection`） | 3 | |

新規ファイル・新規モジュール・新規例外クラスはゼロ。`PartialRestoreError` / `_restore_partial_error` / `_merge_pending_inverse` / `_dispose_state` / `_push_evicting` / `_capture_page_blob` / `_blob_bytes` / `_release_blob` / `_handle_partial_restore` / `_page_digest` / `_make_fake_app` / `_make_full_fake_app` / `_make_stress_app` / `_blob_files` / `stress_pdf_bytes` / `err_undo_restore_failed_partial` / `err_redo_restore_failed_partial` はすべて既存シンボルであり、本プランは内部実装とシグネチャのみを変更する。
</artifacts_produced>

<source_audit>
## Multi-Source Coverage Audit

| Source | 項目 | 状態 | 担当 |
|---|---|---|---|
| GOAL（ROADMAP Success Criterion 5 前半） | Undo/Redo の復元処理が失敗した場合、対象状態がスタックへ戻され履歴が失われず Document が部分変更のまま残らない | COVERED | Task 1（CR-02）・Task 2（WR-04）・Task 3（WR-05） |
| GOAL（ROADMAP Success Criterion 5 後半） | duplicate / merge / merge_resize の 4 手往復回帰テスト（V190-UNDO-02） | COVERED（既存・非退行のみ） | Task 2・Task 3 acceptance（`TestAllOpsUndoRedoRoundtrip` green） |
| GOAL（Success Criteria 1〜4） | 保存暗号化維持 / OCR OFF 一貫化 / 挿入・複製ロールバック / LLM 設定 Apply-Cancel 契約 | 対象外（01-01〜01-04 で VERIFIED・01-VERIFICATION.md Truths 1〜4） | — |
| REQ | V190-UNDO-01（復元失敗時の state 保全・Document 完全性） | COVERED | Task 1〜3 |
| REQ | V190-SAFE-01〜05 / V190-CFG-01・02 / V190-UNDO-02 | 対象外（01-01〜01-05 で SATISFIED・01-VERIFICATION.md Requirements Coverage 表） | — |
| GAP（01-VERIFICATION.md `missing[]` #1） | `delete_page` 成功後の `insert_pdf` 失敗で即時ロールバックを試みる、または `captured` を解放せず `remaining_state` に保持して専用の強い警告を出す | COVERED | Task 0（方式確定）・Task 1 (5-a)/(5-b) と (1)〜(4) |
| GAP（01-VERIFICATION.md `missing[]` #2） | `delete_page` 成功後・`insert_pdf` 失敗のケースを page_edit 部分失敗テストへ追加し、(a) 内容が失われないこと・(b) 隣接ページが巻き添えにならないことを digest 一致で検証 | COVERED | Task 1 (6) の 2 テスト |
| GAP（01-VERIFICATION.md `missing[]` #3・WR-04） | delete / insert_undo / merge_undo / merge_resize / merge_resize_undo の `tmp` を `finally` で保護 | COVERED（+ `duplicate_undo` と `_capture_page_blob` の同型 2 箇所も同時対応） | Task 2 |
| GAP（01-VERIFICATION.md `missing[]` #4・WR-05） | `insert`（base op）の削除ループへ部分適用保護を展開 | COVERED | Task 3 |
| GAP（検証の推奨対応） | 上記 4 項目を同一の是正プランに含める | COVERED（本プラン 1 本に集約） | Task 1〜3 |
| RESEARCH（01-RESEARCH.md Pitfall 4） | 復元失敗時に `_dispose_state` を呼ぶと二重解放になる | COVERED（不変条件として維持・Task 1 の release スパイと Task 3 の解放テストで機械検証） | Task 1・Task 3 |
| RESEARCH（01-RESEARCH.md §Undo/Redo 復元失敗時の保護） | `PartialRestoreError` / `_restore_partial_error` / `_push_evicting` による state 保全 | COVERED（拡張のみ・既存挙動は不変） | Task 1 (1)〜(3) |
| RESEARCH（01-RESEARCH.md §Package Legitimacy Audit） | 新規外部パッケージなし | COVERED（対象なし・本プランも追加しない） | `<threat_model>` T-01-SC |
| RESEARCH（01-PATTERNS.md §Undo Blob ライフサイクル規約） | 直接 append / pop / clear 禁止・`_push_evicting` 経由必須 | COVERED | Task 1 (3)(5-a)・Task 3 のリークテスト |
| RESEARCH（01-PATTERNS.md §i18n 追加パターン） | LANG は ja / en 同一キーで追加 | COVERED | Task 1 (4)・`test_lang_parity.py` |
| RESEARCH（01-PATTERNS.md §messagebox エラー通知パターン） | 復元失敗はブロッキング通知 | COVERED | Task 1 (3) |
| CONTEXT | D-13（復元失敗は messagebox でブロック通知・トースト不採用） | COVERED（既存挙動を維持しつつ文言のみ 2 分岐へ拡張） | Task 1 (3) |
| CONTEXT | D-14（`_push_evicting` / `_clear_redo_stack` 経由・失敗 state の Blob を dispose しない） | COVERED | Task 1 (3)(5-a)・Task 3 (4) |
| CONTEXT | D-12（記録後置パターンの水平展開は対象 op のみ・棚卸しは記録に留める） | COVERED（`merge` base op を棚卸し記録のみとし、コード変更しない方針の根拠） | Task 3 (7) |
| CONTEXT | D-01〜D-11 / D-15〜D-19 | 対象外（01-01〜01-05 で実装済み・本プランの変更面に一切現れない） | — |
| CONTEXT | Deferred Ideas（rotate / crop / delete 等への記録後置の水平展開・Save As の平文コピー導線） | 対象外（Deferred） | — |

MISSING 項目なし。フェーズ分割の必要なし。
</source_audit>

<edge_coverage>
## Edge Coverage（spec-less probe fallback・no-silent-drop 記録）

本フェーズには `*-SPEC.md` が存在しないため、決定的 edge probe を 9 要件へ実行した結果（`applicable: 18` / `resolved: 0` / `unresolved: 18`）をここに全件記録する。**18 行 = 本プランで扱う flagged assumption 1 件 + 既存プラン所管の out-of-scope 記録 17 件**（silent drop ゼロ）。

### 本プラン所管（V190-UNDO-01）

| # | Requirement | Category | Probe | Status | 本プランでの扱い |
|---|---|---|---|---|---|
| 13 | V190-UNDO-01 | unclassified | unclassified — review manually | **unresolved（flagged assumption）** | **手動レビュー結果:** probe が分類できなかったのは要件文（「復元処理が失敗した場合、対象状態がスタックへ戻され履歴が失われない。Document が部分的に変更されたまま残らない」）が入出力ではなく**失敗時の不変条件**を述べているためである。プランナーの解釈として、本要件の実質的なエッジは「1 つの op が複数の fitz 呼び出しに分解されるとき、その**中間**で失敗した場合の doc の状態」であり、これは `must_haves.truths` の第 1〜3 項（page_edit の 2 段階 mutation 中間失敗）・第 5 項（`insert` 削除ループの中途失敗）として明示的に truth 化した。**backstop 化はしていない**（unclassified は自動 resolve 対象外）。この解釈が正しいかは再検証時に人間の確認対象として残る |

### 既存プラン所管（本プランの対象外・silent drop ではない記録）

| # | Requirement | Category | Status | 所管プラン（01-VERIFICATION.md Requirements Coverage） |
|---|---|---|---|---|
| 1 | V190-SAFE-01 | idempotency | unresolved | 01-01（✓ SATISFIED） |
| 2 | V190-SAFE-01 | concurrency | unresolved | 01-01（✓ SATISFIED） |
| 3 | V190-SAFE-02 | idempotency | unresolved | 01-01（✓ SATISFIED） |
| 4 | V190-SAFE-02 | concurrency | unresolved | 01-01（✓ SATISFIED） |
| 5 | V190-SAFE-03 | unclassified | unresolved | 01-02（✓ SATISFIED） |
| 6 | V190-SAFE-04 | boundary | unresolved | 01-04（✓ SATISFIED） |
| 7 | V190-SAFE-04 | precision | unresolved | 01-04（✓ SATISFIED） |
| 8 | V190-SAFE-04 | concurrency | unresolved | 01-04（✓ SATISFIED） |
| 9 | V190-SAFE-05 | unclassified | unresolved | 01-04（✓ SATISFIED） |
| 10 | V190-CFG-01 | idempotency | unresolved | 01-03（✓ SATISFIED） |
| 11 | V190-CFG-01 | concurrency | unresolved | 01-03（✓ SATISFIED） |
| 12 | V190-CFG-02 | concurrency | unresolved | 01-03（✓ SATISFIED） |
| 14 | V190-UNDO-02 | boundary | unresolved | 01-05（✓ SATISFIED・`TestAllOpsUndoRedoRoundtrip` の境界エッジ 5 件） |
| 15 | V190-UNDO-02 | adjacency | unresolved | 01-05（✓ SATISFIED・同上） |
| 16 | V190-UNDO-02 | empty | unresolved | 01-05（✓ SATISFIED・同上） |
| 17 | V190-UNDO-02 | ordering | unresolved | 01-05（✓ SATISFIED・同上） |
| 18 | V190-UNDO-02 | precision | unresolved | 01-05（✓ SATISFIED・同上） |

**No-silent-drop 等式:** probe 提示 18 件 == flagged assumption 1 件 + out-of-scope 記録 17 件。

### Prohibitions（descriptor-less・flagged-unverified）

SPEC の `## Prohibitions` も不在のため、prohibition recall を要件テキストに対して実行した。Stage 2 の精査で routine engineering（裸 `except:` 禁止・`# type: ignore` 禁止・`pyproject.toml` 編集禁止＝いずれも CLAUDE.md の既存リント/レビュー規約が所管）を DROP し、価値・安全に属する 3 件を `must_haves.prohibitions` へ **descriptor-less**（`check_kind` 等の wired-check 記述子なし）で計上した。canon security（OWASP 等）への referral 該当はなし（本フェーズはネットワーク境界を持たないローカル文書操作）。
</edge_coverage>

<assumption_delta_decision>
## Assumption-Delta Architecture Checkpoint

`gsd-tools query assumption-delta scan 01` は `detected: true` / `kind: chosen` / `term: "custom"` を返したが、そのスニペットは Success Criterion 4 の外部プロンプトファイル名 `ocr_custom_prompt.md` に一致した**ファイル名由来の誤検出**である。設定値が導出値から選択値へ変わった事実も、定数がパラメータへ変わった事実も本フェーズには存在しない。

- **primary な noun:** 変化なし（本プランの中心 noun は「1 つの undo 操作に対応する逆デルタ state」であり、01-06 で確定した `_pending_inverse` 蓄積方式のまま primary である）
- **decision:** `no-change` — 検出は誤検出であり、一般化（singular→plural / required→optional / derived→chosen）は発生していない。既存の primary 表現を降格・追加する理由がない
- **invariant test の提案（任意・本プランでは必須化しない）:** 「どの op でも、部分失敗を経由した往復のあと `inverse["data"]` が当初の対象ページ集合を過不足なく表す」という不変条件は、01-06 が追加した 8 件の 5 手往復テストが既に符号化している
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

本ギャップの現実的な脅威はネットワークや認証ではなく、**ユーザー文書のデータ完全性（サイレントな破損・喪失）**である。

| Boundary | Description |
|----------|-------------|
| 復元処理の中間状態 → `fitz.Document` のページ構成 | 1 つの op が複数の fitz 呼び出しへ分解される区間。中間で失敗すると doc は「操作前でも操作後でもない」状態で確定する |
| doc から取り除かれたページ内容 → `UndoBlobStore` の Blob | ページ内容の唯一のコピーが Blob 側にしか存在しない瞬間が生じる境界。ここで誤って解放すると内容は恒久的に失われる |
| 部分失敗した state → 再試行時のインデックス基準 | `remaining_state` が前提とする doc の形と、実際の doc の形がずれると、再試行が無関係なページを破壊する |
| `UndoBlobStore` の tempfile → ファイルシステム | PDF ページの内容が一時ファイルとしてディスク上に置かれる。保持経路の解放漏れは内容の残置になる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22 | Tampering | `pagefolio/file_ops.py:_restore_state` の `page_edit` 分岐（555-589）— `delete_page` 成功後の `insert_pdf` 失敗で `captured` を無条件解放しページ内容が恒久喪失する | critical | mitigate | 中間失敗時にロールバックを試み、復旧不能な場合のみ内容を保持する（Task 1 (5-a)）、あるいは mutation 順序を反転して内容喪失の窓を構造的に消す（Task 1 (5-b)）。Evidence B の再現手順を `fitz.Document` メソッド差し替えによる失敗注入テストとして固定（Task 1 (6)） |
| T-01-23 | Tampering | 再試行時のインデックスずれによる隣接ページの巻き添え削除（`_capture_page_blob(page_i)` が無関係な別ページを捕捉し、その後の `delete_page` が確定させる） | critical | mitigate | 復旧エントリの先頭再挿入（option-a）または `_page_edit_inserted` マーカーによる挿入スキップ（option-b）で、再試行が常に整合したインデックス基準で動くようにする。digest 列一致による回帰テストで固定（Task 1） |
| T-01-24 | Repudiation | 再試行が「エラーなく成功したように見える」のに doc が壊れている（ユーザーへの通知ゼロ） | high | mitigate | `PartialRestoreError.content_at_risk` を導入し、復旧不能時のみ専用の強い警告を `messagebox.showerror` でブロッキング表示する（Task 1 (1)〜(4)）。通知キーをテストで assert する |
| T-01-25 | Tampering | `insert`（base op）の削除ループ中途失敗 → 再試行で `num` 回ぶん再実行され、挿入対象外の既存ページを過剰削除する（WR-05） | high | mitigate | 削除できた件数を追跡し、残り件数のみの `remaining_state` を `PartialRestoreError` で返す。あわせて逆デルタ捕捉を削除の瞬間へ移し、失敗時に捕捉済み Blob が `inverse` ごと破棄されるリークも閉じる（Task 3 (1)〜(3)） |
| T-01-26 | Information Disclosure | 保持した復旧用 Blob（`UndoBlobStore` の tempfile）の解放漏れによるページ内容の残置 | medium | mitigate | `_dispose_state` を保持キーまで走査するよう拡張し（Task 1 (5-a)）、`_pop_page_recovery` の pop による所有権移譲で二重解放も同時に防ぐ。`_clear_undo_stacks` 後の tempfile 数ゼロを機械検証（Task 3 (4)） |
| T-01-27 | Denial of Service | 復元ループの例外経路で一時 `fitz.Document` が未クローズのまま積み上がる（WR-04）。継続的な失敗下で Undo/Redo を連打すると増え続ける | medium | mitigate | 対象 7 箇所を `finally` で保護し、AST ガードテストで恒久的に固定する（Task 2） |
| T-01-28 | Denial of Service | 復旧用 Blob 保持による Undo スタックのメモリ / ディスク増 | low | accept | 保持されるのは「復旧不能な中間失敗が起きたページ」のみで、再試行完了時に解放される。`MAX_UNDO = 20` と `UndoBlobStore` の 64KiB 以上ディスク退避により従来と同じ上限に収まる。`tests/test_undo_stress.py::TestUndoRedoStress::test_memory_and_blob_invariants` の既存上限アサーションを verify ゲートに含める |
| T-01-SC | Tampering | npm / pip / cargo installs | low | accept | 本プランは新規パッケージを一切導入しない（01-RESEARCH.md「Package Legitimacy Audit」＝対象パッケージなし・`[SLOP]` / `[SUS]` / `[ASSUMED]` いずれもゼロ）。`requirements.txt` / `pyproject.toml` は変更しない |

ASVS L1 / block-on-high 基準に照らし、`critical` / `high` の 4 件はすべて `mitigate` として本プラン内にタスクが割り当てられているため、ブロッキング脅威は残っていない。
</threat_model>

<verification>
- `pytest tests/test_pdf_ops.py tests/test_undo_stress.py tests/test_lang_parity.py -q` が exit 0
- `pytest tests/test_pdf_ops.py::TestUndoRedoRestoreFailure -q` が exit 0 で、本プランで追加した 4 メソッドと 01-04〜01-06 由来の既存 11 メソッドがすべて収集・実行される
- `pytest tests/test_pdf_ops.py::TestTempDocumentCloseGuard -q` が exit 0
- `pytest tests/test_pdf_ops.py::TestAllOpsUndoRedoRoundtrip tests/test_pdf_ops.py::TestInsertRollback tests/test_pdf_ops.py::TestDuplicateUndoTiming -q` が exit 0（V190-UNDO-02・V190-SAFE-04/05 の非退行）
- `pytest tests/test_password.py tests/test_ocr.py tests/test_provider_ui.py -q` が exit 0（V190-SAFE-01/02/03・V190-CFG-01/02 の非退行）
- `pytest -q`（フルスイート）が exit 0。既知の Tcl/Tk フレーキーが出た場合は該当ファイル単体の再実行 green を SUMMARY に記録する
- `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
- `grep -c 'content_at_risk' pagefolio/file_ops.py` の値が 5 以上
- `grep -c 'finally:' pagefolio/file_ops.py` の値が 8 以上
- `git diff --stat pagefolio/constants.py 開発履歴.md README.md` の出力が空
- `git status --short .planning/phases/01-safety-rollback/` に `01-01-PLAN.md`〜`01-06-PLAN.md` の変更が現れない（既存プランは不可侵）
</verification>

<success_criteria>
1. 01-VERIFICATION.md Evidence B の再現手順（4 ページ doc・`targets=[0,1]`・`app.doc` の 2 回目 mutation で失敗注入）で、障害解除後の undo 再試行が完了したあとの doc が 4 ページ・digest 列が page_edit 実行前と完全一致する（欠陥時は 3 ページで `Page 3` が通知なく消失）
2. ロールバックにも失敗する注入下では専用の強い警告（`err_undo_restore_failed_content_at_risk` / `err_redo_restore_failed_content_at_risk`）が messagebox でブロッキング表示され、サイレント完了が起きない
3. 復旧不能経路を経由しても、障害解除後の再試行で doc が完全復旧し、その後の redo → undo 往復まで digest 列が一致する（逆デルタの完全性が保たれる）
4. `_restore_state` 周辺で `tmp` として開かれる一時 `fitz.Document` 全 7 箇所が `finally` で保護され、AST ガードテストで恒久的に固定されている（WR-04）
5. `insert`（base op）の削除ループが部分適用保護に乗り、部分失敗 → 再試行で挿入対象外の既存ページを過剰削除しない（WR-05）
6. 保持・蓄積されたすべての Blob が evict / redo クリア / 消費 / クローズ経路で確実に一度だけ解放され、`_clear_undo_stacks()` 後の一時ファイル数が 0 になる（D-14）
7. cb5344e / 01-06 が閉じた既存の保護（部分適用保護・逆デルタ蓄積方式・D-13 のブロッキング通知）が退行していない
8. `ruff check .` / `ruff format --check .` / `pytest -q` の 3 ゲートがすべて green
</success_criteria>

<output>
Create `.planning/phases/01-safety-rollback/01-07-SUMMARY.md` when done
</output>
