---
phase: 01-safety-rollback
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - tests/test_pdf_ops.py
autonomous: true
requirements:
  - V190-UNDO-02

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "duplicate op について do→undo→redo→undo の 4 手往復でページ数とページ digest 列が操作前と一致する（V190-UNDO-02）"
    - "merge op について do→undo→redo→undo の 4 手往復でページ数とページ digest 列が操作前と一致する（V190-UNDO-02）"
    - "merge_resize op について do→undo→redo→undo の 4 手往復でページ数とページ digest 列が操作前と一致する（V190-UNDO-02）"
    - "Undo 記録が実処理より先に置かれたままの op 一覧が棚卸しされ、SUMMARY に記録される（D-12・次マイルストーン候補として残す）"
    - "1 ページのみの Document に対する duplicate でも 4 手往復でページ構成が一致する（probe: V190-UNDO-02 / boundary）"
    - "先頭（index 0）および末尾（index len-1）に隣接する位置への merge 挿入でも、4 手往復後のページ順序が操作前と一致する（probe: V190-UNDO-02 / adjacency）"
    - "Undo スタックが空・Document が 1 ページのみ・マージ対象ファイルが 1 件のいずれの入力でも、4 手往復テストのアサーションが成立する（probe: V190-UNDO-02 / empty）"
    - "merge_resize の 4 手往復後、各ページの digest 列が操作前と同順で一致する（要素の順序が入れ替わらない）（probe: V190-UNDO-02 / ordering）"
    - statement: "merge_resize の 4 手往復後、元から存在していたページの MediaBox 幅・高さが操作前と一致する（リサイズ処理の丸め誤差が既存ページへ波及しない）（probe: V190-UNDO-02 / precision）"
      verification: backstop
  prohibitions:
    - statement: "D-12 の棚卸しを理由に、rotate / crop / delete / bulk_move / bulk_crop / merge 等への「記録後置」パターンの水平展開を本フェーズで実施してはならない。棚卸しは一覧の記録までに留める（CONTEXT.md Deferred Ideas・変更面の意図的な抑制）"
      category: values
      requirement_id: V190-UNDO-02
      status: resolved
      verification: test
    - statement: "Document 全体スナップショット（doc.tobytes() 全文）方式のロールバックを導入してはならない。往復テストは既存の op 別逆デルタ設計をそのまま検証する（REQUIREMENTS.md Out of Scope・BUG-02 / V180-D-17）"
      category: values
      requirement_id: V190-UNDO-02
      status: resolved
      verification: test
  artifacts:
    - path: "tests/test_pdf_ops.py"
      provides: "duplicate / merge / merge_resize の 4 手往復回帰テストと境界・隣接エッジ"
      contains: "undo_redo_undo_roundtrip"
    - path: ".planning/phases/01-safety-rollback/01-05-SUMMARY.md"
      provides: "D-12 の棚卸し一覧（Undo 記録が先置きのままの op 一覧）"
      contains: "_save_undo"
  key_links:
    - from: "tests/test_pdf_ops.py:TestAllOpsUndoRedoRoundtrip"
      to: "pagefolio/file_ops.py:_restore_state"
      via: "4 手目の undo で insert_redo / delete_redo 系の逆デルタ対称性を検証（v1.8.0 D-17 の水平展開）"
      pattern: "undo_redo_undo_roundtrip"
    - from: "tests/test_pdf_ops.py:_page_digest"
      to: "fitz.Page"
      via: "ページ内容ハッシュによる往復前後の同一性比較（ページ数一致だけでは検出できない内容の入れ替わりを捕捉）"
      pattern: "_page_digest"
---

<objective>
`duplicate` / `merge` / `merge_resize` の各 op について do→undo→redo→undo の 4 手往復回帰テストを整備し、v1.8.0 Phase 6 で `insert` に対してのみ行われた対称性検証（D-17）を水平展開する。既存 `tests/test_pdf_ops.py` の該当テストは do→undo→redo の 3 手で止まっており、2 回目の undo で起こる非対称復元（v1.8.0 で `insert_redo` に実在したページ重複バグと同型）を検出できない。あわせて D-12 に従い「Undo 記録が実処理より先に置かれたままの op 一覧」を棚卸しして SUMMARY へ記録し、次マイルストーンの候補として残す。

Purpose: Core Value（大きな PDF でも Undo/Redo が正しく・速く動作する）の直接の安全網。`.planning/codebase/CONCERNS.md`「Missing Critical Features」が挙げ、STATE.md の Blockers/Concerns で v1.9.0 Phase 1 へ持ち越された項目の正式消化。
Output: 4 手往復テスト 3 件 + 境界・隣接エッジテスト 2 件、`merge_resize` の寸法検証、D-12 の棚卸し一覧。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-rollback/01-CONTEXT.md
@.planning/phases/01-safety-rollback/01-RESEARCH.md
@.planning/phases/01-safety-rollback/01-PATTERNS.md
@.planning/phases/01-safety-rollback/01-04-SUMMARY.md
@CLAUDE.md
@pagefolio/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: duplicate / merge / merge_resize の 4 手往復テスト追加（V190-UNDO-02）</name>
  <precondition>Plan 04 の `_undo` / `_redo` 例外保護が main へコミット済みであり、`pytest tests/test_pdf_ops.py -q` が exit 0 である（4 手往復テストは修正後の `_undo` / `_redo` を対象に書くため）</precondition>
  <files>tests/test_pdf_ops.py</files>
  <read_first>
    - `tests/test_pdf_ops.py` の `TestInsertUndoRedo::test_insert_undo_redo_undo_roundtrip`（758-802 行目）— 本タスクの雛形。do → undo → redo → 2 回目の undo という 4 手構成と、ページ数・digest 列の比較の書き方
    - `tests/test_pdf_ops.py` の `_page_digest`（628 行目付近）
    - `tests/test_pdf_ops.py` の `TestAllOpsUndoRedoRoundtrip`（808 行目〜）の `_make_fake_app`（812 行目）と、拡張対象の 3 テスト: `test_duplicate_roundtrip`（960-986 行目）・`test_merge_roundtrip`（988-1011 行目）・`test_merge_resize_roundtrip`（1107-1170 行目）— いずれも現状 do→undo→redo の 3 手で止まっている
    - `pagefolio/file_ops.py` の `_save_undo`（132-173 行目）— `duplicate` は `state["data"] = pno`、`merge` は `state["data"] = len(self.doc)`、`merge_resize` は `{"insert_at", "merged_bytes", "orig_pages"}` の dict
    - `pagefolio/file_ops.py` の `_restore_state`（370-440 行目付近）— 各 op の undo / redo 実装と、v1.8.0 D-17 で修正された `insert_redo` の降順 `delete_page` 対称パターン
    - `tests/conftest.py`（`sample_pdf_doc` / `multi_pdf_files` フィクスチャ）
  </read_first>
  <action>
`tests/test_pdf_ops.py` の `TestAllOpsUndoRedoRoundtrip` へ、既存 3 テストと同型・同フィクスチャの 4 手往復テストを 3 件追加する。既存の 3 手テスト（`test_duplicate_roundtrip` / `test_merge_roundtrip` / `test_merge_resize_roundtrip`）は削除も改変もせず、そのまま残す（3 手の非回帰を保ったまま 4 手目の検証を上乗せする）。

追加するテスト:
- `test_duplicate_undo_redo_undo_roundtrip`: `sample_pdf_doc` から `_make_fake_app` を作り、操作前のページ数と全ページの digest 列を記録する。`_duplicate_page()` 相当の do（既存 `test_duplicate_roundtrip` と同じ駆動方法を踏襲する）→ `app._undo()`（ページ数が元に戻る）→ `app._redo()`（ページ数が +1 になる）→ `app._undo()`（4 手目）の順に呼び、最終状態のページ数と digest 列が操作前と要素単位で一致することを検証する。
- `test_merge_undo_redo_undo_roundtrip`: `multi_pdf_files` を使い `merge` op に対して同型の 4 手往復を行う。`merge` op の Undo state は結合前のページ数（整数）なので、4 手目の undo 後にページ数が結合前の値へ戻ることと digest 列が一致することを検証する。
- `test_merge_resize_undo_redo_undo_roundtrip`: 既存 `test_merge_resize_roundtrip` と同じセットアップ（`merge_resize` op の `orig_pages` Blob を含む state 構築）を踏襲し、4 手目の undo 後にページ数・digest 列が操作前と一致することを検証する。

3 件すべてで、比較は「ページ数」だけでなく「全ページの digest 列」で行う（ページ数だけの比較では、v1.8.0 で `insert_redo` に実在したような内容の入れ替わり・重複を検出できないため）。Document は明示的に `close()` する。テスト追加後に `ruff format .` と `ruff check .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py -x -q -k "AllOpsUndoRedoRoundtrip"</automated>
  </verify>
  <acceptance_criteria>
    - Test: `pytest tests/test_pdf_ops.py::TestAllOpsUndoRedoRoundtrip -q` が exit 0 で、`test_duplicate_undo_redo_undo_roundtrip` / `test_merge_undo_redo_undo_roundtrip` / `test_merge_resize_undo_redo_undo_roundtrip` の 3 メソッドが収集・実行される
    - Source: `grep -c 'undo_redo_undo_roundtrip' tests/test_pdf_ops.py` が 4 以上を返す（既存の insert 版 1 件 + 新規 3 件）
    - Source: 既存の `test_duplicate_roundtrip` / `test_merge_roundtrip` / `test_merge_resize_roundtrip` が `grep -n 'def test_duplicate_roundtrip\|def test_merge_roundtrip\|def test_merge_resize_roundtrip' tests/test_pdf_ops.py` で 3 件とも出力される（既存 3 手テストの非削除）
    - Behavior: 3 op すべてで 4 手目の undo 完了後、`len(app.doc)` が操作前の値と等しく、`[_page_digest(app.doc[i]) for i in range(len(app.doc))]` が操作前の digest 列とリスト比較で等しい
    - Test: `pytest tests/test_pdf_ops.py tests/test_undo_stress.py -q` が exit 0
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>duplicate / merge / merge_resize の 3 op について、2 回目の undo でページ構成が壊れないことが digest 比較で機械的に固定されている。</done>
</task>

<task type="auto">
  <name>Task 2: 境界・隣接・順序・精度エッジの往復検証（probe 由来 5 件）</name>
  <files>tests/test_pdf_ops.py</files>
  <read_first>
    - `tests/test_pdf_ops.py` の Task 1 で追加した 3 件の 4 手往復テスト
    - `tests/test_pdf_ops.py` の `_page_digest`（628 行目付近）と `TestAllOpsUndoRedoRoundtrip._make_fake_app`（812 行目）
    - `tests/conftest.py`（`sample_pdf_doc` のページ数と、1 ページだけの Document を作る方法）
    - `pagefolio/page_ops.py` の `_do_merge`（803 行目付近〜）と `_do_merge_resize`（862 行目付近）— 挿入位置の決まり方
    - `pagefolio/file_ops.py` の `_restore_state` の merge / merge_resize 分岐 — ページ寸法（MediaBox）の扱い
  </read_first>
  <action>
`TestAllOpsUndoRedoRoundtrip` へ、スペックレス・プローブが挙げた V190-UNDO-02 のエッジを表現するテストを追加する。既存フィクスチャとヘルパーを再利用し、新しいテスト基盤は導入しない。

- `test_duplicate_single_page_doc_roundtrip`（probe: boundary / empty）: 1 ページだけの Document を作り、その唯一のページに対して duplicate の 4 手往復を行う。最小境界（複製対象の前後にページが存在しない）でもページ数と digest 列が操作前と一致することを検証する。
- `test_merge_head_and_tail_adjacent_roundtrip`（probe: adjacency）: 先頭（index 0）へのマージと末尾（index len-1 の直後）へのマージをそれぞれ 4 手往復させ、隣接位置でもページ**順序**（digest 列の順番）が操作前と完全に一致することを検証する。ページ数だけでなく順序を見ることが本テストの主眼である。
- `test_merge_resize_preserves_original_page_order`（probe: ordering）: `merge_resize` の 4 手往復後、元から存在していたページの digest 列が操作前と**同順**で一致することを検証する。集合として一致するだけでは不十分であり、リストとしての比較を行う。
- `test_merge_resize_preserves_original_page_dimensions`（probe: precision）: `merge_resize` の 4 手往復後、元から存在していたページの MediaBox の幅・高さが操作前と一致することを `pytest.approx` で検証する。リサイズ処理の丸め誤差が既存ページへ波及していないことを確認する。
- `test_roundtrip_with_single_merge_source_file`（probe: empty）: マージ対象ファイルが 1 件だけの最小入力で 4 手往復が成立することを検証する。

Document は明示的に `close()` する。テスト追加後に `ruff format .` と `ruff check .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_pdf_ops.py -x -q -k "AllOpsUndoRedoRoundtrip"</automated>
  </verify>
  <acceptance_criteria>
    - Test: `pytest tests/test_pdf_ops.py::TestAllOpsUndoRedoRoundtrip -q` が exit 0 で、`test_duplicate_single_page_doc_roundtrip` / `test_merge_head_and_tail_adjacent_roundtrip` / `test_merge_resize_preserves_original_page_order` / `test_merge_resize_preserves_original_page_dimensions` / `test_roundtrip_with_single_merge_source_file` の 5 メソッドが収集・実行される
    - Behavior: 1 ページのみの Document に対する duplicate の 4 手往復後、ページ数が 1 に戻り digest が操作前と一致する
    - Behavior: 先頭マージと末尾マージのどちらでも、4 手往復後の digest 列が操作前の digest 列とリスト比較（順序込み）で等しい
    - Behavior: `merge_resize` の 4 手往復後、元ページの MediaBox 幅・高さが `pytest.approx` の既定許容誤差の範囲で操作前と一致する
    - Test: `pytest tests/test_pdf_ops.py tests/test_undo_stress.py tests/test_v150_regression.py -q` が exit 0
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>境界（単一ページ・単一ファイル）・隣接（先頭/末尾）・順序・寸法精度の各エッジが往復テストで表現されている。</done>
</task>

<task type="auto">
  <name>Task 3: D-12 の棚卸し一覧作成とフェーズ最終ゲート</name>
  <files>.planning/phases/01-safety-rollback/01-05-SUMMARY.md</files>
  <read_first>
    - `pagefolio/page_ops.py`（`_save_undo` の全呼び出し箇所とその前後の制御フロー。`grep -n '_save_undo(' pagefolio/page_ops.py` で 12 箇所）
    - `pagefolio/dnd.py`（`grep -n '_save_undo(' pagefolio/dnd.py` で 2 箇所: `bulk_move` / `move`）
    - `pagefolio/redact_ops.py`（`grep -n '_save_undo(' pagefolio/redact_ops.py` で 1 箇所: `page_edit`）
    - `pagefolio/file_ops.py` の `_save_undo`（132-173 行目）— 各 op が Blob キャプチャを伴うか否か（キャプチャを伴う op は記録タイミングの移動が単純でないため、棚卸し時に区別して記録する）
    - `.planning/phases/01-safety-rollback/01-CONTEXT.md` の D-12 と `<deferred>` セクション（水平展開は次マイルストーン候補として残す方針）
    - `.planning/phases/01-safety-rollback/01-04-SUMMARY.md`（Plan 04 で記録した grep 結果）
    - `開発履歴.md` の先頭 30 行（エントリが常に `APP_VERSION = vX.Y.Z` を伴うリリース単位の書式であることの確認）
  </read_first>
  <action>
(1) D-12 の棚卸しを行う。`grep -n '_save_undo(' pagefolio/*.py` で全 15 箇所（`page_ops.py` 12 / `dnd.py` 2 / `redact_ops.py` 1）を列挙し、各呼び出しについて「op 名」「ファイル:行」「実処理の前に記録しているか後に記録しているか」「Blob キャプチャを伴うか」「先置きのままの場合に起こり得る不正な Undo の内容」を表にまとめ、`01-05-SUMMARY.md` の「D-12 棚卸し: Undo 記録が先置きのままの op 一覧」節へ記録する。本フェーズで後置化した `duplicate`（Plan 04）と、もともと巻き戻し実装を持つ `insert`（Plan 04）は「対応済み」として区別して記載する。

(2) 水平展開は行わない。rotate / crop / delete / bulk_move / bulk_crop / merge / page_edit などへの「記録後置」パターンの適用と、共通コンテキストマネージャの新設は CONTEXT.md の Deferred Ideas であり、本フェーズのスコープ外である。棚卸し表の末尾に「次マイルストーン候補」として明記し、コードは一切変更しない。

(3) `開発履歴.md` と `APP_VERSION` の扱いを SUMMARY へ明記する。`開発履歴.md` の既存エントリはすべて `APP_VERSION = vX.Y.Z` を伴うリリース単位の書式であり、マイルストーン途中の Phase 1 完了時点で追記するとこの書式が崩れる。したがって `開発履歴.md` への v1.9.0 エントリ追記と `pagefolio/constants.py` の `APP_VERSION` 更新は Phase 3（V190-QA-03 のリリースゲート）で行う、という判断とその根拠を SUMMARY へ記録する。本タスクではどちらのファイルも変更しない。

(4) フェーズ最終ゲートを実行する。`ruff format .` → `ruff check .` → `pytest -q`（フルスイート）を順に走らせ、結果を SUMMARY へ記録する。Tcl/Tk 由来の既知フレーキー（STATE.md Blockers 参照: 単一 pytest プロセスでの大量 `tk.Tk()` 生成による `_tkinter.TclError`）で ERROR が出た場合は、該当テストファイルを単体で再実行して green を確認し、フレーキーであった旨と再実行結果を SUMMARY へ記録する（V190-QA-01 として Phase 3 が引き取る）。
  </action>
  <verify>
    <automated>pytest -q &amp;&amp; ruff check . &amp;&amp; ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `grep -c '_save_undo(' pagefolio/page_ops.py pagefolio/dnd.py pagefolio/redact_ops.py` の合計が SUMMARY の棚卸し表の行数と一致する
    - Source: `.planning/phases/01-safety-rollback/01-05-SUMMARY.md` に「D-12 棚卸し」を含む見出しがあり、その表に `op` 列・ファイル:行 列・記録タイミング列・Blob キャプチャ有無列が存在する
    - Source: `git diff --stat 開発履歴.md pagefolio/constants.py` が空（Phase 3 へ委譲した旨が SUMMARY に記録されている）
    - Source: `git diff --stat pagefolio/` が空（本プランはテストとドキュメントのみ・プロダクションコード無変更）
    - Test: `pytest -q` が exit 0（フルスイート。フレーキー ERROR が出た場合は単体再実行で green を確認し SUMMARY へ記録）
    - Lint: `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
  </acceptance_criteria>
  <done>Undo 記録が先置きのままの op 一覧が SUMMARY に残り、水平展開が次マイルストーン候補として明示されている。フェーズ全体の lint / テストゲートが green である。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

本フェーズ（Plan 01〜05）が**新規に作成**するシンボル一覧。plan-review-convergence の source-grounding パスはこの表を読み、新規シンボルを drift 検証の対象から除外する。

| 種別 | シンボル | 配置 | Plan |
|---|---|---|---|
| 関数 | `derive_pdf_has_password` | `pagefolio/file_ops.py`（モジュールレベル純関数） | 01 |
| 例外クラス | `OCRDisabledError` | `pagefolio/ocr_providers/errors.py`（`__init__.py` から re-export） | 02 |
| メソッド | `_update_batch_menu_state` | `pagefolio/app.py` | 02 |
| 属性 | `self._tools_menu` / `self._batch_menu_index` | `pagefolio/app.py` | 02 |
| i18n キー | `ocr_disabled_msg` / `batch_menu_item_off` | `pagefolio/lang.py`（ja / en） | 02 |
| i18n キー | `warn_rollback_title` / `err_insert_rollback_failed` / `err_undo_restore_failed` / `err_redo_restore_failed` | `pagefolio/lang.py`（ja / en） | 04 |
| テストクラス | `TestSavePathsKeepEncryption` / `TestDerivePdfHasPassword` | `tests/test_password.py` | 01 |
| テストクラス | `TestOCRDisabledGuard` | `tests/test_ocr.py` | 02 |
| テストクラス | `TestApplyOnlyPromptFileWrite` / `TestUnsavedTemplateChangesSinglePath` | `tests/test_provider_ui.py` | 03 |
| テストクラス | `TestInsertRollback` / `TestDuplicateUndoTiming` / `TestUndoRedoRestoreFailure` | `tests/test_pdf_ops.py` | 04 |
| テストメソッド | `test_duplicate_undo_redo_undo_roundtrip` / `test_merge_undo_redo_undo_roundtrip` / `test_merge_resize_undo_redo_undo_roundtrip` / `test_duplicate_single_page_doc_roundtrip` / `test_merge_head_and_tail_adjacent_roundtrip` / `test_merge_resize_preserves_original_page_order` / `test_merge_resize_preserves_original_page_dimensions` / `test_roundtrip_with_single_merge_source_file`（既存 `TestAllOpsUndoRedoRoundtrip` へ追加） | `tests/test_pdf_ops.py` | 05 |
| ドキュメント節 | 「D-12 棚卸し: Undo 記録が先置きのままの op 一覧」 | `01-05-SUMMARY.md` | 05 |

Plan 01 の PLAN.md に完全版がある。上記以外のシンボルはすべて既存であり、本フェーズは内部実装のみを変更する（新規モジュール・新規ファイルはゼロ）。
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Undo/Redo の逆デルタ実装 → ユーザーのページ構成 | 逆デルタが非対称だと、往復のたびにページが重複・欠落し、ユーザーが気づかないまま構成が壊れる（v1.8.0 の `insert_redo` バグの実例） |
| 棚卸し記録 → 次マイルストーンの意思決定 | 先置き op の一覧が失われると、同型バグの再発リスクが可視化されないまま残る |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Tampering | `pagefolio/file_ops.py:_restore_state` の duplicate / merge / merge_resize 逆デルタ | medium | mitigate | 4 手往復テストを 3 op へ水平展開し、ページ数だけでなく digest 列（順序込み）で比較する（Task 1）。v1.8.0 で `insert_redo` に実在した非対称復元と同型のバグを検出可能にする |
| T-01-19 | Tampering | `merge_resize` のリサイズ処理によるページ寸法の丸め誤差波及 | low | mitigate | 4 手往復後に元ページの MediaBox 幅・高さを `pytest.approx` で検証する（Task 2） |
| T-01-20 | Repudiation | D-12 棚卸しの未記録 | low | mitigate | `_save_undo` 全 15 呼び出し元の記録タイミングを表として SUMMARY へ残し、次マイルストーン候補として明示する（Task 3） |
| T-01-21 | Denial of Service | フルスイート実行時の Tcl/Tk フレーキー ERROR | low | accept | STATE.md に記録済みの既知事象。Phase 3（V190-QA-01）が切り分け・修復を引き取る。本プランでは単体再実行で green を確認し、事象を SUMMARY へ記録するに留める |
| T-01-SC | Tampering | npm / pip / cargo installs | low | accept | 本フェーズは新規パッケージを一切導入しない（RESEARCH.md「Package Legitimacy Audit」＝対象パッケージなし） |
</threat_model>

<verification>
- `pytest tests/test_pdf_ops.py tests/test_undo_stress.py tests/test_v150_regression.py -q` が exit 0
- `pytest -q`（フルスイート）が exit 0
- `ruff check .` が exit 0 かつ `ruff format --check .` が exit 0
- `grep -c 'undo_redo_undo_roundtrip' tests/test_pdf_ops.py` が 4 以上
- `git diff --stat pagefolio/` が空（本プランはプロダクションコードを変更しない）
</verification>

<success_criteria>
1. `duplicate` / `merge` / `merge_resize` の各 op で do→undo→redo→undo の 4 手往復テストがページ構成の一致（ページ数 + digest 列の順序込み比較）を担保する（V190-UNDO-02）
2. 既存の 3 手往復テスト 3 件が削除・改変されずに green のまま残っている
3. 境界（単一ページ・単一ファイル）・隣接（先頭/末尾）・順序・寸法精度のエッジがテストで表現されている
4. Undo 記録が先置きのままの op 一覧が SUMMARY へ記録され、水平展開は次マイルストーン候補として明示されている（D-12）
5. フェーズ全体で `ruff check .` / `ruff format --check .` / `pytest -q` の 3 ゲートが green
</success_criteria>

<output>
Create `.planning/phases/01-safety-rollback/01-05-SUMMARY.md` when done
</output>
