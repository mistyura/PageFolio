---
phase: 05-claude-provider-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/settings.py
  - pagefolio/lang.py
  - tests/test_settings_keyguard.py
autonomous: true
requirements: [OCR-SEC-01, OCR-UI-01]
must_haves:
  truths:
    - "_save_settings が機密キー（claude_api_key 等）を JSON へ書き込まない（成功基準1・最優先）"
    - "保存後の pagefolio_settings.json に API キー相当のフィールドが一切現れない（成功基準1）"
    - "DEFAULT_SETTINGS に claude_model / ocr_effort（無害な設定値のみ）が含まれる（OCR-UI-01 基盤）"
    - "Phase 5 で必要な日英文言（キー未設定エラー・コスト注記・セッションキーラベル・待機中・provider/effort ラベル）が ja/en 両方に存在する"
  artifacts:
    - path: "pagefolio/settings.py"
      provides: "_save_settings の機密キーガード・_SENSITIVE_KEYS 定数・DEFAULT 追加"
      contains: "_SENSITIVE_KEYS"
    - path: "pagefolio/lang.py"
      provides: "Phase 5 最小文言（ja/en）"
      contains: "ocr_api_key_missing"
    - path: "tests/test_settings_keyguard.py"
      provides: "キー非永続化の機械的検証テスト"
      contains: "_SENSITIVE_KEYS"
  key_links:
    - from: "_save_settings"
      to: "pagefolio_settings.json"
      via: "機密キー除去後の dict のみを json.dump"
      pattern: "_SENSITIVE_KEYS"
---

<objective>
APIキー漏洩防止の構造的ガード（成功基準1・最優先）を `pagefolio/settings.py` に実装し、Phase 5 で UI/ダイアログが消費する日英文言を `pagefolio/lang.py` に最小追加する。`_save_settings` は `settings` 辞書をそのまま JSON 化するため、機密キーが万一 settings に混入しても書き込まないガードを設ける（D-01 の構造的ガードの最後の砦）。

すべての記述・docstring・コメント・文言・コミットメッセージは日本語で書くこと（変数名・関数名・コマンドは英語のまま。en 文言は英語）。

Purpose: 成功基準1「pagefolio_settings.json に API キー相当のフィールドが一切書き込まれない」を機械的に検証可能な状態で達成する。あわせて UI プラン（05-04/05-05）が参照する文言キーを先行確定し、wave 依存を最小化する。
Output: `_SENSITIVE_KEYS` 定数・`_save_settings` のガード・DEFAULT 追加・Phase 5 文言（ja/en）・キー非永続化テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces（このプランで新規作成するシンボル）

- `_SENSITIVE_KEYS` — `pagefolio/settings.py` のモジュール定数（set）。機密キー名集合 `{"claude_api_key", "gemini_api_key", "anthropic_api_key", "api_key"}`。
- `_save_settings` のガード処理（既存関数の改修）— 保存前に `_SENSITIVE_KEYS` を dict から除外。
- DEFAULT_SETTINGS への新規キー: `claude_model`（既定 `"claude-sonnet-4-6"`）・`ocr_effort`（既定 `"low"`）。**これらは API キーではない無害な設定値**。
- `pagefolio/lang.py` 新規文言キー（ja/en 両方）:
  - `ocr_api_key_missing` — キー未設定エラー（{env_var} を含む）
  - `ocr_session_key_label` — セッションキー入力欄ラベル
  - `ocr_cost_confirm_title` — コスト確認ダイアログタイトル
  - `ocr_cost_confirm_msg` — コスト確認本文（{host} / {count} / {cost} を含む）
  - `ocr_waiting_retry` — 「待機中（リトライ {n}/{max}）」
  - `ocr_provider_label` — provider ドロップダウンラベル
  - `ocr_effort_label` — effort 選択欄ラベル
  - `ocr_provider_off_hint` — off 時の OCR ボタン誘導文言
  - `ocr_model_refresh` — 「モデル更新」ボタンラベル
- `tests/test_settings_keyguard.py`（新規ファイル）
</artifacts_produced>

<read_first>
**触る前に必ず読むこと:**
- `pagefolio/settings.py` — 変更対象。`_load_settings`（defaults dict・`ocr_provider: "off"` 既定）・`_save_settings`（settings をそのまま json.dump する箇所が漏洩の急所）。
- `.planning/phases/05-claude-provider-ui/05-PATTERNS.md` §`pagefolio/settings.py` — _save_settings のガードパターン抜粋・DEFAULT 追加箇所・§`pagefolio/lang.py` の追加キー表。
- `pagefolio/lang.py` — 変更対象。`ocr_provider_unsupported`（行 288 付近・ja）/ 同（行 616 付近・en）・`ocr_text_skip_notice`・`ocr_models_*` の既存スタイル。ja 辞書と en 辞書の両方に同一キーを追加する。
- `.planning/research/PITFALLS.md` Pitfall 1（APIキー平文漏洩）・Security Mistakes 表（logger に settings dict を渡さない・キーを repr しない）。
- `.planning/phases/05-claude-provider-ui/05-CONTEXT.md` D-01/D-04/D-05（キー非永続化の設計・マスク・ログ非出力）。
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _save_settings 機密キーガードと DEFAULT_SETTINGS 追加</name>
  <files>pagefolio/settings.py, tests/test_settings_keyguard.py</files>
  <read_first>
    - pagefolio/settings.py（_load_settings の defaults・_save_settings 本体・_get_settings_path）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §settings.py（ガードパターン抜粋）
    - .planning/research/PITFALLS.md Pitfall 1 / Security Mistakes
  </read_first>
  <behavior>
    - Test: _save_settings に claude_api_key を含む dict を渡しても、保存された JSON ファイルに "claude_api_key" 文字列が現れない（成功基準1）
    - Test: 保存された JSON に "anthropic_api_key" / "gemini_api_key" / "api_key" のいずれも現れない
    - Test: _save_settings は機密キー以外（theme / ocr_provider 等）は通常どおり保存する
    - Test: 入力 settings dict 自体は破壊的変更されない（除去は保存用コピーに対してのみ行う）
    - Test: _load_settings() の戻り値に "claude_model"=="claude-sonnet-4-6" と "ocr_effort"=="low" が含まれる
  </behavior>
  <action>
    `pagefolio/settings.py` のモジュールレベルに `_SENSITIVE_KEYS = {"claude_api_key", "gemini_api_key", "anthropic_api_key", "api_key"}` を定義する（D-01・Pitfall 1）。
    `_save_settings(settings)` の冒頭で、`_SENSITIVE_KEYS` のいずれかが `settings` に含まれる場合は `logger.error` で警告を出し（**キー値そのものはログに出さない**・名前のみ）、`{k: v for k, v in settings.items() if k not in _SENSITIVE_KEYS}` の除外済みコピーを作って json.dump する。混入がなければ従来どおり保存する。除外コピーを保存対象にすることで入力 dict の破壊的変更を避ける。テスト容易性のため `_get_settings_path()` を使う既存構造は維持する（テストは monkeypatch でパスを差し替える）。
    `_load_settings()` の defaults dict に `"claude_model": "claude-sonnet-4-6"`（STACK.md 推奨・OCR メイン）と `"ocr_effort": "low"`（OCR 用途の既定・D-17）を追加する。これらは API キーではない無害な設定値である。
    新規テストファイル `tests/test_settings_keyguard.py` を作成し、上記 behavior を monkeypatch（tmp_path に設定ファイルパスを向ける）で検証する。`tests/**` は S101 除外のため assert を使ってよい。
    `<action>` 内に完全なコード本体は書かない。read_first の PATTERNS 抜粋を参照すること。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; python -m pytest tests/test_settings_keyguard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - source: `pagefolio/settings.py` に `_SENSITIVE_KEYS` 定数が存在し、claude_api_key/gemini_api_key/anthropic_api_key/api_key を含む
    - behavior: claude_api_key を入れた dict を _save_settings に渡し、保存ファイルを読み込むと "claude_api_key" が含まれない（成功基準1）
    - behavior: `_load_settings()["claude_model"] == "claude-sonnet-4-6"` かつ `_load_settings()["ocr_effort"] == "low"`
    - test: `python -m pytest tests/test_settings_keyguard.py -x -q` が exit 0
  </acceptance_criteria>
  <done>機密キーが構造的に永続化されないガードが実装され、保存 JSON にキー文字列が現れないことがテストで機械的に保証される（成功基準1 の核心）。</done>
</task>

<task type="auto">
  <name>Task 2: Phase 5 最小文言を lang.py の ja / en 両辞書に追加</name>
  <files>pagefolio/lang.py</files>
  <read_first>
    - pagefolio/lang.py（ja 辞書の ocr_provider_unsupported / ocr_text_skip_notice 付近・en 辞書の同キー付近。両辞書の構造）
    - .planning/phases/05-claude-provider-ui/05-PATTERNS.md §lang.py（追加キー表）
    - .planning/phases/05-claude-provider-ui/05-CONTEXT.md D-12（プライバシー注記3点）/ D-06（キー未設定エラー）/ D-15（待機中）/ D-17（effort ラベル）
  </read_first>
  <action>
    `pagefolio/lang.py` の ja 辞書と en 辞書の **両方** に Phase 5 文言キーを追加する（既存 `ocr_provider_unsupported` と同じスタイル）。本格的な多言語整備は Phase 7 のため、ここは Phase 5 機能が参照する最小キーのみ。
    追加キー（ja は日本語・en は英語）:
    - `ocr_api_key_missing`: 「APIキーが設定されていません（{env_var}）。環境変数を設定するか、入力欄にキーを入力してください。」（D-06・{env_var} プレースホルダ）
    - `ocr_session_key_label`: 「APIキー（このセッションのみ・保存されません）:」（D-03/D-04 マスク入力欄ラベル）
    - `ocr_cost_confirm_title`: 「クラウド送信の確認」
    - `ocr_cost_confirm_msg`: 送信先ホスト {host}・対象 {count} ページ・概算コスト {cost}・「ページ画像が外部 API に送信されます」「従量課金が発生します」の3点を含む本文（D-12 の3点必須）
    - `ocr_waiting_retry`: 「p.{page}: レート制限のため待機中（リトライ {n}/{max}）」（D-15）
    - `ocr_provider_label`: 「OCR プロバイダ:」（D-07）
    - `ocr_effort_label`: 「推論強度 (effort):」（D-17）
    - `ocr_provider_off_hint`: 「OCR は無効です。設定でプロバイダを選択してください。」（D-09）
    - `ocr_model_refresh`: 「モデル更新」（D-08 のモデル更新ボタン）
    en 辞書には同じキーで英語訳を追加する（例: `ocr_provider_off_hint` → "OCR is disabled. Select a provider in Settings."）。プレースホルダ名（{env_var}/{host}/{count}/{cost}/{page}/{n}/{max}）は ja/en で同一に保つ。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; python -c "from pagefolio.constants import LANG; ks=['ocr_api_key_missing','ocr_session_key_label','ocr_cost_confirm_title','ocr_cost_confirm_msg','ocr_waiting_retry','ocr_provider_label','ocr_effort_label','ocr_provider_off_hint','ocr_model_refresh']; assert all(k in LANG['ja'] and k in LANG['en'] for k in ks), 'missing keys'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - source: `pagefolio/lang.py` に上記 9 キーが ja 辞書・en 辞書の両方に存在する
    - behavior: `LANG["ja"]["ocr_api_key_missing"].format(env_var="ANTHROPIC_API_KEY")` が KeyError を出さず {env_var} を展開する
    - behavior: `LANG["ja"]["ocr_cost_confirm_msg"].format(host="api.anthropic.com", count=3, cost="$0.05")` が 3 プレースホルダを展開する（D-12）
    - test: 上記 verify ワンライナーが "OK" を出力して exit 0
  </acceptance_criteria>
  <done>Phase 5 機能が参照する全文言が ja/en 両対応で追加され、プレースホルダが正しく展開できる。</done>
</task>

<task type="auto">
  <name>Task 3: ruff・全テスト・構文確認でグリーンを確定</name>
  <files>pagefolio/settings.py, pagefolio/lang.py, tests/test_settings_keyguard.py</files>
  <read_first>
    - CLAUDE.md（コーディング規約・禁止事項: 裸 except 禁止・ruff/pytest 必須）
    - pyproject.toml（ruff ルール・line-length 88。編集禁止）
  </read_first>
  <action>
    `ruff check . ; ruff format .` を実行し、settings.py / lang.py / 新規テストの lint/format を通す。裸の except は使わず `except Exception as e:` の形を維持する。Phase 5 の wave 2/3 プランがこの settings ガードと文言に依存するため、ここで完全にグリーンにして確定する。
  </action>
  <verify>
    <automated>cd C:\Users\shdwf\work\project\PageFolio; ruff check . ; python -m pytest tests/test_settings_keyguard.py tests/test_imports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test: `ruff check .` が exit 0
    - test: `python -m pytest tests/test_settings_keyguard.py tests/test_imports.py -q` が exit 0
    - source: `pagefolio/settings.py` に裸の `except:` が存在しない
  </acceptance_criteria>
  <done>ruff・pytest が完全グリーン。settings キーガードと文言が後続プランから利用可能な確定状態になる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings 辞書 → pagefolio_settings.json（ディスク） | 機密キーが平文でファイルへ流出しうる最大の信頼境界 |
| settings 辞書 → logger 出力 | キー値がログへ二次流出しうる経路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | _save_settings の JSON 書き込み | mitigate | `_SENSITIVE_KEYS` のキーを保存前に dict から除外（成功基準1・D-01）。保存後 JSON にキー文字列が現れないことをテストで機械的に保証 |
| T-05-06 | Information Disclosure | _save_settings の logger.error | mitigate | 機密キー混入時もキー値はログに出さず、キー名のみ警告（Pitfall 1 Security Mistakes・D-04） |
| T-05-07 | Tampering | DEFAULT_SETTINGS への新規キー | accept | claude_model / ocr_effort は API キーではない無害な設定値。永続化されても漏洩リスクなし |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本プランは新規 pip 依存ゼロ（標準ライブラリのみ）。パッケージインストールなし |
</threat_model>

<verification>
- 成功基準1（settings.json に API キー相当フィールドが一切書き込まれない）が _save_settings ガードと test_settings_keyguard.py で機械的に担保される。
- OCR-UI-01 の基盤（プロバイダ別設定値 claude_model / ocr_effort）が DEFAULT_SETTINGS に追加される。
- Phase 5 機能の文言が ja/en 両対応で揃い、後続 UI/ダイアログプランが参照可能になる。
</verification>

<success_criteria>
- `_save_settings` が機密キーを JSON へ書き込まず、保存ファイルにキー文字列が一切現れない（成功基準1）。
- `_load_settings()` に claude_model / ocr_effort（無害な設定値）が含まれる。
- Phase 5 文言 9 キーが ja/en 両辞書に存在しプレースホルダが展開できる。
- `ruff check .` と関連 pytest が exit 0。
</success_criteria>

<output>
Create `.planning/phases/05-claude-provider-ui/05-02-SUMMARY.md` when done
</output>
