---
phase: 06-gemini-provider
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - pagefolio/ocr_dialog.py
  - pagefolio/ocr.py
  - pagefolio/settings.py
  - pagefolio/dialogs/llm_config.py
  - tests/test_ocr.py
autonomous: true
gap_closure: true
requirements: [OCR-PERF-02, OCR-PERF-05, OCR-QA-01]
must_haves:
  truths:
    - "LM Studio で OCR を起動すると self.concurrency 本のワーカースレッドが起動し、実効並列度が concurrency 設定どおりになる（CR-01・OCR-PERF-02 の後方互換復元）"
    - "全ページ生産完了時およびキャンセル時に self.concurrency 本の終了シグナル None がキューへ送られ、全ワーカーがブロックせず終了する（CR-01）"
    - "複数ワーカーが共有する done 進捗カウンタが threading.Lock で保護され、競合更新で取りこぼし/二重計上が起きない"
    - "キャンセル時に _finish_cancelled が複数回呼ばれても OCR 結果テキストが二重挿入されない（CR-02 冪等ガード）"
    - "全ワーカー終了後に _render_results_ordered と完了/キャンセル処理が一度だけ実行される（多重 worker でも結果描画は 1 回）"
    - "Gemini（max_concurrency=1）では concurrency=1 となり挙動が変わらない（後方互換）"
    - "ocr_scale の例外フォールバック値が 1.5 に統一され D-11 の既定変更と整合する（WR-01）"
    - "executor.shutdown の cancel_futures が Python 3.8 でも TypeError を出さない（WR-02）"
    - "_SENSITIVE_KEYS が google_api_key / GOOGLE_API_KEY 系を含み、Gemini フォールバックキー名が平文保存されない（WR-03）"
  artifacts:
    - path: "pagefolio/ocr_dialog.py"
      provides: "_start_worker_thread の複数ワーカー起動・done カウンタの Lock 化・終了シグナル concurrency 本・_finish_cancelled/complete/error 冪等ガード・全ワーカー終了後の単一終了処理"
      contains: "for _ in range(self.concurrency)"
    - path: "pagefolio/ocr.py"
      provides: "executor.shutdown の Python 3.8 互換分岐・DEFAULT_OCR_SCALE 整合（WR-02/WR-01）"
      contains: "sys.version_info"
    - path: "pagefolio/settings.py"
      provides: "_SENSITIVE_KEYS への google_api_key / GOOGLE_API_KEY 追加（WR-03）"
      contains: "google_api_key"
    - path: "pagefolio/dialogs/llm_config.py"
      provides: "ocr_scale フォールバック既定を 1.5 へ統一（WR-01）"
      contains: "1.5"
    - path: "tests/test_ocr.py"
      provides: "並列度回帰テスト（起動スレッド数==concurrency・終了シグナル数==concurrency）と CR-02 冪等性テスト"
      contains: "TestWorkerConcurrency"
  key_links:
    - from: "pagefolio/ocr_dialog.py _start_worker_thread"
      to: "pagefolio/ocr_dialog.py _worker（concurrency 本）"
      via: "for _ in range(self.concurrency): threading.Thread(...).start()"
      pattern: "for _ in range\\(self\\.concurrency\\)"
    - from: "pagefolio/ocr_dialog.py _render_next_page（全ページ完了/キャンセル）"
      to: "pagefolio/ocr_dialog.py _worker 群（終了）"
      via: "concurrency 本の None 終了シグナルを put"
      pattern: "range\\(self\\.concurrency\\)"
    - from: "pagefolio/ocr_dialog.py _finish_cancelled"
      to: "二重挿入防止"
      via: "冒頭 if self._done: return ガード"
      pattern: "if self\\._done:"
---

<objective>
Phase 06 検証で BLOCKER となった OCR-PERF-02（SC-2）のギャップを閉じる。検証レポート 06-VERIFICATION.md の `gaps` と、コードレビュー 06-REVIEW.md の CR-01 / CR-02 が対象。

核心は **CR-01**: 06-02 で producer-consumer 化したものの `OCRDialog._start_worker_thread` が `threading.Thread` を 1 本しか起動せず、LM Studio の `ocr_concurrency`（最大 8）設定に関わらず実効並列度が常に 1 に退行している。これを `self.concurrency` 本のワーカー起動へ変更し、終了シグナルも `self.concurrency` 本送ること、複数ワーカーが共有する `done` 進捗カウンタを `threading.Lock` で保護すること、全ワーカー終了後に終了処理（結果描画・完了通知）を一度だけ実行することで、06-02-PLAN.md が約束した「LM Studio の並列度を最大 8 まで維持（後方互換）」を満たす。

併せて **CR-02**: キャンセル時に `_finish_cancelled` がメインスレッド直接呼び出しとワーカー終了後の `after(0, ...)` で二重に呼ばれ、`_render_results_ordered()` 経由で OCR 結果が text に二重挿入されるバグを、`_finish_cancelled` / `_finish_complete` / `_finish_error` 冒頭の `if self._done: return` 冪等ガードで修正する。

推奨対応（同一フェーズの技術的負債）として WR-01（ocr_scale フォールバック値 2.0→1.5 で D-11 整合）・WR-02（`cancel_futures` の Python 3.8 互換）・WR-03（`_SENSITIVE_KEYS` に GOOGLE_API_KEY 系追加）を別タスクで閉じる。

Purpose: SC-2 / OCR-PERF-02 を PARTIAL から VERIFIED へ復元し、LM Studio ユーザーの静黙なパフォーマンス退行とキャンセル時の結果二重表示を解消する。
Output: 複数ワーカー化＋Lock＋冪等ガードを施した OCRDialog・並列度/冪等性の回帰テスト・WR-01/02/03 の技術的負債解消。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gemini-provider/06-CONTEXT.md
@.planning/phases/06-gemini-provider/06-VERIFICATION.md
@.planning/phases/06-gemini-provider/06-REVIEW.md
@.planning/phases/06-gemini-provider/06-02-PLAN.md
@.planning/phases/06-gemini-provider/06-02-SUMMARY.md
@pagefolio/ocr_dialog.py
@pagefolio/ocr.py
@tests/test_ocr.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
本プラン（06-04）が新規作成・変更する識別子・シンボル:

- `OCRDialog._worker_threads`（list）属性 — 起動した複数ワーカースレッドの保持（既存 `_worker_thread` を置換または併存）
- `OCRDialog._done_lock`（`threading.Lock`）属性 — `done` 共有カウンタ保護
- `OCRDialog._done_count`（int）属性 — Lock 配下の Vision OCR 完了ページ数共有カウンタ（`_worker` ローカル `done` を共有化）
- `OCRDialog._workers_remaining`（int）属性 + Lock 保護 — 残ワーカー数。0 到達ワーカーのみが終了処理を呼ぶ調整カウンタ
- `OCRDialog._start_worker_thread` の改修（`for _ in range(self.concurrency)` でスレッド起動）
- `OCRDialog._render_next_page` の改修（全ページ完了/キャンセル時に `self.concurrency` 本の `None` 終了シグナルを送る）
- `OCRDialog._worker` の改修（共有 `done` カウンタを Lock 更新・最終ワーカーのみ終了処理を呼ぶ）
- `OCRDialog._finish_cancelled` / `_finish_complete` / `_finish_error` への `if self._done: return` 冪等ガード
- `pagefolio/ocr.py` の `executor.shutdown` Python 3.8 互換分岐（`sys.version_info >= (3, 9)`）と `DEFAULT_OCR_SCALE` の D-11 整合判断
- `pagefolio/settings.py` `_SENSITIVE_KEYS` への `google_api_key` / `GOOGLE_API_KEY` 系追加
- `pagefolio/dialogs/llm_config.py` の `ocr_scale` フォールバック既定 `2.0`→`1.5`
- 新規テストクラス: `TestWorkerConcurrency`（起動スレッド数==concurrency・終了シグナル数==concurrency・`tests/test_ocr.py`）
- 新規テストクラス: `TestFinishIdempotent`（`_finish_cancelled` 二重呼び出しで `_render_results_ordered` が 1 回のみ・`tests/test_ocr.py`）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01（複数ワーカー化 + done Lock + 終了シグナル concurrency 本 + 単一終了処理）と CR-02（_finish_* 冪等ガード）を修正</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（改修対象の現状コードを必読: `__init__` の `_worker_thread = None`/`_done = False` 周辺 L77-81・`_on_run` 末尾の producer-consumer 起動 L867-873・生産者 `_render_next_page` L875-944（キャンセル時 None シグナル L884・全ページ完了時 None シグナル L895・put ループ L925-937）・`_start_worker_thread` L946-949・消費者 `_worker` L951-1061（ローカル `done` カウンタ L959/L988/993/1019/1024/1028/1033・最終終了処理 L1050-1061）・`_render_results_ordered` L1068-1087・`_finish_complete` L1089-1102・`_finish_cancelled` L1104-1112・`_finish_error` L1114-1133）
    - .planning/phases/06-gemini-provider/06-REVIEW.md（CR-01 L49-79 の修正例コード・CR-02 L83-118 の冪等ガード例）
    - .planning/phases/06-gemini-provider/06-VERIFICATION.md（gaps.missing 3 項目 L31-33）
    - .planning/phases/06-gemini-provider/06-RESEARCH.md（§Pattern 5「並列度を維持した producer-consumer」L427-443・§Pitfall-A L532-538「並列維持が必要なら消費者スレッドを複数起動する」・§バッファ上限表 L313-317）
    - CLAUDE.md（裸 except 禁止・after(0) の lambda はデフォルト引数で値束縛・ruff/pytest 必須）
  </read_first>
  <behavior>
    - 起動: `_start_worker_thread(fake)` 呼び出し後、起動されたワーカースレッド数が `self.concurrency` と等しい（concurrency=4 で 4 本・concurrency=1 で 1 本）
    - 終了シグナル: `_render_next_page` が全ページ完了に到達したとき、キューへ送られた `None` の総数が `self.concurrency` 本（concurrency=4 で 4 本）
    - 終了シグナル（キャンセル）: キャンセル検出時もキューへ `self.concurrency` 本の `None` が送られる
    - done Lock: 複数ワーカーが共有 `done` カウンタを Lock 配下で更新し、N ページ処理後に `_done_count == N`（取りこぼし/二重計上なし）
    - 単一終了処理: ワーカーが M 本でも、全ワーカー終了後に `_render_results_ordered` と `_finish_complete`（または `_finish_cancelled` / `_finish_error`）が **一度だけ** 呼ばれる
    - CR-02 冪等: `_finish_cancelled` を 2 回連続呼び出しても `_render_results_ordered` は 1 回だけ実行される（`self._done` ガード）
    - 後方互換: Gemini（max_concurrency=1）では `self.concurrency==1` なので従来挙動（ワーカー 1 本・None 1 本）と一致する
  </behavior>
  <action>
    `pagefolio/ocr_dialog.py` を以下のとおり改修する。

    (1) **CR-01 複数ワーカー起動**: `_start_worker_thread` を `self._worker_threads = []` を初期化し `for _ in range(self.concurrency):` で `threading.Thread(target=self._worker, daemon=True)` を生成・start・append するループへ変更する（06-REVIEW.md L67-72 の修正例に準拠）。`__init__` の `self._worker_thread = None`（L77）は `self._worker_threads = []` へ置換する（参照箇所があれば追従）。

    (2) **CR-01 終了シグナル concurrency 本**: `_render_next_page` の全ページ完了分岐（現状 L895 の `self._render_queue.put(None)` 1 本）を `for _ in range(self.concurrency): self._render_queue.put(None)` に変更する。同様にキャンセル分岐の終了シグナル（現状 L884 と L929 の `put_nowait(None)` 1 本）も `for _ in range(self.concurrency):` でワーカー数分送る（`queue.Full` 時の except は維持・裸 except 禁止）。

    (3) **CR-01 done カウンタの Lock 化**: `__init__` に `self._done_lock = threading.Lock()` と `self._done_count = 0` を追加する。`_worker` 内のローカル変数 `done`（L959）を撤廃し、完了/失敗ごとの `done += 1`（L988/993/1019/1024/1028/1033）を `with self._done_lock: self._done_count += 1` に置換する。進捗表示で使う `total_done`（L1041）は `with self._done_lock: total_done = self._done_count + len(self._skipped_pages)` で読み取る。

    (4) **CR-01 全ワーカー終了後の単一終了処理**: 複数ワーカーが各々 `None` で break するため、終了処理（L1050-1061 の `_render_results_ordered`/`_finish_complete`/`_finish_cancelled`/`_finish_error` への after(0) 投入）は **最後に終了したワーカー 1 本だけ** が実行するよう調整する。`__init__` に `self._workers_remaining = 0` を追加し、`_start_worker_thread` のループで起動本数を `self._workers_remaining = self.concurrency` にセット。各 `_worker` の break 後（whileループ脱出後）に `with self._done_lock: self._workers_remaining -= 1; is_last = self._workers_remaining == 0` とし、`is_last` が True のワーカーのみ既存の終了分岐（fatal_msg→_finish_error / _cancel_flag→_finish_cancelled / else→_render_results_ordered+_finish_complete）を after(0) 経由で呼ぶ。`fatal_msg`/`fatal_kind` は複数ワーカーで観測しうるため `__init__` に `self._fatal_msg = None`/`self._fatal_kind = None` を置き Lock 配下で記録し、最終ワーカーが参照する設計とする（ローカル `fatal_msg`/`fatal_kind` から共有属性へ昇格）。

    (5) **CR-02 冪等ガード**: `_finish_cancelled`（L1104）・`_finish_complete`（L1089）・`_finish_error`（L1114）の各冒頭に `if self._done: return` を追加する（既存の `self._done = True` 代入の直前）。これによりキャンセル時に `_render_next_page` 直接呼び出し（L887）とワーカー終了後 `after(0, self._finish_cancelled)`（L1058）の二重呼び出しでも `_render_results_ordered()` が 1 回しか実行されない。

    実装後 `ruff check . && ruff format .` を通し、`python -c "import ast,io; ast.parse(io.open('pagefolio/ocr_dialog.py',encoding='utf-8').read())"` で構文確認、`pytest tests/test_ocr.py -q` で既存 OCRDialog 回帰なしを確認する。裸 except 禁止・`except Exception as e:` 形を厳守する。
  </action>
  <verify>
    <automated>python -c "import ast,io; ast.parse(io.open('pagefolio/ocr_dialog.py',encoding='utf-8').read()); print('parse ok')" && pytest tests/test_ocr.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr_dialog.py` に `for _ in range(self.concurrency)` がワーカー起動（`_start_worker_thread`）と終了シグナル送出（`_render_next_page`）の両方に存在する（`grep -v '^#' pagefolio/ocr_dialog.py | grep -c 'range(self.concurrency)'` が 2 以上）
    - `pagefolio/ocr_dialog.py` に `self._done_lock` と `threading.Lock()` が存在し、`_worker` 内に `with self._done_lock:` が存在する
    - `_finish_cancelled` 関数本体の冒頭に `if self._done:` ガードが存在する（`_finish_complete`・`_finish_error` にも同様）
    - `_worker` 内に共有カウンタ `self._done_count` への更新が存在し、ローカル変数 `done` のインクリメント `done += 1` が残っていない（`grep -c 'done += 1' pagefolio/ocr_dialog.py` が 0）
    - 裸の `except:` が ocr_dialog.py に存在しない
    - `python -c "import ast..."` が parse ok を出力する
    - `pytest tests/test_ocr.py -q` が exit 0（既存 TestStartOcrCloudGate / TestOcrDialogLlmConfig の回帰なし）
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>OCRDialog が self.concurrency 本のワーカーを起動し concurrency 本の終了シグナルを送り、共有 done カウンタを Lock で保護し、全ワーカー終了後に終了処理を一度だけ実行する。_finish_cancelled / _finish_complete / _finish_error は _done ガードで冪等になり、キャンセル時の結果二重挿入が解消される。Gemini（concurrency=1）は従来挙動を維持する。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 並列度回帰テスト（TestWorkerConcurrency）と冪等性テスト（TestFinishIdempotent）を追加</name>
  <files>tests/test_ocr.py</files>
  <read_first>
    - tests/test_ocr.py（アナログ: `FakeProvider` L23-39・`TestOcrDialogLlmConfig._make_fake` L873-911 の「Tkinter 不使用 SimpleNamespace fake OCRDialog + 未束縛メソッド呼び出し」パターン・`monkeypatch` 使用例 L915-934・`import threading`/`import queue` 有無）
    - pagefolio/ocr_dialog.py（Task 1 改修後の `_start_worker_thread` / `_render_next_page` / `_finish_cancelled` / `_render_results_ordered` のシグネチャと self 属性）
    - .planning/phases/06-gemini-provider/06-VERIFICATION.md（missing 項目を検証手段に落とす観点）
    - CLAUDE.md（tests は S101 免除・ruff/pytest 必須）
  </read_first>
  <behavior>
    - TestWorkerConcurrency.test_starts_concurrency_threads: `concurrency=4` の fake OCRDialog で `OCRDialog._start_worker_thread(fake)` を未束縛呼び出しすると、起動されたワーカースレッド数が 4 になる（`threading.Thread` を monkeypatch で記録、または `daemon=True` の no-op target でカウント）
    - TestWorkerConcurrency.test_single_thread_for_gemini: `concurrency=1` で起動スレッド数が 1（後方互換）
    - TestWorkerConcurrency.test_termination_signals_match_concurrency: 全ページ生産完了経路で `_render_queue`（実 `queue.Queue`）へ送られた `None` の数が `self.concurrency` 本（`_render_next_page` を全ページ分まで駆動し、キューから取り出した None を数える）
    - TestFinishIdempotent.test_finish_cancelled_renders_once: `_finish_cancelled(fake)` を 2 回連続呼び出しても `_render_results_ordered` が 1 回しか呼ばれない（`_render_results_ordered` を呼び出し回数記録の no-op に差し替え、`fake._done=False` 初期・1 回目で True 化・2 回目は早期 return）
  </behavior>
  <action>
    `tests/test_ocr.py` に `TestWorkerConcurrency` と `TestFinishIdempotent` の 2 クラスを追加する。`TestOcrDialogLlmConfig._make_fake`（L873）と同じ「`types.SimpleNamespace` で Tk 不使用 fake を組み、`from pagefolio.ocr_dialog import OCRDialog` の未束縛メソッドを `OCRDialog._method(fake, ...)` で呼ぶ」パターンを踏襲する。

    並列度テスト: fake に `concurrency`・`_worker`（no-op で即 return する callable に差し替え可）・必要属性を持たせ、`threading.Thread` を `monkeypatch.setattr` で記録するか、起動カウンタ付きのスタブに差し替えてワーカー数を数える。`OCRDialog._start_worker_thread(fake)` 呼び出し後に `assert 起動数 == fake.concurrency` を表明する。concurrency=1 と concurrency=4 の双方を検証する。

    終了シグナルテスト: fake に実 `_render_queue = queue.Queue(maxsize=concurrency+1)`・`page_indices`・`_render_idx`・`_cancel_flag`（実 `threading.Event`）・`doc`/`has_embedded_text`/`page_to_png_b64` をスタブ化（全ページをスキップ扱いにしてキューに画像を積まず最短で完了分岐へ到達させる、または最小ページ数で駆動）し、`after` を即時実行スタブ（`lambda _ms, fn=None, *a: fn(*a) if fn else None` 相当）にして `_render_next_page` を全ページ完了まで駆動する。完了後にキューから取り出した `None` の総数 == `fake.concurrency` を表明する。`after` の再帰連鎖がテスト内で完結するよう、即時実行か手動ループで全ページ駆動する。

    冪等性テスト: fake に `_done=False`・`results`/`errors`・`progress_var`（set no-op）・`cancel_btn`/`copy_btn`/`save_btn`（state no-op）・`_L`（必要キーの dict）を持たせ、`_render_results_ordered` を呼び出し回数を記録する no-op に差し替える。`OCRDialog._finish_cancelled(fake)` を 2 回呼び、`assert render_call_count == 1` を表明する。

    テスト追加後 `ruff check . && ruff format .` を通す。30 秒以内に完了すること（デッドロック・実スレッドのブロッキングを避けるため `_worker` は no-op スタブまたは最短経路）。
  </action>
  <verify>
    <automated>pytest tests/test_ocr.py::TestWorkerConcurrency tests/test_ocr.py::TestFinishIdempotent -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_ocr.py` に `class TestWorkerConcurrency` と `class TestFinishIdempotent` が存在する
    - TestWorkerConcurrency に「起動スレッド数 == concurrency」表明（concurrency=4 と concurrency=1 の両ケース）と「終了シグナル None 数 == concurrency」表明が含まれる
    - TestFinishIdempotent に「_render_results_ordered が 2 回呼び出しで 1 回のみ」表明が含まれる
    - `pytest tests/test_ocr.py::TestWorkerConcurrency tests/test_ocr.py::TestFinishIdempotent -q` が exit 0
    - 両テストクラスが 30 秒以内に完了する（デッドロックなし）
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>LM Studio の実効並列度が concurrency 本に復元されたこと（起動スレッド数・終了シグナル数 == concurrency）と、キャンセル時の結果二重挿入が解消されたこと（_render_results_ordered が冪等に 1 回のみ）が機械的に検証される。</done>
</task>

<task type="auto">
  <name>Task 3: 技術的負債 WR-01（ocr_scale フォールバック 1.5 整合）・WR-02（Python 3.8 互換 shutdown）・WR-03（_SENSITIVE_KEYS 強化）を解消</name>
  <files>pagefolio/ocr.py, pagefolio/settings.py, pagefolio/dialogs/llm_config.py</files>
  <read_first>
    - pagefolio/ocr.py（`DEFAULT_OCR_SCALE = 2.0` L35・`run_with_bounded_buffer` 内 `executor.shutdown(cancel_futures=True)` L319・`run_parallel` 内 `executor.shutdown(cancel_futures=True)` L442・ファイル冒頭の import 群で `import sys` 有無）
    - pagefolio/settings.py（`_SENSITIVE_KEYS` L16・`"ocr_scale": 1.5` L43・`gemini_model` 既定 L54・`_save_settings` のキーガードロジック）
    - pagefolio/dialogs/llm_config.py（`get("ocr_scale", 2.0)` L328・`_apply` の例外パス `llm_settings["ocr_scale"] = 2.0` L717）
    - pagefolio/ocr_dialog.py（例外フォールバック `self._ocr_scale = 2.0` L801 — Task 1 と同ファイルだが本タスクで 1.5 へ修正。Task 1 完了後に編集すること）
    - .planning/phases/06-gemini-provider/06-CONTEXT.md（D-11 既定 1.5・既存保存値据え置き L48・D-06 dual env var L39）
    - .planning/phases/06-gemini-provider/06-REVIEW.md（WR-01 L124-144・WR-02 L148-166・WR-03 L170-188 の修正例）
    - CLAUDE.md（裸 except 禁止・Python 3.8+ サポート宣言・ruff/pytest 必須）
  </read_first>
  <action>
    3 つの技術的負債を解消する。各々を独立した修正単位として扱う。

    (WR-01 ocr_scale 例外フォールバックの 1.5 統一・D-11 整合): `pagefolio/dialogs/llm_config.py` L328 の `self.current_settings.get("ocr_scale", 2.0)` を `get("ocr_scale", 1.5)` に、L717 の例外パス `llm_settings["ocr_scale"] = 2.0` を `1.5` に変更する。`pagefolio/ocr_dialog.py` L801 の `except (tk.TclError, ValueError): self._ocr_scale = 2.0` を `1.5` に変更する。`pagefolio/ocr.py` L35 の `DEFAULT_OCR_SCALE = 2.0` を `1.5` に変更し D-11（新規既定 1.5）と整合させる（この定数は `page_to_png_b64` のデフォルト引数。通常フローは settings 由来値で上書きされるが、デフォルト引数も新既定に合わせる）。D-11 の「既存ユーザーの保存値は据え置き」は settings.py L43 の既定値変更（既に完了）と保存値非書き換えで担保済みであり、本変更はフォールバック経路のみに影響する。

    (WR-02 Python 3.8 互換 shutdown): `pagefolio/ocr.py` 冒頭に `import sys`（未追加なら）を加え、`run_with_bounded_buffer`（L319）と `run_parallel`（L442）の `executor.shutdown(wait=False, cancel_futures=True)` を `if sys.version_info >= (3, 9): executor.shutdown(wait=False, cancel_futures=True)` / `else: executor.shutdown(wait=False)` の分岐に置き換える（06-REVIEW.md L160-166 の修正例）。CLAUDE.md が Python 3.8+ をサポート対象と宣言しているため、3.8 で `TypeError: shutdown() got an unexpected keyword argument 'cancel_futures'` を回避する。

    (WR-03 _SENSITIVE_KEYS 強化): `pagefolio/settings.py` L16 の `_SENSITIVE_KEYS` に `"google_api_key"` と `"GOOGLE_API_KEY"` を追加する。D-06 の dual env var フォールバック（GEMINI_API_KEY→GOOGLE_API_KEY）に対応し、防御層として Gemini フォールバックキー名が settings JSON に平文保存されないようにする。既存の `claude_api_key`/`gemini_api_key`/`anthropic_api_key`/`api_key` は維持する（必要に応じ大文字バリアントも 06-REVIEW.md L183-187 に準拠して追加）。

    実装後 `ruff check . && ruff format .` を通し、`pytest tests/test_settings_keyguard.py tests/test_ocr.py -q` で settings キーガード・ocr 回帰なしを確認する。裸 except 禁止。
  </action>
  <verify>
    <automated>pytest tests/test_settings_keyguard.py tests/test_ocr.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr.py` に `import sys` と `sys.version_info >= (3, 9)` 分岐が存在し、`executor.shutdown(...cancel_futures=True)` が無条件呼び出しのまま残っていない（`grep -c 'cancel_futures' pagefolio/ocr.py` の各出現が version 分岐内）
    - `pagefolio/ocr.py` の `DEFAULT_OCR_SCALE` が `1.5`（`grep -v '^#' pagefolio/ocr.py | grep 'DEFAULT_OCR_SCALE = 1.5'` がヒット）
    - `pagefolio/dialogs/llm_config.py` に `get("ocr_scale", 2.0)` が残っていない（`grep -c 'ocr_scale", 2.0' pagefolio/dialogs/llm_config.py` が 0）
    - `pagefolio/ocr_dialog.py` の例外フォールバックに `self._ocr_scale = 2.0` が残っていない（`grep -c '_ocr_scale = 2.0' pagefolio/ocr_dialog.py` が 0）
    - `pagefolio/settings.py` の `_SENSITIVE_KEYS` に `google_api_key` と `GOOGLE_API_KEY` が含まれる
    - `pytest tests/test_settings_keyguard.py tests/test_ocr.py -q` が exit 0
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>ocr_scale の例外フォールバックがすべて 1.5 へ統一され D-11 と整合し、executor.shutdown が Python 3.8 で TypeError を出さず、_SENSITIVE_KEYS が GOOGLE_API_KEY 系を含んで Gemini フォールバックキー名の平文保存を防ぐ。既存テストに回帰がない。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| メインスレッド（生産者・fitz）→ 複数ワーカースレッド（消費者・API） | base64 画像と進捗カウンタが queue / 共有属性を介してスレッド境界を越える |
| 複数ワーカー間の共有状態（done カウンタ・workers_remaining・fatal_msg） | 複数スレッドが同一カウンタ/属性を読み書きする競合境界 |
| ユーザーキャンセル → producer / 複数 consumer | キャンセルフラグと終了シグナルが複数ワーカーへ伝搬する境界 |
| settings 辞書 → pagefolio_settings.json | APIキー相当値の平文書き込みを防ぐ防御境界（WR-03） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | 複数ワーカー共有の done / workers_remaining / fatal_msg カウンタ | mitigate | `threading.Lock`（`self._done_lock`）配下で読み書きし競合更新を防止（Task 1）。Task 2 で done カウンタの取りこぼし/二重計上なしを検証 |
| T-06-11 | Denial of Service / リソース枯渇 | 複数ワーカーのデッドロック・飢餓 | mitigate | 終了シグナル `None` を `self.concurrency` 本送り全ワーカーを確実に終了（Pitfall-E）。put は timeout ループ・get は timeout=1.0 で cancel 確認。Task 2 でデッドロック非発生（30 秒以内完了）を検証 |
| T-06-12 | Repudiation / 表示整合性 | キャンセル時の結果二重挿入 | mitigate | `_finish_cancelled` / `_finish_complete` / `_finish_error` 冒頭の `if self._done: return` 冪等ガード（CR-02）。Task 2 の TestFinishIdempotent で 1 回のみ実行を検証 |
| T-06-13 | Information Disclosure | GOOGLE_API_KEY 系の平文保存 | mitigate | `_SENSITIVE_KEYS` に `google_api_key` / `GOOGLE_API_KEY` を追加し `_save_settings` のキーガードでフィルタ（WR-03）。Task 3・test_settings_keyguard で検証 |
| T-06-14 | Denial of Service | Python 3.8 環境での OCR 機能全停止 | mitigate | `executor.shutdown` の `cancel_futures` を `sys.version_info >= (3, 9)` 分岐でガードし 3.8 の TypeError を回避（WR-02・Task 3） |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 外部 pip パッケージ追加ゼロ（標準ライブラリ `threading` / `queue` / `sys` のみ・RESEARCH §Package Legitimacy Audit「該当なし」）。slopcheck 不要 |
</threat_model>

<verification>
- `pytest tests/ -q`（フルスイート）がグリーン — 既存 373 テスト + 新規並列度/冪等性テストの回帰なし
- `ruff check . && ruff format .` がクリーン
- `_start_worker_thread` がワーカーを `self.concurrency` 本起動する（grep `for _ in range(self.concurrency)` 検証 + TestWorkerConcurrency）
- 終了シグナル `None` が `self.concurrency` 本送られる（TestWorkerConcurrency）
- done 共有カウンタが `threading.Lock` 配下で更新される（grep `with self._done_lock:` 検証）
- `_finish_cancelled` / `_finish_complete` / `_finish_error` が `if self._done: return` ガードを持つ（grep 検証 + TestFinishIdempotent）
- `executor.shutdown(cancel_futures=...)` が `sys.version_info >= (3, 9)` 分岐内（grep 検証）
- `_SENSITIVE_KEYS` に `google_api_key` / `GOOGLE_API_KEY` が含まれる（grep + test_settings_keyguard）
- 裸 except が ocr_dialog.py / ocr.py / settings.py / llm_config.py に存在しない
</verification>

<success_criteria>
- LM Studio で OCR を起動すると `self.concurrency` 本のワーカーが起動し、実効並列度が concurrency 設定どおりに復元される（OCR-PERF-02 の後方互換達成・SC-2 PARTIAL→VERIFIED）
- 全ページ完了時・キャンセル時に `self.concurrency` 本の終了シグナルが送られ、全ワーカーがブロックせず終了する
- 共有 done カウンタが Lock 保護され、複数ワーカーでも進捗が正しく集計される
- 全ワーカー終了後に結果描画と完了処理が一度だけ実行される
- キャンセル時に `_finish_cancelled` が複数回呼ばれても OCR 結果が二重挿入されない（CR-02 解消）
- Gemini（concurrency=1）の挙動が変わらない（後方互換）
- ocr_scale 例外フォールバックが 1.5 に統一され D-11 と整合する（WR-01）
- `executor.shutdown` が Python 3.8 で TypeError を出さない（WR-02）
- `_SENSITIVE_KEYS` が GOOGLE_API_KEY 系を含み平文保存を防ぐ（WR-03）
- フルスイート pytest がグリーンで既存挙動に回帰がない
</success_criteria>

<output>
Create `.planning/phases/06-gemini-provider/06-04-SUMMARY.md` when done
</output>
