---
phase: 06-gemini-provider
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/settings.py
  - pagefolio/lang.py
  - pagefolio/dialogs/llm_config.py
  - pagefolio/ocr_dialog.py
  - pagefolio/constants.py
  - 開発履歴.md
  - README.md
  - tests/test_settings_keyguard.py
  - tests/test_provider_ui.py
autonomous: true
requirements: [OCR-PERF-05, OCR-API-02, OCR-QA-01]
must_haves:
  truths:
    - "DEFAULT_SETTINGS['ocr_scale'] が 1.5 になり、新規ユーザーに 1.5 既定が適用される（既存保存値は据え置き・D-11・OCR-PERF-05）"
    - "llm_config の ocr_scale スライダー近傍にコスト/精度トレードオフの常設ヒントが表示される（D-12・OCR-PERF-05）"
    - "SettingsDialog のプロバイダ一覧に gemini が表示され、選択時に temperature 欄のみ表示され gemini モデル欄が出る（D-09・OCR-API-02）"
    - "OCRDialog のプロバイダ判定（_is_cloud_provider/_needs_session_key/_provider_display_name/_apply_llm_settings/_confirm_cost）が gemini に対応する（Pitfall-F/G）"
    - "Gemini 名・dual env var エラー・ocr_scale ヒントの日英文言が lang.py に追加される（OCR-QA-01 補助）"
  artifacts:
    - path: "pagefolio/settings.py"
      provides: "ocr_scale 既定 1.5・gemini_model 既定追加"
      contains: "gemini_model"
    - path: "pagefolio/lang.py"
      provides: "ocr_provider_name_gemini・ocr_api_key_missing_gemini・ocr_scale_tradeoff_hint（日英）"
      contains: "ocr_scale_tradeoff_hint"
    - path: "pagefolio/dialogs/llm_config.py"
      provides: "gemini プロバイダ選択肢・gemini モデル欄・gemini 時 temperature 表示・ocr_scale 常設ヒント・gemini_model 収集"
      contains: "gemini"
    - path: "pagefolio/ocr_dialog.py"
      provides: "プロバイダ判定系メソッドの gemini 分岐・コスト確認のホスト名対応"
      contains: "gemini"
    - path: "pagefolio/constants.py"
      provides: "APP_VERSION 更新"
      contains: "APP_VERSION"
  key_links:
    - from: "pagefolio/dialogs/llm_config.py provider_combo"
      to: "ocr_provider='gemini' 設定値"
      via: "Combobox values に 'gemini' 追加 + _on_provider_change gemini 分岐"
      pattern: "gemini"
    - from: "pagefolio/ocr_dialog.py _is_cloud_provider/_needs_session_key"
      to: "gemini クラウドゲート（コスト確認・セッションキー欄）"
      via: "name in ('claude','gemini') 判定 + GeminiProvider isinstance"
      pattern: "gemini"
---

<objective>
ocr_scale 既定を 1.5 へ見直し（OCR-PERF-05）、設定欄にトレードオフ常設ヒントを置き、Gemini を UI（プロバイダ選択・モデル欄・temperature 欄）に統合する。`llm_config.py` に gemini 分岐を加え、`ocr_dialog.py` のプロバイダ判定系メソッド（`_is_cloud_provider`/`_needs_session_key`/`_provider_display_name`/`_apply_llm_settings`/`_confirm_cost`）を gemini 対応にする（Pitfall-F/G）。Gemini 名・dual env var エラー・ocr_scale ヒントの日英文言を `lang.py` に最小追加し、`APP_VERSION`・開発履歴.md・README バッジを同期する。

Purpose: OCR-PERF-05（ocr_scale 1.5 + ヒント）と OCR-API-02 の UI 統合部分・OCR-QA-01 補助文言を満たす。Plan 01（GeminiProvider）と並列実行（ファイル非重複）。
Output: 1.5 既定・常設ヒント・gemini UI 欄・gemini プロバイダ判定・文言・バージョン同期。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gemini-provider/06-CONTEXT.md
@.planning/phases/06-gemini-provider/06-RESEARCH.md
@.planning/phases/06-gemini-provider/06-PATTERNS.md
@pagefolio/settings.py
@pagefolio/dialogs/llm_config.py
@pagefolio/ocr_dialog.py
@tests/test_settings_keyguard.py
@tests/test_provider_ui.py
</context>

<artifacts_this_phase_produces>
本プラン（06-03）が新規作成・変更する識別子・ファイル:
- `settings.py` `_load_settings` defaults: `"ocr_scale": 1.5`（2.0→1.5・D-11）・`"gemini_model": "gemini-2.5-flash"`（新規・D-08）
- `lang.py` 新規キー（ja/en 両方）: `ocr_provider_name_gemini`・`ocr_api_key_missing_gemini`・`ocr_scale_tradeoff_hint`
- `llm_config.py`: provider_combo values に `"gemini"` 追加・`gemini_section_frame`/`gemini_model_var`/`gemini_model_combo`（新規）・`_on_provider_change` の gemini 分岐・ocr_scale 常設ヒント Label・`_apply` での `gemini_model` 収集
- `ocr_dialog.py`: `_is_cloud_provider`/`_needs_session_key`/`_provider_display_name`/`_apply_llm_settings`/`_confirm_cost` の gemini 分岐（Pitfall-F/G）・`_estimate_cost` の Gemini ホスト/価格対応
- `constants.py`: `APP_VERSION` 更新（v1.4.0 系へ）
- `開発履歴.md`: Phase 6 変更エントリ追記
- `README.md`: バージョンバッジ同期
- 新規テスト: `test_settings_keyguard.py` の `TestLoadSettingsDefaults` に ocr_scale==1.5・gemini_model 既定テスト追加
- 新規テスト: `test_provider_ui.py` に gemini プロバイダ判定（_is_cloud_provider/_needs_session_key）テスト追加
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: ocr_scale 既定 1.5 化・gemini_model 既定追加・文言追加とそのテスト</name>
  <files>pagefolio/settings.py, pagefolio/lang.py, tests/test_settings_keyguard.py</files>
  <read_first>
    - pagefolio/settings.py（アナログ/改修対象: `_load_settings` defaults line 35-53 の `"ocr_scale": 2.0` line 43・`claude_model`/`ocr_effort` line 51-52・`_SENSITIVE_KEYS` line 16）
    - pagefolio/lang.py（アナログ: `ocr_provider_name_claude` line 308/656・`ocr_api_key_missing` line 293/641・`ocr_temperature_hint` 短文パターン・`settings_ocr_scale` line 325/671）
    - tests/test_settings_keyguard.py（アナログ: `TestLoadSettingsDefaults` line 163・`from pagefolio.settings import _load_settings`）
    - .planning/phases/06-gemini-provider/06-PATTERNS.md（§settings.py・§lang.py の追加パターン）
    - .planning/phases/06-gemini-provider/06-RESEARCH.md（§Code Examples settings.py/lang.py・D-11/D-12）
  </read_first>
  <action>
    `pagefolio/settings.py` の `_load_settings` defaults で `"ocr_scale": 2.0` を `"ocr_scale": 1.5` に変更する（D-11・新規ユーザー向け既定値。既存保存値は `setdefault` のため据え置きで上書きしない）。`claude_model`/`ocr_effort` の隣に `"gemini_model": "gemini-2.5-flash"` を追加する（D-08）。`pagefolio/lang.py` の ja 辞書・en 辞書の両方に `ocr_provider_name_gemini`（"Gemini (Google AI)"）・`ocr_api_key_missing_gemini`（GEMINI_API_KEY / フォールバック GOOGLE_API_KEY を明記・D-06）・`ocr_scale_tradeoff_hint`（ja:「低=速い/安い・高=精度、低スペックは 1.5 推奨」/ en:「Low=fast/cheap, High=accuracy. 1.5 recommended for low-spec PCs.」・D-12）を `ocr_provider_name_claude` 近傍に追加する。`tests/test_settings_keyguard.py` の `TestLoadSettingsDefaults` に `_load_settings()`（ファイル非存在時）の `ocr_scale == 1.5` と `gemini_model == "gemini-2.5-flash"` を表明するテストを追加する（既存ファイルが存在する環境では monkeypatch で `_get_settings_path` を一時ファイルへ向けるか、defaults を直接検証する既存パターンを踏襲）。実装後 `ruff check . && ruff format .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_settings_keyguard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/settings.py` の defaults に `"ocr_scale": 1.5` と `"gemini_model": "gemini-2.5-flash"` が存在し、`"ocr_scale": 2.0` が存在しない
    - `pagefolio/lang.py` に `ocr_provider_name_gemini`・`ocr_api_key_missing_gemini`・`ocr_scale_tradeoff_hint` が ja/en 両方に存在する（grep でキー出現が 2 回以上）
    - `pytest tests/test_settings_keyguard.py -q` が exit 0（ocr_scale==1.5・gemini_model 既定テスト含む）
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>新規ユーザーの ocr_scale 既定が 1.5 になり、既存保存値は据え置かれ、gemini_model 既定と Gemini/ヒント文言（日英）が追加され、テストで検証される。</done>
</task>

<task type="auto">
  <name>Task 2: llm_config.py に gemini プロバイダ欄と ocr_scale 常設ヒントを追加</name>
  <files>pagefolio/dialogs/llm_config.py, tests/test_provider_ui.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config.py（アナログ/改修対象: provider_combo values line 99-100・`claude_section_frame` line 182-215・`_on_provider_change` line 439-458・`scale_row` line 280-306・`_apply` の claude_model 収集 line 610-612・`from pagefolio.ocr_providers import ClaudeProvider, LMStudioProvider` line 13）
    - .planning/phases/06-gemini-provider/06-PATTERNS.md（§llm_config.py — provider Combobox 追加・Gemini 固有欄・_on_provider_change gemini 分岐・ocr_scale ヒント・_apply gemini_model 収集）
    - .planning/phases/06-gemini-provider/06-RESEARCH.md（§Pitfall — gemini UI 連携・D-09 temperature のみ表示）
    - tests/test_provider_ui.py（既存 UI テストの構造把握・回帰確認用）
  </read_first>
  <action>
    `pagefolio/dialogs/llm_config.py` を gemini 対応にする。import に `GeminiProvider` を追加する。provider_combo の `values=["off", "lmstudio", "claude"]` を `["off", "lmstudio", "claude", "gemini"]` に変更する。`claude_section_frame` と同パターンで `gemini_section_frame` を新規作成し、`gemini_model_var`（既定 `current_settings.get("gemini_model","gemini-2.5-flash")`）と `gemini_model_combo`（values=`GeminiProvider.RECOMMENDED_MODELS`）・モデル更新ボタン（`os.environ.get("GEMINI_API_KEY") or os.environ.get("GOOGLE_API_KEY")` を読み取り `GeminiProvider(api_key=..., model="").list_models()`・例外時は静的 RECOMMENDED_MODELS へフォールバック・D-08）を置く。`_on_provider_change` に `elif provider == "gemini":` 分岐を追加し、`gemini_section_frame` を pack・`claude_section_frame`/`effort_frame` を pack_forget・`temperature_frame` を pack（temperature のみ表示・D-09）する。既存の claude 分岐でも `gemini_section_frame.pack_forget()` を呼ぶよう調整する。`scale_row` の直後に `ocr_scale_tradeoff_hint` を `C["TEXT_SUB"]`・`self._font(-2)` で常設 Label として追加する（D-12・テーマ色ハードコード禁止・`C["KEY"]` 使用）。`_apply` に `llm_settings["gemini_model"] = self.gemini_model_var.get().strip() or "gemini-2.5-flash"` を claude_model 収集の隣に追加する（gemini_model は無害な設定値・api_key は絶対に入れない）。`tests/test_provider_ui.py` に provider_combo の values に "gemini" が含まれることを検証する回帰テストを既存パターンで追加する。実装後 `ruff check . && ruff format .` を通す。
  </action>
  <verify>
    <automated>python -c "import ast,io; ast.parse(io.open('pagefolio/dialogs/llm_config.py',encoding='utf-8').read()); print('parse ok')" && pytest tests/test_provider_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `llm_config.py` の provider_combo values に `"gemini"` が含まれる
    - `llm_config.py` に `gemini_section_frame`・`gemini_model_var`・`_on_provider_change` の `gemini` 分岐が存在する
    - `scale_row` 近傍に `ocr_scale_tradeoff_hint` を参照する Label が存在する
    - `_apply` に `gemini_model` 収集が存在し、api_key 系キーの収集は存在しない（grep で `api_key` 収集なしを確認）
    - テーマ色のハードコード（`bg="#`）が新規追加部分に存在しない
    - `pytest tests/test_provider_ui.py -q` が exit 0
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>SettingsDialog で gemini が選択でき、gemini モデル欄と temperature 欄が表示され、ocr_scale スライダー近傍に常設トレードオフヒントが表示され、gemini_model が無害な設定値として収集される。</done>
</task>

<task type="auto">
  <name>Task 3: ocr_dialog.py のプロバイダ判定系を gemini 対応にしバージョン/履歴を同期</name>
  <files>pagefolio/ocr_dialog.py, pagefolio/constants.py, 開発履歴.md, README.md, tests/test_provider_ui.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（アナログ/改修対象: `_provider_display_name` line 498-511・`_is_cloud_provider` line 513-526・`_estimate_cost` line 528-553・`_needs_session_key` line 555-562・`_confirm_cost` line 679-701・`_apply_llm_settings` line 585-629・`_on_run` のセッションキー格納 line 716-729）
    - pagefolio/lang.py（Task 1 で追加した `ocr_provider_name_gemini`・`ocr_api_key_missing_gemini`）
    - .planning/phases/06-gemini-provider/06-PATTERNS.md（§ocr_dialog.py — _is_cloud_provider/_needs_session_key/_apply_llm_settings の gemini 分岐）
    - .planning/phases/06-gemini-provider/06-RESEARCH.md（§Pitfall-F/G）
    - tests/test_provider_ui.py（アナログ: `TestIsCloudProvider` line 200・`TestNeedsSessionKey` line 282・`_make_dialog_stub` line 182）
    - pagefolio/constants.py（`APP_VERSION` line 12）
    - CLAUDE.md（バージョン更新ルール: constants.py・開発履歴.md・README バッジ同期）
  </read_first>
  <action>
    `pagefolio/ocr_dialog.py` のプロバイダ判定系に gemini 分岐を追加する（Pitfall-F/G）。`_provider_display_name` で name=="gemini" または `isinstance(self.provider, GeminiProvider)` のとき `self._L["ocr_provider_name_gemini"]` を返す。`_is_cloud_provider` で `name in ("claude","gemini")` または `isinstance(self.provider, (ClaudeProvider, GeminiProvider))` のとき True。`_needs_session_key` で name=="gemini" のとき `not (os.environ.get("GEMINI_API_KEY") or os.environ.get("GOOGLE_API_KEY"))` を返す（dual env var・D-06）、それ以外（claude）は従来通り。`_apply_llm_settings` に `elif name == "gemini":` 分岐を追加し、`_resolve_api_key("gemini", session_keys)`（OCRAPIKeyError 時 api_key=""）→ `build_provider(self.app.settings, api_key=api_key)` で provider を再生成する。`_on_run` のセッションキー格納で name=="gemini" のときキーを `self.app._session_api_keys["gemini"]` に格納し、未入力時のエラー文言は `ocr_api_key_missing_gemini` 系を使う（claude は従来通り）。`_confirm_cost`/`_estimate_cost` を gemini 対応にする: 送信先ホストを `generativelanguage.googleapis.com`（gemini）/ `api.anthropic.com`（claude）でプロバイダ別に出し分け、コスト概算は gemini モデル（gemini-2.5-flash 等）に対し最低限「従量課金が発生する」警告が出る粗い見積もりを返す（正確性より警告の存在が重要・D-10）。`tests/test_provider_ui.py` に gemini プロバイダ判定テスト（`_is_cloud_provider` gemini True・`_needs_session_key` env 未設定で True・GOOGLE_API_KEY フォールバック）を `_make_dialog_stub` 流用で追加する。CLAUDE.md のバージョン更新ルールに従い `pagefolio/constants.py` の `APP_VERSION` を v1.4.0 系（MEMORY: マイルストーン版番は pagefolio 実版番 semver に揃える）へ更新し、`開発履歴.md` に Phase 6 の変更エントリ（GeminiProvider・逐次レンダリング・ocr_scale 1.5）を追記、`README.md` のバージョンバッジを同期する。実装後 `ruff check . && ruff format .` を通す。
  </action>
  <verify>
    <automated>python -c "import ast,io; ast.parse(io.open('pagefolio/ocr_dialog.py',encoding='utf-8').read()); print('parse ok')" && pytest tests/test_provider_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ocr_dialog.py` の `_is_cloud_provider`・`_needs_session_key`・`_provider_display_name`・`_apply_llm_settings` に gemini 分岐が存在する
    - `_confirm_cost`/`_estimate_cost` が `generativelanguage.googleapis.com` を gemini 時に出力する
    - `_on_run` の gemini セッションキー格納が `self.app._session_api_keys["gemini"]` を使い、settings には書かない（grep 確認）
    - `tests/test_provider_ui.py` に gemini 判定テストが追加され `pytest tests/test_provider_ui.py -q` が exit 0
    - `pagefolio/constants.py` の `APP_VERSION` が `v1.3.0` 以外（v1.4.0 系）に更新されている
    - `開発履歴.md` に Phase 6 のエントリが追記され、`README.md` のバッジが APP_VERSION と一致する
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>OCRDialog の全プロバイダ判定系が gemini を認識し、gemini でもコスト確認・セッションキー欄・プロバイダ表示が機能し、バージョン番号と開発履歴・README が同期される。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー入力（SettingsDialog・セッションキー欄） → settings / セッションメモリ | api_key がセッションメモリに入り settings には入らない境界 |
| llm_config UI → ocr_provider 設定値 | プロバイダ選択がアプリ挙動（外部送信可否）を決める境界 |
| ユーザー → クラウド送信ゲート（コスト確認） | 課金・外部送信の同意境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | llm_config _apply / _save_settings | mitigate | gemini_model は無害な設定値のみ収集し api_key 系を絶対に llm_settings へ入れない（成功基準1）。_SENSITIVE_KEYS に gemini_api_key 既存ガード。Task 2 で api_key 収集不在を grep 検証 |
| T-06-11 | Information Disclosure | ocr_dialog セッションキー格納 | mitigate | gemini セッションキーは `_session_api_keys["gemini"]` へ格納し settings/os.environ に書かない（D-01/D-03）。Task 3 で grep 検証 |
| T-06-12 | Repudiation / 意図しない課金 | クラウド送信ゲート | mitigate | gemini もコスト確認ダイアログ（送信先ホスト・ページ数・概算コスト・プライバシー注記）を毎回表示しキャンセル可能にする（成功基準5 踏襲・OCR-UI-03 既存枠を gemini へ拡張）。Pitfall-F 対策 |
| T-06-13 | Spoofing | gemini モデル一覧取得 | accept | モデル更新ボタンは GEMINI_API_KEY/GOOGLE_API_KEY 読み取りのみ・失敗時は静的 RECOMMENDED_MODELS へフォールバック（D-08）。API 応答の改ざんリスクは低（OCR 用途・モデル名のみ） |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 外部 pip パッケージ追加ゼロ（RESEARCH §Package Legitimacy Audit「該当なし」）。slopcheck 不要 |
</threat_model>

<verification>
- `pytest -q`（フルスイート）がグリーン — settings/provider_ui/既存 UI テストに回帰なし
- `ruff check . && ruff format .` がクリーン
- gemini プロバイダが UI（選択肢・モデル欄・temperature 欄・コスト確認・セッションキー欄）で機能する
- APP_VERSION・開発履歴.md・README バッジが同期している
</verification>

<success_criteria>
- ocr_scale 既定が 1.5 になり既存保存値が据え置かれる
- ocr_scale スライダー近傍に常設トレードオフヒントが表示される
- SettingsDialog で gemini が選択でき gemini モデル欄と temperature 欄が表示される
- OCRDialog のプロバイダ判定系が gemini を認識しコスト確認・セッションキー欄が機能する
- Gemini 名・dual env var エラー・ocr_scale ヒントの日英文言が追加される
- APP_VERSION・開発履歴.md・README が同期される
</success_criteria>

<output>
Create `.planning/phases/06-gemini-provider/06-03-SUMMARY.md` when done
</output>
