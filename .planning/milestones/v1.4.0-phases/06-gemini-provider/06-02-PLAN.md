---
phase: 06-gemini-provider
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - pagefolio/ocr.py
  - pagefolio/ocr_dialog.py
  - tests/test_ocr.py
autonomous: true
requirements: [OCR-PERF-02, OCR-QA-01]
must_haves:
  truths:
    - "100 ページ PDF の OCR 実行中に全ページの base64 画像が同時にメモリに乗らない（ページ単位 render→送信→破棄・成功基準2・OCR-PERF-02）"
    - "ワーカースレッド内に fitz.Document/get_pixmap() の直接呼び出しが一切存在しない（Phase 4 D-03 必達・D-04）"
    - "FakeProvider の ocr_image 呼び出し時点で同時保持される画像数がバッファ上限（並列度+1）を超えない（D-13・機械検証）"
    - "進捗 UI が統合プログレス（処理済み X/総数・スキップ含む）で表示される（D-03）"
    - "LM Studio / Claude / Gemini の既存挙動・キャンセル・APIキーガードを壊さない（後方互換）"
  artifacts:
    - path: "pagefolio/ocr.py"
      provides: "run_with_bounded_buffer（Tk 非依存 producer-consumer ヘルパー・queue.Queue(maxsize)+ThreadPoolExecutor）"
      contains: "def run_with_bounded_buffer"
    - path: "pagefolio/ocr_dialog.py"
      provides: "_render_next_page/_worker を producer-consumer 重なり実行へ改修・統合プログレス"
      contains: "_render_queue"
    - path: "tests/test_ocr.py"
      provides: "メモリ非蓄積リグレッションテスト（同時保持画像数 <= maxsize）"
      contains: "TestProducerConsumerMemory"
  key_links:
    - from: "pagefolio/ocr_dialog.py _render_next_page（生産者・メインスレッド）"
      to: "pagefolio/ocr_dialog.py _worker（消費者・ワーカー）"
      via: "queue.Queue(maxsize=concurrency+1) を介したパイプライン"
      pattern: "_render_queue"
    - from: "pagefolio/ocr.py run_with_bounded_buffer"
      to: "queue.Queue(maxsize) + ThreadPoolExecutor"
      via: "上限付きバッファで同時保持を制限"
      pattern: "queue\\.Queue"
---

<objective>
全ページ base64 画像の一括メモリ保持（現状 `OCRDialog._images = {}` 辞書蓄積）を廃止し、ページ単位の render→送信→破棄でメモリ上限を保証する producer-consumer 化を行う。Tk 非依存の `run_with_bounded_buffer` ヘルパーを `ocr.py` に新設（テスト可能化・D-13）し、`ocr_dialog.py` の `_render_next_page`（生産者・メインスレッドで fitz）と `_worker`（消費者・ワーカーで API）を重なり実行へ作り替える。バッファ上限は `concurrency + 1`（余裕係数 1・D-02）。LM Studio の並列度（最大 8）は ThreadPoolExecutor で維持し後方互換を守る。

Purpose: 成功基準2（100 ページでも全 base64 が同時にメモリに乗らない）と OCR-PERF-02 を満たす。スレッド境界（Phase 4 D-03・ワーカー内 fitz アクセスゼロ）は必達。
Output: run_with_bounded_buffer ヘルパー・producer-consumer 化した OCRDialog・メモリ非蓄積リグレッションテスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gemini-provider/06-CONTEXT.md
@.planning/phases/06-gemini-provider/06-RESEARCH.md
@.planning/phases/06-gemini-provider/06-PATTERNS.md
@pagefolio/ocr.py
@pagefolio/ocr_dialog.py
@tests/test_ocr.py
@.planning/phases/06-gemini-provider/06-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
本プラン（06-02）が新規作成する識別子・ファイル:
- 関数: `run_with_bounded_buffer(provider, render_fn, page_indices, concurrency, prompt, on_done, is_cancelled)` — Tk/Tkinter 非依存の producer-consumer ループ（`pagefolio/ocr.py`）
- `ocr.py` への `import queue`（標準ライブラリ）
- `OCRDialog._render_queue`（`queue.Queue(maxsize=concurrency+1)`）属性
- `OCRDialog._render_next_page` の改修（生産者: render→put・キャンセル時 None シグナル）
- `OCRDialog._worker` の改修（消費者: get→ocr_image→del b64・fitz アクセスゼロ）
- `OCRDialog._images = {}` の撤廃（一括辞書蓄積の廃止）
- 統合プログレス更新ロジック（処理済み X/総数・スキップ含む・D-03）
- 新規テストクラス: `TestProducerConsumerMemory`（同時保持画像数 <= maxsize・D-13）（`tests/test_ocr.py`）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: run_with_bounded_buffer ヘルパーを ocr.py に新設（Tk 非依存・テスト可能化）</name>
  <files>pagefolio/ocr.py</files>
  <read_first>
    - pagefolio/ocr.py（アナログ: `run_parallel` line 126-244 の ThreadPoolExecutor + OCRRetryableError バックオフ・on_progress/is_cancelled・fatal/error 区別パターン）
    - .planning/phases/06-gemini-provider/06-RESEARCH.md（§Pattern 4 producer-consumer・§Pattern 6 Tk 非依存ヘルパー・§バッファ上限の余裕係数決定 表）
    - .planning/phases/06-gemini-provider/06-PATTERNS.md（§ocr.py — producer-consumer キュー変数の切り出しパターン）
  </read_first>
  <action>
    `pagefolio/ocr.py` の冒頭に `import queue`（標準ライブラリ）を追加する。`run_parallel` の近傍に `run_with_bounded_buffer(provider, render_fn, page_indices, concurrency=None, prompt="", on_done=None, is_cancelled=None)` を新設する。内部で `maxsize = max(1, workers + 1)`（余裕係数 1・D-02）の `queue.Queue(maxsize=maxsize)` を作る。生産者は `page_indices` を順に走査し、各 page_idx で `render_fn(page_idx)`（→ b64 文字列 or None=スキップ）を呼び、None でなければ `is_cancelled` をチェックしながら `put((page_idx, b64), timeout=0.1)` のループ（`queue.Full` 時に is_cancelled を確認・Pitfall-B）でバッファへ積む。全件後（またはキャンセル時）に消費者数分の終了シグナル `None` を put する。消費者は `ThreadPoolExecutor(max_workers=workers)` で `concurrency` 本のワーカーを起動し、各ワーカーが `queue.get(timeout=1.0)` で取り出し → `None` なら break → `provider.ocr_image(b64, prompt)` を `run_parallel` と同じ OCRRetryableError バックオフ（MAX_RETRIES・Retry-After 優先）で呼ぶ → 成功/失敗を `on_done(page_idx, status, text_or_err)` で通知 → finally で `del b64`（破棄・成功基準2）。戻り値は `(results, errors, fatal_msg, fatal_kind)`（run_parallel と同形）。生産者はワーカーと並行に走るため、生産者を別スレッドで起動するか呼び出し側がメインで render しワーカーが消費する設計とする（ヘルパーは render_fn を呼ぶ側を内部スレッドで管理し、Tk への依存を持たない＝引数のコールバックのみに依存）。`render_fn` がメインスレッド前提である場合の取り扱いはドキュメント文字列に明記する。裸 except 禁止・`except Exception as e:` 形。実装後 `ruff check . && ruff format .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_ocr.py -k "RunParallel or Constants" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr.py` に `def run_with_bounded_buffer` と `import queue` が存在する
    - run_with_bounded_buffer 内に `queue.Queue(maxsize=` と `del b64`（破棄）が存在する（grep -v '^#' で確認）
    - 既存 `pytest tests/test_ocr.py -k "RunParallel or Constants" -q` が exit 0（run_parallel への回帰なし）
    - 裸の `except:` が ocr.py に存在しない（grep で確認）
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>render_fn/ocr_fn コールバックのみに依存し Tk を import しない producer-consumer ヘルパーが ocr.py に追加され、上限付きバッファで同時保持を制限し送信後に b64 を破棄する。</done>
</task>

<task type="auto">
  <name>Task 2: メモリ非蓄積リグレッションテストを作成（D-13・成功基準2の網）</name>
  <files>tests/test_ocr.py</files>
  <read_first>
    - tests/test_ocr.py（アナログ: `FakeProvider` line 23-39・`TestRunParallel` line 256・`TestRunParallelBackoff` line 632）
    - pagefolio/ocr.py（Task 1 で追加した run_with_bounded_buffer のシグネチャ）
    - .planning/phases/06-gemini-provider/06-PATTERNS.md（§tests/test_ocr.py — TestProducerConsumerMemory のスケルトン）
    - .planning/phases/06-gemini-provider/06-RESEARCH.md（§Validation Architecture・§Specific Ideas 成功基準2 検証観点）
  </read_first>
  <behavior>
    - TestProducerConsumerMemory.test_in_flight_count_never_exceeds_maxsize: maxsize 連動の同時保持上限を超えない。`threading.Lock` で in_flight カウンタを増減し、ocr_fn 内で max_observed を記録。`time.sleep(0.01)` で API 処理を模擬。20 ページ（100 ページの代替）で `max_observed <= concurrency + 1` を表明
    - 全ページの結果が results に揃う（`len(results) == len(page_indices)`・取りこぼしなし）
    - キャンセル経路: is_cancelled が途中で True になると残ページを処理せず終了し worker がブロックしない（タイムアウトしない）
  </behavior>
  <action>
    `tests/test_ocr.py` に `TestProducerConsumerMemory` クラスを追加する。`render_fn=lambda i: f"b64-page-{i}"`、`ocr_fn` 相当は `FakeProvider` の side_effect で `threading.Lock` 管理の in_flight カウンタを増減し `max_observed` を記録する。`ocr.run_with_bounded_buffer(provider=..., render_fn=..., page_indices=list(range(20)), concurrency=2, ...)` を直接呼び（Tk 非依存）、`assert max_observed[0] <= 2 + 1` と `assert len(results) == 20` を表明する。キャンセルテストは `is_cancelled` を呼び出し回数で途中 True にし、worker がデッドロックせず有限時間で戻ることを確認する（pytest のデフォルトで完了する程度）。テスト追加後 `ruff check . && ruff format .` を通す。
  </action>
  <verify>
    <automated>pytest tests/test_ocr.py::TestProducerConsumerMemory -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_ocr.py` に `class TestProducerConsumerMemory` が存在する
    - テスト本体に `max_observed` と `<= ` 上限表明・`len(results)` 表明が含まれる
    - `pytest tests/test_ocr.py::TestProducerConsumerMemory -q` が exit 0
    - テストが 30 秒以内に完了する（デッドロックなし）
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>FakeProvider の ocr_image 呼び出し時点で同時保持される画像数がバッファ上限を超えないことと、全ページの結果が取りこぼしなく揃うことが機械的に検証される。</done>
</task>

<task type="auto">
  <name>Task 3: OCRDialog の _render_next_page/_worker を producer-consumer 化し統合プログレス化</name>
  <files>pagefolio/ocr_dialog.py</files>
  <read_first>
    - pagefolio/ocr_dialog.py（アナログ/改修対象: `_images = {}` line 80・`_on_run` の render 起動 line 803-806・`_render_next_page` line 808-850・`_start_worker_thread` line 852-855・`_worker` line 857-923・`_clear_text` の _images.clear line 483・進捗 on_progress line 868-894）
    - pagefolio/ocr.py（Task 1 の run_with_bounded_buffer・run_parallel の on_progress/is_cancelled 契約）
    - .planning/phases/06-gemini-provider/06-PATTERNS.md（§ocr_dialog.py — _on_run/_render_next_page/_worker 改修パターン・_clear_text への _render_queue リセット）
    - .planning/phases/06-gemini-provider/06-RESEARCH.md（§Pattern 4/5・§Pitfall-B/E キャンセル伝搬・§Pitfall-A LM Studio 並列度）
    - pagefolio/lang.py（既存 `ocr_progress_render` line 258/605・`ocr_progress_ocr` line 259/606 — 統合プログレスで参照）
  </read_first>
  <action>
    `pagefolio/ocr_dialog.py` を producer-consumer 化する。`self._images = {}`（line 80）の全ページ一括辞書蓄積を撤廃し、`_on_run` の末尾で `self._render_queue = queue.Queue(maxsize=self.concurrency + 1)`・`self._render_idx = 0` を初期化してから生産者（`_render_next_page`）と消費者（`_start_worker_thread`→`_worker`）を重ねて起動する。`import queue` を追加。`_render_next_page`（メインスレッド・生産者）は 1 ページずつ `self.doc[page_idx]` を取得し、`has_embedded_text(page)` が True なら `page.get_text()` を results へ直接投入し `_skipped_pages` に加えてキューに積まず次へ（スキップは処理済みに数える・D-03）、False なら `page_to_png_b64(page, scale=self._ocr_scale)` を生成し、キャンセル検出付きブロッキング put（`put((page_idx,b64), timeout=0.1)` ループ・`queue.Full` 時に `_cancel_flag.is_set()` 確認・Pitfall-B）でキューへ積む。全ページ完了またはキャンセル時にワーカー数分の `None` 終了シグナルを put する（Pitfall-E）。`after(0, self._render_next_page)` 連鎖で UI フリーズを回避する。`_worker`（ワーカー・消費者）は **fitz アクセスゼロ**（D-04 必達）で `_render_queue.get(timeout=1.0)` ループ → `None` で break → `provider.ocr_image(b64, prompt)`（既存 run_parallel と同じ OCRRetryableError バックオフ・MAX_RETRIES・Retry-After）→ finally で `del b64`（破棄・成功基準2） → `after(0, ...)` 経由で統合プログレス（処理済み done+skipped/total）を更新する。`queue.Empty` タイムアウト時は `_cancel_flag.is_set()` を確認して break（Pitfall-E）。LM Studio 並列度維持のため消費者は `self.concurrency` 本（`provider.max_concurrency` クランプ済み）で動かす（Task 1 ヘルパーの ThreadPoolExecutor を流用するか同等構成）。`_clear_text` で `self._render_queue = None` をリセットし `self._images` 参照を撤廃する。統合プログレス文言は既存 `ocr_progress_ocr` を主軸にし、レンダリング中 cur/total の 2 段表示は廃止する（D-03・文言の最終調整は Plan 03 と整合）。`after(0, ...)` の lambda はデフォルト引数で値束縛する（既存パターン踏襲）。裸 except 禁止。実装後 `ruff check . && ruff format .` を通す。
  </action>
  <verify>
    <automated>python -c "import ast,io; ast.parse(io.open('pagefolio/ocr_dialog.py',encoding='utf-8').read()); print('parse ok')" && pytest tests/test_ocr.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/ocr_dialog.py` に `self._render_queue` と `import queue` が存在し、`self._images = {}` の一括蓄積が撤廃されている（`_images` 残存なしを grep で確認）
    - `_worker` の本体に `fitz`・`get_pixmap`・`page_to_png_b64`・`self.doc[` が出現しない（grep でワーカー内 fitz アクセスゼロを確認・D-04 必達）
    - `_worker` に `del b64`（送信後破棄）が存在する
    - `pytest tests/test_ocr.py -q` が exit 0（TestStartOcrCloudGate・TestOcrDialogLlmConfig 等の OCRDialog 回帰含む）
    - `ocr_dialog.py` が構文エラーなく ast.parse できる
    - `ruff check .` がエラーゼロ
  </acceptance_criteria>
  <done>OCRDialog が全ページ一括辞書蓄積を廃し、生産者（メインで fitz）と消費者（ワーカーで API・fitz アクセスゼロ）が上限付きバッファで重なり実行され、送信後に b64 を破棄し、統合プログレスで進捗を表示する。既存 OCRDialog テストに回帰がない。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| メインスレッド（生産者・fitz） → ワーカースレッド（消費者・API） | base64 画像が queue を介してスレッド境界を越える |
| OCRDialog → Provider.ocr_image | 信頼できない外部 API への画像送信 |
| ユーザーキャンセル → producer/consumer | キャンセルフラグの伝搬境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Denial of Service / リソース枯渇 | OCRDialog 画像メモリ | mitigate | 上限付きバッファ（maxsize=concurrency+1）で同時保持を制限し送信後 `del b64`（成功基準2・OCR-PERF-02）。Task 2 のメモリテストで <= maxsize を機械検証 |
| T-06-07 | Tampering | ワーカースレッド内 fitz アクセス | mitigate | _worker から fitz/get_pixmap/page_to_png_b64/self.doc[ を排除（D-04 必達）。Task 3 で grep 検証。fitz は生産者（メインスレッド）専属 |
| T-06-08 | Denial of Service | キャンセル時のデッドロック | mitigate | put は timeout=0.1 ループで Full 時に cancel 確認（Pitfall-B）・get は timeout=1.0 で Empty 時に cancel 確認・キャンセル時 None 終了シグナルで worker を終了（Pitfall-E）。Task 2 でデッドロック非発生を検証 |
| T-06-09 | Information Disclosure | 進捗・エラー表示 | mitigate | progress/error 文言に b64 画像データ・api_key を含めない（既存 T-04-09/T-05 ガード踏襲） |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 外部 pip パッケージ追加ゼロ（標準ライブラリ queue/threading のみ・RESEARCH §Package Legitimacy Audit「該当なし」）。slopcheck 不要 |
</threat_model>

<verification>
- `pytest -q`（フルスイート）がグリーン — 既存 OCRDialog・run_parallel・Provider 回帰なし
- `ruff check . && ruff format .` がクリーン
- `_worker` に fitz アクセスがゼロ（D-04 必達・grep 検証）
- メモリ非蓄積テスト（同時保持 <= concurrency+1）がグリーン
</verification>

<success_criteria>
- 全ページ base64 の一括メモリ保持が廃止され、render→送信→破棄のパイプラインになる
- ワーカースレッド内に fitz.Document/get_pixmap() の直接呼び出しが存在しない
- 同時保持画像数がバッファ上限（concurrency+1）を超えないことがテストで保証される
- キャンセルがデッドロックなく伝搬する
- LM Studio / Claude / Gemini の既存挙動・APIキーガードに回帰がない
</success_criteria>

<output>
Create `.planning/phases/06-gemini-provider/06-02-SUMMARY.md` when done
</output>
