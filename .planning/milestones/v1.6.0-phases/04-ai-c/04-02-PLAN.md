---
phase: 04-ai-c
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/ocr.py
  - tests/test_provider_ui.py
autonomous: true
requirements: [V16-AI-02]
must_haves:
  truths:
    - "resolve_ocr_prompt('markdown','claude','') が Claude 向けプロバイダ別テンプレート（汎用 OCR_PROMPTS['markdown'] と異なる文字列）を返す"
    - "resolve_ocr_prompt('markdown','gemini','') が Gemini 向けプロバイダ別テンプレートを返す"
    - "resolve_ocr_prompt(preset, provider, custom) で custom が非空のとき custom をそのまま返す（カスタム上書き温存・成功基準3）"
    - "resolve_ocr_prompt('text','lmstudio','') が汎用 OCR_PROMPTS['text'] にフォールバックする"
    - "resolve_ocr_prompt('text','tesseract','') が汎用 OCR_PROMPTS['text'] にフォールバックする"
    - "未定義 preset でも OCR_PROMPTS['text'] を既定フォールバックとして返す（既存 _on_run の ocr_dialog.py:1090 挙動と一致）"
    - "resolve_ocr_prompt は Tk/ネットワーク非依存の純関数である（文字列合成のみ）"
  artifacts:
    - path: "pagefolio/ocr.py"
      provides: "PROVIDER_OCR_PROMPTS 定数（claude/gemini × text/table/markdown）と純関数 resolve_ocr_prompt"
      contains: "def resolve_ocr_prompt"
    - path: "tests/test_provider_ui.py"
      provides: "resolve_ocr_prompt のプロバイダ別/カスタム上書き/フォールバック unit テスト群"
      contains: "resolve_ocr_prompt"
  key_links:
    - from: "tests/test_provider_ui.py"
      to: "pagefolio/ocr.py"
      via: "from pagefolio.ocr import resolve_ocr_prompt, OCR_PROMPTS — 戻り値を直接アサート"
      pattern: "from pagefolio\\.ocr import.*resolve_ocr_prompt"
    - from: "pagefolio/ocr.py resolve_ocr_prompt"
      to: "pagefolio/ocr.py OCR_PROMPTS"
      via: "未定義 provider/preset 時の汎用フォールバック OCR_PROMPTS.get(preset, OCR_PROMPTS['text'])"
      pattern: "OCR_PROMPTS"
---

<objective>
V16-AI-02 のプロンプト解決純関数層を新設する。現状の単一 `OCR_PROMPTS`（プリセットのみ）を「プリセット × プロバイダ × カスタム」の純関数 `resolve_ocr_prompt(preset, provider_name, custom_prompt="")` へ昇格させ、Claude=XML タグ構造／Gemini=明示指示のプロバイダ別テンプレート `PROVIDER_OCR_PROMPTS` を `pagefolio/ocr.py` に追加する。`tests/test_provider_ui.py` を拡張して挙動を単体検証する。

Purpose: プロンプト分岐を Provider クラス内や `_on_run` に散在させず純関数 1 箇所に集約することで、Tk 非生成 unit テスト（582 件ベースライン方針）が書け、後方互換（カスタム上書き）を構造的に保証できる（04-RESEARCH.md Pattern 2 / Pitfall 3）。Provider 層（`_build_payload`）は prompt を文字列で受けるだけで変更不要。本プランは Wave 2 の `_on_run` 差し替え（04-03）が import する契約を確定する。
Output: `pagefolio/ocr.py` への `PROVIDER_OCR_PROMPTS` 定数 + `resolve_ocr_prompt` 純関数の追加・`tests/test_provider_ui.py` への `TestResolveOcrPrompt` テスト群追加。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-c/04-RESEARCH.md
@.planning/phases/04-ai-c/04-PATTERNS.md
@.planning/phases/04-ai-c/04-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
> 本プラン（04-02）が生成・確定するシンボル。後続プラン（04-03）が import する契約。

| 種別 | シンボル | 場所 | 契約 |
|------|----------|------|------|
| 新規定数 | `PROVIDER_OCR_PROMPTS: dict[str, dict[str, str]]` | `pagefolio/ocr.py`（OCR_PROMPTS 直後・ocr.py:33 付近） | provider→preset→文言。`claude`/`gemini` のみ定義、各 text/table/markdown を網羅。`lmstudio`/`tesseract`/`off` は未定義（汎用フォールバック） |
| 新規関数 | `resolve_ocr_prompt(preset, provider_name, custom_prompt="")` | `pagefolio/ocr.py` | 優先順位: custom 非空 > PROVIDER_OCR_PROMPTS[provider][preset] > `OCR_PROMPTS.get(preset, OCR_PROMPTS["text"])`。Tk/ネットワーク非依存 |
| 拡張 | `class TestResolveOcrPrompt` | `tests/test_provider_ui.py` | resolve_ocr_prompt の unit テスト群 |

新規 LANG キー: なし。新規設定キー: なし（プロバイダ別テンプレートは設定不要の定数）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ocr.py に PROVIDER_OCR_PROMPTS + resolve_ocr_prompt を追加</name>
  <files>pagefolio/ocr.py</files>
  <read_first>
    - pagefolio/ocr.py（既存 OCR_PROMPTS:19-32 の辞書スタイル、clamp_retry_after:58- の純ユーティリティ docstring 様式、モジュール定数集約 18-55）
    - .planning/phases/04-ai-c/04-RESEARCH.md（Pattern 2 150-191 の resolve_ocr_prompt シグネチャ・テンプレート文言、Pitfall 3 238-242 カスタム上書き温存、Pitfall 4 244-247 LMStudio/Tesseract フォールバック）
    - .planning/phases/04-ai-c/04-PATTERNS.md（pagefolio/ocr.py 節 45-65: OCR_PROMPTS 直後への配置・フォールバック既定値 text 踏襲）
    - CLAUDE.md（規約: Tk 依存 import を関数内へ持ち込まない・裸 except 禁止・新規 pip 依存ゼロ）
  </read_first>
  <behavior>
    - Test: resolve_ocr_prompt("markdown", "claude", "MY CUSTOM") == "MY CUSTOM"（custom 上書き最優先・成功基準3）
    - Test: resolve_ocr_prompt("markdown", "claude", "") != OCR_PROMPTS["markdown"]（Claude 別テンプレートを返す）
    - Test: resolve_ocr_prompt("markdown", "gemini", "") != OCR_PROMPTS["markdown"]（Gemini 別テンプレートを返す）
    - Test: resolve_ocr_prompt("text", "lmstudio", "") == OCR_PROMPTS["text"]（汎用フォールバック）
    - Test: resolve_ocr_prompt("text", "tesseract", "") == OCR_PROMPTS["text"]（汎用フォールバック）
    - Test: resolve_ocr_prompt("unknownpreset", "lmstudio", "") == OCR_PROMPTS["text"]（未定義 preset の既定フォールバック）
  </behavior>
  <action>
    `pagefolio/ocr.py` の `OCR_PROMPTS`（19-32）直後（33 付近）に新規定数 `PROVIDER_OCR_PROMPTS: dict[str, dict[str, str]]` を追加する。キーは `"claude"` と `"gemini"` のみ。各々に `"text"` / `"table"` / `"markdown"` の 3 プリセット文言を定義（一貫性のため全 3 プリセットを網羅・04-RESEARCH.md Open Question Q3 の推奨）。`claude` は XML タグ構造（例: task / rules タグで「本文のみ・前置きなし」を指示）、`gemini` は明示的・命令的な指示文（「次の画像を OCR し…前置き・後書き・コードフェンスを付けず本文のみ返してください」）。文言ベースは 04-RESEARCH.md:158-177 を採用。`lmstudio`/`tesseract`/`off` はキーを定義しない（汎用フォールバックに委ねる・Pitfall 4）。

    純関数 `resolve_ocr_prompt(preset, provider_name, custom_prompt="")` を追加する。docstring に優先順位（custom_prompt 非空 > プロバイダ別テンプレート > 汎用プリセット）と後方互換意図（custom 上書きは現行 `_on_run`（ocr_dialog.py:1086-1087）挙動を温存・成功基準3）を明記。実装は 04-RESEARCH.md:179-190:
    1. custom_prompt が非空なら custom_prompt を return
    2. by_provider = PROVIDER_OCR_PROMPTS.get(provider_name, {})
    3. preset in by_provider なら by_provider[preset] を return
    4. それ以外は OCR_PROMPTS.get(preset, OCR_PROMPTS["text"]) を return（既存 ocr_dialog.py:1090 と同じ既定値 text で挙動を変えない）

    関数内に `messagebox` 等の Tk 依存 import / ネットワーク呼び出しを持ち込まない（文字列合成のみ）。例外捕捉は不要（辞書 get のみ）だが、もし加えるなら型指定（裸 except 禁止）。`# type: ignore` は使わない。
  </action>
  <verify>
    <automated>python -c "from pagefolio.ocr import resolve_ocr_prompt, OCR_PROMPTS; assert resolve_ocr_prompt('markdown','claude','MY')=='MY'; assert resolve_ocr_prompt('text','lmstudio','')==OCR_PROMPTS['text']; assert resolve_ocr_prompt('markdown','claude','')!=OCR_PROMPTS['markdown']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ソース表明: `grep -c 'def resolve_ocr_prompt' pagefolio/ocr.py` が 1 以上、`grep -c 'PROVIDER_OCR_PROMPTS' pagefolio/ocr.py` が 2 以上（定義 + 参照）
    - ソース表明: PROVIDER_OCR_PROMPTS に `claude` と `gemini` の両キーが存在し、`lmstudio`/`tesseract` キーは存在しない（`python -c "from pagefolio.ocr import PROVIDER_OCR_PROMPTS as P; assert set(P)=={'claude','gemini'}; assert 'markdown' in P['claude'] and 'markdown' in P['gemini']; print('OK')"` が OK）
    - 挙動表明: custom 上書き温存 — `python -c "from pagefolio.ocr import resolve_ocr_prompt as r; assert r('table','gemini','C')=='C'; print('OK')"` が OK
    - 挙動表明: フォールバック — `python -c "from pagefolio.ocr import resolve_ocr_prompt as r, OCR_PROMPTS as O; assert r('text','tesseract','')==O['text']; assert r('zzz','off','')==O['text']; print('OK')"` が OK
    - CLI 出力: `ruff check pagefolio/ocr.py` がエラー 0 件で通過
  </acceptance_criteria>
  <done>
    `pagefolio/ocr.py` に `PROVIDER_OCR_PROMPTS`（claude/gemini × 3 プリセット）と純関数 `resolve_ocr_prompt` が存在し、custom 上書き > プロバイダ別 > 汎用フォールバックの優先順位で文字列を返す。Tk 非依存。ruff 通過。
  </done>
</task>

<task type="auto">
  <name>Task 2: test_provider_ui.py に resolve_ocr_prompt テスト群を追加</name>
  <files>tests/test_provider_ui.py</files>
  <read_first>
    - tests/test_provider_ui.py（アナログ: 冒頭 docstring 4-8 のロジック層のみ検証方針、TestModelSupportsEffort 31- のクラス様式・Tk ウィジェット生成なし）
    - .planning/phases/04-ai-c/04-RESEARCH.md（プロンプト解決テスト 324-337 の 3 ケース）
    - .planning/phases/04-ai-c/04-PATTERNS.md（tests/test_provider_ui.py 節 122-133: 新規クラス TestResolveOcrPrompt・3 ケース）
    - .planning/phases/04-ai-c/04-VALIDATION.md（Per-Task Verification Map: V16-AI-02 の automated command）
    - pagefolio/ocr.py（Task 1 で確定した resolve_ocr_prompt / PROVIDER_OCR_PROMPTS / OCR_PROMPTS）
  </read_first>
  <action>
    `tests/test_provider_ui.py` に新規テストクラス `class TestResolveOcrPrompt:`（docstring に対応 Req `V16-AI-02` を明記）を追加する。ファイル末尾へ追記し、既存の `TestModelSupportsEffort` 等は変更しない。import は既存 import ブロックに `from pagefolio.ocr import resolve_ocr_prompt, OCR_PROMPTS` を追加（純関数なのでスタブ/Tk 生成は不要）。`tests/**` は S101 除外のため `assert` 直書きでよい。

    以下のメソッドを実装（04-RESEARCH.md:329-336 起点・拡充）:
    - `test_custom_overrides_provider_template`: `resolve_ocr_prompt("markdown","claude","MY CUSTOM") == "MY CUSTOM"`（成功基準3・Pitfall 3）
    - `test_lmstudio_falls_back_to_generic`: `resolve_ocr_prompt("text","lmstudio","") == OCR_PROMPTS["text"]`（Pitfall 4）
    - `test_tesseract_falls_back_to_generic`: `resolve_ocr_prompt("text","tesseract","") == OCR_PROMPTS["text"]`（Pitfall 4）
    - `test_claude_markdown_uses_provider_template`: `resolve_ocr_prompt("markdown","claude","") != OCR_PROMPTS["markdown"]`
    - `test_gemini_markdown_uses_provider_template`: `resolve_ocr_prompt("markdown","gemini","") != OCR_PROMPTS["markdown"]`
    - `test_unknown_preset_falls_back_to_text`: `resolve_ocr_prompt("zzz","off","") == OCR_PROMPTS["text"]`（既定フォールバック）
  </action>
  <verify>
    <automated>python -m pytest tests/test_provider_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ソース表明: `grep -c 'class TestResolveOcrPrompt' tests/test_provider_ui.py` が 1、`grep -c 'resolve_ocr_prompt' tests/test_provider_ui.py` が 6 以上
    - ソース表明: `tests/test_provider_ui.py` に新規の Tk 生成（`tk.Tk(`）が追加されていない
    - テストコマンド: `python -m pytest tests/test_provider_ui.py -q` が全件 passed（custom 上書き・claude/gemini 別テンプレ・lmstudio/tesseract フォールバック・未定義 preset の 6 ケース以上を含む）
    - CLI 出力: `ruff check tests/test_provider_ui.py` がエラー 0 件で通過
    - 回帰: `python -m pytest -q` がフルスイート緑（582 件ベースライン + 新規テスト分・既存に赤を出さない）
  </acceptance_criteria>
  <done>
    `tests/test_provider_ui.py` に `TestResolveOcrPrompt` が追加され、custom 上書き/プロバイダ別テンプレ/汎用フォールバック/未定義 preset の 6 ケース以上が全件パス。Tk 非生成。フルスイート緑。ruff 通過。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| カスタムプロンプト（ユーザー設定）→ resolve_ocr_prompt → Provider | ユーザー由来の `ocr_custom_prompt` 文字列が解決され Provider へ渡る。本プランは文字列合成のみで送信は行わない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-IDISC | Information Disclosure | プロンプト文言/カスタムプロンプトのログ露出 | accept | `resolve_ocr_prompt` は logger を持たずプロンプト本文をログ出力しない（既存規約踏襲・04-RESEARCH.md:438）。プロンプト文言に秘匿情報は含めない（Runtime State Inventory 220） |
| T-04-TAMPER | Tampering | プロバイダ別テンプレート定数 | accept | `PROVIDER_OCR_PROMPTS` はソース定数で実行時改変経路なし。カスタム上書きは既存設定機構のみ経由（新規入力経路を増やさない） |
</threat_model>

<verification>
- `python -m pytest tests/test_provider_ui.py -x -q` 全件 passed
- `python -m pytest -q` フルスイート緑（582 件ベースライン + 新規テスト分・既存に赤なし）
- `ruff check . && ruff format .` 通過
- `pagefolio/ocr.py` に Tk 依存 import が `resolve_ocr_prompt` 内へ持ち込まれていない（純関数）
</verification>

<success_criteria>
- `resolve_ocr_prompt` が custom 上書き > プロバイダ別テンプレート（claude/gemini）> 汎用フォールバックの優先順位で文字列を返す（V16-AI-02 + 成功基準3）
- LMStudio/Tesseract/off は汎用 `OCR_PROMPTS` にフォールバックする（Pitfall 4）
- 全解決が Tk/ネットワーク非依存純関数で `test_provider_ui.py` が Tk 非生成で検証可能
- 04-03 が import する `resolve_ocr_prompt` の戻り値契約が確定する
</success_criteria>

<output>
Create `.planning/phases/04-ai-c/04-02-SUMMARY.md` when done
</output>
