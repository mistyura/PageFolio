---
phase: 02-pagination
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - pagefolio/lang.py
  - pagefolio/ui_builder.py
  - pagefolio/viewer.py
  - pagefolio/dnd.py
  - tests/test_pagination.py
  - 開発履歴.md
autonomous: false
requirements:
  - V16-UI-03
must_haves:
  truths:
    - "サムネイル canvas の下に独立フッター行があり、◀ ▶ ＋ 範囲ラベル ＋ 件数 Spinbox が並ぶ（D-01/D-02/D-03）"
    - "件数 Spinbox は ttk.Spinbox + state=readonly + from_=10/to=100/increment=10 で範囲外値が入らない（D-04・Pitfall 3）"
    - "件数変更が settings に永続化され即再描画される（D-03/D-05）"
    - "前/次窓ボタンが window_nav_state に従い、単一窓でもフッター行は常に表示・ボタンのみ disabled（D-09）"
    - "D&D ドロップ先がローカル位置から to_global で全ページインデックスへ変換され、意図したページが正しい位置へ移動する（D-06）"
    - "操作後に current_page が窓外へ出たら、_refresh_all で current を含む窓へ自動追従する（D-11）"
    - "窓移動・件数変更ハンドラ呼び出しの前後で thumb_cache の同一性（id）が不変で、キャッシュがクリアされない（A1・Pitfall 2・TestThumbCacheRetention で自動検証）"
  artifacts:
    - path: "pagefolio/ui_builder.py"
      provides: "ナビ/件数フッター行（◀ ▶ ＋ 範囲ラベル ＋ 件数 Spinbox）"
      contains: "ttk.Spinbox"
    - path: "pagefolio/viewer.py"
      provides: "窓移動ハンドラ・件数変更ハンドラ・_refresh_all 窓正規化（D-11 追従）"
      contains: "window_for_page"
    - path: "pagefolio/dnd.py"
      provides: "D&D ドロップ先の local→global 換算（D-06）"
      contains: "to_global"
    - path: "pagefolio/lang.py"
      provides: "範囲ラベル・件数ラベルの ja/en 同一キー"
      contains: "page_size"
    - path: "tests/test_pagination.py"
      provides: "窓移動・件数変更ハンドラの thumb_cache 非クリア（id 不変）を SimpleNamespace スタブで検証する TestThumbCacheRetention（A1 自動アサート）"
      contains: "class TestThumbCacheRetention"
  key_links:
    - from: "pagefolio/ui_builder.py"
      to: "pagefolio/viewer.py"
      via: "件数 Spinbox command=_on_page_size_change / ◀▶ ボタン command=_prev_window/_next_window"
      pattern: "_on_page_size_change|_prev_window|_next_window"
    - from: "pagefolio/dnd.py"
      to: "pagefolio/pagination.py"
      via: "window_bounds + to_global でドロップ先ローカル位置を全ページ index へ換算"
      pattern: "to_global"
    - from: "pagefolio/viewer.py"
      to: "pagefolio/pagination.py"
      via: "_refresh_all で clamp_window_start + window_for_page により窓を正規化し、window_nav_state/window_label でナビ更新"
      pattern: "window_for_page|window_nav_state|window_label"
---

<objective>
ナビ/件数フッター UI を組み上げ、D&D ドロップの index 換算と current_page 窓追従を完成させて、ページネーション機能をユーザーが操作可能な状態にする。

これは成功基準 1〜4 をユーザー導線として完成させる仕上げプラン。02-01 の純関数層と 02-02 の窓状態・窓範囲描画の上に、(a) `_build_thumb_panel` canvas 下のフッター行（D-01/D-02/D-03）、(b) 窓移動・件数変更ハンドラ（D-05）、(c) `_refresh_all` での窓正規化と D-11 追従、(d) D&D ドロップ先の local→global 換算（D-06）、(e) ja/en 同一キーの新規 LANG、(f) 開発履歴.md / APP_VERSION 同期を載せる。

Purpose: index 変換は 02-01 の純関数 1 箇所のみを経由し、`+ window_start` を散在させない（Anti-Pattern）。selected_pages は全ページ index のまま（D-07）で既存 bulk_move を無改修で再利用する（Don't Hand-Roll）。
Output: フッター UI・窓/件数ハンドラ・_refresh_all 正規化・D&D 換算・LANG キー・ドキュメント同期。末尾に人手 UAT チェックポイント。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pagination/02-CONTEXT.md
@.planning/phases/02-pagination/02-RESEARCH.md
@.planning/phases/02-pagination/02-PATTERNS.md
@.planning/phases/02-pagination/02-VALIDATION.md
@pagefolio/pagination.py
@pagefolio/ui_builder.py
@pagefolio/viewer.py
@pagefolio/dnd.py
@pagefolio/lang.py
@pagefolio/constants.py
</context>

<artifacts_this_phase_produces>
> このプラン（02-03）が作るシンボルを ★ で示す。全体一覧は 02-01-PLAN.md 参照。

**新規ウィジェット参照（_build_thumb_panel フッター行・D-02）:**
- ★ 前窓ボタン ◀（`command=self._prev_window`）
- ★ 次窓ボタン ▶（`command=self._next_window`）
- ★ 範囲ラベル `tk.Label`（「1–20 / 全120」・window_label が文字列生成）
- ★ 件数 Spinbox `ttk.Spinbox`（from_=10, to=100, increment=10, state="readonly"）＋ `self.page_size_var`（IntVar・初期値 settings 復元）

**新規ハンドラ（viewer.py）:**
- ★ `self._on_page_size_change`（件数変更 → 保存 → 窓正規化 → 再描画・D-03/D-05）
- ★ `self._prev_window` / `self._next_window`（窓移動・保存不要・thumb_cache 非クリア）

**新規テストクラス（tests/test_pagination.py・02-01 で作成済みファイルに追記）:**
- ★ `TestThumbCacheRetention`（SimpleNamespace スタブで上記 3 ハンドラ呼び出し前後の `id(thumb_cache)` 不変を自動アサート・A1/Pitfall 2・W2）

**変更（D-06 換算・D-11 追従）:**
- ★ `dnd.py` `_dnd_drop`（dest ローカル → to_global → 既存 move_page/bulk_move）
- ★ `viewer.py` `_refresh_all`（clamp_window_start + window_for_page で窓正規化・window_nav_state/window_label でナビ更新）

**追加 LANG キー（ja/en 同一キー・文言は実装裁量）:**
- ★ 範囲ラベル雛形（例 `window_label_fmt`）・件数ラベル（例 `page_size_label`）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: lang.py に ja/en 同一キーを追加し、ui_builder にナビ/件数フッター行を構築</name>
  <files>pagefolio/lang.py, pagefolio/ui_builder.py</files>
  <read_first>
    - pagefolio/lang.py（ja:12-15 / en:452-455 の `panel_pages` / `dnd_hint` / `select_all` / `deselect` キー。新規キーは ja/en 両方へ同一キーで追加しキー数を一致させる・CLAUDE.md）
    - pagefolio/ui_builder.py:173-239（`_build_thumb_panel`。hdr〔174-189〕/ sel_frame〔191-198〕/ zoom_frame〔200-211〕/ canvas 群〔213-239〕。新規フッターは canvas 群の後ろ）
    - pagefolio/ui_builder.py:200-211（`zoom_frame` の作法 — `tk.Frame` を `pack(fill="x")`・`self.settings.get(...)` で初期値・var バインド）
    - .planning/phases/02-pagination/02-RESEARCH.md `## Common Pitfalls` Pitfall 3（ttk.Spinbox readonly）・`### 前/次窓ボタンの enable/disable`・`### 範囲ラベルの生成`
    - .planning/phases/02-pagination/02-PATTERNS.md `### pagefolio/ui_builder.py`・`### pagefolio/lang.py`
    - MEMORY: tkinter-readonly-widget-gotchas（ttk.Spinbox + state=readonly を使う理由）
  </read_first>
  <action>
    `pagefolio/lang.py` の ja 辞書（`panel_pages` 近傍 12-15 行）と en 辞書（452-455 行）の**両方に同一キーで**新規 LANG キーを追加する（CLAUDE.md: キー数の左右一致を維持）。最低限: 件数ラベル（例 `page_size_label` = ja「表示:」/ en「Show:」）と件数単位（例 ja「件」/ en 空または「pages」）。範囲ラベルの数値部分は window_label（pagination.py）が生成するため、LANG には文言の固定部分のみ置く（区切り記号・文言は実装裁量・D-01）。
    `pagefolio/ui_builder.py` の `_build_thumb_panel` で canvas 群（213-239 行）の**後ろ**に新規フッター `tk.Frame` を `pack(fill="x", padx=6, pady=...)` で追加する（D-02）。zoom_frame（200-211）と同じ作法で以下を配置する:
    - 前窓ボタン `ttk.Button(text="◀", command=self._prev_window)` を `self._prev_window_btn` 参照に保持（state 制御用）。
    - 次窓ボタン `ttk.Button(text="▶", command=self._next_window)` を `self._next_window_btn` 参照に保持。
    - 範囲ラベル `tk.Label`（bg=`C["BG_PANEL"]`, fg=`C["TEXT_MAIN"]`, font=`self._font(...)`）を `self._window_range_label` 参照に保持。
    - 件数 Spinbox は `ttk.Spinbox(from_=10, to=100, increment=10, state="readonly", textvariable=self.page_size_var, command=self._on_page_size_change, width=4)`。`self.page_size_var = tk.IntVar(value=self.settings.get("thumb_page_size", 20))`。件数ラベル（`self._t("page_size_label")`）を左に添える。
    色は必ず `C["..."]`、フォントは `self._font(...)`、ボタンスタイルは通常操作 "TButton"（CLAUDE.md 規約）。**`tk.Spinbox` ではなく `ttk.Spinbox` + state="readonly"** を使う（手入力禁止で範囲外値が原理的に入らない・暗テーマ readonly 背景色問題回避・Pitfall 3 / MEMORY）。ハンドラ本体（_prev_window / _next_window / _on_page_size_change）は Task 2 で viewer.py に実装する（このタスクでは command 参照のみ束縛）。
  </action>
  <verify>
    <automated>python -c "from pagefolio.lang import LANG; assert set(LANG['ja'])==set(LANG['en']), 'ja/en キー不一致'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from pagefolio.lang import LANG; assert set(LANG['ja'])==set(LANG['en'])"` が exit 0（ja/en キー数一致・CLAUDE.md）
    - `grep -c 'page_size_label' pagefolio/lang.py` が 2（ja/en 両方）
    - `grep -c 'ttk.Spinbox' pagefolio/ui_builder.py` が 1 以上、`grep -c 'state="readonly"' pagefolio/ui_builder.py` が 1 以上
    - `grep -c 'tk.Spinbox' pagefolio/ui_builder.py` が 0（ttk のみ使用・Pitfall 3）
    - `grep -c '_prev_window\|_next_window\|_on_page_size_change' pagefolio/ui_builder.py` が 3 以上（command 束縛）
    - `ruff check pagefolio/lang.py pagefolio/ui_builder.py` が exit 0
  </acceptance_criteria>
  <done>
    lang.py の ja/en に件数ラベル等の同一キーが追加されキー数一致。_build_thumb_panel の canvas 下に ◀ ▶ ＋ 範囲ラベル ＋ ttk.Spinbox(readonly) のフッター行が組まれ、page_size_var が settings から初期化。ruff 通過。
  </done>
</task>

<task type="auto">
  <name>Task 2: viewer に窓/件数ハンドラと _refresh_all 窓正規化、dnd に local→global 換算を実装し、thumb_cache 非クリアを自動テストで固定</name>
  <files>pagefolio/viewer.py, pagefolio/dnd.py, tests/test_pagination.py</files>
  <read_first>
    - pagefolio/viewer.py:146-154（`_on_thumb_zoom_release` — 「設定保存 → 無効化 → 再描画」の定石。件数変更ハンドラの手本。ただし窓移動では _invalidate_thumb_cache を呼ばない）
    - pagefolio/viewer.py:157-172（`_refresh_all` — 描画直前の集約点。prev/next ボタン state 制御〔165-172〕が窓ナビ state 制御の手本。ここに窓正規化を載せる）
    - pagefolio/viewer.py:108-118（`_prev_page` / `_next_page` — current_page を変える箇所。後続 _refresh_all で D-11 追従が一元化される）
    - pagefolio/dnd.py:94-135（`_dnd_drop` — 冒頭 src/dest〔95-100〕。dest ローカルを to_global で変換し既存クランプ〔100〕を変換後に適用。bulk_move〔101-117〕/ move_page〔118-135〕は無改修）
    - pagefolio/dnd.py:73-92（`_dnd_dest_index` — 返す値は窓内フレーム位置=ローカル。変換は _dnd_drop の 1 箇所のみ）
    - .planning/phases/02-pagination/02-RESEARCH.md `## Common Pitfalls` Pitfall 2（thumb_cache 非クリア）・Pitfall 4（窓末尾ドロップのクランプ）・Pitfall 5（操作後の窓正規化）・`## Architecture Patterns` Pattern 2/3・Anti-Patterns
    - .planning/phases/02-pagination/02-PATTERNS.md `### pagefolio/viewer.py`（3〜5 番）・`### pagefolio/dnd.py`
    - tests/test_viewer.py:1-62（`_make_stub` 方式の SimpleNamespace スタブで Mixin メソッドを GUI なし呼び出しするパターン。TestThumbCacheRetention の手本）
  </read_first>
  <action>
    `pagefolio/viewer.py` の冒頭 import に `window_for_page, clamp_window_start, window_nav_state, window_label, clamp_page_size`（と既存の window_bounds, to_global）を `from pagefolio.pagination import ...` で追加する（`clamp_page_size` は 02-01 の確定名・W1）。
    ハンドラを実装する: `_on_page_size_change(self, event=None)` は `page_size_var.get()` の生値を 02-01 の `clamp_page_size` に通して読み（`page_size_var.get()` が空文字で TclError を投げうるので呼び出しを `except (ValueError, tk.TclError)` で囲み既定 20 へフォールバック。clamp_page_size 自体は tkinter 非依存で ValueError/TypeError のみ捕捉する純関数なので、TclError は本ハンドラ側で捕捉する）、`self.settings["thumb_page_size"]` に保存し `_save_settings(self.settings)`、`self._page_size` を更新、`self._page_window_start = window_for_page(self.current_page, self._page_size)`（D-11）してから `_refresh_all()` を呼ぶ（_on_thumb_zoom_release の定石に倣う・ただし thumb_cache は不変なので _invalidate_thumb_cache は呼ばない・Pitfall 2）。`_prev_window` / `_next_window` は `self._page_window_start` を ±`self._page_size` して `clamp_window_start` で正規化し `_refresh_all()` を呼ぶ（保存不要・thumb_cache 非クリア）。
    `_refresh_all`（157-172）に窓正規化を集約する（描画 `_build_thumbnails` の前）: `n = len(self.doc) if self.doc else 0` を取り、`self._page_window_start = clamp_window_start(self._page_window_start, self._page_size, n)` → `self._page_window_start = window_for_page(self.current_page, self._page_size)`（current を含む窓へ追従・D-11・Pitfall 5）。続けて `window_nav_state` で `self._prev_window_btn` / `self._next_window_btn` の state を制御（165-172 の prev/next パターンに倣い単一窓でも行は表示・ボタンのみ disabled・D-09）、`self._window_range_label.configure(text=...)` に `window_label(self._page_window_start, self._page_size, n)` を反映（doc 未オープン時も安全な文字列）。
    `pagefolio/dnd.py` の冒頭に `from pagefolio.pagination import window_bounds, to_global` を追加。`_dnd_drop`（94-135）冒頭で `dest = self._dnd_dest_index(event)`（ローカル）を取得後、`lo, hi = window_bounds(self._page_window_start, self._page_size, n)` → `dest = to_global(dest, lo)` → 既存クランプ `dest = max(0, min(dest, n))`（100 行）を変換後に適用する（D-06・Pitfall 4 の窓末尾クランプ）。`src`（`_dnd_src_idx`）は _add_thumb_placeholder が全ページ index で束縛するため変換不要。bulk_move（101-117）/ move_page（118-135）は無改修（selected_pages が全ページ index のため new_order がそのまま正しい・D-07・Don't Hand-Roll）。`_dnd_dest_index` / `_dnd_show_indicator` のインジケータ描画はローカルフレーム配列で完結するため変換不要（変換は _dnd_drop の 1 箇所のみ・Anti-Pattern「散在させない」）。
    あわせて W2（thumb_cache 非クリアのリグレッション耐性）を自動アサート化するため、`tests/test_pagination.py` に `TestThumbCacheRetention` クラスを追加する: `types.SimpleNamespace`（または最小スタブ）で `_page_window_start` / `_page_size` / `current_page` / `doc=None` / `thumb_cache={...}`（既知の dict オブジェクト）と、副作用を無効化したスタブメソッド（`_refresh_all`・`_save_settings`・`settings={}`・`page_size_var` は固定値を返す簡易スタブ）を持つオブジェクトに、`ViewerMixin._prev_window` / `_next_window` / `_on_page_size_change` を非束縛で `func(stub)` 呼び出しする。各ハンドラ呼び出しの前後で `id(stub.thumb_cache)` が不変であること（=`_invalidate_thumb_cache` も `thumb_cache = {}` 再代入も行われていない）を assert する（Pitfall 2・A1）。これによりキャッシュ非クリアが目視 UAT 依存から自動 unit へ昇格する。GUI 起動なしでロジック部分のみ検証できるよう、これらハンドラは `_refresh_all` を最後に 1 回呼ぶだけの構造（状態更新→再描画委譲）を保つこと。
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` 全スイートが exit 0（既存回帰なし）
    - `grep -c 'window_for_page' pagefolio/viewer.py` が 1 以上（D-11 追従が _refresh_all/件数ハンドラに）
    - `grep -c 'window_nav_state' pagefolio/viewer.py` が 1 以上、`grep -c 'window_label' pagefolio/viewer.py` が 1 以上
    - `grep -c 'to_global' pagefolio/dnd.py` が 1 以上（D&D ドロップ先換算）
    - `pytest tests/test_pagination.py::TestThumbCacheRetention -x -q` が exit 0（_prev_window / _next_window / _on_page_size_change 呼び出し前後で `id(thumb_cache)` 不変＝キャッシュ非クリアを自動アサート・Pitfall 2 / A1 — 目視 UAT 依存を解消）
    - `grep -c 'class TestThumbCacheRetention' tests/test_pagination.py` が 1
    - `grep -v '^#' pagefolio/viewer.py | grep -c 'except:'` が 0、`grep -v '^#' pagefolio/dnd.py | grep -c 'except:'` が 0（裸 except なし）
    - `ruff check pagefolio/viewer.py pagefolio/dnd.py tests/test_pagination.py` が exit 0
  </acceptance_criteria>
  <done>
    _on_page_size_change / _prev_window / _next_window が実装され、_refresh_all が clamp_window_start + window_for_page で窓正規化し window_nav_state/window_label でナビ更新。_dnd_drop が dest を to_global 換算。bulk_move/move_page 無改修。TestThumbCacheRetention が thumb_cache 同一性（id 不変）を自動検証し A1 のリグレッション耐性を確保。`pytest` 全 green、ruff 通過。
  </done>
</task>

<task type="auto">
  <name>Task 3: 開発履歴.md に変更を追記し APP_VERSION / README バッジを同期</name>
  <files>開発履歴.md, pagefolio/constants.py, README.md</files>
  <read_first>
    - pagefolio/constants.py（`APP_VERSION` — バージョンの真の情報源。CLAUDE.md 規約）
    - 開発履歴.md（最新エントリの記載フォーマット。既存ルール通りに日本語で追記）
    - README.md（バージョンバッジ箇所。APP_VERSION と同期）
    - CLAUDE.md 「変更時のチェックリスト」「言語ルール」
  </read_first>
  <action>
    `開発履歴.md` に Phase 2（大量ページのページネーション表示・V16-UI-03）の変更内容を既存エントリのフォーマットに倣って日本語で追記する: サムネイル一覧をページ単位（既定 20・範囲 10〜100）で窓表示、件数を `thumb_page_size` として `pagefolio_settings.json` に永続化、ナビフッター（◀ ▶ ＋ 範囲ラベル ＋ 件数 Spinbox）追加、ページング下でも D&D 並び替え・複数選択が全ページインデックスと整合（local↔global 変換層 `pagination.py` 新設）、current_page 窓追従（D-11）。`pagefolio/constants.py` の `APP_VERSION` を次パッチ/マイナー版へ更新し（具体値は実装時に既存版番から決定）、`README.md` のバージョンバッジを同値に同期する。pyproject.toml は編集しない（CLAUDE.md 禁止事項）。
  </action>
  <verify>
    <automated>python -c "from pagefolio.constants import APP_VERSION; print(APP_VERSION)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from pagefolio.constants import APP_VERSION; print(APP_VERSION)"` が新バージョンを出力
    - `grep -qF "$(python -c 'from pagefolio.constants import APP_VERSION; print(APP_VERSION)')" README.md` が exit 0（README バッジが APP_VERSION と一致）
    - 開発履歴.md に Phase 2 / V16-UI-03 / ページネーション に言及する新規エントリが存在する
    - `git diff --name-only pyproject.toml` が空（pyproject.toml 未編集・CLAUDE.md 禁止事項）
  </acceptance_criteria>
  <done>
    開発履歴.md に Phase 2 変更が日本語で追記され、APP_VERSION が更新されて README バッジと一致。pyproject.toml 未編集。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    大量ページのページネーション表示一式: サムネイル窓表示（既定 20）、ナビフッター（◀ ▶ ＋ 範囲ラベル ＋ 件数 Spinbox）、件数永続化、ページング下の D&D・複数選択の全ページインデックス整合、current_page 窓追従。純ロジック層は test_pagination.py で全境界値検証済み（自動）、thumb_cache 非クリア（A1）は TestThumbCacheRetention で id 不変を自動アサート済み。本チェックポイントは 02-VALIDATION.md `## Manual-Only Verifications` の GUI 実機確認（A2/S4 ＋ A1 の体感速度）を確定する。
  </what-built>
  <how-to-verify>
    `python pagefolio.py` で起動し、20 ページ超（できれば 47 ページ程度）の PDF を開いて以下を確認する:
    1. サムネイル一覧がページ単位で区切られ、フッターに「1–20 / 全47」等の範囲ラベルと ◀ ▶・件数 Spinbox が表示される（D-01/D-02/D-09）。
    2. ▶ で次窓へ送ると「21–40 / 全47」→「41–47 / 全47」（端数最終窓・D-10）と推移し、◀ で戻れる。前後端で対応ボタンが disabled。
    3. 件数 Spinbox を 20→10 や 20→50 に変更すると即再描画され、アプリを再起動しても件数が復元される（D-03/D-05・永続化）。
    4. 窓を往復してもサムネイルが毎回ちらつかず即時表示される（thumb_cache 非クリア・A1。キャッシュ不変は TestThumbCacheRetention で自動検証済み・本項は体感速度の補助確認）。
    5. 窓2 を表示中に 1 ページをドラッグして別位置へ移動 → 意図したページが正しい全ページ位置へ移る（D-06・A2）。
    6. 窓をまたいで複数選択（窓1 と窓2 のページを Ctrl 選択）し、削除または回転 → 選択どおり全ページに適用され、選択ハイライトが正しいページに出る（D-07/S4）。
    7. 「全選択」を押すと全ドキュメントが選択され、窓を移動しても選択が維持される（D-08）。
    8. ページ削除・前/次ページで current_page が窓外へ出ても、current を含む窓へ自動追従する（D-11）。
    確認後、`ruff check . && ruff format .` と `pytest` が緑であることを最終確認する。
  </how-to-verify>
  <resume-signal>すべて意図どおりなら「approved」、問題があれば該当番号と症状を記載してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー件数入力（Spinbox）→ settings 永続化 | 件数が 10〜100 範囲・数値であることを保証する境界（唯一の現実的な堅牢性ポイント） |
| D&D ドロップ位置 → ページ並び替え | ローカル位置を全ページ index へ正しく換算しないと誤ページ移動になる（整合性・自己障害） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Denial of Service（ローカル・自己起因） | 件数 Spinbox / 件数読み出し（範囲外・空文字が窓計算を破綻） | mitigate | `ttk.Spinbox(from_=10, to=100, increment=10, state="readonly")` で手入力を禁止し範囲外値を原理的に排除（Pitfall 3）。読み出し時も件数クランプヘルパー（02-02）＋ `except (ValueError, tk.TclError)` 既定 20 フォールバックで二重防御 |
| T-2-02 | Tampering（整合性・自己障害） | _dnd_drop の dest 換算（窓末尾ドロップで len(doc) 超過） | mitigate | `dest = max(0, min(to_global(dest, lo), n))` で全ページ範囲にクランプ（Pitfall 4）。変換は純関数 1 箇所経由で散在させない |
| T-2-03 | Information Disclosure | thumb_page_size の settings 永続化 | accept | 数値設定のみで機密に非該当。`_SENSITIVE_KEYS` ガードは既存・本キーは無関係 |

> ASVS Level 1 / Block on: high。**high 相当の脅威なし**。本フェーズはローカル UI のページネーション表示で、認証・セッション・ネットワーク送信・暗号・外部機密の取り扱いを含まない（02-RESEARCH.md `## Security Domain`）。現実的な観点は件数入力の値域クランプ（V5 入力検証）と D&D 換算のクランプ（整合性）のみで、いずれも堅牢性として実装・確認する。
</threat_model>

<verification>
- `pytest` 全スイート green（既存 + test_pagination 全テスト・TestThumbCacheRetention 含む・回帰なし）
- `ruff check . && ruff format .` 通過
- ja/en LANG キー数一致（`set(LANG['ja']) == set(LANG['en'])`）
- _build_thumb_panel に ttk.Spinbox(readonly) フッター行（tk.Spinbox 不使用）
- _dnd_drop が to_global で dest 換算（grep 確認）
- _refresh_all が window_for_page で D-11 追従（grep 確認）
- thumb_cache 非クリア（A1）が TestThumbCacheRetention で自動アサート（id 不変・目視 UAT 依存を解消）
- 人手 UAT チェックポイント承認（窓送り・件数永続化・D&D 整合・複数選択・窓追従。A1 の速さ体感は補助確認）
- APP_VERSION 更新 + README バッジ + 開発履歴.md 同期、pyproject.toml 未編集
</verification>

<success_criteria>
- サムネイル canvas 下にナビ/件数フッター行があり、◀ ▶ ＋ 範囲ラベル ＋ ttk.Spinbox(readonly) が D-01/D-02/D-03 どおりに配置される
- 件数変更が即再描画され settings に永続化・次回復元される（D-04/D-05）
- 単一窓でもフッター行は常に表示・ボタンのみ disabled（D-09）、端数最終窓が正しく表示（D-10）
- D&D ドロップ先がローカル → to_global で全ページ index へ換算され意図したページが正しく移動（D-06）
- 複数選択が全ページ index を保ち窓またぎでも維持され、ページ操作が選択どおり適用（D-07/D-08）
- current_page が窓外へ出たら自動追従（D-11）、窓往復で thumb_cache 非クリアで高速描画（A1・id 不変を TestThumbCacheRetention が自動アサート）
- 人手 UAT で全 8 項目が意図どおり、ドキュメント/バージョン同期完了
</success_criteria>

<output>
Create `.planning/phases/02-pagination/02-03-SUMMARY.md` when done
</output>
