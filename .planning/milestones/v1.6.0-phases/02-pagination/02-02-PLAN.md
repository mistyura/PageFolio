---
phase: 02-pagination
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pagefolio/settings.py
  - pagefolio/app.py
  - pagefolio/viewer.py
autonomous: true
requirements:
  - V16-UI-03
must_haves:
  truths:
    - "表示件数の既定 20 が DEFAULT_SETTINGS にあり、旧設定ファイルでも setdefault で補完される（D-04/D-05・後方互換）"
    - "settings からの件数読み出しが 10〜100 にクランプされ、空文字/非数値は既定 20 にフォールバックする（D-04・堅牢性）"
    - "窓状態 self._page_window_start / self._page_size が __init__ で初期化され、件数は settings から復元される（D-05）"
    - "_build_thumbnails が窓範囲 [lo, hi) のみ描画し、_add_thumb_placeholder に渡す i は全ページ index のまま（D-06 の src 整合）"
    - "_refresh_thumbs_selection_only が enumerate 位置を to_global で全ページ index へ変換して selected_pages と照合する（Pitfall 1・D-07）"
  artifacts:
    - path: "pagefolio/settings.py"
      provides: "thumb_page_size 既定 20（件数クランプ純関数 clamp_page_size は 02-01 で pagination.py に集約済み・本プランは import して使用・W1）"
      contains: "thumb_page_size"
    - path: "pagefolio/viewer.py"
      provides: "窓範囲描画と選択窓化（local↔global 変換適用）"
      contains: "window_bounds"
    - path: "pagefolio/app.py"
      provides: "窓状態属性 _page_window_start / _page_size の初期化"
      contains: "_page_window_start"
  key_links:
    - from: "pagefolio/viewer.py"
      to: "pagefolio/pagination.py"
      via: "from pagefolio.pagination import window_bounds, to_global で描画範囲と選択照合を変換"
      pattern: "from pagefolio.pagination import"
    - from: "pagefolio/app.py"
      to: "pagefolio/settings.py"
      via: "thumb_page_size をクランプ読み出しして self._page_size を初期化"
      pattern: "thumb_page_size"
---

<objective>
ページネーションの「窓状態の単一の真実」（`self._page_window_start` / `self._page_size`）を確立し、サムネイル描画と選択ハイライト照合を窓範囲化する。表示件数を `pagefolio_settings.json` に永続化（既定 20・範囲 10〜100）し、後方互換と値域クランプを担保する。

これは成功基準 1（件数で区切る表示）・成功基準 2（件数永続化）・成功基準 4（複数選択が全ページインデックスを正しく指す）の描画側の核。02-01 の純関数層を viewer.py が import し、`+ window_start` の算術を直書きせず変換を 1 箇所経由にする（02-CONTEXT.md integration point）。

Purpose: 「フレーム位置 = 全ページ index」という暗黙前提（_refresh_thumbs_selection_only の enumerate、_build_thumbnails の range(len)）を、明示的な local↔global 変換へ置き換える。selected_pages は全ページ index の不変条件を絶対に保つ（D-07）。
Output: settings.py の件数キー + クランプヘルパー、app.py の窓状態属性初期化、viewer.py の窓範囲描画・選択窓化。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pagination/02-CONTEXT.md
@.planning/phases/02-pagination/02-RESEARCH.md
@.planning/phases/02-pagination/02-PATTERNS.md
@pagefolio/pagination.py
@pagefolio/viewer.py
@pagefolio/settings.py
@pagefolio/app.py
</context>

<artifacts_this_phase_produces>
> このプラン（02-02）が作る/初期化するシンボルを ★ で示す。全体一覧は 02-01-PLAN.md 参照。

**新規設定キー:**
- ★ `thumb_page_size`（DEFAULT_SETTINGS、既定 20・範囲 10〜100・D-04/D-05）

**件数クランプ読み出し（02-01 で確定済み・本プランは import して使用）:**
- `clamp_page_size`（10〜100 + 既定 20 フォールバック）は **02-01 で pagefolio/pagination.py に集約済みの確定名**。本プランは新規定義せず `from pagefolio.pagination import clamp_page_size` で参照する（W1: 名称をフェーズ内で 1 つに固定）。空文字での Tk TclError は呼び出し側でハンドルし、本純関数は ValueError/TypeError のみ捕捉する。

**新規 self.* 属性（app.py __init__ で初期化）:**
- ★ `self._page_window_start`（窓オフセット・既定 0）
- ★ `self._page_size`（表示件数・settings からクランプ復元）

**変更（窓化適用）:**
- ★ `viewer.py` `_build_thumbnails`（窓範囲 [lo, hi) のみ描画）
- ★ `viewer.py` `_refresh_thumbs_selection_only`（enumerate 位置を to_global で全ページ index 化）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: settings に thumb_page_size 既定 20 と件数クランプ読み出しヘルパーを追加</name>
  <files>pagefolio/settings.py, tests/test_pagination.py</files>
  <read_first>
    - pagefolio/settings.py:45-76（`_load_settings` の `defaults` dict と setdefault マージ機構。`ocr_scale: 1.5`〔53 行〕が新規数値設定の作法手本）
    - pagefolio/settings.py:79-89 付近（`_save_settings` の `_SENSITIVE_KEYS` ガード。新規数値キーは機密に該当しない）
    - .planning/phases/02-pagination/02-RESEARCH.md `## Common Pitfalls` Pitfall 3（Spinbox/クランプ）・`## Runtime State Inventory`（後方互換 setdefault）・`## Security Domain`（V5 入力検証クランプ）
    - .planning/phases/02-pagination/02-PATTERNS.md `### pagefolio/settings.py`
  </read_first>
  <behavior>
    - DEFAULT_SETTINGS に thumb_page_size==20 が存在する
    - 旧設定ファイル（thumb_page_size キーなし）を _load_settings で読むと 20 が補完される（後方互換）
    - settings 読み出し → clamp_page_size（02-01・pagination.py）を通すと範囲外/非数値が安全側に倒れる: 入力 5 → 10、入力 200 → 100、入力 20 → 20、空文字/None/非数値 → 20、文字列 "30" → 30
  </behavior>
  <action>
    `pagefolio/settings.py` の `_load_settings` 内 `defaults` dict（45-65 行）に既存 `ocr_scale`（53 行）と同作法で `"thumb_page_size": 20`（D-04: 既定 20・許容 10〜100）を 1 行追加する。setdefault マージ機構（71-72 行）により旧設定ファイルでも 20 が補完される（後方互換・移行コード不要）。新規キーは数値設定で `_SENSITIVE_KEYS` に追加しない。
    件数の値域クランプ純関数は **02-01 で `pagefolio/pagination.py` に確定名 `clamp_page_size` として集約済み**（W1）。本プランで新規定義・別名定義はせず、件数を読み出す箇所（02-02 Task 2 の `self._page_size` 初期化・02-03 のハンドラ）は `from pagefolio.pagination import clamp_page_size` で同一関数を参照する。settings から読んだ生値を `clamp_page_size(self.settings.get("thumb_page_size", 20))` の形で必ず通すことで、範囲外/非数値が窓計算へ流れない。
    検証は 02-01 で作成済みの `tests/test_pagination.py` の `TestPageSizePersist` クラス（02-01 で純ロジック 5/200/20/"30"/空/None/非数値を固定済み）に、本プランで「DEFAULT_SETTINGS の thumb_page_size==20 存在」「旧設定ファイルでの setdefault 補完」「`clamp_page_size(_load_settings()['thumb_page_size'])`==20」という settings 結合の assert を追記する（SC2 の settings 永続化部分）。
  </action>
  <verify>
    <automated>pytest tests/test_pagination.py::TestPageSizePersist -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_pagination.py::TestPageSizePersist -x -q` が exit 0
    - `python -c "from pagefolio.settings import _load_settings; print(_load_settings()['thumb_page_size'])"` の出力が `20`
    - `python -c "from pagefolio.pagination import clamp_page_size; from pagefolio.settings import _load_settings; print(clamp_page_size(_load_settings()['thumb_page_size']))"` の出力が `20`（settings → clamp_page_size 結合・W1）
    - `clamp_page_size` は 02-01 の pagination.py を import して参照し、settings.py に同等関数を別名/再定義しない（`grep -c 'def clamp_page_size' pagefolio/settings.py` が 0）
    - `grep -v '^#' pagefolio/settings.py | grep -c 'except:'` が 0（裸 except なし）
    - `ruff check pagefolio/settings.py` が exit 0
  </acceptance_criteria>
  <done>
    DEFAULT_SETTINGS に thumb_page_size=20 が追加され、件数読み出しは 02-01 の確定名 clamp_page_size（pagination.py）経由に統一（W1）、TestPageSizePersist が settings 結合分含め green。後方互換は setdefault が担保。
  </done>
</task>

<task type="auto">
  <name>Task 2: app.py に窓状態属性を初期化し、viewer の描画・選択を窓範囲化</name>
  <files>pagefolio/app.py, pagefolio/viewer.py</files>
  <read_first>
    - pagefolio/app.py:73-93（`__init__` の状態属性初期化ブロック。`self.current_page`〔75〕`self.selected_pages`〔76〕`self.thumb_cache`〔78〕`self._thumb_gen`〔93〕の隣に窓状態を足す）
    - pagefolio/viewer.py:197-225（`_build_thumbnails` — `range(len(self.doc))` で全描画。窓範囲化の中核。`render_next` の `placeholder_labels[i]` 添字が窓ローカルへ要対応）
    - pagefolio/viewer.py:227-287（`_add_thumb_placeholder` — クロージャ `idx=i` 束縛。`i` を全ページ index のまま渡す＝D-06 の src 整合・02-RESEARCH.md Pattern 3 / A2）
    - pagefolio/viewer.py:174-195（`_refresh_thumbs_selection_only` — enumerate 位置 `i` を直接 selected_pages と照合〔177-179〕。Pitfall 1 の修正対象）
    - pagefolio/viewer.py:126-144（`_invalidate_thumb_cache` / `_get_thumb_photo` — thumb_cache キーは全ページ index。窓移動でクリア不要・Pitfall 2）
    - .planning/phases/02-pagination/02-RESEARCH.md `## Common Pitfalls` Pitfall 1・Pitfall 2・Anti-Patterns、`## Architecture Patterns` Pattern 1/3
    - .planning/phases/02-pagination/02-PATTERNS.md `### pagefolio/viewer.py`（1〜2 番）
  </read_first>
  <action>
    `pagefolio/app.py` の `__init__` 状態初期化ブロック（73-93 行付近）に窓状態属性を追加する: `self._page_window_start = 0` と `self._page_size = clamp_page_size(self.settings.get("thumb_page_size", 20))`（`from pagefolio.pagination import clamp_page_size` で 02-01 の確定名を import して使う・W1・D-05 復元）。
    `pagefolio/viewer.py` の冒頭に `from pagefolio.pagination import window_bounds, to_global` を追加（02-01 の純関数層を参照）。
    `_build_thumbnails`（197-225）を窓範囲描画へ変更する: `lo, hi = window_bounds(self._page_window_start, self._page_size, len(self.doc))` を取り、プレースホルダ生成を `placeholder_labels = [self._add_thumb_placeholder(i) for i in range(lo, hi)]` にする（`i` は全ページ index のまま渡す＝D-06 src 整合）。`render_next` の起点を `render_next(lo)` にし終端を `i >= hi` に、`placeholder_labels` の添字は窓ローカル化して `placeholder_labels[i - lo]` で参照する。`_thumb_gen` 世代ガード（198-199, 210-216）は一切変更しない（Anti-Pattern「世代ガードを外さない」）。
    `_refresh_thumbs_selection_only`（174-195）の照合ループ（177-179）を窓変換する: `g = to_global(i, self._page_window_start)` を取り `is_sel = g in self.selected_pages` / `is_cur = g == self.current_page` にする（Pitfall 1）。`selected_pages` 自体はローカル化しない（D-07 不変条件を絶対に守る）。`thumb_cache` は窓移動でクリアしない（Pitfall 2: キーが全ページ index のため別窓と衝突しない）。`p.{i + 1}` のページ番号ラベル（_add_thumb_placeholder 239 行）は全ページ index ベースのため改修不要（窓 2 でも実ページ番号を表示）。
  </action>
  <verify>
    <automated>pytest tests/test_pagination.py tests/test_viewer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` 全スイートが exit 0（既存 test_viewer / test_pdf_ops の回帰なし）
    - `grep -c 'window_bounds' pagefolio/viewer.py` が 1 以上（描画範囲が窓化されている）
    - `grep -c 'to_global' pagefolio/viewer.py` が 1 以上（選択照合が変換経由）
    - `grep -c 'range(len(self.doc))' pagefolio/viewer.py` が 0（_build_thumbnails の全描画が撤廃されている）
    - `grep -c '_page_window_start' pagefolio/app.py` が 1 以上（窓状態属性が初期化されている）
    - `grep -v '^#' pagefolio/viewer.py | grep -c 'except:'` が 0（裸 except なし）
    - `ruff check pagefolio/app.py pagefolio/viewer.py` が exit 0
  </acceptance_criteria>
  <done>
    app.py に _page_window_start / _page_size が初期化され、_build_thumbnails が窓範囲のみ描画、_refresh_thumbs_selection_only が to_global で照合。selected_pages はローカル化せず全ページ index のまま。`pytest` 全 green、ruff 通過。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー件数入力 → settings 永続化 | 件数値が 10〜100 範囲・数値であることを保証する境界（唯一の現実的な堅牢性ポイント） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Denial of Service（ローカル・自己起因） | settings 件数読み出し（範囲外・空文字・非数値が窓計算を破綻させる） | mitigate | 02-01 の純関数 `clamp_page_size`（pagination.py）で 10〜100 にクランプし ValueError/TypeError は既定 20 にフォールバック。settings 直読み時も同関数を通すことで不正値が窓計算へ流れない（02-RESEARCH.md Security V5・W1 で名称固定） |
| T-2-02 | Information Disclosure | thumb_page_size の settings 永続化 | accept | 追加するのは数値設定のみで機密に該当しない。`_SENSITIVE_KEYS` ガード（settings.py:79-89）は既存・本キーは無関係（02-RESEARCH.md Security Domain） |

> ASVS Level 1 / Block on: high。**high 相当の脅威なし**。本プランは件数の値域クランプ（V5 入力検証）のみが現実的な堅牢性対象で、認証・ネットワーク・暗号・外部機密の取り扱いは含まない。
</threat_model>

<verification>
- `pytest` 全スイート green（test_pagination の TestPageSizePersist 追加分含む・既存回帰なし）
- `ruff check . && ruff format .` 通過
- _build_thumbnails が窓範囲のみ描画（`range(len(self.doc))` 撤廃を grep で確認）
- _refresh_thumbs_selection_only が to_global 経由で照合（grep 確認）
- selected_pages がローカル化されていない（全ページ index 不変条件・D-07）
</verification>

<success_criteria>
- thumb_page_size が既定 20 で永続化・後方互換され、件数読み出しが 10〜100 にクランプされる
- 窓状態 self._page_window_start / self._page_size が初期化され件数は settings から復元
- サムネイル描画が窓範囲 [lo, hi) に絞られ、大量ページで描画件数が page_size 上限に
- 選択ハイライト照合が全ページ index へ変換されて窓またぎでもずれない（Pitfall 1 解消）
</success_criteria>

<output>
Create `.planning/phases/02-pagination/02-02-SUMMARY.md` when done
</output>
