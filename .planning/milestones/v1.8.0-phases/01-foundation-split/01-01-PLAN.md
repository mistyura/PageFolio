---
phase: 01-foundation-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_imports.py
autonomous: true
requirements: [V180-REFAC-01, V180-REFAC-02]
tags: [refactor, packaging, backward-compat, tests]

must_haves:
  truths:
    - "`from pagefolio.ocr_providers import` で全モジュールレベルシンボル（OCRProvider / ClaudeProvider / GeminiProvider / LMStudioProvider / OllamaProvider / RunPodProvider / TesseractProvider / OCRAPIKeyError / OCRRetryableError / OCRContextLengthError / parse_retry_after / looks_like_context_error / _require_http_scheme / _raise_mapped_http_error / _retryable_http_scheme相当 / _detect_tesseract / _ALLOWED_URL_SCHEMES / _CONTEXT_ERROR_MARKERS）が import できる（現行 monolith で緑・分割後も緑を維持する後方互換契約 D-11）"
    - "`from pagefolio.dialogs.llm_config import LLMConfigDialog` と `from pagefolio.dialogs import LLMConfigDialog` の両経路が import できる（D-11）"
    - "`pagefolio` トップレベルには ocr_providers のプロバイダクラスも LLMConfigDialog も露出しない（Pitfall 4・公開面を広げない）"
  artifacts:
    - "tests/test_imports.py に TestOcrProvidersImports（ocr_providers パッケージ直下 import の後方互換テスト）が追加されている"
  key_links:
    - "package-level import 面（`pagefolio.ocr_providers` / `pagefolio.dialogs.llm_config`）が分割の前後で不変であること — この安全網が Wave 2/3 の分割の回帰検知装置になる"
---

<objective>
Wave 2 以降の分割（ocr_providers パッケージ化・llm_config Mixin 化）に先立ち、後方互換 import 面を検証するテストを **分割前に先行拡張**する（CONTEXT.md の Wave 0 必達要件・「先行拡張 → 分割 → 全テスト緑」サイクルの起点）。

このプランのテストは現行の monolith 実装（`pagefolio/ocr_providers.py` モジュール・`pagefolio/dialogs/llm_config.py` モジュール）に対して **今すぐ緑** になる package-level import アサーションであり、分割後も緑を維持することが後方互換の契約（D-11）そのものになる。1つでも赤くなれば分割が公開面を壊したことを即座に検知できる。

Purpose: 純粋リファクタリングフェーズの回帰検知装置を、リスクのある分割作業の前に確立する。V180-REFAC-01（ocr_providers 分割）・V180-REFAC-02（llm_config 分割）の後方互換 import を保証する安全網。
Output: `tests/test_imports.py` に拡張された後方互換 import テスト群（現行コードで全緑）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-foundation-split/01-CONTEXT.md
@.planning/phases/01-foundation-split/01-RESEARCH.md
@.planning/phases/01-foundation-split/01-PATTERNS.md

@tests/test_imports.py
@pagefolio/ocr_providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ocr_providers 後方互換 import テスト（TestOcrProvidersImports）を追加</name>
  <files>tests/test_imports.py</files>
  <read_first>
    - tests/test_imports.py（既存の TestDialogsImports の記法・クラス構成をそのまま複製する受け皿）
    - pagefolio/ocr_providers.py（re-export 対象となる全モジュールレベル定義を実物から列挙するための一次情報源）
    - tests/test_ocr_providers.py（どの private シンボルが `from pagefolio.ocr_providers import ...` で直接 import されているかの確認元）
    - .planning/phases/01-foundation-split/01-PATTERNS.md（TestDialogsImports の複製パターン・全シンボルリスト）
  </read_first>
  <action>
    tests/test_imports.py に新クラス `TestOcrProvidersImports` を追加する。既存 `TestDialogsImports`（84-176 行付近）と同一記法（各シンボルごとに `def test_...` メソッドを持ち、`from pagefolio.ocr_providers import X` 後に `assert X is not None`）で、`pagefolio.ocr_providers` パッケージ直下からの import を検証する。

    対象シンボルは monolith `pagefolio/ocr_providers.py` の **全モジュールレベル定義を実物から列挙**して漏れなく網羅すること（RESEARCH の「17シンボル」表記は目安。実際は grep で機械的に列挙し確定する。確認済みの module-level 定義: `_ALLOWED_URL_SCHEMES` / `_require_http_scheme` / `OCRProvider` / `OCRAPIKeyError` / `OCRRetryableError` / `_retryable_http_message` / `OCRContextLengthError` / `_CONTEXT_ERROR_MARKERS` / `parse_retry_after` / `looks_like_context_error` / `_raise_mapped_http_error` / `LMStudioProvider` / `ClaudeProvider` / `GeminiProvider` / `_detect_tesseract` / `TesseractProvider` / `OllamaProvider` / `RunPodProvider`）。

    private ヘルパー（`_require_http_scheme` / `parse_retry_after` / `looks_like_context_error` / `_raise_mapped_http_error` / `_detect_tesseract` 等）は「非公開だから」という理由で除外してはならない（Pitfall 1/2）。これらは `tests/test_ocr_providers.py`（D-03 により無修正で緑を保つ必要がある凍結ファイル）と本番コード（`ocr.py:519` が `_detect_tesseract` を、`ocr.py`/`ocr_dialog.py` が各エラークラスを）が package-level から直接 import しており、re-export から漏れると即座に赤くなる。

    これらのテストは現行 monolith 実装に対して **今すぐ全緑** になる（`from pagefolio.ocr_providers import parse_retry_after` はモジュールレベル def なので現状で成功する）。分割後も緑を保つことが D-11 の後方互換契約の検証点になる。クラス docstring に「REFAC-01 — ocr_providers パッケージ分割後の後方互換 import 検証（D-11 完全再エクスポート契約）」と記す。プロバイダクラスはインスタンス化せずシンボル存在のみ確認する（既存 TestDialogsImports の作法と一致）。
  </action>
  <verify>
    <automated>pytest tests/test_imports.py -k OcrProviders -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_imports.py -k OcrProviders -q` が全緑（現行 monolith 実装に対して成功する）
    - TestOcrProvidersImports が monolith `ocr_providers.py` の全モジュールレベル定義（クラス・def・大文字定数・private ヘルパー）を package-level import でカバーしている
    - private ヘルパー `_require_http_scheme` / `parse_retry_after` / `looks_like_context_error` / `_raise_mapped_http_error` / `_detect_tesseract` それぞれに対応するテストメソッドが存在する
    - `ruff check tests/test_imports.py && ruff format --check tests/test_imports.py` がクリーン
  </acceptance_criteria>
  <done>ocr_providers パッケージ直下 import の後方互換テストが現行コードで全緑になり、Wave 2 の分割の回帰検知装置が確立される</done>
</task>

<task type="auto">
  <name>Task 2: llm_config 両経路 import と公開面ガードを確認・補完</name>
  <files>tests/test_imports.py</files>
  <read_first>
    - tests/test_imports.py（既存 test_individual_module_llm_config / test_llm_config_via_dialogs_subpackage / TestPackageSurface 相当の有無を確認）
    - pagefolio/dialogs/__init__.py（LLMConfigDialog を re-export している既存 DEBT-01 前例）
    - .planning/phases/01-foundation-split/01-RESEARCH.md（Pitfall 4: pagefolio トップレベルに ocr_providers/LLMConfigDialog を新規公開しない）
  </read_first>
  <action>
    tests/test_imports.py で llm_config の両経路 import テスト（`from pagefolio.dialogs.llm_config import LLMConfigDialog` と `from pagefolio.dialogs import LLMConfigDialog` の2経路）が既に存在することを確認する。既存であれば内容を確認するのみで変更しない。欠けている経路があれば既存記法で補完する。これは V180-REFAC-02 の後方互換安全網であり、Wave 3 の llm_config Mixin 分割の前提として緑であることを確定させる。

    加えて公開面ガード（Pitfall 4・D-11 の裏返し）を確認する。`pagefolio` トップレベル（`import pagefolio` の名前空間）に ocr_providers のプロバイダクラス（例 `ClaudeProvider`）と `LLMConfigDialog` が **露出していない** ことを検証するアサーションが存在するか確認し、無ければ追加する。検証は `import pagefolio; assert not hasattr(pagefolio, "ClaudeProvider")` および `assert not hasattr(pagefolio, "LLMConfigDialog")` の形で行う（`pagefolio/__init__.py` の公開面を本フェーズで一切広げない方針の回帰ガード）。これも現行実装に対して今すぐ緑になる。
  </action>
  <verify>
    <automated>pytest tests/test_imports.py -k "LlmConfig or PackageSurface or llm_config" -q</automated>
  </verify>
  <acceptance_criteria>
    - `from pagefolio.dialogs.llm_config import LLMConfigDialog` と `from pagefolio.dialogs import LLMConfigDialog` の両方を検証するテストが存在し緑
    - `pagefolio` トップレベルに `ClaudeProvider` と `LLMConfigDialog` が露出しないことを検証するアサーションが存在し緑
    - `pytest tests/test_imports.py -q`（ファイル全体）が全緑
    - `ruff check tests/test_imports.py && ruff format --check tests/test_imports.py` がクリーン
  </acceptance_criteria>
  <done>llm_config 両経路 import と公開面の負ガードが緑になり、REFAC-02 分割の後方互換安全網が確立される</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces（このプランが新規作成するシンボル）

- **テストクラス**: `TestOcrProvidersImports`（`tests/test_imports.py`）
- **テストメソッド群**: ocr_providers 各シンボルの package-level import 検証メソッド、`pagefolio` トップレベル公開面の負ガードメソッド（既存 TestPackageSurface に追加の場合あり）

> 本プランは既存 monolith シンボル（`OCRProvider` 等）を新規作成しない。それらは Wave 2 で物理的にファイル移動されるが同一シンボル identity を保つ。ここで作るのはテスト側の新規クラス/メソッドのみ。
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| アプリ内部 → `import pagefolio` の公開名前空間 | 内部ヘルパー・プロバイダ実装が意図せずトップレベル公開面へ露出する境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Information Disclosure | `pagefolio/__init__.py` の公開面 / re-export 面 | low | mitigate | Task 2 で `pagefolio` トップレベルに ocr_providers クラス・LLMConfigDialog が露出しない負ガードを緑で確立（Pitfall 4）。本プランは公開面を一切広げない |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージ導入ゼロ（RESEARCH「Package Legitimacy Audit: 該当なし」）。install タスクなし・Legitimacy Gate 対象外 |
</threat_model>

<verification>
- `pytest tests/test_imports.py -q` が全緑（現行 monolith 実装に対して成功）
- 追加した ocr_providers import テストが monolith の全モジュールレベル定義を網羅
- `ruff check . && ruff format --check .` がクリーン
- 本プラン単体では既存の他テストに影響を与えない（テスト追加のみ・ソース無変更）
</verification>

<success_criteria>
- TestOcrProvidersImports が現行コードで全緑
- llm_config 両経路 import・公開面負ガードが緑
- 後方互換 import 安全網が Wave 2/3 の分割の前に committed される
</success_criteria>

<output>
Create `.planning/phases/01-foundation-split/01-01-SUMMARY.md` when done
</output>
