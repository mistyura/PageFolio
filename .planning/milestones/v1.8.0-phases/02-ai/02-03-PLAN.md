---
phase: 02-ai
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - pagefolio/dialogs/llm_config/sections.py
  - pagefolio/dialogs/llm_config/dialog.py
  - pagefolio/lang.py
  - tests/test_provider_ui.py
autonomous: true
requirements:
  - V180-FALL-01
  - V180-FALL-03
must_haves:
  truths:
    - "LLM 設定ダイアログに『🔁 フォールバック』独立セクションがあり、有効化トグル + 順序リスト（Listbox + 上へ/下へボタン）で構成される（D-13/D-15/D-16）"
    - "既定はトグルOFF・空チェーンで、トグルONにすると順序リストが現れる（V180-FALL-01 安全側既定・D-16）"
    - "候補一覧には全実行可能プロバイダ（lmstudio/ollama/runpod/claude/gemini/tesseract + プラグイン登録）が APIキー未設定でも表示される（D-14）"
    - "フォールバック候補プロバイダ名は既知プロバイダ一覧のホワイトリストに限定される（Input Validation・ASVS L1）"
    - "有効化トグルと順序リストが _apply 経由で ocr_fallback_enabled / ocr_fallback_chain として永続化される（V180-FALL-01/V180-FALL-03 の設定面）"
  artifacts:
    - "pagefolio/dialogs/llm_config/sections.py（フォールバックセクション）"
  key_links:
    - "fallback_enabled_var のトグル → 順序リスト frame の pack/pack_forget 切替（provider_row/url_section_frame と同型・D-16）"
    - "_apply が ocr_fallback_enabled/ocr_fallback_chain を llm_settings へ収集し _save_settings される（02-04 のオーケストレーションが読む設定源）"
---

<objective>
LLM 設定ダイアログに**フォールバック順設定 UI**（新規独立セクション「🔁 フォールバック」）を追加する。`MergeOrderDialog` の Listbox+上へ/下へボタンという完成済みウィジェット構成を、Toplevel でなく埋め込みセクションへ移植する（D-13/D-15）。

対象: V180-FALL-01（フォールバック順の明示設定・未設定＝発火しない安全側既定）・V180-FALL-03 の設定面（プロバイダ連鎖の永続化）。ロック済み決定 D-13/D-14/D-15/D-16 を実装する。実行時のオーケストレーション（送信先確認再提示・provider 再構築）は 02-04 が担う。

Purpose: ユーザーがプロバイダ障害時のフォールバック連鎖を安全側既定（既定OFF）のまま明示的に構成できるようにする。
Output: `sections.py`（フォールバックセクション）・`dialog.py`（`_apply` 収集）・`lang.py`（ja/en キー）・`test_provider_ui.py`（拡張）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-CONTEXT.md
@.planning/phases/02-ai/02-RESEARCH.md
@.planning/phases/02-ai/02-PATTERNS.md
@.planning/phases/02-ai/02-01-SUMMARY.md
@.planning/phases/02-ai/02-02-SUMMARY.md
@pagefolio/dialogs/llm_config/sections.py
@pagefolio/dialogs/llm_config/dialog.py
@pagefolio/dialogs/merge.py
</context>

## Artifacts this phase produces (Plan 02-03)

新規シンボル（drift 検証で新規作成として扱うこと）:
- `sections.py`（SectionsMixin 拡張）: `self.fallback_enabled_var`、`self.fallback_listbox`、`self.fallback_up_btn`、`self.fallback_down_btn`、`self.fallback_list_frame`、属性 `self._fallback_chain`、メソッド `_build_fallback_section`、`_on_fallback_toggle`、`_fallback_move_up`、`_fallback_move_down`、`_reload_fallback_list`、`_fallback_add`/`_fallback_remove`（候補追加/除外）、`_base_fallback_providers`
- `dialog.py`（DialogMixin._apply 拡張）: `llm_settings["ocr_fallback_enabled"]`、`llm_settings["ocr_fallback_chain"]`
- `lang.py` 新規キー（ja/en 両方）: `fallback_section_title`、`fallback_enable_toggle`、`fallback_up_btn`、`fallback_down_btn`、`fallback_add_btn`、`fallback_remove_btn`、`fallback_hint`
- `tests/test_provider_ui.py`（拡張）: `TestFallbackSection`

<tasks>

<task type="auto">
  <name>Task 1: sections.py に🔁フォールバックセクション（トグル + Listbox + 上下ボタン）を追加</name>
  <files>pagefolio/dialogs/llm_config/sections.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/dialogs/merge.py（`MergeOrderDialog` の `tk.Listbox` 構築＝bg/fg/selectbackground/activestyle 設定・上へ/下へボタン・`_move_up`/`_move_down`/`_reload_list` の移動ロジックの写経元）
    - pagefolio/dialogs/llm_config/sections.py（`_base_providers` リスト・`provider_combo`/`_plugin_manager.list_ocr_providers()`・プロバイダ固有セクションの `pack`/`pack_forget` 動的表示切替＝`_on_provider_change` パターン・02-02 で追加したテンプレートセクションの位置関係）
    - .planning/phases/02-ai/02-RESEARCH.md（Pattern 5「フォールバック順設定 UI」・D-14 の候補一覧に全プロバイダを含める・D-16 のトグルON時のみリスト表示）
    - .planning/phases/02-ai/02-CONTEXT.md（D-13 Listbox+上下ボタン・D-15 新規独立セクション・D-16 既定OFF）
    - pagefolio/lang.py（ja/en 両ブロック・キーは両方へ同一キーで追加）
  </read_first>
  <action>
    `sections.py` の `_build` 内、OCR プロバイダ選択セクション群の後（テンプレートセクションとは別の独立ブロック・D-15）に「🔁 フォールバック」セクションを追加する。構成:

    (1) 有効化トグル: `self.fallback_enabled_var = tk.BooleanVar(value=self.current_settings.get("ocr_fallback_enabled", False))` と `ttk.Checkbutton`（text=`fallback_enable_toggle`、command=`self._on_fallback_toggle`）。既定 False（V180-FALL-01・D-16）。

    (2) 順序リスト frame `self.fallback_list_frame`: `MergeOrderDialog` と同型の `tk.Listbox`（`self.fallback_listbox`・bg=`C["BG_CARD"]`/fg=`C["TEXT_MAIN"]`/selectbackground=`C["ACCENT"]`/activestyle="none"/bd=0/highlightthickness=0）+ 縦 `ttk.Scrollbar`。上へ=`self.fallback_up_btn`（command=`self._fallback_move_up`）・下へ=`self.fallback_down_btn`（command=`self._fallback_move_down`）。加えて候補追加/除外のため、全プロバイダ候補 combobox または「追加/除外」ボタンを設ける（D-14: 候補一覧は `_base_fallback_providers` = `["lmstudio", "ollama", "runpod", "claude", "gemini", "tesseract"]` + `self._plugin_manager.list_ocr_providers()`。`off` は除外。APIキー未設定プロバイダも表示する）。`self._fallback_chain` を `list(self.current_settings.get("ocr_fallback_chain", []))` で初期化し、既知プロバイダ一覧に無い名前は読み込み時に除外する（ホワイトリスト検証・不正プロバイダ名の混入防止）。

    (3) `_on_fallback_toggle`: トグル状態に応じて `self.fallback_list_frame` を `pack`/`pack_forget` で表示/非表示する（`_on_provider_change` の動的表示切替パターンと同型・D-16）。既定OFFなので初期は非表示。

    (4) `_fallback_move_up`/`_fallback_move_down`: `merge.py` の `_move_up`/`_move_down` をそのまま移植し、対象を `self.paths` ではなく `self._fallback_chain`（プロバイダ名リスト）に置き換える。`_reload_fallback_list(select_index)` で Listbox を再描画し選択を復元する。`_fallback_add`/`_fallback_remove` で候補をチェーンへ追加/除外する（重複追加は無視）。Toplevel 化・callback 経由の親子通信は不要（同一ダイアログ内 `self` 属性で完結・D-15）。

    `lang.py` の ja/en 両ブロックへ**同一キーで**追加: `fallback_section_title`、`fallback_enable_toggle`、`fallback_up_btn`、`fallback_down_btn`、`fallback_add_btn`、`fallback_remove_btn`、`fallback_hint`（`fallback_hint` は「未設定時はフォールバックしません」相当＝安全側既定の明示・V180-FALL-01）。

    ruff（line-length 88・S/B・裸 except 禁止）を通す。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py tests/test_lang_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('pagefolio/dialogs/llm_config/sections.py', encoding='utf-8').read())"` が成功
    - `grep -c 'fallback_listbox' pagefolio/dialogs/llm_config/sections.py` が 1 以上、`grep -c 'def _fallback_move_up' pagefolio/dialogs/llm_config/sections.py` が 1
    - `python -c "from pagefolio.lang import LANG; ja=set(LANG['ja']); en=set(LANG['en']); need={'fallback_section_title','fallback_enable_toggle','fallback_up_btn','fallback_down_btn','fallback_hint'}; assert need<=ja and need<=en; assert ja==en"` が成功
    - `pytest tests/test_provider_ui.py tests/test_lang_parity.py -x -q` が pass
    - `ruff check pagefolio/dialogs/llm_config/sections.py pagefolio/lang.py && ruff format --check pagefolio/dialogs/llm_config/sections.py pagefolio/lang.py` がクリーン
  </acceptance_criteria>
  <done>🔁フォールバックセクション（トグル + Listbox + 上下ボタン + 候補追加/除外）が構築され、既定OFF・ホワイトリスト候補・D-13〜D-16 を満たし、lang.py の ja/en キーが一致してテストがグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: dialog.py _apply にフォールバック設定収集を追加し、test_provider_ui.py にフォールバックテストを追加</name>
  <files>pagefolio/dialogs/llm_config/dialog.py, tests/test_provider_ui.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config/dialog.py（`_apply` の `llm_settings` 収集・数値クランプの記法・`on_apply(llm_settings)` 経路）
    - pagefolio/ocr_dialog.py（`_apply_llm_settings` の `self.app.settings.update`→`_save_settings` 経路）
    - tests/test_provider_ui.py（`_read_llm_config_package_source()` source-scan ヘルパー・`_apply` スタブ経由のヘッドレス検証記法）
    - .planning/phases/02-ai/02-CONTEXT.md（D-14 ホワイトリスト・D-16 既定OFF）
  </read_first>
  <action>
    `dialog.py` の `_apply` に、フォールバック設定を `llm_settings` へ収集する処理を追加する: `llm_settings["ocr_fallback_enabled"] = bool(self.fallback_enabled_var.get())` と `llm_settings["ocr_fallback_chain"] = list(self._fallback_chain)`。収集時、`_fallback_chain` の各要素が既知プロバイダ一覧（`_base_fallback_providers` + プラグイン登録名）に含まれることを確認し、含まれない名前は除外してから格納する（ホワイトリスト検証・ASVS L1 Input Validation）。`_apply_llm_settings`（ocr_dialog.py）が `self.app.settings.update(llm_settings)`→`_save_settings` するため両キーが永続化される。

    `tests/test_provider_ui.py` に `TestFallbackSection` を追加する: (a) source-scan で `fallback_listbox`/`_fallback_move_up`/`fallback_enabled_var` が sections.py に存在すること、(b) `_apply` が `ocr_fallback_enabled`（bool）と `ocr_fallback_chain`（list）を収集すること、(c) 未知プロバイダ名がチェーンから除外されるホワイトリスト検証、(d) 既定値（enabled False・chain []）の後方互換をヘッドレスで検証。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py::TestFallbackSection -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ocr_fallback_enabled' pagefolio/dialogs/llm_config/dialog.py` が 1 以上、`grep -c 'ocr_fallback_chain' pagefolio/dialogs/llm_config/dialog.py` が 1 以上
    - `pytest tests/test_provider_ui.py::TestFallbackSection -x` が pass
    - `pytest tests/test_provider_ui.py tests/test_imports.py -x -q` が pass（後方互換 import 回帰なし）
    - `ruff check pagefolio/dialogs/llm_config/dialog.py tests/test_provider_ui.py && ruff format --check pagefolio/dialogs/llm_config/dialog.py tests/test_provider_ui.py` がクリーン
  </acceptance_criteria>
  <done>_apply が ocr_fallback_enabled/ocr_fallback_chain をホワイトリスト検証つきで収集して永続化し、TestFallbackSection がグリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| フォールバック順リスト（プロバイダ名） → pagefolio_settings.json | ユーザー構成のプロバイダ連鎖が永続化され、02-04 の実行時オーケストレーションが消費する |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | フォールバックチェーンへの未知/不正プロバイダ名混入（Input Validation V5/ASVS L1） | medium | mitigate | `_fallback_chain` の各要素を既知プロバイダ一覧（`_base_fallback_providers`+プラグイン登録）でホワイトリスト検証し、読み込み時と `_apply` 収集時の両方で不正名を除外 |
| T-02-08 | Tampering | 既定でのフォールバック意図しない発火 | high | mitigate | `ocr_fallback_enabled` 既定 False・`ocr_fallback_chain` 既定 []（V180-FALL-01・D-16）を 02-01 デフォルト値 + トグルOFF初期表示で二重に担保。トグルONにするまでリスト非表示 |
| T-02-09 | Information Disclosure | フォールバック順リストへの機密情報混入 | low | accept | リスト要素はプロバイダ名（列挙値）のみで自由入力なし。ホワイトリスト検証で列挙外を除外 |
</threat_model>

<verification>
- `pytest tests/test_provider_ui.py tests/test_lang_parity.py tests/test_imports.py -x -q`
- `pytest`（フルスイート）
- `ruff check . && ruff format --check .`
</verification>

<success_criteria>
- 🔁フォールバック独立セクション（トグル + Listbox + 上下ボタン + 候補追加/除外）が存在し、D-13/D-14/D-15/D-16 を満たす
- 既定OFF・空チェーンでフォールバックが発火しない安全側既定（V180-FALL-01）が UI とデフォルト値で担保される
- 候補一覧に全実行可能プロバイダが表示され、チェーンはホワイトリスト検証される
- ocr_fallback_enabled/ocr_fallback_chain が _apply 経由で永続化される
- lang.py の ja/en キー一致、test_provider_ui.py/フルスイートがグリーン、ruff クリーン
</success_criteria>

<output>
Create `.planning/phases/02-ai/02-03-SUMMARY.md` when done
</output>
