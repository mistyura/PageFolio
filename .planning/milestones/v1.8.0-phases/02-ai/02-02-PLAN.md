---
phase: 02-ai
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pagefolio/dialogs/llm_config/sections.py
  - pagefolio/dialogs/llm_config/dialog.py
  - pagefolio/lang.py
  - tests/test_provider_ui.py
autonomous: true
requirements:
  - V180-TMPL-01
  - V180-TMPL-02
  - V180-TMPL-03
  - V180-TMPL-04
  - V180-TMPL-05
must_haves:
  truths:
    - "LLM 設定ダイアログに『📄 テンプレート』セクションがあり、combobox でテンプレートを選択・切替できる（V180-TMPL-02・D-01）"
    - "保存/削除/リネームボタンでテンプレート CRUD 操作ができ、02-01 の settings.py 純関数を UI から呼ぶ（V180-TMPL-01/V180-TMPL-03）"
    - "テンプレート名の重複は保存/リネーム時に messagebox.showerror で拒否される（D-04・ShortcutsDialog パターン）"
    - "アクティブテンプレートの削除ボタンは無効化される（D-03）"
    - "テンプレート切替時、外部mdファイル連動モードでは未保存差分の確認ダイアログ（askyesno）を挟み、キャンセルで切替を中止する（D-05）"
    - "テンプレート切替後、選択テンプレートの内容で外部mdファイルを上書きし『アクティブテンプレートのライブ編集内容』の不変条件を保つ（D-07/D-08）"
    - "アクティブテンプレート名が _apply 経由で pagefolio_settings.json へ永続化される"
  artifacts:
    - "pagefolio/dialogs/llm_config/sections.py（テンプレートセクション）"
  key_links:
    - "template_combo の <<ComboboxSelected>> → _on_template_change → 02-01 の get_template/save_prompt_file を呼ぶ D-05〜D-07 フロー"
    - "_apply が prompt_templates.active を llm_settings へ収集し _apply_llm_settings（ocr_dialog.py）経由で _save_settings される"
---

<objective>
LLM 設定ダイアログ（`pagefolio/dialogs/llm_config/`）に**テンプレート管理 UI** を追加する。02-01 で確立した settings.py の CRUD 純関数の上に、既存の完成済み UI パターン（`provider_combo` の combobox・`ShortcutsDialog` の保存時重複拒否）を複製して構築する（新規 UI パターンの発明を避ける）。

対象: V180-TMPL-01（名前付き保存）・V180-TMPL-02（一覧選択切替）・V180-TMPL-03（削除・リネーム）・V180-TMPL-04（外部mdファイル連動との共存）・V180-TMPL-05（全プロバイダ横断＝02-01 の load_*_prompt が既に担保・本プランで UI 経路を接続）。ロック済み決定 D-01/D-03/D-04/D-05/D-06/D-07/D-08 を実装する。

Purpose: ユーザーが OCR/サマリプロンプトを名前付きテンプレートとして GUI から管理できるようにする。
Output: `sections.py`（テンプレートセクション）・`dialog.py`（`_apply` 収集追加）・`lang.py`（ja/en キー）・`test_provider_ui.py`（拡張）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-CONTEXT.md
@.planning/phases/02-ai/02-RESEARCH.md
@.planning/phases/02-ai/02-PATTERNS.md
@.planning/phases/02-ai/02-01-SUMMARY.md
@pagefolio/dialogs/llm_config/sections.py
@pagefolio/dialogs/llm_config/dialog.py
@pagefolio/dialogs/shortcuts.py
@pagefolio/settings.py
</context>

## Artifacts this phase produces (Plan 02-02)

新規シンボル（drift 検証で新規作成として扱うこと）:
- `sections.py`（SectionsMixin 拡張）: `self.template_var`、`self.template_combo`、`self.template_delete_btn`、属性 `self._active_template_name`、メソッド `_build_template_section`（または `_build` 内テンプレート行）、`_on_template_change`、`_on_template_save`、`_on_template_delete`、`_on_template_rename`、`_has_unsaved_template_changes`、`_reload_template_combo`、`_refresh_template_delete_state`
- `dialog.py`（DialogMixin._apply 拡張）: `llm_settings["prompt_templates"]`（active 名の収集）
- `lang.py` 新規キー（ja/en 両方）: `tmpl_section_title`、`tmpl_save_btn`、`tmpl_delete_btn`、`tmpl_rename_btn`、`tmpl_name_prompt`、`tmpl_dup_error`、`tmpl_empty_error`、`tmpl_switch_discard_confirm`、`tmpl_active_delete_blocked`
- `tests/test_provider_ui.py`（拡張）: `TestTemplateSection`（source-scan + 純ロジック連携）

<tasks>

<task type="auto">
  <name>Task 1: sections.py にテンプレートセクション（combobox + 保存/削除/リネーム + 切替フロー）を追加</name>
  <files>pagefolio/dialogs/llm_config/sections.py, pagefolio/lang.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config/sections.py（`provider_combo` ブロック=`_base_providers`+`state="readonly"`+`<<ComboboxSelected>>` バインドの写経元・`ocr_prompt_text`/`ocr_summary_prompt_text` の構築箇所=テンプレートセクションの挿入位置・`_add_prompt_file_notice` の流用・`self.current_settings` の参照方法）
    - pagefolio/dialogs/shortcuts.py（`_on_save` の重複拒否→`messagebox.showerror`→`return` パターン=D-04 の写経元）
    - pagefolio/settings.py（02-01 追加の `list_template_names`/`get_template`/`save_template`/`delete_template`/`rename_template`/`template_name_exists`、`load_prompt_file`/`save_prompt_file`/`prompt_file_exists`、`CUSTOM_PROMPT_FILE`/`SUMMARY_PROMPT_FILE`）
    - pagefolio/lang.py（ja/en 両ブロックの `err_title`/`confirm_title` 近辺・キーは ja/en 同一キーで両方へ追加する規約）
    - .planning/phases/02-ai/02-RESEARCH.md（Pattern 2「テンプレート選択 UI」・Pattern 3「外部mdファイル連動との関係」の `_on_template_change` 3手順コード例）
  </read_first>
  <action>
    `sections.py` の `_build` 内、カスタムプロンプト行（`self.ocr_prompt_text` を構築する `prompt_row`）の**直前**に「📄 テンプレート」セクションを挿入する（D-01: ペア保存＝1セレクタで custom+summary を同時切替）。構成:

    (1) `self.template_var = tk.StringVar(value=self.current_settings.get("prompt_templates", {}).get("active", ""))` と `self.template_combo = ttk.Combobox(..., textvariable=self.template_var, values=list_template_names(self.current_settings), state="readonly", font=self._font(-1))` を既存 `provider_combo` と同型で構築し、`<<ComboboxSelected>>` に `self._on_template_change` をバインドする（V180-TMPL-02）。`self._active_template_name` を現在の active 名で初期化する。

    (2) 保存/削除/リネームの3ボタンを同じ行に配置する。保存=`self._on_template_save`（`tk.simpledialog.askstring` で新規名を入力→`name.strip()` 空なら `tmpl_empty_error` を showerror→`template_name_exists` が True なら `tmpl_dup_error` を showerror して return＝D-04・ShortcutsDialog 型→問題なければ現在の入力欄内容 `ocr_prompt_text`/`ocr_summary_prompt_text` を `save_template` でペア保存＝D-06 ファイル連動時も入力欄内容をそのままコピー→`_save_settings` 呼び出し→combobox 再読込）、削除=`self.template_delete_btn`（`self._on_template_delete`: active テンプレートに対しては `_refresh_template_delete_state` でボタンを disabled にしておく＝D-03、押下時も二重防御で active 一致なら `tmpl_active_delete_blocked` を showinfo→`delete_template`→`_save_settings`→再読込）、リネーム=`self._on_template_rename`（askstring→重複は `tmpl_dup_error`＝D-04→`rename_template`→`_save_settings`→再読込）。

    (3) `_on_template_change(self, _event=None)` を D-05〜D-07 の3手順で実装する（RESEARCH Pattern 3）: 現在の入力欄内容が保存済みテンプレート/外部ファイル最終保存内容と異なる場合（`_has_unsaved_template_changes`）、ファイル連動モード（`prompt_file_exists(CUSTOM_PROMPT_FILE)` 等）なら `messagebox.askyesno(self._L["confirm_title"], self._L["tmpl_switch_discard_confirm"], parent=self)` を挟み、No なら `self.template_var.set(self._active_template_name)` で選択を戻して return（D-05 キャンセルで切替中止）。承認後、選択テンプレートを `get_template` で取得し `ocr_prompt_text`/`ocr_summary_prompt_text` を delete+insert で置換。ファイル連動モードなら `save_prompt_file(CUSTOM_PROMPT_FILE, tpl["custom_prompt"])`/`save_prompt_file(SUMMARY_PROMPT_FILE, tpl["summary_prompt"])` で外部ファイルを上書きし「アクティブテンプレートのライブ編集内容」の不変条件を保つ（D-07）。`self._active_template_name = new_name` を更新し、`prompt_templates["active"]` を `self.current_settings` 上で更新する。`_refresh_template_delete_state` で削除ボタン活性を再評価する。

    (4) `_has_unsaved_template_changes`/`_reload_template_combo`/`_refresh_template_delete_state` のヘルパーを追加する。専用の不一致検知 UI は新設せず既存 `_add_prompt_file_notice` 注記のみを流用する（D-08）。

    `lang.py` の ja/en 両ブロックへ本タスクの UI 文言キーを**同一キーで**追加する: `tmpl_section_title`、`tmpl_save_btn`、`tmpl_delete_btn`、`tmpl_rename_btn`、`tmpl_name_prompt`、`tmpl_dup_error`、`tmpl_empty_error`、`tmpl_switch_discard_confirm`、`tmpl_active_delete_blocked`。文言は Claude's Discretion（D-05 の切替確認は「未保存の変更が破棄されます。切り替えますか？」相当）。

    ruff（line-length 88・S/B・裸 except 禁止）を通す。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('pagefolio/dialogs/llm_config/sections.py', encoding='utf-8').read())"` が成功
    - `grep -c 'template_combo' pagefolio/dialogs/llm_config/sections.py` が 1 以上、`grep -c 'def _on_template_change' pagefolio/dialogs/llm_config/sections.py` が 1
    - `python -c "from pagefolio.lang import LANG; ja=set(LANG['ja']); en=set(LANG['en']); need={'tmpl_section_title','tmpl_dup_error','tmpl_switch_discard_confirm','tmpl_active_delete_blocked'}; assert need<=ja and need<=en; assert ja==en"` が成功（ja/en キー一致）
    - `pytest tests/test_provider_ui.py tests/test_lang_parity.py -x -q` が pass
    - `ruff check pagefolio/dialogs/llm_config/sections.py pagefolio/lang.py && ruff format --check pagefolio/dialogs/llm_config/sections.py pagefolio/lang.py` がクリーン
  </acceptance_criteria>
  <done>テンプレートセクション（combobox + CRUD ボタン + D-05/D-07 切替フロー）が sections.py に構築され、lang.py の ja/en キーが一致し、test_provider_ui.py/test_lang_parity.py がグリーン。</done>
</task>

<task type="auto">
  <name>Task 2: dialog.py _apply にアクティブテンプレート名収集を追加し、test_provider_ui.py にテンプレートテストを追加</name>
  <files>pagefolio/dialogs/llm_config/dialog.py, tests/test_provider_ui.py</files>
  <read_first>
    - pagefolio/dialogs/llm_config/dialog.py（`_apply` の `llm_settings` 収集フロー・`ocr_custom_prompt`/`ocr_summary_prompt` の書き戻し・`on_apply(llm_settings)` へ渡す構造・遅延 import で monkeypatch 互換を保つパターン）
    - pagefolio/ocr_dialog.py（`_apply_llm_settings` が `self.app.settings.update(llm_settings)`→`_save_settings` する経路・`load_custom_prompt` 同期）
    - tests/test_provider_ui.py（`_read_llm_config_package_source()` 共有ヘルパーで package 全体を走査する source-scan テストの記法・`TestLLMConfigDialogMRO` のヘッドレス検証スタイル）
    - pagefolio/settings.py（02-01 追加の CRUD 純関数・`prompt_templates` スキーマ）
  </read_first>
  <action>
    `dialog.py` の `_apply` に、アクティブテンプレート名を `llm_settings` へ収集する処理を追加する。既存の `prompt_templates` 辞書（`self.current_settings` 由来）の `items` は保持したまま `active` のみ現在の `self.template_var.get()`（または `self._active_template_name`）で更新した dict を `llm_settings["prompt_templates"]` へ格納する（`items` を落とさないよう `dict(self.current_settings.get("prompt_templates", {"active":"","items":{}}))` をコピーして active を差し替える）。CRUD ボタン（保存/削除/リネーム）は Task 1 で既に `self.current_settings` を直接変更し `_save_settings` 済みのため、`_apply` は active の最終確定のみを担う。`_apply_llm_settings`（ocr_dialog.py）が `self.app.settings.update(llm_settings)` するため、items+active の両方が正しく永続化される。ファイル書き戻し（`ocr_custom_prompt`/`ocr_summary_prompt` の外部ファイル同期）は既存経路を変更しない。

    `tests/test_provider_ui.py` に `TestTemplateSection` を追加する: (a) source-scan で `template_combo`/`_on_template_change`/`save_template` 参照が sections.py に存在すること、(b) 純ロジック連携＝`save_template`→`load_custom_prompt`（active 設定時にテンプレート値が解決される・V180-TMPL-05 の全プロバイダ共通経路）を settings dict レベルで検証、(c) `_apply` が `prompt_templates` を `items` を保持したまま active を差し替えて収集することのヘッドレス検証（既存 `_apply` スタブ経路の記法に合わせる）。
  </action>
  <verify>
    <automated>pytest tests/test_provider_ui.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'prompt_templates' pagefolio/dialogs/llm_config/dialog.py` が 1 以上（_apply が active を収集）
    - `pytest tests/test_provider_ui.py::TestTemplateSection -x` が pass
    - `python -c "from pagefolio.settings import save_template, load_custom_prompt, _get_base_dir" ` が import 成功（連携シンボルが揃う）
    - `pytest tests/test_provider_ui.py tests/test_imports.py -x -q` が pass（後方互換 import 回帰なし）
    - `ruff check pagefolio/dialogs/llm_config/dialog.py tests/test_provider_ui.py && ruff format --check pagefolio/dialogs/llm_config/dialog.py tests/test_provider_ui.py` がクリーン
  </acceptance_criteria>
  <done>_apply が active テンプレート名を items 保持のまま収集して永続化し、TestTemplateSection がグリーン。全プロバイダ共通の load_*_prompt 経路でテンプレートが解決されることが検証される。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー入力（テンプレート名） → settings.py CRUD | askstring 由来の名前が永続テンプレート辞書のキーになる |
| LLM 設定ダイアログ → pagefolio_settings.json | prompt_templates（active+items）が永続化される |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | テンプレート名の空文字・重複入力（Input Validation V5/ASVS L1） | low | mitigate | `_on_template_save`/`_on_template_rename` が空名は `tmpl_empty_error`、重複は `tmpl_dup_error` を showerror して return（D-04・ShortcutsDialog 型） |
| T-02-05 | Tampering | テンプレート切替時の外部mdファイル書き戻し競合（未保存編集の消失） | medium | mitigate | D-05 の未保存差分確認ダイアログ（askyesno・キャンセルで切替中止）+ D-07 の「アクティブテンプレートのライブ編集内容」不変条件を厳守 |
| T-02-06 | Information Disclosure | テンプレート内容へのAPIキー誤混入 | low | accept | テンプレート内容は非機密前提。`_SENSITIVE_KEYS` ガードは無改造で維持（02-01 で新規キーの非衝突を検証済み） |
</threat_model>

<verification>
- `pytest tests/test_provider_ui.py tests/test_lang_parity.py tests/test_imports.py -x -q`
- `pytest`（フルスイート）
- `ruff check . && ruff format --check .`
</verification>

<success_criteria>
- LLM 設定ダイアログに📄テンプレートセクション（combobox + 保存/削除/リネーム）が存在し、D-01/D-03/D-04/D-05/D-06/D-07/D-08 を満たす
- テンプレート名重複・空名が UI で拒否され、アクティブ削除ボタンが無効化される
- テンプレート切替が未保存差分確認（D-05）→外部ファイル書き戻し（D-07）の順で動作する
- active テンプレート名が items を保持したまま永続化される
- lang.py の ja/en キーが一致、test_provider_ui.py/test_lang_parity.py/フルスイートがグリーン、ruff クリーン
</success_criteria>

<output>
Create `.planning/phases/02-ai/02-02-SUMMARY.md` when done
</output>
