---
phase: 04-ocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/batch_ocr_state.py
  - tests/test_batch_ocr_state.py
autonomous: true
requirements:
  - V180-BATCH-01
  - V180-BATCH-02
must_haves:
  truths:
    - "enqueue_files は同一パスの重複投入を1件にまとめる（dedup）"
    - "enqueue_files は既にキューにあるパスを再投入しても件数を増やさない"
    - "空リストを enqueue_files に渡してもエラーにならずキュー件数は変わらない（空キュー始点の安全性）"
    - "BatchFileEntry の初期状態は STATUS_PENDING（待機）である"
    - "BatchState.mark_completed / mark_failed / mark_cancelled で files_done() が単調増加し total_files を超えない（進捗が100%を超えない）"
    - "BatchState.files_done() は completed + failed + cancelled の合計であり、ページ軸カウンタ（OCRRunEngine.progress_count 相当）から逆算されない（落とし穴5・二軸独立）"
    - "壊れた/開けないPDFは page_count=0 の代わりに STATUS_ERROR として記録できる（Open Question 2・D-06 整合）"
    - "count_pending(entries) は STATUS_PENDING のエントリ数のみを返し、STATUS_ERROR/STATUS_DONE 等は算入しない。BatchState.total_files はこの値で構築されるため STATUS_ERROR ファイルは実行対象に含まれず remaining() が完了時に 0 へ収束する（レビュー懸念6・進捗収束の構造的保証）"
    - statement: "batch_ocr_state.py はトップレベルで tkinter / fitz を import しない（grep で import 行に tkinter・fitz が現れない）"
      verification: backstop
  artifacts:
    - pagefolio/batch_ocr_state.py
    - tests/test_batch_ocr_state.py
  key_links:
    - "BatchState.files_done()（ファイル軸）と OCRRunEngine.progress_count()（ページ軸）は独立カウンタ（Pitfall 3 の構造的予防）"
    - "BatchFileEntry は per-file 状態を独立保持し使い回さない（ocr_engine.py D-09/D-11 原則のファイル単位外挿）"
    - "count_pending(entries)（実行対象=STATUS_PENDING のみ）→ BatchState(total_files=count_pending(...))（04-02 が構築）→ STATUS_ERROR 除外で remaining() 収束（レビュー懸念6）"
  prohibitions:
    - "バッチOCRは、集約コスト確認（D-03）・サマリ手動トリガー（D-13）・過大入力警告（D-14）を経ずにクラウドAPIへページ画像やテキストを自動送信してはならない（明示同意なしの外部送信禁止・security-canon: threat_model T-04-01 で追跡）"
    - "バッチOCRは、ファイル失敗時に別プロバイダへ自動フォールバック切替してはならない（Phase 2 の送信先確認再提示フローと混同せず、D-09 の単純スキップに限定する）"
    - "バッチOCRは、キュー内容・OCR結果・APIキーをアプリ再起動を跨いで永続化してはならない（D-11・Anti-Feature、ダイアログを閉じたら破棄する）"
---

<objective>
バッチOCRのファイルキュー状態遷移とファイル軸進捗集計を担う純ロジック層 `pagefolio/batch_ocr_state.py`（Tk/fitz 非依存）を新設し、その単体テストを整備する。

Purpose: `ocr_pipeline.py`（`PipelineState`）と同格の Tk/fitz 非依存の純ロジック層を Wave 2 の `BatchOCRDialog` より先に確立し、UI 実装前に状態遷移・進捗集計・重複除外・二軸独立性（落とし穴5）をテストで固める。Phase 3 の `OCRRunEngine` が「実行ごとに新規生成（D-11）」なのと同じ per-run 独立原則をファイル単位へ外挿する（V180-BATCH-01 のキュー投入ロジック・V180-BATCH-02 の状態/進捗集計）。
Output: `pagefolio/batch_ocr_state.py`（`BatchFileEntry` / `BatchState` / `enqueue_files` / STATUS 定数）と `tests/test_batch_ocr_state.py`。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ocr/04-CONTEXT.md
@.planning/phases/04-ocr/04-RESEARCH.md
@.planning/phases/04-ocr/04-PATTERNS.md

# 純ロジック層の analog（Lock 保護カウンタ・Tk/fitz 非依存の作法）
@pagefolio/ocr_pipeline.py
@pagefolio/ocr_engine.py
@tests/test_ocr_pipeline.py
</context>

## Artifacts this phase produces

このプラン（04-01）が新規に作る/公開するシンボル。plan-review-convergence の source-grounding は本節を新規シンボルの許可リストとして読む。

- 新規ファイル: `pagefolio/batch_ocr_state.py`
- 新規ファイル: `tests/test_batch_ocr_state.py`
- クラス: `BatchFileEntry`（フィールド: `path` / `display_name` / `page_count` / `status` / `results`（page_idx→text の dict）/ `errors`）
- クラス: `BatchState`（メソッド: `__init__(total_files)` / `mark_completed()` / `mark_failed()` / `mark_cancelled()` / `files_done()` / `remaining()`。属性: `total_files` / `completed` / `failed` / `cancelled`）
- 関数: `enqueue_files(entries, paths, page_counts=None)`（重複除外つきキュー追加。既存 `BatchFileEntry` リストへ新規パスのみを追加して返す）
- 関数: `count_pending(entries)`（`status == STATUS_PENDING` のエントリ数を返す。04-02 が `BatchState(total_files=count_pending(entries))` を構築する際の実行対象件数。STATUS_ERROR/STATUS_DONE を除外し remaining() 収束を保証・レビュー懸念6）
- モジュール定数: `STATUS_PENDING` / `STATUS_RUNNING` / `STATUS_DONE` / `STATUS_FAILED` / `STATUS_ERROR`

> 後続フェーズが作るシンボル（`BatchOCRDialog` 等）は 04-02 / 04-03 の Artifacts 節を参照。

## Review Incorporation (Antigravity 04-REVIEWS.md)

本プランに対するクロスAIレビュー（Antigravity・04-REVIEWS.md）の actionable finding の反映状況。

- **懸念6（LOW・04-01）STATUS_ERROR ファイルの進捗集計遷移**: 反映済み。`count_pending(entries)`（STATUS_PENDING のみを算入）を新設し、`BatchState.total_files` は STATUS_ERROR を除外した実行対象件数で構築する契約を確立（Task 2 の behavior/action・docstring に明記、Task 1 に `test_error_file_excluded_from_total` を追加）。これにより STATUS_ERROR ファイルは実行対象に含まれず `remaining()` が完了時に 0 へ収束する（`_confirm_batch_cost` が STATUS_ERROR をページ合計から除外する 04-02 の設計とも一貫）。
- **懸念2（MEDIUM・`test_no_unused_lang_keys` 段階実装競合）**: 04-01 には該当しない（本プランの `files_modified` は `batch_ocr_state.py` / `tests/test_batch_ocr_state.py` のみで `pagefolio/lang.py` を一切変更しないため、`tests/test_lang_parity.py::test_no_unused_lang_keys` の未使用キー検査を発火させない）。lang キーを追加する 04-02 / 04-03 側でこの懸念に対する具体的対処（同一タスク内でのキー参照・必要時 `_ALLOWLIST` フォールバック）を反映する（各プランの Review Incorporation 節参照）。silent drop ではなく、影響プランへ明示的に転送した記録。
- 強みとして評価された「Tk/fitz 非依存の純ロジック層」「Lock 保護の二軸独立進捗カウンタ」は既存設計を維持する（構造変更なし）。

<tasks>

<task type="auto">
  <name>Task 1: test_batch_ocr_state.py を先行作成（Wave 0 / RED テスト先行）</name>
  <files>tests/test_batch_ocr_state.py</files>
  <read_first>
    - tests/test_ocr_pipeline.py（PipelineState の Lock 保護テストパターン・テスト記法の analog）
    - pagefolio/ocr_pipeline.py（PipelineState の __init__/カウンタ設計を参照し、BatchState の期待挙動を確定する）
    - .planning/phases/04-ocr/04-RESEARCH.md（Pattern 2「BatchState 薄い純関数層」・Validation Architecture の Test Map）
  </read_first>
  <action>
    Wave 0 のテスト先行スキャフォールドとして `tests/test_batch_ocr_state.py` を新規作成する。まだ実装していない `pagefolio.batch_ocr_state` に対する RED テストを、後続 Task 2 が満たすべき仕様として記述する。`tests/test_ocr_pipeline.py` の import/クラス記法（`sys.path.insert` 前置き・`class TestXxx:` 構成）をそのまま踏襲し、新規パターンを持ち込まない。

    次のテスト関数を定義する（RESEARCH の Test Map の関数名に合わせる）:
    - `test_enqueue_files`: 空キューに2パスを enqueue → 2件。同じパスを再度 enqueue → 件数不変（dedup）。空リスト enqueue → 件数不変（空キュー始点）。`page_counts` を渡すと各 `BatchFileEntry.page_count` に反映される。
    - `test_state_transitions`: `BatchFileEntry` 初期 `status == STATUS_PENDING`。STATUS 定数（PENDING/RUNNING/DONE/FAILED/ERROR）が全て相異なる文字列であること。
    - `test_progress_aggregation`: `BatchState(total_files=3)` で `files_done()==0` 始点 → `mark_completed()`・`mark_failed()`・`mark_cancelled()` 各1回で `files_done()==3`・`remaining()==0`。`files_done()` は `total_files` を超えない。
    - `test_broken_pdf_error_status`: `page_count==0`（fitz.open 失敗相当）のエントリを `STATUS_ERROR` として表現できる（Open Question 2）。
    - `test_error_file_excluded_from_total`（レビュー懸念6）: STATUS_PENDING 2件 + STATUS_ERROR 1件の混在エントリ列に対し `count_pending(entries)==2`（STATUS_ERROR は算入しない）。`BatchState(total_files=count_pending(entries))`（=2）で、対象2件を `mark_completed`/`mark_failed` 相当で処理すると `remaining()==0` へ収束する（STATUS_ERROR ファイルが分母に残らないことを検証）。
    - `test_no_tk_fitz_toplevel_import`: `pagefolio/batch_ocr_state.py` のソースを読み、トップレベル import 行に `tkinter` / `fitz` / `import fitz` が現れないことを assert する（`ocr_pipeline.py` と同じ純ロジック層作法・backstop 相当を自動化）。

    テストは決定的・実 fitz/ネットワーク非依存にすること。BatchState の並行安全性（Lock 保護）は Task 2 の実装で担保するため、本テストでは API 契約（メソッド戻り値・単調性）のみを検証する。
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/test_batch_ocr_state.py', encoding='utf-8').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_batch_ocr_state.py` が存在し、上記6テスト関数（test_enqueue_files / test_state_transitions / test_progress_aggregation / test_broken_pdf_error_status / test_error_file_excluded_from_total / test_no_tk_fitz_toplevel_import）を定義している
    - `python -c "import ast; ast.parse(...)"` が exit 0（構文的に妥当）
    - この時点で `pytest tests/test_batch_ocr_state.py -x` は RED（`pagefolio.batch_ocr_state` 未実装による ImportError/ModuleNotFoundError）であり、構文エラーではないこと
    - テストは `tests/test_ocr_pipeline.py` の import 記法を踏襲し、新規外部依存を追加していない
  </acceptance_criteria>
  <done>test_batch_ocr_state.py が6テスト（STATUS_ERROR 進捗収束の懸念6テスト含む）を定義し構文妥当。batch_ocr_state.py 未実装のため RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: batch_ocr_state.py を実装してテストを GREEN 化</name>
  <files>pagefolio/batch_ocr_state.py</files>
  <read_first>
    - tests/test_batch_ocr_state.py（Task 1 で固定した RED 仕様。これを満たす実装を書く）
    - pagefolio/ocr_pipeline.py（PipelineState の `threading.Lock` 保護カウンタパターン・モジュール docstring 規約をそのまま踏襲する analog）
    - pagefolio/ocr_engine.py（docstring 1-27行の「トップレベルで tkinter/fitz を import しない」制約・per-run 新規生成原則）
  </read_first>
  <behavior>
    - enqueue_files(entries, paths, page_counts=None): entries（既存 BatchFileEntry のリスト）に、まだ含まれない path のみ新規 BatchFileEntry として追加し、更新後リストを返す。dedup キーは path。page_counts（path→int の dict）指定時は各 entry.page_count に反映。空 paths は no-op。
    - BatchFileEntry(path, page_count=0): status は生成時 STATUS_PENDING。display_name は os.path.basename(path) 相当（表示用）。results（page_idx→text）/ errors を per-file の独立辞書として所有し使い回さない。
    - BatchState(total_files): completed/failed/cancelled=0 始点。mark_completed/mark_failed/mark_cancelled はそれぞれのカウンタを +1（全て `with self._lock:` 保護）。files_done()=completed+failed+cancelled。remaining()=max(0, total_files-files_done())。files_done() は total_files を超えない範囲で単調増加。
    - count_pending(entries): `status == STATUS_PENDING` のエントリ数を返す純関数。04-02 は `BatchState(total_files=count_pending(entries))` を構築し、STATUS_ERROR（壊れたPDF）を実行対象＝分母から除外する。これにより実行完了時に `remaining()` が 0 へ収束する（レビュー懸念6）。
    - STATUS_PENDING/RUNNING/DONE/FAILED/ERROR は相異なる文字列定数。
    - モジュールはトップレベルで logging/threading/os のみ import（tkinter/fitz を import しない）。
  </behavior>
  <action>
    `pagefolio/batch_ocr_state.py` を新規作成する。先頭に既存モジュール（`ocr_engine.py` / `ocr_pipeline.py`）と同一の3行 MIT ヘッダーコメントと、Tk/fitz 非依存・純ロジック層である旨のモジュール docstring を置く（`ocr_engine.py:1-27` の docstring 規約に倣う）。docstring 内に「STATUS_ERROR エントリは実行対象に含めない（BatchState.total_files は count_pending で算出＝STATUS_PENDING のみ）。これによりファイル軸進捗 remaining() が完了時に 0 へ収束する」という進捗集計の不変条件を明記する（レビュー懸念6）。

    `STATUS_PENDING`/`STATUS_RUNNING`/`STATUS_DONE`/`STATUS_FAILED`/`STATUS_ERROR` を相異なる文字列定数として定義する。`BatchFileEntry` は per-file 状態（path・display_name・page_count・status・results dict・errors dict）を独立保持する（`ocr_engine.py` の per-run 新規生成原則をファイル単位へ外挿・使い回さない）。`BatchState` は `threading.Lock` で全メソッドを保護し（`PipelineState.__init__` の `self._lock = threading.Lock()` パターン）、ファイル軸カウンタ（completed/failed/cancelled）のみを集計する。`files_done()`（ファイル軸）は `OCRRunEngine.progress_count()`（ページ軸）から一切逆算しない（落とし穴5の二軸独立を構造的に担保）。`enqueue_files` は path をキーに dedup してキュー追加する純関数とする。`count_pending(entries)` は `status == STATUS_PENDING` のエントリ数を返す純関数として定義し、STATUS_ERROR/STATUS_DONE 等を算入しない（レビュー懸念6・進捗収束契約の情報源）。

    トップレベル import は `logging`/`threading`/`os` に限定し、`tkinter`・`fitz` を import しない。`ruff check . && ruff format .` が通ることを確認する。
  </action>
  <verify>
    <automated>pytest tests/test_batch_ocr_state.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pagefolio/batch_ocr_state.py` が存在し、`from pagefolio.batch_ocr_state import BatchFileEntry, BatchState, enqueue_files, count_pending, STATUS_PENDING, STATUS_RUNNING, STATUS_DONE, STATUS_FAILED, STATUS_ERROR` が成功する
    - `pytest tests/test_batch_ocr_state.py -x` が exit 0（6テスト全 green）
    - `count_pending(entries)` が STATUS_PENDING のみを算入し、`test_error_file_excluded_from_total` が green（STATUS_ERROR 除外・remaining() 収束・レビュー懸念6）
    - `pagefolio/batch_ocr_state.py` のトップレベル import に `tkinter` / `fitz` が現れない（test_no_tk_fitz_toplevel_import が green）
    - `ruff check pagefolio/batch_ocr_state.py tests/test_batch_ocr_state.py` が exit 0
    - `pytest` フルスイートが green（既存 880件超の回帰なし）
  </acceptance_criteria>
  <done>batch_ocr_state.py が BatchFileEntry/BatchState/enqueue_files/count_pending を提供し、test_batch_ocr_state.py 全6テスト green（STATUS_ERROR 進捗収束含む）。フルスイート回帰なし。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー選択/D&D パス → キュー状態 | ローカルファイルパス（未検証入力）が `enqueue_files` に渡される。ページ数スキャン結果（0=壊れ）を状態へ反映する |
| 純ロジック層 ↔ 外部 | `batch_ocr_state.py` は Tk/fitz/ネットワークに一切触れない（純ロジック層。外部境界を持たない） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering/Input Validation | `enqueue_files` / `BatchFileEntry`（壊れたPDF・非PDFパス） | low | mitigate | 壊れた/開けないPDF（page_count=0）は STATUS_ERROR として明示的に表現し、下流（04-02）で削除のみ可能な行として扱う。拡張子フィルタ自体は 04-02 の D&D ハンドラが担う |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | 本フェーズは新規パッケージ導入なし（RESEARCH Package Legitimacy Audit: 該当なし）。install タスクは存在しない |
</threat_model>

<verification>
- `pytest tests/test_batch_ocr_state.py -x` が green
- `pytest` フルスイートが green
- `ruff check . && ruff format .` がクリーン
- `python -c "import ast; ast.parse(open('pagefolio/batch_ocr_state.py', encoding='utf-8').read())"` が exit 0
</verification>

<success_criteria>
- `pagefolio/batch_ocr_state.py` が Tk/fitz 非依存の純ロジック層として存在し、`BatchFileEntry`/`BatchState`/`enqueue_files`/STATUS 定数を提供する
- ファイル軸進捗（`BatchState.files_done()`）とページ軸進捗が独立カウンタとして分離されている（落とし穴5の構造的予防）
- キュー投入の重複除外・空キュー始点・壊れたPDF（STATUS_ERROR）が単体テストで green
- `count_pending` により STATUS_ERROR が実行対象＝分母から除外され、`remaining()` が完了時に 0 へ収束する（レビュー懸念6・進捗集計の収束保証）
</success_criteria>

<output>
Create `.planning/phases/04-ocr/04-01-SUMMARY.md` when done
</output>
