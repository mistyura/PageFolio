---
phase: 05-blob-shortcutsdialog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/thumb_cache.py
  - pagefolio/pagination.py
  - tests/test_thumb_cache.py
  - tests/test_pagination.py
  - tests/test_selection_invariant.py
autonomous: true
requirements: [V180-PERF-01, V180-PERF-02, V180-PERF-03]
must_haves:
  truths:
    - "compute_visible_range(view_top, view_bottom, frame_bounds) は viewport [view_top, view_bottom) と交差するフレームの半開ローカル区間 (vis_lo, vis_hi) を返し、frame_bounds が空・交差なしなら (0, 0) を返す（V180-PERF-01）"
    - "prioritized_render_order(lo, hi, vis_lo, vis_hi) は可視 global index を先頭に、残り窓内 index を lo→hi 順で続け、[lo, hi) の各 index をちょうど1回ずつ含む（V180-PERF-01）"
    - "LruCache は size が maxsize を超えた時に最古参照エントリを1件エビクトし、既存キーの読み取り（__getitem__）で recency が更新される（V180-PERF-02）"
    - "seed 固定の random.Random 操作列（選択/スクロール/D&D）を 500 ページ相当へ適用しても selected_pages の全値が [0, n_pages) に留まり、窓ローカル添字の混入が起きない（V180-PERF-03）"
  artifacts:
    - pagefolio/thumb_cache.py
    - tests/test_thumb_cache.py
    - tests/test_selection_invariant.py
  key_links:
    - "05-02（viewer.py 統合）が pagination.compute_visible_range / prioritized_render_order と thumb_cache.LruCache を消費する — シグネチャは本プランで確定する"
  prohibitions: []
---

<objective>
サムネイル仮想化（画像遅延レンダリング型・D-01〜D-08）と selected_pages 不変条件保証（D-04）の Tk/fitz 非依存な純ロジック基盤層を新設する。可視範囲判定・優先描画順序・LRU コンテナを純関数/純クラスとして先に確定し、05-02 の viewer.py 統合が消費できるようにする。

Purpose: 落とし穴1（仮想化が selected_pages 全ページインデックス不変条件を破壊）を構造的に回避するため、可視範囲・描画順序の計算を pagination.py 純関数へ集約し、新規座標系モジュールを作らない。落とし穴2（キャッシュとウィジェット再利用の責務混同）を回避するため LRU コンテナはキー=ページ番号のみの汎用コンテナとして分離する。
Output: pagefolio/thumb_cache.py（LruCache）・pagination.py への compute_visible_range / prioritized_render_order・3種のテスト（LRU ユニット・可視範囲/描画順序ユニット・selected_pages 不変条件プロパティ風テスト）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-blob-shortcutsdialog/05-CONTEXT.md
@.planning/phases/05-blob-shortcutsdialog/05-RESEARCH.md
@.planning/phases/05-blob-shortcutsdialog/05-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 05-01)

新規に生成するシンボル（05-02 以降および drift 検証はこれらを本フェーズ産物として扱う）:

- 新規ファイル `pagefolio/thumb_cache.py`
- クラス `LruCache`（`pagefolio/thumb_cache.py`）— コンストラクタ `LruCache(maxsize)`、dict 風 API: `__contains__` / `__getitem__`（ヒット時 move_to_end）/ `__setitem__`（超過時 popitem(last=False)）/ `pop(key, default=None)` / `clear()` / `__len__`
- 関数 `compute_visible_range(view_top, view_bottom, frame_bounds)`（`pagefolio/pagination.py`）
- 関数 `prioritized_render_order(lo, hi, vis_lo, vis_hi)`（`pagefolio/pagination.py`）
- 新規ファイル `tests/test_thumb_cache.py`
- 新規ファイル `tests/test_selection_invariant.py`
- `tests/test_pagination.py` への追加テストクラス（compute_visible_range / prioritized_render_order 用）

<tasks>

<task type="auto">
  <name>Task 1: LruCache コンテナ新設（pagefolio/thumb_cache.py）+ ユニットテスト</name>
  <files>pagefolio/thumb_cache.py, tests/test_thumb_cache.py</files>
  <read_first>
    - pagefolio/undo_store.py（Tk/fitz 非依存の純クラス設計・モジュール docstring 作法・__slots__ 規約の analog）
    - pagefolio/pagination.py:1-22（「fitz / tkinter を一切 import しない」純ロジック層宣言の書式）
    - .planning/phases/05-blob-shortcutsdialog/05-RESEARCH.md「Pattern 3: OrderedDict ベース LRU」（collections.OrderedDict の move_to_end / popitem(last=False) 引用）
    - .planning/phases/05-blob-shortcutsdialog/05-PATTERNS.md「現行 thumb_cache」（viewer.py:136-154 の呼び出し面 = in 判定 / [i] 取得 / [i]= 代入 / .pop(p, None) / .clear()）
  </read_first>
  <action>
    D-08 の配置判断を「新規モジュール pagefolio/thumb_cache.py」に確定する（pagination.py は窓計算専用に保ち責務を混ぜない・RESEARCH.md Open Question 1 推奨）。モジュール docstring 冒頭に「サムネイル画像 LRU キャッシュの汎用コンテナ — Tkinter / fitz 非依存」と undo_store.py と同じ純ロジック層宣言を書く。標準ライブラリ collections.OrderedDict のみ import する（tkinter / fitz / pagefolio 内部モジュールは import しない）。

    クラス LruCache を定義する。コンストラクタ LruCache(maxsize) は maxsize（正の int）と内部 OrderedDict を保持する。既存 viewer.py 呼び出し面（05-02 で無改造にする）を維持するため dict 風 API を実装する: __contains__(key)、__getitem__(key)（ヒット時に move_to_end(key) で recency を更新して値を返し、ミス時は KeyError を送出）、__setitem__(key, value)（既存キーは move_to_end、格納後に len > maxsize なら popitem(last=False) で最古を1件エビクト）、pop(key, default=None)、clear()、__len__。エビクションは純粋 LRU（容量到達時に最古参照分だけ自然に押し出し、窓移動時の積極パージは設けない・D-07）とする。値は不透明オブジェクト（ImageTk.PhotoImage 等）として扱い型検査しない。

    THUMB_CACHE_MAX 定数の定義は本プランでは行わない（LruCache は maxsize を引数で受ける汎用クラスのまま。定数定義と app.py 配線は 05-02 の責務）。裸 except は使わない（本タスクは例外捕捉不要）。

    tests/test_thumb_cache.py を新設し、LruCache の以下をユニット検証する: 容量到達後の put で最古参照分だけがエビクトされること（maxsize=2 で 3 キー投入 → 最古消滅・最新2キー残存）、__getitem__ による recency 更新後は別キーが先にエビクトされること（LRU 順序の正しさ）、pop(既存/欠損, default) と clear() と __contains__ と __len__ の挙動、ミスキー __getitem__ が KeyError を送出すること。Tk root は生成しない（純クラスのみ）。
  </action>
  <verify>
    <automated>pytest tests/test_thumb_cache.py -x</automated>
  </verify>
  <acceptance_criteria>
    - pagefolio/thumb_cache.py が存在し `class LruCache` を含む
    - pagefolio/thumb_cache.py が `from collections import OrderedDict`（または `import collections`）のみを外部依存として持ち、`import tkinter` / `import fitz` を含まない
    - LruCache が `__contains__` `__getitem__` `__setitem__` `pop` `clear` `__len__` を実装している
    - `pytest tests/test_thumb_cache.py -x` が exit 0
    - maxsize=2 で 3 件投入すると len==2 かつ最古キーが `in` 判定 False（LRU エビクション動作）
  </acceptance_criteria>
  <done>LruCache が dict 風 API で LRU エビクションと recency 更新を提供し、専用ユニットテストがグリーン。viewer.py の既存呼び出し面（in/[]/[]=/pop/clear）が 05-02 で無改造で載る API 契約が確定している。</done>
</task>

<task type="auto">
  <name>Task 2: 可視範囲・優先描画順序の純関数を pagination.py へ追加 + ユニットテスト</name>
  <files>pagefolio/pagination.py, tests/test_pagination.py</files>
  <read_first>
    - pagefolio/pagination.py（全体・特に window_bounds / to_global / to_local の docstring 書式・堅牢性エッジケース明記・決定ID引用スタイル）
    - pagefolio/dnd.py:12-29（compute_dnd_dest_index — frame_bounds=[(y, height), ...] を受ける Tk 非依存純関数の前例。可視範囲計算も同型の座標比較にする）
    - pagefolio/viewer.py:280-310（_build_thumbnails の現行 window_bounds→[lo,hi) 描画。05-02 がこの純関数を consume する形を把握する）
    - tests/test_pagination.py（TestWindowBounds 等の既存テストクラス構成・純関数のみのテストスタイル）
  </read_first>
  <action>
    pagination.py に module-level 純関数 compute_visible_range(view_top, view_bottom, frame_bounds) を追加する。frame_bounds は窓ローカル順の [(y, height), ...]（dnd.compute_dnd_dest_index と同じ形。Tk 依存の座標収集は 05-02 の viewer 側が担い、比較のみを本純関数が担う・落とし穴1回避策）。フレーム i が viewport と交差する条件を「frame_y < view_bottom かつ frame_y + height > view_top」とし、交差する最初のローカル index を vis_lo、最後のローカル index + 1 を vis_hi として半開区間 (vis_lo, vis_hi) を返す。frame_bounds が空、または交差フレームが無い場合は (0, 0) を返す。戻り値は必ず 0 <= vis_lo <= vis_hi <= len(frame_bounds)。docstring は既存純関数の書式（簡潔な日本語・不変条件と堅牢性エッジケースの明記・参照 V180-PERF-01/落とし穴1）に従う。

    pagination.py に module-level 純関数 prioritized_render_order(lo, hi, vis_lo, vis_hi) を追加する。lo/hi は窓の半開区間（window_bounds 由来）、vis_lo/vis_hi は可視 global index の半開区間（呼び出し側で lo/hi 内へクランプ済みを前提としつつ、防御的に [lo, hi) へクランプする）。戻り値は list であり、まず range(vis_lo, vis_hi) の可視 index を順に、続いて [lo, hi) のうち可視区間に含まれない index を lo→hi 昇順で並べる。不変条件: 返り値は [lo, hi) の全 index をちょうど1回ずつ含み、長さは hi - lo に等しい。vis_lo>=vis_hi（可視なし）の場合は list(range(lo, hi)) と等価になる。docstring に落とし穴2（キャッシュキーはページ番号のみ・本関数は描画スケジューリングの順序決定のみを担い LRU ヒット判定と混同しない）を明記する。

    どちらも fitz / tkinter を import しない（pagination.py の既存宣言を維持）。tests/test_pagination.py に TestVisibleRange / TestPrioritizedRenderOrder クラスを追加し、compute_visible_range の交差判定・空入力→(0,0)・上端/下端クランプ、prioritized_render_order の「可視先頭・残り lo→hi・全 index 1回被覆・長さ==hi-lo・可視なし時の等価性」を検証する。
  </action>
  <verify>
    <automated>pytest tests/test_pagination.py -k "Visible or PrioritizedRender or visible or render_order" -x</automated>
  </verify>
  <acceptance_criteria>
    - pagefolio/pagination.py が `def compute_visible_range(` と `def prioritized_render_order(` を含む
    - pagefolio/pagination.py が `import fitz` / `import tkinter` を含まない（純ロジック層維持）
    - `pytest tests/test_pagination.py -k "Visible or PrioritizedRender or visible or render_order" -x` が exit 0
    - prioritized_render_order(0, 5, 2, 4) == [2, 3, 0, 1, 4]（可視先頭→残り昇順・全 index 1回被覆）
    - compute_visible_range(0, 0, []) == (0, 0)
  </acceptance_criteria>
  <done>可視範囲判定と優先描画順序が pagination.py の Tk 非依存純関数として確定し、専用ユニットテストがグリーン。05-02 の viewer.py はこの2関数を呼ぶだけで新規座標系を作らずに済む。</done>
</task>

<task type="auto">
  <name>Task 3: selected_pages 全ページインデックス不変条件のプロパティ風テスト新設</name>
  <files>tests/test_selection_invariant.py</files>
  <read_first>
    - pagefolio/pagination.py（window_bounds / to_global / to_local / window_for_page / reconcile_window_start / clamp_window_start・本テストが駆動する純関数群）
    - pagefolio/dnd.py:94-140（_dnd_dest_index / _dnd_drop の to_global 経由変換 = 窓ローカル位置→全ページ index の正しい変換規約）
    - tests/test_pagination.py（純関数のみ・Tk root 不要のテストスタイルの前例）
    - .planning/phases/05-blob-shortcutsdialog/05-RESEARCH.md「純関数のみのテスト」（500+ページ相当は実 fitz.Document 不要・n_pages を整数パラメータでシミュレート）
  </read_first>
  <action>
    tests/test_selection_invariant.py を新設する。hypothesis 等の新規依存は使わず random.Random(seed) で決定的なランダム操作列を生成する（D-04・V14-D-01 依存追加なし方針）。実 fitz.Document は生成せず、n_pages（例: 500）を整数パラメータとし、selected_pages を set、window_start / current_page / page_size を整数で保持するシミュレーションモデルを組む。

    1回のシード実行で数百ステップのランダム操作列を適用する。各ステップは random.Random(seed) から操作種別を選ぶ: (a) 選択トグル — 可視窓内のローカル位置を選び pagination.to_global(local, window_start) で全ページ index へ換算してから selected_pages に add/discard する（絶対に窓ローカル添字を直接 selected_pages へ入れない）、(b) スクロール/窓移動 — window_start を別の窓先頭へ動かし reconcile_window_start/clamp_window_start で正規化する、(c) D&D 並び替え相当 — dnd.compute_dnd_dest_index が返すローカル dest を to_global で全ページ index へ換算し、選択集合を並び替え後の index へ写像する（src→dest のインデックスシフトを全ページ空間で適用）。

    各ステップ後に不変条件をアサートする: selected_pages の全要素が 0 <= p < n_pages に収まること（窓ローカル添字＝小さすぎる/大きすぎる値の混入がないこと）、current_page が [0, n_pages) に収まること、window_bounds が常に 0<=lo<=hi<=n_pages を満たすこと。複数シード（例: range(20) の seed）でパラメトライズし、各シードで再現可能な失敗が出せるようにする（失敗時にシード値を assert メッセージへ含める）。可視範囲純関数（compute_visible_range）が絡むスクロールステップも1種類含め、可視区間の計算結果が selected_pages を書き換えないこと（描画順序と選択状態の分離）を確認する。
  </action>
  <verify>
    <automated>pytest tests/test_selection_invariant.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_selection_invariant.py が存在し `random.Random(` を使う（hypothesis を import しない）
    - テストが n_pages>=500 のシミュレーションで selected_pages 全要素の 0<=p<n_pages 不変条件を各ステップでアサートする
    - `pytest tests/test_selection_invariant.py -x` が exit 0
    - 複数シード（>=10）でパラメトライズされ、失敗時に seed 値が特定可能
  </acceptance_criteria>
  <done>500+ページ相当のランダム選択/スクロール/D&D 操作列で selected_pages が全ページインデックス不変条件を保つことがシード固定で決定的に検証され、05-02 の viewer 統合が導入しうる窓ローカル添字混入を回帰検知できる。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （なし） | 単一プロセス内・ローカルデスクトップアプリの内部純ロジック改修。ネットワーク/認証/永続化境界を跨がない |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | thumb_cache の無制限成長（メモリ枯渇・V180-PERF-02） | medium | mitigate | LruCache(maxsize) で上限を設ける汎用コンテナを本プランで新設し、実際の上限適用は 05-02 で配線する |
| T-05-02 | Tampering | selected_pages への窓ローカル添字混入（選択状態の破壊・落とし穴1） | medium | mitigate | pagination.py 純関数（to_global 経由）でのみ index 変換し、プロパティ風テスト（Task 3）で全ページインデックス不変条件を回帰検証する |
</threat_model>

<verification>
- `pytest tests/test_thumb_cache.py tests/test_pagination.py tests/test_selection_invariant.py -x` が全てグリーン
- `ruff check . && ruff format .` がクリーン
- pagefolio/thumb_cache.py と pagination.py の新規追加が fitz / tkinter を import しない（純ロジック層維持）
</verification>

<success_criteria>
- LruCache が LRU エビクション + recency 更新を提供し dict 風呼び出し面を持つ（V180-PERF-02 の基盤）
- compute_visible_range / prioritized_render_order が pagination.py の純関数として確定（V180-PERF-01 の基盤）
- selected_pages 全ページインデックス不変条件がシード固定プロパティ風テストで保証される（V180-PERF-03）
- 新規依存ゼロ（標準ライブラリのみ）
</success_criteria>

<output>
Create `.planning/phases/05-blob-shortcutsdialog/05-01-SUMMARY.md` when done
</output>
