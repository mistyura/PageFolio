---
phase: 05-blob-shortcutsdialog
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pagefolio/dialogs/shortcuts.py
  - pagefolio/app.py
  - tests/test_shortcuts_dialog.py
autonomous: true
requirements: [V180-ROBUST-03]
must_haves:
  truths:
    - "ShortcutsDialog でキャプチャ対象を切り替えると、前行のキーラベルが元の表示へ復元され「キーを押してください」表示が残留しない（_start_capture が旧 cmd を _refresh_row する・V180-ROBUST-03 WR-01）"
    - "should_suppress_for_focused_input(keysym, focused_widget_class) は Ctrl/Alt を含まない keysym かつ入力系クラス（Entry/TEntry/Spinbox/TSpinbox/Text）フォーカス時のみ True を返し、Ctrl/Alt を含む組合せは常に False を返す（V180-ROBUST-03 WR-02）"
    - "_bind_shortcuts は各バインド（および shift variant）をフォーカスガードでラップし、root.focus_get() が None を返すケースを防御する（V180-ROBUST-03 WR-02）"
  artifacts:
    - pagefolio/dialogs/shortcuts.py
    - pagefolio/app.py
    - tests/test_shortcuts_dialog.py
  key_links:
    - "app._bind_shortcuts の発火ラムダ → should_suppress_for_focused_input(keysym, focus クラス) ゲート → f()"
  prohibitions:
    - statement: "フォーカスガードは Ctrl/Alt を含む組合せ（例: Ctrl+O・Ctrl+S・Ctrl+Z）の発火を抑止してはならない（入力ウィジェットフォーカス中も従来どおり発火する）。WR-02 対処はキャプチャ時の単キー登録拒否ではなく発火側ガードで行い、登録の自由度を維持する（D-09/D-10）"
      status: flagged-unverified
---

<objective>
ShortcutsDialog の2つの既知バグ（v1.7.1 Phase 4 コードレビュー 04-REVIEW.md 由来・ROBUST-03）を解消する。WR-01: キャプチャ対象を切り替えると前行の「キーを押してください」表示が残留する。WR-02: 修飾キーなしの単キーショートカットが root 直下の入力系ウィジェット（Spinbox 等）の通常入力と衝突しうる。

Purpose: WR-01 は _start_capture の二重発火時に旧行を _refresh_row しないことが原因（Pitfall 1）。WR-02 は Tk のイベント伝播が break されず root バインドのアプリコマンドと入力系ウィジェットの既定動作が両方発火することが原因（Pitfall 2）。WR-02 はキャプチャ時の登録制限ではなく発火側フォーカスガード（D-09/D-10）で根治し、既定キー <Delete>/<F5> の潜在衝突も同時に解消する。
Output: shortcuts.py の _start_capture WR-01 修正、app.py の should_suppress_for_focused_input 純関数 + _bind_shortcuts ガード挿入、新規 tests/test_shortcuts_dialog.py。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-blob-shortcutsdialog/05-CONTEXT.md
@.planning/phases/05-blob-shortcutsdialog/05-RESEARCH.md
@.planning/phases/05-blob-shortcutsdialog/05-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 05-04)

- `pagefolio/app.py` module-level 定数 `_INPUT_WIDGET_CLASSES`（新規・入力系ウィジェットクラス名集合）
- `pagefolio/app.py` module-level 純関数 `should_suppress_for_focused_input(keysym, focused_widget_class)`（新規）
- `pagefolio/app.py` `_bind_shortcuts` の発火ラムダへのフォーカスガード挿入（改修）
- `pagefolio/dialogs/shortcuts.py` `_start_capture` の WR-01 修正（旧行復元）
- 新規ファイル `tests/test_shortcuts_dialog.py`

<tasks>

<task type="auto">
  <name>Task 1: WR-01 修正（_start_capture でキャプチャ切替時に旧行表示を復元）</name>
  <files>pagefolio/dialogs/shortcuts.py</files>
  <read_first>
    - pagefolio/dialogs/shortcuts.py（全体・特に _start_capture:189-197 / _end_capture:199-204 / _refresh_row:179-182 / _display_text:165-171 / _on_capture_keypress:206-235）
    - .planning/phases/05-blob-shortcutsdialog/05-RESEARCH.md「Pitfall 1: WR-01 の正確な原因」
    - .planning/phases/05-blob-shortcutsdialog/05-PATTERNS.md「_start_capture WR-01修正」（修正案そのもの・prev_cmd 保持→_end_capture→_refresh_row(prev_cmd)）
  </read_first>
  <action>
    shortcuts.py の _start_capture(cmd_name) を修正する。現行は `if self._capturing_cmd is not None: self._end_capture()` のみで、直前にキャプチャ中だった行のラベルが「キーを押してください」（shortcuts_capture_waiting）のまま取り残される。修正: `if self._capturing_cmd is not None:` の分岐内で、旧 `self._capturing_cmd` を prev_cmd へ退避してから `self._end_capture()` を呼び、続けて `self._refresh_row(prev_cmd)` を呼んで旧行のラベルを通常の keysym 表示（_display_text 経由）へ復元する。その後は現行どおり `self._capturing_cmd = cmd_name` を設定し、新行を shortcuts_capture_waiting 表示にして <KeyPress> を bind、focus_set する。

    _refresh_row / _display_text / _end_capture は無改造で流用する（旧行が捕捉中でなくなれば _display_text は通常の keysym 表示 or shortcuts_unassigned を返す）。例外処理の書式は既存の except Exception as e: を踏襲する（本タスクでは新規 try は不要）。新規 LANG キーは追加しない（既存キーのみで完結）。
  </action>
  <verify>
    <automated>pytest tests/test_shortcuts_dialog.py -k "wr01 or WR01 or start_capture or restore" -x</automated>
  </verify>
  <acceptance_criteria>
    - pagefolio/dialogs/shortcuts.py の _start_capture が旧 _capturing_cmd を退避し `_refresh_row(` を呼ぶ
    - _start_capture 内で _end_capture 呼び出し後に旧コマンドの行が復元される（Task 3 のテストで検証）
    - shortcuts.py に新規 LANG キー参照が追加されていない（既存キーのみ）
    - Task 3 の WR-01 テストが exit 0
  </acceptance_criteria>
  <done>キャプチャ対象を切り替えても前行の「キーを押してください」表示が残留せず、旧行が元の keysym 表示へ復元される。</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 修正（should_suppress_for_focused_input 純関数 + _bind_shortcuts フォーカスガード）</name>
  <files>pagefolio/app.py</files>
  <read_first>
    - pagefolio/app.py:35-104（純関数群 merge_shortcuts / shift_variant_keysym / build_keysym_from_event / find_duplicate_binding / keysym_to_display — 新規純関数の配置先と書式）
    - pagefolio/app.py:229-260（_bind_shortcuts の現行発火面 = root.bind(keysym, lambda e, f=func: f()) と shift variant バインド）
    - pagefolio/app.py:200-225（_default_shortcuts の <Delete>/<F5> 修飾なし既定キー・_cmd_map）
    - .planning/phases/05-blob-shortcutsdialog/05-RESEARCH.md「Pattern 5: フォーカスガード純関数」「Pitfall 2」「Open Question 2」（focus_get() None 防御・ラムダ改修）
    - .planning/phases/05-blob-shortcutsdialog/05-PATTERNS.md「app.py フォーカスガード純関数追加・_bind_shortcuts 改修」
  </read_first>
  <action>
    app.py に module-level 定数 _INPUT_WIDGET_CLASSES を追加する。値は入力系ウィジェットの winfo_class() 文字列集合 {"Entry", "TEntry", "Spinbox", "TSpinbox", "Text"}（ttk/tk 双方の綴りを含む）。find_duplicate_binding の直後・keysym_to_display の前（app.py:87 付近）に配置する。

    app.py に module-level 純関数 should_suppress_for_focused_input(keysym, focused_widget_class) を追加する（Tk 非依存・Pattern 5 準拠）。keysym は "<Delete>" 等の bind 文字列。判定: keysym に "Control" または "Alt" が含まれる場合は False を返す（Ctrl/Alt を含む組合せは入力フォーカス中も従来どおり発火・D-10）。それ以外（修飾なし単キー / Shift のみの組合せ）は focused_widget_class in _INPUT_WIDGET_CLASSES を返す。docstring に D-09/D-10 と「発火抑止判定のみを担い、実際の focus_get() 呼び出しは app 側」を明記する。既存純関数群と同じ日本語 docstring 書式に揃える。

    _bind_shortcuts の発火ラムダを改修する。現行の `self.root.bind(keysym, lambda e, f=func: f())` を、event を実際に使いガード判定を挿入する形へ変更する: keysym と shift variant の両方について、ラムダ内で self.root.focus_get() を取得し（None のときは空文字列 "" を渡す防御）、その winfo_class() と keysym を should_suppress_for_focused_input へ渡して True なら None（発火抑止・何もしない）、False なら f() を呼ぶ。ラムダのデフォルト引数で keysym（ks）と func（f）を束縛し late-binding を避ける。既存の try/except Exception as ex: のログ書式・_bound_keysyms の記録・unbind ループは維持する。
  </action>
  <verify>
    <automated>pytest tests/test_shortcuts_dialog.py -k "wr02 or WR02 or suppress or focus" -x</automated>
  </verify>
  <acceptance_criteria>
    - pagefolio/app.py が `def should_suppress_for_focused_input(` と `_INPUT_WIDGET_CLASSES` を定義する
    - should_suppress_for_focused_input("<Control-o>", "Entry") が False を返す（Ctrl 組合せは抑止しない）
    - should_suppress_for_focused_input("<Delete>", "Entry") が True、should_suppress_for_focused_input("<Delete>", "TButton") が False を返す
    - _bind_shortcuts が should_suppress_for_focused_input を呼び、focus_get() が None のケースを防御（空文字列 fallback）する
    - Task 3 の WR-02 テストが exit 0
  </acceptance_criteria>
  <done>入力系ウィジェットフォーカス中は修飾なし単キー/Shift のみのショートカット発火が抑止され、Ctrl/Alt 組合せは従来どおり発火する。既定キー <Delete>/<F5> の入力衝突も同時に根治する。</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_shortcuts_dialog.py 新設（WR-01/WR-02 回帰テスト）</name>
  <files>tests/test_shortcuts_dialog.py</files>
  <read_first>
    - pagefolio/dialogs/shortcuts.py（Task 1 改修後の _start_capture / _end_capture / _refresh_row / _display_text / _key_labels / _shortcuts）
    - pagefolio/app.py（Task 2 追加の should_suppress_for_focused_input / _INPUT_WIDGET_CLASSES）
    - tests/test_batch_ocr_dialog.py（@pytest.fixture(scope="module") で tk.Tk() を withdraw して共有する実 Tk root パターン）
    - tests/test_viewer.py:15-23（types.SimpleNamespace スタブ + メソッド __get__ バインドの純ロジックテストパターン）
    - .planning/phases/05-blob-shortcutsdialog/05-PATTERNS.md「実 Tk root を使うテストパターン」「純関数のみのテスト」
  </read_first>
  <action>
    tests/test_shortcuts_dialog.py を新設する。ShortcutsDialog 専用テストはこれまで存在しないため新規に整備する。

    WR-02（フォーカスガード純関数）は Tk root 不要で検証する: should_suppress_for_focused_input を app からインポートし、(a) "<Control-o>"/"Entry" → False、(b) "<Alt-x>"/"Spinbox" → False、(c) "<Delete>"/"Entry"・"<Delete>"/"Spinbox"・"<Delete>"/"TSpinbox"・"<F5>"/"Text" → True、(d) "<Delete>"/"TButton"・"<Delete>"/""（フォーカスなし相当）→ False、(e) Shift のみの組合せ "<Shift-A>"/"Entry" → True、"<Shift-A>"/"TButton" → False を網羅する。

    WR-01（キャプチャ切替時の旧行復元）は実 Tk 挙動を検証する: tests/test_batch_ocr_dialog.py と同じ scope="module" の tk.Tk()+withdraw() フィクスチャを用いる。ShortcutsDialog を生成するには app スタブ（_default_shortcuts / settings / _bind_shortcuts / _cmd_map 相当を最小限持つ types.SimpleNamespace か軽量 FakeApp）と font_func を与える。ダイアログ生成後、コマンドAの _start_capture を呼び行Aが shortcuts_capture_waiting 表示になることを確認、続けて保存/クリアせずコマンドBの _start_capture を呼び、行A（旧行）のラベル text が shortcuts_capture_waiting ではなく _display_text(cmdA) の通常表示へ復元されていること、行B が shortcuts_capture_waiting になっていることをアサートする（WR-01 の残留がないこと）。実ウィジェット生成が重い/不安定な場合は、_key_labels に tk.Label を持たせた最小ハーネスで _start_capture のロジック（_refresh_row 呼び出し）を検証する方式でもよい（Tk root は共有フィクスチャ経由）。

    テストのマーカー/命名は verify の -k フィルタ（wr01/wr02/suppress/focus/restore/start_capture）に一致させる。
  </action>
  <verify>
    <automated>pytest tests/test_shortcuts_dialog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_shortcuts_dialog.py が存在する
    - should_suppress_for_focused_input の Ctrl/Alt 非抑止・入力系抑止・非入力系非発火抑止・フォーカスなし防御を網羅するテストがある
    - WR-01 の「キャプチャ切替で旧行が shortcuts_capture_waiting のまま残らない」ことを検証するテストがある
    - `pytest tests/test_shortcuts_dialog.py -x` が exit 0
  </acceptance_criteria>
  <done>WR-01 の表示残留修正と WR-02 のフォーカスガードが専用回帰テストで固定される（ShortcutsDialog 初の単体テスト整備）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| キーボード入力→アプリコマンド | ショートカットキー押下がアプリコマンド発火へ渡る境界。ASVS V5（入力検証）が部分的に該当（既存 build_keysym_from_event/find_duplicate_binding の検証を踏襲・新規入力経路は増やさない） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | 入力系ウィジェット編集中に修飾なし単キーショートカット（<Delete> 等）が誤発火しページ削除等の意図しない破壊操作を起こす | medium | mitigate | should_suppress_for_focused_input フォーカスガードで入力系フォーカス中の修飾なし発火を抑止（D-09/D-10）。Ctrl/Alt 組合せは従来どおり発火 |
| T-05-11 | （信頼性・STRIDE外） | root.focus_get() が None を返すケースでガード判定が例外化 | low | mitigate | ラムダ側で focus_get() None を空文字列へフォールバックし純関数は非入力系扱いで False を返す（Open Question 2） |
</threat_model>

<verification>
- `pytest tests/test_shortcuts_dialog.py -x` がグリーン（WR-01/WR-02 回帰）
- `pytest` 全件グリーン（回帰なし・特に既存ショートカット純関数テスト）
- `ruff check . && ruff format .` がクリーン
- 手動確認（任意・非ブロッキング）: ShortcutsDialog で「変更」ボタンを複数コマンド連続クリックしても前行表示が固まらない／Spinbox 編集中に <Delete> でページ削除が発火しない
</verification>

<success_criteria>
- WR-01（キャプチャ切替時の表示残留）が解消される（V180-ROBUST-03）
- WR-02（修飾なし単キーの入力衝突）がフォーカスガードで解消され、Ctrl/Alt 組合せは従来どおり発火する（V180-ROBUST-03）
- ShortcutsDialog の初の単体テストが整備され回帰検知装置が確立される
- 既定キー <Delete>/<F5> の潜在衝突も同時に根治される
</success_criteria>

<output>
Create `.planning/phases/05-blob-shortcutsdialog/05-04-SUMMARY.md` when done
</output>
